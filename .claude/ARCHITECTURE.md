# ARCHITECTURE.md
**Satellite context file — detailed directory structure, systems, and APIs**

**Parent:** `.claude/MASTER_CONTEXT.md`

**Last Updated:** 2026-07-23 (share-token data-exposure boundary — Wave 4 named-singleton #5)

---

## Status

Wave 1 Project scaffold complete: Next.js app shell exists and builds. Supabase project + migration workflow section complete: project linked, credentials captured, `supabase/` workspace initialized. Schema — platform + identity foundation section complete: `platform` enum, `players`, and `player_id_crosswalk` are live in the database (verified 2026-07-21). Schema — league + config section complete: `leagues`, `generate_share_token()`, and `league_config` are live (verified 2026-07-21). Schema — league-scoped state section complete: `rosters`, `roster_players`, `matchups`, `standings`, and `player_scores` are live (verified 2026-07-22). Schema — draft state section complete: `draft_state` + `draft_pick_source` enum are live (verified 2026-07-22). Schema — integrity section complete (verified 2026-07-22): FK sweep confirmed all 18 FKs shipped at table creation (no migration), 17 integrity indexes live, and the RLS owner-policy layer live — `fantasy_owner_all` on all ten tables, full owner CRUD pinned to the admin `auth.uid()` via `is_fantasy_admin()`; the entire Wave 1 schema block is now built. Types + client wiring section complete (2026-07-22): generated `Database` types committed and the typed browser/server client utilities live in `src/lib/supabase/` (see Supabase Client Wiring below). Env/deploy section complete (2026-07-22): `.env.example` contract committed, the Vercel project/repo-link and env-var items verified as pre-completed manual setup, the initial deploy confirmed — GitHub→Vercel auto-deploy is live (see Vercel Deployment below) — and the health-check completion gate passed: deployed production `/api/health` performs a real authenticated Supabase read via the secret-key admin client and returns 200, functionally proving the Vercel env vars and DB reachability. **Wave 1 (`01_foundation.md`) is 🟢 Complete.** Wave 2 (`02_data_pipeline.md`) is 🟡 in progress: the Sleeper client + player catalog sub-section is complete (2026-07-22) — typed server-only Sleeper HTTP client (`src/services/sleeper/`), full `/players/nfl` catalog sync with validate-before-persist and absent-player inactive marking (two new `players` columns), verified by a live production run (12,200 players upserted; see Service API Reference below). The Sleeper league-scoped sync sub-section is 3/5 complete (2026-07-22, one folded session): league metadata → `leagues`+`league_config`, rosters+users → `rosters`/`roster_players`/`standings`, weekly matchups → `matchups`+`player_scores` (standings and player_scores ingestion folded in by Nick-signed build-file scope corrections) — all live-verified against Nick's real league `1257464256710856704` ("10 enter 1 Leaves", season 2025, complete): 45 raw scoring keys, 10 rosters/174 membership rows, 18 weeks/180 matchup rows/3,152 player scores, starter-sum-vs-points integrity exact on all 180 roster-weeks. Remaining in the sub-section: Sleeper draft-state write path, per-league orchestrator. **Cron/polling sub-section complete except one plan-gated residue (2026-07-22, 3-item fold):** three secret-gated cron routes under `src/app/api/cron/` (Bearer `CRON_SECRET`, fail-closed), the `sync_runs` telemetry table live (migration `20260722160525`) with a once-per-day catalog guard, and `vercel.json` shipping two daily crons (catalog 09:00 UTC, league-state 10:00 UTC — registration confirmed in the Vercel dashboard); all verified in production (401s unauthenticated, guard skip, full league-state run from the deployed function). The build file's 15-minute league-state cadence is Vercel-Hobby-plan-gated and deferred to season start by Nick's decision (item `[~]`); the draft fast path is an authenticated route with no schedule until Wave 3b. With the 9 ESPN items `[!]`, `02_data_pipeline.md` has no remaining `[ ]` items — the next buildable wave is 3a. **Wave 3a (`03a_draft_assistant_static_board.md`) is 🟡 in progress: the ADP schema + ingestion sub-section is complete (2026-07-22, one folded session — all 9 items).** `adp_rankings` is live (migration `20260722175619`; long per-format rows — Nick-signed amendment of the pre-source-confirmation single-row spec), ingestion runs against Sleeper's undocumented projections endpoint on `api.sleeper.com` via the structurally separate `src/services/adp/` (see Service API Reference), `sync_runs` gained the `'adp_ingestion'` source (migration `20260722180354`), and scheduled ingestion piggybacks on the daily sync-leagues cron (Nick-signed; both Hobby cron slots taken) with `/api/cron/sync-adp` as the on-demand route. Live-verified end to end: 2,946 rows across 10 scoring formats for season 2026, idempotency confirmed by back-to-back runs, 0 unmapped players. **The draft-board data layer sub-section is also complete (2026-07-22, second fold same day):** `src/services/draft-board.ts` — the one-query-service fold — produces the merged ADP-ordered board pool (ADP carriers ∪ league-rostered, Nick-signed) with explicit availability, league context from `derived_config`, and the Nick-signed league-format → `scoring_format` bucketed mapping (`resolveAdpScoringFormat`: superflex→'2qb' precedence, then PPR buckets; no cross-format fallback). Live-verified against the real league (276-player pool, ordering/availability/boundary/not-found invariants asserted). **The draft-board route + page shell sub-section is also complete (2026-07-22, third 3a fold — all 4 items): the app's first real UI surface and the full owner auth path.** Real sign-in shipped (Nick-signed over an interim gate): `src/proxy.ts` (Next 16 proxy convention — Supabase session refresh per request + unauth `/leagues/*` → `/login`; `/api` excluded so the cron Bearer surface stays isolated), `/login` page with `signIn`/`signOut` server actions, the `(admin)` route-group gate (`getAdminAuthState` → `is_fantasy_admin` RPC; non-admin shared-namespace sessions get a zero-data not-authorized view), the admin-only RSC draft route at `app/(admin)/leagues/[leagueId]/draft/` feeding the client `DraftBoardShell` (header/toolbar with league selector, platform badge, season year, ADP source + freshness; player-table and roster-sidebar regions as placeholders for the next sub-sections), root auto-land on the first connected league, and `listConnectedLeagues()` in the draft-board service. Verified live: unauth 307s, `/login` response asserted free of league/admin data, and the first real Supabase Auth sign-in in the app's history (Nick, landing on the real board; sign-out verified). Flagged, not built: DESIGN_SYSTEM.md's final dark-only Sleeper palette is still unapplied — surfaces render on scaffold light tokens; palette reconciliation is its own pending fold (RESOLVED same day — the palette reconciliation fold applied it; see DESIGN_SYSTEM.md). **The states + resilience sub-section is also complete (2026-07-22, final 3a fold — all 3 items), closing Wave 3a's buildable scope: `03a` is complete except the schedule-source-gated bye-week `[>]` deferral.** Route-segment `loading.tsx` skeleton mirrors the board's real density (shadcn `skeleton` primitive, `bg-muted` per DESIGN_SYSTEM); empty states (Nick-signed: banner-over-rostered-list + command+schedule copy) via `adp-notice.tsx`'s pure `deriveAdpNoticeKind` (three honest variants: no snapshot / format unresolved / format zero-rows — no cross-format fallback ever), a full-region `BoardEmptyState` for a truly empty pool, and the designed zero-filter-match state ("Clear filters" resets filters, never sort); route-segment `error.tsx` (Nick-signed: retry + back-to-leagues escape; generic copy, digest as log-correlation ref only) with the gap/failure boundary verified — data gaps degrade to nulls + per-surface notices through zero platform branching, only real failures reach the boundary. 14/14 functional checks incl. live no-false-positive on the real league. **Wave 4 (`04_league_dashboard.md`) is 🟡 in progress (authorized ahead of ESPN-blocked 3b by Nick's 2026-07-22 ruling): the Admin data-access layer sub-section is complete (2026-07-22, one 5-item fold — Nick-ruled full-sub-section as one query-service artifact per the 03a data-layer precedent).** `src/services/dashboard.ts` shipped (see Service API Reference): `getStandings` (Nick-signed wins→PF→roster-id ordering — declared wiki silence, no wire ordering key exists; flat, no divisions in v1), `getMatchups` (matchup-endpoint-grounded pairing on `native_matchup_id` with nulls-as-byes and non-two groups degrading to unpaired + anomaly flag; `effective_points`; full-roster score lines with `was_starter` + `fetched_at`/`is_final` pass-through), `getPowerRankings` (All-Play record — the wiki's decided schedule-neutral measure; regular-season weeks via raw `playoff_week_start`; weekly ties = half-wins; lowConfidence flag under 6 counted weeks; rank-delta vs the shared standings ordering), and `getPlayerCard` (identity + current roster status + per-week lines with holding-team attribution; FA-gap weeks as explicit `not_rostered` entries — never invented). All live-verified against the real league (exact independent all-play recomputation; starter-sum == effective_points on every checked side; anon publishable-key client → zero data through every getter). **The admin UI display layer is complete (2026-07-22, 4-component + freshness fold — see `components/dashboard/` in the tree), and the admin dashboard page is now assembled and live (2026-07-22, page-assembly + league-selector fold):** the owner league dashboard mounts at `app/(admin)/leagues/[leagueId]/` (Nick-signed: the league root — the draft board stays the `/draft` sibling; root auto-land re-signed to land on the dashboard), composing MatchupsGrid full-width (URL-driven `?week=N`; default week = latest league-scored week via the new `listScoredWeeks`) over StandingsTable + PowerRankingsList side-by-side (lg-up), with the PlayerCard panel opening as a URL-driven `?player=` side sheet (server-rendered `PlayerCardSheet`, Link-dismissed — no client JS; Nick-signed) and matchup player names linking into it; the draft board's `LeagueSelector` generalized with a `subPath` prop (Nick-signed reuse-over-duplicate) and mounted in the dashboard header; route-segment loading skeleton + error boundary follow the draft route's established pattern (generic copy, no league data). Verified tsc/lint/build clean + 21/21 live render-harness checks against the real league (read-only SELECTs; harness deleted after). **The admin navigation shell + information-architecture sub-section is complete (2026-07-23, one 4-item IA-artifact fold):** a persistent `AdminSidebar` (client, rendered by a new `[leagueId]/layout.tsx` so it persists across sections) owns the global chrome the page headers used to duplicate — the reused `LeagueSelector` (Nick's Clarify: sidebar owns selector + sign-out; the draft-board header, dashboard shell, and their prop chains were de-duplicated accordingly) — with Home/Dashboard/Draft nav (usePathname-highlighted) and 8 visible-disabled Wave 5/6 "coming soon" slots (Nick's Clarify). Route topology changed (Nick's Clarify): the league root `/leagues/[id]` is now the **command-center home** (`CommandCenterHome` — standings/power snapshot cards deep-linking to the full pages + a draft-board link), the full standings/matchups/power dashboard **moved to `/leagues/[id]/dashboard`**, and root auto-land now lands on the home. Verified tsc/lint/build clean + 14/14 durable suite + a runtime boot smoke (routes gate correctly behind owner auth). **The share-token data-exposure boundary is also complete (2026-07-23, Wave 4 named-singleton #5 — one MIGRATION fold, Rule 13):** spectator `SELECT` RLS on the EIGHT tables the read-only surface reads (the six from the build file + `players` + `roster_players`, Nick-signed correctness amendment since the reused `getMatchups`/`getPlayerCard` getters read them), gated on a `share_token` presented via the `x-share-token` header and read by `public.current_share_token()` (migration `20260723072917`); `draft_state`/`draft_sessions` deliberately get NO spectator policy (unreachable to anon even with a valid token). Owner-only `regenerate_share_token(uuid)` RPC (SECURITY INVOKER; RLS UPDATE policy authorizes; EXECUTE revoked from anon — migration `20260723074134`) behind the `regenerateShareToken` server action. `services/spectator.ts` is the server-side loader (`loadSpectatorDashboard`/`loadSpectatorPlayerCard`) via the token-scoped anon client (`lib/supabase/spectator.ts`), reusing the `dashboard.ts` getters and importing nothing draft-related. Verified tsc/lint/build clean + 23/23 durable suite (9 new spectator boundary tests: cross-league isolation + draft-tables-never-queried against an RLS-enforcing fake client, Rule 13) + live read-only RLS verification against the real league (valid token unlocks all 8 tables scoped to the league; `draft_state`/`draft_sessions` return 0 rows even with the token; wrong/no token → nothing; regenerate revokes the old link and the new one works; anon RPC → 42501). Remaining in Wave 4: share-link settings panel UI (the `regenerateShareToken` action it calls now exists), the spectator UI surface (route + mobile-first components + page composition + the no-admin-markup render test), and the loading/empty/error-states resilience item. **Wave 3b (`03b_draft_assistant_live_draft.md`) is 🟡 in progress (2026-07-22, first fold since the Sleeper-snake restructure): the manual click-to-draft write path sub-section is complete (all 4 items, one write-surface fold).** `src/services/draft-picks.ts` shipped — `recordManualPick` (full referential validation, Nick-signed any-unclaimed-pick + server-side duplicate-player rejection, round cross-check as validation only, first-write-wins via ON CONFLICT DO NOTHING with a typed `accepted`/`conflict`/`validation_error` result) and `undoLastManualPick` (highest `source='manual'` row only; poll rows untouchable by construction) — plus auth-gated server actions under the draft route (`actions.ts`: in-action `getAdminAuthState` gate + RLS as the second wall). Live-verified 21/21 against the real league (all validation reasons, conflict vs the poller's rows, accepted+undo round-trip leaving `draft_state` untouched, anon zero-data/RLS rejection); tsc/lint/build clean. No migration — `draft_state` is Wave 1 schema. Next 3b sub-section: active-draft polling orchestration (`draft_sessions` table + cadence). **The client-side live sync sub-section is 2/5 (2026-07-22, items 1–2 folded under the live-behavior ceiling):** the board is now live-updating — each executed `pollActiveDraft` tick returns the league's full `draft_state` snapshot (`listDraftPicks`, full-snapshot-over-incremental Nick-signed so undo deletions self-heal), the `DraftPollTicker` is tab-visibility-gated (skip while hidden, instant catch-up tick on return) and delivers snapshots to the shell's live-pick state (fingerprint bail-out dedup — identical snapshots never re-render; the merge replaced refresh-on-picks, Nick-signed), and the pure merge layer (`components/draft-board/live-picks.ts`) overlays picks onto the board pool by `pick_number`: a third client-merge-only availability value `'drafted'` ("Drafted · Pick N" dimmed rows; `'rostered'` wins precedence; out-of-pool picks skipped — the recent-picks feed item later surfaces all picks; overlay applies outside active sessions too, Nick-signed after the real-data finding that 40 of the league's 170 drafted players are unrostered). Live-verified 24/24 against the real league (snapshot order/RLS-anon zero-data, precedence on real data, record→merge→undo→heal cycle, fingerprint semantics, available-only filter, drafted-row render). Remaining in the sub-section: optimistic UI, conflict rollback, status indicator (items 3–5, next fold). Remaining inventory sections (Type Definitions, CI/CD Pipeline, Testing Infrastructure) are populated as subsequent waves actually build things — not fabricated in advance. Update this file at session end whenever structure changes, per `MASTER_CONTEXT.md`'s Session-End Steps.

---

## Source Directory Structure

```
src/
  proxy.ts          — Next 16 proxy (replaces middleware.ts): Supabase session refresh on every matched request + unauth /leagues/* → /login; matcher excludes /api (cron Bearer surface stays isolated), _next statics, favicon
  app/
    layout.tsx      — root layout; registers Geist (--font-sans) + Geist Mono (--font-geist-mono), imports globals.css; mounts the app-wide sonner <Toaster /> (Wave 3b)
    page.tsx        — auth-aware root router: unauth → /login; non-admin → not-authorized; admin → first connected league's COMMAND-CENTER HOME (the league root; full dashboard + draft board one click away in the persistent sidebar; auto-land re-signed 2026-07-22, home shipped 2026-07-23); zero leagues → minimal note
    globals.css     — Tailwind v4 entry + the app's single always-on dark-only Sleeper token set (see DESIGN_SYSTEM.md; palette reconciliation fold 2026-07-22)
    favicon.ico
    login/
      page.tsx      — owner sign-in page (RSC; authed sessions bounce to /); never touched by the Wave 4 spectator surface
      login-form.tsx — client form (useActionState → signIn action; generic error only)
      actions.ts    — 'use server': signIn (signInWithPassword → redirect to landing) + signOut (→ /login)
    (admin)/        — owner working-tool route group; layout.tsx gates every child (unauth → /login, non-admin → zero-data NotAuthorizedCard); never serves the spectator path
      leagues/[leagueId]/
        layout.tsx  — persistent admin shell (Wave 4 nav-shell, 2026-07-23): fetches listConnectedLeagues once past the (admin) auth gate, 404s an unknown league up front, renders AdminSidebar + the content column; a SEGMENT layout so it persists across Home/Dashboard/Draft (renders once, child pages swap — the sticky league context) and the league-list fetch runs once per league, not per section-nav
        page.tsx    — command-center HOME RSC (Wave 4 nav-shell, the league root — Nick-signed): getStandings + getPowerRankings via the RLS server client → CommandCenterHome snapshot cards; league_not_found → 404
        loading.tsx — route-segment skeleton mirroring the home's slim header + snapshot-card grid; shadcn Skeleton on bg-muted, CSS pulse only
        error.tsx   — route-segment error boundary covering home + dashboard (draft route's pattern): generic copy ("This league couldn't load"), digest ref only, reset() retry + / escape link
        dashboard/
          page.tsx    — admin league dashboard RSC (Wave 4, MOVED here 2026-07-23 from the league root — Nick-signed): standings/matchups/power-rankings/weeks in parallel via the RLS server client → LeagueDashboardShell (the sidebar owns the selector + sign-out now, so no leagues fetch/headerActions); ?week=N (invalid → default = latest scored week), ?player=<id> → server-rendered PlayerCardSheet (closeHref = /leagues/[id]/dashboard?week=N; player_not_found → honest in-sheet message); league_not_found → 404
          loading.tsx — route-segment skeleton mirroring the dashboard's real layout (slim identity header/week pills/mirrored matchup cards/standings+power 2-col); shadcn Skeleton on bg-muted, CSS pulse only
          actions.ts  — 'use server' (Wave 4 named-singleton #5): regenerateShareToken(leagueId) — owner-gated INSIDE the action (getAdminAuthState), calls the regenerate_share_token RPC via the RLS server client, revalidates the dashboard; the share-link settings panel (unbuilt) is its consumer
        draft/
          page.tsx    — admin-only draft board RSC (Wave 3a base, 3b live): getDraftBoardData (incl. the league roster list) + getDraftSessionState + listDraftPicks (initial snapshot) in parallel via the RLS server client → DraftBoardShell (the sidebar owns the selector now, so no listConnectedLeagues fetch); league_not_found → 404
          actions.ts  — 'use server' draft actions (Wave 3b): submitManualPick + undoManualPick (→ services/draft-picks), startDraftSession + endDraftSession + pollActiveDraft (→ services/draft-sessions) — auth gate INSIDE each action (getAdminAuthState → typed 'unauthorized', zero data; server actions are POST endpoints, the layout gate doesn't cover them), all via the RLS server client (never the admin client); pollActiveDraft attaches the full draft_state snapshot (listDraftPicks) to every executed poll — the client merge consumes it, so it deliberately does NOT revalidate on picks (Nick-signed; the other actions revalidatePath on success)
          loading.tsx — route-segment skeleton mirroring the board's real layout density (header/toolbar/12-row six-column table/sidebar cards); shadcn Skeleton on bg-muted, CSS pulse only
          error.tsx   — route-segment error boundary: generic copy (raw errors never rendered; digest = log-correlation ref only), reset() retry + / escape link; data GAPS never land here — they degrade per-surface
    api/
      health/route.ts — GET /api/health: trivial authenticated DB read via admin client; public, force-dynamic, returns only { ok } (Wave 1 completion gate)
      cron/           — Wave 2 scheduled-sync surface; every route Bearer-CRON_SECRET-gated (auth.ts), force-dynamic, maxDuration 300
        auth.ts       — requireCronSecret(): fail-closed shared gate (missing env → 500, mismatch → 401; constant-time compare)
        sync-players/route.ts — daily catalog job; once-per-day guard on sync_runs (skips within 24h of a running/success run; failure rows allow retry)
        sync-leagues/route.ts — scheduled league-state sync: orchestrator with current-week-only matchups via /state/nfl (offseason skips matchups); one sync_runs row per league; also triggers the isolated ADP-ingestion piggyback (Wave 3a — own sync_runs row, failure never affects league-state outcome)
        sync-draft/route.ts — active-draft poll path: per-league draft-state sync, isolated; NO vercel.json schedule until Wave 3b tunes cadence
        sync-adp/route.ts — on-demand ADP ingestion (Wave 3a); NO vercel.json schedule (Hobby cap) — scheduled runs ride the sync-leagues piggyback
  components/
    ui/             — shadcn-generated components (button, input, label, card, badge, select, separator, skeleton, dropdown-menu, sonner); never hand-edited casually. dropdown-menu = Base UI Menu vendored shadcn-style (the action-list counterpart to select — commands, not value choice); sonner = app-wide toast outlet (theme hardcoded dark, token CSS vars only), mounted once in the root layout. error-boundary.tsx (Wave 3b resilience) = the app's ONE React class component: a reusable render-error boundary taking a `fallback` + optional `resetKeys` (clears the error when fresh data lands so a region revived by the next pick recovers); the sanctioned "wrapped in an error boundary (major components)" mechanism, catching render/commit throws only (async/event faults handled at their own surfaces), never rendering raw error content (Access Model)
    auth/
      not-authorized-card.tsx — zero-data view for authed non-admin sessions (shared prolabel namespace); sign-out only
      sign-out-button.tsx     — signOut-action form button (header + not-authorized card)
    draft-board/    — admin draft-board UI (Wave 3a); client components, data via RSC props
      draft-board-shell.tsx  — tablet/PC-first shell: header + PlayerBoard region + RosterPanel sidebar (sidebar hidden below lg); owns live-pick custody (Wave 3b): draft_state snapshot state seeded by the server render, replaced per poll tick with a fingerprint bail-out (identical snapshots never update state), PLUS the optimistic pending overlay (items 3–4): handleDraft claims next pick = max(snapshot ∪ pending)+1 (round = ceil(pick/league_size); refs mirror both pick sets so rapid clicks claim distinct numbers), reconciles accepted/conflict by adopting the returned authoritative row into the snapshot, rolls back every non-accepted outcome with a sonner toast (conflict names the winner; no retry loop); merged pool memoized and fed to BOTH board regions; Draft actions render only with an active session + known league size + a non-empty roster list. Live sync item 5 + UI item 2 additions (2026-07-22): aggregates poll health via onStatus→applyTickReport (reset on session re-arm via render-time epoch adjustment, passed to the header), stores the poll-delivered DraftOrderMeta (stringify bail-out), and memoizes nextPickNumber (snapshot ∪ pending) for the LiveDraftStrip. UI items 3–4 additions (2026-07-22): passes livePicks + rosters to RosterPanel (pick→team attribution — the merge deliberately carries none), mounts RecentPicksFeed below the panel in a flex-col sidebar, and owns handleUndo (undoManualPick action → local snapshot heal + toast; undoInFlight disables the feed's button). BPA items 7–9 additions (2026-07-22): mounts BpaRecommendationsPanel at the TOP of the sidebar (Nick's placement) with leagueId/livePicks/rosters/draftEnabled/pendingPlayerIds + the SAME handleDraft (item 8 — shared write path, never a parallel submit); handleDraft's param narrowed to DraftablePlayer (services/draft-board.ts — the minimal {sleeperPlayerId, fullName} the handler consumes) so non-table surfaces reuse the one path with no cast. Resilience (2026-07-23): each live-enhancement region (LiveDraftStrip, BpaRecommendationsPanel, DraftQueuePanel, RosterPanel, RecentPicksFeed; the run badge in board-toolbar + LiveStatusIndicator in the header) is wrapped in its OWN ErrorBoundary so a render fault degrades only that region (a muted RegionFallback notice, or null for appear-only micro-elements) while the static board + siblings stay usable (Nick's Clarify: per-region). Shell derivations feeding the STATIC table are guarded at the source since a child boundary can't catch a shell-render throw: mergedPlayers → base 3a `players` on a merge throw (Nick's Clarify: fall back to the 3a pool), runBoard → EMPTY_RUN_BOARD, the prune effect try/caught, fireAutoPick disarms auto-pick on a selection fault (its hidden-tab fire uses a 0ms timer so the disarm setState never runs synchronously in the effect)
      draft-board-header.tsx — league name, platform badge, season year, DraftSessionToggle (LiveStatusIndicator passed via its statusSlot), ADP source + ingested-at (UTC); takes pollHealth from the shell. The selector + sign-out moved to the persistent admin sidebar (Wave 4 nav-shell, 2026-07-23) — the header no longer duplicates that global chrome or takes a `leagues` prop
      draft-session-toggle.tsx — toolbar Start/End draft control (Wave 3b): startDraftSession/endDraftSession actions; pulsing teal "Draft live" dot while active (the live semantic — sanctioned teal), stale TTL reads "Session expired"; optional statusSlot renders right after the live label (home of the status indicator, Nick's placement)
      draft-poll-ticker.tsx  — renders nothing: 5s pollActiveDraft interval while board mounted + session active; in-flight guard; tab-visibility gated (skip hidden, instant catch-up tick on return); delivers each executed poll's snapshot to the shell via onPicks (merge replaced refresh-on-picks — router.refresh only on stand-down outcomes); reports each EXECUTED tick's outcome via onStatus (skipped/stand-down ticks report nothing) and each successful sync's DraftOrderMeta via onDraftOrder (failed syncs keep the last known order)
      live-picks.ts          — pure merge layer (Wave 3b): picksFingerprint (order-insensitive dedup key) + mergePicksIntoPlayers (overlay by pick identity: available+picked → 'drafted' with pick number; optimistic pending set → 'drafted' with draftPending=true and no number, confirmed rows always win over pending; 'rostered' wins precedence; out-of-pool picks skipped; unchanged input returns the same reference) + computeNextPickNumber (max of snapshot ∪ pending, +1 — gaps stay backfillable) + latestManualPickNumber (UI item 4: the one row undo would delete — highest manual pick; mirrors server semantics) + projectOnClock (UI item 2: team-on-clock PROJECTION from DraftOrderMeta — snake/linear direction, reversal_round flip for rounds ≥ reversal (declared 3RR interpretation), contiguous-map guard (partial map → unknown, never misprojects), auction → unknown (nomination order), past rounds×slots → complete; recorded picks stay ground truth)
      live-status.ts         — pure poll-health layer (live sync item 5, live-picks pattern): LivePollHealth + applyTickReport (success stamps lastSuccessAt/resets failures; failure increments) + isDegraded (≥2 consecutive failures, Nick-signed) + formatSyncAge (1s granularity under a minute, then m/h)
      live-status-indicator.tsx — toolbar live status text beside the "Draft live" label (Nick-signed placement + states): "Sleeper · synced Ns ago" (relative, 1s re-render tick), "syncing…" before the first success, "Paused — tab in background" while hidden, degraded muted-tier "sync issue — retrying" (instant recovery); hidden entirely when no session is active
      live-draft-strip.tsx   — slim live strip between header and board (UI item 2, Nick's placement), session-active only: Pick N (nextPickNumber) · Round (ceil/league_size; omitted when size unknown) · "On the clock: <rosterLabel>" from projectOnClock (honestly omitted when unknown); past the final scheduled pick reads "Draft complete · all N picks recorded"
      league-selector.tsx    — Select over all connected leagues; generalized 2026-07-22 (Nick-signed): pushes /leagues/{id}{subPath}; as of the Wave 4 nav-shell it lives in the persistent AdminSidebar with subPath='' (switching league lands on that league's command-center home); query params deliberately don't survive a switch
      player-board.tsx       — player-list region: owns filter/sort state, composes ADP notice + toolbar + table via usePlayerList; truly-empty pool → full-region BoardEmptyState (banner/toolbar/table suppressed — no double-messaging)
      board-toolbar.tsx      — debounced search, position multi-select pill-chips (canonical six), available/all toggle, match count
      player-table.tsx       — the board grid (sanctioned tabular surface): sortable headers (ADP/pos rank/name, direction toggle, aria-sort), sticky thead; zero-filter-match → BoardEmptyState row with "Clear filters" (resets filters, never sort)
      adp-notice.tsx         — pure deriveAdpNoticeKind (the ONE ADP-absence classifier — never re-derive) + warning-tier banner: no snapshot → sync:adp / format unresolved → sync:league / format zero-rows → explicit empty state (no cross-format fallback)
      board-empty-state.tsx  — shared empty-state body (title/detail/action slot) for the table and region call sites
      command-chip.tsx       — inline bg-well terminal-command chip for owner-facing next-action copy
      player-row.tsx         — one row: name + injury chip, position badge, team, ADP (1-dec), POS+rank composite, availability; rostered AND live-drafted rows dim via opacity ('drafted' shows "Drafted · Pick N", Nick-signed); optimistic pending rows read "Drafting…" (pulse, drafted-look, Nick-signed); while draftEnabled, an available row's Status cell swaps to the Draft button → per-click roster dropdown (Nick-signed placement + picker), in-flight disable structural (the row leaves 'available' instantly); bye week deferred ([>] item — no schedule source exists); exports rosterLabel (teamName ?? owner ?? "Roster N") — THE roster display convention on the draft surface, reused by the live strip
      position-badge.tsx     — pill badge on the --pos-* tokens (bg /15 + full text); non-canonical positions → neutral fallback
      injury-chip.tsx        — Q/D → warning, O/IR → destructive, unknown designations → muted raw tag (open enumeration per wiki)
      use-player-list.ts     — pure filterAndSortPlayers (nulls last both directions, sleeper_player_id tie-break) + usePlayerList memo hook + useDebouncedValue (named exports)
      roster-fill.ts         — pure computeRosterFill, live since UI item 3 (blend rules Nick-signed 2026-07-22): per-team slot-group totals + per-position fill vs dedicated starter slots (need = count < slots; flex never force-assigned) + confirmed live picks blended union-by-player (rostered baseline wins; a pick fills only for a player rostered nowhere — position from the pool row, else the pick's catalog-joined playerPosition per the item-4 backsweep) + per-team draftedCount (every attributed pick — a draft-history dimension, drafted-then-dropped still counts); ST/BN/IR stay roster-sync-only (picks carry no lineup slot); teams with no rostered players are created from picks (rosterLabel naming)
      roster-panel.tsx       — roster/positional-need sidebar, live as picks land (UI item 3): slot-group totals vs derived_config capacities + "N drafted", position fill with --warning need flags, flex slots as their own line; pending picks deliberately invisible here (confirmed-only, Nick-signed)
      recent-picks-feed.tsx  — compact feed below the rosters (UI item 4, Nick-signed placements): newest first, visible whenever picks exist (overlay-always precedent); per row pick number, joined player name/position, R+rosterLabel, muted source badge (manual/sleeper — espn label pre-exists for 03c); Undo button on exactly latestManualPickNumber's row, session-active only, disabled in flight
      bpa-recommendations-panel.tsx — BPA panel at the TOP of the sidebar (BPA items 7–9, Nick's placement + count=8/re-pick-each-session Clarify): calls fetchBpaRecommendations(topN=8), renders top-N ranked PURELY by base VORP with market ADP beside the value and the independent need badge (NEED_LABELS starter/bench/full via muted-tier chips — 'starter' borrows --warning like RosterPanel, teal reserved) shown ALONGSIDE, never merged into, the ranking; item 9 recompute = a useEffect keyed on livePicks (any pick from any source → refetch off the SAME snapshot; shell's fingerprint bail-out means a steady poll never refetches), request-seq guard drops out-of-order responses, stale-while-revalidate keeps the prior list across refetches (only first load shows loading); my-team picker over rosters sets selfRosterId (no persisted marker — value-only + no badges until picked); rows wire to the shared handleDraft (item 8) via the roster dropdown, pending players read "Drafting…", Draft control on the same draftEnabled gate as the table. Tier-cliff item 4 (2026-07-22): a `TierDepthStrip` sub-component renders above the ranked rows — the per-position "N remaining in the best available tier" counter (QB/RB/WR/TE only, Nick's Clarify) from `context.tiers[pos].topTierSize`, consuming the SAME fetch (no second query/tier-compute; recomputes for free on the per-pick refetch). Strictly informational/non-blocking, uniform muted, tabular-nums, no scarcity emphasis (league-size-scarcity-effects.md — tier breaks are a weighted input, never a "draft now" trigger; Nick's Clarify); no new token
    dashboard/      — admin surface UI (Wave 4); server + client components over services/dashboard.ts results; the dashboard is assembled at (admin)/leagues/[leagueId]/dashboard/; NEVER imported by the spectator surface (separate rendering path)
      admin-sidebar.tsx — persistent admin nav shell (Wave 4 nav-shell, 2026-07-23; client, rendered by the [leagueId] layout): sidebar not top tabs (scales past ~5 sections). PRIMARY_NAV = Home/Dashboard/Draft board with usePathname active-highlighting (Home 'exact' so it isn't lit on child sections, the rest 'prefix'); FUTURE_NAV = 8 Wave 5/6 sections as visible disabled "Soon" rows (the `wave` field in the config is the documented mount point — future waves light their slot, not reframe the nav; Nick's Clarify). Owns the global chrome the page headers used to duplicate: the reused LeagueSelector (subPath='' — the sticky league context: switching sections never re-picks the league) + SignOutButton. Tablet/PC-first: labelled left rail from md up, compact icon top-bar below (mobile rough by design). Tokens: active bg-muted, coming-soon text-muted-foreground/40, no teal, no new token
      command-center.tsx — the command-center HOME body (Wave 4 nav-shell item 2): three snapshot cards — Standings (top 3) + Power Rankings (top 3, rank-delta chevron + all-play win%, provisional caveat on lowConfidence) deep-linking to /dashboard, and a Draft Board deep-link to /draft; renders getStandings/getPowerRankings output as-is (no second ordering/formula); empty arrays → honest pre-sync copy; tabular-nums throughout. Future Wave-5 cards (luck/waiver/playoff/trade) mount here as those features ship
      league-dashboard-shell.tsx — the dashboard composition (Nick-signed layout): a slim league-identity header (name/platform badge/season only — the selector, draft-board link, and sign-out moved to the persistent sidebar, 2026-07-23), MatchupsGrid full-width, StandingsTable + PowerRankingsList 2-col lg-up; pure composition — never re-derives service results
      player-card-sheet.tsx  — URL-driven side sheet for the player panel (?player=): server-rendered, Link-based dismissal (backdrop + close button, week param preserved), role=dialog; no client JS — no Escape-key close until a fold wants a client dialog primitive (disclosed)
      standings-table.tsx    — dense standings (the second sanctioned tabular surface): rank/team(two-line)/record(W-L, ties column only when a tie exists)/PF/PA (2-dec); renders the service's order as-is, never re-derives
      matchups-grid.tsx      — one week's matchups: URL-driven WeekSelector (?week=N), mirrored pair cards, unpaired (bye/anomaly) singles + muted anomaly notice, honest empty week
      matchup-card.tsx       — mirrored H2H card: team headers with bold totals on outer edges + UnofficialChip, starters index-paired points-desc (slot-by-slot pairing impossible — was_starter only, no historical slot attribution), bench behind native <details>, AsOfTime from freshest side; player names link ?week=N&player=<id> into the card sheet (page-assembly fold)
      week-selector.tsx      — query-only ?week=N pill links (teal active state); page decides the offered range
      power-rankings-list.tsx — all-play rankings as hybrid row-in-card list: rank/team/all-play record+win%(.xxx)/PF/rank-delta (positive teal up, destructive down); low-confidence (<6 wks) + unofficial-weeks captions per wiki luck-analysis rules
      player-card.tsx        — player detail panel (presentational; opened by the page via PlayerCardSheet on ?player=): identity (status + injury chip as independent fields), roster/availability, per-week lines with holding-team attribution + honest not_rostered FA-gap rows; reuses draft-board PositionBadge/InjuryChip
      unofficial-chip.tsx    — muted "unofficial" marker for is_final=false scores (Nick-signed: muted tier, never a status color — most rows read non-final until finality promotion ships)
      as-of-time.tsx         — deterministic "as of <time> UTC" caption from fetched_at (the freshness pairing: caption = when fetched, chip = whether final)
  lib/
    utils.ts        — cn() class-merge helper (shadcn), named export
    supabase/
      database.types.ts — generated (npm run gen:types) — never hand-edited; full public-schema types incl. foreign prolabel tables
      client.ts     — createClient() for browser/Client Components (named export)
      server.ts     — async createClient() for RSC/route handlers/Server Actions (named export)
      admin.ts      — createClient() secret-key admin client, server-only, bypasses RLS (named export)
      spectator.ts  — createClient(shareToken) (Wave 4 named-singleton #5): anonymous, session-less client on the publishable key with the token in the x-share-token header — the transport the spectator RLS policies read. RLS is the wall; server-only, the spectator loader is the sole caller (named export, path-disambiguated per convention)
      auth.ts       — getAdminAuthState(): getUser() + is_fantasy_admin RPC → unauthenticated | not_admin | admin (never a hardcoded admin UUID in app code)
  services/
    sleeper/        — server-only Sleeper integration (Wave 2); ESPN later gets the structurally separate sibling services/espn/ (no shared code path)
      client.ts     — sleeperGet<T>() + SleeperApiError: typed HTTP client, backoff/retry + global 250ms request-pacing gate per sleeper-api/rate-limits (named exports)
      types.ts      — permissive wire types (SleeperPlayerRecord, SleeperPlayersCatalog, SleeperLeague, SleeperRoster, SleeperLeagueUser, SleeperMatchup, SleeperDraft, SleeperDraftPick); unrecognized fields preserved
      players-catalog.ts — syncPlayersCatalog(): daily /players/nfl dump → validate → upsert players → mark absentees inactive
      league-config.ts — syncLeagueConfig(): /league/{id} → leagues + league_config (raw + derived_config)
      league-rosters.ts — syncLeagueRosters(): rosters+users → rosters/roster_players/standings (current snapshot, stale rows removed)
      league-matchups.ts — syncLeagueMatchups(): per-week matchups → matchups + player_scores (upsert-only historical record)
      draft-state.ts — syncLeagueDraftState(): /league/{id}/drafts selection → /draft/{id}/picks → draft_state (first-write-wins, source='sleeper_poll')
      nfl-state.ts   — getNflState() (/state/nfl) + resolveMatchupSyncWeek(): scoreable week iff season_type='regular', else null
      sync-orchestrator.ts — syncAllSleeperLeagues(): config → rosters → matchups → draft per league, per-league failure isolation; skipMatchups option; exports discoverConnectedSleeperLeagueIds()
    adp/            — ADP + projections ingestion from Sleeper's UNDOCUMENTED projections surface on api.sleeper.com (Wave 3a ADP; Wave 3b fused projections persist); deliberately quarantined from services/sleeper/ — no shared code path, never in the orchestrator chain
      client.ts     — fetchSeasonProjections() + AdpSourceError: separate api.sleeper.com client, own 250ms pacing gate + backoff/retry (documented-host limits NOT assumed)
      ingestion.ts  — syncAdpRankings(): validate-before-swap → pattern-extract adp_* → positional-rank derivation → chunked upsert → stale-row cleanup; FUSED projections persist (Wave 3b): same single fetch also lands player_projections (full stats as-received, own gates, own stale cleanup); runTrackedAdpIngestion(): never-throws sync_runs-tracked wrapper
      types.ts      — permissive wire types (AdpProjectionRecord) + AdpEntry/AdpRow/PlayerProjectionRow/AdpIngestionResult
    sync-runs.ts      — platform-agnostic sync_runs write/read surface: start/finish/record(+Safely) + hasRecentCatalogRun() guard
    draft-board.ts    — draft-board query service (Wave 3a): getDraftBoardData() merged ADP-ordered board pool + league context + the full LeagueRoster list (Wave 3b manual-pick targets, from the same rosters query); resolveAdpScoringFormat() league-format→scoring_format mapping; admin surface only
    dashboard.ts      — league-dashboard query service (Wave 4): getStandings/getMatchups/getPowerRankings/getPlayerCard/listScoredWeeks; single home of the standings ordering + all-play power formula; owner dashboard primary consumer, spectator loader may reuse (data-access sharing sanctioned, UI never)
    spectator.ts      — spectator data loader (Wave 4 named-singleton #5): loadSpectatorDashboard/loadSpectatorPlayerCard resolve a share_token → league via the token-scoped anon client (lib/supabase/spectator), then reuse the dashboard.ts getters; invalid/revoked/blank token → clean invalid_token; imports NOTHING draft-related — the read-only-surface data boundary. RLS is the enforcement wall; this adds token resolution + compose-only orchestration
    draft-picks.ts    — manual draft-pick write path (Wave 3b): recordManualPick + undoLastManualPick into the shared draft_state table (source='manual', first-write-wins) + listDraftPicks (full per-league snapshot, the live-sync payload); every RecordedPick path carries catalog-joined playerFullName/playerPosition via the players embed (UI item 4 — DST names composed from the split name fields); admin surface only; 3b auto-pick MUST write through recordManualPick (never its own insert)
scripts/            — tsx-run operational scripts (outside src; type-checked by tsc)
  sync-players.ts   — manual runner for syncPlayersCatalog (npm run sync:players; loads .env.local via --env-file)
  sync-league.ts    — manual runner for syncLeagueConfig (npm run sync:league -- <sleeper_league_id>)
  sync-rosters.ts   — manual runner for syncLeagueRosters (npm run sync:rosters -- <sleeper_league_id>)
  sync-matchups.ts  — manual runner for syncLeagueMatchups (npm run sync:matchups -- <sleeper_league_id> [week])
  sync-draft.ts     — manual runner for syncLeagueDraftState (npm run sync:draft -- <sleeper_league_id>)
  sync-sleeper.ts   — manual runner for syncAllSleeperLeagues (npm run sync:sleeper [-- <league_id> ...])
  sync-adp.ts       — manual runner for syncAdpRankings (npm run sync:adp [-- <season_year>]; season defaults from /state/nfl league_season; fused — also persists player_projections)
public/             — static assets (default scaffold SVGs, replaceable)
vercel.json         — Vercel Cron schedule: /api/cron/sync-players daily 09:00 UTC, /api/cron/sync-leagues daily 10:00 UTC (Hobby cap = 2 daily crons; 15-min league cadence deferred to season start)
```

## Build Configuration

- **Next.js 16.2.11** — App Router, `src/` directory, Turbopack (default), TypeScript strict (`tsconfig.json`: `strict: true`, `isolatedModules: true`)
- **Path alias:** `@/*` → `./src/*` (tsconfig `paths`; verified by real cross-directory import)
- **Tailwind CSS v4** (`@tailwindcss/postcss`) — CSS-first config: no `tailwind.config.*` file; theme lives in `globals.css` via `@theme inline`. Default spacing scale only.
- **shadcn/ui** — CLI 4.x, `components.json`: style `base-nova`, `cssVariables: true`, base color neutral, `iconLibrary: lucide`, aliases `@/components`, `@/components/ui`, `@/lib`, `@/lib/utils`, `@/hooks`
- **ESLint** — flat config (`eslint.config.mjs`), `eslint-config-next`
- **Package manager:** npm (package-lock.json committed). Node 24.x locally.
- **Scripts:** `dev` (turbopack), `build`, `start`, `lint`, `gen:types` (regenerates `src/lib/supabase/database.types.ts` from the linked project — the canonical regeneration command after any schema migration), `sync:players` (manual Sleeper player-catalog sync via `tsx --env-file=.env.local` — at most once per day per sleeper-api guidance), `sync:league` / `sync:rosters` / `sync:matchups` / `sync:draft` (manual per-league Sleeper sync runners, same tsx pattern — native league ID as CLI arg; `sync:matchups` takes an optional week), `sync:sleeper` (orchestrated all-leagues run — config → rosters → matchups → draft per league with per-league failure isolation; optional league-ID args for a subset), `sync:adp` (manual ADP ingestion, same tsx pattern — optional season-year arg, defaults from `/state/nfl` `league_season`)
- **Script runner:** `tsx` devDependency — runs TypeScript with tsconfig `@/` path resolution outside the Next runtime (the `server-only` package idiom is NOT used in services because it throws under tsx; server-only-ness is enforced by convention + non-`NEXT_PUBLIC_` env vars failing loudly in client bundles)
- **Scripts (added 2026-07-23):** `test` (`vitest run`), `test:watch` (`vitest`)

---

## Testing

Established Wave 3b resilience item 2 (2026-07-23, Nick's Clarify) — before this every verification was a throwaway `tsx` harness, deleted after use.

- **Runner:** **Vitest** (`vitest.config.ts` at repo root — `node` environment, `@/`→`./src` alias mirroring tsconfig `paths`, `include: src/**/*.test.ts`). The durable foundation Waves 4–6 reuse.
- **Location:** tests are **co-located** as `*.test.ts` beside their source. They are typechecked by `tsc --noEmit` (in `**/*.ts`) and linted, but never imported by a route so `next build` excludes them from the bundle.
- **DB tests never touch the shared prolabel DB (Rule 13):** `recordManualPick`'s first-write-wins race is tested against a **fake in-memory Supabase client** (`draft-picks.test.ts`) that models the `(league_id, pick_number)` ON-CONFLICT-DO-NOTHING constraint with a synchronous check-and-insert — the exact PK guarantee, zero DB.
- **Current suite (23 tests, 6 files):** `services/draft-picks.test.ts` (race → exactly one accepted; poller-already-won; uncontested), `services/bpa/board.test.ts` (shrinking-pool replacement deepen + survivor VORP rise), `services/bpa/tiers.test.ts` (tier recompute 2→1), `services/bpa/runs.test.ts` (run window trigger/reset/shallow/K-DEF), `components/draft-board/draft-queue.test.ts` (auto-pick hard-rule skip + fall-through), and `services/spectator.test.ts` (Wave 4 named-singleton #5 — 9 spectator-boundary tests against an RLS-enforcing fake client: draft_state/draft_sessions never queried, cross-league isolation per token, invalid/blank/revoked token → not-found, week selection, player-card resolution).

---

## Code Conventions

These conventions are declared in `MASTER_CONTEXT.md` and repeated here as the satellite reference. Follow them when adding or editing code; update both locations if either changes (Dual-Location Instruction Rule).

**Imports:**
- Cross-directory imports use the `@/` alias.
- Same-directory and sibling-directory imports use relative paths.
- Never use `@/` to reach a sibling file.
- Never use `../`-walking imports.

**Exports:**
- React components: default exports.
- Services, hooks, stores, utilities, types: named exports.
- Type-only exports: `export type`.

**Barrel exports (`index.ts`):**
- Directory-level public surface, where present.
- Import from the directory, not the file, when an `index.ts` exists.
- Re-export new public symbols from an existing `index.ts` rather than creating a new one.

**TypeScript:** no `any` without explicit justification.

**JSX runtime:** default React JSX runtime (Next.js standard, no custom pragma).

---

## Supabase Infrastructure

- **Project:** `tszssadgsxjoymcttlwd` — `https://tszssadgsxjoymcttlwd.supabase.co` (linked 2026-07-21). **This is Nick's shared multi-app "prolabel" project, not a fantasy-dedicated one** — it hosts live tables and data from several other apps (todd chat, elliott pricing, trip planner, loan/valuation) plus 2 pre-existing auth users. Confirmed intentional by Nick 2026-07-21; see Shared-Database Constraints below and the decision of record in `MANUAL_SETUP_CHECKLIST.md`.
- **CLI:** `supabase@2.109.1` as npm dev dependency — invoked via `npx supabase`, version pinned in `package.json` so all three of Nick's environments resolve the same CLI
- **Local workspace:** `supabase/` (created by `supabase init`) — `config.toml` committed; `supabase/migrations/` is the canonical schema-change path once the first migration exists; `supabase/.temp/` (holds the linked project-ref) is gitignored by Supabase's own `supabase/.gitignore`
- **Link state:** linked to `tszssadgsxjoymcttlwd`; database password handed over 2026-07-21 as `SUPABASE_DB_PASSWORD` in gitignored `.env.local`. Note: a project-level Bash allow rule for `npx supabase db push*` (`.claude/settings.json`, added 2026-07-21 with Nick's approval) lets Claude run migration pushes directly; the CLI reads `SUPABASE_DB_PASSWORD` from the environment (sourced from `.env.local` in-shell, never echoed). The connector's `apply_migration` remains blocked — pushes go through the CLI.
- **API keys:** standardized on the modern key pair — publishable (`sb_publishable_...`) for client-side, secret (`sb_secret_...`) for server-side — not the legacy anon/service_role JWTs. Both validated live against `rest/v1/` (secret key returns 200 with a non-browser client; Supabase actively rejects secret keys sent from browser-like user agents).

## Database Schema (live)

Fantasy-owned objects in the shared `public` schema, all created via `supabase migration new` + push (migration files in `supabase/migrations/`). **This inventory is the definitive list of fantasy-owned objects for Absolute Rule 13's blast-radius verification (`MASTER_CONTEXT.md` → Shared Database Protection): anything in the shared database not listed here is foreign and off-limits.**

- **`platform`** — Postgres enum, values `sleeper` | `espn` (extensible via `alter type ... add value` in a new migration; never a boolean)
- **`players`** — Sleeper-anchored canonical identity store. `sleeper_player_id text` PK (text, never numeric — D/ST rows use team abbreviations like `'DET'`), `full_name`/`first_name`/`last_name`/`position`/`team`/`status`/`injury_status`/`search_full_name` text, `fantasy_positions text[]`, `search_rank integer`, `birth_date date`, `metadata jsonb`, timestamps. All non-PK columns nullable (Sleeper field presence is uneven). `status` and `injury_status` are independent fields that must be read together (see `sleeper-api/player-data-quirks`); no `bye_week` column by design — byes derive from team schedule at read time. Catalog-presence columns (Wave 2, migration `20260722134911`): `is_active_in_catalog boolean not null default true` + `catalog_last_seen_at timestamptz` — presence in the daily `/players/nfl` response, deliberately distinct from Sleeper's own `status` (which has its own 'Inactive' value); absentees from a completed response are marked inactive, never deleted. `metadata` holds each player's full raw Sleeper record per sync (includes the cross-provider ID block the crosswalk item reads). RLS enabled; `fantasy_owner_all` owner policy live (see RLS policy layer below)
- **`player_id_crosswalk`** — mapping layer keyed on `sleeper_player_id` (PK, FK → `players` on delete cascade); nullable text columns `espn_player_id`, `gsis_id`, `yahoo_id`, `sportradar_id`, `pfr_id`; partial unique index on `espn_player_id` where not null. RLS enabled; `fantasy_owner_all` owner policy live (see RLS policy layer below). Population pipeline is Wave 2
- **`leagues`** — league identity root. `platform_league_uuid uuid` PK (default `gen_random_uuid()`) — the value every league-scoped table's `league_id` FK points to; never a provider's native ID. `platform` enum, `season_year integer`, `native_league_id text` (stored as-received, re-query/debug only, never a join key), `previous_platform_league_uuid` nullable self-FK on delete set null (Sleeper `previous_league_id` chain resolved at ingestion; always null for ESPN), `name text`, `share_token text` not null unique (default `generate_share_token()`), `owner_id uuid` nullable FK → `auth.users(id)` on delete set null (backfilled when the integrity item creates the admin user), timestamps. Unique `(platform, native_league_id, season_year)` — one row per platform-season-league combination. RLS enabled; `fantasy_owner_all` owner policy live (see RLS policy layer below)
- **`league_config`** — league configuration as data (Schema Rule #5), shape per the `schema-reference/league-configuration-data-model` ADR. `league_id uuid` PK doubling as FK → `leagues.platform_league_uuid` on delete cascade (1:1 extension of the league row — `platform`/`season_year` deliberately not duplicated; the parent row carries both). `scoring_settings_raw jsonb` + `roster_settings_raw jsonb` (provider-native payloads stored as-received: Sleeper `scoring_settings`/`roster_positions`+settings, ESPN scoring payload/`lineupSlotCounts`), `derived_config jsonb` (normalized subset — PPR value, TE-premium flag, superflex flag, active/bench/IR slot counts, league size — computed at ingestion and re-derived on every refresh; derivation is Wave 2), all three not null; timestamps. RLS enabled; `fantasy_owner_all` owner policy live (see RLS policy layer below)
- **`rosters`** — league-scoped team identity. Composite PK `(league_id, native_roster_id)` — the wiki-decided identity pair; `league_id uuid` FK → `leagues.platform_league_uuid` on delete cascade; `native_roster_id integer` as-received (Sleeper `roster_id` / ESPN `teamId`, both wire integers); `platform` enum + `season_year integer` (Schema Rules #2/#3 — duplicated here, unlike league_config, because rosters is N:1 with leagues); `owner_native_id text` nullable (Sleeper `owner_id` / ESPN `primaryOwner` — orphaned rosters are a normal state, ownership never part of the key), `co_owner_native_ids text[]` not null default `'{}'` (full co-owner list, primary never by array position); nullable `team_name`/`owner_display_name` display columns (mutable attributes, never join keys; populated by Wave 2 sync); timestamps. No standings figures or player arrays — those belong to the standings and roster_players tables. RLS enabled; `fantasy_owner_all` owner policy live (see RLS policy layer below)
- **`roster_slot`** — Postgres enum, values `starter` | `bench` | `reserve` | `taxi` — the normalized cross-platform roster role, resolved once at Wave 2 ingestion (Sleeper bench = three-way subtraction of `players` minus `starters`/`reserve`/`taxi`; ESPN `lineupSlotId` 20→bench, 21→reserve, active slots→starter; `taxi` is Sleeper-only)
- **`roster_players`** — roster membership join, one row per rostered player per team. Composite PK `(league_id, native_roster_id, sleeper_player_id)`; `league_id uuid` FK → `leagues.platform_league_uuid`, composite FK `(league_id, native_roster_id)` → `rosters`, `sleeper_player_id text` FK → `players` — all on delete cascade. Always keyed by `sleeper_player_id` (canonical identity; ESPN `playerId` resolves through `player_id_crosswalk` at ingestion; D/ST rows carry team-abbreviation IDs). `platform` enum + `season_year integer` (N:1 with leagues, rosters precedent); `slot roster_slot` not null; nullable native detail `starter_slot_index integer` (Sleeper starters[] order, meaningful only against league_config's slot layout — check-constrained to sleeper starters) and `espn_lineup_slot_id integer` (raw as-received — check-constrained to espn); `unique (league_id, sleeper_player_id)` so one player on two rosters in a league fails loudly at sync. Current-snapshot semantics — no `week` column; historical lineups belong to matchups. No acquisition metadata by decision (no registered v1 consumer). RLS enabled; `fantasy_owner_all` owner policy live (see RLS policy layer below)
- **`matchups`** — weekly matchup participant rows, one row per roster per week (Sleeper's native response grain; ESPN schedule entries split into two rows per side at ingestion). Composite PK `(league_id, week, native_roster_id)`; `league_id uuid` FK → `leagues.platform_league_uuid` + composite FK `(league_id, native_roster_id)` → `rosters`, both on delete cascade; `platform` enum + `season_year integer` (N:1 duplication precedent). Dual period columns per the espn-api matchup ADR — never collapsed: `week integer` (NFL stat-accumulation interval: Sleeper week / ESPN `scoringPeriodId`) and `matchup_period integer` (fantasy contest interval: ESPN `matchupPeriodId`; check-enforced `= week` on Sleeper rows, which have no native contest-interval ID — a multi-week ESPN playoff round shares one `matchup_period` across its weeks). `native_matchup_id integer` nullable — weekly pairing key as-received (Sleeper `matchup_id` / ESPN schedule entry id, both wire integers); null = bye; pairings are reconstructed by grouping on `(league_id, week, native_matchup_id)` with group-size validation, never forced into home/away pairs. `is_home boolean` nullable (ESPN-only, check-constrained). Scores: `points numeric` nullable (platform-computed weekly total as-received), `custom_points numeric` (Sleeper-only commissioner override, check-constrained), stored generated `effective_points = coalesce(custom_points, points)` — the wiki precedence rule realized once in schema. Finality: `fetched_at timestamptz` nullable + `is_final boolean not null default false` — neither platform signals finality; Wave 2+ sync stamps reads and promotes per our own policy (games complete + correction window + stable repeat reads). No ESPN live/projected columns yet (exact field names are an open wiki question — added by a Wave 2 migration once verified live). RLS enabled; `fantasy_owner_all` owner policy live (see RLS policy layer below)
- **`standings`** — season-cumulative standings, current snapshot only (Wave 2 sync overwrites in place; no weekly history — v1 rejects season-over-season views, and per-week points live on matchups). Composite PK `(league_id, native_roster_id)` mirroring rosters; `league_id uuid` FK → `leagues.platform_league_uuid` + composite FK `(league_id, native_roster_id)` → `rosters`, both on delete cascade; `platform` enum + `season_year integer` (N:1 duplication precedent). `wins`/`losses`/`ties` integer + `points_for`/`points_against` numeric — all not null default 0 (a fresh league genuinely is 0-0 with 0.00 points; zero is a true state, not missing data). Points columns hold the single recombined decimal (Sleeper's split `fpts`/`fpts_decimal` pair recombined at ingestion per the roster-endpoint ADR — the wire pair is never stored); ESPN totals land as-received from the mTeam view. ESPN rank/playoff-seed and Sleeper waiver/transaction counters excluded by decision (no registered v1 consumer; ESPN's exact record field names are an open wiki question — Wave 2 adds them via new migration once verified live). No `fetched_at` — standings have no finality concept. RLS enabled; `fantasy_owner_all` owner policy live (see RLS policy layer below)
- **`player_scores`** — per-player weekly fantasy scores, one row per rostered player per league row per week (a player's fantasy value is always `(player, league)` — Schema Rule #1). PK `(league_id, sleeper_player_id, week)` with `season_year` duplicated as a non-PK column (a leagues row is already per-season); `league_id uuid` FK → `leagues.platform_league_uuid`, `sleeper_player_id text` FK → `players`, composite FK `(league_id, native_roster_id)` → `rosters` — all on delete cascade; `platform` enum (N:1 duplication precedent). `week` is the NFL stat-accumulation interval (Sleeper week / ESPN `scoringPeriodId`) — contest-interval grouping stays on matchups. Sources: Sleeper matchup endpoint's `players_points` map (league-scored by Sleeper, never the generic `pts_std`/`pts_ppr` presets); ESPN player stat records selected at ingestion by all three discriminators (`scoringPeriodId` + `statSourceId` + `statSplitTypeId`) so projections/season splits are never mistaken for weekly actuals. Full per-week attribution: `native_roster_id integer` not null (every row is inherently a rostered player — free-agent scores never appear in these payloads; this is the only historical per-week attribution record, since roster_players is current-snapshot and mRoster can't reconstruct past weeks) + `was_starter boolean` not null (Sleeper starters[] membership / ESPN lineupSlotId within the league's active slot set per league_config). `points numeric` not null — as-received, zero is as-received, negatives legitimate; ESPN projected/live columns excluded by decision (field names are an open wiki question — Wave 2 adds them via new migration once verified live). Finality mirrors matchups: `fetched_at timestamptz` nullable + `is_final boolean not null default false`, promoted only by our own sync policy. RLS enabled; `fantasy_owner_all` owner policy live (see RLS policy layer below)
- **`draft_pick_source`** — Postgres enum, values `manual` | `sleeper_poll` | `espn_poll` — which write path recorded a draft pick (app-internal provenance, not a wire value)
- **`draft_state`** — shared draft-pick log, one row per completed pick per league row. All three write paths (manual click-to-draft, Sleeper polling, ESPN polling) write to this one table. PK `(league_id, pick_number)` — the first-write-wins guarantee itself: first writer of a pick number wins, later writers conflict; rows are immutable under that semantic, so there is no `fetched_at`/`is_final` machinery. `league_id uuid` FK → `leagues.platform_league_uuid`, `sleeper_player_id text` FK → `players` (ESPN `playerId` resolves through `player_id_crosswalk` at ingestion), composite FK `(league_id, native_roster_id)` → `rosters` — all on delete cascade. `pick_number`/`round` integer as-received (Sleeper `pick_no`/`round`, ESPN `overallPickNumber`/`roundId` — never reconstructed from snake-order arithmetic; reversal rounds, keepers, traded picks, and commissioner edits all break the formula silently). `native_roster_id integer` not null — Sleeper `roster_id` (the ownership field — never `draft_slot`/`picked_by`) / ESPN `teamId`; the manual path always selects the receiving team. `source draft_pick_source` not null, check-constrained so poll sources match their platform (`source_matches_platform`). `platform` enum + `season_year integer` (N:1 duplication precedent). `native_draft_id text` nullable, sleeper-only check — Sleeper `draft_id` provenance as-received (drafts arrays carry never-deleted aborted/restarted drafts; the Wave 2 poller records its season/type/status selection here); null for ESPN and manual picks. `amount numeric` nullable — auction winning bid (Sleeper `metadata.amount` string coerced at ingestion / ESPN `bidAmount`; the only meaningful value signal in auctions, where pick order is nomination order); null for snake/linear picks. Snake/linear + auction formats only — dynasty multi-draft seasons are out of scope by decision, one draft per league row assumed. `is_keeper`/`draft_slot`/`picked_by` excluded by decision (no registered v1 consumer). Write logic is Wave 2 (poll paths) + Wave 3b (manual). RLS enabled; `fantasy_owner_all` owner policy live (see RLS policy layer below)
- **`draft_sessions`** — admin-only draft-session state (Wave 3b active-draft orchestration, migration `20260723001620`): the `is_draft_active` flag whose toggling elevates draft polling to live cadence. `league_id uuid` PK doubling as FK → `leagues.platform_league_uuid` on delete cascade (1:1 extension — `platform`/`season_year` deliberately not duplicated, league_config precedent; operational state, no `season_year`, sync_runs precedent). `is_draft_active boolean not null default false`; `activated_at timestamptz` (the 6-hour soft-TTL anchor, Nick-signed — staleness computed at READ time by `services/draft-sessions.ts`, no job mutates the row; check `active_requires_activated_at` guarantees the timestamp whenever the flag is on); `deactivated_at timestamptz` (last explicit stop); timestamps + `set_updated_at`. Deliberately its OWN table, never a column on `leagues`/`league_config` — both receive spectator SELECT policies in Wave 4 and draft state must never be spectator-reachable at any wave; this table carries `fantasy_owner_all` ONLY and never gets a spectator policy. Collision-checked against the live shared schema before creation
- **`sync_runs`** — sync-run telemetry (Wave 2 cron sub-section, migration `20260722160525`): one row per sync unit — a global players-catalog run (`league_id` null) or one league's slice of a league-state/draft-poll run. `id bigint` identity PK; `source text` check (`players_catalog` | `league_state` | `draft_poll` | `adp_ingestion` — the last added by Wave 3a migration `20260722180354`; ESPN sources extend via new migration); `league_id uuid` nullable FK → `leagues` on delete cascade; `platform` enum not null; `status text` check (`running` | `success` | `failure` | `skipped` — skipped = the once-per-day guard declined to start, never a completed sync); `started_at` not null / `completed_at` nullable; `counts jsonb` (per-job shapes); `error_summary text` (may contain native league IDs — owner surfaces only); timestamps + `set_updated_at`. Deliberately no `season_year` (operational telemetry, not stat data — Schema Rule #3 scope). Indexes: `idx_sync_runs_league_id`, `idx_sync_runs_source_status_started_at` (the guard/recency path). RLS enabled; `fantasy_owner_all` owner policy live
- **`adp_rankings`** — current-snapshot ADP per player/source/season/scoring-format (Wave 3a, migration `20260722175619`). Composite PK `(sleeper_player_id, adp_source, season_year, scoring_format)` — long per-format rows (Nick-signed 2026-07-22 amendment of the build file's pre-source-confirmation single-row spec): the source carries 12+ per-format `adp_*` fields per player with a demonstrably open inventory, so formats are rows, pattern-ingested from the field suffix. `sleeper_player_id text` FK → `players` on delete cascade; `adp_source text` check (`'sleeper'` — extensible via new migration, sync_runs precedent); `scoring_format text` deliberately unconstrained (open set — unrecognized `adp_*` variants ingest as rows); `adp_overall numeric` check `>0 and <999` (schema-level backstop of the wiki's 999.0-sentinel rule); `positional_rank integer` nullable (platform-derived at ingestion — ADP asc within `players.position` per format, `sleeper_player_id` tie-break; the source provides no positional ADP); `ingested_at timestamptz`. NOT league-scoped by design — ADP is market data global to a source/season/format population, so Schema Rules #1/#2 don't apply (`season_year` per Rule #3 is in the key). Current-snapshot semantics: idempotent upsert overwrites in place; validate-before-swap gates (response shape, ≥200-record floor, ≥50%-of-existing ratio) run before any write so a bad fetch preserves the last good snapshot; stale rows (absent from a fully-persisted validated response) are deleted after success. Read-path index `idx_adp_rankings_source_season_format_adp`. RLS enabled; `fantasy_owner_all` owner policy live
- **`player_projections`** — current-snapshot season projection record per player/source/season (Wave 3b BPA prerequisite, migration `20260723023127` — Nick-signed correctness amendment 2026-07-22; projections-source decision of record in `MANUAL_SETUP_CHECKLIST.md` Wave 3b). Composite PK `(sleeper_player_id, projection_source, season_year)`; `sleeper_player_id text` FK → `players` on delete cascade; `projection_source text` check (`'sleeper'` — extensible via new migration, adp_rankings/sync_runs precedent); `stats jsonb` not null — the record's FULL stats object as-received from the undocumented endpoint (component projection stats, preset `pts_*` totals, AND `adp_*` fields including their 999.0 sentinels — raw snapshot per the wiki's permissive-parsing decision). ADP is NEVER read from this table (`adp_rankings` is the sentinel-filtered ADP read path); the BPA engine reads the component stats and scores them with `league_config` weights at read time — preset `pts_*` format buckets are never used as league values (Nick's decision of record). Provenance as-received: `company text` (observed `'rotowire'`) + `source_last_modified`/`source_updated_at timestamptz` (epoch-ms wire values converted, implausible → null); `ingested_at timestamptz` not null; timestamps + `set_updated_at`. NOT league-scoped by design (adp_rankings precedent — vendor/market data global per player/season; Schema Rule #3's `season_year` in the key). Populated ONLY by the fused adp ingestion run (same single fetch as `adp_rankings`; own validate-before-swap gates; current-snapshot stale cleanup). Read-path index `idx_player_projections_source_season`. RLS enabled; `fantasy_owner_all` owner policy live. Collision-checked against the live shared schema (information_schema) before creation
- **`generate_share_token()`** — returns 32 random bytes hex-encoded (64 chars, 256 bits) via `extensions.gen_random_bytes` (pgcrypto, pre-installed on the shared project); `search_path = ''`. Default on `leagues.share_token`; revocation/regeneration is a plain UPDATE — no immutability constraint
- **`current_share_token()`** (Wave 4, migration `20260723072917`) — stable, `search_path = ''`; the `share_token` presented on the current request via the `x-share-token` header (PostgREST `request.headers` GUC, lower-cased), or null when absent/blank. Every spectator SELECT policy calls it; anon keeps EXECUTE (policies evaluate as anon)
- **`regenerate_share_token(p_league_id uuid)`** (Wave 4, migration `20260723072917`) — SECURITY INVOKER, `search_path = ''`; `update leagues set share_token = generate_share_token() where platform_league_uuid = p_league_id returning share_token`. Owner-only regenerate == revoke (share_token NOT NULL, no null-token state); the caller's `fantasy_owner_all` UPDATE policy is the authorization wall (non-admin updates 0 rows → null). EXECUTE revoked from `anon` (migration `20260723074134`) + `public`, granted to `authenticated`
- **`set_updated_at()`** — shared trigger function (`search_path = ''`), `before update` trigger on all ten tables
- **`is_fantasy_admin()`** — stable, `search_path = ''`; true iff `auth.uid()` equals the fantasy admin's UUID (`1d5dab52-9b93-4544-a99b-6d4dc3c84a66` — the pre-existing prolabel auth user nick@prolabelco.com, created 2026-05-15 and reused as fantasy admin by Nick's 2026-07-22 ruling; a duplicate email cannot exist in the shared namespace). The single rotation point for admin identity — every fantasy policy calls it
- **RLS policy layer** — one `fantasy_owner_all` policy per fantasy table (all ten): `FOR ALL TO authenticated`, `using`/`with check` = `(select public.is_fantasy_admin())` (InitPlan-wrapped, evaluated once per statement). Full owner CRUD pinned to Nick's specific `auth.uid()` — never the blanket `authenticated` role (Shared-Database Constraints). Everything else is deny-by-default; verified live: the other prolabel user and `anon` both rejected (42501)
- **Spectator RLS policy layer** (Wave 4 named-singleton #5, migration `20260723072917`) — one `spectator_share_read` policy (`FOR SELECT TO anon`) on EIGHT tables: `leagues` (`share_token = (select current_share_token())`), the six league-scoped tables `league_config`/`rosters`/`roster_players`/`matchups`/`standings`/`player_scores` (`league_id in (select platform_league_uuid from leagues where share_token = (select current_share_token()))`), and `players` (global identity catalog — `exists (...matching league...)`, readable to any valid-token holder). `players` + `roster_players` beyond the build file's six are the Nick-signed correctness amendment (the reused `getMatchups`/`getPlayerCard` getters read them). Base `GRANT SELECT ... TO anon` on the eight tables backs the policies (RLS still gates every row). **`draft_state` and `draft_sessions` deliberately have NO spectator policy — unreachable to anon even with a valid token.** Writes stay owner-only everywhere. Verified live read-only: valid token unlocks all 8 tables scoped to the one league, draft tables return 0 rows with the token, wrong/no token → 0 rows
- **Integrity indexes** — 17 single-column `idx_*` indexes: `leagues.previous_platform_league_uuid` (the one unindexed FK → `platform_league_uuid`), `sleeper_player_id` on `roster_players`/`player_scores`/`draft_state`, and `platform` + `season_year` on the six N:1 tables plus `season_year` on `leagues`. No duplicate index anywhere a PK/unique btree already leads with the column (Postgres prefix rule); `leagues.owner_id` deliberately unindexed (outside the item's list, trivial table)

## Supabase Client Wiring (src/lib/supabase/)

- **Packages:** `@supabase/ssr@0.12.3` + `@supabase/supabase-js@2.110.8` — the current Supabase App Router standard (the older auth-helpers package is deprecated).
- **`database.types.ts`** — generated via `npm run gen:types` (`supabase gen types typescript --linked --schema public`); committed, never hand-edited. Contains the entire shared public schema by decision (2026-07-22): the CLI has no per-table filter, so the ~49 foreign prolabel tables appear as inert generated types alongside the ten fantasy tables — regenerated wholesale after every schema migration.
- **`client.ts`** — `createClient()` (named export): `createBrowserClient<Database>` with the publishable key, for Client Components.
- **`server.ts`** — `async createClient()` (named export): `createServerClient<Database>` with `await cookies()` (Next 15+ async API), getAll/setAll cookie bridge, and a no-op catch for Server Component set attempts (cookie writes happen in middleware/route handlers). For RSC, route handlers, and Server Actions.
- **Naming convention (Nick's Clarify decision, 2026-07-22):** both factories export the same name `createClient`, disambiguated by import path (`@/lib/supabase/client` vs `@/lib/supabase/server`) — the Supabase-docs convention. No barrel `index.ts` in this directory (impossible under same-name exports; the path is the disambiguator).
- **`admin.ts`** — `createClient()` (named export): plain `createClient<Database>` from `@supabase/supabase-js` with `SUPABASE_SECRET_KEY`, `persistSession`/`autoRefreshToken` off (no cookie context). Server-only, bypasses RLS — never import from client code (the secret key isn't `NEXT_PUBLIC_`, so a client-bundle import would fail loudly rather than leak). Created 2026-07-22 by the health-check session (Nick's Clarify decision resolving the deliberately deferred auth-approach question); first consumer is `/api/health`, Wave 2 sync jobs are the intended main consumers.

## Service API Reference (src/services/)

First populated by Wave 2 (2026-07-22). Convention: one directory per external platform, structurally separate (no shared code path between Sleeper and ESPN — Data Source Architecture rule); named exports; no barrel `index.ts`; server-only by convention (never import from client components).

**`services/sleeper/client.ts`**
- `sleeperGet<T>(path, options?: { timeoutMs?: number }): Promise<T>` — GET against `https://api.sleeper.app/v1` (base URL per `sleeper-api/authentication`). No credentials anywhere (the read API has none). Bounded retries (3 attempts, exponential backoff + full jitter, base 1s); retriable = throttle signals + 5xx; 404 is definitive and never retried. Default timeout 10s, per-request override. The generic is a caller-side assertion — persisting callers must structurally validate. Global pacing gate (Wave 2 orchestrator session, 2026-07-22): physical request starts (retries included) are spaced ≥250ms apart process-wide (≤240/min worst case) — substantial headroom below the ~1,000 req/min documented ceiling and a bound on short-window burstiness, per `sleeper-api/rate-limits`. No interactive Sleeper traffic exists (UI reads Supabase), so this single gate is the bulk budget; revisit budget isolation only if interactive Sleeper calls ever appear.
- `SleeperApiError` — structured error translation: `kind` (`timeout | network | throttled | not_found | http | invalid_response`), `status`, `path`, `isThrottleSignal` (true for the rate-limits page's one backoff category: 429 / non-JSON body / connection-level failure). Paths may contain league IDs — server logs only (credential-level care per `sleeper-api/authentication`).

**`services/sleeper/types.ts`** — `SleeperPlayerRecord` (every field optional + unknown-preserving index signature — Sleeper's field inventory is unversioned), `SleeperPlayersCatalog` (map keyed by `player_id`, not an array), `SleeperLeague`, `SleeperRoster`, `SleeperLeagueUser`, `SleeperMatchup`, `SleeperDraft` (one element of the `/league/{id}/drafts` array — selection never trusts array order or the league object's single `draft_id`), `SleeperDraftPick` (`roster_id` = ownership; `draft_slot`/`picked_by` never interchangeable with it; `metadata` is a point-in-time snapshot whose auction `amount` is a string).

**`services/sleeper/players-catalog.ts`**
- `syncPlayersCatalog(db: SupabaseClient<Database>): Promise<PlayersCatalogSyncResult>` — the daily catalog job: fetch `/players/nfl` (60s timeout), validate before persisting (plain-object shape; ≥5,000 records; ≥80% of existing row count; core-field sample check — a failed fetch/validation leaves last known-good rows untouched), chunked upsert (500/batch) keyed on the catalog map key, then — only after every chunk succeeds — one set-based mark-inactive UPDATE (`is_active_in_catalog=true AND (catalog_last_seen_at < run start OR null)`). Reappearing players reactivate via the upsert. Returns counts + timestamps. **At most once per day** (`sleeper-api/rate-limits` — the most load-bearing Sleeper contract). Callers: `scripts/sync-players.ts` (manual, `npm run sync:players`); the Wave 2 cron route will be the scheduled caller.

**`services/sleeper/league-config.ts`** (Wave 2 league-scoped sync, 2026-07-22)
- `syncLeagueConfig(db, nativeLeagueId): Promise<LeagueConfigSyncResult>` — fetches `/league/{league_id}`, validates (identity echo, 4-digit season, sport, NOT NULL name, settings/scoring/roster shapes, positive league size), resolves `previous_league_id` against existing rows only (null when the prior season isn't held — v1 never creates historical rows), upserts `leagues` (onConflict `platform,native_league_id,season_year`; `share_token` never touched on conflict; `owner_id` = the fantasy admin UUID per Nick's ruling) and `league_config` (raw payloads as-received: `scoring_settings_raw` = the open scoring map never zero-filled, `roster_settings_raw` = `{ roster_positions, settings }`; `derived_config` re-derived in full every run). `DerivedLeagueConfig` keys: `ppr` (`rec` ?? 0), `te_premium` (`bonus_rec_te` > 0 — wiki-silent wire key, declared), `superflex` (SUPER_FLEX label or ≥2 QB slots), `active_slot_count`/`bench_slot_count` (label counts), `ir_slot_count` (fixed 2026-07-22, Nick-signed: `max(IR-label count, settings.reserve_slots)` — Sleeper expresses IR capacity either way per `sleeper-api/league-endpoint` (settings roster-mechanics + open question #4's count-only shape, confirmed live on Nick's league: reserve_slots 1, zero IR labels); capacity never understated), `league_size` (`total_rosters` ?? `num_teams`). League config is a slow-changing resource — refresh on explicit trigger/low-frequency schedule, never per roster/matchup fetch. Caller: `scripts/sync-league.ts`.
- `LeagueConfigSyncResult` includes `leagueUuid` (the minted/found `platform_league_uuid`), `previousChainResolved` (null = no chain on the wire), and the derived config.

**`services/sleeper/league-rosters.ts`** (Wave 2 league-scoped sync, 2026-07-22)
- `syncLeagueRosters(db, nativeLeagueId): Promise<LeagueRostersSyncResult>` — requires the league connected (errors otherwise); parallel `/rosters` + `/users` fetch; validates (integer `roster_id` on every entry; `user_id` on every user; player arrays may be absent pre-draft). Classifies membership per the roster-endpoint ADR: `starters` positionally (placeholder entries — null/`"0"` — skipped as players but advancing `starter_slot_index`), `reserve`, `taxi`, bench = strict three-way subtraction; precedence starter > reserve > taxi on transactional-window overlap. Persist order: upsert `rosters` → delete stale rosters → delete stale membership → upsert `roster_players` (onConflict `league_id,sleeper_player_id` — the one-player-one-roster unique pair, so team-moves update in place) → upsert `standings` (split `fpts`/`fpts_decimal` pairs recombined `int + dec/100` at ingestion; wire pair never stored; absent figures are true zeros). Display columns stored raw-per-column: `team_name` = `metadata.team_name` (missing/empty → null), `owner_display_name` = `display_name` ?? `username`; the users-endpoint fallback chain is read-time presentation logic. Missing-catalog players are skipped and reported in the result (`skippedMissingPlayerIds`, Nick's 2026-07-22 ruling) — self-heals after the next daily catalog run. Caller: `scripts/sync-rosters.ts`.

**`services/sleeper/league-matchups.ts`** (Wave 2 league-scoped sync, 2026-07-22)
- `syncLeagueMatchups(db, nativeLeagueId, options?: { week? }): Promise<LeagueMatchupsSyncResult>` — requires the league connected; default full-season sweep (weeks 1–18, empty weeks skipped), single-week mode for the future in-season/cron cadence. Upserts `matchups` at the wire's own per-roster grain (`matchup_period = week` on Sleeper rows per the schema check; `native_matchup_id` as-received, null = bye/unpaired; `is_home` null; `points`/`custom_points` as-received — `effective_points` is a stored generated column and is never inserted) and `player_scores` from `players_points` (already league-scored by Sleeper — pure ingestion, never computation; `was_starter` from that week's `starters` set; roster attribution from the row's `roster_id`). Finality policy slice: one `/league/{id}` fetch per sync — lifecycle status `complete` stamps every row `is_final = true`, anything else `false` with `fetched_at` stamped and in-season promotion (games complete + correction window + stable repeat reads) deferred to the cron sub-section. Upsert-only — these tables are the per-week historical record, no delete pass. Missing-catalog players skip-and-record as in league-rosters; non-numeric `players_points` values counted in `skippedNonNumericScores`. Chunked upserts (500/batch). Caller: `scripts/sync-matchups.ts`.

**`services/sleeper/draft-state.ts`** (Wave 2 league-scoped sync, 2026-07-22)
- `syncLeagueDraftState(db, nativeLeagueId): Promise<LeagueDraftSyncResult>` — requires the league connected AND its rosters synced (both explicit precondition errors — the picks' roster FK needs rosters). Walks `/league/{id}/drafts` and selects the season's draft by Nick's status-preference ruling: `drafting` > `complete`; a tie within the preferred status, or no candidate in either with any non-`pre_draft` draft present (e.g. `paused`), errors explicitly naming the candidates; a season whose drafts are all `pre_draft` returns a valid zero-pick result (`nativeDraftId: null`) so the orchestrator runs it unconditionally offseason. Fetches `/draft/{draft_id}/picks`, validates every entry (integer `pick_no`/`round` ≥1, usable `player_id`, integer `roster_id`), and inserts into `draft_state` with `source='sleeper_poll'`, the selected draft recorded in `native_draft_id`. First-write-wins realized as ON CONFLICT `(league_id, pick_number)` DO NOTHING (`ignoreDuplicates`) — existing rows never rewritten; the returned representation counts only newly inserted rows (`picksWritten` vs `picksAlreadyRecorded`). A pick referencing a roster outside the synced set hard-fails (structural, distinct from the missing-player ruling); missing-catalog players skip-and-record (`skippedMissingPlayerIds` — self-heals: later re-runs fill the gaps). Auction drafts coerce `metadata.amount` (string wire value) to numeric (`nonNumericAmounts` counts drift); `amount` stays null for snake/linear. Chunked inserts (500/batch). Since the Wave 3b live-strip item (2026-07-22): the result also carries `draftOrder: DraftOrderMeta | null` — the selected draft's `slot_to_roster_id` (sanitized, integer values only) + `type` + `settings.reversal_round`/`rounds` — the client on-clock projection's input (Nick-signed: carried through the poll path, never persisted; null pre-draft). The drafts-ARRAY element omits `slot_to_roster_id` (verified live — it carries only the user-keyed `draft_order`, which is deliberately not used), so a missing map triggers a best-effort `GET /draft/{draft_id}` detail fetch (one extra paced request per executed sync; a failed detail fetch degrades the metadata, never the sync). Caller: `scripts/sync-draft.ts`.

**`services/sleeper/sync-orchestrator.ts`** (Wave 2 league-scoped sync, 2026-07-22)
- `syncAllSleeperLeagues(db, options?: { leagueIds?, week?, skipMatchups? }): Promise<SleeperSyncRunResult>` — the per-league orchestrator: discovers all `leagues` rows where `platform='sleeper'` (or takes an explicit `leagueIds` subset; Nick's ruling — full runs sync every connected league, completed leagues being cheap idempotent re-syncs) and runs each league's chain in dependency order `league_config → rosters → matchups → draft` (draft after rosters per its FK; added to the chain by the same-day scope correction). Per-league failure isolation: a step failure records `failedStep` + `error` on that league's `LeagueSyncOutcome`, skips that league's remaining steps (`skippedSteps`), and continues to the next league — one bad league never blocks the others. `week` passes through to the matchups sync (the in-season cron cadence); `skipMatchups` drops the matchups step entirely (cron sub-section addition — scheduled offseason runs must not full-sweep immutable weeks); leagues process sequentially in deterministic `native_league_id` order under the client's global pacing gate (no extra throttling layer). Deliberately excludes the player catalog (its once-per-day rule gives it its own cadence). Callers: `scripts/sync-sleeper.ts` (exit 1 if any league failed) and the `/api/cron/sync-leagues` route (scheduled).
- `discoverConnectedSleeperLeagueIds(db): Promise<string[]>` — native IDs of every connected Sleeper league in deterministic order (extracted for the cron draft route; the orchestrator uses it internally).

**`services/sleeper/nfl-state.ts`** (Wave 2 cron sub-section, 2026-07-22)
- `getNflState(): Promise<SleeperNflState>` — `/state/nfl`, small unauthenticated payload, fetched fresh per scheduled run (inside the wiki page's minutes-order cache guidance).
- `resolveMatchupSyncWeek(state): number | null` — the week the scheduled league sync should pull matchups for: `week` iff `season_type === 'regular'` and an integer in 1–18 (defensive bounds — the endpoint is unversioned), else null (`pre`/`post`/`off` have no scoreable fantasy week; the undocumented `off` is handled as a real state). Never date arithmetic, never `display_week` (UI-only), per `sleeper-api/nfl-state-endpoint`.

**`services/sync-runs.ts`** (Wave 2 cron sub-section, 2026-07-22 — platform-agnostic; ESPN sync records through these same functions when unblocked)
- `startSyncRun(db, {source, platform, leagueId?}): Promise<number>` / `finishSyncRun(db, id, {status, completedAt?, counts?, errorSummary?})` — running-row-then-terminal pattern (a crashed job leaves a visible `running` row).
- `recordSyncRun(db, record)` / `recordSyncRunSafely(db, record)` — one-shot terminal rows for per-league slices; the Safely variant logs and swallows (tracking must never mask a sync's own outcome).
- `hasRecentCatalogRun(db, windowHours=24): Promise<boolean>` — the players-catalog once-per-day guard: `running`/`success` rows within the window block; `failure` rows allow a retry (the rule's sanctioned exception); `skipped` rows never count. Throws on read error — the catalog route fails CLOSED. Note: the manual `npm run sync:players` script calls the service directly and bypasses this guard — manual cadence discipline still applies to the script path.

**`services/adp/`** (Wave 3a ADP sub-section + Wave 3b fused projections persist, 2026-07-22 — the platform's ADP AND projections source of record: Sleeper's UNDOCUMENTED projections surface on `api.sleeper.com`, per `wiki/topics/sleeper-api/projections-endpoint.md`. Quarantined at ESPN grade: no shared code path with `services/sleeper/`, never part of the orchestrator's league chain, failures degrade only the ADP/projections features. The projections consumer lives INSIDE this quarantine by standing rule — never a second api.sleeper.com client.)
- `fetchSeasonProjections(seasonYear): Promise<unknown>` (`client.ts`) — one GET of the season projections array using exactly the wiki-verified parameter combination (`season_type=regular`, repeatable `position[]` for QB/RB/WR/TE/K/DEF — Nick-signed — and `order_by`; unverified variations never assumed). Separate module-local 250ms pacing gate, bounded backoff/retry, 30s timeout; `AdpSourceError` mirrors the documented client's taxonomy without sharing code. Documented-host rate knowledge is NOT assumed to transfer.
- `syncAdpRankings(db, options?: { seasonYear? }): Promise<AdpIngestionResult>` (`ingestion.ts`) — validate-before-swap ingestion: structural validation (array shape, ≥200-record floor, sampled `player_id`+`stats` presence) and plausibility gates (zero-insertable abort; <50%-of-existing abort) before any write; pattern-extracts `adp_*` fields (open set — unrecognized variants become rows; ≥999 sentinel and ≤0/non-numeric values excluded and counted); resolves identity against `players` (unmapped recorded, never minted — self-heals after next catalog run); derives `positional_rank` (ADP asc within position per format, `sleeper_player_id` tie-break); chunked idempotent upsert on the composite PK; stale-row cleanup only after full success. Season defaults from `/state/nfl` `league_season` (nfl-state page's season-selection decision). FUSED projections persist (Wave 3b BPA prerequisite, Nick-signed 2026-07-22): the same validated response additionally lands one `player_projections` row per fetched player — full `stats` object as-received + company/timestamp provenance; candidates dedup by `player_id` (first wins); the projections snapshot gates independently (zero-insertable abort; <50%-of-existing abort) and BOTH tables' gates run before EITHER writes; the ADP swap completes first, so a late projections failure degrades only the projections snapshot (each table keeps its own last good rows). Result gains `projectionRowsPersistedCount`/`projectionStaleRowsDeletedCount`. Callers: `scripts/sync-adp.ts`, `runTrackedAdpIngestion`.
- `runTrackedAdpIngestion(db): Promise<TrackedAdpIngestionOutcome>` (`ingestion.ts`) — never-throws containment wrapper writing the `sync_runs` start/finish pair (source `'adp_ingestion'`, platform `'sleeper'`, `league_id` null; `counts` includes `projections_persisted`/`projections_stale_deleted` since the fused persist). Callers: the `/api/cron/sync-adp` route (maps `ok:false` → 500) and the sync-leagues piggyback (logs and continues — an ADP failure never affects the league-state outcome).

**`services/draft-board.ts`** (Wave 3a data-layer sub-section, 2026-07-22 — admin draft-board surface only, never spectator)
- `getDraftBoardData(db, leagueId): Promise<DraftBoardResult>` — the board query: league identity (explicit columns — `share_token`/`owner_id`/`native_league_id` never selected anywhere in the module; live-asserted absent from the serialized result) + `derived_config` context (defensive JSONB parsing, malformed → nulls, never invented defaults) + resolved ADP format + the merged player pool (Nick-signed: ADP carriers ∪ league-rostered; explicit `availability: 'rostered'|'available'` from `roster_players` presence with team attribution; ADP asc nulls-last, `sleeper_player_id` tie-break). Player-list additions (2026-07-22): each rostered player carries `rosteredSlot` (`roster_players.slot` — starter/bench/reserve/taxi), and the context carries `slotLayout` (parsed from `roster_settings_raw` — see below; the raw column itself never crosses into the response). ADP season = latest ingested snapshot for the source (current-market semantics, returned in context with `adpIngestedAt` for the board header). Malformed `league_id` UUID-rejected before any query; unknown → typed `league_not_found`; never a partial response. Platform-agnostic by construction — every join keyed on `sleeper_player_id` (ESPN resolves through the crosswalk at Wave 2 ingestion), zero platform branching; ESPN boards use this exact path once ESPN sync lands.
- `parseRosterSlotLayout(raw): RosterSlotLayout | null` (player-list sub-section, 2026-07-22 — Nick-signed raw-column read per the league-configuration-data-model ADR's escape hatch) — shape-tolerant, never throws: parses the Sleeper raw shape's `roster_positions` into `{ dedicated (per-label counts, layout order), flex (any label containing FLEX), bench, ir, taxi }`; anything else (including ESPN's native shape until its parser lands) → null and consumers degrade. Pattern-based classification — no closed label list (IDP labels land as dedicated naturally). Note: layout `ir` is the raw label count; display capacity reads `derived_config.ir_slot_count` (reserve_slots-aware). Exported for Wave 3b BPA reuse — never re-derive a layout elsewhere.
- `resolveAdpScoringFormat(derived): string | null` — Nick-signed bucketed mapping (2026-07-22): superflex → `'2qb'` regardless of PPR (QB replacement value is the most format-sensitive axis per `league-mechanics/multi-platform-adp-divergence`); else `ppr >= 1` → `'ppr'`, `0 < ppr < 1` → `'half_ppr'`, else `'std'`. Dynasty/IDP formats deliberately unmapped in v1 (no such league; `derived_config` has no dynasty flag — raw-column escape hatch per the ADR). `te_premium` never affects format (no source ADP axis). NO cross-format fallback — a format with no rows is an explicit empty state (`average-draft-position` format-leakage rule).
- `listConnectedLeagues(db): Promise<ConnectedLeague[]>` (route + shell sub-section, 2026-07-22) — every connected league for the board's selector and post-sign-in landing: explicit columns only (`platform_league_uuid`/`name`/`platform`/`season_year` — the module's boundary discipline applies), ordered season desc / name asc / id tie-break. No hardcoded league count anywhere; ESPN leagues appear automatically once connected.

**`services/dashboard.ts`** (Wave 4 admin data-access sub-section, 2026-07-22 — the league-dashboard query service; owner dashboard is the primary consumer through the RLS server client; the Wave 4 spectator loader may reuse these functions with its own share_token-scoped client — shared data-access logic is sanctioned, shared UI never. Boundary discipline throughout: explicit columns only; `share_token`/`owner_id`/`native_league_id` never selected; zero `draft_state` references; malformed/unknown `league_id` → typed `league_not_found`, never partial. Season scoping is structural — a leagues row is per-season, so `league_id` alone pins the season.)
- `getStandings(db, leagueId): Promise<StandingsResult>` — team records + PF/PA from the `standings` snapshot, display names joined from `rosters`, ordered by the Nick-signed rule (wins desc → points_for desc → native_roster_id asc; declared wiki silence — Sleeper exposes no standings-ordering key), rank = position, flat (no division grouping in v1; the live league's settings carry no divisions key). Empty teams = honest unsynced state.
- `getMatchups(db, leagueId, week): Promise<MatchupsResult>` — one week's head-to-head pairs reconstructed per the matchup-endpoint ADR: group on `native_matchup_id` (nulls = byes → unpaired entries), size-two groups pair, any other size degrades to unpaired + `hasPairingAnomaly` (never forced, never a throw — Nick-signed). Sides carry stored `effective_points` (commissioner-override precedence realized in schema) + full-roster `player_scores` lines (`was_starter`, `fetched_at`/`is_final` passed through for the freshness item's UI). Week validated 1–18 → typed `invalid_week`.
- `getPowerRankings(db, leagueId): Promise<PowerRankingsResult>` — All-Play record (Nick-signed; the wiki's decided schedule-neutral team-quality measure): every scored roster plays every other each counted week; regular-season weeks only via raw-settings `playoff_week_start` (unparseable → all scored weeks); weekly ties = half-wins; ordering all-play win% desc → counted-week PF desc → roster id. Exposes `weeksCounted`/`nonFinalWeeksCounted` + `lowConfidence` (<6 weeks, per the wiki's early-season flag) and per-team `standingsRank`/`rankDelta` against `fetchRankedStandings` — the module-internal single definition of standings order shared with getStandings.
- `getPlayerCard(db, sleeperPlayerId, leagueId): Promise<PlayerCardResult>` — Sleeper-anchored identity (status + injury_status returned together), current roster/availability from `roster_players`, and per-week entries: scored rows as-received with holding-team attribution (player_scores is the only historical per-week attribution record), and every other league-scored week (derived from matchups) as an explicit `not_rostered` entry — the Nick-signed FA-week gap rendered honestly, never invented. Player IDs opaque, never format-checked (D/ST = team abbrevs).
- `listScoredWeeks(db, leagueId): Promise<number[]>` (page-assembly fold, 2026-07-22) — weeks with any scored matchup row (`effective_points` non-null), ascending; the dashboard page's week-selector range and default-week source (max = latest scored week: the in-season current synced week, or a complete season's final week). Empty array for unknown/unscored leagues — honest, never a throw.

**`services/draft-picks.ts`** (Wave 3b manual click-to-draft write path, 2026-07-22 — admin surface only, never spectator; the app-internal mutation surface all local pick-writes go through, including 3b's future auto-pick by build-file mandate)
- `recordManualPick(db, input: ManualPickInput): Promise<ManualPickResult>` — validates everything the row references before writing: UUID pre-check + league lookup (`league_not_found`), positive-integer pick/round, opaque player ID (`"0"` sentinel rejected), catalog existence, roster membership in the league, server-side duplicate-player rejection (Nick-signed — application-level by design; a unique constraint would fight the poller's authoritative ingestion), non-negative `amount` when supplied (persisted as given — null/unused for Sleeper snake; 03c auction signature stability), and round↔pick consistency (`round === ceil(pick/league_size)` when `derived_config.league_size` parses; validation-only per the draft-endpoint snake-math rule — no absolute pick upper bound exists because total rounds isn't persisted anywhere, declared decline-to-invent). Any unclaimed pick number is accepted (Nick-signed — enables mid-draft backfill; the UI defaults to next pick). Insert = upsert `ignoreDuplicates` on `(league_id, pick_number)` → `accepted` (with the written row), or `conflict` carrying the authoritative winning row (a poller won that pick — never retried, never overwritten), or typed `validation_error` with a reason code. `platform`/`season_year` copied from the leagues row; `source='manual'`, `native_draft_id` null.
- `undoLastManualPick(db, leagueId): Promise<UndoManualPickResult>` — deletes only the single highest-`pick_number` row where `source='manual'` (build-file semantics); the delete is keyed by PK AND `source='manual'`, so `sleeper_poll` rows are undeletable by construction. `no_manual_picks` when none remain; a read-delete race degrades to `no_manual_picks`, never an error.
- `listDraftPicks(db, leagueId): Promise<RecordedPick[]>` (client-side live sync fold, 2026-07-22) — the league's full `draft_state` snapshot, `pick_number` ascending: the live-sync per-tick payload and the board's initial server-rendered pick state (full-snapshot-over-incremental Nick-signed — an undone pick's deleted row self-heals on the next tick). Malformed IDs → empty snapshot (listScoredWeeks precedent); genuine query errors throw. RLS-walled like everything else: anon/non-admin clients read zero rows (live-verified).
- `RecordedPick` (UI extensions item 4, 2026-07-22, Nick-signed) — every read/write path (accepted/conflict/undone/snapshot rows) additionally carries `playerFullName`/`playerPosition`, joined from the players catalog via the `draft_state→players` FK embed (`PICK_SELECT`) — never from pick metadata (a point-in-time snapshot per the wiki). DST rows have no `full_name`, so the name composes from the split first/last name fields (sleeper-api/dst-and-free-agents); live-verified 170/170 named on the real league incl. its 10 drafted defenses.
- Consumed by the auth-gated server actions at `app/(admin)/leagues/[leagueId]/draft/actions.ts` (`submitManualPick`/`undoManualPick`): gate INSIDE each action via `getAdminAuthState` (server actions are POST endpoints — the layout gate doesn't cover invocations), typed `unauthorized` with zero data, writes through the RLS server client as the signed-in admin (never the secret-key client), `revalidatePath` of the draft route on success. Live-verified 2026-07-22: 21/21 harness checks against the real league (every validation reason; conflict vs poller rows; accepted+undo round-trip restoring baseline; anon publishable-key client → `league_not_found` zero-data through the mutation AND direct-insert RLS rejection). Since UI extensions item 4, `undoManualPick`'s one UI caller is the shell's `handleUndo` behind the recent-picks feed's Undo button (session-active only, single-row affordance mirroring the server's highest-manual-pick semantics).

**`services/draft-sessions.ts`** (Wave 3b active-draft polling orchestration, 2026-07-22 — admin surface only, never spectator; the read/write surface for `draft_sessions`)
- `getDraftSessionState(db, leagueId): Promise<DraftSessionStateResult>` — TTL-aware session read: `isDraftActive` is the stored flag AND `activated_at` within the 6-hour soft TTL (`DRAFT_ACTIVE_TTL_HOURS`, Nick-signed — read-time computation, no job mutates the row); `isStale` distinguishes "expired" from "explicitly stopped". No row = valid inactive state. Malformed/unknown league → typed `league_not_found`.
- `activateDraftSession(db, leagueId)` — upsert flag on + reset the TTL clock, then one inline recorded draft poll (Nick-signed: the board starts from fresh authoritative picks; a sync failure is reported in `initialSync` and recorded in `sync_runs` but never rolls back the flag). Non-Sleeper leagues → `skipped_non_sleeper` (03c extends).
- `deactivateDraftSession(db, leagueId)` — idempotent stop; stamps `deactivated_at`; no row = already inactive.
- `runActiveDraftPoll(db, leagueId): Promise<ActiveDraftPollResult>` — one elevated poll tick, the server side of the client-driven trigger (Nick-signed 2026-07-22: Hobby crons are daily-only, so the open admin board drives the 5s cadence). The active check is authoritative HERE and TTL-aware — a declined tick (`inactive`) makes no Sleeper request and records nothing (not a poll run). Every executed poll runs `syncLeagueDraftState` and records a `sync_runs` `draft_poll` row, success or failure (Nick-signed: every run as specced, ~2k small rows per draft day accepted). The success outcome carries `draftOrder` (the sync result's `DraftOrderMeta`) through to the client's on-clock projection. Normal-cadence fallback = the daily league-state cron chain — nothing extra runs when no draft is live.
- Consumed by the draft route's server actions `startDraftSession`/`endDraftSession`/`pollActiveDraft` (same in-action auth gate + RLS pattern as the manual-pick actions; since the client-side live sync fold, `pollActiveDraft` attaches the full `listDraftPicks` snapshot to every executed poll and deliberately does NOT revalidate on picks — the client merge is the live path, Nick-signed) and by two board components: `DraftSessionToggle` (toolbar Start/End control, teal live indicator, stale = "Session expired") and `DraftPollTicker` (renders nothing; 5s interval while mounted + active + tab visible, in-flight guard, instant catch-up tick on visibility return, delivers snapshots via `onPicks`, tick outcomes via `onStatus`, and order metadata via `onDraftOrder`, stands down on any non-`polled` outcome; the shell's fingerprint-deduped state + `live-picks.ts` merge consume the snapshots — the client-side live sync sub-section is complete 5/5 as of 2026-07-22). Live-verified 37/37 against the real league (TTL lapse/reset, declined-vs-recorded ticks, check constraint, anon zero-data at both walls) + 24/24 for the live-sync fold + 22/22 for the live-strip fold (projection matched all 170 recorded picks).

**`services/bpa/`** (Wave 3b BPA recommendation engine + tier-cliff detection, 2026-07-22 — the VORP calculation + recommendation layer. The pure sub-layer (scoring/replacement/calibration/base-value/board/need/tiers) is deterministic — points desc → `sleeper_player_id` asc tie-breaks module-wide — which is what lets the whole chain recompute against the shrinking undrafted pool after every pick; `recommendations.ts` is the one I/O home (item 6). Admin draft surface only, never spectator (STATE.yml Rule-13 warning: `player_projections`/`adp_rankings` have no spectator consumer). Layout parsing and format resolution are NOT re-derived here — `parseRosterSlotLayout` and `resolveAdpScoringFormat` (services/draft-board.ts) remain the single homes.)
- `parseScoringWeights(raw)` / `scoreProjectionStats(stats, weights)` (`scoring.ts`) — THE single projection-scoring home (STATE.yml centralization mandate realized): league weights from `scoring_settings_raw` (shape-tolerant, non-numeric dropped, `adp_*`/`pts_*` key families structurally excluded per the decision-of-record canon), points = Σ weight × stat over the intersecting key namespace (empirically confirmed live: 22/38 of the real league's scoring keys intersect the projection stats), absent stats contribute zero at calc time only (league-endpoint rule), DST through the league's own `def_*`/`st_*` keys. Scores PROJECTED value only — actual `player_scores` stay ingested platform-scored, never computed.
- `computeReplacementLevels(pool, layout, leagueSize)` (`replacement.ts`) — item 1's pure structural replacement level per position: greedy league-wide fill (dedicated slots consume each position's top projected players, then flex slots — narrowest eligibility first — consume the best remaining across eligible positions; Nick-signed, resolving the VBD wiki page's open pooling question); replacement = last player consumed per position. Startable-only demand (bench depth enters via ADP calibration, Nick-signed). Flex eligibility: FLEX/SUPER_FLEX wiki-grounded; REC_FLEX/WRRB_FLEX/IDP_FLEX general-knowledge under declared silence; unknown flex labels excluded from demand + surfaced (`unknownFlexLabels`). Exhaustion floors at 0 with a flag, never a guess.
- `calibrateReplacementLevels(structural, pool, adp)` (`calibration.ts`) — item 2's ADP-window depth calibration (Nick-signed): depth_p = position-p players with `adp_overall` ≤ the structural startable window; replacement = that depth-th player by projection order (market sets depth, projections set value). Calibrated REPLACES structural when computable; structural always carried alongside for visible reasoning. Flagged fallbacks: league-level `structural_only` (unresolved format/empty snapshot — never cross-format) and position-level zero-carrier fallback (thin K/DEF markets keep structural depth). ADP input comes from sentinel-filtered `adp_rankings` only — never `player_projections.stats`.
- `computeBaseValues(pool, levels)` (`base-value.ts`) — item 3's base value/VORP: projection − operative replacement line per pool player, ranked baseValue desc; negative values kept (below-replacement is data); players at positions the league can't start are excluded + surfaced (`unrankableCount`/`unrankablePositions` — unrankable, never zero-baseline). Each row carries its replacement line + source (`adp`/`structural`) for the item-7 panel's separated-signals display.
- `computeBpaBoard(pool, layout, leagueSize, adp)` (`board.ts`, item 4 — dynamic recompute) — THE one composition of the chain (replacement → calibration → base value → tier-cliff detection), pure, no cached baseline. The shrinking pool is realized by the CALLER passing the current undrafted set, so replacement is always the last startable among those still available (value-based-drafting.md dynamic-recalc Key Decision). Recompute model (Nick-signed, disclosed): full league-wide demand re-run against the shrinking pool — does not subtract already-filled league slots (negligible with the ~3.3k pool; possible future refinement). Returns `{ baseValues, replacementLevels, tiers }` — tiers recompute here off the same base-value ranking on the same per-pick re-call, no separate cache/driver.
- `computePositionTiers(players)` / `summarizeTiers(board)` (`tiers.ts`, tier-cliff detection items 1–3, 2026-07-22) — pure per-position tier boundaries from gap detection over the base-value ranked pool. Tiers ONLY players at/above each position's operative replacement line (Nick's Clarify — down-to-replacement depth scope; full-tail tiering manufactures noise per league-size-scarcity-effects.md). A break sits where the consecutive projected-points gap exceeds `TIER_GAP_MULTIPLE[position] × the position's MEDIAN gap` (Nick's Clarify: multiple-of-typical-gap not absolute — adp-ecr-differential.md; median over mean is a live-verified robustness fix so one dominant elite-separation gap can't swallow QB/TE secondary cliffs; mean fallback when median is 0; within-position projection gaps ≡ base-VORP gaps → no z-score). Multiples (declared wiki silence — league-size-scarcity-effects.md refuses specific thresholds → live-tuned constants): RB 3.0 / WR 3.2 / QB 2.0 / TE 1.5 / K,DEF 2.0 / default 2.5; deep positions carry the higher multiple (dense tiny-median tail) yet a smaller ABSOLUTE point threshold = the build-file's "tighter for RB/WR" sensitivity. Live-verified on the real 2026 PPR pool: QB 3 / TE 4 (target 3-5), RB 8 / WR 9 (target 6-9), all in band; counts are a sanity check, never enforced (Nick's Clarify; wiki rejects rigid enforcement). A `<3`-tiered-player position collapses to one tier (relative-gap degenerate, documented, harmless). A parallel display signal — NEVER an input to base value or ordering. `summarizeTiers` derives the per-position `{ tierCount, topTierSize, topTierTopProjection }` the board counter renders (item 4 — the `TierDepthStrip` in bpa-recommendations-panel.tsx, shipped 2026-07-22).
- `computeRosterNeed(rosterPositions, layout)` / `classifyPositionNeed(signal, position)` (`need.ts`, item 5 — independent roster-need signal) — pure per-team greedy fill (dedicated then flex pooled via replacement.ts's exported `FLEX_ELIGIBILITY` single home). Bench is GENERIC (Sleeper BN shared pool per roster-construction-starting-lineups.md — not per-position): per-position `starterSlotsRemaining` + one roster-wide `benchSlotsRemaining`. Need kinds (Nick's Clarify): `starter` (open dedicated/flex slot eligible), `bench` (starters full, bench room), `full` (no starter slot AND no bench room — per-position caps deferred to auto-pick). A SEPARATE signal — item 6 attaches it as a parallel annotation, NEVER a value input (the sub-section's hard constraint). IR/taxi out of the draft-need model.
- `getBpaRecommendations(db, leagueId, { topN?, selfRosterId? })` (`recommendations.ts`, item 6 — the first I/O in bpa/) — top-N candidates ranked purely by base VORP against the current undrafted pool. Loads config (weights/layout/format/size), paginated projections (`.range` loop — the season snapshot exceeds PostgREST's page cap; 3292 rows), format ADP carriers, and `listDraftPicks` once (→ drafted set for the shrinking pool + self-roster positions for need); shrinks pool, runs `computeBpaBoard`, slices top-N (default 10 — Nick's Clarify), annotates each row with `classifyPositionNeed`. `selfRosterId` optional (Nick's Clarify — no "my team" marker in schema; need null when absent, item-7 panel supplies it). Typed reasons `league_not_found`/`config_unusable`/`projections_unavailable`; ADP absence is NOT a failure (structural_only). Boundary discipline (draft-board precedent): explicit columns, platform-agnostic, admin-only (no spectator consumer). Consumed by the `fetchBpaRecommendations` server action (draft/actions.ts — same in-action `getAdminAuthState` + RLS-server-client gate as the other 5 actions; a read, so no revalidate; the item-7 panel re-invokes it per pick, item 9). Tier-cliff surfacing (tier-cliff items 1–3): each `BpaRecommendation` carries its within-position `tier` (null below the replacement line) and `context.tiers` carries whole-board per-position tier summaries — recomputed every call against the shrinking pool, the data item 4's live counter renders.
- Verified 2026-07-22: BPA items 1–3 = 62/62 pure + live full-chain; items 4–6 = 39/39; items 7–9 = the recommendations panel (tsc+build, live read-only data contract). Tier-cliff items 1–3 = 11/11 pure edge tests (empty/single/2-player-degenerate/huge-gap-split/median-robustness/determinism/multi-position) + live read-only on the real league (target bands hit on the full pool; full-vs-170-drafted tier structure diverges = dynamic recompute proven; ordering invariant vs value-only). Tier-cliff item 4 (2026-07-22) = the `TierDepthStrip` per-position counter in the BPA panel (client UI over the already-surfaced `context.tiers`; tsc/lint/build clean + live read-only harness confirming topTierSize populated for QB/RB/WR/TE on the real 170-drafted pool — numbers diverge from the full-pool bands = counter data rides the per-pick recompute). BPA sub-section 9/9; tier-cliff sub-section COMPLETE 4/4. READ-ONLY sessions, no migration.
- `detectPositionalRuns(picks, { windowSize? })` (`runs.ts`, positional-run detection items 1/2/4, 2026-07-23) — pure, deterministic, ZERO imports (fully standalone). Flags an active positional run when same-position selections in a fixed sliding window of the last `DEFAULT_RUN_WINDOW_SIZE` (=6, Nick's Clarify) picks by `pick_number` meet a per-position count: `RUN_COUNT_THRESHOLD` QB 2 / TE 2 (shallow) / RB 3 / WR 3 (deep). Presence in that map IS the run-detectable set — K/DEF and any other position are absent (never flagged; still occupy window slots), same position scope the tier counter omits (Nick's Clarify). Null-position picks occupy a slot but contribute no count; undo self-heals (removed row leaves the window on the next call). Consumes a minimal `RunPick {pickNumber, position}` — decoupled from the pick-write path's RecordedPick (callers map; DraftablePlayer minimal-shape precedent). Returns `{ windowSize, windowPickCount, windowStartPick, windowEndPick, byPosition, activeRuns }` (activeRuns ordered count desc then position asc). COUNT-ONLY by design (Nick's Clarify): the wiki's tier-crossing "does this run matter" refinement (in-season-management/positional-run-detection-draft-day.md — raw counts over-fire on parallel roster-building) is composed at the item-3 badge (next fold) by pairing this flag with `summarizeTiers` topTierSize, keeping this a standalone signal. Window/counts are DECLARED WIKI SILENCE (that page's Open Question #1 — tunable parameter pending backtesting → live-tuned platform constants, like tiers.ts's multiples). SUPERFLEX/2QB format sensitivity DEFERRED (Nick's Clarify — no superflex league connected; the page's Key Decision mandates it → future refinement, flagged not silently filled). ITEM 4 — informational-only invariant, verified structurally: `runs.ts` imports nothing from the value pipeline (scoring/replacement/calibration/base-value/board) and nothing imports it (grep-confirmed both directions); a parallel display signal like need.ts/tiers.ts, NEVER an input to VORP/ordering — the shrinking-pool recompute in board.ts captures a run's replacement-level effect, this flag never feeds it. No `server-only` dep → the item-3 client badge calls it directly on the shell's livePicks, not through fetchBpaRecommendations. Verified 26/26 synthetic edge cases + live read-only on the real 170-pick 2025 draft (32 plausible run-activation events — RB run at pick 4, WR at 8, QB at 31 on 2-in-window; tsc/lint/build clean; READ-ONLY, no migration). Positional-run sub-section COMPLETE 4/4 — item-3 badge shipped 2026-07-23 (see the draft-board component entry below).

  UI consumer (item 3, 2026-07-23): `components/draft-board/positional-run-badges.tsx` `PositionalRunBadges` — a non-blocking inline badge rendered in `BoardToolbar` right after the position-filter chip group. THE one run-detection consumer: the shell (`draft-board-shell.tsx`) computes `detectPositionalRuns` in a `useMemo` over its `livePicks` snapshot (RecordedPick→RunPick via `playerPosition`; confirmed picks only) and passes the `RunBoard` down through `PlayerBoard`→`BoardToolbar`. Nick's Clarify pairs the run flag with top-tier depth: `BpaRecommendationsPanel` gained an optional `onTiers` callback reporting its already-computed `context.tiers` (summarizeTiers) up to the shell (state `panelTiers`) — the SAME fetch, no second tier compute — which flows to the badge as `runTiers`; the tooltip reads "N POS in the last W picks · M left in the best available tier", degrading to run-only (tier clause omitted) when `panelTiers` is null (panel unmounted below `lg`). Appear-only, count-desc→position-asc order, muted pill reusing `PositionBadge`, no new token. NOT routed through `fetchBpaRecommendations` — run detection stays decoupled from the value path (item 4). Verified 18/18 static-render harness (deleted) + tsc/lint/build clean; READ-ONLY, no migration.

**`components/draft-board/draft-queue.ts` + queue/auto-pick UI (Draft queue and auto-pick sub-section COMPLETE 4/4, 2026-07-23 — NAMED SINGLETON fold #3)** — a pure client layer + a thin localStorage store; the last non-resilience 03b sub-section. READ-ONLY to the DB: the only write is the EXISTING `recordManualPick` path auto-pick reuses.
- Queue storage (item 1, Nick's Clarify — CLIENT-SIDE per league, no shared-DB migration): pure ops `addToQueue`/`removeFromQueue`/`toggleQueue`/`moveInQueue` (reorder is the ONLY order change, always user-driven — never engine-driven, the item-1 hard rule) + `loadQueue`/`saveQueue` (client-only, SSR/error → empty) + a `useSyncExternalStore` adapter (`getQueueSnapshot`/`getQueueServerSnapshot`/`subscribeQueue`/`setLeagueQueue`) — the endorsed hydration-safe read (getServerSnapshot empty ⇒ SSR/hydration match, adopt stored queue after, NO setState-in-effect; `setLeagueQueue` is THE one writer). UI: shared `queue-toggle.tsx` `QueueToggle` star on the main player rows AND the BPA recommendation rows (both build the ONE per-league queue); `draft-queue-panel.tsx` `DraftQueuePanel` sidebar (below the BPA panel) with up/down reorder + remove. `selfRosterId` LIFTED from `BpaRecommendationsPanel` to the shell (controlled props) so one "my team" picker feeds both the need signal and the auto-pick caps.
- Auto-remove/promote/notify (item 2): a shell effect keyed on the confirmed snapshot (`livePicks` → `draftedIds`) runs `pruneDraftedFromQueue` — a drafted queued player drops off (promotion implicit — the rest slide up) and a sonner toast names it; silent on the first pass, toasts on every pick after. ONE snapshot source — no second pick-event feed.
- Roster-aware auto-pick (item 3, Nick's Clarify — per-position caps + fall-through; armed toggle + away-triggered): `positionCap(pos, layout)` = dedicated starters + flex eligibility (reads `FLEX_ELIGIBILITY` from replacement.ts, the one home) + a per-position bench margin (`AUTO_PICK_BENCH_MARGIN` QB 1 / RB 4 / WR 4 / TE 2 / K 0 / DEF 0, default 3 — DECLARED WIKI SILENCE per roster-construction-starting-lineups.md, live-tuned like tiers/runs constants; K/DEF hard-capped at 1, QB at 2 in 1QB [the build-file 3rd-QB example], superflex lifts QB for free). `selectAutoPickCandidate` walks draftable queue entries, skips any at cap, returns the first under cap; if all are capped, falls through to the top draftable one (`overCapFallThrough` — never fail to pick); null position / null layout → take the top (honest degradation). Trigger (shell): an armed toggle (off by default) fires when `projectOnClock(nextPickNumber, draftOrder) === selfRoster` AND away — hidden tab immediate, else a 12s dwell (`AUTO_PICK_DWELL_MS`, local proxy for the clock winding down, declared trigger-silence); `autoPickAttemptRef` = one attempt per pick number (no retry loop). Fires through the shell's SAME `handleDraft` → `recordManualPick` (source='manual', undoable, first-write-wins protects a real Sleeper draft — a poller-won pick makes the attempt conflict + roll back, never desyncing). NEVER a Sleeper write (build-file mandate + MASTER_CONTEXT read-only rule).
- Consume via existing live sync (item 4): the prune reads the shell's `livePicks` (existing `DraftPollTicker`/`pollActiveDraft` path), auto-pick reads availability from the ONE client merge (`mergedPlayers` → `availableIds`) + roster shape from `livePicks`, writes via `handleDraft`. No second poll driver, no draft-API/WebSocket, no Sleeper read, single Sleeper league.
- Verified: tsc/lint/build clean + 32/32 pure harness (deleted) — caps (incl. superflex), ops, prune promotion, selection (fresh / 3rd-QB-skip / all-capped-fall-through / drafted-skip / unknown-pos / null-layout / K-cap). Timing trigger + UI wiring tsc/build-verified, reusing live seams (no live draft running to drive headlessly). 03b now: only the Resilience sub-section (2 items) remains.

**Owner auth surface (Wave 3a route + shell sub-section, 2026-07-22)** — three walls, no single point of failure: (1) `src/proxy.ts` refreshes the Supabase session per matched request and 307s unauthenticated `/leagues/*` hits to `/login` (matcher excludes `/api` — the cron Bearer surface never sees cookie plumbing); (2) the `(admin)` layout gate calls `getAdminAuthState` (`src/lib/supabase/auth.ts` — `is_fantasy_admin` RPC, the single DB-side rotation point; a signed-in non-fantasy prolabel user gets the zero-data `NotAuthorizedCard`, children never render); (3) per-table RLS pinned to the admin `auth.uid()`. Sign-in is `signInWithPassword` in a server action (`app/login/actions.ts`); no signup flow exists by decision. First real sign-in verified live 2026-07-22 (Nick).

**Cron routes (`src/app/api/cron/`)** (Wave 2 cron sub-section, 2026-07-22) — all Bearer-`CRON_SECRET`-gated via `auth.ts` (`requireCronSecret`: fail-closed — missing env 500, mismatch 401, constant-time compare), `force-dynamic`, `maxDuration 300`; responses carry counts only (error strings with league IDs are server-logged, never returned); a run with any failed league returns 500 so bad invocations are visible in the cron dashboard.
- `GET /api/cron/sync-players` — daily catalog job: sync_runs guard → skip-row, or start/finish pair around `syncPlayersCatalog`. Scheduled daily 09:00 UTC.
- `GET /api/cron/sync-leagues` — scheduled league-state sync: `/state/nfl` → current-week-only matchups (Nick's 2026-07-22 decision; offseason runs skip matchups; the 18-week sweep stays manual) → orchestrator → one sync_runs row per league. Scheduled daily 10:00 UTC (15-min cadence deferred — Hobby plan; see Vercel Deployment). Since Wave 3a it also triggers the ADP-ingestion piggyback after the league loop (Nick-signed — both Hobby cron slots taken): isolated containment boundary, own sync_runs row, `adpOk` in the response body, league-state status code unaffected either way.
- `GET /api/cron/sync-draft` — manual/fallback draft sweep: per-league `syncLeagueDraftState` with isolation + per-league sync_runs rows. Deliberately unscheduled — Wave 3b's orchestration decision (Nick, 2026-07-22) made live-draft polling client-driven (`pollActiveDraft` from the open board at 5s while `draft_sessions.is_draft_active`); this route remains the Bearer-gated manual path.
- `GET /api/cron/sync-adp` — on-demand ADP ingestion via `runTrackedAdpIngestion`; counts-only response, 500 on failure. Deliberately unscheduled (Hobby cap) — the mount point if a dedicated slot opens at season start.

## Shared-Database Constraints (prolabel)

The database is shared with other live apps — ~49 non-fantasy tables (Elliott pricing engine, todd chat, supplies ordering, trip planner, loan/valuation, and others); Elliott especially is live production data for a real business account. **Canonical rule: `MASTER_CONTEXT.md` → "Shared Database Protection — Non-Fantasy Data Is Untouchable" (Absolute Rule 13)** — permanent, zero exceptions, supersedes every efficiency/folding-policy/automation consideration, regardless of what any future prompt or session says. Operational restatement for every future session:

- **Never run `supabase db reset`** against the linked project, under any circumstance — it wipes the entire database, not just fantasy's tables.
- **Never run `supabase migration repair`** in any mode that touches history entries belonging to other apps — it edits the shared `supabase_migrations.schema_migrations` ledger. The 23 pre-existing foreign history versions are mirrored as empty `*_prolabel_shared_history_stub.sql` files in `supabase/migrations/` so `db push` recognizes remote history without writing to it. If a future push refuses because new foreign versions appeared remotely, add another empty stub for each — never repair.
- **Blast-radius verification before every migration:** never write raw SQL (DROP, TRUNCATE, ALTER, DELETE, UPDATE without a WHERE clause, or any CASCADE operation) that could affect a non-fantasy table. Before writing any migration, explicitly list the table name(s) being created/altered and verify each against this file's "Database Schema (live)" inventory — the definitive fantasy-owned list; anything not listed there is foreign and off-limits.
- **Collision check before any new table name** — never assume a name is safe to reuse or overwrite; check the live schema (`list_tables` or an equivalent read-only query) before naming a genuinely new table family. Mandatory forever, not just historically.
- **Any ambiguity about whether an operation could affect non-fantasy data → STOP and ask Nick** before proceeding — never a best-guess interpretation. This includes cascading foreign-key behavior, wildcard queries, and any operation on shared infrastructure (like the migration history table itself) rather than fantasy's own tables.
- Owner-write RLS policies must match Nick's specific `auth.uid()` (via `is_fantasy_admin()`), never the blanket `authenticated` role — the auth namespace contains other apps' users.
- **Every session that touches the live database reports its blast radius** — the mandatory `BLAST RADIUS:` completion-report line (defined in `COMPLETION_TEMPLATES.md`): "Blast radius confirmed: only fantasy-owned tables were touched this session", or an explicit account of what else was touched and why, with Nick's prior sign-off cited.

## Vercel Deployment

- **Project:** `fantasy` (`prj_SaeX3FOGNaO9X2AhQaNkOoKHippO`) on team "Nick's projects" (`team_qugPBspyTKnjNX5bHNAWwkCD`) — created manually 2026-07-21, connected to GitHub `tacctile/fantasy`, framework preset `nextjs`, Node 24.x, region `iad1`.
- **Auto-deploy:** every push to `main` builds and deploys to production (`source: git`) — verified live 2026-07-22: the env-contract push triggered a production deployment that reached READY (~23s build). No `.vercel/` directory in the repo — the link lives in Vercel's GitHub integration, not local files.
- **Domains:** `fantasy-virid-three.vercel.app` (primary), `fantasy-nicks-projects-e170820e.vercel.app`, `fantasy-git-main-nicks-projects-e170820e.vercel.app` (branch alias).
- **Env vars:** `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, `SUPABASE_SECRET_KEY` set for Production + Preview (manually, 2026-07-21). Functionally proven 2026-07-22: deployed production `/api/health` (secret-key authenticated read) returned 200 `{"ok":true}` — all three values correct in the Production environment.
- **Health check:** `GET https://fantasy-virid-three.vercel.app/api/health` — public, no data returned, 200 `{ ok: true }` on success / 500 `{ ok: false }` on DB failure (real error server-logged only). The standing deployed-environment liveness probe.
- **Cron jobs (2026-07-22):** two daily crons from `vercel.json` — `/api/cron/sync-players` `0 9 * * *`, `/api/cron/sync-leagues` `0 10 * * *` — registration confirmed in the dashboard. **Plan constraint:** the project is on Hobby (max 2 crons, once daily each, hour-imprecise timing); the build file's 15-minute league-state cadence needs Pro and is deferred to season start by Nick's decision (see `MANUAL_SETUP_CHECKLIST.md` decision of record). Vercel invokes cron paths with `Authorization: Bearer ${CRON_SECRET}` (env var set in Production). Sub-daily schedules fail deployment on Hobby — a plan-invalid `vercel.json` can't ship silently.

## Environment Variables

Live values in gitignored `.env.local`; the committed `.env.example` documents the contract (created 2026-07-22 — placeholders only, never real secrets).

| Variable | Exposure | Purpose |
| -------- | -------- | ------- |
| `NEXT_PUBLIC_SUPABASE_URL` | client + server | Supabase project API URL |
| `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` | client + server | modern publishable key (replaces legacy anon JWT) |
| `SUPABASE_SECRET_KEY` | server only — never `NEXT_PUBLIC_` | modern secret key (replaces legacy service_role JWT) |
| `SUPABASE_DB_PASSWORD` | CLI only — never shipped, never set in Vercel | database password for `supabase db push` (read by the Supabase CLI) |
| `CRON_SECRET` | server only — set in Vercel Production (2026-07-22) | random 64-hex shared secret gating `/api/cron/*`; Vercel Cron sends it as `Authorization: Bearer` on scheduled invocations; routes fail closed if unset |
| `ESPN_S2` | server only — Wave 2 consumer | ESPN `espn_s2` cookie for private leagues; stored exactly as extracted, one layer of URL-encoding, never re-encoded (`espn-api/format-requirements`) |
| `ESPN_SWID` | server only — Wave 2 consumer | ESPN `SWID` cookie for private leagues; keeps its literal curly braces (`espn-api/format-requirements`) |

## Supabase Migration Rule

Never edit the baseline migration. All schema changes go through `supabase migration new`. This is a hard operational constraint, not a style preference — applies from Wave 1 onward.

---

_End of ARCHITECTURE.md_
