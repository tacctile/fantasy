# 01_foundation.md
**Wave 1 — Foundation**
**Status:** 🟡 In progress
**Registered:** 2026-07-21

---

## Scope

Supabase schema (with `league_id`, `platform`, `season_year`, Sleeper-anchored player identity, `league_config`, `share_token` per `MASTER_CONTEXT.md` Schema Rules), env/secrets setup, initial Vercel deploy. Nothing beyond this — no ingestion logic, no ESPN cookie auth flow, no RLS spectator-read policies (those are Wave 4's concern once the spectator surface actually exists). This wave proves the schema is correct and the app deploys; it does not populate real data.

This build file was scoped by convergence-filtering 6 independent AI model responses (chathub-style panel) against the project's actual `BUILD_INDEX.md` Wave 1 bullet, per the same methodology used for `wiki/` discovery ingestion. Full panel responses are not retained here — this file is the synthesized, project-scoped result.

---

## WIKI PAGES TO CONSULT

Read these before starting, per Session-Start Protocol (max 3 unless the task genuinely needs more):

- `wiki/topics/schema-reference/league-identity-and-scoping.md` — `platform_league_uuid` primary-key pattern, `previous_platform_league_uuid` continuity
- `wiki/topics/schema-reference/player-identity-mapping.md` — Sleeper-anchored crosswalk table shape and population order
- `wiki/topics/schema-reference/league-configuration-data-model.md` — `league_config` raw + derived column shape

---

## Checklist

### Project scaffold
- [x] Initialize Next.js (App Router, TypeScript strict, `src/` directory) via `create-next-app`
- [x] Configure `tsconfig.json` so `@/*` resolves to `src/*` — verify with one trivial cross-directory import
- [x] Install and initialize shadcn/ui; configure `components.json` for CSS-variable theming
- [x] Configure `globals.css` with shadcn token set (`--background`, `--foreground`, `--radius`, `--card`, `--border`, etc.) — zero inline hex anywhere
- [x] Install `lucide-react` as the sole icon library
- [x] Confirm Tailwind default spacing scale only (no arbitrary pixel values) and `font-variant-numeric: tabular-nums` available for data displays

### Supabase project + migration workflow
- [x] Create the Supabase project; capture `SUPABASE_URL`, anon key, service-role key — project `tszssadgsxjoymcttlwd` pre-existed per MANUAL_SETUP_CHECKLIST; URL + publishable key pulled via Supabase connector, secret key handed over by Nick; all three captured in gitignored `.env.local`. Standardized on the modern key pair (`sb_publishable_...` as `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, `sb_secret_...` as `SUPABASE_SECRET_KEY`) rather than legacy anon/service_role JWTs, per Nick's Clarify-step decision
- [x] Install Supabase CLI, `supabase login`, `supabase link` to the project — CLI 2.109.1 as npm dev dependency (version pinned in `package.json`, invoked via `npx supabase`); Nick ran the interactive `supabase login`; linked to `tszssadgsxjoymcttlwd` (db password deliberately skipped at link — needed later at first `supabase db push`, tracked in MANUAL_SETUP_CHECKLIST)
- [x] `supabase init` to establish the local `supabase/migrations/` directory as the canonical schema-change path — `supabase/config.toml` committed; `migrations/` appears with the first migration
- [x] Confirm the baseline-migration rule is understood: never edit the baseline directly, always `supabase migration new` for every subsequent change (this applies from Wave 1 onward, permanently) — confirmed; no migrations exist yet, so the first schema item creates the baseline via `supabase migration new` and every change after that gets its own new migration file

### Schema — platform + identity foundation
- [x] Migration: `platform` Postgres enum type, values `sleeper` and `espn` (extensible enum, never a boolean) — `20260722035454_create_platform_enum.sql`, applied to live DB and verified 2026-07-21
- [x] Migration: `players` table, `sleeper_player_id` (text) as primary key, plus name/position/team/status/metadata columns — this is the Sleeper-anchored canonical identity store, per `schema-reference/player-identity-mapping.md` — `20260722035457_create_players_table.sql`, applied live 2026-07-21; RLS enabled at creation (deny-by-default; auth user + owner-write policies remain the integrity-section item's scope); shared `set_updated_at()` trigger function created here. Corrected same day by `20260722042128_add_players_sleeper_reference_fields.sql` after verification against `sleeper-api/players-endpoint` + `sleeper-api/player-data-quirks`: `fantasy_positions text[]`, `search_rank`, `injury_status`, `search_full_name`, `birth_date` promoted to first-class columns per wiki-assigned roles (relevance-filter quartet; status/injury_status two-field availability rule; last-resort name-match key)
- [x] Migration: `player_id_crosswalk` table keyed on `sleeper_player_id`, with nullable string columns `espn_player_id`, `gsis_id`, `yahoo_id`, `sportradar_id`, `pfr_id` (all stored/compared as strings, never numeric — Sleeper D/ST abbreviations and dash-formatted GSIS IDs would be corrupted by integer casting) — unique constraint on `espn_player_id` where not null. D/ST entities get their own crosswalk row anchored on Sleeper's team-abbreviation-style `player_id` (e.g. `"DET"`), with `espn_player_id` populated from ESPN's separate D/ST `playerId` — never derived from or compared against `proTeamId`. This is a schema-only migration in Wave 1; the population/reconciliation pipeline (Sleeper direct → nflverse/DynastyProcess crosswalk → local ESPN reconciliation pass → last-resort flagged name matching) is Wave 2's concern, per `schema-reference/player-identity-mapping.md`. — `20260722035500_create_player_id_crosswalk.sql`, applied live 2026-07-21; partial unique index on `espn_player_id` where not null, FK → `players` on delete cascade, RLS enabled at creation

### Schema — league + config
- [x] Migration: `leagues` table — `platform_league_uuid` (internally-generated UUID) as primary key, `platform` enum, `season_year`, `native_league_id` (text — Sleeper's `league_id` string or ESPN's `leagueId` integer, stored as-received, never used as a join key), `previous_platform_league_uuid` (nullable self-reference, populated by walking Sleeper's `previous_league_id` chain at ingestion and left null for ESPN), display name, `share_token` (text, unique, not null), owner reference, timestamps. Every other table's `league_id` column in this and later waves is a foreign key to `leagues.platform_league_uuid` — that FK is what "league_id" means everywhere in this project's schema rules and build files. No fixed league-count assumption anywhere in schema or code. Per `schema-reference/league-identity-and-scoping.md`. — `20260722044457_create_leagues_table.sql`, applied live 2026-07-21 (folded with the share_token generation item per Nick's approval). Display name column is `name` (Nick's Clarify decision, matches Sleeper's top-level field); `owner_id uuid` nullable FK → `auth.users(id)` on delete set null, backfilled when the integrity item creates the admin user (Nick's Clarify decision); unique `(platform, native_league_id, season_year)` per the ADR's one-row-per-platform-season-league rule; RLS enabled at creation; `set_updated_at` trigger attached
- [x] Migration: `share_token` generation — Postgres function using `pgcrypto`/`gen_random_bytes` (or equivalent) producing a cryptographically random, unguessable token; trigger or column default so every league row gets one automatically at creation. Must be revocable/regeneratable later (Wave 4 concern for the regenerate UI, but the schema must not prevent it — no immutability constraint on this column beyond not-null+unique). — `20260722044500_create_share_token_generation.sql`, applied live 2026-07-21; `public.generate_share_token()` returns 32 random bytes hex-encoded via `extensions.gen_random_bytes` (pgcrypto v1.3 pre-installed on the shared project — no create extension), set as the column default; verified live: 64-char hex, distinct per call
- [x] Migration: `league_config` table, keyed by `league_id` (FK to `leagues.platform_league_uuid`), storing scoring settings and roster slots as JSONB ("raw" provider-native shape), plus a small derived/normalized subset (PPR value, TE-premium flag, superflex flag, slot counts) — per `schema-reference/league-configuration-data-model.md`. Never hardcode scoring/roster assumptions in application code. — `20260722045532_create_league_config_table.sql`, applied live 2026-07-21. `league_id uuid` doubles as PK (1:1 with leagues; the ADR's "keyed by platform_league_uuid"), FK on delete cascade (Nick's Clarify decision); no `platform`/`season_year` duplication — ADR-exact three-column shape per Nick's Clarify decision, the 1:1 parent leagues row already carries both; `scoring_settings_raw`/`roster_settings_raw`/`derived_config` jsonb all not null (Nick's Clarify decision); timestamps + `set_updated_at` trigger; RLS enabled at creation. Verified live: columns/nullability, PK+FK-cascade, RLS, trigger, migration history row

### Schema — league-scoped state
- [x] Migration: `rosters` table — `league_id` (FK to `leagues.platform_league_uuid`), `platform`, `season_year`, team/roster identifiers, owner metadata — `20260722050430_create_rosters_table.sql`, applied live 2026-07-22. Composite PK `(league_id, native_roster_id)` per the sleeper-api/roster-endpoint "(league_id, roster_id) pair" and espn-api/team-owner-member-id "(league, season, teamId)" identity decisions — season carried by the parent leagues row (Nick's Clarify decision); `native_roster_id integer` as-received — Sleeper `roster_id` and ESPN `teamId` are both wire integers, no string-corruption risk unlike league/player IDs (Nick's Clarify decision); owner metadata as first-class columns: `owner_native_id text` nullable (Sleeper `owner_id` / ESPN `primaryOwner` — orphaned rosters are a normal state) + `co_owner_native_ids text[] not null default '{}'` (full co-owner list preserved, primary never by array position) (Nick's Clarify decision); nullable `team_name`/`owner_display_name` display columns now, populated by Wave 2 sync (Nick's Clarify decision). FK on delete cascade, RLS enabled at creation, `set_updated_at` trigger. No standings figures or player arrays — those belong to the standings and roster_players items. Verified live: columns/nullability, PK, FK-cascade, RLS, trigger, migration history row
- [x] Migration: `roster_players` (or equivalent join) — resolves to `sleeper_player_id`, never a platform-specific player ID directly — `20260722051505_create_roster_players_table.sql`, applied live 2026-07-22. One row per rostered player per team; composite PK `(league_id, native_roster_id, sleeper_player_id)`; composite FK → `rosters` plus direct FKs → `leagues`/`players`, all on delete cascade; `platform` + `season_year` duplicated (N:1 with leagues, rosters precedent). Slot model (Nick's Clarify decision): `roster_slot` enum (`starter`/`bench`/`reserve`/`taxi`) normalized once at Wave 2 ingestion — Sleeper bench via the wiki's three-way subtraction, ESPN lineupSlotId 20→bench/21→reserve/active→starter — plus native detail columns `starter_slot_index` (Sleeper starters[] order, meaningful only against league_config slot layout) and `espn_lineup_slot_id` (raw, as-received), each check-constrained to its platform; `unique (league_id, sleeper_player_id)` so a player on two rosters in one league fails loudly at sync (Nick's Clarify decision); ESPN acquisition metadata deliberately excluded (Nick's Clarify decision — no registered v1 consumer). Current-snapshot semantics, no `week` column — historical lineups belong to matchups per both roster wiki pages. RLS enabled at creation, `set_updated_at` trigger. Verified live: columns/nullability, PK, all 3 FKs + cascade, unique, both checks, enum values, RLS, trigger, migration history row
- [x] Migration: `matchups` table — `league_id` (FK to `leagues.platform_league_uuid`), `platform`, `season_year`, week, participant/score fields — `20260722052726_create_matchups_table.sql`, applied live 2026-07-22. Per-roster weekly grain (Nick's Clarify decision): one row per roster per week — Sleeper's native response grain; ESPN schedule entries split into two rows per side at ingestion; pairings reconstructed by grouping on `(league_id, week, native_matchup_id)` with group-size validation, never forced home/away pairs (byes/odd team counts/consolation produce non-2 groups; median-league math needs every roster's weekly points). PK `(league_id, week, native_roster_id)`; composite FK → `rosters` + FK → `leagues`, both cascade; `platform` + `season_year` duplicated (N:1 precedent). Dual period columns per the espn-api ADR: `week` (scoring period) + `matchup_period` (ESPN matchupPeriodId; check-enforced `= week` on Sleeper rows, which have no native contest-interval ID). Scores (Nick's Clarify decision): raw `points` (nullable, as-received) + `custom_points` (Sleeper commissioner override, sleeper-only check) + stored generated `effective_points = coalesce(custom_points, points)`; ESPN live/projected columns deliberately excluded — exact field names are an open wiki question, Wave 2 adds them once verified live. Finality (Nick's Clarify decision): `fetched_at timestamptz` + `is_final boolean not null default false`, promoted only by our own sync policy per the wiki finality heuristic — no payload field claims finality. Native detail (Nick's Clarify decision): `native_matchup_id integer` nullable (both wire integers; null = bye) + `is_home boolean` ESPN-only check; ESPN `winner` excluded (provisional, derivable). RLS enabled at creation, `set_updated_at` trigger. Verified live: columns/nullability, generated column, PK, both FKs + cascade, all 3 checks, RLS, trigger, migration history row
- [x] Migration: `standings` table — `league_id` (FK to `leagues.platform_league_uuid`), `platform`, `season_year`, wins/losses/points-for/points-against — `20260722053523_create_standings_table.sql`, applied live 2026-07-22. Season-cumulative current snapshot (Nick's Clarify decision): one row per roster per league, PK `(league_id, native_roster_id)` mirroring rosters — no weekly history (v1 rejects season-over-season views; per-week points live on matchups); Wave 2 sync overwrites in place. Column set (Nick's Clarify decision): minimal five — `wins`/`losses`/`ties` integer + `points_for`/`points_against` numeric, all not null default 0 (Nick's Clarify decision — a fresh league genuinely is 0-0 with 0.00 points; zero is a true state, not missing data). Points columns hold the single recombined decimal per the roster-endpoint ADR (Sleeper's `fpts` + `fpts_decimal`/100 recombined at ingestion, split wire pair never stored); ESPN totals land as-received from mTeam. ESPN rank/playoff-seed and Sleeper waiver/transaction counters deliberately excluded (Nick's Clarify decision — no registered v1 consumer; ESPN's exact record field names are an open wiki question, Wave 2 adds them via new migration once verified against a live payload). No `fetched_at` (Nick's Clarify decision — standings have no finality concept; `updated_at` + trigger suffice). Composite FK → `rosters` + FK → `leagues`, both on delete cascade; `platform` + `season_year` duplicated (N:1 precedent). RLS enabled at creation, `set_updated_at` trigger. Pre-flight prolabel collision check passed (no existing standings table). Verified live: columns/nullability/defaults, PK, both FKs + cascade, RLS, trigger, migration history row
- [x] Migration: `player_scores` table — composite scoping on `(league_id, sleeper_player_id, season_year, week)` where `league_id` is a FK to `leagues.platform_league_uuid`, since fantasy value is never a single global number (Schema Rule #1) — `20260722054425_create_player_scores_table.sql`, applied live 2026-07-22. One row per rostered player per league row per week; PK `(league_id, sleeper_player_id, week)` with `season_year` duplicated as a non-PK column (Nick's Clarify decision — matchups precedent; a leagues row is already per-season, so season_year in the PK would be redundant). Sources: Sleeper matchup endpoint's `players_points` map (league-scored by Sleeper — never the generic `pts_std`/`pts_ppr` presets); ESPN player stat records selected at ingestion by all three discriminators (`scoringPeriodId` + `statSourceId` + `statSplitTypeId`) so a projection or season split is never mistaken for a weekly actual. `week` is the stat-accumulation interval (Sleeper week / ESPN scoringPeriodId); contest-interval grouping stays on matchups. Full attribution (Nick's Clarify decision): `native_roster_id integer not null` (composite FK → rosters, cascade — the only per-week attribution record; roster_players is current-snapshot and mRoster can't reconstruct history) + `was_starter boolean not null` (Sleeper starters[] membership / ESPN lineupSlotId vs. league_config's active slot set). Score (Nick's Clarify decision): single `points numeric not null` — a row only exists when the source reported a value, zero lands as-received, negatives legitimate; ESPN projected/live columns deliberately excluded (field names are a declared wiki open question — Wave 2 adds them via new migration once verified live). Finality (Nick's Clarify decision): `fetched_at` + `is_final` mirroring matchups exactly — same payloads, same correction-window reality, uniform Wave 2 promotion policy. FKs → leagues and players both on delete cascade; `platform` + `season_year` duplicated (N:1 precedent). RLS enabled at creation, `set_updated_at` trigger. Pre-flight prolabel collision check passed (no existing player_scores or *score* table). Verified live: columns/nullability/defaults, PK, all 3 FKs + cascade, RLS, trigger, migration history row

### Schema — draft state (shared, not staged)
- [x] Migration: `draft_state` table, keyed by `league_id` (FK to `leagues.platform_league_uuid`) — pick number, round, drafted `sleeper_player_id`, target roster, `source` (`manual` | `sleeper_poll` | `espn_poll`), timestamps, unique constraint on `(league_id, pick_number)` for first-write-wins semantics. Manual click-to-draft (Wave 3b), Sleeper live polling (Wave 2's write path, Wave 3b's live consumption), and ESPN live polling (Wave 3b) all write to this single table — no separate code paths, no staged manual-first/poller-later sequencing. This table only needs to exist correctly in Wave 1; the write logic itself is Wave 2 (Sleeper/ESPN poll paths) and Wave 3b (manual path). — `20260722055716_create_draft_state_table.sql`, applied live 2026-07-22. PK `(league_id, pick_number)` — the spec'd first-write-wins unique constraint serving as the natural PK (rosters/matchups/player_scores precedent; Nick's Clarify decision). Draft formats locked by Nick's Clarify: snake/linear + auction only, dynasty/multi-draft-per-season explicitly out of scope — one draft per league row assumed; `native_draft_id text` nullable records Sleeper `draft_id` provenance as-received (drafts arrays carry never-deleted aborted/restarted drafts; Wave 2's poller selects by season/type/status and records its choice here; null for ESPN — mDraftDetail has no distinct draft ID — and for manual picks; sleeper-only check). `pick_number`/`round` as-received (Sleeper `pick_no`/`round`, ESPN `overallPickNumber`/`roundId`) — never reconstructed via snake-order arithmetic, per both wiki pages' ground-truth decisions. `sleeper_player_id` FK → players (ESPN playerId resolves through the crosswalk at ingestion). `native_roster_id integer not null` (Nick's Clarify decision — every pick attributed), composite FK → rosters: Sleeper `roster_id` is the wiki-decided ownership field (never `draft_slot`/`picked_by`) / ESPN `teamId`. `source` as new `draft_pick_source` enum (`manual`|`sleeper_poll`|`espn_poll`; platform-enum precedent) with a `source_matches_platform` coherence check (poll sources pinned to their platform). `amount numeric` nullable — auction winning bid (Sleeper `metadata.amount` string coerced at ingestion / ESPN `bidAmount`), per Nick's explicit Clarify instruction; null for snake/linear picks, where it's the only meaningful value signal in auctions. Otherwise spec-exact (Nick's Clarify decision): `is_keeper`, `draft_slot`/`picked_by` deliberately excluded — no registered v1 consumer, new migration when one registers. No `fetched_at`/`is_final` — first-write-wins rows are immutable, no correction-window semantics (unlike matchups/player_scores). `platform` + `season_year` duplicated (N:1 precedent). All 3 FKs on delete cascade; RLS enabled at creation; `set_updated_at` trigger. Pre-flight prolabel collision check passed (no existing *draft* table or type). Verified live: columns/nullability/defaults, PK, all 3 FKs + cascade, both checks, enum values, RLS, trigger, migration history row

### Schema — integrity
- [x] Add foreign keys from every league-scoped table back to `leagues.platform_league_uuid`, and from every player-scoped table back to `players.sleeper_player_id` — verification sweep 2026-07-22: no migration needed; all FKs shipped with their tables' creation migrations. Verified live via `pg_constraint`: 18 FKs — 7 league-scoped tables + the `leagues` self-reference → `leagues.platform_league_uuid`; 4 player-scoped tables → `players.sleeper_player_id`; 5 composite `(league_id, native_roster_id)` → `rosters`; `leagues.owner_id` → `auth.users`. Delete rules as documented (cascade throughout; set null on `previous_platform_league_uuid` and `owner_id`)
- [x] Add indexes on `league_id` (i.e. `leagues.platform_league_uuid` and every FK referencing it), `platform`, `season_year`, `sleeper_player_id`, `share_token`, and the crosswalk's `espn_player_id` column — `20260722063616_add_integrity_indexes.sql`, applied live 2026-07-22. 17 new single-column indexes; judgment call (declared wiki silence — no indexing page in schema-reference): no duplicate index where the column already leads an existing PK/unique btree (Postgres prefix rule), so `league_id` (leading PK column on all 7 league-scoped tables + leagues PK itself), `share_token` (unique), `espn_player_id` (partial unique), `sleeper_player_id` on players/crosswalk (PKs), and `platform` on leagues (leads the platform/native/season unique) were verified-covered, not re-indexed. Added: `previous_platform_league_uuid` (the one unindexed FK → platform_league_uuid), `sleeper_player_id` on roster_players/player_scores/draft_state, `platform` + `season_year` on the 6 N:1 tables plus `season_year` on leagues. `leagues.owner_id` left unindexed (not in the item's list; set-null cascade on a tiny table). Verified live: all 17 present in pg_indexes, migration history row confirmed
- [x] Set up Supabase Auth for Nick as the sole admin user, email/password — create the one auth user, no signup flow, no other users created by this app. (Correction 2026-07-21: the Supabase project is the shared multi-app `prolabel` database, whose auth namespace already contains 2 pre-existing users from another app — the fantasy admin user is created alongside them, and every owner-write policy must match Nick's specific `auth.uid()`, never the blanket `authenticated` role.) Enable Row Level Security on every table created this wave. Write owner-only write policies matching `auth.uid()` against that one user. Do NOT build share_token-based spectator read policies yet — that's Wave 4 scope, once the spectator surface exists to actually use them; Wave 1 only needs RLS *enabled* with owner-write policies so nothing is silently open by default. — `20260722064037_add_owner_rls_policies.sql`, applied live 2026-07-22. Correction discovered live: one of the 2 pre-existing prolabel auth users already IS nick@prolabelco.com (created 2026-05-15; duplicate emails impossible in a shared namespace), so "create the one auth user" was unbuildable as written — Nick's Clarify ruling: reuse that existing user (`1d5dab52-9b93-4544-a99b-6d4dc3c84a66`) as the fantasy admin; no new user, no password handoff needed (policies only need the UUID). Policies pin via `public.is_fantasy_admin()` helper — stable, `search_path = ''`, single rotation point (Nick's Clarify decision) — wrapped in `(select ...)` per policy for once-per-statement evaluation. One `fantasy_owner_all` FOR ALL policy per table, full owner CRUD (Nick's Clarify decision — a literal writes-only reading would leave even the owner unable to read, breaking the wave's health-check completion gate; spectator reads stay Wave 4). RLS itself was already enabled at each table's creation — this item adds the 10 policies. `leagues.owner_id` backfill: no league rows exist yet, nothing to backfill — Wave 2 ingestion stamps it at insert (disclosed). Verified live: 10/10 policies (FOR ALL, authenticated) + RLS on 10/10 tables + function pins the UUID + history row; functional — admin-uid insert+select pass (rolled back), other-user insert denied 42501, anon insert denied 42501; security advisors report zero fantasy-owned lints

### Types + client wiring
- [x] Generate TypeScript types from the live schema (`supabase gen types typescript`) into a committed types file — `src/lib/supabase/database.types.ts`, generated 2026-07-22 via `npx supabase gen types typescript --linked --schema public` (canonical `gen:types` npm script added per Nick's Clarify decision). Full public-schema output committed as-is per Nick's Clarify decision — the shared prolabel project's ~49 foreign tables are included as inert generated types (no per-table filter exists in the CLI; hand-pruning would break regeneration). Verified: all 10 fantasy tables, all 3 enums (`platform`, `draft_pick_source`, `roster_slot`), and both functions (`generate_share_token`, `is_fantasy_admin`) present; `tsc --noEmit` passes. Declared wiki silence: no wiki category covers codegen/tooling (checked ROUTING.md + index.md); no schema decision was made here — types are read mechanically from the live DB
- [x] Create Supabase client utilities: browser client, server client (RSC/route-handler), both typed against the generated schema — named exports, per Code Conventions — `src/lib/supabase/client.ts` + `src/lib/supabase/server.ts`, 2026-07-22, via `@supabase/ssr@0.12.3` + `@supabase/supabase-js@2.110.8` (the current Supabase App Router standard; auth-helpers is deprecated — stated, not asked). Both export `createClient` (named export, same name disambiguated by import path — Nick's Clarify decision, Supabase-docs convention); no barrel index.ts (Nick's Clarify decision — impossible under same-name exports anyway); secret-key admin client deliberately deferred to Wave 2 when a consumer registers (Nick's Clarify decision — matches item text and no-speculative-building ethos). Server client: async, `await cookies()` (Next 15+), getAll/setAll pattern with RSC set-cookie no-op catch; both pass `Database` generic from `./database.types` (relative import per Code Conventions). Verified: `tsc --noEmit` + `npm run build` pass; temporary type probe confirmed `.from("leagues")`/`.from("draft_state")` resolve exact row/enum types through both clients (probe deleted after passing)

### Env, secrets, deploy
- [x] Create `.env.local` (gitignored) and a committed `.env.example` with placeholders for Supabase URL/anon key/service-role key. ESPN `espn_s2`/`SWID` placeholders included in `.env.example` now even though ESPN auth flow itself is Wave 2 — the env contract should exist before the wave that consumes it. — `.env.example` committed 2026-07-22; `.env.local` pre-existed from the Supabase setup sessions (verified gitignored: `.env.*` + `!.env.example` negation, `git check-ignore` both directions). Contract documents the modern key pair actually in use (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, `SUPABASE_SECRET_KEY`) rather than the item's stale anon/service-role naming, plus `SUPABASE_DB_PASSWORD` included and marked CLI-only/never-in-Vercel (Nick's Clarify decision) and ESPN placeholders named `ESPN_S2`/`ESPN_SWID` in conventional SCREAMING_SNAKE (Nick's Clarify decision) with wire-format comments per `espn-api/format-requirements.md` — SWID keeps its literal curly braces, espn_s2 keeps exactly one layer of URL-encoding, values extracted from the DevTools Network tab raw Cookie header, never re-encoded. Declared wiki silence on env-file/deploy tooling conventions generally (no covering category in ROUTING.md/index.md)
- [ ] Create Vercel project, link to the repository
- [ ] Configure Supabase secrets in Vercel (Production + Preview environments)
- [ ] Trigger initial Vercel deploy — confirm build succeeds
- [ ] Add a minimal server-side health-check route that performs a trivial authenticated Supabase read, to prove the deployed environment (not just local) can actually reach the database — this is the wave's actual completion gate, not just "build succeeded"

---

## Explicitly NOT in this wave

- ESPN cookie-auth flow / credential storage (Wave 2)
- Any Sleeper or ESPN data ingestion/sync logic (Wave 2)
- Share-token-gated spectator RLS read policies or the spectator route surface itself (Wave 4)
- Manual or automated draft-pick write logic (Wave 3) — only the `draft_state` table shape is created here
- Any UI beyond the default scaffolded shell needed to prove the deploy works

---

## Session-End requirements (per COMPLETION_TEMPLATES.md)

Use the `feature-build` report template. Update `STATE.yml` completely, log to `.claude/logs/`, update this file's checklist items, update `ARCHITECTURE.md`'s inventory sections (they're currently placeholders — this is the wave that first populates them) and `DESIGN_SYSTEM.md`'s token values (currently placeholders — Wave 1 is where real token values get decided against a real UI, per that file's own Status note).
