# 03b_draft_assistant_live_draft.md
**Wave 3b — Draft Assistant — Live Draft (Sleeper Snake)**
**Status:** 🟡 In progress
**Registered:** 2026-07-21
**Restructured:** 2026-07-22 (split from the original combined Sleeper+ESPN+auction file — see `03c_draft_assistant_espn_and_auction.md`)

---

## Scope

Sleeper snake-draft live mechanics only: manual click-to-draft write path, Sleeper live-draft polling orchestration, client-side live sync, live board UI extensions, the BPA/VORP recommendation engine, tier-cliff detection, positional run detection, draft queue/auto-pick, and resilience — all scoped to Sleeper snake/linear drafts. No ESPN references, no auction mechanics (Sleeper does not support auction drafts on this platform per Nick's decision — see Restructure Note below). Both write to the same shared `draft_state` table (schema from Wave 1; the Sleeper poll write path for `source='sleeper_poll'` was already built in Wave 2), enforcing first-write-wins via the existing `(league_id, pick_number)` unique constraint. No staged manual-first/poller-later sequencing. Depends on Wave 3a (static board UI/data layer) and Wave 2's Sleeper sync (already live). This wave extends and makes live the Wave 3a board — it does not rebuild the player table, row component, or filter/search/sort toolbar from scratch.

This build file was scoped by convergence-filtering 6 independent AI panel responses against `BUILD_INDEX.md`'s Wave 3b bullet, same methodology used for Waves 1, 2, and 3a; restructured 2026-07-22 to separate buildable Sleeper-snake scope from ESPN-blocked/auction scope.

### Restructure Note (2026-07-22)

ESPN leagues are commissioner-locked until ESPN's 2026 season setup opens (~mid-August 2026) — a hard external blocker unrelated to build sequencing, already recorded in `02_data_pipeline.md`'s ESPN sub-sections (`[!]`). The original version of this file bundled Sleeper snake, ESPN snake, and no auction support into one wave, which meant the entire wave was blocked on ESPN even though Sleeper snake mechanics have zero ESPN dependency and are buildable today. Nick's decision of record: Sleeper supports snake draft only (no auction on this platform); ESPN will support both snake and auction once unblocked. This file now scopes Sleeper snake only, so a build session can self-locate here immediately. All ESPN and auction items were moved to `03c_draft_assistant_espn_and_auction.md`, which is registered but blocked.

The manual-pick write path below accepts an optional `amount` field in its mutation signature (unused for Sleeper snake, since Sleeper does not support auction) so that when `03c`'s auction work lands later, the shared mutation does not need a breaking signature change — the `draft_state.amount` column already exists from Wave 1.

---

## WIKI PAGES TO CONSULT

Read these before starting, per Session-Start Protocol (max 3 unless the task genuinely needs more):

- `wiki/topics/sleeper-api/draft-endpoint.md` — draft pick response shape, `roster_id` as the ownership field, polling considerations, snake-draft ordering rules

---

## Checklist

### Manual click-to-draft write path
- [x] Add a server-side manual-pick mutation (Route Handler or server action) that validates `league_id`, `pick_number`, `round`, `sleeper_player_id`, and target roster, then inserts into `draft_state` with `source='manual'`. Mutation signature accepts an optional `amount` field (nullable, unused for Sleeper snake) so the shared signature does not change when `03c`'s auction path lands — *2026-07-22: server action (Nick's Clarify choice) `submitManualPick` wrapping `recordManualPick` in `src/services/draft-picks.ts`; any unclaimed pick accepted + server-side duplicate-player rejection (both Nick-signed); round cross-checked via ceil(pick/league_size) as validation only*
- [x] Rely solely on the existing `(league_id, pick_number)` unique constraint for conflict detection (`INSERT ... ON CONFLICT DO NOTHING` or equivalent) — return a typed result discriminating `accepted` vs. `conflict` (a poller already won that pick) vs. `validation_error` — *2026-07-22: upsert `ignoreDuplicates`; `conflict` carries the authoritative winning row; live-verified against the real league's poller rows*
- [x] Restrict the manual-pick endpoint to Nick's authenticated admin session only — never reachable from the spectator/share-token surface — *2026-07-22: auth gate inside each server action (`getAdminAuthState`, typed `unauthorized`, zero data) + deny-by-default RLS as the second wall; anon publishable-key client live-verified at both layers*
- [x] Add an admin-only "undo last manual pick" action that deletes only the highest `pick_number` row for the league where `source='manual'` — must never delete `sleeper_poll` rows — *2026-07-22: `undoManualPick` action / `undoLastManualPick` service; delete keyed by PK + `source='manual'` guard, poll rows untouchable by construction; live-verified (170 poll rows intact, `no_manual_picks` when none remain)*

### Active-draft polling orchestration (Sleeper)
- [x] Add an `is_draft_active` flag on a new admin-only table (e.g. `draft_sessions`, keyed by `league_id`) — NOT on `leagues` or `league_config`, since both of those tables receive `share_token`-gated spectator SELECT access in Wave 4, and draft state must never be spectator-reachable at any wave. Add a start/stop control surfacing it — toggling this is what elevates polling cadence — *2026-07-22: `draft_sessions` (collision-checked, owner-RLS only, migration `20260723001620`); 6h read-time soft TTL on `activated_at` + immediate sync on activate (both Nick-signed); toolbar toggle (Nick's placement choice) via auth-gated `startDraftSession`/`endDraftSession` server actions*
- [x] Tune the existing Wave 2 Sleeper draft-state poller to a high-frequency interval (e.g. every 5 seconds) while `is_draft_active` is true for that league, falling back to its normal cadence otherwise — *2026-07-22: client-driven trigger at 5s (Nick-signed — Hobby crons are daily-only, so the open admin board's `DraftPollTicker` invokes the `pollActiveDraft` action; the server re-checks the TTL-aware flag before any Sleeper request, a stale tab can never force polling); normal cadence = the existing daily league-state cron chain, nothing extra when no draft is live*
- [x] Record every active-draft poll run (source, league, status, timestamps, accepted/rejected counts, error summary) in the existing sync-run tracking table — *2026-07-22: every executed poll (activation sync + each elevated tick) records a `sync_runs` `draft_poll` row (Nick-signed as specced); a declined tick makes no Sleeper request and records nothing by definition; live-verified*

### Client-side live sync
- [x] Build a client polling/subscription mechanism (interval-based re-fetch, or Supabase Realtime if adopted) scoped to the active `league_id`, active only while the draft board is visible and `is_draft_active` is true — *2026-07-22: interval re-fetch on the existing 5s DraftPollTicker tick (Nick-signed — Realtime declined: shared prolabel realtime publication config + a second live channel); every executed `pollActiveDraft` returns the league's full `draft_state` snapshot via `listDraftPicks` (full-snapshot-over-incremental Nick-signed — undo deletions self-heal); tab-visibility gated (skip while hidden, instant catch-up tick on return, Nick-signed); shell owns snapshot custody, seeded by the server render*
- [x] Merge incoming `draft_state` rows into the board's existing player/availability derivation (from Wave 3a) by `pick_number`, deduplicating so repeated snapshots don't cause redundant re-renders — *2026-07-22: pure merge layer `components/draft-board/live-picks.ts` — third availability value `'drafted'` (client-merge-only; `'rostered'` wins precedence), "Drafted · Pick N" dimmed row (Nick-signed); out-of-pool picks skipped (Nick-signed — recent-picks feed later surfaces all picks); dedup = order-insensitive fingerprint bail-out in setState + same-reference no-op merge; the merge REPLACES refresh-on-picks (Nick-signed — refresh survives only for stand-down). Overlay applies outside active sessions too (Nick-signed after the real-data finding: 40 of 170 drafted players are unrostered on the 2025 league and honestly read as drafted). Live-verified 24/24 against the real league incl. record→merge→undo→heal cycle and anon RLS zero-data on the snapshot path*
- [x] Add optimistic UI for a manual pick: immediately mark the player unavailable and pending in local state, then reconcile on server response — *2026-07-22: shell-owned pending overlay through THE merge path (`draftPending` client-only flag on the 'drafted' overlay; "Drafting…" cue, Nick-signed); next pick = max(snapshot ∪ pending)+1 (Nick-signed default), round = ceil(pick/league_size), no Draft action rendered when league size unknown; accepted picks adopt the confirmed row into the snapshot immediately*
- [x] On conflict (a poller won the pick first), roll back the optimistic update, adopt the authoritative row, and surface a non-blocking toast — no retry loop — *2026-07-22: conflict's authoritative row inserted into the livePicks snapshot (loser heals to available via the same merge); sonner toast (Nick-signed), naming the winner when in-pool; validation/unauthorized/network outcomes also roll back with a toast (disclosed extension); live-verified through the real write path, net-zero*
- [x] Add a live status indicator (last successful poll time, active polling source, connection health) in the board toolbar — *2026-07-22: ticker gains an `onStatus` callback reporting each EXECUTED tick's outcome upward (skipped/stand-down ticks report nothing; authority stays server-side); shell aggregates into `LivePollHealth` (pure layer `live-picks`-style in `live-status.ts`), reset on session (re)arm; `LiveStatusIndicator` renders beside the "Draft live" label via a `statusSlot` on the toggle (Nick's placement) — "Sleeper · synced Ns ago" relative live-updating, degraded after 2 consecutive failures (muted-tier text, instant recovery), "Paused — tab in background" while hidden, absent entirely with no active session (all four Nick-signed); logic verified 18/18, tsc/lint/build clean*

### Live board UI extensions (built on top of Wave 3a, not replacing it)
- [x] Add a "Draft" action to the existing player row, visible only when `is_draft_active` is true and the player is available, disabled while its own request is in flight — *2026-07-22 (folded with live-sync items 3–4 as one verifiable artifact, Nick-signed): Status-cell Draft button (Nick's placement) opening a per-click roster picker (Nick-signed; Base UI Menu vendored as ui/dropdown-menu); in-flight disable is structural — the optimistic row leaves 'available' instantly, removing the action*
- [x] Derive and display the current pick number, round, and team on the clock from `draft_state` count plus snake-draft order from the selected draft object's `slot_to_roster_id` (Nick-signed correctness amendment 2026-07-22: `league_config` has no draft-order concept — order metadata is per-draft state, carried through the existing poll path per Nick's Clarify choice, never persisted) — *2026-07-22: `DraftOrderMeta` extracted in `syncLeagueDraftState` (drafts-array element omits `slot_to_roster_id` — verified live; best-effort `GET /draft/{draft_id}` detail fallback added), rides `DraftPollSyncOutcome.success` → ticker `onDraftOrder` → shell state; pure `projectOnClock` in live-picks.ts (snake/linear + reversal_round flip, contiguous-map guard, auction honestly unknown, draft-complete past rounds×slots); slim `LiveDraftStrip` above the board (Nick's placement) — pick = computeNextPickNumber(snapshot ∪ pending), round = ceil(pick/league_size), on-clock label via shared `rosterLabel`; projection accepted plain-label (Nick-signed — recorded picks stay ground truth). Verified 22/22 incl. projection matching ALL 170 recorded picks on the real league (non-identity slot map) + net-zero write; tsc/lint/build clean*
- [x] Extend the existing static roster/positional-need panel from Wave 3a to update live as picks land from any source (manual, Sleeper poll) — *2026-07-22: `computeRosterFill` gains confirmed-snapshot + rosters params (all four blend rules Nick-signed): union by player — rostered baseline wins, a pick adds fill only for a player rostered nowhere; ST/BN/IR stay roster-sync-only with a separate honest per-team "N drafted" pick count; confirmed rows only (pending overlay panel-invisible); out-of-pool picks attribute drafted-count-only. Teams with no rostered players are created from picks (rosterLabel naming). Verified 31/31 pure + 8/8 live on the real league (170 picks; 40 unrostered = 26 in-pool fill adds + 14 out-of-pool attribution-only; slot totals byte-identical to baseline)*
- [x] Add a compact recent-picks feed (pick number, round, player, roster, source badge distinguishing manual/sleeper_poll) — *2026-07-22 (all four surface decisions Nick-signed): sidebar section below the rosters, newest first, rendered whenever picks exist (overlay-always precedent); player identity via a catalog join added to every RecordedPick read/write path (`PICK_SELECT` players embed — DST names composed from the split name fields per sleeper-api/dst-and-free-agents); undo action surfaced here (its natural home per the standing warning) — Undo button on exactly the row `undoLastManualPick` would delete (highest manual pick, pure `latestManualPickNumber`), session-active only, shell-owned handler + local heal + toast. Nick-signed backsweep: the join's position data supersedes item 3's out-of-pool count-only rule — out-of-pool picks now feed position fill. Verified 8/8 pure + 7/7 live (170/170 joined names incl. 10 DSTs; true-conflict embed; net-zero)*
- [x] Ensure the drafted-player set continues to correctly drive the existing filter/search/sort toolbar's "available only" behavior live — *2026-07-22: verification-only, zero code change (declared at Clarify — the merge's third availability value already flows through `filterAndSortPlayers`' `availability !== 'available'` exclusion on the merged pool both regions read). Verified 6/6: rostered/confirmed/pending exclusion, undo-heal re-inclusion, position-filter composition; prior live-sync session had already live-verified the 40 drafted-unrostered exclusions on the real league*

### Projections ingestion (prerequisite for BPA — Nick-signed correctness amendment, 2026-07-22)

Registered when the projections-source decision landed (`MANUAL_SETUP_CHECKLIST.md` Wave 3b decision of record, 2026-07-22): the BPA items below compute VORP from projected points, but no projections ingestion existed and no build file registered it. Source = the already-adopted quarantined `api.sleeper.com` projections endpoint — its response already carries component projection stats alongside the `adp_*` fields (`wiki/topics/sleeper-api/projections-endpoint.md`). Landing shape = component stats as-received, scored by `league_config` weights at read time (BPA item 3's base-value function) — preset `pts_*` buckets are never used as league values.

- [x] Migration: new `player_projections` table (collision-checked against the live shared schema), PK `(sleeper_player_id, projection_source, season_year)`, full `stats` jsonb as-received plus `company`/source-timestamp provenance columns, owner-only RLS per the `fantasy_owner_all` pattern. NOT league-scoped by design (`adp_rankings` precedent — vendor/market data global per player/season; `season_year` per Schema Rule #3) — *2026-07-22: migration `20260723023127`; collision verified against live `information_schema` (not just generated types); RLS + `fantasy_owner_all` + `set_updated_at` + `(projection_source, season_year)` read index confirmed live post-push*
- [x] Extend the quarantined `src/services/adp/` ingestion to persist `player_projections` from the SAME single fetch as `adp_rankings` (fused, Nick-signed — zero added requests to the contractless host), with its own validate-before-swap plausibility gates and current-snapshot stale-row cleanup; telemetry rides the existing `adp_ingestion` `sync_runs` row's counts (Nick-signed — no new source value, no second run row) — *2026-07-22: candidate dedup by `player_id` (first wins), catalog-mapped union drives one unmapped list; both tables' gates run before EITHER writes; ADP swap completes before projections writes begin (a late projections failure degrades only projections); `counts` gains `projections_persisted`/`projections_stale_deleted`*
- [x] Verify live against the real 2026 endpoint: both tables populated from one run, `adp_rankings` behavior unchanged, idempotent re-run, and failure containment (a failed/implausible fetch preserves both tables' last good snapshots) — *2026-07-22: 3292 records → 3292 projection rows (0 unmapped, 0 null stats/company/provenance), adp_rankings byte-identical behavior (2946 entries/10 formats/0 stale); idempotent re-run identical; bogus-year 2035 run aborted at the gate with ZERO writes to either table (verified by count); tracked-path run confirmed `sync_runs` counts extension live; tsc/lint/build clean*

### BPA recommendation engine

Base mechanism is Value-Over-Replacement (VORP/VBD), computed dynamically, with need kept as an independent signal rather than blended into the ranking score. This is a hard design constraint, not a preference: research across the panel is unanimous that blended "trust the algorithm" scores are distrusted by serious drafters, while separated signals with visible reasoning are preferred.

- [x] Build a pure replacement-level calculation per position: the projected points of the last startable player at that position given the league's actual roster/flex/bench settings from `league_config` (e.g., a 12-team, 2-RB-starter league implies a replacement RB around RB24-30 depending on flex and bench demand) — no hardcoded roster shape — *2026-07-22 (fold = items 1–3, declared at session start; four Clarify decisions Nick-signed): new `src/services/bpa/` — `scoring.ts` (`parseScoringWeights`/`scoreProjectionStats`, THE projection-scoring home; `adp_*`/`pts_*` structurally excluded per canon) + `replacement.ts` `computeReplacementLevels`: greedy league-wide fill (dedicated slots, then flex narrowest-eligibility-first), flex pooled across eligible positions (resolves the VBD wiki page's open pooling question — Nick-signed), startable-only demand (bench depth arrives via item 2's ADP calibration — Nick-signed), layout via `parseRosterSlotLayout`, nothing hardcoded. Declared wiki silence: non-standard flex-label eligibility (REC_FLEX/WRRB_FLEX/IDP_FLEX general-knowledge; unknown labels excluded from demand + surfaced). Verified 35/35 pure + live on the real league (10-team 2-FLEX: WR wins 17/20 flex slots, QB10=300.5 / RB23=206.0 / WR37=193.7 / TE10=185.1; 22/38 scoring keys intersect the stats namespace — empirical key-namespace confirmation); tsc/lint/build clean*
- [x] Calibrate replacement-level depth against market ADP (from `adp_rankings`, Wave 3a) rather than a fixed round-count baseline, so roughly half of players at a position sit above/below their ADP-implied replacement line — this avoids classic VBD's tendency to overvalue QB/TE — *2026-07-22 (three Clarify decisions Nick-signed): `src/services/bpa/calibration.ts` `calibrateReplacementLevels` — ADP-window depth (depth_p = position-p players with `adp_overall` ≤ item 1's totalStartableSlots; replacement = that depth-th player by projection order — market sets depth, projections set value); calibrated level REPLACES structural when computable, structural carried alongside for visible reasoning; flagged fallbacks at league level (unresolved format/empty snapshot → `structural_only`; never cross-format) and position level (zero in-window carriers → structural depth, never a zero-depth line). ADP read from sentinel-filtered `adp_rankings` only, format via `resolveAdpScoringFormat`. Verified 15/15 pure + live on the real league (ppr, 245 carriers, window 100: RB deepens 23→35 / 206.0→161.5, WR 37→42, QB/TE ~structural, K/DEF zero-carrier fallback flagged — the signed guard working live; above-line splits 29–45% ≈ the item's half-above/half-below gloss); tsc/lint/build clean*
- [x] Build a pure base-value function: `player_projection − replacement_projection` for each undrafted player, using the league's scoring weights from `league_config` — no hardcoded scoring weights — *2026-07-22 (zero open Clarify questions — declared plainly per protocol; both inputs already Nick-signed via items 1–2): `src/services/bpa/base-value.ts` `computeBaseValues` — subtraction over the pool against item 2's calibrated levels (the operative line per the signed replace decision), scoring via item 1's centralized `scoring.ts`, negative values kept (below-replacement is data), unrankable positions (no startable slot) excluded + surfaced by count/list, deterministic ordering (baseValue desc → projection desc → id asc). Disclosed judgment calls: unrankable exclusion + ordering. Verified 12/12 pure + live full-chain on the real league (3292 pool → 3218 rankable + 74 unrankable [CB/FB/P]; top-15 = plausible 2026 PPR board — Bijan 182.6 / Gibbs 164.4 / Nacua 158.1, RB-heavy per deepened market replacement, zero QBs in top 25 for the 1QB league); tsc/lint/build clean*
- [x] Make replacement level and base value recompute after every pick as the available player pool shrinks — this dynamic recompute is what makes positional scarcity real-time rather than a pre-draft snapshot; do not cache a static pre-draft replacement baseline — *2026-07-22 (fold = items 4–6, one verifiable artifact "the BPA query layer"; zero open Clarify questions for item 4 — pure composition of already-Nick-signed functions, declared plainly): `src/services/bpa/board.ts` `computeBpaBoard` — THE one composition of scoring→replacement→calibration→base-value, pure/no-cache; the shrinking pool is realized by the caller passing the current UNDRAFTED set (drafted removed), so replacement is always the last startable among those still available. Recompute model disclosed (Nick-signed via the pure layer + STATE self-location): full league-wide demand re-run against the shrinking pool (does not subtract filled league slots — negligible with the ~3.3k pool; a possible future refinement). Verified: composition ≡ manual chain, shrinking pool deepens RB replacement + raises a survivor's base value (live: pool 3292→3122 after 170 picks, RB replacement 39.4)*
- [x] Build a separate, independent roster-need signal (remaining starter slots vs. remaining bench slots by position, from current roster state and `league_config`) — this must never multiply into or reorder the base VORP ranking — *2026-07-22: `src/services/bpa/need.ts` `computeRosterNeed`/`classifyPositionNeed` — pure, per-team greedy fill (dedicated then flex pooled via the exported `FLEX_ELIGIBILITY` single home, never re-derived). Bench modeled GENERIC (Sleeper BN is a shared pool per roster-construction wiki — not per-position): per-position starter demand + one roster-wide `benchSlotsRemaining`. Need kinds (Nick's Clarify): 'starter' (open dedicated/flex slot eligible), 'bench' (starters full, generic bench room), 'full' (no starter slot AND no bench room — per-position caps deferred to auto-pick). Structurally separate signal — item 6 attaches it as a parallel annotation, never a value input (verified live: need run's base ordering byte-identical to value-only). Verified 15/15 pure incl. empty/3-RB-flex/full/SUPER_FLEX/unknown-flex-label*
- [x] Expose the BPA function via a server-side query/action returning the top-N ranked candidates (ranked purely by base VORP value) for the selected league, given current `draft_state`, each annotated with its independent need signal — *2026-07-22: `src/services/bpa/recommendations.ts` `getBpaRecommendations` (first I/O in bpa/) + `fetchBpaRecommendations` action (draft/actions.ts — same in-action `getAdminAuthState` + RLS-server-client pattern as the other 5 actions; read, no revalidate). Loads config (weights/layout/format/size), paginated projections (>1000-row-safe `.range` loop — 3292 rows), format ADP carriers, and `listDraftPicks` (one read → drafted set for the shrinking pool + self-roster positions for need); shrinks pool, runs `computeBpaBoard`, slices top-N (default 10 — Nick's Clarify), annotates each with `classifyPositionNeed`. `selfRosterId` optional (Nick's Clarify — no "my team" marker exists; need null when absent, item-7 panel supplies it). Typed reasons `league_not_found`/`config_unusable`/`projections_unavailable`; ADP absence is not a failure (structural_only). Boundary discipline (draft-board precedent): explicit columns, platform-agnostic, admin-only (no spectator consumer). Verified live read-only on the real league: default N=10, topN=25, pool excludes all 170 drafted, calibration=adp, need annotations present + ordering-invariant, anon client→league_not_found (RLS wall). 39/39 total; tsc/lint/build clean*
- [x] Build a recommendations panel in the board sidebar showing the top few candidates with ADP, base VORP value, and an inline need badge/tag ("Fills starter need," "Bench depth," "Position full") displayed alongside — never merged into — the value ranking, tabular-nums throughout — *2026-07-22 (fold = items 7–9 "the BPA recommendations panel", one verifiable artifact + at the live-behavior ceiling of 3; three Clarify decisions Nick-signed): `src/components/draft-board/bpa-recommendations-panel.tsx` at the TOP of the sidebar (Nick's placement) — `fetchBpaRecommendations(topN=8)` (Nick's count), rows ranked PURELY by base VORP with market ADP beside the value and the need badge (NEED_LABELS starter/bench/full, verbatim build-file copy) shown alongside via muted-tier chips ('starter' borrows `--warning` like RosterPanel, teal reserved per DESIGN_SYSTEM/STATE), tabular-nums on VORP/ADP; PositionBadge + rosterLabel reused; my-team picker over rosters sets selfRosterId (Nick's Clarify — no persisted marker, re-pick each session; value-only + no badges until picked); honest loading/error/empty + structural_only ADP note*
- [x] Wire recommendation rows to the same one-click draft action as the main table (shared logic, not duplicated) — *2026-07-22: rows call the shell's SAME `handleDraft` via the roster dropdown (never a parallel submit — build-file + STATE warning mandate); `handleDraft`'s param narrowed to the new `DraftablePlayer` ({sleeperPlayerId, fullName} — the minimal shape it consumes) in `services/draft-board.ts` so the panel reuses the one optimistic write path with no cast/duplication; pending players read "Drafting…", Draft control on the same `draftEnabled` gate as the table*
- [x] Recalculate base VORP and replacement levels immediately after any new pick lands, from any source — *2026-07-22: a `useEffect` keyed on `livePicks` re-invokes `fetchBpaRecommendations` off the SAME shell-owned snapshot (never a second pick-event feed — STATE warning); the shrinking-pool recompute is realized in `getBpaRecommendations` (pool = projections − draft_state), not cached; the shell's fingerprint bail-out means a steady 5s poll returning an identical snapshot keeps the same reference and does NOT refetch — only real pick changes (manual/Sleeper poll/undo) do; request-seq guard drops out-of-order responses. Live-verified read-only on the real league: topN=8 returns 8, calibration=adp, pool 3122 (3292−170), need annotations present with selfRosterId + ORDERING/VORP invariant vs value-only (need never reorders), need null when no self roster; tsc/lint/build clean; read-only session, no migration*

### Tier-cliff detection
- [x] Build a pure tier-boundary calculation per position: sort undrafted players within the position by projection (or VORP z-score), insert a tier boundary wherever the point-gap between consecutive players exceeds a per-position threshold — tune thresholds tighter for RB/WR (deeper positions) than QB/TE (shallower positions) — *2026-07-22 (fold = items 1–3 "the tier-detection layer", one verifiable artifact; three Clarify decisions Nick-signed): `src/services/bpa/tiers.ts` `computePositionTiers` — pure, groups the base-value ranked pool by position, tiers only players at/above the operative replacement line (Nick's Clarify: down-to-replacement depth scope — full-tail tiering manufactures noise per league-size-scarcity-effects.md), break where consecutive projected-points gap > per-position multiple × the position's MEDIAN gap (Nick's Clarify: multiple-of-typical-gap, not absolute — adp-ecr-differential.md; median over mean is a live-verified robustness fix so one dominant elite-separation gap can't swallow QB/TE secondary cliffs; within-position projection gaps ≡ base-VORP gaps so no z-score step). Within-position ordering never affects the VORP ranking (parallel display signal like need). Wiki grounding: league-size-scarcity-effects.md (fixed-threshold gap detection, position-sensitivity, dynamic recompute); exact numeric thresholds are DECLARED WIKI SILENCE (that page's Key Decision refuses specific thresholds) → live-tuned platform constants*
- [x] Target tier counts as a starting calibration, not a hard rule: roughly 3-5 tiers for QB/TE, roughly 6-9 tiers for RB/WR — *2026-07-22: per-position `TIER_GAP_MULTIPLE` (RB 3.0 / WR 3.2 / QB 2.0 / TE 1.5 / K,DEF 2.0 / default 2.5), Nick's Clarify = fixed multiple with counts as a sanity check, NOT adaptive-to-band (the wiki rejects rigid tier-count enforcement). Live-verified on the real 2026 PPR pool: QB 3 / TE 4 (band 3-5), RB 8 / WR 9 (band 6-9) — all four targeted positions in band; deep positions carry the higher multiple (dense tiny-median tail needs it to avoid noise tiers) while their ABSOLUTE point threshold stays smaller = the "tighter for RB/WR" sensitivity*
- [x] Recompute tier boundaries alongside the base VORP recompute after every pick, since the shrinking pool shifts where gaps fall — *2026-07-22: composed into `computeBpaBoard` (BpaBoard gains `tiers`) off the same base-value ranking, so tiers recompute against the shrinking undrafted pool on the SAME per-pick re-call as VORP — no separate cache, no second recompute driver (STATE centralization). Surfaced through `getBpaRecommendations`: each row carries its within-position `tier`, context carries whole-board per-position tier summaries (count + "N in the best available tier") for item 4's counter. Live-verified: full pre-draft pool (RB 8 tiers) vs real 170-drafted pool (RB 10, QB 3→2) genuinely diverge = dynamic recompute, not a snapshot; ordering invariant vs value-only; 11/11 pure edge tests (empty/single/2-player-degenerate/huge-gap-split/median-robustness/determinism). tsc/lint/build clean; READ-ONLY session, no migration*
- [ ] Surface a live "N remaining in this tier" counter per position in the board UI — this is the highest-leverage piece of the tier system, since it tells the drafter whether to reach now or wait for the next tier

### Positional run detection
- [ ] Build a pure run-detection function using a sliding window over the last N picks (recommend N=5-6) with a same-position count threshold (recommend 3-4 within that window) to flag an active run
- [ ] Use position-specific thresholds: lower trigger threshold for shallow positions (QB/TE) than deep ones (RB/WR)
- [ ] Surface run detection as a non-blocking inline badge near the position filter — never a modal or blocking interrupt
- [ ] Keep run detection purely informational: it must never auto-adjust BPA ranking or base VORP directly — only the dynamic replacement-level recompute (already triggered by each pick) is allowed to move rankings. Run detection and value recalculation are separate, independently inspectable mechanisms even though a run will naturally shift replacement level too

### Draft queue and auto-pick
- [ ] Build a user-ordered queue (priority list) per league/session, stored independently of and never silently reordered by the BPA/recommendation engine
- [ ] When a queued player is drafted by anyone else (manual or Sleeper poll): remove from queue, promote the next queued player automatically, and surface a brief non-blocking notification
- [ ] Build roster-construction-aware auto-pick as a purely local mechanism: when the draft clock expires or the user is away, walk the queue top-to-bottom and skip any player who would violate a hard roster rule (e.g., a 3rd QB before the bench is otherwise full) rather than blindly taking queue position 1 — auto-pick must never draft a queue entry that breaks a hard roster constraint. Auto-pick writes ONLY to this app's own `draft_state` table, through the same manual-pick write path (`source='manual'`, same first-write-wins conflict semantics as any manual pick, undoable via the same source='manual'-only undo action) — it never calls any Sleeper write endpoint, under any circumstance. If Sleeper's own clock expires in a real Sleeper-hosted draft, Sleeper's own platform auto-pick makes the actual pick and the poller ingests that authoritative row; this tool's auto-pick exists to keep local draft_state/UI/recommendation state consistent, never to act on Nick's behalf on any external platform
- [ ] Queue and auto-pick consume pick events from the shared `draft_state` table, fed by the existing manual/`sleeper_poll` write paths and the client-side live sync mechanism above — read-only against Sleeper, no direct draft-API/WebSocket connection of their own. Queue/auto-pick scope stays single-Sleeper-hosted-league — no generic multi-platform sync layer

### Resilience
- [ ] Ensure the board remains usable read-only (falls back to Wave 3a's static behavior) if live polling, realtime, or BPA calculation degrades or errors
- [ ] Add tests covering: concurrent manual + poll writes racing on `(league_id, pick_number)` with only one accepted, BPA/replacement-level recompute after a pick is recorded, tier-boundary recompute after a pick shifts the pool, run-detection triggering/resetting across a sliding window, and auto-pick skipping a queue entry that violates a hard roster rule

---

## Explicitly NOT in this wave

- Rebuilding the static board's core display primitives (player table, row component, filter/search/sort toolbar) from scratch — extended and made live, not replaced
- ADP ingestion, ADP schema, or ADP source management (Wave 3a, already built)
- Any ESPN draft mechanics (client, auth, crosswalk sync, polling, draft-state writes) — `03c_draft_assistant_espn_and_auction.md`, blocked pending ESPN commissioner unlock
- Any auction-draft mechanics (nomination/bid state, budget tracking, auction-specific valuation) — Sleeper does not support auction on this platform; auction support lands with ESPN in `03c_draft_assistant_espn_and_auction.md`
- Applying league scoring rules to compute live `player_scores` — Wave 4
- Standings, matchups, power rankings, or any in-season dashboard content — Wave 4
- The mobile-first read-only spectator/share-link surface — no live draft data or draft-board markup may ever reach that surface, at any wave
- Score charts, lucky/unlucky tracking, positional breakdowns, playoff picture, trade evaluation, waiver/FAAB recommendations — Wave 5
- League report generation, free agent board, PWA manifest/service worker — Wave 6
- Any staged "manual-first, then add polling later" sequencing — both ship together in this one wave
- Any write to Sleeper or any other external platform — including by auto-pick, which writes solely to this app's own `draft_state`; when Sleeper's clock expires in a real draft, Sleeper's own platform auto-pick makes the actual pick (read-only rule reaffirmed — this restates the existing MASTER_CONTEXT.md rule, it is not a new exception)

---

## Session-End requirements (per COMPLETION_TEMPLATES.md)

Use the `feature-build` report template. Update `STATE.yml` completely, log to `.claude/logs/`, update this file's checklist items, update `ARCHITECTURE.md`'s Service API Reference section (adds the manual-pick mutation, Sleeper active-draft poll orchestration, BPA/replacement-level scoring service, tier-boundary service, run-detection service, and queue/auto-pick service).
