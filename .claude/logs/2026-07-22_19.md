# Session Log — 2026-07-22_19
**Session type:** non-code (dedicated wiki-maintenance session — Nick-prompted, Claude Code environment)
**Scope:** Single-source ingestion of the Sleeper undocumented projections/ADP endpoint into the sleeper-api wiki category, per wiki/DISCOVERY_PROTOCOL.md + wiki/WIKI_CHAT_CONTEXT.md governance; clear the wiki-silence known_issue; update MANUAL_SETUP constraint note. Explicitly NOT a build session — no src/ changes, no migrations.

---

## Session start

- Session type declared by Nick's prompt: wiki-maintenance (the Rule 2/Rule 22 wiki write restriction is session-type-gated, not environment-gated — a Claude Code wiki-maintenance session writes wiki/ directly via local git, per DISCOVERY_PROTOCOL's environment header and MAINTAINER.md).
- Project session-start reads: MASTER_CONTEXT.md (auto-loaded), STATE.yml, wiki/index.md, wiki/ROUTING.md. BUILD_INDEX.md deliberately not read — no build-file work in scope; STATE.yml's current_build_files untouched by this session type. Wiki-maintenance session-start reads per MAINTAINER.md: schema.yml, session-state.md, tags.md, verification-cache.md, _queue_master.md (all cycles COMPLETED — no ACTIVE queue work; confirms single-source mode), DISCOVERY_PROTOCOL.md, MAINTAINER.md, WIKI_CHAT_CONTEXT.md.
- Source material reads: .claude/MANUAL_SETUP_CHECKLIST.md (Wave 3a ADP decision of record) and .claude/logs/2026-07-22_18.md (the recorded live verification — response-shape knowledge). COMPLETION_TEMPLATES.md for the report format and mandatory lines.
- Counters initialized: blocks_executed = 0, pages_read = 0.

## Mode determination

- Nick provided the source directly (prompt + the recorded 2026-07-22 verification) → Single-Source Ingest Protocol (WIKI_CHAT_CONTEXT.md), not a Discovery panel triple. The endpoint postdates the Notebook 5 subject list (18/18 COMPLETED), so no queue entry exists or was created — queue files untouched.

## Live re-verification (fact-checking step, within the 5-minute cap)

- One credential-free GET to `api.sleeper.com/projections/nfl/2026?season_type=regular&position[]=RB&order_by=adp_ppr` (first ~3.5KB inspected). Confirmed and sharpened the recorded knowledge:
  - Top-level shape is a JSON **array** of projection objects, NOT an ID-keyed map (the _18 log's "keyed by native player_id" phrasing was ambiguous; each record carries a top-level `player_id` field). Distinction recorded explicitly on the page since the documented players dump IS a map — an easy trap.
  - ADP fields live INSIDE the `stats` object, mixed with projection stats (pts_ppr, rush_yd, …).
  - **12** adp_* fields observed — the _18 inventory missed `adp_dynasty_2qb`. Direct proof the field set is open, not closed → drove the pattern-based-ingestion Key Decision.
  - `999.0` observed consistently in format-inapplicable fields (adp_rookie on veterans; bare adp_dynasty while suffixed dynasty variants carry real values) → inferred not-ranked sentinel; recorded as observation-derived, filed in Pending Verification, and turned into a treat-as-null Key Decision.
  - `company: "rotowire"` on each record → ADP provenance caveat (Sleeper-native draft data vs vendor-supplied is unconfirmed; the population-matching rationale is plausible but unverified) — recorded honestly as an Open Question rather than laundered into fact.
  - Same-day `last_modified` epoch-ms timestamps; Bijan Robinson `"9509"` adp_ppr 1.4 — identical to the _18 verification, so two independent same-day pulls agree.

## Ingest Plan and execution (2 blocks, both completed)

- **BLOCK 1 — CREATE `wiki/topics/sleeper-api/projections-endpoint.md`** (23.4KB — target tier). type: domain-knowledge; confidence: **medium** (deliberate: the shape is directly live-verified, but the calibration standard reserves high for "verified against official source or specification" and no official spec exists for an undocumented surface; population parameters/cadence/stability all unknown). Tags (all pre-existing in tags.md — no new tags): adp, undocumented-endpoint, endpoint-structure, rest-api, read-only-api, player-id-mapping, caching-strategy. Body: documentation status/provenance (absent from docs.sleeper.com's 17 documented endpoints AND the community undocumented canon; knowledge rests on 2×-same-day live verification), the api.sleeper.com vs api.sleeper.app/v1 host split (load-bearing both directions — unreachable from the pinned client; documented-host rate-limit knowledge does not transfer), verified URL/parameter structure (only the verified combination asserted; everything else flagged unverified), response shape (array; per-record field inventory; embedded player object subordinated to the players dump as identity source), the stats object + 12-field ADP inventory + open-set caution, the 999.0 sentinel, what the endpoint does NOT provide (no dispersion/population metadata — tied to the ADP domain page's dispersion doctrine, with an explicit no-fabrication rule), the rotowire provenance caveat, the three-leg defensive posture (isolation / last-good snapshot / permissive parsing), and decision provenance citing the MANUAL_SETUP_CHECKLIST Wave 3a decision of record with all four alternatives (FFC, FantasyPros, Underdog, ESPN) and their rejection grounds. 7 Key Decisions in the established Decision/Reasoning/Rejected format; 7 Open Questions. Reciprocal `related:` links added in the same block to sleeper-api/players-endpoint, sleeper-api/authentication, league-mechanics/average-draft-position (bidirectionality rule). Commit `15f4625` (`wiki: create projections-endpoint`).
- **BLOCK H — HOUSEKEEPING.** sleeper-api/_index.md: new page row (medium) + footer note rewritten (notebook complete + one post-notebook single-source page; also removed the stale "nb6a is now ACTIVE" sentence). wiki/index.md: Sleeper API 16→17, Total 132→133, category description notes the post-notebook page. verification-cache.md: 2 verified-claim rows (endpoint shape/ADP fields, 2×-same-day verification, next review 2026-10-20; undocumented-status + host-split claim) + 3 Pending Verification rows (ADP provenance/population; 999.0 sentinel semantics; field-set completeness/update cadence/unverified request behaviors). MAINTAINER.md ingestion-order table: notebooks 5 and 6 corrected to ✅ COMPLETED (stale "ACTIVE/LOCKED" since 2026-07-21, contradicted _queue_master — correctness fix, disclosed). Commit `d9d275a`. No new tags → tags.md untouched. No notebook completed by this session → WIKI_ROADMAP untouched. Queue files untouched.

## Session counters / health

- blocks_executed = 1 (housekeeping excluded per session-state.md); pages_read = 3 (players-endpoint, authentication, average-draft-position — read in full for reciprocal-link edits and to match page conventions; system files/_index files excluded). **SESSION: FRESH.**

## Session-end bookkeeping (outside wiki/, per MASTER_CONTEXT Session-End Steps + Nick's explicit instruction)

- STATE.yml: complete overwrite. **known_issues: the WIKI SILENCE entry removed** — the flagged condition is resolved (the covering page exists); this is exactly the active-pruning case. All other array entries re-examined and kept (npm audit, ir_slot_count, finality-promotion; all six nick_pending entries; blockers; all four warnings — each still genuinely true). current_focus rewritten for this session; the 2026-07-23/24 catalog-cron 'skipped'-row expectation retained (still pending).
- MANUAL_SETUP_CHECKLIST.md: Wave 3a ADP item's constraint (4) rewritten — wiki-silent → resolved, pointing at the new page as source of record and directing the ADP build session to include it in wiki reads. Correctness update to a decision-of-record annotation, consistent with the file's own append/annotate norms.
- Log cap enforced: 5 logs existed (_14.._18); this file is the 6th → _14 condensed into ARCHIVE.md (newest-first, matching established practice) and deleted.
- PROGRESS.md: NOT updated — wiki maintenance is not one of the four milestone categories.
- ARCHITECTURE.md / DESIGN_SYSTEM.md: untouched — no app structure or token changes.
- Build files: untouched (03a's item text already accurate; nothing checked off — nothing was built).
- Push: pushed after final commit — the next Wave 3a build session may run from any environment (claude.ai/Cowork read from GitHub), and the new page + cleared flag gate that session's wiki reads; MAINTAINER.md's batch-push guidance yields to that portability need (substantial-batch criterion met: a page that unblocks the next build session).

## Assumptions / judgment calls (disclosed)

- **Confidence medium, not high**, on the new page — reasoning above; the calibration definition decided it, not doubt about the observations.
- **Reciprocal-link edits did not bump `last_updated`** on the three touched pages — only frontmatter `related:` changed, no content re-synthesis; last_updated tracks content currency.
- **MAINTAINER.md stale-status fix** folded into housekeeping though not strictly part of the ingest — a two-line correctness fix against _queue_master, within wiki/, disclosed here and in the report.
- **BLAST RADIUS: no live database operations** — the single Sleeper GET is an external read, not a prolabel DB operation; nothing touched Supabase.
- Page name `projections-endpoint` follows the category's established `{surface}-endpoint` naming convention.
- Inline backticks for endpoint paths/field names used throughout the page — matches every existing sleeper-api page; the "zero code" rule bars code blocks/implementation snippets, not identifier formatting (established practice).

## What the prompt got right / friction

- Excellent prompt: named the session type, the governing protocol files, the exact content list, the source-of-record citation requirement, and the known_issues cleanup. The only inferences needed: single-source vs discovery mode (self-evident — all queues COMPLETED), page naming, confidence level, and the related-link set.
- Friction: none material. The live re-verification materially improved the page over a transcription of the _18 log (array shape, 12th field, sentinel, provenance caveat) — cheap and worth repeating whenever ingesting a live-API source same-window.

## WIKI NOTE

WIKI NOTE: none — this session performed the wiki maintenance itself; the previously outstanding WIKI NOTE (projections-endpoint silence, flagged 2026-07-22_18) is resolved by `wiki/topics/sleeper-api/projections-endpoint.md`. Remaining candidates from earlier sessions (matchup-endpoint ADR addendum for the two signed finality operationalizations; league-endpoint bonus_rec_te / ir_slot_count evidence) are still open and tracked in their original logs/known_issues — deliberately NOT folded into this session's scope.
