# Session Log — 2026-07-22_06

**Session type:** feature-build (via BUILD_PROTOCOL autonomous mode)
**Task:** Wave 1, sole item of the "Schema — draft state (shared, not staged)" section of `01_foundation.md` — `draft_state` table migration, applied live and verified. Section complete.

---

## Files read

- `.claude/MASTER_CONTEXT.md` — auto-loaded; governs rules/stack/constraints.
- `.claude/BUILD_PROTOCOL.md` — dropped in by Nick ("read this and lets get to work"); governs the session.
- `.claude/STATE.yml` — located last session's end state: league-scoped state section complete; next item = draft_state.
- `.claude/BUILD_INDEX.md` — confirmed Wave 1 (01_foundation.md) active; roadmap unchanged.
- `.claude/MANUAL_SETUP_CHECKLIST.md` — Manual Setup Flag check: all Wave 1 items `[x]` except admin-auth `[~]`, which belongs to the integrity item, not this one. No halt.
- `wiki/index.md` + `wiki/ROUTING.md` — category identification: draft shapes are platform-behavior territory → sleeper-api + espn-api; schema-reference (complete, 3/3) has no draft/DDL page.
- `wiki/topics/sleeper-api/draft-endpoint.md` — three-endpoint layering, pick fields, roster_id-is-ownership decision, multi-draft warning, auction amount, snake-math-as-fallback-only.
- `wiki/topics/espn-api/draft-detail-response-structure.md` — mDraftDetail pick fields, teamId-is-truth decision, keeper/bidAmount semantics, live-draft polling behavior.
- `.claude/build/01_foundation.md` — the item itself.
- `supabase/migrations/20260722054425_create_player_scores_table.sql`, `20260722052726_create_matchups_table.sql`, `20260722035454_create_platform_enum.sql` — repo DDL pattern (comment style, named checks, RLS-at-creation, trigger, FK style, enum style).
- `.claude/settings.json` — confirmed the `Bash(npx supabase db push*)` allow rule.
- `.claude/COMPLETION_TEMPLATES.md` — report template + log naming rule.
- `.claude/logs/` (glob) — 5 files existed; this log is the 6th → cap triggered (see below).
- `.claude/logs/2026-07-22_01.md` + `ARCHIVE.md` — read for the cap rotation.
- `.claude/ARCHITECTURE.md` + `.claude/PROGRESS.md` — session-end updates/milestone check.

## Pre-implementation coverage map (recorded before any code was written)

| Decision | Coverage |
| -------- | -------- |
| `league_id` FK → `leagues.platform_league_uuid` | Build item text + league-identity ADR precedent |
| `platform` enum + `season_year` duplicated | Schema Rules #2/#3; N:1 duplication precedent (rosters/matchups/player_scores) |
| `pick_number` = overall 1-indexed, as-received | sleeper-api/draft-endpoint (`pick_no`) + espn-api/draft-detail-response-structure (`overallPickNumber`); both pages' ground-truth-over-snake-math Key Decisions |
| `round` as-received; nomination order in auctions | Same two pages (`round` / `roundId`) |
| Target roster = `native_roster_id` | sleeper draft-endpoint Key Decision: `roster_id` is the sole ownership field (never `draft_slot`/`picked_by`); espn Key Decision: `teamId` directly, never reconstruction |
| Drafted player = `sleeper_player_id` FK → players | Build item + schema-reference/player-identity-mapping precedent; ESPN playerId via crosswalk at ingestion |
| `source` enum values | Build item literal (`manual`/`sleeper_poll`/`espn_poll`) |
| Unique `(league_id, pick_number)` first-write-wins | Build item literal |
| Multi-draft-per-league risk | sleeper draft-endpoint (drafts array, never-deleted aborted drafts) → Clarify Q1/Q2 |
| `amount` (auction bid) | sleeper draft-endpoint (`metadata.amount`, string → coerce) + espn (`bidAmount`); included per Nick's explicit Clarify instruction |
| `native_draft_id` provenance | sleeper draft-endpoint (`draft_id`; poller selects by season/type/status); null for ESPN (no distinct draft ID in mDraftDetail) + manual |
| Keeper/draft_slot/picked_by exclusion | Both pages document the fields; excluded per Nick's Clarify (no registered v1 consumer) |
| DDL conventions (named checks, RLS at creation, cascade, trigger, timestamps) | Repo pattern from matchups/player_scores migrations |

**Declared wiki silence (at decision time, per Absolute Rule 12 — sixth consecutive session):** schema-reference (complete, 3/3) still has no league-scoped-state DDL ADR. After a real ROUTING.md/index.md search, DDL-level specifics beyond the wire principles above were wiki-silent: literal PK shape (natural composite vs. surrogate under the unique pair), enum-vs-text for `source`, finality columns or not, provenance/amount column inclusion. Resolved via Clarify + established repo precedent, not silently filled. Standing WIKI NOTE continues.

## Clarify decisions (Nick, via AskUserQuestion)

1. **Draft formats (real-league fact):** snake/linear + auction only; dynasty and multi-draft-per-season explicitly out of scope. The spec'd `(league_id, pick_number)` uniqueness is airtight. Nick's free-text answer explicitly instructed adding a numeric `amount` column for auction winning bids — this overrides the "spec-exact" selection in Q4 for that one column.
2. **PK shape:** `(league_id, pick_number)` natural composite PK (the first-write-wins constraint itself) + nullable `native_draft_id text` provenance data column (recommended option).
3. **Roster attribution:** `native_roster_id integer not null` — every pick attributed; composite FK → rosters cascade (recommended option).
4. **Extra dimensions:** none — spec-exact (recommended option); `is_keeper`, `draft_slot`/`picked_by` excluded, no registered v1 consumer. (`amount` included per Q1's explicit instruction, see above.)

## Judgment calls (mine, disclosed)

- **`source_matches_platform` check** — added from the enum's own semantics (a `sleeper_poll` write on an espn league row is incoherent), following the platform-check constraint precedent (matchups' three checks, roster_players' two). Not wiki-derived, not Clarify-asked; flagged in the completion report.
- **`native_draft_id_sleeper_only` check** — same precedent; ESPN has no distinct draft ID to store.
- **No `fetched_at`/`is_final`** — first-write-wins rows are immutable; the matchups/player_scores correction-window rationale doesn't apply. Stated pre-Clarify in chat; Nick raised no objection via the "Other" path.
- **`round integer not null`** — both platforms always supply it; the manual path supplies it from board position (Wave 3b).
- **`amount` left unconstrained numeric** (no `>= 0` check) — as-received philosophy; ingestion owns coercion/validation.

## Build

- Pre-flight prolabel collision check (read-only, live): no table or type matching `*draft*` anywhere in the shared DB. Passed.
- `npx supabase migration new create_draft_state_table` → `supabase/migrations/20260722055716_create_draft_state_table.sql`; SQL written following the matchups/player_scores comment + DDL style (named check constraints).
- Pushed via the approved `npx supabase db push` Bash rule, password sourced from `.env.local` in-shell (never echoed). Only the one new migration applied. Docker warning = CLI local catalog cache, cosmetic, same as prior pushes.

## Verification (live, read-only via Supabase connector `execute_sql`)

- Columns: all 12 present, correct types/nullability/defaults.
- PK: `PRIMARY KEY (league_id, pick_number)`.
- FKs: `league_id → leagues(platform_league_uuid)`, `sleeper_player_id → players(sleeper_player_id)`, `(league_id, native_roster_id) → rosters` — all `ON DELETE CASCADE`.
- Checks: `native_draft_id_sleeper_only`, `source_matches_platform` — both present with correct definitions.
- Enum: `draft_pick_source = {manual, sleeper_poll, espn_poll}`.
- RLS: enabled (deny-by-default; policies come with the integrity item).
- Trigger: `set_updated_at BEFORE UPDATE` attached.
- Migration history: row `20260722055716` present in `supabase_migrations.schema_migrations`.

## Log cap rotation

This log is the 6th individual file → oldest (`2026-07-22_01.md`, the rosters session) condensed into `ARCHIVE.md` and deleted, per MASTER_CONTEXT Session-End Steps.

## What the prompt got right / friction

BUILD_PROTOCOL self-location worked cleanly again — STATE.yml's pointer was exact. One UI hiccup: the multiSelect Clarify question wouldn't let Nick submit two checkboxes, so he answered via free text in the option notes; the answer itself was unambiguous. The free-text answer to Q1 partially overrode the Q4 checkbox ("spec-exact") by explicitly requesting the `amount` column — resolved in favor of the explicit instruction, disclosed in the report.

## WIKI NOTE

WIKI NOTE: (standing, sixth consecutive session) schema-reference is marked complete (3/3) but has no decision-record coverage for the league-scoped state tables' DDL — now including `draft_state` (natural-PK-as-first-write-wins pattern, source enum, provenance/amount columns, platform-coherence checks). All such decisions live only in build-file annotations and session logs. Suggest a short schema-reference addendum ADR — "league-scoped state tables" — capturing these recurring DDL conventions so Wave 2 ingestion and later waves read decisions instead of reconstructing them.

---

_End of log 2026-07-22_06_
