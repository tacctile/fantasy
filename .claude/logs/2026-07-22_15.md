# Session Log — 2026-07-22_15

**Session type:** feature-build (autonomous, BUILD_PROTOCOL.md)
**Fold declared:** 02_data_pipeline.md → "Sleeper — league-scoped sync" final 2 items:
(4) Sleeper draft-state fetch → `draft_state` with `source='sleeper_poll'`;
(5) per-league Sleeper sync orchestrator.
Fold conditions verified: same sub-section, same wiki category (sleeper-api), no named
singleton, no new external service, no open MANUAL_SETUP dependency. Next sub-section
(ESPN) is a hard stop on both wiki-category and manual-setup grounds — natural boundary.

**Wiki pages read this session:** `sleeper-api/draft-endpoint.md` (1 of 3 budget).
Category navigation via `wiki/index.md`, `wiki/ROUTING.md`, `sleeper-api/_index.md`.

---

## Item 4 — Clarify answers (Nick, this session)

1. **Draft selection policy:** filter `/league/{id}/drafts` to the league's season;
   prefer status `drafting`, then `complete`; ties within the preferred status or no
   candidate in either status → explicit error naming the candidates (never guess).
   Selected `draft_id` recorded in `native_draft_id` as provenance.
2. **Missing-player policy:** same skip-and-record ruling as matchups/player_scores —
   skip the pick row, report IDs loudly, self-heals after next daily catalog run +
   re-run (first-write-wins makes re-runs safe).
3. **Pre-draft league:** when the only season-matching draft(s) are `pre_draft`,
   succeed with zero picks (valid offseason state; lets the orchestrator include
   draft sync unconditionally).

## Item 4 — Pre-implementation coverage map (recorded BEFORE code, per Absolute Rule 12)

| Decision | Covered by |
| --- | --- |
| Endpoint walk: `/league/{id}/drafts` (array; league's single `draft_id` never trusted as exhaustive) → `/draft/{draft_id}/picks` | draft-endpoint.md § Three Endpoints / Key Decision 1 |
| Selection filter on `season` + `status`; statuses `pre_draft`/`drafting`/`paused`/`complete` | draft-endpoint.md § Draft Object Fields + Nick Clarify #1 |
| `paused` handling: falls into "no candidate in drafting/complete" → explicit error naming candidates (Wave 3b may revisit for live-draft UX) | Nick Clarify #1 (disclosed judgment call: paused not given its own carve-out) |
| Pick ownership = `roster_id`, never `draft_slot`/`picked_by` | draft-endpoint.md § roster_id Is the Ownership Field / Key Decision 2; draft_state migration comment |
| `pick_no`/`round` as-received, snake arithmetic never used to compute/override | draft-endpoint.md § Snake Draft Math / Key Decision 4; migration comment |
| Auction value = `metadata.amount`, string on wire, explicit numeric coercion; null for snake/linear | draft-endpoint.md § Auction Drafts / Key Decision 5; migration `amount` comment |
| Pick `metadata` is a point-in-time snapshot — not persisted as current player data; only `amount` consumed | draft-endpoint.md § Pick Metadata Is a Point-in-Time Snapshot |
| `player_id` opaque string incl. D/ST team-abbreviation IDs | draft-endpoint.md § Pick Object Fields (defers to players-endpoint scheme); established `asPlayerId` service convention (league-matchups.ts, from players-endpoint.md read in prior session) |
| First-write-wins persistence: insert with ON CONFLICT (league_id, pick_number) DO NOTHING (`ignoreDuplicates: true`); rows immutable, no fetched_at/is_final machinery | draft_state migration (PK comment); build item text |
| Column values: `source='sleeper_poll'`, `platform='sleeper'`, `season_year` from leagues row, `native_draft_id` = selected draft | draft_state migration; build item text |
| League lookup by (platform, native_league_id) → uuid + season_year; not-connected → explicit error | established service pattern (league-config/league-rosters/league-matchups) |
| Rosters precondition: FK (league_id, native_roster_id) → rosters requires rosters synced first; zero rosters → explicit "run sync:rosters first" error; a pick referencing a roster_id absent from the synced set → hard error (structural, distinct from the missing-player skip ruling) | draft_state migration FK; disclosed judgment call (same class as existing not-connected precondition errors) |
| Missing player → skip-and-record | Nick Clarify #2 |
| Pre-draft → succeed, zero picks | Nick Clarify #3 |
| `is_keeper`, `draft_slot`, `picked_by` not persisted (no columns — Wave 1 schema decision) | draft_state migration (column set) |
| Wire types permissive/optional with index signature; IDs never numerically coerced | established types.ts convention (players-endpoint.md, prior session) |

**Declared wiki silence:** none for item 4 — every decision above has a covering page,
a live-schema comment tracing to Wave 1's decision records, or an explicit Nick ruling
from this session's Clarify.

---

## Item 4 — verification (live, league 1257464256710856704)

- Selected draft 1257464256710856705 (status=complete, type=snake) from `/league/{id}/drafts`.
- 170 picks fetched, 170 written first run; re-run: 0 written / 170 already recorded (first-write-wins verified).
- SQL cross-checks: 170 rows, 170 distinct pick_numbers spanning 1–170 (no gaps), 17 rounds × exactly 10 picks, 10 rosters × exactly 17 picks, 0 bad provenance rows (source/platform/season_year), all rows carry native_draft_id 1257464256710856705, amount null on all rows (snake).
- Wire comparison: all 170 stored rows match Sleeper's picks payload exactly on (round, player_id, roster_id) — 0 mismatches.
- `npx tsc --noEmit` and `npm run lint` clean.

## Item 5 — Clarify answers (Nick, this session)

1. **Chain scope:** draft-state sync joins the orchestrator chain — config → rosters →
   matchups → draft (draft after rosters, per its FK). Nick-signed scope amendment to
   the item text (which predated item 4's addition to "the above").
2. **League set:** all connected Sleeper leagues every full run (idempotent re-syncs are
   cheap; no status guessing); runner accepts explicit league IDs for subset runs.
3. **Rate limiter:** global minimum-gap gate in the Sleeper client, 250ms between
   request starts (≤240/min worst case — substantial headroom below the ~1,000/min
   documented ceiling per rate-limits.md; also bounds short-window burstiness, a
   called-out gap in per-minute-average-only designs).

## Item 5 — Pre-implementation coverage map (recorded BEFORE code, per Absolute Rule 12)

| Decision | Covered by |
| --- | --- |
| Chain order config → rosters → matchups → draft | build item text (dependency order) + draft_state FK (rosters precede draft) + Nick Clarify #1 |
| Discovery = `leagues` rows where platform='sleeper' | live schema (leagues table, Wave 1) + Nick Clarify #2 |
| Per-league failure isolation: try/catch → record failed step + error → continue to next league | build item text ("isolates a single bad league response") |
| Dependency-skip semantics: a failed step skips that league's downstream steps, never other leagues | build item text (dependency order) — direct consequence |
| Client-side limiter with substantial headroom below ceiling, not near it | rate-limits.md Key Decision 1 + Nick Clarify #3 (250ms gap) |
| 429 / non-JSON / connection failure = one backoff category; exponential backoff + jitter, bounded retries, no Retry-After reliance | rate-limits.md Key Decisions 2 & 4 (already implemented in client.ts — unchanged) |
| Pacing applies to every physical request incl. retries (gate at request level, not call level) | rate-limits.md § Bursts vs. sustained averages (disclosed implementation choice) |
| Bulk-vs-interactive budget isolation: not yet applicable — no interactive Sleeper traffic exists (UI reads Supabase, never Sleeper); the global gate is the bulk budget; revisit only if interactive Sleeper calls ever appear | rate-limits.md Key Decision 5 (declared: satisfied vacuously in v1's server-side-ingestion-only architecture) |
| Deterministic league processing order (order by native_league_id) | disclosed judgment call — determinism aids log comparison; wiki silent after ROUTING/index check (no page governs orchestration ordering) |
| `week` passthrough option on orchestrator for future cron cadence; default full-season sweep | league-matchups.ts existing `week` option (prior session); cron sub-section owns cadence tuning |
| Runner `sync:sleeper`, positional league-ID args for subset runs | established scripts convention; naming not dictated by MASTER_CONTEXT (disclosed choice) |

**Declared wiki silence (item 5):** orchestration ordering/naming decisions (two rows
above) — genuine silence after checking `wiki/ROUTING.md` and `wiki/index.md`: no
category covers internal job orchestration; decided by convention and disclosed.

---

## Item 5 — build + verification (live)

- `client.ts`: added module-global pacing gate (`MIN_REQUEST_GAP_MS = 250`) at `requestOnce` entry — slot reserved synchronously before any await, so concurrent callers serialize on the event loop; retries included (rate-limits.md: bound bursts, not just averages). Header doc updated.
- `sync-orchestrator.ts`: `syncAllSleeperLeagues(db, { leagueIds?, week? })` — discovery via `leagues` where platform='sleeper' ordered by native_league_id; per-league chain config → rosters → matchups → draft; step failure records failedStep/error/skippedSteps on that league's outcome and continues to the next league.
- `scripts/sync-sleeper.ts` + `npm run sync:sleeper` (optional league-ID args; exit 1 on any failure).
- Verification: full-discovery run OK (1 league, all four steps, idempotent — 0 new picks/170 existing, ~11.6s incl. pacing). Isolation run `sync:sleeper -- 999999999999999999 1257464256710856704`: bad league FAIL at league_config (404, structured error, remaining steps skipped), good league fully OK, exit=1 — exactly the item's isolation requirement.
- `npx tsc --noEmit` + `npm run lint` clean after all changes.

## Files read this session (and why)

Protocol order: MASTER_CONTEXT (auto), BUILD_PROTOCOL, STATE.yml, BUILD_INDEX, MANUAL_SETUP_CHECKLIST, wiki/index, wiki/ROUTING, 02_data_pipeline.md. Wiki: sleeper-api/_index (navigation), draft-endpoint.md (item 4 primary), rate-limits.md (item 5 throttle decision — read BEFORE writing pacing code, per Rule 12). Source: sleeper client/types/league-config/league-rosters/league-matchups (patterns + signatures), draft_state migration (schema + first-write-wins semantics), database.types.ts (draft_state Insert grep), package.json, scripts/sync-matchups.ts (runner pattern), ARCHITECTURE.md (blast-radius inventory + sections to update), COMPLETION_TEMPLATES.md, PROGRESS.md, PROGRESS_ARCHIVE.md, logs listing (NN computation).

## Judgment calls disclosed

- Item 4: `paused` drafts fall into the explicit-error branch (not carved out like pre_draft); a completed draft with zero picks is accepted (writes nothing) rather than erroring; unknown-roster pick hard-fails (structural — distinct from the missing-player skip ruling); auction `amount` accepts a numeric wire value as drift tolerance.
- Item 5: deterministic league order (native_league_id); runner takes positional league-ID args only (week stays a service-level option for cron); orchestrator excludes the player catalog (once-per-day rule has its own cadence); runner named `sync:sleeper`.

## Session-end bookkeeping

- Build file: items 4 and 5 checked `[x]`; item 5 text amended (draft added to chain — Nick-signed scope correction). Both Sleeper sub-sections now fully checked; file stays 🟡 (ESPN + cron remain).
- STATE.yml overwritten completely; stale rate-limiter deferral removed (landed); ESPN manual-setup dependency promoted to blockers (next unchecked item halts on the Manual Setup Flag).
- ARCHITECTURE.md: tree, scripts line, client pacing note, draft-state + orchestrator Service API entries.
- PROGRESS.md: milestone entry added (Sleeper sync surface complete); oldest entry (Rule 13 governance) rolled verbatim into PROGRESS_ARCHIVE.md (5-entry cap).
- Log cap: this file is the 6th → 2026-07-22_10.md key decisions concatenated into logs/ARCHIVE.md and the file deleted.
- Blast radius: no DDL this session. Live DB writes limited to draft_state (fantasy-owned, in ARCHITECTURE.md inventory); reads on leagues/rosters/players/matchups/player_scores + read-only verification SELECTs. Only fantasy-owned tables touched.

WIKI NOTE: none

