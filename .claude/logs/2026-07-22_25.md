# Session Log — 2026-07-22_25

**Session type:** feature-build (autonomous, BUILD_PROTOCOL.md)
**Fold declared (at session start, before Clarify):** 03a_draft_assistant_static_board.md → the full
"States + resilience" sub-section (all 3 items): loading skeleton, empty states (no-ADP-ingested +
zero-filter-match, each with a clear next action), route error boundary with ESPN-gap graceful
degradation. One full sub-section; no hard stops apply (no new wiki category, both Wave 3a
MANUAL_SETUP items `[x]`, no new external service, not a live-behavior section, no named singleton).
The player-list sub-section's `[>]` bye-week item stays deferred — not part of this fold.

**Wave self-location:** 02 has no `[ ]` items (9 ESPN `[!]`, cron cadence `[~]`, finality `[>]`) →
first unchecked item is 03a's loading-skeleton item, exactly as STATE.yml predicted.

**Wiki pages read this session (1):** `league-mechanics/average-draft-position` (build-file list) —
bears on the no-ADP empty state's format semantics (format leakage rule → a format with no rows is an
explicit empty state, never another format's numbers). The other two build-file-listed pages
(league-configuration-data-model, multi-platform-adp-divergence) govern data-layer decisions already
shipped in prior folds and carry no decision for these UI-state items; Rule 12 override remains
available if a decision surfaces mid-build.

---

## Pre-implementation coverage map (recorded BEFORE any code, per Rule 12)

### Item 1 — loading skeleton
- Skeleton surface token = `--muted` ← DESIGN_SYSTEM.md Supporting Tokens table (documents `--muted`
  as "Hover/skeleton surface"). Not a wiki decision — design-system satellite governs.
- Layout/density mirrored from the real board ← existing components (draft-board-shell/header/
  board-toolbar/player-table/player-row/roster-panel structure and paddings), app-internal.
- Mechanism = route-segment `loading.tsx` ← Next.js framework convention. DECLARED WIKI SILENCE:
  ROUTING.md/index.md have no category for framework routing/UI-state patterns (checked — the
  established below-wiki-altitude category from prior sessions); governed by build item text +
  framework convention.
- Animation = CSS `animate-pulse` ← Animation Governance (prefer CSS transitions; no rAF loop).

### Item 2 — empty states
- No-ADP variants, all rendered honestly and distinctly ← service contract + wiki:
  (a) nothing ingested for source (adpSeasonYear null) ← build item text ("no ADP ingested yet for
  the current season/source");
  (b) resolved format has zero rows (adpIngestedAt null with a season present) ←
  league-mechanics/average-draft-position format-leakage rule (read this session) + draft-board.ts's
  documented no-cross-format-fallback contract;
  (c) scoringFormat null (unusable derived_config) ← service's degraded-null contract — surfaced as
  ADP-unavailable, never an invented default format.
- Next-action content (`npm run sync:adp` + daily 10:00 UTC piggyback timing) ← app-internal
  (MANUAL_SETUP decision of record 2026-07-22; 03a cron-item annotation). DECLARED WIKI SILENCE on
  empty-state UX copy — no covering category; owner-facing surface, governed by Clarify.
- Zero-filter-match state replaces the inline fallback in player-table.tsx ← build item text +
  STATE.yml carry-over note. Clear-next-action semantics (reset filters, preserve sort) = judgment
  call disclosed at build time; presentation per Clarify.
- Empty-pool state (no ADP AND no rostered players) ← board-pool contract (ADP ∪ rostered can be
  genuinely empty for a fresh league) — degrades to the no-ADP surface with sync-league guidance.

### Item 3 — error boundary + ESPN-gap degradation
- Mechanism = route-segment `error.tsx` ('use client', reset() retry) ← Next.js framework
  convention. Same declared wiki silence as item 1's mechanism.
- No raw error details in the fallback ← build item text + Access Model data-exposure posture
  (MASTER_CONTEXT) — generic copy, no error.message/stack render.
- ESPN-gap graceful degradation ← 03a Scope clause ("must not crash or degrade the whole board if an
  ESPN league's data is stale/missing") + MASTER_CONTEXT Data Source Architecture (ESPN fragile,
  isolated). Concrete surface: the data layer already degrades missing config/rosters/layout to
  nulls (structural, platform-agnostic); this item makes the degradation visible/honest at the UI
  (notice presentation per Clarify) and verifies null-path rendering, rather than adding platform
  branching (none exists anywhere in the board path, by design).
- Query errors (thrown) → boundary; missing DATA (null/empty rows) → degraded render ← the
  service's existing error contract (throws are real failures; absence is typed/null). This
  distinction is the item's load-bearing line: gaps degrade, failures bound.

## Clarify plan (per-item batches, per Folding Policy)
- Item 1: zero open questions — DESIGN_SYSTEM + existing layout fully specify it; stated plainly.
- Item 2: batch of 2 (no-ADP presentation: banner-over-rostered-list vs full-region empty state;
  next-action copy depth).
- Item 3: batch of 1 (degradation-notice visibility: silent honest-nulls vs visible notice line).

---

## Item 1 — loading skeleton

**Clarify:** zero open questions — stated plainly per protocol (DESIGN_SYSTEM.md fixes the skeleton
surface token `--muted`; the layout to mirror is the shipped board; `loading.tsx` is the framework's
own mechanism). **Built:** shadcn `skeleton` primitive added via CLI (`npx shadcn add skeleton --yes`)
— vendored file already uses `bg-muted`, exactly DESIGN_SYSTEM's documented skeleton surface, zero
edits needed. `app/(admin)/leagues/[leagueId]/draft/loading.tsx`: server component mirroring the
shell's three regions — header strip (title/badge/season left; selector/ADP-line/sign-out right),
toolbar (search w-56, six position chips, toggle chip, count), a 12-row six-column table at the live
px-3 py-2 cell density (per-column bar widths from a ROW_CELLS spec so the shimmer reads as the real
table), and the lg-up w-80 sidebar with 5 team cards. CSS `animate-pulse` only (Animation
Governance); `aria-busy` + sr-only text. **Judgment calls disclosed:** 12 rows / 5 sidebar cards
(viewport-filling counts, not data-derived); skeleton table built from divs (a table element buys
nothing with no data).

## Item 2 — empty states

**Clarify (Nick, this session):** (1) banner-over-rostered-list — the no-ADP notice renders above the
still-rendered rostered pool, never hiding real data; full-region empty state ONLY when the pool is
truly empty; (2) command+schedule next-action copy ("Run npm run sync:adp — or it lands with the
daily sync at 10:00 UTC"). **Built:** `adp-notice.tsx` — pure `deriveAdpNoticeKind(context)` returns
a discriminated kind or null: `no_adp_ingested` (adpSeasonYear null), `format_unresolved`
(scoringFormat null), `format_empty` (adpIngestedAt null with season+format present — the
format-leakage rule's explicit empty state, wiki-grounded in average-draft-position read this
session). Precedence disclosed as the HEALING order (no-snapshot first — sync:adp fixes it regardless
of config state; then config; then format hole), not severity. Warning-tier banner (role=status,
bg-card, --warning headline, muted detail, bg-well command chips via new `command-chip.tsx` — deduped
after briefly existing in two files). `board-empty-state.tsx` shared presentational (title/detail/
action slot). `player-board.tsx`: takes `context` now (shell passes it); truly-empty pool →
full-region BoardEmptyState with BOTH next actions (sync:adp + sync:league), banner + toolbar + table
suppressed — disclosed: no double-messaging, and filtering an empty pool is meaningless.
`player-table.tsx`: the player-list fold's inline zero-match fallback replaced by the designed state —
header row stays for column context, pool-size line, "Clear filters" (secondary pill) resetting
query/positions/availability but NEVER the chosen sort (disclosed); button renders only when a filter
is genuinely active (defensive — zero matches with no filter means an empty pool, which never reaches
the table).

## Item 3 — error boundary + ESPN-gap degradation

**Clarify (Nick, this session):** (1) per-surface degradation posture — each gap announces itself
where it bites (ADP banner variants, roster panel's existing lines); NO aggregate summary banner;
(2) fallback offers retry + back-to-leagues. **Built:**
`app/(admin)/leagues/[leagueId]/draft/error.tsx` ('use client' per the framework contract): generic
copy, raw error details never rendered (Access Model data-exposure posture); Next's `digest` shown as
a small log-correlation ref (disclosed: it is a hash reference, never error content); `reset()` retry
+ `/` escape link — the league selector dies with the page on error, so without the link one
persistently broken league (the future bad-ESPN-league case) would dead-end navigation away from
healthy boards. **Degradation verification (the item's second half):** confirmed the gap/failure
boundary line — data GAPS (missing config row, null layout, empty rosters, no ADP) degrade to nulls
and per-surface notices through the service's typed contract and never throw
(`computeRosterFill(null)` and `parseRosterSlotLayout(garbage)` harness-proved no-throw); only real
query/render FAILURES throw into the boundary. Zero platform branching anywhere in the board path —
a stale/broken ESPN league renders whatever rows exist through the identical code path.

## Verification

- `npm run lint` clean (after each item); `npx tsc --noEmit` clean; `npm run build` clean (all 9
  routes compile; draft route now carries loading + error segments).
- Functional harness (temporary `scripts/verify-states-resilience.tmp.ts`, tsx, deleted after run —
  READ-ONLY, no writes): 14/14 — deriveAdpNoticeKind all four branches + precedence ×2;
  computeRosterFill null-layout no-throw + need-flag correctness; parseRosterSlotLayout garbage/empty
  → null; LIVE (admin client, read-only): ≥1 connected league, board query ok, pool non-empty
  (empty-pool branch correctly not taken), healthy league raises NO false ADP notice, boundary fields
  (share_token/owner_id/native_league_id) absent from serialized result, unknown UUID → not_found.
- NOT eyeballed: all authed surfaces (no session minted — Rule 13 posture, shared auth namespace);
  visual check carried in nick_pending, updated for the new states.

## Files read this session (and why)

Protocol order: MASTER_CONTEXT (auto), BUILD_PROTOCOL, STATE.yml, BUILD_INDEX,
MANUAL_SETUP_CHECKLIST, wiki/index, wiki/ROUTING, 03a build file. Wiki:
league-mechanics/average-draft-position (fold-relevant listed page). Source: draft route page.tsx,
draft-board-shell/player-board/player-table/player-row/board-toolbar/use-player-list/roster-panel/
roster-fill/draft-board-header (layout + null-degradation behavior to mirror/verify), draft-board.ts
service (context shape + error contract), (admin)/layout.tsx + app/layout.tsx (wrapper context for
the fallback), button.tsx + skeleton.tsx (variant/token verification), scripts/sync-adp.ts (harness
client pattern), package.json, DESIGN_SYSTEM.md, ARCHITECTURE.md, PROGRESS.md, COMPLETION_TEMPLATES.md,
logs listing (NN computation) + _20 (rotation).

## Session-end bookkeeping

- Build file: all 3 states + resilience items `[x]` with item-specific annotations; Status header →
  all five sub-sections complete, sole remainder the bye-week `[>]` (file stays 🟡 — a `[>]` is not
  checked; goes 🟢 when it ships or is re-ruled).
- BUILD_INDEX: header note + roadmap row 3a ⬜→🟡 (the roadmap cell had lagged the registry).
- STATE.yml overwritten completely; every carried array entry re-examined (visual-check entry
  updated for the new states; NEW wave-order-decision entry added — no `[ ]` item exists anywhere
  self-locatable; npm-audit entry re-verified zero new packages).
- ARCHITECTURE.md: Status paragraph + directory tree (loading/error segments, four new draft-board
  components, ui/skeleton).
- PROGRESS.md: milestone entry (Wave 3a buildable scope complete — the static draft board is
  feature-complete); oldest entry (cron sub-section) rolled verbatim into PROGRESS_ARCHIVE.md.
- DESIGN_SYSTEM.md: NOT updated — zero token changes (skeleton consumes the documented --muted;
  command chips consume --well).
- Log cap: this file is the 6th → 2026-07-22_20's key decisions concatenated into logs/ARCHIVE.md,
  original deleted (done at session start, before this log was created).
- Blast radius: NO live database writes or DDL this session. Reads only: leagues/league_config/
  adp_rankings/roster_players/rosters/players via the verification harness (all fantasy-owned per
  ARCHITECTURE.md's inventory).

WIKI NOTE: none
