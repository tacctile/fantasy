# Session Log — 2026-07-22_26

**Session type:** non-code (BUILD_PROTOCOL self-located session that became a wave-order ruling + Nick-signed correctness amendment; zero app code written, no live-DB access of any kind)

**Self-location:** Session-start protocol run in full (MASTER_CONTEXT auto-loaded; STATE.yml; BUILD_INDEX; BUILD_PROTOCOL; MANUAL_SETUP_CHECKLIST; wiki index + ROUTING). Verified STATE.yml's claim against the actual build files: grep confirmed zero `[ ]` items in 01/02/03a — unchecked items exist only in 03b (42, ESPN-blocked), 04 (35), 05 (49), 06 (24). 03b is next in roadmap order but blocked → this is exactly the wave-order decision point STATE.yml's nick_pending flagged. Asked Nick directly rather than idling: he authorized **Wave 4 ahead of 3b** (options presented: 4-ahead / bye-week fold closing 03a / hold ~4 weeks).

**Fold declared (before Clarify):** Folding Policy named singleton #4 — Wave 4 scoring engine core: sub-section items 1–3 (pure `computeFantasyPoints` + unit tests + batch job); items 4–5 outside the singleton per its named artifact set. Manual Setup Flag checked: Wave 4's two checklist items are explicitly post-build real-world actions — no halt.

**Wiki pages read (3):** `schema-reference/league-configuration-data-model` (the `league_config` shape the engine would consume — raw JSONB is authoritative, `derived_config` convenience-only), `league-mechanics/ppr-half-ppr-standard-scoring` (scoring variants + points-above-replacement principle), `sleeper-api/matchup-endpoint` (read under Rule 12 to verify the raw-stats premise — the decisive read).

---

## The contradiction (found pre-Clarify, resolved at Clarify)

The scoring-engine sub-section (registered 2026-07-21) presumes Wave 2 synced raw per-player stat lines ("turns raw synced stats into `player_scores`"; scope dependency "(raw stats/rosters/matchups)"). Wave 2 as shipped (2026-07-22, one day after registration) decided the opposite: `syncLeagueMatchups` ingests Sleeper's `players_points` — already league-scored by Sleeper per each league's own settings — into `player_scores` as-received ("pure ingestion, never computation"; 3,152 rows live for the real league). Confirmed from three directions:

- ARCHITECTURE.md "Database Schema (live)": no raw-stats table exists anywhere in the fantasy-owned inventory; `player_scores` sources = platform-scored values on both platforms (ESPN as-received when unblocked).
- `sleeper-api/matchup-endpoint`: the payload carries `points`/`starters_points`/`players_points` only — no raw stat lines; "Sleeper's `points` should be treated as authoritative for Sleeper-side results." Sleeper's raw-stats surfaces exist but are "documented separately" — and no wiki page covers them (genuine silence; see WIKI NOTE).
- Timeline: the build file predates the ingestion decision by one day; the backsweep practice (Amendment Hygiene #1, added later that same day) was never applied to Wave 4's registered text. This session is that backsweep.

Item-level consequences: item 3's batch job has no input to read; item 4's pipeline wiring already exists (ingestion writes `player_scores` in the same sync pass, per-league isolated via the orchestrator); item 5's machinery (`fetched_at`/`is_final`) shipped in Wave 1/2 with only UI surfacing left. The one genuine capability platform-scoring can't provide: score rows for weeks a player was on no roster (absent from every platform payload).

## Clarify answers (Nick, this session)

1. **Engine ruling — amend; platform-scored is canon.** Alternatives presented honestly: build-with-new-raw-stats-source (major unregistered scope, wiki-silent undocumented endpoint, new table + quarantined ingestion) and pure-function-only (shelf code against the no-speculative-building ethos, flagged as such). Nick took the recommended amendment.
2. **Dispositions — as proposed:** items 1–4 `[-]` cut with dated rationale; item 5 re-worded to "surface the existing `fetched_at`/`is_final` freshness machinery in dashboard UI", kept `[ ]` and annotated as belonging with the admin UI sub-section's fold.
3. **FA-week gap — accepted for v1:** weeks a player was on no roster render honestly as not-rostered, never invented. Scope note added to the `getPlayerCard` item.

## Pre-decision coverage map (recorded before any edit — this session's "implementation" is the amendment itself)

| Decision | Covered by |
| --- | --- |
| `player_scores` ingested platform-scored, never computed | ARCHITECTURE Database Schema (live) + Service API Reference (`syncLeagueMatchups`) — shipped Nick-signed Wave 2 decision; `sleeper-api/matchup-endpoint` (players_points league-scored; Sleeper points authoritative for Sleeper-side results) |
| No raw-stats input exists for a batch job | ARCHITECTURE live inventory (no such table) + matchup-endpoint field reference (no raw stat lines in the payload) |
| Never substitute generic preset fields (`pts_std`/`pts_ppr`) for league scoring | matchup-endpoint Key Decision #4 — consistent with the cut: we ingest league-scored values, we never compute from presets |
| Pipeline wiring already exists, per-league isolated | ARCHITECTURE `sync-orchestrator` (per-league failure isolation) |
| Freshness machinery exists; only surfacing remains | ARCHITECTURE `matchups`/`player_scores` schema (`fetched_at`/`is_final`) + matchup-endpoint finality Key Decision #3 |
| FA-week gap is real and platform-wide | matchup-endpoint (`players_points` covers the roster only) + `player_scores` schema note ("free-agent scores never appear in these payloads") |
| `league_config` shape (would-be engine input; still governs future consumers) | `schema-reference/league-configuration-data-model` ADR |
| Wave-order ruling | Nick's decision — no wiki dimension (roadmap sequencing) |

**Declared silences:** none required a general-knowledge fill — the amendment removes decisions rather than making new ones. The raw-stats-surface wiki gap is recorded (WIKI NOTE) but no decision this session depended on that endpoint's content.

## Files changed

- `.claude/build/04_league_dashboard.md` — status 🟡 + ruling note; scope premise corrected (dated); sub-section amendment note; items 1–4 `[-]` with per-item rationale; item 5 re-worded `[ ]`; `getPlayerCard` FA-gap scope note; session-end line drops "scoring engine"
- `.claude/build/02_data_pipeline.md` — exclusions line superseded (backsweep)
- `.claude/BUILD_INDEX.md` — header note (wave order + amendment); Wave 4 roadmap row 🟡 + scoring clarifier; dependency note (Wave 4 ESPN-independent; 3b position unchanged); registry 04 🟡
- `.claude/MANUAL_SETUP_CHECKLIST.md` — decision of record (wave order + amendment + FA-gap acceptance)
- `.claude/STATE.yml` — full overwrite; WAVE-ORDER pending entry pruned as resolved; ESPN blocker's downstream sentence corrected (roadmap no longer stalled); new standing warning "PLAYER_SCORES IS INGESTED, NEVER COMPUTED"; bye-week entry re-framed as non-blocking; Wave-4-specific notes added to the Rule 13 and auth-surface warnings
- `.claude/PROGRESS.md` — governance milestone entry added; oldest entry (ADP pipeline) rolled verbatim to `PROGRESS_ARCHIVE.md`
- `.claude/logs/` — this file (6th of the day); `2026-07-22_21.md` condensed into `ARCHIVE.md` and deleted (5-file cap)
- `ARCHITECTURE.md` / `DESIGN_SYSTEM.md` — NOT updated (no structural or token change; no code exists from this session)

## Judgment calls / friction

- The declared fold was never built — Clarify invalidated its premise before implementation. Per the amend-and-stop hard-stop spirit, the session did NOT roll forward into the admin data-access sub-section: a fold is never extended mid-session, and that sub-section deserves its own Clarify batch fresh.
- 04 set 🟡 on the strength of item-state changes + active-wave designation, not built items — disclosed; Nick can veto back to ⬜.
- The re-worded freshness item carries a "belongs with the admin UI sub-section's fold" annotation so the next session's self-location doesn't treat one orphan item as a standalone fold anchor.
- PROGRESS entry judged milestone-worthy as a governance change (roadmap-order ruling affects every future session's self-location; precedent: the ESPN re-route entry from log _16 was recorded as a milestone). Disclosed for veto.
- Both AskUserQuestion batches kept to the protocol's pointed-question bar: the wave-order question presented STATE.yml's own three options; the amendment batch presented the contradiction with evidence and a recommended disposition rather than an open-ended "what do you want."

## What the prompt got right / what caused friction

Nick's prompt was the sanctioned minimal form ("read BUILD_PROTOCOL.md and let's get to work") — BUILD_PROTOCOL self-location worked exactly as designed, including correctly halting at a decision only Nick could make instead of guessing past it. Friction: none procedural. The one systemic observation: this is the second time (after the ADP constraint origin) that a build file registered before a decision landed carried a stale premise to build time — the backsweep practice existed but Wave 4's file wasn't in the 2026-07-22 sweep's blast radius because its trigger (the players_points ingestion decision) predated the practice itself. One-day-old governance can't retroactively sweep; this session closed the last known pre-practice gap.

## WIKI NOTE

WIKI NOTE: sleeper-api has no page covering Sleeper's raw per-player weekly stats surfaces — `sleeper-api/matchup-endpoint` explicitly references them ("documented separately from this endpoint"; the `pts_std`/`pts_ppr` preset-field warning) but no covering page exists. Not blocking (the Wave 4 amendment removed the only would-be consumer), but if a compute-from-raw-stats capability is ever revisited (full-season player-card lines, independent score verification), that endpoint needs wiki ingestion BEFORE any build session touches it — same single-source protocol used for projections-endpoint.

---

_End of log 2026-07-22_26_
