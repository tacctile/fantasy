# Session Log — 2026-07-22_37

**Session type:** feature-build (autonomous BUILD_PROTOCOL session — "read BUILD_PROTOCOL.md and let's get to work")

**Self-location:** Session-start protocol in full: MASTER_CONTEXT (auto-loaded), STATE.yml, BUILD_INDEX.md, MANUAL_SETUP_CHECKLIST.md, wiki/index.md + wiki/ROUTING.md, 03b_draft_assistant_live_draft.md. STATE.yml's pointer confirmed against the build file: first unchecked item = client-side live sync item 5 (toolbar live status indicator); next consecutive = Live-board-UI-extensions item 2 (pick/round/on-clock). Manual Setup Flag: the two Wave 3b manual items are draft-day real-time actions, not build dependencies — no halt.

**Fold declared (before Clarify):** 2 items — live sync item 5 + UI extensions item 2. Rationale: both decision-dense, live-behavior ceiling (2–3) territory, and item 2 carried a known unresolved data-source question (STATE.yml flagged slot_to_roster_id/draft_order as unpersisted) that deserved room over a third item risking tail compression. Adjacent sub-sections, identical WIKI PAGES footprint. Fold not extended mid-session.

---

## Wiki reads (3 pages, within budget)

1. `sleeper-api/draft-endpoint.md` (build file's listed page) — slot_to_roster_id as the authoritative slot→roster bridge; snake arithmetic as validation-only for RECORDED picks; reversal_round semantics; auction = nomination order; the three-endpoints/three-layers split (which turned out load-bearing — see Live Finding below).
2. `schema-reference/league-configuration-data-model.md` — confirmed derived_config's closed key set has no draft-order concept: the registered item's "snake order from league_config" premise was genuinely stale, triggering the Nick-signed correctness amendment.
3. (Second page counted the whole session; the third slot stayed unused — no further gaps required more pages.)

## Item 1 — toolbar live status indicator (live sync item 5) → [x]

**Clarify (4 questions, all recommended options Nick-signed):** placement beside the "Draft live" dot; degraded after 2 consecutive failed ticks; relative live-updating sync age (1s granularity under a minute); hidden entirely when no session active + "Paused — tab in background" while hidden.

**Coverage map (recorded pre-code):** onStatus-callback architecture ← shipped orchestration design (authority stays server-side; STATE.yml standing warning); source label = Sleeper ← the pollActiveDraft path is v1's only live source; state machine/format/placement ← Nick-signed this session; visual treatment ← DESIGN_SYSTEM token rules (muted tier, teal stays live-dot-only). UI presentation gaps = genuine wiki silence, declared at decision time, settled by Clarify.

**Shipped:** `DraftPollTicker` gains required `onStatus` reporting each EXECUTED tick (`{outcome:'success'|'failure', at}`) — skipped ticks (hidden/in-flight) and stand-down outcomes deliberately report nothing. Pure layer `live-status.ts` (live-picks pattern): `LivePollHealth`, `applyTickReport`, `isDegraded` (≥2), `formatSyncAge`. Shell aggregates + resets on session re-arm; `LiveStatusIndicator` renders via a new `statusSlot` on `DraftSessionToggle` (matches the signed "Draft live · Sleeper · synced 3s ago" layout). Lint's `react-hooks/set-state-in-effect` rejected two first-draft spots — session-epoch reset moved to React's render-time adjustment pattern; the indicator's immediate `setNow` dropped (first-second age renders from lastSuccessAt itself = truthful "0s ago").

**Verified:** 18/18 pure-logic harness (state machine transitions incl. never-synced degraded, instant recovery; format boundaries 59s/1m/59m/1h/negative clamp); tsc/lint/build clean. Harness deleted. Commit `d225e29`. **Client-side live sync sub-section now 5/5 — complete.**

## Item 2 — live-draft strip: pick/round/on-clock (UI extensions item 2) → [x]

**Grounding before Clarify:** ARCHITECTURE's schema inventory confirmed nothing persists draft order (draft_state excludes draft_slot by decision; draft_sessions is flag-only); `SleeperDraft` already types slot_to_roster_id/settings on the drafts-array objects the poll path fetches every tick.

**Clarify (3 questions, all recommended options Nick-signed):**
1. Order source = carried through the poll path (extend sync result; zero migration, zero persistence) — this choice IS the correctness amendment to the item's stale "from league_config" text (Amendment Hygiene practice 1: the stale premise predates the orchestration design).
2. Placement = slim strip between header and board, session-active only.
3. On-clock accepted as a plain-label projection (recorded picks stay ground truth; keepers/in-draft trades can shift reality; Sleeper exposes no on-clock field — Nick's leagues are redraft snake).

**Coverage map (recorded pre-code):** slot_to_roster_id (never draft_order) ← draft-endpoint; selection = shipped drafting>complete ruling; reversal_round flip ← draft-endpoint phrase "flips snake order again partway through" — DECLARED interpretation: rounds ≥ reversal_round invert plain snake (reproduces standard 3RR); linear = same order every round — DECLARED (complement of snake's "alternating" wording; wiki has no explicit linear definition); auction → no projection ← draft-endpoint (nomination order); pick = computeNextPickNumber(snapshot ∪ pending) + round = ceil(pick/league_size) ← shipped Nick-signed semantics; league_size ← league-configuration ADR; draft-complete past rounds×slots ← settings fields wiki-covered, inference disclosed; team label ← shipped rosterLabel convention (exported from player-row, not re-invented).

**Shipped:** `DraftOrderMeta` extracted in `syncLeagueDraftState` (`resolveDraftOrderMeta`), rides `DraftPollSyncOutcome.success` → `pollActiveDraft` result → ticker `onDraftOrder` (successful syncs only; failures keep last known) → shell state (stringify bail-out) → pure `projectOnClock` (live-picks.ts) → `LiveDraftStrip`. Strip shows Pick N (nextPickNumber memo), Round (omitted when league size unknown), On the clock (omitted when unknown), or "Draft complete · all N picks recorded" past the final scheduled pick.

**LIVE FINDING (wire truth):** the drafts-ARRAY element does NOT carry slot_to_roster_id — first live run returned null; raw-wire inspection confirmed the array element has only the user-keyed draft_order (which we deliberately don't use) while `GET /draft/{draft_id}` carries the map. Exactly the wiki's three-layer split. Fix: best-effort detail-endpoint fallback when the array's map is missing (one extra paced request per executed sync; a failed detail fetch degrades metadata, never the sync). Second finding from the first harness run: a partial slot map would silently misproject (map size drives every round boundary) — added a contiguity guard (keys must be exactly 1..N, else honest unknown).

**Verified:** 22/22 — pure checks (4-team snake round boundaries, 3RR flip r3/r4, linear, auction unknown + auction complete-detection, null/empty/partial maps, 1-slot degenerate map) + LIVE against the real 2025 league: `projectOnClock` matched ALL 170 recorded picks (the league's slot map is non-identity — slot 3→roster 5, slot 9→roster 3 — so this is a genuine validation, not an identity-map tautology); pick 171 → complete; picksWritten=0 (net-zero DB). tsc/lint/build clean. Harness deleted. Commit `3c0d7af`.

## Judgment calls (disclosed)

- Ticker reports only EXECUTED ticks; the indicator observes tab visibility itself for the paused label (presentational — polling authority and skip logic stay in the ticker).
- Failed sync ticks do not clear the last known draft order (order metadata is near-immutable per draft; staleness there is harmless, clearing would flicker the strip).
- Round display stays ceil(pick/league_size) (the shipped write-path rule) while projection internals use the slot-map size — identical on every real league; divergence would mean a config/draft mismatch that projection honestly can't resolve.
- `rosterLabel` exported from player-row rather than duplicated or moved — smallest honest reuse.
- PROGRESS.md not updated: sub-section completion (live sync 5/5) is not a listed milestone trigger; 03b remains 🟡 mid-file.

## Session-end

STATE.yml full overwrite (arrays reviewed; visual-check entry extended, all else re-verified current). Log cap: `_32` condensed into ARCHIVE.md and deleted (6th-file trigger). ARCHITECTURE.md updated (draft-board component inventory: 3 new files + 4 amended entries; Service API Reference: syncLeagueDraftState draftOrder + detail fallback, draft-sessions success outcome). BUILD_INDEX header updated. Both items committed separately (`d225e29`, `3c0d7af`) + this session-end commit; pushed to main.

WIKI NOTE: none (the drafts-array omission of slot_to_roster_id is consistent with the draft-endpoint page's three-layer framing — the page never claims the array elements carry the map; no correction needed).

## WIKI COVERAGE CHECK

Complete — with two declared interpretations at decision time (reversal_round flip formula; linear ordering definition), both recorded in the coverage map before code and validated against 170 real picks. No field name, value, or shape invented without a covering page or an explicit declaration.

## BLAST RADIUS

Blast radius confirmed: only fantasy-owned tables were touched this session — and only via reads plus one net-zero `draft_state` upsert (`ignoreDuplicates`, 0 rows written, verified in the harness output). No migrations, no schema changes, no writes to any other table.
