# 2026-07-21 — Session 04

**Session type:** non-code (build-file reconciliation against external research)

## Files read and why

- `.claude/MASTER_CONTEXT.md` — session-start protocol, mandatory
- `.claude/STATE.yml` — last-session state, mandatory
- `.claude/BUILD_INDEX.md` — wave roadmap and registry, mandatory
- `wiki/index.md`, `wiki/ROUTING.md` — wiki navigation, mandatory
- `.claude/build/03b_draft_assistant_live_draft.md` — target file for BPA/tier/run-detection/queue amendment
- `.claude/build/05_eye_candy.md` — target file for presentation-layer + trade/waiver amendment
- `.claude/build/04_league_dashboard.md` — read to evaluate where cross-cutting nav/IA guidance belongs; became a second target file
- `.claude/COMPLETION_TEMPLATES.md` — report template lookup
- `wiki/topics/league-mechanics/trade-value-calculation.md` — confirmed the settled trade-value math the prompt described (VBD, ~15-20% consolidation premium, dynasty/redraft split) actually matches current wiki content before treating it as settled
- `wiki/topics/league-mechanics/waiver-wire-faab-strategy.md` — confirmed FAAB strategy content exists but no scoring formula, validating the prompt's instruction to defer formula work

Wiki page count: 5, within the audit-session budget of 5 per `wiki/ROUTING.md`'s Session Budget table. The task's scope (two build files, cross-referencing trade/waiver/nav content) justified reading beyond the standard 3-page default.

## Decisions made and reasoning

**Wave ownership for trades/waivers (Part 3/4):** amended into Wave 5 rather than registering a new build file (07). Reasoning: both features are read-only analysis/recommendation surfaces over data Wave 5 already assembles (`player_scores`, `league_config`, roster state); the trade evaluator's positional-fit panel explicitly reuses Wave 5's own positional-breakdown bar component; Wave 5 is still unstarted (⬜) so the amendment costs nothing in rework; a new build file would just recreate a dependency on Wave 5's chart primitives and positional component, effectively becoming "Wave 5b" for no structural reason. This matches the Build File Amendment Norm's "genuine requirement change caught before work begins" exception, not scope-padding after registration.

**Cross-cutting navigation/IA guidance (Part 5) placement:** split across Wave 4 and Wave 5 rather than filed as a standalone note. The persistent sidebar shell, command-center home, and sticky-context mechanism were added to Wave 4 (`04_league_dashboard.md`) as a new "Admin navigation shell" section, since Wave 4 already owns "assemble the admin league dashboard page" and the base admin route conventions — it's the natural foundational layer other waves mount into, not a Wave 5 concern. Wave 5 got the cross-feature contextual-link guidance (e.g., "weak at WR" linking to waiver/trade views) and instructions to mount its six sections into Wave 4's shell rather than building its own nav frame. This was flagged as a new, previously undocumented addition to Wave 4's scope in that file's Scope section, dated and reasoned, per the Amendment Norm.

**PROGRESS.md: not updated.** Evaluated against the milestone definition (first working implementation, full checklist completion, governance change affecting all future sessions, or core infra activation) — this session amended build-file *scope*, wrote no code, and made no governance-system change. Despite substantial content added, it doesn't match any listed milestone category, so it was correctly left out per the "routine... partial progress are not milestones" rule. This required judgment since the amendment is significant in size; erred toward the letter of the milestone definition rather than a judgment call on "significance."

## Content synthesis notes

- BPA section of 03b was a genuine rewrite, not an addition — replaced the prior input-only listing (ADP + positional scarcity + league weights, no formula) with the full VORP/VBD mechanism, ADP-calibrated replacement level, dynamic per-pick recompute, and an explicit hard constraint that need must stay a separate signal from base value (never blended/multiplied into the ranking) — this was called out in the prompt as something research was unanimous and strong on, so it was written as a "hard design constraint... not just a suggestion" rather than a soft guideline.
- Tier-cliff and run-detection were net-new checklist sections in 03b — the prompt noted these existed only at wiki level or nowhere at all; treated as genuinely new build scope, not an amendment to something previously specified.
- Queue/auto-pick was entirely new scope with zero prior documentation anywhere — added as its own checklist section, single-platform (Sleeper) per the prompt's explicit instruction not to build ESPN/Yahoo fallback logic for it.
- For Wave 5's presentation layer: did not touch or reinterpret the luck or playoff-clinch calculation logic anywhere — confirmed by re-reading the existing checklist items before editing; only display/visualization checklist items were rewritten. The playoff section's language was written carefully to explicitly reject any probability-style visual, since the underlying mechanism is deterministic clinch/eliminate, not simulation-based — a probability-shaped display would misrepresent the actual computation.
- Added a project-scoped chart anti-pattern note to Wave 5's "Shared foundations" section (radar, gauges, pie-for-comparison, scenario trees, opaque composite scores, >4-series spaghetti lines, decorative 3D) since this was described as unanimous across all 6 in-season-analytics research passes — treated as a standing constraint applying to "every visualization built in this wave," not just the four originally-scoped chart types.
- Waiver/FAAB: explicitly did NOT invent a scoring formula, per direct instruction. Marked the formula item as `DEFERRED` in the checklist itself (not just prose) so a future build session cannot accidentally check it off without the formula existing, and added a corresponding `nick_pending` entry in STATE.yml pointing back to this exact checklist item.

## Assumptions made where the prompt was ambiguous

- The prompt didn't specify exactly where Part 5 (cross-cutting nav) should live — inferred Wave 4 vs. Wave 5 split from reading both build files' existing scope statements rather than defaulting to "add it to Wave 5" (which the prompt's structure might have implied by proximity). This is a judgment call worth flagging: if Nick disagrees with putting the nav shell in Wave 4, that's a one-file move (delete the "Admin navigation shell" section from `04_league_dashboard.md`, fold it into `05_eye_candy.md` instead) since nothing downstream depends on the split yet.
- Treated "amend Wave 5" (the prompt's own recommendation for Part 3/4) as requiring my own independent confirmation rather than just executing it verbatim — reasoning was given explicitly per the prompt's requirement #1 ("make the call and state your reasoning").

## What the prompt got right / caused friction

- Prompt was extremely well-specified — concrete formulas, explicit constraints ("do NOT invent the formula," "do NOT touch DESIGN_SYSTEM.md"), and explicit ordering instructions made this close to zero-ambiguity execution.
- Minor friction: Part 5's guidance was written as general principles ("persistent sidebar," "command-center home") without saying which build file should own it, requiring a placement judgment call (see above).

## WIKI NOTE

none — both consulted wiki pages (`trade-value-calculation.md`, `waiver-wire-faab-strategy.md`) matched the prompt's description of settled vs. unsettled content exactly; no discrepancy found.
