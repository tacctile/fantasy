# Session Log — 2026-07-22_34
**Session type:** feature-build (autonomous BUILD_PROTOCOL session — Nick: "read this and lets get to work")
**Scope:** Wave 3b, `03b_draft_assistant_live_draft.md` → "Active-draft polling orchestration (Sleeper)" sub-section, declared fold: all 3 items (`draft_sessions` flag + start/stop control; elevated poller cadence; sync-run recording). The sub-section is named in the Folding Policy's live-behavior ceiling (cap 2–3 items) and is exactly 3 items — the fold is the full sub-section, nothing more. Client-side live sync (the adjacent sub-section) explicitly NOT in this fold.

---

## Session start / self-location

- Reads in protocol order: MASTER_CONTEXT (auto-loaded), BUILD_PROTOCOL, STATE.yml, BUILD_INDEX, MANUAL_SETUP_CHECKLIST, wiki/index, wiki/ROUTING, 03b build file. Self-located exactly where STATE.yml pointed: 03b's first unchecked item opens the orchestration sub-section.
- Manual Setup Flag: Wave 3b's two open `[ ]` items ("be present to toggle draft active during the real draft"; "live judgment call on ESPN rate limits") are explicitly in-the-moment draft-day actions the checklist itself says need no advance setup — the first is literally what this fold's control enables. Nothing halted.
- Capacity-collision surfaced up front (Amendment Hygiene practice 2): item 2's "e.g. every 5 seconds" collides with the documented Hobby 2-crons-daily cap — the Wave 2 sync-draft route was deliberately left unscheduled for exactly this reason. Carried into item 2's Clarify as a known constraint with options, not a mid-build surprise.
- Wiki reads (2 of 3 budget): `sleeper-api/draft-endpoint` (the build file's listed page — polling/pick-shape grounding, already realized in the Wave 2 service this fold triggers) and `schema-reference/league-identity-and-scoping` (the `league_id` FK target and scoping conventions for the new table).
- Source reads: cron sync-draft route, `services/sync-runs.ts`, `services/sleeper/draft-state.ts`, `services/draft-picks.ts` (validation/typed-result conventions), draft page + shell + header + actions.ts (control placement + server-action auth pattern), sign-out button (action-call pattern), sync-runs migration (SQL conventions), globals.css (teal token confirmation).

## Clarify (per-item batches, all Nick-signed)

**Item 1 batch (×3):** control placement = draft board toolbar (the screen he's on during a draft); stale-flag safety = 6-hour soft TTL computed at read time from `activated_at` (no job mutates the row; re-activation resets the clock); activation runs one immediate inline draft-state sync (failure reported, never rolls back the flag).

**Item 2 batch (×2):** elevated polling mechanism = client-driven trigger — the open admin board ticks an auth-gated server action; server re-checks the TTL-aware flag before any Sleeper request (a stale tab can never force polling); rejected alternatives presented: chained 300s server loops (function-hours burn, hard to stop) and external schedulers (~1min granularity, can't do 5s). Cadence = 5 seconds (build file's figure; ~720 polls/hr, trivial against the ~1,000 req/min ceiling and the 250ms pacing gate).

**Item 3 batch (×1):** record EVERY executed poll run in `sync_runs` as specced (~2,100 rows per 3-hour draft accepted as fidelity); framed the volume honestly with an eventful-only alternative (would have needed a Nick-signed amendment) — Nick chose as-specced. Declined ticks (flag off/stale) make no Sleeper request, are not poll runs, and record nothing — decided from the item text's own definition, disclosed in the question.

## Pre-implementation coverage maps (recorded before each item's code)

- **Item 1 (`draft_sessions` + control):** table keyed by `league_id` ← item text + `schema-reference/league-identity-and-scoping` (FK target = `leagues.platform_league_uuid`, never a native ID); no `platform`/`season_year` duplication ← league_config 1:1-extension precedent (parent row carries both) + sync_runs operational-telemetry precedent (Schema Rule #3 scope); separate-table-not-column rationale ← item text (Wave 4 spectator SELECT on leagues/league_config); RLS shape ← established `fantasy_owner_all`/`is_fantasy_admin()` pattern; TTL/activation semantics ← Nick's Clarify (declared wiki silence — app-internal operational state, no covering category in ROUTING/index); collision check ← Rule 13 procedure (live `list_tables`: 62 public tables, no `draft_sessions` in any foreign family). Check constraint `active_requires_activated_at` = platform judgment call (the TTL computation needs a timestamp whenever the flag is on).
- **Item 2 (elevated cadence):** poll mechanics ← the shipped Wave 2 `syncLeagueDraftState` (grounded in `sleeper-api/draft-endpoint`, re-read this session) — this fold triggers it, never reimplements it; trigger architecture + cadence ← Nick's Clarify (declared wiki silence on scheduling mechanisms — below-wiki-altitude platform territory, Hobby constraint documented in ARCHITECTURE); rate posture ← `sleeper-api/rate-limits` knowledge already encoded in the client's global 250ms gate (no new Sleeper-facing code).
- **Item 3 (recording):** row shape/status vocabulary ← existing `sync_runs` schema + `services/sync-runs.ts` (`draft_poll` source already registered by Wave 2); recording granularity ← Nick's Clarify; best-effort posture ← `recordSyncRunSafely` precedent (tracking never masks the sync's own outcome).

## Built

- **Migration `20260723001620_create_draft_sessions_table.sql`** — `draft_sessions` (league_id PK/FK cascade, `is_draft_active` default false, `activated_at`, `deactivated_at`, timestamps, `active_requires_activated_at` check), RLS + `fantasy_owner_all`, `set_updated_at` trigger. Pushed live; `npm run gen:types` regenerated (+35 lines).
- **`src/services/draft-sessions.ts`** — `getDraftSessionState` (TTL-aware; no row = valid inactive state; `isStale` distinguishes expired from stopped), `activateDraftSession` (upsert + inline recorded sync via shared `runRecordedDraftPoll`), `deactivateDraftSession` (idempotent update; no row = already inactive), `runActiveDraftPoll` (items 2+3 server side: authoritative TTL-aware active check → declined ticks return `inactive` with zero Sleeper traffic and zero rows; executed polls record a `sync_runs` `draft_poll` row success or failure). `DRAFT_ACTIVE_TTL_HOURS = 6` exported. UUID-precheck + `league_not_found` typed error per draft-picks precedent.
- **`actions.ts` (draft route)** — `startDraftSession` / `endDraftSession` / `pollActiveDraft`, each with the gate INSIDE the action (`getAdminAuthState` → typed `unauthorized`, zero data) + RLS second wall, writes through the RLS server client; `pollActiveDraft` revalidates the draft path only when new picks landed.
- **`src/components/draft-board/draft-session-toggle.tsx`** — toolbar Start/End control: teal (`--primary`) pulsing "Draft live" indicator (teal = interactive/live token rule), "Session expired" muted text for the stale state, destructive-variant End button, non-blocking notice when the activation sync fails (flag stays on).
- **`src/components/draft-board/draft-poll-ticker.tsx`** — renders nothing; 5s interval while mounted AND session active; in-flight guard (slow responses never stack), stands down on any non-`polled` outcome, transient failures just wait for the next tick; `router.refresh()` only when picks were written. Deliberately trigger-only — board-state merging, optimistic UI, and the live status indicator remain the client-side live sync sub-section's scope.
- **Wiring:** page.tsx fetches session state alongside board data (parallel), threads it shell → header (toggle) + shell (ticker). Cron sync-draft route JSDoc updated to record the now-decided trigger architecture (route = manual/fallback sweep; normal cadence = the daily league-state cron chain — no code change needed for "falling back to its normal cadence": nothing extra runs when no draft is live).

## Verification

- tsc clean, eslint clean, `npm run build` clean (no new routes; ticker/toggle compile into the draft route's client bundle).
- **Live harness** (temporary `scripts/verify-draft-sessions.ts`, tsx against the real league — deleted after the run): **37/37 PASS.** Highlights: no-row read = inactive/not-stale; malformed + unknown league → `league_not_found`; activate → flag on + `activated_at` set + inline sync success (170 picks already recorded / 0 written — the immutable board honored) + `sync_runs` row delta +1; TTL aged to 7h → reads inactive AND stale; re-activation resets the clock; deactivate idempotent with `deactivated_at` stamped; DB check constraint rejects active-without-activated_at; anon publishable-key client: zero rows on read, insert RLS-rejected, `league_not_found` through both `getDraftSessionState` and `runActiveDraftPoll`; declined tick (flag off) records nothing; executed tick records exactly one row; TTL-lapsed tick declined with nothing recorded.
- NOT browser-eyeballed: the toggle/ticker live behind the auth wall (no session minted — Rule 13 posture, same as prior folds); added to Nick's standing visual-check list in STATE.yml. The race/isolation tests mandated for live-behavior sections (concurrent manual+poll writes) remain registered in the Resilience sub-section — this fold's live checks covered the orchestration semantics themselves.

## Assumptions / judgment calls (beyond the coverage maps)

- `deactivated_at` column added as a cheap audit anchor (last explicit stop) — not in item text but not speculative scope; the row is the session record.
- Activation sync on a non-Sleeper league returns typed `skipped_non_sleeper` (03c extends the poll path; no ESPN league can be connected today).
- Multi-tab duplicate ticks accepted as harmless by design (first-write-wins + 250ms pacing gate) rather than adding a server-side min-gap guard — disclosed in item 2's Clarify option text.
- Poll interval const lives in the ticker component (client) rather than the service — importing the server service for a number would drag server-only modules toward the client bundle; the server does not enforce cadence, it enforces the flag.
- The ticker's `router.refresh()` on picks-written is deliberately minimal freshness (server components re-render) — not the live-sync merge machinery, which stays next-fold scope.

## What the session got right / friction

- STATE.yml's self-location note was exact (including the pre-flagged collision check and the live-behavior ceiling note); the capacity-collision practice worked as designed — the Hobby constraint entered Clarify as options, not a mid-build surprise.
- The Wave 2 seams (`syncLeagueDraftState`'s typed result, `recordSyncRunSafely`, the unscheduled route) fit this fold with zero changes to any Wave 2 module — the deferred-cadence decision from the cron session resolved cleanly.
- Friction: none material. The `supabase migration new` CLI stamped a UTC filename (20260723...) while the session date is 2026-07-22 local — cosmetic, noted for anyone diffing dates.

## WIKI NOTE

WIKI NOTE: none — no wiki content appeared missing, outdated, or incorrect this session. (The scheduling-mechanism and TTL silences are app-internal platform territory the wiki deliberately doesn't cover; declared at decision time, not gaps.)
