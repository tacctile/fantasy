# Session Log — 2026-07-21_06
**Session type:** feature-build (autonomous, via BUILD_PROTOCOL.md)
**Scope:** 01_foundation.md — "Schema — league + config" items 1–2, folded with Nick's approval: (a) `leagues` table migration, (b) `share_token` generation function + default migration

---

## Session start

- Self-located via STATE.yml → 01_foundation.md → first unchecked item: "Migration: leagues table".
- Manual Setup Flag check: all Wave 1 items `[x]` except admin-auth `[~]`, which belongs to the later Schema–integrity item. No halt.
- Log cap triggered: 5 logs existed for today; 2026-07-21_01.md condensed into ARCHIVE.md (created) and deleted before this file (the 6th) was created.

## Clarify answers (Nick, via AskUserQuestion)

1. **DB push:** add a project-level Bash allow rule for `npx supabase db push*` (done — `.claude/settings.json` created), then Claude pushes directly and verifies live. Update MANUAL_SETUP_CHECKLIST.md and STATE.yml `nick_pending` to reflect automated pushes enabled.
2. **Owner column:** nullable FK now — `owner_id uuid NULL REFERENCES auth.users(id)`, backfilled when the integrity session creates the fantasy admin auth user.
3. **Fold:** yes — leagues table + share_token generation in one session (BUILD_PROTOCOL adjacent-trivial-items edge case, same as prior fold approvals).
4. **Display name column:** `name` (matches Sleeper's top-level `name` field, stored as-received).

## Pre-implementation wiki coverage map (Absolute Rule 12 — recorded BEFORE any code)

Wiki pages read this session (2 of 3 budget):
1. `wiki/topics/schema-reference/league-identity-and-scoping.md` (ADR, confidence high)
2. `wiki/topics/sleeper-api/league-endpoint.md` (domain-knowledge, confidence high)

### Item A — `leagues` table migration

| Decision | Covering source |
| --- | --- |
| PK `platform_league_uuid` uuid, internally generated, never a native ID | schema-reference/league-identity-and-scoping (ADR Decision section) |
| UUID generation mechanism = `gen_random_uuid()` default | **Wiki silent — declared.** ADR mandates "internally-generated UUID" but not the mechanism; searched ROUTING.md/index.md, no covering page. Core Postgres function chosen (general knowledge, declared at decision time). |
| `platform` enum not null | MASTER_CONTEXT Schema Rule 2 + existing `20260722035454_create_platform_enum.sql` |
| `season_year` column present on every league record | ADR ("every league record carries `platform`, `season_year`, `native_league_id`") + Schema Rule 3 |
| `season_year` type = integer | **Wiki silent — declared.** Grepped `wiki/topics/schema-reference/` for `season_year`: only the ADR line naming the column, no type. Sleeper returns season as string ("2026"), ESPN as integer; internal normalized integer chosen (general knowledge, declared at decision time). |
| `native_league_id` text not null, stored as-received, never a join key | ADR + build file item text; sleeper-api/league-endpoint confirms `league_id` is a string |
| `previous_platform_league_uuid` nullable self-FK, populated by walking Sleeper's `previous_league_id` chain at ingestion, null for ESPN | ADR (Decision + Rationale); sleeper-api/league-endpoint confirms chain semantics and breakability |
| Self-FK ON DELETE SET NULL | **Wiki silent — declared.** No covering page for FK delete behavior; deleting a predecessor-season row must not cascade-delete successors (general knowledge, declared at decision time). |
| `name` text | Build file ("display name") + Nick's Clarify decision; source field confirmed as top-level `name` on sleeper-api/league-endpoint |
| `share_token` text not null unique | Build file item + MASTER_CONTEXT Schema Rule 6 |
| `owner_id` uuid null REFERENCES auth.users(id), ON DELETE SET NULL | Nick's Clarify decision (shape); **wiki silent on delete behavior — declared** (leagues must survive auth-user deletion; general knowledge, declared at decision time). Shared-auth-namespace constraint noted (ARCHITECTURE.md Shared-Database Constraints). |
| Unique `(platform, native_league_id, season_year)` | ADR Rationale: "a single row per platform-season-league combination" — uniqueness is the direct schema expression of that sentence |
| `created_at`/`updated_at` timestamptz + shared `set_updated_at()` trigger | Established pattern from `20260722035457_create_players_table.sql` (function already exists) |
| RLS enabled at creation, no policies yet (deny-by-default) | Prior migration pattern + build file: owner-write policies are the Schema–integrity item's scope |
| No extra indexes beyond PK/unique constraints | Build file: index item is the Schema–integrity section's scope; not pulled forward |

### Item B — `share_token` generation migration

| Decision | Covering source |
| --- | --- |
| Postgres function using pgcrypto `gen_random_bytes` | Build file item text (explicit) |
| pgcrypto availability | Verified live pre-implementation: installed v1.3 in `extensions` schema (read-only pg_extension query) — no CREATE EXTENSION needed; function references `extensions.gen_random_bytes` |
| Token = 32 random bytes, hex-encoded (64 chars) | **Wiki silent — declared.** Searched ROUTING.md/index.md — no page covers token format. Build file requires "cryptographically random, unguessable"; 256 bits entropy, hex is URL-safe (general knowledge, declared at decision time). |
| Column DEFAULT (not trigger) | Build file offers "trigger or column default"; default chosen — simpler, and NOT NULL + default covers every insert path |
| `set search_path = ''` on the function | Established pattern (`set_updated_at()`) + Supabase security convention |
| Revocable/regeneratable: no immutability constraint, regenerate = plain UPDATE | Build file item text (explicit) |

**WIKI COVERAGE CHECK: gap found and flagged** — every gap above was searched against ROUTING.md/index.md at decision time and declared here before implementation; no post-hoc fills. (Initially drafted as "complete"; corrected to "gap found and flagged" after re-reading COMPLETION_TEMPLATES.md's definitions — declared genuine-silence fills mean the second value, not the first.)

---

## Implementation notes

- `.claude/settings.json` created (project-level, committed): `permissions.allow: ["Bash(npx supabase db push*)"]`. Validated as JSON via node. The rule worked this same session — dry-run and real push both executed without a manual handoff.
- pgcrypto confirmed live pre-implementation: v1.3 in `extensions` schema (with uuid-ossp 1.1) via read-only pg_extension query — migration therefore references `extensions.gen_random_bytes` fully qualified and creates no extension.
- Migration files created via `npx supabase migration new` (timestamps 20260722044457 / 20260722044500, UTC), SQL written in the established style of the identity-foundation migrations (lowercase, schema-qualified, header comment carrying the constraint rationale, RLS enabled at creation, shared `set_updated_at()` trigger reused).
- `name text not null` — nullability not specified by build file or wiki; a league always has a display name on both providers (Sleeper top-level `name`; ESPN settings name). Minor shape call, noted here.
- Push: `npx supabase db push --dry-run --yes` showed exactly the two new migrations (stub strategy healthy, no foreign-history complaints), then the real push applied both. `SUPABASE_DB_PASSWORD` sourced from `.env.local` inside the shell command — never echoed into the transcript. Harmless CLI warning about a Docker-based local catalog cache (Docker Desktop not running) — remote push unaffected.
- Live verification (read-only): all 10 columns with expected types/nullability/defaults; PK + unique (platform, native_league_id, season_year) + unique share_token; both FKs with on delete set null; RLS enabled, 0 policies; set_updated_at trigger present; generate_share_token() returns 64-char lowercase hex, distinct per call; both rows present in supabase_migrations.schema_migrations.
- Log cap executed: `.claude/logs/ARCHIVE.md` created with 2026-07-21_01's condensed decisions; original deleted.
- Session-end file updates: build file items checked with factual annotations; MANUAL_SETUP_CHECKLIST Wave 1 push note updated (allow rule resolved, handoff retired); ARCHITECTURE.md Status/link-state/schema inventory updated (leagues + generate_share_token added, trigger note now "all three tables"); STATE.yml fully overwritten (nick_pending allow-rule entry dropped as resolved; ADP + waiver-formula entries kept; npm-audit known_issue and shared-DB warning re-examined and kept). PROGRESS.md not updated — partial section progress, no milestone threshold met. DESIGN_SYSTEM.md untouched — no token changes.

## Verification

- Dry-run push: exactly 2 migrations listed, nothing else
- Real push: both applied cleanly
- Live schema queries: columns, constraints, FK delete rules, RLS, trigger, function output, migration history — all as specified above
- `.claude/settings.json`: valid JSON, rule effective in-session

## WIKI NOTE

WIKI NOTE: schema-reference/league-identity-and-scoping.md fully specifies league identity semantics (UUID PK, native ID handling, continuity chain) but is silent on implementation-level schema mechanics: UUID generation mechanism, season_year column type, FK on-delete behaviors, and share_token length/encoding. These were filled with general knowledge, declared at decision time (see coverage map). If Nick wants these wiki-governed, a small addendum to the ADR (or the league-configuration-data-model page) could record them as decisions of record; alternatively they can be treated as below-wiki-altitude implementation detail — flagging so the choice is deliberate.
