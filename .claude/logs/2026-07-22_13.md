# Session Log — 2026-07-22_13
**Session type:** feature-build (autonomous, via BUILD_PROTOCOL.md)
**Scope:** 02_data_pipeline.md → "Sleeper — client + player catalog" — full sub-section fold (3 items): typed server-only Sleeper HTTP client; full player-catalog fetch + bulk upsert into `players`; absent-from-catalog inactive marking. First Wave 2 session.

---

## Session start

- Protocol-order reads: MASTER_CONTEXT.md (auto-loaded), BUILD_PROTOCOL.md (Nick's drop-in), STATE.yml, BUILD_INDEX.md, MANUAL_SETUP_CHECKLIST.md, wiki/index.md, wiki/ROUTING.md, build/02_data_pipeline.md, ARCHITECTURE.md (Rule 13 blast-radius inventory + conventions), players migrations (column ground truth).
- **Fold declared in chat before Clarify:** the full "Sleeper — client + player catalog" sub-section (3 items) as one independently verifiable artifact — verified by a live catalog sync against real Sleeper + production `players`. Nothing beyond; league-scoped sync waits for next session.
- **Judgment call disclosed at declaration:** this fold is the first-ever live Sleeper call, and "first touch of a new external service" is a Folding Policy hard stop. Read as barring a fold from *extending past* a new-service boundary mid-session — not as barring the deliberately scoped first-touch unit itself: the wave's scope line and STATE.yml note (c) order Sleeper's trivial no-auth case first, and the policy's own rationale says an HTTP client with no consumer isn't independently verifiable. Nick was offered the shrink-to-item-1 alternative in the declaration and did not take it.
- **Manual Setup Flag check:** all Wave 2 items still `[ ]` open; none is a dependency of this fold (catalog fetch is league-independent and unauthenticated). Sleeper league ID becomes a hard dependency at the next sub-section (league-scoped sync) — flagged for nick_pending. ESPN items and the cron secret gate later sub-sections. No halt.
- Log cap: this file is the 6th individual log — `2026-07-22_08.md` condensed into `ARCHIVE.md` (newest-first) and deleted.

## Wiki pages read (3-page budget + 2 Rule 12 coverage-check overrides)

- `sleeper-api/players-endpoint` — mandatory for this fold: dump shape (map keyed by ID), once-daily rule, validation-before-swap, permissive parsing, D/ST abbreviation IDs, metadata retention.
- `sleeper-api/rate-limits` — backoff policy (exponential + jitter, bounded), throttle-signal taxonomy (429 = non-JSON = connection failure), once-daily dump rule as the most load-bearing contract.
- `sleeper-api/player-data-quirks` — `status`/`injury_status` independence (bears on what the catalog upsert stores and what it must NOT infer).
- Coverage-check reads (Rule 12 overrides the budget): `schema-reference/player-identity-mapping` (checked for a players presence-flag decision — the ADR is silent on it) and `sleeper-api/authentication` (base URL `api.sleeper.app/v1`, no-credential client design, no 401/403 taxonomy, string IDs — read specifically to avoid inventing the base URL from general knowledge).

## Clarify — item 1 batch (answers received before item 1 code)

- **Module home:** `src/services/sleeper/` (Nick chose the recommended option). Fixes the Wave 2 service-directory convention; ESPN later gets the structurally separate sibling `src/services/espn/`.
- **Verification invocation:** tsx devDependency + `scripts/sync-players.ts` + `npm run sync:players` (Nick chose the recommended option). Durable manual-sync runner; the later cron route calls the same service function.

## Pre-implementation coverage map — item 1: typed Sleeper HTTP client (recorded before code)

| Decision | Coverage |
| -------- | -------- |
| Base URL `https://api.sleeper.app/v1` | `sleeper-api/authentication` (coverage-check read — not invented) |
| No auth headers / credential logic anywhere in the client | `sleeper-api/authentication` Key Decision |
| Error taxonomy: no 401/403 on this API; 404 = ordinary not-found; only volume-based throttling | `sleeper-api/authentication` (Error Behavior) |
| Throttle-signal classification: 429, non-JSON 2xx body, and connection-level failure/timeout are ONE backoff category | `sleeper-api/rate-limits` Key Decision |
| Backoff: exponential + jitter, bounded retry count; never depend on `Retry-After` | `sleeper-api/rate-limits` Key Decision |
| 404 never retried | `sleeper-api/authentication` (not-found is a definitive answer, not a transient) |
| IDs handled as opaque strings end-to-end, no numeric coercion | `sleeper-api/authentication` + `players-endpoint` Key Decisions |
| Permissive response handling (never reject unrecognized fields) | `players-endpoint` (Field-Schema Drift) |
| League IDs treated with credential-level care in logs/errors | `sleeper-api/authentication` Key Decision — error objects carry the request path for server-side diagnosis only; module is server-only and Vercel logs are private (disclosed judgment call on the mechanism) |
| Module location `src/services/sleeper/`, named exports, `@/` alias, no barrel in a new directory | Nick's Clarify decision + MASTER_CONTEXT Code Conventions |
| Timeout values (10s default, 60s catalog override) | **WIKI SILENT** — searched ROUTING.md/index.md; no page fixes numeric timeouts, and rate-limits explicitly calls exact numeric targets "a design choice rather than a documented figure." Declared here at decision time; constants disclosed as judgment calls. |
| Retry constants (3 attempts, base 1s, full jitter) | **WIKI SILENT** on numbers (policy shape is wiki-fixed, constants are not) — same declaration as above. |
| Server-only enforcement mechanism (JSDoc + server-only import discipline; NOT the `server-only` npm package, which would throw under the tsx runner outside a React server context) | **WIKI SILENT** — app-infrastructure concern, no covering category. Declared; disclosed judgment call. |
| General substantial-headroom rate limiter (rate-limits Key Decision) | **DEFERRED, disclosed** — this fold's call pattern is one request per day; the limiter's real design constraints (multi-league fan-out, bulk-vs-interactive isolation) arrive with league-scoped sync next session. Deferral noted here and in the report, not silently dropped. |

## Item 1 — build notes

- `src/services/sleeper/client.ts` — `sleeperGet<T>()` + `SleeperApiError` (kind: timeout/network/throttled/not_found/http/invalid_response; `isThrottleSignal` groups 429/non-JSON/connection-failure per the rate-limits page). Bounded retries (3 attempts), exponential backoff + full jitter (base 1s), 404 never retried, 5xx retried. Constants disclosed as judgment calls (wiki fixes policy shape, not numbers). No barrel `index.ts` (new directory — convention forbids creating one).
- `src/services/sleeper/types.ts` — `SleeperPlayerRecord` (all fields optional + unknown-preserving index signature per Field-Schema Drift) and `SleeperPlayersCatalog` (map keyed by ID, not array).
- `npx tsc --noEmit` clean after item 1.
- tsx installed as devDependency (3 packages; npm audit unchanged at the pre-existing 6 vulns — 4 moderate / 2 high, same as known_issues).

## Clarify — items 2+3 batch (answers received before their code)

- **Snapshot-retention wiki conflict (item 2):** Nick ruled build-file scope wins — NO snapshot/diff table. Upsert in place + inactive marking; each player's full raw record still lands in `players.metadata` per sync (current snapshot preserved per player; only day-over-day history dropped, consistent with v1 rejecting historical views). Conflict flagged in the completion report per ROUTING.md conflict rules.
- **Catalog-presence mechanism (item 3, wiki-silent — declared):** Nick chose flag + last-seen: `is_active_in_catalog boolean not null default true` + `catalog_last_seen_at timestamptz` on `players`. Names deliberately catalog-scoped so they can't be confused with Sleeper's own `status` field (which has its own 'Inactive' value meaning something else).

## Pre-implementation coverage map — items 2+3: catalog sync + inactive marking (recorded before code)

| Decision | Coverage |
| -------- | -------- |
| Endpoint `/players/nfl`; response is a map keyed by `player_id`, not an array | `players-endpoint` |
| Fetch at most once per day | `players-endpoint` + `rate-limits`. Enforcement this fold: manual invocation only (the runner); scheduled enforcement is the cron sub-section's own item, and the sync-run tracking table (also that sub-section) is the proper substrate for a programmatic guard. Disclosed judgment call: no hard in-service guard yet. |
| Validate before persisting: top-level object shape, plausible record count, sample core-field presence, non-truncated body | `players-endpoint` (refresh validation). Numeric constants are judgment calls (wiki gives none): absolute floor 5,000 records; when the table is non-empty, fetched count must be ≥ 80% of existing row count; sample = 50 records, majority must carry a core field. Disclosed. |
| Validation/fetch failure leaves last known-good rows untouched (no writes before validation passes) | `players-endpoint` (atomic-refresh guidance, adapted: validate-then-upsert; the mark-absent pass runs only after a fully completed upsert) |
| Upsert keyed on the catalog map key (`sleeper_player_id`), never record fields, never names | `players-endpoint` + Schema Rule 4 + identity ADR |
| Column mapping to Wave 1 columns; full raw record into `metadata` jsonb | Wave 1 players migrations (ground truth) + `players-endpoint` field inventory; metadata-holds-full-raw-record satisfies preserve-unrecognized-fields guidance — disclosed judgment call |
| Wire-value type guards (drift-tolerant: non-string/non-array values null out rather than crash) | `players-endpoint` (Field-Schema Drift) — mechanism is a disclosed judgment call |
| D/ST abbreviation IDs (`'DET'`) flow through as ordinary opaque strings | `players-endpoint` + identity ADR |
| No numeric coercion of any ID | `authentication` + `players-endpoint` Key Decisions |
| `status` / `injury_status` stored independently, never conflated or derived from each other | `player-data-quirks` |
| No `bye_week` stored | `player-data-quirks` + Wave 1 migration comment |
| No snapshot/diff table | Nick's Clarify ruling (wiki conflict resolved; flagged in report) |
| Catalog fetch timeout 60s; upsert chunk size 500 | **WIKI SILENT** (searched ROUTING.md/index.md — app-infrastructure, no covering category) — disclosed judgment calls |
| Inactive mechanism: `is_active_in_catalog` + `catalog_last_seen_at`, additive ALTER on `players` | Nick's Clarify ruling (wiki-silent, declared at decision time) |
| Never delete absent players | build item + `players-endpoint` (historical roster/draft references must resolve) |
| Mark-absent = single set-based UPDATE (`is_active_in_catalog=true` AND (`catalog_last_seen_at` < run start OR null)) after all chunks succeed | mechanism follows from the chosen columns — disclosed |
| Reactivation on reappearance (upsert always sets `is_active_in_catalog = true`) | logical consequence of flag semantics — disclosed judgment call |

**Blast radius plan:** one additive ALTER on `public.players` (in ARCHITECTURE.md's fantasy-owned inventory) + upserts/updates on `public.players` rows only. No new table (no collision check needed). No other table touched.

## Items 2+3 — build notes

- Migration `20260722134911_add_players_catalog_presence.sql` (created via `npx supabase migration new` per Rule 8): additive ALTER on `public.players` only — `is_active_in_catalog boolean not null default true` + `catalog_last_seen_at timestamptz`. Blast radius verified against ARCHITECTURE.md inventory before push; pushed via the allowed `npx supabase db push` rule (password from `.env.local` in-shell, never echoed; same cosmetic Docker cache warning as every prior session, remote apply clean). `npm run gen:types` regenerated; new columns confirmed present in generated types.
- `src/services/sleeper/players-catalog.ts` — `syncPlayersCatalog(db)`: existing-count read → fetch (60s timeout) → validate (plain object; ≥5,000 records; ≥80% of existing count; 50-record core-field sample majority) → chunked upsert (500/batch, `onConflict: 'sleeper_player_id'`, map key as identity) → mark-inactive pass ONLY after all chunks succeed (single set-based UPDATE: `is_active_in_catalog=true AND (catalog_last_seen_at < run start OR null)`, exact count returned). Drift guards null out unexpected wire types (empty strings normalize to null); full raw record → `metadata` jsonb; reappearance reactivates via the upsert row.
- `scripts/sync-players.ts` + `"sync:players": "tsx --env-file=.env.local scripts/sync-players.ts"` in package.json. tsx resolves the `@/` alias via tsconfig paths; tsconfig's `**/*.ts` include means scripts/ is tsc-checked too.
- `npx tsc --noEmit` clean; `npm run lint` clean; `npm run build` passed (routes unchanged).

## Live verification (the fold's completion gate — all passed)

- **First live Sleeper call ever:** `npm run sync:players` → `12200 fetched, 12200 upserted, 0 marked inactive` in ~33s (13:52:28 → 13:53:00 UTC). 0 marked inactive is provably correct for a first run — every row was stamped by the run itself.
- Live-DB read-only checks (execute_sql): 12,200 total rows = fetched count; 12,200 active; 0 unstamped; **32 non-numeric IDs = exactly the 32 `position='DEF'` team defenses** (the wiki's best-corroborated quirk confirmed end-to-end); 12,168 rows carry `espn_id` in metadata (the 32 defenses lack it — per wiki, defenses have no external IDs); 12,168 named rows (defenses lack `full_name` — per wiki).
- Spot checks: `'DET'` D/ST row (abbreviation ID opaque string, `fantasy_positions=['DEF']`, no name/status — wiki-consistent); Josh Allen `'4984'` (full column mapping; metadata `espn_id=3918298`, `gsis_id=00-0034857` intact for the crosswalk item); Drake Maye `'11564'` (null `espn_id`/`gsis_id` in Sleeper's dump — a live example of the uneven cross-provider coverage the identity ADR's nflverse backfill step exists to close).
- Not exercised: a non-zero mark-inactive pass (would require a second same-day catalog fetch, violating the once-per-day rule, or planting a fake production row). The UPDATE executed successfully with a provably-correct 0; the non-zero path gets exercised naturally by a future daily run. Disclosed rather than force-tested.

## Session-end

- Build file: 3 items `[x]`; Status ⬜ → 🟡. BUILD_INDEX: registry row + Wave Roadmap row → 🟡, header updated.
- ARCHITECTURE.md: Status paragraph, directory tree (services/sleeper + scripts/), Build Configuration (sync:players script + tsx runner note), players schema entry (+2 columns, metadata note), and the first real **Service API Reference** section (per the build file's session-end requirement).
- PROGRESS.md: milestone entry added (first working Wave 2 feature / first live data ingestion); cap rolled the oldest entry (Folding Policy governance) into PROGRESS_ARCHIVE.md verbatim.
- npm audit after tsx install: unchanged at the pre-existing 6 vulns (4 moderate, 2 high) — known_issues note refreshed.
- STATE.yml overwritten completely; committed + pushed to main.

## Quality-tripwire self-check (Folding Policy)

Every folded item carries item-specific wiki citations and disclosed judgment calls: item 1 (base URL, backoff taxonomy, string IDs + timeout/retry-constant silences), item 2 (once-daily rule, validation floors, map-key identity + chunk-size silence, snapshot-conflict Clarify), item 3 (never-delete rationale, column-mechanism Clarify on declared silence). No tail compression.

## WIKI NOTE

WIKI NOTE: (1) `sleeper-api/players-endpoint` — its snapshot-retention Key Decision asserts "other planned platform features already depend on" reconstructing past player state; that conflicts with v1's explicitly rejected historical views, and Nick ruled this session that the build-file scope (in-place upsert + inactive marking, no snapshot/diff table) wins. The page should note that ruling or soften the dependency claim. (2) Standing schema-reference DDL-addendum note, extended: the catalog-presence columns (`is_active_in_catalog`, `catalog_last_seen_at`) were wiki-silent and decided via Clarify this session — a players-table DDL/decision addendum would let future sessions (crosswalk population, league-scoped sync) read the presence semantics instead of reconstructing them.
