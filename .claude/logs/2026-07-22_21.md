# Session Log — 2026-07-22_21

**Session type:** feature-build (Nick-directed continuation: governance edits + the next fold)
**Governance first (Nick's explicit directive):** two standing practices added to BUILD_PROTOCOL.md
as a new "Amendment Hygiene — Standing Practices" section (between Manual Setup Flag and Clarify):
(1) confirmed-decision backsweep — confirmations recorded after related items were registered must be
swept back against those items for staleness by the session recording the confirmation, before the
next build session reaches them; (2) capacity-collision annotation — items implying new instances of
capacity-limited resources (cron slots, plan caps, rate-limited keys) carry the documented constraint
in their own text so it surfaces at Clarify, not mid-build. Both cite their 2026-07-22 origins (ADP
constraint staleness; Hobby cron-cap surprise). Framed as operationalizing MASTER_CONTEXT's Build File
Amendment Norm (when amendments trigger), not a new amendment type — no dual-location update needed.

**Fold declared (before Clarify):** 03a → "Draft-board data layer" sub-section, all 6 items, as one
query service — the Folding Policy's own named example. Same wiki category as the prior fold, no
manual-setup dependency, no new external service.

**Wiki pages:** no new reads needed beyond the five from log _20 (same session's context) — the
format-mapping decision was grounded in average-draft-position (format-context + format-leakage
rules), multi-platform-adp-divergence (QB format sensitivity), and league-configuration-data-model
(derived_config keys). nfl-state not needed here. Rule 12 checks found no uncovered decision requiring
a new page.

---

## Clarify answers (Nick, this session)

1. **Format mapping — bucketed:** superflex → '2qb' regardless of PPR (QB replacement value is the
   most format-sensitive axis); else ppr ≥ 1 → 'ppr', 0 < ppr < 1 → 'half_ppr' (nearest
   fractional-reception market, covers 0.25 leagues), 0 → 'std'. Dynasty/IDP formats unmapped in v1
   (no such league; derived_config has no dynasty flag — ADR raw-read escape hatch). te_premium never
   affects format (no source axis); resolved format always returned in context for display. NO
   cross-format fallback — empty is an explicit state. This resolves the STATE.yml known_issue
   recorded by the previous fold.
2. **ADP season — latest ingested snapshot** for the source (current-market semantics; consistent with
   the table's current-snapshot design and v1's no-historical-views rule); resolved season returned in
   context. The 2025 league renders a real board today (Nick's signed verify target).
3. **Board pool — ADP carriers ∪ league-rostered** (ADP-less rostered sort last, null ADP; deep
   catalog outside both sets not board-searchable in v1).

## Pre-implementation coverage map (recorded before code)

| Decision | Covered by |
| --- | --- |
| Bucketed format mapping + superflex precedence | Nick Clarify #1 + multi-platform-adp-divergence Key Decision (QB/TE format-blend risk) + average-draft-position (format context always displayed) |
| No cross-format fallback | average-draft-position § format leakage ("most dangerous failure mode") |
| te_premium display-only | projections-endpoint field inventory (no TE-premium ADP axis exists) |
| ADP season = latest ingested | Nick Clarify #2 + adp_rankings current-snapshot semantics + v1 no-historical-views |
| Pool = ADP ∪ rostered | Nick Clarify #3 |
| Availability explicit from roster_players presence + attribution | build item text; roster_players current-snapshot (ARCHITECTURE) |
| ESPN identity resolution at ingestion → platform-agnostic service | ARCHITECTURE (roster_players/draft_state keyed by sleeper_player_id, crosswalk applied at Wave 2 ingestion) + schema-reference/player-identity-mapping; live ESPN check declared impossible until leagues open |
| League context from derived_config, defensive JSONB parsing, no invented defaults | league-configuration-data-model ADR |
| share_token/owner_id/native_league_id excluded via explicit column selects | build item + Access Model boundary (MASTER_CONTEXT) |
| Malformed ID pre-rejected (UUID regex) so DB errors never masquerade as not_found | build item text (never partial/leaked); disclosed implementation choice |
| Ordering ADP asc nulls-last, sleeper_player_id tie-break | build file player-list sub-section's deterministic tie-breaker convention |
| Module src/services/draft-board.ts, named exports, db param | Code Conventions + sync-runs.ts single-file precedent (declared convention choice, wiki-silent) |
| adp_source pinned 'sleeper' constant, carried in context | single-source v1 (declared) |

**Declared silences:** module naming/location; adp_source constant — internal conventions, disclosed.

## Build

- `src/services/draft-board.ts` — `getDraftBoardData(db, leagueId)` → typed
  `DraftBoardResult` (`ok`/`league_not_found`); `resolveAdpScoringFormat` exported pure;
  `DraftBoardPlayer` (identity + adp + availability + roster attribution),
  `DraftBoardLeagueContext` (league identity subset + derived_config subset + resolved format +
  adpSource/adpSeasonYear/adpIngestedAt for the board header). Explicit column selects everywhere;
  chunked players lookup (500); defensive JSONB parsing degrades to nulls.
- Judgment calls disclosed: adpIngestedAt included in context now (the route sub-section's header item
  needs it; deriving it here avoided a second query later — data, not UI); ownership check is
  structural (single-owner platform + caller-supplied client so the route layer's RLS client is the
  second wall).

## Verification (live, real league f3fcc67f…/1257464256710856704)

Temporary runner scripts/__verify_draft_board.tmp.ts (deleted before commit):
- Context exact: platform sleeper, season 2025, scoringFormat 'ppr' (full-PPR 1QB league), ppr 1,
  te_premium false, superflex false, slots 10/7/0, size 10, adpSource sleeper, adpSeasonYear 2026,
  adpIngestedAt = the production cron run's stamp (18:15Z) — pipeline chain feeding the board.
- Pool 276 = 245 ADP (ppr format) ∪ 174 rostered (143 overlap). Top-5 = Bijan 1.4/RB1, Gibbs, Chase,
  Nacua, JSN — market order. ADP-less tail = rostered kickers with team attribution.
- Asserted: full-list ordering invariant (adp asc nulls-last); rostered count == roster_players count
  exactly (174); serialized result contains no share_token/owner_id/native_league_id in any casing;
  unknown UUID → not_found; malformed string → not_found.
- npx tsc --noEmit + npm run lint clean.

## Files read this session

BUILD_PROTOCOL.md (edit target), league-config.ts (DerivedLeagueConfig key shapes grep),
COMPLETION_TEMPLATES.md (already in context), logs _16 (rotation digest), ARCHIVE.md anchors,
build file 03a (checklist edits). Everything else carried from this conversation's earlier fold.

## Session-end bookkeeping

- Build file: all 6 data-layer items `[x]` with item-specific annotations; two sub-sections of 03a now
  complete (ADP + data layer); route/page shell is the next unchecked sub-section.
- BUILD_PROTOCOL.md: Amendment Hygiene section added (Nick-directed governance change).
- STATE.yml overwritten; the ADP-format-mapping known_issue REMOVED (resolved by this fold's Clarify
  #1 + resolveAdpScoringFormat); all other arrays reviewed and carried.
- ARCHITECTURE.md: status, tree, Service API Reference (draft-board service).
- PROGRESS.md: NOT updated — partial progress within 03a, not a milestone (the governance change is
  recorded here and in BUILD_PROTOCOL itself; it refines session mechanics rather than changing what
  any session may build, so it was judged below the governance-milestone bar — flagged for Nick's veto).
- Log cap: this file is the 6th → 2026-07-22_16.md condensed into logs/ARCHIVE.md, original deleted.
- Blast radius: no DDL this session. Live DB access read-only (leagues, league_config, adp_rankings,
  roster_players, rosters, players) + the temp verification's SELECTs. Only fantasy-owned tables
  touched.

WIKI NOTE: none

---

_End of log 2026-07-22_21_
