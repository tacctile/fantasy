# Session Log — 2026-07-22_04
**Session type:** feature-build (autonomous, via BUILD_PROTOCOL.md)
**Scope:** 01_foundation.md — "Schema — league-scoped state" item 4: `standings` table migration

---

## Session start

- Self-located via STATE.yml → 01_foundation.md → first unchecked item: "Migration: standings table" (fourth item of the league-scoped state section).
- Files read per Session Start Protocol: MASTER_CONTEXT.md (auto-loaded), STATE.yml, BUILD_INDEX.md, MANUAL_SETUP_CHECKLIST.md, wiki/index.md, wiki/ROUTING.md, 01_foundation.md, sleeper-api/_index.md + espn-api/_index.md (navigation), then 2 content pages (below).
- Manual Setup Flag check: all Wave 1 items `[x]` except admin-auth `[~]`, which belongs to the later Schema–integrity item — not this one. DB password + push allow rule already live. No halt.
- Log cap triggered: 5 individual logs existed; 2026-07-21_06.md condensed into ARCHIVE.md and deleted before this file (which would have been the 6th) was created.

## Clarify answers (Nick, via AskUserQuestion — all four recommended options)

1. **Grain/PK:** current snapshot — one row per roster per league, PK `(league_id, native_roster_id)` mirroring rosters; Wave 2 sync overwrites in place. Weekly-snapshot rows rejected: v1 explicitly rejects historical views, and the Wave 5 luck tracker computes from matchups' per-week points, not standings history.
2. **Column set:** minimal five — `wins`/`losses`/`ties`/`points_for`/`points_against`. Sleeper waiver/transaction counters and ESPN rank/seed excluded: no registered v1 consumer, and ESPN's exact field names are unverified (matchups-session precedent). Extras arrive via new migration when a consumer exists.
3. **Nullability:** all five not null default 0 — a fresh league genuinely is 0-0 with 0.00 points; zero is a true state, not missing data. (Deliberate divergence from matchups' nullable as-received `points`: a matchup's points for an unplayed week genuinely don't exist yet; a roster's cumulative record always exists once the roster does.)
4. **fetched_at:** skipped — standings have no finality concept (matchups needed it for the stable-repeat-reads promotion policy); `created_at`/`updated_at` + trigger only, like rosters/roster_players.

## Pre-implementation wiki coverage map (Absolute Rule 12 — recorded BEFORE any code)

Wiki pages read this session (2 of 3 budget):
1. `wiki/topics/sleeper-api/roster-endpoint.md` (domain-knowledge, confidence high)
2. `wiki/topics/espn-api/view-parameter-reference.md` (domain-knowledge, confidence high)

| Decision | Covering source |
| --- | --- |
| Sleeper standings source + field names (`settings.wins/losses/ties`, `fpts`/`fpts_decimal`, `fpts_against`/`fpts_against_decimal`, `waiver_position`, `waiver_budget_used`, `total_moves`) | sleeper-api/roster-endpoint ("Standings and Scoring Fields") |
| Recombine the split integer/decimal points pair into one decimal at ingestion; never store or pass through the wire pair | sleeper-api/roster-endpoint (Key Decision — explicit ADR) |
| ESPN standings source = `mTeam` view (record, rank/seed, PF/PA totals; season-level team state) | espn-api/view-parameter-reference (Per-View Reference) |
| ESPN exact nested record field names | **Wiki-declared open question** (view-parameter-reference Open Questions: `mStandings` et al. undocumented) — ESPN-exclusive columns excluded, deferred to Wave 2 live-payload verification; matchups-session precedent |
| Grain: season-cumulative current snapshot | Both pages (Sleeper: figures ride the current roster snapshot; ESPN: mTeam is "season-level team state, not proof of any specific historical week") + v1 no-history rule + Nick's Clarify |
| PK `(league_id, native_roster_id)` | rosters identity precedent (wiki-decided `(league_id, roster_id)` pair) + Nick's Clarify; **schema-reference DDL ADR silent — declared** (see below) |
| Minimal five columns | Build item text + Nick's Clarify |
| not null default 0 | **Wiki silent (DDL-level) — declared**; Nick's Clarify decision |
| Composite FK → rosters + FK → leagues, both on delete cascade | Established precedent (roster_players, matchups); **DDL ADR silent — declared** |
| `platform` + `season_year` duplicated | MASTER_CONTEXT Schema Rules #2/#3 + N:1 precedent (rosters/roster_players/matchups) |
| RLS enabled at creation, owner-write policies deferred | Build file (integrity item's scope) + established pattern |
| `set_updated_at` trigger | Established pattern (shared function from the players migration) |
| No `fetched_at` | Nick's Clarify decision (no finality concept) |

**Declared genuine silence (4th consecutive session):** `schema-reference` has no league-scoped-state DDL ADR — ROUTING.md/index.md searched, category complete at 3 pages, none covers DDL-level conventions for state tables (PK composition mechanics, nullability/defaults, FK delete behavior). Declared at decision time, resolved via established migration precedent + Nick's Clarify answers. Standing WIKI NOTE continues.

**WIKI COVERAGE CHECK: gap found and flagged**

---

## Implementation notes

- Pre-flight prolabel collision check (read-only information_schema query): no `standings` table in any schema of the shared project. Clean.
- Migration created via `npx supabase migration new create_standings_table` → `20260722053523_create_standings_table.sql`. SQL in the established style: lowercase, schema-qualified, header comment carrying constraint rationale, RLS at creation, shared trigger reused.
- One tooling hiccup: `migration new` creates an empty file, and the Write tool requires reading it first — read returned the empty-file warning, then the write landed. No content impact.
- Push: `npx supabase db push` under the committed allow rule; `SUPABASE_DB_PASSWORD` sourced from `.env.local` in-shell, never echoed. Applied cleanly. Same harmless Docker-catalog-cache warning as prior sessions (Docker Desktop not running — local cache only, remote push unaffected).
- Live verification (single read-only JSON query): 11 columns with expected types/nullability/defaults (record + points columns not null default 0); PK `(league_id, native_roster_id)`; both FKs present with ON DELETE CASCADE; RLS enabled; `set_updated_at` trigger; migration history row `20260722053523` present.
- Session-end file updates: build-file item checked with factual annotation; ARCHITECTURE.md schema inventory gained the standings entry and the trigger note moved to "all eight tables"; STATE.yml fully overwritten (nick_pending re-examined — both entries genuinely still pending, kept; npm-audit known_issue and shared-DB warning re-examined and kept). PROGRESS.md not updated — single-item progress, no milestone threshold. DESIGN_SYSTEM.md untouched — no token changes.

## Verification

- Push output: exactly 1 migration listed and applied.
- Live schema query confirmed every object (columns/defaults, PK, FKs + cascade, RLS, trigger, history row) — details above.
- No app code touched; no build/type-check surface changed (schema-only session).

## What the prompt/protocol got right and friction

- BUILD_PROTOCOL self-location worked cleanly again; STATE.yml's "next unchecked item" pointer matched the build file exactly.
- The four Clarify questions were all genuine judgment calls the build item left open; all four recommended options accepted, consistent with the last two sessions — the recommendation-first pattern is earning its keep.
- Friction: none material. The empty-migration-file read-before-write dance is a known tool quirk, seconds of cost.

## WIKI NOTE

WIKI NOTE: `schema-reference` still lacks a league-scoped-state DDL page (fourth consecutive session declaring this gap — rosters, roster_players, matchups, now standings). The accumulated body of precedent (composite-PK identity pairs, N:1 platform/season_year duplication, FK cascade rules, RLS-at-creation, not-null-default semantics for cumulative vs. as-received figures) now lives only in migration files, build-file annotations, and ARCHITECTURE.md. A wiki-maintenance session should consider a "league-scoped state tables" DDL addendum ADR under schema-reference recording these as decisions of record; alternatively declare them below-wiki-altitude deliberately. Additionally: espn-api/view-parameter-reference's open question on exact mTeam record field names (and the undocumented `mStandings` view) will need resolution against a live payload before Wave 2 can add ESPN rank/seed columns.
