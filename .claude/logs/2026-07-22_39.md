# Session Log — 2026-07-22_39
**Session type:** feature-build (autonomous BUILD_PROTOCOL session — Nick: "read this and lets get to work")
**Scope:** Wave 3b, `03b_draft_assistant_live_draft.md` → NEW "Projections ingestion (prerequisite for BPA)" sub-section — registered THIS session as a Nick-signed correctness amendment, then built and verified as the declared fold (one independently verifiable artifact: migration + fused ingestion extension + live verification).

---

## Session start / self-location / Manual Setup Flag — the session's defining sequence

- Reads in protocol order: MASTER_CONTEXT (auto-loaded), BUILD_PROTOCOL, STATE.yml, BUILD_INDEX, MANUAL_SETUP_CHECKLIST, wiki/index, wiki/ROUTING, 03b build file. Self-located exactly where STATE.yml pointed: 03b's first unchecked item = BPA recommendation engine item 1 (02's remainder all `[!]`/`[~]`/`[>]`; 03a's sole remainder the `[>]` bye-week deferral).
- **Manual Setup Flag TRIPPED exactly as STATE.yml predicted:** BPA items 1–4 compute VORP from `player_projection` values; no projections ingestion existed (`adp_rankings` stores ADP fields only); the projections-source decision was absent from `MANUAL_SETUP_CHECKLIST.md` → open/missing state → protocol halt. Read `sleeper-api/projections-endpoint.md` (wiki page 1 of 3) so the flag stated the decision precisely, appended the item under Wave 3b, output the MANUAL SETUP REQUIRED block, and stopped short of Clarify/Build.
- **Nick resolved the flag in-session** (AskUserQuestion; he was present and chose to decide rather than defer): see decisions below. Session continued on 03b per his explicit choice (alternate 04 nav-shell path declined).

## Decisions of record (Nick-signed, recorded in MANUAL_SETUP_CHECKLIST.md Wave 3b `[x]`)

1. **Projections source = the same quarantined `api.sleeper.com/projections/nfl/{year}` endpoint** already adopted for ADP — its response carries component projection stats and preset `pts_*` totals alongside the `adp_*` fields (vendor `company: "rotowire"`), native Sleeper IDs, zero new host/credential.
2. **Landing shape = component stats as-received, scored by each league's actual `league_config` weights at read time** (BPA item 3 honored literally; preset `pts_*` format buckets never used as league values — mirrors the player_scores never-substitute canon's posture).
3. **Fused single fetch** — one request per run feeds both `adp_rankings` and `player_projections`; zero added traffic to the contractless host; one shared structural validation.
4. **Full `stats` object stored as-received + provenance** (`company`, epoch-ms timestamps as timestamptz) — permissive-parsing posture; `adp_rankings` remains the only ADP read path.
5. **Telemetry extends the existing `adp_ingestion` `sync_runs` row's counts** — no new source value, no second run row, no sync_runs migration.

## Amendment (Hygiene practice 1 — confirmation swept back same session)

- The ingestion work was registered in NO build file (BPA items assumed `player_projection` exists). Registered a new 3-item "Projections ingestion (prerequisite for BPA — Nick-signed correctness amendment, 2026-07-22)" sub-section in 03b, ahead of the BPA sub-section. Backsweep check against other registered text: BPA item 3's wording ("league's scoring weights from `league_config`") is exactly what the landing-shape decision implements — no staleness introduced; BPA item 2's `adp_rankings` calibration unaffected.

## Fold declaration (declared in chat before Clarify)

- Fold = the new sub-section's 3 items as ONE independently verifiable artifact (a data layer with no consumer yet — the exact later-wave case the Folding Policy's artifact unit exists for). BPA item 1 explicitly excluded: it needs the league-mechanics wiki category (VBD/VORP pages) — a new-category boundary is a hard stop, so it anchors the NEXT session.

## Clarify

- One batch of 3 (fetch shape / row payload / telemetry — answers above). The migration item's remaining shape (PK, provenance columns, RLS) follows repo precedent so directly that separate questions would have been filler — stated as the plan instead, per the no-manufactured-questions rule.

## Pre-implementation coverage map (recorded before any code)

- Request form/params, response shape (array, `player_id`, `stats` mixing projections+ADP), open field inventory, epoch-ms provenance fields, `company` caveat ← `sleeper-api/projections-endpoint.md` (read this session; the only wiki page needed — 1 of 3 budget).
- Full-stats permissive storage; validate-before-swap/last-good snapshot; 999.0 sentinel ← same page's Key Decisions. Sentinel handling for THIS table: retained raw inside the jsonb snapshot by design (raw snapshot, not curated); ADP reads stay on `adp_rankings`; BPA consumers read component stats only — recorded in the migration comment and ARCHITECTURE.
- Table scoping (NOT league-scoped; `season_year` in key), source-check/RLS/trigger/index conventions, telemetry shape ← repo-established precedent (`adp_rankings` inventory entry + `fantasy_owner_all` layer + `sync_runs`), plus Nick-signed Clarify. No decision filled from general knowledge; no wiki gap → no genuine-silence declaration needed beyond the app-internal storage territory the wiki deliberately doesn't cover.

## Built

- **Migration `20260723023127_create_player_projections_table.sql`** — `player_projections`: PK `(sleeper_player_id, projection_source, season_year)`; FK → players cascade; `projection_source` check `('sleeper')`; `stats jsonb not null` (full as-received object incl. `pts_*` and raw `adp_*` sentinels); `company text`, `source_last_modified`/`source_updated_at timestamptz`; `ingested_at`; timestamps; RLS + `fantasy_owner_all`; `set_updated_at`; `idx_player_projections_source_season`. Collision-checked against the LIVE shared schema via information_schema (empty result) — not just the generated types. Pushed; RLS + policy confirmed live post-push; `npm run gen:types` regenerated.
- **`src/services/adp/types.ts`** — `PlayerProjectionRow` insert type; `AdpIngestionResult` gains `projectionRowsPersistedCount`/`projectionStaleRowsDeletedCount`; unmapped-list doc updated to the union semantics.
- **`src/services/adp/ingestion.ts`** — fused persist: projection candidates deduped by `player_id` (first wins — required for single-statement upsert; duplicates unobserved but the surface has no contract); catalog lookup widened to the union of ADP-entry IDs and candidate IDs (one unmapped list); projections-specific gates (zero-insertable abort; <50%-of-existing abort vs `player_projections` count) run alongside the ADP gates BEFORE either table writes; ADP swap completes first, then projections upsert (chunked, onConflict composite PK) + stale-row cleanup — a late projections failure degrades only the projections snapshot; `epochMsToIso` permissive provenance conversion; `runTrackedAdpIngestion` counts gain `projections_persisted`/`projections_stale_deleted`. Header docstring documents the fused contract.
- **`scripts/sync-adp.ts`** — output line extended with projections figures. Cron routes untouched (they consume the wrapper's ok/error; counts ride sync_runs automatically).

## Verification (all live against the real endpoint/database)

1. **First run:** 3292 records fetched → 3292 projection rows persisted, 0 unmapped, 0 null stats/company/provenance; `adp_rankings` behavior byte-identical to the pre-change baseline (2946 entries, 10 formats, 36526 sentinel-skipped, 0 stale). Sample row (Bijan `9509`): component stats present (`rush_yd` 1372, `rec` 64, `rec_yd` 537), presets (`pts_ppr` 324.9), `company` rotowire, provenance timestamptz, raw `adp_ppr` 1.4 retained in jsonb. 9 distinct catalog positions across rows (six fetched positions; catalog position may differ, e.g. FB — expected).
2. **Idempotent re-run:** identical counts, 0 stale removed on both tables.
3. **Containment (live failure injection):** `npm run sync:adp -- 2035` aborted at the zero-insertable-ADP gate with exit 1 and ZERO writes — verified both tables unchanged (3292/2946) and no season_year=2035 rows anywhere. Gate ordering guarantees the same containment for the projections gates (all gates precede all writes).
4. **Tracked path:** scratchpad runner invoked `runTrackedAdpIngestion` exactly as the cron routes do — `sync_runs` success row carries `projections_persisted: 3292`, `projections_stale_deleted: 0`.
5. tsc clean, eslint clean, `npm run build` clean.

## Judgment calls (disclosed)

- Dedup keep-first on duplicated `player_id` (deterministic; duplicates unobserved live).
- Both tables' gates evaluated before either writes (one accept/reject decision per table per response; no partial acceptance of a degraded response) — a deliberate strengthening consistent with validate-before-swap, disclosed rather than silently chosen.
- ADP-swap-first write order: the pre-existing feature keeps priority if the fused run dies mid-flight.
- Manual script path continues to bypass sync_runs telemetry (players-catalog precedent — script calls the service directly); the 2035 test therefore left no failure row, expected.

## What the session got right / friction

- The Manual Setup Flag worked exactly as designed: STATE.yml's warning → protocol halt → checklist append → Nick resolved in-session at zero build-time cost, then the amendment/backsweep landed before any code. The fused-fetch structural win (projections were ALREADY in the fetched response) made the artifact cheap — the fetch path needed zero changes.
- Friction: none material. Same UTC-vs-local migration filename cosmetic note as prior sessions (20260723... on a 2026-07-22 local session). tsx scratchpad runner needed the async-main wrapper (CJS output — no top-level await).

## WIKI NOTE

WIKI NOTE: none — no wiki content appeared missing, outdated, or incorrect. `sleeper-api/projections-endpoint.md` fully covered the surface; its "projected statistics" description matched live observation exactly. (Its open question on whether weekly variants exist remains open — season-level records were all this session needed.)
