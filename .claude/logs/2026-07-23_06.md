# Session Log — 2026-07-23_06

**Session type:** feature-build (autonomous, BUILD_PROTOCOL.md)
**Self-location:** roadmap order 01→02→03a→03b→04. 02 (ESPN `[!]`, cron `[~]`, finality `[>]`) and 03a (bye-week `[>]`) hold only blocked/deferred remainders; 03b 🟢. First self-locatable `[ ]` = Wave 4. File-order first unchecked = the share-link settings panel (line 54), but it RIDES the share-token infra → the self-locatable fold is **named-singleton #5** (BUILD_PROTOCOL): the share-token data-exposure boundary.
**Fold declared (before Clarify):** named-singleton #5 — spectator RLS SELECT policies + no-draft_state policy + owner regenerate/revoke action + spectator data loader + the data-boundary tests, folded together, nothing else. A MIGRATION fold → Rule 13 blast-radius applies. Explicitly NOT in fold: the share-link settings panel UI (54), all spectator UI/route (72–77), and line 81's rendered-markup test (needs the spectator route — rides the spectator-UI fold).
**Manual Setup Flag:** Wave 4's two manual items (confirm scoring settings — factual; decide when to share the link — distribution, "your call, not a technical step") are both non-blocking. No halt.

---

## Wiki reads this session (1 page — schema-reference)

1. `schema-reference/league-identity-and-scoping.md` — confirmed `platform_league_uuid` is the internal PK every league-scoped `league_id` FK points to, and all queries join on it (never native IDs). The RLS child-table predicate `league_id in (select platform_league_uuid from leagues where share_token = …)` is exactly consistent. The `share_token` transport mechanism itself is football-domain-silent — declared.

Build-file WIKI PAGES (league-config, ppr-scoring, points-for-against) are for the scoring/dashboard surfaces, not this security boundary — not re-read (already-shipped getters, no new decision).

## Clarify (3, Nick-signed — one batch, interdependent)

1. **Spectator tables = ADD BOTH (8 tables).** The reused `getMatchups`/`getPlayerCard` getters also read `players` (global catalog) + `roster_players` (league membership), neither in the build file's six-table list. Nick-signed correctness amendment: add both. `players` readable to any valid-token holder (public NFL identity data); `roster_players` league-scoped like the rest.
2. **RLS mechanism = anon key + `x-share-token` header + RLS `to anon` as the wall.** RLS is the true data-exposure boundary (build-file mandate "add RLS SELECT policies"); draft tables get no anon policy. (Rejected: secret-admin-client-scoped, which makes RLS not the wall.)
3. **Regenerate token gen = DB `generate_share_token()` via RPC.** One canonical generator; revoke == regenerate (share_token NOT NULL).

## Pre-implementation coverage map (recorded BEFORE code — Rule 12)

| Decision | Coverage |
|----------|----------|
| share_token = access control; regenerate invalidates old link | MASTER_CONTEXT Access Model + Schema Rule #6 (canonical decision-of-record) |
| `league_id`/`platform_league_uuid` scoping for child-table policies | `schema-reference/league-identity-and-scoping` (read this session) |
| 8 spectator tables; draft_state/draft_sessions never reachable | Nick-signed this session + Access Model / build-file mandate |
| RLS transport (anon + x-share-token header GUC), grants, InitPlan form | **DECLARED WIKI SILENCE** — pure Supabase/Postgres impl, not football-domain |
| Standings order / power formula / matchup pairing / player-card | Already shipped in `dashboard.ts` — reused as-is, no new decision |

No new-category search needed; the one build-file-vs-code divergence (6→8 tables) was surfaced to Nick and signed, not silently filled.

## Rule 13 blast-radius (MIGRATION session)

- Tables touched: leagues, league_config, rosters, roster_players, matchups, standings, player_scores, players — all fantasy-owned, all in ARCHITECTURE.md "Database Schema (live)". Two new fantasy functions. NO foreign prolabel object referenced/altered. Owner policies + is_fantasy_admin() unchanged. GRANT SELECT to anon on fantasy tables only.
- Confirmed before push: blast radius = only fantasy-owned tables.

## Build log (final)

- **Migration `20260723072917`** (pushed, remote confirmed): `current_share_token()` (STABLE, reads `request.headers` GUC → x-share-token; null-safe), `regenerate_share_token(uuid)` (SECURITY INVOKER; UPDATE…RETURNING; EXECUTE revoked from public, granted authenticated), `grant select` to anon on the 8 tables, `spectator_share_read` SELECT policies `to anon` on all 8 (leagues by share_token; six child tables by league_id subselect; players by exists-valid-league). All predicates wrapped `(select current_share_token())` for InitPlan.
- **Migration `20260723074134`** (follow-up, pushed): `revoke execute on regenerate_share_token from anon` — live check found Supabase grants function EXECUTE to anon directly (default privileges), so `revoke from public` alone left anon able to invoke (a no-op — the RLS UPDATE policy blocked the write, verified token unchanged — but weaker than the comment claimed). Revoke makes the "spectators can't invoke the mutation" boundary real.
- `src/lib/supabase/spectator.ts` — `createClient(shareToken)`: anon, session-less, publishable key + x-share-token header. Path-disambiguated `createClient` per convention.
- `src/services/spectator.ts` — `loadSpectatorDashboard`/`loadSpectatorPlayerCard`: resolve token→league via the anon client (RLS returns the one league), then reuse the `dashboard.ts` getters; `pickWeek` = latest scored / honor-valid-requested / null-empty; invalid/blank/revoked → `invalid_token`. Imports nothing draft-related.
- `src/app/(admin)/leagues/[leagueId]/dashboard/actions.ts` — `regenerateShareToken(leagueId)`: in-action getAdminAuthState gate → `regenerate_share_token` RPC via the RLS server client → revalidate. Typed regenerated/unauthorized/not_found.
- `src/services/spectator.test.ts` — 9 durable tests against an RLS-enforcing recording fake client (Rule 13, no live write; injected via `vi.mock` of the client factory): draft_state/draft_sessions never queried (dashboard + player card), cross-league isolation per token (A vs B from the same loader, no B identity in A's JSON), invalid/blank/bad-token → not-found with no data queries, week selection (default latest / honor valid / out-of-range→latest), player-card resolve + player_not_found.
- Verified: tsc + lint + `npm run build` clean (routes: home/dashboard/draft/login); `npm test` 23/23 (14 prior + 9 new). Types regenerated (`current_share_token`/`regenerate_share_token` present).
- **Live read-only RLS verification** (temp `scripts/_verify_spectator.ts`, deleted): against the real league — anon+valid-token reads all 8 tables scoped (leagues 1, rosters 10, roster_players 174, matchups 180, standings 10, player_scores 1000, players 1000, league_config 1); **draft_state 0 + draft_sessions 0 even WITH the valid token**; anon+wrong-token → all 0; anon+no-token → all 0; anon rpc regenerate → 42501 permission denied (after follow-up migration); regenerate round-trip via admin RPC revoked the old link (anon+OLD → 0) and the new link worked (anon+NEW → 1), then restored the exact prior token (net-zero, anon+OLD → 1).
- Fold complete exactly as declared. NEXT LOGICAL TASK: the share-link settings panel UI (line 54 — the `regenerateShareToken` action exists), then the spectator UI surface (route + mobile-first components + page composition + the no-admin-markup render test on line 81), then the loading/empty/error resilience item.

## STATE.yml array review (prune stale/resolved, never carry forward unexamined)

- `nick_pending`: VISUAL CHECK BACKLOG carried, extended with the share-link surface (the settings panel + spectator surface aren't built yet, but the boundary they'll sit on is live and owner-verifiable once the panel exists). POST-LOGIN LANDING, superflex-run-sensitivity, demand/panic classification, per-league memory, finality-promotion, ESPN external-timing, season cadence, bye-week, league-renewal, waiver-scoping — all still genuinely live, carried. Added: a note that the spectator link is now technically issuable (the "decide when to share" manual item is a real option now).
- `blockers`: ESPN external-timing unchanged. Buildable frontier updated — the share-token singleton is now DONE; next self-locatable is the share-link settings panel + spectator UI folds.
- `known_issues`: npm-audit unchanged (no deps added — spectator client uses the existing @supabase/supabase-js). finality-promotion gap unchanged. Both carried.
- `warnings`: Rule 13 line rewritten for this MIGRATION session (blast radius = 8 fantasy tables + 2 functions, verified). Added an ADMIN/SPECTATOR-boundary warning update (the loader is the data seam; spectator UI is a later separate rendering path). Testing-infra warning updated to 23 tests / 6 files.

## WIKI NOTE

None. The share_token access model + RLS transport are app-architecture/decision-of-record concerns (MASTER_CONTEXT Access Model, Schema Rule #6), genuinely outside the football-domain wiki — declared as silence at decision time, not a gap in wiki coverage. `league-identity-and-scoping` fully covered the `platform_league_uuid` scoping the child-table policies depend on.
