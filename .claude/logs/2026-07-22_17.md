# Session Log — 2026-07-22_17

**Session type:** feature-build (autonomous BUILD_PROTOCOL mode — Nick dropped the protocol file in with "read this and lets get to work")
**Build file:** `.claude/build/02_data_pipeline.md` — Cron / polling strategy sub-section
**Fold:** all 3 sub-section items (declared in chat before Clarify, per Folding Policy)

---

## Self-location

Wave 2, `02_data_pipeline.md`. Both ESPN sub-sections `[!]` (external timing, Nick-signed 2026-07-22) → first unchecked `[ ]` item is the authenticated Vercel Cron routes, exactly as STATE.yml predicted. Session-start reads completed in protocol order: MASTER_CONTEXT → STATE.yml → BUILD_INDEX → MANUAL_SETUP_CHECKLIST → wiki index/ROUTING → build file.

## Fold declaration (made in chat before Clarify)

Fold = the full **Cron / polling strategy** sub-section: (1) authenticated cron routes, (2) `vercel.json` schedule, (3) sync-run tracking table. Basis: one independently verifiable artifact — the scheduled-sync surface. The routes are what the schedule invokes; the tracking table is what makes scheduled runs observable (the item's own text: sync health visible without digging through Vercel logs).

Disclosed fold judgment calls:
- **Build order 1 → 3 → 2** (routes, tracking table, then schedule+deploy), not strict list order. Routes must write sync-run rows, so the table precedes full route verification; the `vercel.json` deploy is the **first touch of Vercel Cron** (STATE.yml's flagged hard-stop candidate), so it goes last — nothing folds past it, and a live verification failure there ends the session.
- Per-item Clarify batches (no mega-batch): item 1's batch asked at session start; items 3 and 2 get their own batch or an explicit zero-questions declaration immediately before each.

## Manual Setup Flag

Only dependency: Wave 2 cron-secret item, state `[~]` (ready, awaiting handoff) → does NOT stop the session; asked in item 1's Clarify batch per protocol. Verified no `vercel.json` exists (nothing overwritten). Cross-cutting paid-tier item surfaced as the Vercel-plan Clarify question rather than a halt (decision resolved in-batch, below).

## Wiki pages read this session (3 — at budget)

1. `sleeper-api/rate-limits` — once-daily players-dump rule (most load-bearing), headroom-below-ceiling limiter, differential cadence by resource change rate
2. `sleeper-api/players-endpoint` — single centrally-scheduled catalog job, validate-before-swap, never per-request
3. `sleeper-api/nfl-state-endpoint` — current-week procedure: `week` gated by `season_type`; only `regular` is a scoreable fantasy week; `pre`/`off`/`post` → no matchup week; never date arithmetic; cache on the order of minutes (a scheduled run fetching it fresh each run trivially satisfies this)

## Clarify — item 1 batch (asked and answered at session start)

1. **Vercel plan / cadence:** Hobby (max 2 crons, once daily each; 15-min needs Pro). **Nick: Hobby — daily crons for now.** Catalog daily + league-state daily; 15-min cadence deferred to season start (same window the ESPN block clears). Build-file item 2 gets `[~]` with a dated deferral note — not silently dropped.
2. **CRON_SECRET handoff (the `[~]` manual item):** session generates 64-hex secret → `.env.local` + `.env.example` contract; Vercel sends it as `Authorization: Bearer` on cron invocations when set as env var. **Nick: yes — Production only.** Nick copies the value from `.env.local` into Vercel (Production); item flips `[x]` once confirmed in use.
3. **Scheduled league-sync scope:** **Nick: current week only** — config + rosters/standings + draft + only the current NFL week's matchups (week per `/state/nfl`); full 18-week sweep stays manual (`npm run sync:sleeper`).
4. **Draft fast path:** **Nick: route now, schedule in 3b** — authenticated route wired and provably invokable; no `vercel.json` entry until 3b tunes cadence.

## Pre-implementation coverage map (Rule 12 — recorded before any code)

### Item 1 — authenticated cron routes
| Decision | Coverage |
| -------- | -------- |
| Route auth = `Authorization: Bearer ${CRON_SECRET}`; Vercel sends it automatically on cron invocations when the env var is set | **GENUINE WIKI SILENCE, declared now:** checked `wiki/ROUTING.md` + `wiki/index.md` — no Vercel/infra category exists (categories are fantasy-domain + sleeper-api/espn-api/schema-reference). Using Vercel's own documented convention. |
| Fail closed when `CRON_SECRET` env var absent (reject every request; never open) | Same declared silence (infra security judgment, disclosed) |
| Catalog route cadence: at most once per day; single centrally-scheduled job | `sleeper-api/rate-limits` (players-dump once-daily rule) + `sleeper-api/players-endpoint` (single scheduled job decision) |
| Catalog route once-per-day guard consulting sync_runs (skip if a successful run < 24h ago) | `sleeper-api/rate-limits` cadence rule; STATE.yml warning names the tracking table as the intended guard substrate. Guard lives in the route (service stays pure) — disclosed judgment |
| Current-week derivation: `GET /state/nfl`, use `week` iff `season_type === 'regular'`; `pre`/`off`/`post` → skip matchups entirely; handle undocumented `off` as real | `sleeper-api/nfl-state-endpoint` (Key Decisions 1–3; fantasy playoffs happen inside `regular`, so regular-only is sufficient — `post` means every league's fantasy season is over) |
| Never derive week from calendar arithmetic | `sleeper-api/nfl-state-endpoint` |
| Draft route = per-league `syncLeagueDraftState` loop with per-league failure isolation, no schedule | Build-file item text ("separate faster cadence path reserved") + orchestrator isolation pattern (ARCHITECTURE Service API Reference); cadence tuning explicitly Wave 3b |
| Route paths `/api/cron/sync-players`, `/api/cron/sync-leagues`, `/api/cron/sync-draft`; `force-dynamic`; `maxDuration` | Declared silence (Next.js/Vercel conventions; mirrors existing `/api/health` route pattern) |

### Item 3 — sync-run tracking table
| Decision | Coverage |
| -------- | -------- |
| Column set: source, league_id, platform, status, started_at/completed_at, record counts, error summary | Build-file item text enumerates exactly this — not invented |
| `league_id uuid` FK → `leagues.platform_league_uuid`, nullable (catalog runs are global, not league-scoped) | MASTER_CONTEXT Schema Rule 1 + ARCHITECTURE live-schema convention (league_id is always the internal uuid); nullability is a disclosed judgment from the item's own field list |
| `platform` enum not null | MASTER_CONTEXT Schema Rule 2 |
| No `season_year` | Schema Rule 3 scopes it to stat/score/matchup tables; telemetry is none of those — disclosed |
| status values `running`/`success`/`failure`/`skipped` | Disclosed judgment (`skipped` = the once-per-day guard outcome); no covering wiki page (checked — schema-reference covers league scoping/identity/config, not telemetry) |
| counts as jsonb (per-job shapes differ) | Disclosed judgment, mirrors raw-payload-as-jsonb precedent (`league_config`) |
| RLS enabled + `fantasy_owner_all` policy | ARCHITECTURE RLS-layer convention (all fantasy tables) |
| Collision check for the name `sync_runs` | Grep of generated full-public-schema `database.types.ts`: no match. Live `list_tables` check to be run at migration time (Rule 13) |

### Item 2 — vercel.json schedule
| Decision | Coverage |
| -------- | -------- |
| Two daily crons (Hobby cap is exactly 2): catalog then league-state | Build file (daily catalog) + Nick's Clarify decision (league-state daily on Hobby; 15-min deferred, dated) |
| Times 09:00 UTC (catalog) / 10:00 UTC (leagues): catalog first so new players exist before roster refs; early-morning ET after MNF corrections settle; 1h gap because Hobby cron timing is hour-imprecise | Disclosed judgment; declared silence on Vercel cron mechanics (no covering category) |
| Draft route gets no schedule entry | Nick's Clarify decision + build-file item text (cadence tuned in 3b) |

### Known deferral surfaced during mapping (reported, not silently expanded)
`league-matchups.ts` finality promotion ("games complete + correction window + stable repeat reads") was noted in ARCHITECTURE as "deferred to the cron sub-section", but the cron sub-section's three registered items do not include it, and Nick's Clarify answer fixed scheduled scope to current-week-only. Consequence: in-season, a completed week's rows keep `is_final=false` once the week advances (only the league-status='complete' sweep promotes today). Flagged in completion report as NEXT LOGICAL TASK material — building it here would exceed the declared fold.

---

## Build narrative

### Item 1 — authenticated cron routes (built first; verified locally)

- `src/services/sleeper/nfl-state.ts`: `getNflState()` (typed `/state/nfl` fetch; new `SleeperNflState` wire type in types.ts) + `resolveMatchupSyncWeek()` — `week` iff `season_type === 'regular'` and integer 1–18, else null (defensive bounds per the page's no-versioning warning). Never date arithmetic.
- `src/app/api/cron/auth.ts`: `requireCronSecret()` — Bearer `CRON_SECRET` check, constant-time compare (`crypto.timingSafeEqual`, length short-circuit), fail-closed: missing env → 500 + server-logged misconfig error; mismatch → 401. Generic bodies.
- Three routes (`force-dynamic`, `maxDuration 300`): `sync-players` (catalog job), `sync-leagues` (orchestrator with current-week/skipMatchups), `sync-draft` (per-league `syncLeagueDraftState` loop with isolation; NO schedule until 3b). Responses carry counts only; error strings with league IDs go to server logs only (client.ts precedent).
- Orchestrator changes (additive, disclosed): extracted `discoverConnectedSleeperLeagueIds()` (draft route reuses it — no duplicated discovery), added `skipMatchups` option (offseason scheduled runs must not full-sweep; `chain` filtered, skippedSteps computed against the filtered chain).
- CRON_SECRET: generated via crypto RNG (PS5.1 `RNGCryptoServiceProvider` — first attempt used the .NET Core-only `::Fill` API, failed, left an empty `CRON_SECRET=` line that was repaired before regenerating), appended to `.env.local` (never echoed to chat — Nick copies from the file), `.env.example` contract row added.
- Verification: lint + build clean (3 routes registered ƒ); local prod server: 401 no-header ×3, 401 wrong-bearer, authorized sync-draft 200 (1 league, 0 new picks — first-write-wins idempotence), authorized sync-leagues 200 (`seasonType: "off"` → `matchupWeek: null` → matchups skipped, config+rosters+draft ran). Catalog authorized test deliberately deferred until item 3's guard existed (a real invocation would have been a second same-day fetch — cadence rule).

### Item 3 — sync_runs tracking (zero-question Clarify, declared)

- Live collision check (Rule 13): `list_tables` on the shared project — 59 tables, 10 fantasy (matching ARCHITECTURE inventory exactly), 49 foreign, no `sync_runs` anywhere. Also greped the generated full-schema types: no match.
- Migration `20260722160525_create_sync_runs_table.sql`: exactly the item's field list (source/league_id/platform/status/started_at/completed_at/counts/error_summary) + identity PK + timestamps/trigger + RLS `fantasy_owner_all` + 2 indexes (FK; guard path source/status/started_at desc). league_id nullable (catalog runs are global); no season_year (telemetry, not stat data — Schema Rule 3 scope); status includes `skipped` (guard outcome). Pushed clean (one migration listed); `gen:types` regenerated.
- `src/services/sync-runs.ts` (platform-agnostic — ESPN reuses it): `startSyncRun`/`finishSyncRun` (running → terminal, crash leaves a visible `running` row), `recordSyncRun` + `recordSyncRunSafely` (best-effort — tracking never masks sync outcome), `hasRecentCatalogRun` (guard: running/success within 24h blocks; failure rows allow retry; skipped rows never count; throws → caller fails CLOSED).
- Route integration: catalog route = guard → skip-row or start/finish pair; sync-leagues = one row per league post-run (uuid from outcome or a native→uuid map so failed slices still attribute; per-league window from step timestamps), counts flat per step; sync-draft = one row per league slice (failure slices: league_id null, native ID in error_summary — minor asymmetry, disclosed).
- Honest backfill: today's real manual catalog run (12,200 players, pre-dating the table) inserted as a `success` row timestamped from its session's commit (`343f52b`, 13:59:16Z) — makes the guard see reality and prevents tomorrow's... see consequence below.
- Verification: rebuild clean; authorized local runs: catalog → `{ok, skipped:true}` (guard live), leagues + draft → 200; `select * from sync_runs` showed all four row shapes correct (backfill success / guard skipped / league_state success with counts+league / draft_poll success).

### Item 2 — vercel.json schedule (zero remaining questions, declared; substance decided in the item-1 batch)

- `vercel.json`: 2 daily crons — exactly Hobby's cap: `/api/cron/sync-players` `0 9 * * *`, `/api/cron/sync-leagues` `0 10 * * *`. Times disclosed judgment: early-morning ET after MNF stat corrections settle; catalog before leagues so new players exist before roster refs; 1h gap because Hobby timing is hour-imprecise. Draft route deliberately unscheduled (Nick).
- Nick pasted CRON_SECRET into Vercel (Production) BEFORE the push so the triggered deploy picked it up.
- Commit `e6684ef` pushed → deploy `dpl_v6BNjniJvNibgqsVhmW56Do1bKYU` READY in ~21s (a plan-invalid cron config would have failed the deploy — Hobby rejects sub-daily at deploy time).
- Production verification: 401 unauthenticated ×3; authorized sync-players → guard skip (proves CRON_SECRET live in Production AND prod→Supabase reads); authorized sync-leagues → 200 full run from the deployed function. Cron registration confirmed by Nick in the dashboard (both jobs listed). First-touch-of-Vercel-Cron hard stop: this was the fold's final item; nothing folded past it.

### Consequences and notes for future sessions

- **Tomorrow's 09:00 UTC catalog cron will record a `skipped` row, not a fetch** — the backfill row (13:59Z today) is <24h before it. That is CORRECT guard behavior, not a bug. First real scheduled catalog fetch: 2026-07-24 ~09:00 UTC.
- **Finality-promotion gap surfaced (reported, not built):** ARCHITECTURE's matchups-sync note deferred in-season `is_final` promotion "to the cron sub-section", but the three registered cron items don't include it and Nick's current-week-only decision means a past in-season week stops being re-synced once the week advances — its rows keep `is_final=false` (only the league-status='complete' sweep promotes today). Needs its own registered decision/build moment (correction-window guidance lives on `sleeper-api/matchup-endpoint`). Recorded in STATE.yml known_issues.
- Manual `npm run sync:players` bypasses the guard (script calls the service directly) — manual cadence discipline still applies to the script path; the STATE.yml warning was updated accordingly.
- Session-end mechanics: log cap rotated `_12` (Wave 1 health-check gate) into ARCHIVE.md, deleted; PROGRESS.md milestone added (core infra activation — automated pipeline), oldest entry (Wave 1 complete) rolled verbatim to PROGRESS_ARCHIVE.md; MANUAL_SETUP cron-token item → `[x]`, new decision of record (Hobby daily cadence); build file: items 1+3 `[x]`, item 2 `[~]` dated; ARCHITECTURE.md updated (tree, schema inventory, service reference, Vercel cron, env table); STATE.yml fully overwritten with arrays actively reviewed.

## WIKI NOTE

WIKI NOTE: none — the Vercel-cron/deploy-tooling silence is the same deliberately-below-wiki-altitude category declared and noted in prior sessions (env-contract, health-check); trail continuity maintained in the coverage map above, no action urged.

---

_End of log 2026-07-22_17_
