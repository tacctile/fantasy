# Session Log — 2026-07-22_40

**Session type:** feature-build (autonomous, BUILD_PROTOCOL.md)
**Self-location:** 03b_draft_assistant_live_draft.md → BPA recommendation engine, first unchecked item (item 1)
**Fold declared (before Clarify):** BPA items 1–3 — (1) pure replacement-level calculation, (2) ADP-calibrated replacement depth, (3) pure base-value/VORP function. 3 decision-dense items, same sub-section = standard fold cap. Live-behavior ceiling does not apply (pure calculation; STATE.yml pre-ruled). Items 4+ wait.
**Manual Setup Flag:** BPA prerequisite (projections source) is `[x]` resolved+built. Remaining Wave 3b manual items are draft-day realtime actions, not dependencies. No halt.

---

## Wiki reads this session (8 pages — 3-page budget exceeded under Rule 12 coverage-check override, each read decision-driven)

1. `league-mechanics/value-based-drafting.md` — items 1+3 core mechanism
2. `league-mechanics/roster-construction-starting-lineups.md` — item 1 startable-slot demand
3. `league-mechanics/average-draft-position.md` — item 2 calibration posture
4. `sleeper-api/projections-endpoint.md` — projection stats shape (coverage check: scoring inputs)
5. `schema-reference/league-configuration-data-model.md` — scoring/roster raw-column read pattern
6. `sleeper-api/league-endpoint.md` — scoring_settings semantics + roster_positions labels (coverage check)
7. `league-mechanics/flex-spot-configuration.md` — flex eligibility semantics (coverage check)
8. `player-evaluation/value-over-replacement.md` — VORP formal definition + flex-pool pitfall (coverage check)

## Item 1 Clarify (Nick-signed, this session, all four)

1. **Single ADP-calibrated baseline method, no baseline-method configuration** (build file wins over VBD page's configurable-input Key Decision — conflict flagged in report).
2. **Flex pooling = greedy projection fill**: fill dedicated starter slots from top projected players per position, then fill flex slots league-wide from best remaining across eligible positions; replacement per position = last player consumed there. (Resolves the VBD page's open implementation question for this platform.)
3. **Item 1 is startable-only** (starters + flex pooling): no explicit bench-demand term — item 2's ADP calibration supplies market depth (roster-construction page rejects fixed bench formulas).
4. **Module home = new `src/services/bpa/`** (pure modules, named exports, no index.ts — adp/ precedent). Reuses `parseRosterSlotLayout` / `resolveAdpScoringFormat` from draft-board.ts.

## Item 1 pre-implementation coverage map (recorded BEFORE code — Rule 12)

| # | Decision | Coverage |
|---|----------|----------|
| 1 | VORP definition: projection − replacement-level projection | `player-evaluation/value-over-replacement` (formal definition); `league-mechanics/value-based-drafting` (last-starter baseline) |
| 2 | Replacement derives from the league's exact roster shape; no fixed positional ranks, no hardcoded roster shape | `roster-construction-starting-lineups` Key Decisions (rejects fixed replacement-rank formulas); build-file item text |
| 3 | Flex slots pool replacement across eligible positions — never independent per-position baselines | `value-based-drafting` Key Decision; `flex-spot-configuration` Key Decision (recompute baselines whenever flex present); `value-over-replacement` pitfalls (unified flex pool) |
| 4 | Flex-pooling implementation = greedy projection fill | Wiki OPEN question (`value-based-drafting` open Q2) → resolved by Nick's Clarify #2 this session (signed decision, not silent invention) |
| 5 | Superflex behaves as a de facto second QB slot | `flex-spot-configuration` Key Decision — falls out naturally from greedy fill with QB-eligible slots |
| 6 | Startable-only demand in item 1 (bench via item 2 ADP) | Nick's Clarify #3; grounded in `roster-construction-starting-lineups` (bench formulas rejected as uncorroborated) |
| 7 | Sleeper flex-label → eligible-position mapping | PARTIAL coverage: `flex-spot-configuration` gives standard flex = RB/WR/TE and QB-eligible superflex = +QB; `sleeper-api/league-endpoint` lists labels (FLEX, SUPER_FLEX, REC_FLEX) but not eligibility. **DECLARED WIKI SILENCE** (searched ROUTING.md, index.md, league-mechanics + sleeper-api categories) on exact eligibility for REC_FLEX / WRRB_FLEX / IDP_FLEX: general-knowledge mapping used (REC_FLEX=WR/TE, WRRB_FLEX=RB/WR, IDP_FLEX=DL/LB/DB), declared here at decision time. Unknown flex-family labels are excluded from demand and surfaced in the result (`unknownFlexLabels`) — honest degradation, never a guess |
| 8 | Scoring application: points = Σ weight[key] × stats[key]; absent stat key contributes 0 at calc time; DST scored via the league's own def_*/st_* keys | `sleeper-api/league-endpoint` (scoring_settings = flat stat-key→point map; absent-as-zero-at-calc Key Decision; DST-by-league-keys Key Decision) |
| 9 | Component stats as scoring input; preset pts_* buckets NEVER used as league values; ADP never read from player_projections | `sleeper-api/projections-endpoint`; MANUAL_SETUP Wave 3b decision of record; player_projections migration canon. Enforced structurally: scoring excludes `adp_*`/`pts_*` keys |
| 10 | scoring_settings_raw read as-received (open map); derived_config only for the normalized subset | `schema-reference/league-configuration-data-model` ADR |
| 11 | scoring_settings key namespace matches projection stats keys | Not explicitly wiki-stated (STATE.yml carries last session's observation). **Verified empirically this session against live data** (key-intersection check in the live verification run) — declared, not silently assumed |
| 12 | Roster layout parsing | `parseRosterSlotLayout` (draft-board.ts) is THE parser — STATE.yml centralization warning; reused, never re-derived |
| 13 | Position identity for replacement grouping = players catalog primary `position`; multi-eligible handling | `sleeper-api/projections-endpoint` (embedded player object never substitutes catalog). Multi-position eligibility is a wiki OPEN question (`value-over-replacement` open Qs) → v1 judgment call, disclosed: primary position only |
| 14 | Players without a scoreable projection are excluded from the replacement pool | Judgment call under the permissive-parsing posture (disclosed); they cannot be ranked, and inventing zeros would poison the baseline |

**Wiki conflicts flagged (for report):**
- `value-based-drafting` Key Decision (configurable baseline input) vs build file single method → build file wins (ROUTING conflict rule), Nick-signed.
- `value-over-replacement` Key Decision (fixed-positional-rank default; rejects optimization-based replacement) vs build file's computed last-startable + Nick-signed greedy fill → build file wins; note the VORP page's own pitfalls section endorses the unified flex pool, so the wiki is internally split; `value-based-drafting` (newer, high confidence) mandates pooling.

## Item 2 Clarify (Nick-signed, this session, all three)

1. **Mechanism = ADP-window depth**: depth_p = count of position-p players with `adp_overall` ≤ item 1's `totalStartableSlots` (the league's startable window); replacement = that depth-th player by projection order.
2. **Replace, not blend**: the calibrated level IS the line when computable ("rather than" per item text); structural is the fallback. Both carried in the result for visible reasoning (item 7).
3. **Fallbacks flagged**: league-level (unresolved format / empty snapshot → structural_only, flagged; no cross-format fallback ever) and position-level (zero in-window carriers → that position falls back to structural depth — never a zero-depth line).

## Item 2 pre-implementation coverage map (recorded BEFORE code — Rule 12)

| # | Decision | Coverage |
|---|----------|----------|
| 1 | ADP = market price, used only as a depth/availability signal, never a valuation | `league-mechanics/average-draft-position` Key Decisions |
| 2 | Format-matched ADP only; `resolveAdpScoringFormat` is THE league→format mapping; no cross-format fallback | `average-draft-position` (format leakage = the classic error); STATE.yml centralization warning; draft-board.ts |
| 3 | Sentinel-filtered `adp_rankings` is the ONLY ADP read path (never `player_projections.stats`) | `sleeper-api/projections-endpoint` (999.0 sentinel Key Decision); player_projections migration canon |
| 4 | Calibration mechanism (window depth) | No wiki page prescribes a mechanism (checked ROUTING/index: `value-based-drafting` mandates market-aware dynamic baselines directionally, prescribes nothing numeric) → build-file item text + Nick-signed Clarify #1 |
| 5 | Replace vs blend | Item text ("rather than a fixed round-count baseline") + Nick-signed Clarify #2 |
| 6 | Fallback + zero-carrier guard | Nick-signed Clarify #3; grounded in `average-draft-position` (late/thin ADP increasingly unreliable — a zero-depth line from a thin market would be market noise, not signal) |
| 7 | Position identity for ADP carriers = catalog primary position (same grouping key as item 1's pool) | ingestion.ts precedent (positional_rank derives from `players.position`); item 1 coverage row 13 |
| 8 | "Roughly half above/below" phrase = expected-behavior gloss, verified empirically in the live run, not a separate mechanism | Item text; live verification reports the above/below split per position |

## Item 3 Clarify

Declared: **zero open questions** (per BUILD_PROTOCOL — no filler). The item's two inputs are already Nick-signed: scoring application = item 1's centralized `scoring.ts`; replacement input = item 2's calibrated levels (replace-not-blend decision). Two implementation judgment calls disclosed, not asked: (a) players at positions the league cannot start are excluded from the ranked output and surfaced via count + distinct-position list (consistent with replacement.ts's unrankable-not-zero-baseline design); (b) deterministic output ordering = baseValue desc → projectedPoints desc → sleeperPlayerId asc (module-wide convention).

## Item 3 pre-implementation coverage map (recorded BEFORE code — Rule 12)

| # | Decision | Coverage |
|---|----------|----------|
| 1 | Base value = projection − replacement (VORP) | `player-evaluation/value-over-replacement` (formal definition); `league-mechanics/value-based-drafting` |
| 2 | League scoring weights from league_config, never hardcoded | `sleeper-api/league-endpoint` (scoring map semantics); item 1's `scoring.ts` (THE home, STATE.yml mandate) |
| 3 | Operative replacement line = item 2's calibrated output (structural fallback internal to it) | Nick-signed item 2 Clarify #2 |
| 4 | Input = the undrafted pool; per-pick recompute is item 4's scope | Build-file items 3/4 split |
| 5 | Negative base values kept (below-replacement players are meaningful data) | `value-over-replacement` (zero-VORP trap discussion) |
| 6 | Unrankable-position exclusion + surfacing | Judgment call, disclosed (replacement.ts design: absent level = unrankable, never zero-baseline) |
| 7 | Deterministic ordering | Judgment call, disclosed (module-wide determinism convention) |

## Build log (final)

- Item 1 SHIPPED (commit 32acc9a): `src/services/bpa/scoring.ts` + `replacement.ts`; 35/35 pure, live-verified on the real league (WR 17/20 flex wins; QB10=300.5 / RB23=206.0 / WR37=193.7 / TE10=185.1; 22/38 key-namespace intersection confirmed — coverage row 11 resolved empirically); tsc/lint/build clean. Temp live-verify script deleted after run.
- Item 2 SHIPPED (commit ef94e69): `src/services/bpa/calibration.ts`; 15/15 pure + live (ppr, 245 carriers, window 100: RB 23→35 / 206.0→161.5, WR 37→42, QB 10→11, TE 10→11; K/DEF ZERO in-window carriers → structural fallback flagged, the signed guard observed live; above-line splits 29–45% ≈ the item's half-above/half-below gloss); tsc/lint/build clean.
- Item 3 SHIPPED (commit 4bf5921): `src/services/bpa/base-value.ts`; zero-open-questions Clarify declared plainly; 12/12 pure + live full chain (3292 → 3218 rankable + 74 unrankable [CB/FB/P]; top-15 plausible 2026 PPR board — Bijan 182.6, Gibbs 164.4, Nacua 158.1; zero QBs in top 25 on the 1QB league; 3099 negative VORP rows kept honestly); tsc/lint/build clean.
- Fold complete exactly as declared — items 4+ untouched (no mid-session extension).
- Session-end: STATE.yml overwritten (arrays actively reviewed — all entries still live; centralization warning updated: BPA scoring home REALIZED; new BPA PURE-LAYER warning added); ARCHITECTURE.md Service API Reference gains `services/bpa/`; log _35 rotated to ARCHIVE (cap 5); PROGRESS.md NOT updated (3/9 sub-section — not a milestone); BUILD_INDEX header updated.

## Files read and why

Session-start protocol: MASTER_CONTEXT (auto), BUILD_PROTOCOL (Nick's prompt), STATE.yml, BUILD_INDEX, MANUAL_SETUP_CHECKLIST, wiki index + ROUTING, 03b build file. Wiki: the 8 pages listed above. Source: draft-board.ts (parseRosterSlotLayout/resolveAdpScoringFormat contracts + import conventions), adp/types.ts + adp/ingestion.ts header (projection row shape, fused-persist posture), sleeper/league-config.ts (scoring_settings_raw storage + derived_config), migration 20260723023127 (player_projections columns), scripts/sync-adp.ts (script client pattern), COMPLETION_TEMPLATES.md + ARCHITECTURE.md service section (session-end), logs/_35 + ARCHIVE.md (rotation).

## Assumptions / friction

- The item-2 "roughly half above/below" phrasing is a calibration gloss, not a mechanism — treated as an empirical check (29–45% live), disclosed in the report. If Nick intended a literal median-split mechanism, the Clarify options surfaced it and he chose the window mechanism instead.
- Prior sessions' pure verifications used ephemeral scripts (no test runner exists in package.json); followed that precedent. The build file's Resilience sub-section carries the durable-test item for the engine — those tests land with that item, not this fold.
- tsx cannot resolve `@/` aliases from outside the repo, so live-verify scripts sat at repo root as `*.tmp.ts` (deleted after each run); pure-verify scripts sat in the session scratchpad using file:// dynamic imports (type-only imports erase, so no alias needed).

## WIKI NOTE

WIKI NOTE: Four observations for the next wiki-maintenance session. (1) `league-mechanics/value-based-drafting` Key Decision prescribes baseline-method configurability (three methods as a user input) — the registered build file (and Nick's 2026-07-22 signed Clarify) ships exactly one method (ADP-window-calibrated last-startable) for v1; the page's Key Decision reads as aspirational platform scope beyond v1 and could carry a dated note. (2) `player-evaluation/value-over-replacement` Key Decision adopts the fixed-positional-rank method and rejects optimization-based replacement as opaque, while `league-mechanics/value-based-drafting` (newer, high confidence) mandates flex-pooled, dynamically recalculated baselines — and the VORP page's own Edge Cases section endorses the unified flex pool it earlier rejects; the two pages (and the one page internally) disagree and should be reconciled, with the shipped greedy-fill+ADP-window mechanism available as the platform's decided answer. (3) Genuine silence: no sleeper-api page documents Sleeper's flex-label eligibility semantics (which positions FLEX/SUPER_FLEX/REC_FLEX/WRRB_FLEX/IDP_FLEX accept) — `league-endpoint` lists the labels without eligibility; the BPA engine ships FLEX/SUPER_FLEX wiki-grounded (flex-spot-configuration's generic semantics) and the rest as declared general knowledge; a short addendum on the league-endpoint page would close the gap. (4) `league-mechanics/average-draft-position` calls for surfacing dispersion "wherever the underlying data supports it" — the projections-endpoint page already records that this source has no dispersion metadata; no action needed, noted only to confirm the calibration deliberately uses point means.
