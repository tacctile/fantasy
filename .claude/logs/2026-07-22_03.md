# Session Log — 2026-07-22_03
**Session type:** feature-build (autonomous, via BUILD_PROTOCOL.md)
**Scope:** 01_foundation.md → "Schema — league-scoped state" → third item: `matchups` table migration

---

## Session start

- Read in protocol order: MASTER_CONTEXT.md (auto-loaded), STATE.yml, BUILD_INDEX.md, MANUAL_SETUP_CHECKLIST.md, wiki/index.md, wiki/ROUTING.md, build/01_foundation.md.
- STATE.yml pointer confirmed against the build file: first unchecked item is `matchups`. No stale-pointer situation.
- Manual Setup Flag: clear. Everything this item needs (`db push` rule, password in `.env.local`) is `[x]`; the `[~]` admin-auth item belongs to the integrity item's session.
- Log cap triggered: this file is the 6th individual log, so `2026-07-21_05.md` was condensed into `ARCHIVE.md` (newest-first placement) and deleted before this file was created.

## Wiki pages read (2 of 3 budget)

- `wiki/topics/sleeper-api/matchup-endpoint.md` — response grain (one row per roster per week), matchup_id grouping semantics + null-on-bye, points/custom_points precedence decision, no-finality-signal + retrieval-timestamp decision, median-league invisibility, bracket lives elsewhere.
- `wiki/topics/espn-api/matchup-response-structure.md` — schedule/home/away shape, scoringPeriodId vs matchupPeriodId ADR (store both, never collapse), multi-week summation pitfall, authoritative totalPoints, missing-side byes, live-field names flagged as unverified open question.

## Pre-implementation coverage map (recorded before any code)

- Row grain + pairing reconstruction → sleeper-api/matchup-endpoint (Key Decision: group on (league_id, week, matchup_id), validate size, exclude nulls)
- Dual period columns (week + matchup_period) → espn-api/matchup-response-structure (Key Decision: two distinct fields, per-league mapping from settings)
- points/custom_points precedence → sleeper-api/matchup-endpoint (Key Decision: effective = custom_points when non-null else points)
- Finality columns (fetched_at/is_final) → both pages' no-finality-signal findings + the Sleeper page's snapshot/finality-heuristic Key Decision
- league_id FK target, platform/season_year duplication, RLS-at-creation, set_updated_at, cascade → established 01_foundation.md precedent (leagues/rosters/roster_players items)
- Concrete DDL (PK shape, column set, check constraints) → WIKI SILENT — schema-reference has no league-scoped-state DDL ADR (third consecutive session). Declared at decision time; resolved via Clarify. Report: "gap found and flagged" + standing WIKI NOTE.

## Clarify step (4 questions via AskUserQuestion; Nick chose all recommended options)

1. **Row grain** → per-roster weekly rows. PK (league_id, week, native_roster_id); ESPN contests split into two rows at ingestion; byes = null pairing key; median-league math needs every roster's weekly points anyway. ESPN multi-week playoff rounds land as one row per constituent week sharing a matchup_period.
2. **Score columns** → raw pair + stored generated `effective_points = coalesce(custom_points, points)`. ESPN live/projected columns deliberately excluded — exact field names are an open wiki question; Wave 2 adds them via new migration once verified against a live payload.
3. **Finality** → `fetched_at timestamptz` nullable + `is_final boolean not null default false`, promoted only by Wave 2+ sync per the wiki finality heuristic (games complete + correction window + stable repeat reads).
4. **Native detail** → `native_matchup_id integer` nullable (Sleeper matchup_id / ESPN schedule entry id — both wire integers, roster-id precedent; null = bye) + `is_home boolean` ESPN-only check. ESPN `winner` excluded (provisional per wiki, derivable from points + finality).

## Decisions and reasoning

- Pre-flight collision check per Shared-Database Constraints: `list_tables` (read-only) confirmed no `matchups` in prolabel's public schema.
- `npx supabase migration new create_matchups_table` → `20260722052726_create_matchups_table.sql` (single migration, single table, no new enums).
- `matchup_period_equals_week_on_sleeper` check encodes the approved grain decision (Sleeper has no native contest-interval ID; rows carry matchup_period = week). If a later wave wants Sleeper playoff-round grouping via the bracket resources, that is a new decision + new migration.
- `custom_points_sleeper_only` check follows the roster_players platform-constrained-native-column precedent (ESPN folds commissioner adjustments into its total; no ESPN analogue exists to store).
- `points` nullable — as-received semantics; not-null would be an invented constraint stronger than the wire guarantees (ESPN placeholder/future entries can lack side totals).
- No indexes beyond the PK/unique side effects — the integrity item owns the index sweep (same call as the last two sessions).
- Pushed via the allowed `npx supabase db push` rule, password sourced from `.env.local` in-shell (`--password "$(sed -n ... | tr -d '\r')"`). Same cosmetic Docker/pg-delta warning as prior sessions; remote apply clean.

## Verification (read-only, live DB — all passed)

- `information_schema.columns`: 15 columns, expected types/nullability; `effective_points` is GENERATED ALWAYS AS `COALESCE(custom_points, points)` STORED.
- `pg_constraint`: PK `(league_id, week, native_roster_id)`; FK `league_id` → `leagues` (cascade); composite FK `(league_id, native_roster_id)` → `rosters` (cascade); checks `matchup_period_equals_week_on_sleeper`, `is_home_espn_only`, `custom_points_sleeper_only` with expected definitions.
- `pg_class.relrowsecurity` = true; `set_updated_at` trigger present (non-internal); `supabase_migrations.schema_migrations` row for `20260722052726` present.
- `npm run build` passes (no app code changed this session).

## Assumptions / friction

- None material. The item's own wording ("participant/score fields") left the entire DDL open, but that is exactly what the coverage map + Clarify flow absorbed; all four shape decisions were Nick's explicit choices.
- The schema-reference DDL silence is now three sessions running — the standing WIKI NOTE below repeats the suggestion so the next wiki-maintenance session sees it.

## WIKI NOTE

WIKI NOTE: `schema-reference` still has no page covering the league-scoped state tables' concrete DDL decisions (rosters / roster_players / matchups so far — grain, PK shapes, platform-constrained native columns, finality columns). A "league-scoped state tables" DDL addendum/decision-record page would let future waves consume these shapes from the wiki instead of from build-file archaeology. Third consecutive session flagging this.
