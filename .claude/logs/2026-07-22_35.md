# Session Log — 2026-07-22_35

**Session type:** feature-build (autonomous, BUILD_PROTOCOL.md — Nick: "read this and lets get to work")
**Fold declared (at session start, before Clarify):** 03b_draft_assistant_live_draft.md → Client-side
live sync items 1–2: (1) client polling/subscription mechanism scoped to the active league, alive only
while the board is visible and `is_draft_active` is true; (2) merge incoming `draft_state` rows into
the board's existing player/availability derivation by `pick_number`, deduplicated against redundant
re-renders. Exactly the fold STATE.yml pre-staged, at 2 items under the live-behavior ceiling (2–3 max
for this sub-section — race/timing failure modes get full attention). Items 3–5 (optimistic UI,
conflict rollback, status indicator) wait for the next session. No hard stops: same sub-section, no
new wiki category, no new external service, no `[ ]` manual-setup dependency (the two Wave 3b
MANUAL_SETUP items are explicitly draft-day in-the-moment actions, not build dependencies).

**Wave self-location:** 01 🟢; 02 no `[ ]` (9 ESPN `[!]`, cadence `[~]`, finality `[>]`); 03a complete
except bye-week `[>]`; 03b first unchecked = client-side live sync item 1. Matches STATE.yml exactly.

**Wiki pages read this session (1):** `sleeper-api/draft-endpoint.md` (the build file's listed page —
pick identity/ordering semantics, `roster_id` as the ownership field, `pick_no` as ground truth).

---

## Pre-implementation coverage map (recorded BEFORE any code, per Rule 12)

### Item 1 — client polling mechanism
- Transport = interval re-fetch piggybacked on the existing 5s DraftPollTicker (no Supabase Realtime)
  ← Nick-signed Clarify Q1 this session, composing with the prior orchestration decisions (STATE.yml
  standing warning: "extend it, don't fork a second poll driver"; client-driven 5s cadence
  Nick-signed 2026-07-22). Realtime was declined explicitly: it would add `draft_state` to the shared
  prolabel database's realtime publication (shared-infra config, Rule 13 ambiguity) plus a second
  live channel.
- Fetch shape = full `draft_state` snapshot per executed tick ← Nick-signed Clarify Q2 (a snapshot
  self-heals manual-pick UNDO deletions, which an incremental since-max-pick fetch can never see).
- Visibility gating = pause while `document.visibilityState === 'hidden'`, immediate tick on return
  ← build-item text ("only while the draft board is visible") + Nick-signed Clarify Q3. The
  visibilitychange mechanism itself is a generic web API — below wiki altitude (established declared
  silence category; checked ROUTING.md/index.md: no category covers client runtime mechanics).
- router.refresh role = client merge replaces refresh-on-picks; refresh survives only for
  stand-down/session-end outcomes ← Nick-signed Clarify Q4.
- Rate posture: no new Sleeper request type — each executed tick still runs the existing
  `runActiveDraftPoll` → `syncLeagueDraftState` path, recorded in `sync_runs`; the added snapshot
  read is a Supabase query, not a Sleeper call. 5s per-league cadence remains inside the documented
  ceiling, absorbed by the client's 250ms min-gap gate (established Wave 2 citation, sleeper-api
  rate-limits; unchanged this session).
- Snapshot row fields (`pick_number`, `round`, `sleeper_player_id`, `native_roster_id`, `source`) ←
  existing Wave 1 `draft_state` schema via the existing `RecordedPick` shape (services/draft-picks.ts);
  ownership dimension = roster, per sleeper-api/draft-endpoint.md (read this session): `roster_id` is
  the ownership field, `pick_no` is ground truth ordering — the merge keys on our persisted
  `pick_number`/`native_roster_id`, which Wave 2 ingestion already mapped from those fields.

### Item 2 — merge/dedup into board state
- Merge key = `pick_number` ← build-item text; `pick_no` ground-truth rule ←
  sleeper-api/draft-endpoint.md (read).
- Availability overlay: a third `DraftBoardAvailability` value `'drafted'`, produced ONLY by the
  client-side merge (the Wave 3a service never emits it). Precedence: `'rostered'` (from
  roster_players) wins over the drafted overlay — rostered rows carry richer attribution (team/owner
  name) and the fully-rostered 2025 league must not regress to bare "Drafted" labels; during a live
  draft rosters aren't yet synced, so the overlay is the live signal. JUDGMENT CALL, disclosed here
  at decision time. Wiki: checked ROUTING.md + index.md — no category covers app-internal UI
  availability naming (schema-reference covers league scoping/identity/config, not UI state) —
  DECLARED SILENCE; display treatment Nick-signed in item-2 Clarify.
- Dedup = canonical fingerprint (sorted `pickNumber:playerId:rosterId:source`) with setState bail-out
  on equality (same reference → React skips re-render); merged list memoized on
  `[players, livePicks]` ← engineering mechanism, below wiki altitude.
- Available-only filter behavior flows unchanged from the availability union
  (`availability !== 'available'` already excludes `'drafted'`) ← existing use-player-list contract.
- A picked player absent from the board pool (no ADP row, not rostered): merge skips it this fold —
  pool membership is the data layer's (Nick-signed Wave 3a pool rule); the recent-picks feed (later
  item) surfaces every pick regardless ← judgment call put to Nick in item-2 Clarify.

## Clarify (per-item batches, per Folding Policy)
- Item 1 batch (4 questions, all Nick-signed as recommended): interval re-fetch on the existing 5s
  tick; full snapshot per tick; pause-when-hidden + instant tick on return; merge replaces
  refresh-on-picks.
- Item 2 batch (2 questions, both Nick-signed as recommended, asked immediately before item 2 was
  built): drafted-row display = "Drafted · Pick N" + dimmed (same whole-row opacity as rostered);
  out-of-pool picks skipped silently this fold (pool membership stays the Wave 3a data-layer rule;
  the recent-picks feed item later surfaces every pick).
- MID-VERIFICATION PREMISE CORRECTION (third mini-batch, Nick-signed): the item-2 Clarify premise
  "your fully-rostered 2025 league looks unchanged" was WRONG — real data showed 40 of the 170
  drafted players were dropped mid-season (in draft_state, on no roster), so they flip from
  "Available" to "Drafted · Pick N". Nick signed OVERLAY-ALWAYS (recommended) over session-gating:
  on a draft board, drafted-then-dropped reading as drafted is honest draft history, and
  session-gating has a real failure (picks vanish at End draft until the roster sync catches up).
  The 2026 draft is unaffected (new league_id, fresh draft_state).

---

## Item 1 — client polling mechanism

**Built:**
- `services/draft-picks.ts` + `listDraftPicks(db, leagueId)` — full league snapshot, pick_number
  asc; malformed ID → [] (listScoredWeeks precedent), genuine errors throw.
- `actions.ts` `pollActiveDraft`: attaches the `listDraftPicks` snapshot to every executed poll
  (including sync-failure ticks — current DB state is still the honest board state; next tick
  retries the sync); REMOVED revalidatePath-on-picksWritten (merge replaces refresh, Nick-signed).
  Result type = Extract<polled> & {picks} | Exclude<polled> | unauthorized.
- `draft-poll-ticker.tsx`: visibility gating (tick short-circuits while document hidden — interval
  keeps running but makes zero requests; visibilitychange listener fires an immediate catch-up tick
  on return); `onPicks` prop (required, referentially stable — in the effect dep list) receives each
  executed poll's snapshot; router.refresh() now ONLY on stand-down outcomes.
- `draft/page.tsx`: `listDraftPicks` joined the parallel fetch → `initialPicks` prop (board shows
  drafted state on first paint, not 5s later).
- `draft-board-shell.tsx`: live-pick custody — `useState(initialPicks)` + stable `handlePicks`.

## Item 2 — merge/dedup into board state

**Built:**
- `components/draft-board/live-picks.ts` (NEW, pure): `picksFingerprint` (order-insensitive
  `pick:player:roster:source` sorted join) + `mergePicksIntoPlayers` (Map by player, earliest pick
  wins if a duplicate ever appeared; 'rostered' precedence; unchanged input → same array reference;
  changed rows new objects, untouched rows keep identity for downstream memo stability).
- `services/draft-board.ts`: `DraftBoardAvailability` extended with `'drafted'` (doc: client-merge-
  only, service never emits) + `draftedPickNumber: number | null` on DraftBoardPlayer (service
  always null).
- Shell: fingerprint bail-out in `handlePicks` (identical snapshot → same state reference → React
  skips re-render); `mergedPlayers` memo feeds BOTH PlayerBoard and RosterPanel (one source of
  truth; roster-fill reads 'rostered' only, so drafted rows are invisible to it by design — the
  roster panel's own live extension is a later 03b item).
- `player-row.tsx`: drafted rows dim (opacity-60, same as rostered) + "Drafted · Pick N"
  (tabular-nums) in the availability cell; plain "Drafted" if pick number ever null.
- `use-player-list.ts`: untouched — `availableOnly` already excludes any non-'available' value.

**Verified (temporary harness scripts/__verify-live-sync.tsx, tsx + admin client + anon client,
deleted after): 24/24 PASS against the real league** — snapshot: 170 rows, ordered, all merge
fields, malformed → [], ANON CLIENT → ZERO ROWS (RLS deny-by-default on the new read path); merge
on real data: overlay marks exactly the drafted-but-unrostered in-pool players, rostered rows win
precedence with object identity preserved, every drafted row carries its pick number; write cycle
through the real path: manual pick accepted at 171 → snapshot grows → fingerprint changes /
order-insensitive on shuffle → merged row drafted with pick 171 → only that row changes vs baseline
merge → available-only filter excludes it → rendered row shows "Drafted · Pick 171" + opacity-60 →
out-of-pool synthetic pick skipped without crash → undo removes exactly pick 171 → snapshot
fingerprint-identical to baseline → merge heals (player available again) → all 170 poller rows
intact, all source='sleeper_poll'. First harness run: 4 FAILs, ALL harness premise errors (the
40-dropped-players finding above), zero product defects; assertions corrected to compute the
expected changed set from data, then 24/24. tsc clean, lint clean, `npm run build` clean (route
table unchanged).

**Not harness-verifiable, carried to Nick's visual check:** the ticker's browser-runtime behavior
(5s cadence delivering into state, visibility pause/catch-up) — no DOM test runtime exists in the
repo (the mandated race/isolation tests are the resilience item, a later fold); the server side of
every tick (TTL gate, recording) was live-verified 37/37 last session and is unchanged.

## Session-end bookkeeping
- 03b build file: client-side live sync items 1–2 `[x]` with Nick-signed decision annotations
  (including the overlay-always premise correction). Items 3–5 remain — next fold.
- ARCHITECTURE.md: Status paragraph extended (2/5 sub-section); tree updated — actions.ts entry
  corrected (it was stale from the orchestration session: missing the three session actions) and
  draft-session-toggle/draft-poll-ticker added (also missing), plus live-picks.ts, shell custody,
  player-row drafted state; Service API Reference: listDraftPicks bullet + draft-sessions
  consumed-by paragraph updated (snapshot delivery, no revalidate-on-picks).
- PROGRESS.md: NOT updated — 2/5 items of a sub-section is partial progress, not a milestone.
- DESIGN_SYSTEM.md: NOT updated — zero token changes (reused opacity-60, text-secondary-foreground,
  tabular-nums).
- Log cap: this file is the 6th → 2026-07-22_30's key decisions concatenated into logs/ARCHIVE.md,
  original deleted (done before this log was created).
- STATE.yml overwritten completely; arrays re-examined (visual-check entry extended with the
  live-sync behaviors + the 40-drafted-players display change; all other entries re-verified still
  true; npm-audit note carried — zero new packages this session).
- Blast radius: live database writes this session = the verification harness's ONE draft_state
  insert (pick 171, source='manual') and its undo DELETE of that same row — both through the
  established service write path, both on the fantasy-owned draft_state table, net zero rows;
  everything else read-only (leagues, rosters, roster_players, league_config, draft_state,
  draft_sessions, players, adp_rankings via services). No DDL, no migration.

## Files read this session (and why)
Protocol order: MASTER_CONTEXT (auto-loaded), BUILD_PROTOCOL, STATE.yml, BUILD_INDEX,
MANUAL_SETUP_CHECKLIST, wiki/index, wiki/ROUTING, 03b build file, wiki draft-endpoint page. Source:
draft-poll-ticker/actions/draft-board-shell/page (the seams being extended), draft-sessions.ts
(poll result types), draft-picks.ts (RecordedPick + write path), draft-board.ts (player type +
availability derivation; excerpts), use-player-list.ts + player-row.tsx (merge consumers),
admin.ts + package.json (harness setup), ARCHITECTURE.md sections (updates), logs listing + _30
(rotation), ARCHIVE.md head (ordering).

WIKI NOTE: none

