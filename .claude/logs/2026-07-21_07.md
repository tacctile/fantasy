# Session Log — 2026-07-21_07
**Session type:** feature-build (autonomous, via BUILD_PROTOCOL.md)
**Scope:** 01_foundation.md — "Schema — league + config" section, third item: `league_config` table migration

---

## Files read and why

- `.claude/BUILD_PROTOCOL.md` — Nick dropped it in ("read this and lets get to work"); governs the session
- `.claude/MASTER_CONTEXT.md` — auto-loaded via CLAUDE.md
- `.claude/STATE.yml` — located last session's stopping point; next unchecked item = `league_config` migration
- `.claude/BUILD_INDEX.md` — confirmed roadmap/registry unchanged; Wave 1 🟡, 01_foundation.md active
- `.claude/MANUAL_SETUP_CHECKLIST.md` — protocol step 3a; all Wave 1 credential items `[x]`, the `[~]` admin-auth item belongs to the integrity/RLS item, not this one — nothing halts
- `wiki/index.md`, `wiki/ROUTING.md` — schema-reference is the covering category
- `.claude/build/01_foundation.md` — located the first unchecked item
- `wiki/topics/schema-reference/league-configuration-data-model.md` — the item's covering ADR (1 of 3 page budget; the other two schema-reference pages were read in prior sessions and cover already-built items)
- `.claude/logs/2026-07-21_02.md` — oldest log, condensed into ARCHIVE.md under the 5-file cap
- `supabase/migrations/20260722044457_create_leagues_table.sql` — style/pattern reference for the new migration
- `.claude/COMPLETION_TEMPLATES.md` — feature-build template
- `.claude/ARCHITECTURE.md` — schema inventory section to update at session end
- `.claude/logs/ARCHIVE.md` — append target for the _02 condensation

## Clarify step (3 questions, all answered before any code)

1. **Schema Rules 2/3 letter vs. ADR-exact shape** — `platform`/`season_year` on league_config? Nick chose **ADR-exact**: no duplicated columns; the 1:1 FK to a leagues row that is already one-row-per-platform-season-league satisfies the rules' intent without denormalization drift.
2. **Nullability** — Nick chose **all three JSONB columns NOT NULL** (ingestion writes raws + derived together; the invariant lives in the schema).
3. **FK on-delete** — Nick chose **ON DELETE CASCADE** (config is a 1:1 extension of its league row; consistent with crosswalk→players).

## Pre-implementation wiki coverage map (recorded BEFORE any code — Absolute Rule 12)

| Decision | Covered by |
| -------- | ---------- |
| Table exists, named `league_config`, config-as-data | MASTER_CONTEXT Schema Rule 5 + ADR `schema-reference/league-configuration-data-model` |
| Key column `league_id uuid` FK → `leagues.platform_league_uuid` | Build item text + leagues item's standing definition of `league_id` + ADR ("keyed by platform_league_uuid") |
| `league_id` as PRIMARY KEY (1:1 with leagues, no surrogate id) | ADR: "one `league_config` table keyed by `platform_league_uuid`" |
| Column names + types: `scoring_settings_raw jsonb`, `roster_settings_raw jsonb`, `derived_config jsonb` | ADR, explicit |
| What each raw column holds per platform | ADR, explicit (comments in migration quote it) |
| `derived_config` key contents | ADR lists them — but JSONB shape is not schema-enforced; derivation is Wave 2. No schema decision required this session |
| No `platform`/`season_year` duplication | Governance conflict (Schema Rules 2/3 letter vs. ADR three-column shape) → Nick's Clarify decision |
| NOT NULL on all three JSONB columns | **ADR silent** (searched ROUTING.md/index.md — schema-reference is the covering category, no more specific page). Declared at decision time; resolved by Nick's Clarify decision |
| FK on-delete behavior (CASCADE) | **ADR silent** (same search). Declared at decision time; resolved by Nick's Clarify decision |
| `created_at`/`updated_at` + `set_updated_at` trigger | **ADR silent.** Established repo pattern (players, leagues migrations) — pattern fill, declared here at decision time |
| RLS enabled at creation, deny-by-default, owner-write policies deferred | Build file integrity item scope + established pattern from all prior table migrations |
| Migration via `supabase migration new`, never editing baseline | MASTER_CONTEXT Supabase Migration Rule |
| Pre-push table-name collision check against shared prolabel DB | ARCHITECTURE.md Shared-Database Constraints |

Coverage verdict for the report: **gap found and flagged** — three DDL-level silences (nullability, on-delete, timestamps), all declared at decision time and resolved via Clarify/repo pattern, never filled silently.

## Decisions and reasoning

1. **One item only** — the two remaining league+config siblings from last session are done; this is the section's last item. No folding question needed (nothing adjacent within the section).
2. **Log cap exercised** — creating this file made 6; `2026-07-21_02.md` condensed into `ARCHIVE.md` and deleted, per MASTER_CONTEXT Session-End Steps.
3. **PK doubles as the league_id index** — the integrity item's later "index on every league_id FK" requirement is already satisfied for this table by the primary key itself; noted so the integrity session doesn't add a redundant index.
4. **Collision check before push** — `league_config` is a genuinely new table family; verified against live `public` schema table list before pushing (Shared-Database Constraints).

## Implementation (filled in as work completed)

- Migration created via `npx supabase migration new create_league_config_table` → `supabase/migrations/20260722045532_create_league_config_table.sql`
- SQL: `league_config` per the coverage map above — league_id uuid PK/FK cascade, three NOT NULL jsonb columns, timestamps, RLS enabled, `set_updated_at` trigger
- Collision check: no existing `league_config` table in the shared `public` schema (verified via live table list)
- Pushed via `npx supabase db push` (allowed rule; `SUPABASE_DB_PASSWORD` sourced from `.env.local` in-shell, never echoed)
- Verified live via read-only queries: columns/types/nullability, PK + FK (cascade) constraints, RLS enabled, trigger present, migration history row recorded

## Friction / what would have improved the session

- None material. The ADR was precise enough that all three Clarify questions were genuine DDL-altitude judgment calls, not gaps in the decision itself. BUILD_PROTOCOL self-location worked cleanly from STATE.yml.

## WIKI NOTE

WIKI NOTE: `schema-reference/league-configuration-data-model.md` fixes the table's shape but is silent on three DDL-level details a migration must decide: column nullability, the league FK's on-delete behavior, and audit timestamps. All three were declared at decision time and resolved via Clarify (NOT NULL on all three JSONB columns; ON DELETE CASCADE) and established repo pattern (timestamps + `set_updated_at`). If wiki-maintenance wants the ADR to be fully self-sufficient for re-derivation, a short addendum recording these three choices would close the gap.

## Session-end checklist

- STATE.yml overwritten completely ✔
- This log written (NN=07; highest existing for 2026-07-21 was 06) ✔
- Log cap: _02 condensed into ARCHIVE.md and deleted ✔
- PROGRESS.md NOT updated — single-item migration session; section completion is not a milestone trigger (full checklist completion is) ✔
- ARCHITECTURE.md updated (league_config added to Database Schema (live); Status line advanced) ✔
- DESIGN_SYSTEM.md not touched (no token changes) ✔
- 01_foundation.md: league_config item [ ] → [x] with annotation ✔
- Commit + push to main ✔ (build-side pushes every session)
