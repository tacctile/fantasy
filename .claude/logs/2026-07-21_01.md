# Session Log — 2026-07-21_01
**Session type:** feature-build (autonomous, via BUILD_PROTOCOL.md)
**Scope:** 01_foundation.md — Project scaffold section (items 1-6, folded with Nick's approval)

---

## Files read and why

- `.claude/MASTER_CONTEXT.md` — auto-loaded via CLAUDE.md (rules, stack, constraints)
- `.claude/BUILD_PROTOCOL.md` — Nick opened it and said "read this and lets get to work"; governs the session
- `.claude/STATE.yml` — self-location: pointed at 01_foundation.md
- `.claude/BUILD_INDEX.md` — confirmed roadmap/registry unchanged; Wave 1 first not-fully-checked file
- `.claude/MANUAL_SETUP_CHECKLIST.md` — Manual Setup Flag check (step 3a of protocol)
- `wiki/index.md`, `wiki/ROUTING.md` — session-start protocol steps
- `.claude/build/01_foundation.md` — located first unchecked item (Next.js scaffold)
- `.gitignore`, `README.md` — pre-merge conflict check before create-next-app merge
- `tsconfig.json`, `src/app/layout.tsx`, `src/app/page.tsx`, `components.json`, `src/app/globals.css` — post-scaffold verification
- `.claude/ARCHITECTURE.md`, `.claude/DESIGN_SYSTEM.md`, `.claude/COMPLETION_TEMPLATES.md`, `.claude/PROGRESS.md` — session-end updates

Wiki pages: the three schema-reference pages listed in 01_foundation.md's WIKI PAGES TO CONSULT were deliberately NOT read — they inform the migration items, not the scaffold. Deferred to the schema sessions to keep reads within budget and relevant.

## Decisions and reasoning

1. **Fold items 1-6 into one session** — BUILD_PROTOCOL's edge case says ask Nick rather than decide unilaterally; asked via AskUserQuestion, Nick chose "Fold all six." shadcn init literally requires the Next app to exist and performs the globals.css token setup itself, so the six items are one coherent pass.
2. **npm as package manager** — asked Nick; chose npm (already installed, boring default, native Vercel support). package-lock.json committed.
3. **Scaffold in scratchpad, merge into root** — create-next-app refuses non-empty dirs (repo root has .claude/, wiki/, CLAUDE.md). Scaffolded `fantasy-scaffold` in the session scratchpad with `--skip-install`, copied in only: src/, public/, package.json, tsconfig.json, next.config.ts, postcss.config.mjs, eslint.config.mjs, next-env.d.ts. Excluded the scaffold's .git, .gitignore, README.md, and its generated CLAUDE.md/AGENTS.md (would have clobbered the governance loader). Renamed package to "fantasy". Ran npm install in the root.
4. **Kept the repo's existing .gitignore** — it is already a comprehensive Node template covering .next/, node_modules/, .env* with !.env.example.
5. **shadcn CLI flags** — the current shadcn CLI (4.13.1) no longer has --base-color; used `init -y -d --css-variables` (defaults: template next, preset base-nova, neutral base). components.json: cssVariables true, iconLibrary lucide, @/ aliases.
6. **Font variable fix** — shadcn's generated globals.css maps `--font-sans: var(--font-sans)` in @theme inline, but the create-next-app layout registered the Geist font as `--font-geist-sans`. Changed the layout's Geist variable to `--font-sans` so the token chain actually resolves. Geist_Mono left as `--font-geist-mono` (globals maps --font-mono to that name directly).
7. **Replaced boilerplate page.tsx** — the create-next-app default page contains inline hex (#383838, #ccc, #1a1a1a) and arbitrary values, violating Design Token Discipline. Replaced with a minimal token-only shell that also serves as the verification vehicle: `@/lib/utils` cross-directory import (alias check), lucide-react icon, tabular-nums on a data display, default spacing scale only. JSDoc added per component checklist. Error boundary not added — placeholder shell, not a major component.
8. **Did not touch .gitignore's `logs` pattern** — discovered it ignores .claude/logs/ (this very file will not be pushed). Governance-adjacent; flagged in STATE.yml warnings and the completion report instead of changing it unilaterally mid-scaffold-session.

## Assumptions where ambiguous

- Metadata title set to "fantasy", description from MASTER_CONTEXT's one-line project definition — build file didn't specify.
- Turbopack enabled (create-next-app default) — no rule against it; Vercel supports it.
- npm audit's 3 transitive vulns (postcss/sharp pinned inside next@16.2.11) accepted as upstream's problem — recorded in STATE.yml known_issues.

## Patterns followed / deviated

- Followed: Design Token Discipline (zero hex verified by grep over src/), Tailwind default spacing only, tabular-nums, default React JSX runtime, @/ for cross-directory imports, default export for the page component.
- Deviated: none.

## What was missing / friction

- The build file's scaffold items predate shadcn CLI 4.x — flag names changed (--base-color gone). Cosmetic friction only.
- create-next-app now emits its own CLAUDE.md/AGENTS.md — future scaffold-adjacent instructions should mention excluding them; handled here without damage.

## Verification

- `npm run build` — passes (Next.js 16.2.11, Turbopack, TypeScript check clean, static prerender of / and /_not-found)
- `npm run lint` — passes, zero warnings
- `grep '#[0-9a-fA-F]{3,8}' src/` — zero matches
- console.log count in src/: 0

## WIKI NOTE

WIKI NOTE: none

