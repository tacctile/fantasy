# Session Log — 2026-07-22_18
**Session type:** non-code (investigation + registration; Nick-prompted, two-turn)
**Scope:** Investigate the FINALITY-PROMOTION GAP known_issue (mechanism, wiki coverage, implementation proposal, urgency); after Nick's sign-off, register the design as a build item and record deadline/scheduling in STATE.yml. Explicitly NO code, NO migrations, NO build.

---

## Session start

- Protocol-order reads: MASTER_CONTEXT.md (auto-loaded), STATE.yml, BUILD_INDEX.md, wiki/index.md, wiki/ROUTING.md, then build files from `current_build_files` as needed (02_data_pipeline.md full; 03a not read — its content wasn't needed for this investigation and the prompt scoped reads to the gap).
- Source files read to confirm the mechanism (prompt-required investigation targets): `src/services/sleeper/league-matchups.ts` (the `is_final` writer), `src/app/api/cron/sync-leagues/route.ts` (current-week-only scoping), glob of `src/app/api/cron/` (route inventory), grep for `is_final` across `src/` (confirmed: no reader exists — only the writer and generated types).
- `.claude/ARCHITECTURE.md` consulted narrowly (grep + targeted read of the schema/service sections) for the deferral note the known_issues entry cites and the matchups/player_scores finality column documentation.
- `.claude/COMPLETION_TEMPLATES.md` read for the non-code template and report lines.

## Wiki pages read (2 of 3-page budget)

- `sleeper-api/matchup-endpoint` — the covering page named by known_issues: "Stat-Correction Window and a Practical Finality Heuristic" section + Key Decision #3 (promote a week only after: all games complete + correction window through the following Thursday at minimum + repeated stable reads). Open question #4 notes the Thursday cutoff is a planning assumption, not a documented contract.
- `sleeper-api/nfl-state-endpoint` — week semantics load-bearing for the design (Tuesday rollover, `week` gated by `season_type`, never date arithmetic, `display_week` UI-only).

## Findings (turn 1 — investigation, reported to Nick before any file changes)

1. **Mechanism confirmed exactly as recorded:** `syncLeagueMatchups` computes finality from league lifecycle status alone (`'complete'` → all rows true, else false). The daily cron syncs only `resolveMatchupSyncWeek(state)` — current regular-season week, matchups skipped outside `regular`. So in-season rows land false and are never revisited after the Tuesday rollover; even at league completion the cron only touches the then-current week. The only promotion path was Nick manually running `npm run sync:sleeper` after completion. The "league-complete sweep" was never a mechanism — just a manual script plus remembering.
2. **Wiki verdict: unbuilt, not undesigned.** The finality policy is a decided ADR (matchup-endpoint Key Decision #3), restated in ARCHITECTURE.md's matchups/player_scores entries. Only the operational vehicle (which job, when) is unspecified — which is *how*, correctly outside wiki scope. Additional framing point surfaced: even an automated league-complete sweep alone would not satisfy the ADR — it demands rolling per-week promotion in-season.
3. **Two genuine wiki-silence sub-decisions declared at decision time** (searched ROUTING.md/index.md — derivable from decided pages but stated nowhere): (a) NFL-state week advancement as the "all games for week N complete" proxy; (b) "repeated stable reads" operationalized as one Friday-or-later UTC re-read matching the stored values, at daily cadence. Both flagged for Nick's sign-off per Rule 12's genuine-silence provision.
4. **Urgency assessment: not urgent.** Offseason (matchups skipped, verified live per prior session); zero affected rows (the only ingested league is 2025, swept while `complete` → all rows final); no consumer reads `is_final` anywhere in src/; failure mode is soft and fully retroactively fixable (weeks re-syncable any time). First real bite: week 1→2 rollover ~2026-09-15, promotion first needed ~2026-09-18. Recommendation: does not jump Wave 3a.

## Nick's decisions (turn 2 — all signed 2026-07-22)

- Both wiki-silent sub-decisions approved as proposed (week-advance proxy; Friday-or-later stable-read trigger at daily cadence).
- Register the one-item amendment to 02_data_pipeline.md's cron sub-section as a scope-error correction (ARCHITECTURE's note deferred promotion "to the cron sub-section" but the three registered cron items omitted it — Build File Amendment Norm's correction case).
- Record design + deadline in nick_pending AND known_issues so it travels with the repo.
- Do NOT build now. Next buildable session returns to Wave 3a. Build trigger: own standalone fold once Wave 3a is progressing, OR bundled with the season-start cadence session — whichever comes first; hard deadline before the week 1→2 rollover.

## Changes made (turn 2)

- `.claude/build/02_data_pipeline.md` — one item added to "Cron / polling strategy": in-season finality promotion (previous-week re-sync + promotion pass per the matchup-endpoint ADR; league-complete backstop sweep; caller-driven finality extension to `syncLeagueMatchups` noted as required). **Marked `[>]` deferred, not `[ ]`** — judgment call, disclosed to Nick in the report: an autonomous BUILD_PROTOCOL session self-locates onto unchecked items, and Nick's explicit ruling is that Wave 3a comes first; `[>]` with flip-to-`[ ]` conditions written into the item encodes the sequencing in the item state itself instead of relying on STATE.yml prose alone.
- `.claude/STATE.yml` — complete overwrite. Arrays actively reviewed: all prior nick_pending/known_issues/warnings entries verified still true and carried deliberately (npm audit note, ir_slot_count, ESPN timing blocker, season-start cadence, league renewal, Wave 4 sign-in, Wave 5 waiver formula, all four warnings). New nick_pending entry: finality-promotion build scheduling + deadline. Season-start cadence entry amended to name itself the fallback vehicle. known_issues finality entry rewritten: designed + registered, still unbuilt, deadline recorded. current_focus rewritten for this session; kept the 2026-07-23/24 catalog-cron 'skipped'-row expectation (still pending, prevents a future session misreading normal telemetry).
- `.claude/logs/` — cap enforced: this file is the 6th individual log; `2026-07-22_13.md` condensed into `ARCHIVE.md` (newest-first) and deleted.
- NOT updated (with reasoning): `PROGRESS.md` — a build-file registration amendment is not a milestone under any of the four listed categories. `ARCHITECTURE.md` — no structure changed; its existing matchups-note deferral language ("deferred to the cron sub-section") is now *accurate* because the cron sub-section actually holds the item. `BUILD_INDEX.md` — no new build file, no status change (02 stays 🟡). No wiki files touched (read-only, ordinary session).

## Assumptions / judgment calls

- `[>]` vs `[ ]` for the new item — see above; disclosed in the completion report.
- Committing the bookkeeping — Nick's "so it travels with the repo and doesn't get lost" read as authorization to commit, consistent with the repo's portability goal and every prior session's practice.
- Read 02_data_pipeline.md in full and ARCHITECTURE.md sections beyond the strict Session-Start list — required to place the amendment correctly and verify the cited deferral note; within the prompt's investigation scope.

## What the prompt got right / friction

- Excellent prompt: named the known_issues entry, the covering wiki page, the four questions, and the no-build boundary explicitly. Both turns left essentially nothing to guess. Only inference needed: the checklist-state marker for a registered-but-deferred item (not specified; resolved as `[>]` with disclosure).

## Addendum — turns 3–5 (same session): Wave 3a manual-item dependency check + ADP source verification

- **Turn 3 (Nick's question):** are the two Wave 3a MANUAL_SETUP items season-gated like the ESPN block? Checked the actual dependency chain — 03a build file (Scope + checklist), MANUAL_SETUP_CHECKLIST (including the 2026-07-22 decision of record already declaring them plain-chat-resolvable), BUILD_INDEX dependency notes, and `league-mechanics/average-draft-position` (3rd wiki page, hitting the session budget; `multi-platform-adp-divergence` cited from its _index description only, deliberately not opened). Verdict reported: NO on both — ADP is a population/market statistic requiring no league access; Sleeper-only satisfies 03a (ESPN leagues join automatically via the no-hardcoded-count league selector). Disclosed caveat: 03a's ESPN-crosswalk board item can be code-built but not live-verified until the ESPN unlock — a build-session concern, not a manual-item gate.
- **Turn 4 (Nick's confirmations):** (a) run the ADP source check now; (b) which-leagues RESOLVED — all connected leagues, 2025 "10 enter 1 Leaves" acceptable as initial build-and-verify board target.
- **Turn 5 (verification, live-tested via WebFetch/WebSearch):**
  - Sleeper documented API (docs.sleeper.com): NO ADP endpoint (17 documented endpoints inventoried). Community undocumented-endpoint canon (joeyagreco/sleeper discussion #11): no ADP endpoint listed either — but it pointed at undocumented stats/projections surfaces on `api.sleeper.com`.
  - **Live-verified: `api.sleeper.com/projections/nfl/2026?season_type=regular&position[]=RB&order_by=adp_half_ppr` returns projection objects carrying per-format ADP fields** — `adp_half_ppr`, `adp_ppr`, `adp_std`, `adp_2qb`, `adp_dynasty`, `adp_dynasty_half_ppr`, `adp_dynasty_ppr`, `adp_dynasty_std`, `adp_idp`, `adp_idp_1qb`, `adp_rookie` — keyed by native Sleeper `player_id` (sample: Bijan Robinson `"9509"`, adp_ppr 1.4). No auth, no key. Caveats recorded: undocumented (could change/vanish — ESPN-grade defensive isolation required, which 03a's items already spec), no dispersion/sample-size metadata, population parameters (league sizes sampled, window) unpublished, different host than the documented API (`api.sleeper.com`, not `api.sleeper.app/v1` — the existing client is pinned to the latter; ingestion must handle this).
  - **Live-verified: FantasyFootballCalculator** `fantasyfootballcalculator.com/api/v1/adp/ppr?teams=12&year=2026` — free, no key, attribution requested, once-daily update; meta carries sample size (2,700 drafts) + date window (Jul 15–22); per-player `adp`, `high`, `low`, `stdev`, `times_drafted`, `bye`; league-size param. BUT proprietary `player_id` + name only — name-based mapping to `sleeper_player_id`, the wiki crosswalk page's last-resort join. PPR verified live; standard documented; half-ppr/2QB reportedly same URL pattern, unverified.
  - **FantasyPros:** API key by request (secure.fantasypros.com/api-keys/request); free prototype key bundled with MVP/HOF membership (~$5.99/mo annual); members get personal non-commercial production keys; ADP/consensus rankings across redraft/dynasty by scoring format; attribution required; FP player IDs → extra mapping layer (FP not in the Wave 1 crosswalk columns).
  - **Rejected with wiki grounding:** Underdog (best-ball ADP — `average-draft-position` rules best-ball↔redraft non-transferable) and ESPN ADP (fragile platform + the wiki's autodraft/default-ranking contamination pattern, most severe on casual platforms).
  - **Recommendation presented:** the Sleeper undocumented endpoint — the only source population-matched to the platform Nick's league actually drafts on AND natively keyed by `sleeper_player_id` (zero identity mapping), zero cost. Nick's pick still pending — MANUAL_SETUP item annotated with full findings, kept `[ ]`.
- **Files this addendum covers:** MANUAL_SETUP_CHECKLIST.md (item 2 → `[x]` with decision of record; item 1 annotated with verification findings, still `[ ]` awaiting pick), STATE.yml (overwrite: nick_pending top entry now the one-word ADP pick; new known_issues entry recording the wiki silence on the projections endpoint), this log. 03a build file deliberately untouched (its item text remains accurate until the pick lands). PROGRESS.md still not updated (manual-item resolution ≠ milestone).

## Addendum — turns 6–8: Nick's ADP pick lands; Wave 3a fully unblocked

- Turn 6–7: Nick asked for a push (already up to date — both session commits were pushed at commit time; confirmed clean).
- Turn 8: **Nick picked SLEEPER** — the undocumented projections endpoint, as verified. Closures: MANUAL_SETUP ADP item → `[x]` decision of record (source, live-verified shape, the four build-session constraints: defensive isolation, api.sleeper.com host split, no dispersion metadata, wiki silence; rejected alternatives recorded); 03a's ADP-ingestion item annotated (verify-before-building condition satisfied 2026-07-22 — correctness annotation per the Amendment Norm, item itself stays `[ ]` since the ingestion service is unbuilt); STATE.yml overwritten (ADP gate removed from nick_pending — Wave 3a has NO remaining gate; blockers + known_issues wiki-silence entry updated to picked-source wording; next build session self-locates onto 03a's first unchecked item, the adp_rankings migration). PROGRESS.md still not updated — closing manual items isn't one of the four milestone categories; the milestone will be 03a's first working feature.

## WIKI NOTE

WIKI NOTE: The `sleeper-api` category (18/18 subjects complete) does not cover the undocumented projections endpoint `api.sleeper.com/projections/nfl/{year}` that carries Sleeper's per-format ADP fields — it postdates the notebook's subject list. If Nick picks it as the ADP source, the Wave 3a ADP-ingestion build session will be writing against a wiki-silent surface (response-shape knowledge currently lives only in this session's live verification, recorded here and on the MANUAL_SETUP item). A wiki-maintenance ingestion of that endpoint — shape, ADP field inventory, fragility posture, unpublished population parameters, and the `api.sleeper.com` vs `api.sleeper.app/v1` host split — should ideally land before or alongside that build session. (Earlier-turn status unchanged: `sleeper-api/matchup-endpoint` and `sleeper-api/nfl-state-endpoint` were accurate and sufficient for the finality-promotion investigation; the two signed operationalizations remain candidates for a matchup-endpoint ADR addendum.)
