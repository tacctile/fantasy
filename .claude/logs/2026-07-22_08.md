# Session Log — 2026-07-22_08
**Session type:** feature-build (autonomous, via BUILD_PROTOCOL.md)
**Scope:** 01_foundation.md → "Schema — integrity" — full-section fold (3 items): FK sweep, index sweep, admin auth + RLS owner-write policies. First session run under the Folding Policy adopted earlier today.

---

## Session start

- Read in protocol order: MASTER_CONTEXT.md (auto-loaded), BUILD_PROTOCOL.md (Nick's drop-in), STATE.yml, BUILD_INDEX.md, MANUAL_SETUP_CHECKLIST.md, wiki/index.md, wiki/ROUTING.md, build/01_foundation.md, plus ARCHITECTURE.md's Shared-Database Constraints (referenced by STATE.yml's warning).
- STATE.yml pointer confirmed: first unchecked item = first integrity item; STATE.yml itself pre-mapped the whole section as a legitimate fold (1 decision-dense + 2 mechanical, one sub-section, identical wiki footprint).
- **Fold declared in chat before Clarify:** all 3 integrity items, nothing beyond. No hard stops applied: no new wiki category, no new external service, and the one MANUAL_SETUP dependency (admin auth) was `[~]` — per protocol that does not stop a fold; the value is requested in that item's Clarify batch.
- Log cap triggered: this file is the 6th individual log — `2026-07-22_03.md` condensed into `ARCHIVE.md` (newest-first) and deleted.

## Wiki pages read (2 of 3 budget)

- `wiki/topics/schema-reference/league-identity-and-scoping.md` — league FK target (`platform_league_uuid`), native IDs never join keys.
- `wiki/topics/schema-reference/player-identity-mapping.md` — player FK anchor (`sleeper_player_id`), crosswalk shape, `espn_player_id` partial-unique rationale.

## Pre-implementation coverage maps (recorded per item, before that item's code)

**Item 1 — FK sweep (verify-and-check-off):**
- League-scoped FK target → league-identity-and-scoping ADR.
- Player-scoped FK anchor → player-identity-mapping ADR.
- Table inventory / expected FK set → build-file annotations + ARCHITECTURE.md live inventory.
- No invented decisions — pure verification. No gap.

**Item 2 — index sweep:**
- Target column list → the build item's own enumeration (spec).
- `share_token` unique semantics → Schema Rule 6 + leagues item annotation; `espn_player_id` partial unique → player-identity-mapping ADR.
- Index composition (single-column shape, prefix-redundancy skips) → **WIKI SILENT** — searched ROUTING.md/index.md at decision time; no indexing/performance category exists. Declared in chat before writing the migration; resolved on Postgres general knowledge (leading-column prefix rule).

**Item 3 — admin auth + RLS policies:**
- Pin to Nick's specific `auth.uid()`, never blanket `authenticated` → MANUAL_SETUP decision of record + ARCHITECTURE.md Shared-Database Constraints.
- Table set (10, RLS already enabled at creation, zero policies) → build file + live pre-flight.
- Function conventions (`search_path = ''`, stable) → `generate_share_token()`/`set_updated_at()` precedent.
- Policy shape (FOR ALL vs per-verb, helper vs literal, InitPlan wrapping) → **WIKI SILENT** — searched ROUTING.md/index.md; no auth/RLS category. Declared at decision time; resolved via Clarify (Nick's decisions) + Supabase-standard patterns.

## Clarify (per item, per Folding Policy — never one mega-batch)

- **Item 1:** zero open questions — stated plainly, proceeded (protocol's no-filler rule).
- **Item 2:** zero open questions — the item enumerates the columns; composition handled as disclosed judgment calls.
- **Item 3 (batch 1, 3 questions via AskUserQuestion; all recommended options chosen):**
  1. Credential handoff → Nick creates the user in dashboard, pastes UUID only (password never touches chat/repo).
  2. Pinning mechanism → `public.is_fantasy_admin()` helper; single rotation point.
  3. Policy verbs → full owner CRUD (a literal "writes-only" reading would leave even the owner unable to read — breaking the wave's health-check completion gate; spectator reads stay Wave 4).
- **Item 3 (batch 2 — live discovery):** before asking Nick to create the user, checked `auth.users` — **nick@prolabelco.com already existed** (UUID `1d5dab52-9b93-4544-a99b-6d4dc3c84a66`, created 2026-05-15; it is one of the 2 pre-existing prolabel users, alongside knox.velander@gmail.com). Duplicate emails are impossible in the shared namespace, so the recorded 2026-07-21 decision ("create the one auth user as nick@prolabelco.com") was unbuildable as written. Asked Nick: reuse existing vs. new user under a different email. **Nick ruled: reuse.** Consequence disclosed: any session authenticated as that user (from his other prolabel apps) carries fantasy owner rights — consistent with the shared-project decision. Net effect: no user creation, no password handoff needed at all — policies only need the UUID.

## Decisions and reasoning

**Item 1 (no migration):** queried `pg_constraint` across all 10 tables — 18 FKs present exactly as designed: 7 league-scoped tables + leagues self-ref → `platform_league_uuid`; crosswalk/roster_players/player_scores/draft_state → `players.sleeper_player_id`; 5 composite `(league_id, native_roster_id)` → rosters; `leagues.owner_id` → auth.users. Delete rules: cascade throughout; set-null on `previous_platform_league_uuid` and `owner_id`. Checked off with no DDL — the annotations claiming FKs shipped at creation were verified true, not trusted on paper.

**Item 2 (`20260722063616_add_integrity_indexes.sql`):** 17 single-column indexes. Skipped (verified already covered): `league_id` (leading PK column everywhere it exists + leagues PK), `share_token` (unique), `espn_player_id` (partial unique), `sleeper_player_id` on players/crosswalk (PKs), `platform` on leagues (leads the platform/native/season unique). Added: `previous_platform_league_uuid` (the one unindexed FK → `platform_league_uuid` — the item's "every FK referencing it" clause caught exactly this), `sleeper_player_id` × roster_players/player_scores/draft_state, `platform` × 6 N:1 tables, `season_year` × those 6 + leagues. Judgment calls (disclosed in chat): no duplicate index over a covering prefix; `leagues.owner_id` left unindexed (outside the item's list, tiny table); single-column shape per the item's literal list.

**Item 3 (`20260722064037_add_owner_rls_policies.sql`):** `public.is_fantasy_admin()` (stable, `search_path = ''`, compares `auth.uid()` to the admin UUID; commented as the single rotation point) + one `fantasy_owner_all` policy per table × 10: `FOR ALL TO authenticated`, `using`/`with check` = `(select public.is_fantasy_admin())` — the subselect makes it an InitPlan, evaluated once per statement, avoiding the per-row auth-function lint. `TO authenticated` scopes which roles the policy applies to; the *check* is the specific UUID — satisfying the never-blanket-authenticated rule. Pre-flight collision check (shared DB): no existing policies on the 10 tables, no `is_fantasy_admin` name collision. `leagues.owner_id` backfill: zero league rows exist — nothing to backfill; Wave 2 ingestion stamps it at insert (disclosed).

- Both pushes via the allowed `npx supabase db push` rule, password sourced from `.env.local` in-shell, never echoed. Same cosmetic Docker/pg-delta cache warning as every prior session; remote applies clean.
- MANUAL_SETUP_CHECKLIST: admin-auth item `[~]` → `[x]` with the resolution; new decision of record added (admin identity = reused pre-existing prolabel user).

## Verification (live DB — all passed)

- Item 1: `pg_constraint` full sweep — 18/18 expected FKs, delete rules exact.
- Item 2: `pg_indexes` — all 17 `idx_*` present with expected definitions; migration history row `20260722063616` present.
- Item 3 structural: 10/10 `fantasy_owner_all` policies (cmd=ALL, roles={authenticated}); RLS enabled 10/10; function source pins the admin UUID; history row `20260722064037` present.
- Item 3 functional (all writes rolled back): admin-uid identity — INSERT into players passed RLS and the row was visible to its own SELECT (1 row); other prolabel user's uid — INSERT rejected 42501; `anon` — INSERT rejected 42501.
- Security advisors run post-change: zero lints on any fantasy-owned object (all findings pre-existing, foreign prolabel objects — no-touch by standing rule).
- `npm run build` passes (no app code changed).

## Assumptions / friction

- The one real surprise was pre-existing-user discovery in item 3 — resolved live via an extra Clarify question rather than stopping, since it arrived during the fold's last item (nothing was folded past it) and Nick was present to rule. The MANUAL_SETUP `[~]` entry's premise ("will ask for email + password") was half-wrong: the email pre-existed and no password was ever needed.
- Folding Policy worked as designed on its first outing: two mechanical items produced zero filler questions; the decision-dense item got its own two-round Clarify; per-item coverage maps and verification passes stayed uncompressed.

## WIKI NOTE

WIKI NOTE: `schema-reference` has no page covering the platform's auth/RLS security model — now decided and live: admin identity (reused pre-existing prolabel user, UUID pinning, the shared-namespace duplicate-email constraint), the `is_fantasy_admin()` single-rotation-point pattern, full-owner-CRUD policy shape, and the InitPlan-wrapped policy idiom. Wave 4's spectator-RLS session (a named singleton) will need exactly this context — a decision-record page would let it read the security model instead of reconstructing it from build-file archaeology. Related: the standing league-scoped-state DDL note (fourth consecutive session's adjacent gap; index composition was this session's variant of it).
