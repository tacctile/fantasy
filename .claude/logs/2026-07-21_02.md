# Session Log — 2026-07-21_02
**Session type:** feature-build (autonomous, via BUILD_PROTOCOL.md)
**Scope:** 01_foundation.md — "Supabase project + migration workflow" section (4 items, folded with Nick's explicit approval)

---

## Files read and why

- `.claude/BUILD_PROTOCOL.md` — Nick dropped it in ("read this and lets get to work"); governs the session
- `.claude/MASTER_CONTEXT.md` — auto-loaded via CLAUDE.md
- `.claude/STATE.yml` — located last session's stopping point (scaffold section done; next item = Supabase credentials capture, flagged as [~] ready-awaiting-handoff)
- `.claude/BUILD_INDEX.md` — confirmed roadmap/registry unchanged; Wave 1 🟡, 01_foundation.md is the active file
- `.claude/MANUAL_SETUP_CHECKLIST.md` — per protocol step 3a; confirmed both Supabase items were [~] (proceed and ask, don't halt)
- `wiki/index.md`, `wiki/ROUTING.md` — per Session-Start Protocol; schema-reference is the wave's category
- `.claude/build/01_foundation.md` — located first unchecked item
- `.claude/COMPLETION_TEMPLATES.md` — feature-build template + PROMPT SCORE rubric
- `.claude/ARCHITECTURE.md`, `.claude/PROGRESS.md` — session-end updates
- `.gitignore` (root), `supabase/.gitignore` — verified .env.local and supabase/.temp are ignored before writing secrets/committing

Wiki pages: none read. The build file's three WIKI PAGES TO CONSULT entries are all schema-shape pages (league identity, player identity mapping, league config model) — relevant to the *next* section (migrations), not to credentials/CLI/link/init. Reading them this session would have burned the 3-page budget on nothing this session's items use. The next session (first migration) should read them.

## Decisions and reasoning

1. **Fold the 4-item section into one session** — asked Nick per BUILD_PROTOCOL's edge case (never decide unilaterally); he approved. Rationale offered: credentials-capture alone produces almost no artifact and the CLI/login/link/init items are tightly coupled to it.
2. **Pull credentials via the Supabase MCP connector instead of asking Nick to paste everything** — connector was available in-session; fetched project URL + publishable/anon keys myself. Only the secret key (not exposed via connector) was asked for. Reduced the handoff to one paste.
3. **Modern publishable/secret key pair over legacy anon/service_role JWTs** — asked Nick; he chose publishable (recommended). Env var names follow the modern convention: `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, `SUPABASE_SECRET_KEY`. The build file's literal "anon key" wording predates this distinction — noted in the checked-off item text.
4. **CLI as npm dev dependency (supabase@2.109.1)** — asked Nick; he chose npm over scoop. Version pinned in package.json, consistent across his three environments, invoked via `npx supabase`.
5. **DB password deliberately skipped at link** — offered Nick paste-now vs. skip; he chose skip. Link works without it; first `supabase db push` will need it. Appended a new [ ] item to MANUAL_SETUP_CHECKLIST.md Wave 1 per the "Appending new items mid-build" rule, and carried it in nick_pending.
6. **Key validation before declaring done** — hit `rest/v1/` with both keys. First attempt via Invoke-WebRequest returned "Forbidden use of secret API key in browser" — Supabase rejects secret keys from browser-like user agents (Invoke-WebRequest's UA). Retried with curl.exe (non-browser UA): HTTP 200. Publishable key was recognized (root endpoint itself demands a secret key, which is expected). Both keys are live and correct.
7. **PROGRESS.md updated** — "activation of a core infrastructure service (Supabase…)" is an explicit milestone trigger in both MASTER_CONTEXT.md and PROGRESS.md's own header. This session activated Supabase (credentialed + linked + CLI workflow). Qualifies.

## Assumptions where the protocol was ambiguous

- "Capture" (item 1) needs a destination; used gitignored `.env.local`. The later "Env, secrets, deploy" section's `.env.local`/`.env.example` item remains unchecked — this session created the live-values file as the capture vessel, but the committed `.env.example` contract (including ESPN placeholders) is still that later item's scope.
- The baseline-migration-rule confirmation item (item 4) is a statement of understanding, not an artifact. Marked [x] with the concrete implication recorded: no migrations exist yet, so the *first* schema item creates the baseline via `supabase migration new`, and nothing ever edits it afterward.

## Friction / what would have improved the session

- BUILD_PROTOCOL worked as designed — the [~] three-state handoff flow in MANUAL_SETUP_CHECKLIST.md got its first real exercise and did exactly what it was built for (no halted session, one pointed ask, values injected live).
- The build file's "anon key" wording and `SUPABASE_URL` naming predate Supabase's modern key/naming conventions; resolved via Clarify. Future build files touching Supabase should say "publishable + secret key" and `NEXT_PUBLIC_SUPABASE_URL`.
- npm audit went 3 → 6 vulns with the supabase dev-dep install (all dev-time). Recorded in known_issues; no action taken — same posture as the existing next@16.2.11 transitive vulns.

## WIKI NOTE

WIKI NOTE: none

## Session-end checklist

- STATE.yml overwritten completely ✔
- This log written (NN=02; highest existing for 2026-07-21 was 01) ✔
- PROGRESS.md updated (core infra activation milestone) ✔
- ARCHITECTURE.md updated (Supabase Infrastructure + Environment Variables sections populated, Status updated) ✔
- DESIGN_SYSTEM.md not touched (no token changes) ✔
- 01_foundation.md: 4 items [ ] → [x] with decision annotations ✔
- MANUAL_SETUP_CHECKLIST.md: 2 items [~] → [x]; 1 new [ ] item appended (db password) ✔
- Commit + push to main ✔ (build-side pushes every session)

Note: this log file is currently NOT committed — the repo-root .gitignore's bare 'logs' pattern ignores .claude/logs/ (standing warning in STATE.yml, governance decision pending).
