# Session Log — 2026-07-22_10
**Session type:** non-code (governance)
**Scope:** Install Absolute Rule 13 — "Non-fantasy data is untouchable — no exceptions" — per Nick's direct, hand-written instruction (permanent, non-negotiable governance addition protecting the shared prolabel database's ~49 non-fantasy tables).

---

## Files read and why

- `.claude/MASTER_CONTEXT.md` — auto-loaded via CLAUDE.md; the edit target for the canonical rule text
- `.claude/COMPLETION_TEMPLATES.md` — edit target for the mandatory BLAST RADIUS report line (Nick named it explicitly)
- `.claude/BUILD_PROTOCOL.md` — Dual-Location check: restates Session-End Steps and the build-step rule list
- `.claude/BUILD_INDEX.md` — Dual-Location check: restates Session-Start Protocol and atomicity only — no shared-DB restatement, no edit needed
- `.claude/ARCHITECTURE.md` — the new rule anchors "fantasy-owned" to this file's "Database Schema (live)" inventory (verified it exists — ten tables, three enums, three functions, RLS layer, indexes); also holds the pre-existing Shared-Database Constraints restatement that had to be reconciled
- `.claude/STATE.yml` — session-start protocol + its `warnings` array restates the shared-DB constraints (a restatement site)
- Repo-wide grep for `db reset` / `migration repair` / `prolabel` / `blast radius` / `Shared-Database` — grounded the Dual-Location check in actual occurrences rather than the known-pattern list alone. Found restatement sites: ARCHITECTURE.md, STATE.yml warnings, BUILD_PROTOCOL.md (singleton exception #5 mention only), migration stub-file comments (self-documenting, correct as-is, untouched), MANUAL_SETUP_CHECKLIST.md (decision-of-record that the project is shared — records the *fact*, not the prohibitions; untouched), PROGRESS.md history entries (historical record; untouched)
- `.claude/logs/2026-07-22_05.md` + `ARCHIVE.md` — log-cap rotation (this log is the 6th)
- `.claude/PROGRESS.md` + `PROGRESS_ARCHIVE.md` — milestone entry (governance change affecting all future sessions) + 5-entry cap roll

## What was installed

**Canonical text — `MASTER_CONTEXT.md`:**
- New section "Shared Database Protection — Non-Fantasy Data Is Untouchable (Absolute Rule 13; canonical full text)" after the Supabase Migration Rule — Nick's six prohibitions substantially verbatim: (1) never `supabase db reset`; (2) never `migration repair` touching foreign history — stub-file technique only; (3) no raw SQL (DROP/TRUNCATE/ALTER/DELETE/UPDATE-without-WHERE/CASCADE) that could affect a non-fantasy table, with explicit table-name verification against ARCHITECTURE.md's live schema inventory before every migration; (4) collision check before any table-name reuse, mandatory forever; (5) ANY ambiguity → STOP and ask Nick; (6) mandatory blast-radius confirmation in every live-DB session's completion report. Closing clause: supersedes efficiency, folding-policy, and automation considerations.
- New **Absolute Rule 13** in the Absolute Rules list (compressed but standing-alone, pointing to the canonical section).
- **Rule 11 (Dual-Location)** known-patterns list extended with the new pattern: MASTER_CONTEXT.md canonical ↔ ARCHITECTURE.md Shared-Database Constraints ↔ COMPLETION_TEMPLATES.md BLAST RADIUS line ↔ STATE.yml standing warning.
- **Session-End Steps** report bullet + **Prompt Format SESSION END block** report line both extended with the mandatory BLAST RADIUS line (the known dual-location pair — both halves updated together).

**Report machinery — `COMPLETION_TEMPLATES.md`:**
- New "BLAST RADIUS Line (mandatory on every report, added 2026-07-22)" section defining the three legal values: confirmed-fantasy-only / no-live-DB-operations / other-objects-touched-with-cited-sign-off (the third is only legal with citable prior sign-off; without it, it's an incident).
- `BLAST RADIUS:` line added to all three templates (feature-build, audit-fix, non-code), directly after WIKI COVERAGE CHECK.
- Dual-Location Instruction Rule section's pattern list extended to match Rule 11.

**Operational restatement — `ARCHITECTURE.md`:**
- "Database Schema (live)" intro now declares the inventory as the definitive fantasy-owned list for Rule 13's blast-radius verification.
- "Shared-Database Constraints (prolabel)" upgraded from 5 bullets to the full operational restatement: cites Rule 13 as canonical, adds the raw-SQL/blast-radius-verification bullet, the collision-check-forever bullet, the ambiguity-stop bullet, and the BLAST RADIUS report-line bullet; keeps the 23-stub specifics and the `is_fantasy_admin()` RLS constraint.

**Protocol wiring — `BUILD_PROTOCOL.md`:**
- Build step 2 now names Shared Database Protection in the rule list and spells out the pre-push blast-radius verification (list table names → verify against the inventory → stop on ambiguity; supersedes the Folding Policy).
- Session-End step 7 now names both mandatory report lines (WIKI COVERAGE CHECK + BLAST RADIUS).

**`STATE.yml`:** warnings[0] rewritten to cite Absolute Rule 13 as canonical and carry the full prohibition set.

## Decisions and reasoning

1. **Canonical location = MASTER_CONTEXT.md dedicated section + one-line-style Absolute Rule pointer** — matches the file's established pattern (Wiki Protocol section ↔ Rule 2/12; Access Model section ↔ pointers). Nick's text installed substantially verbatim; only formatting and cross-references added.
2. **BLAST RADIUS line made mandatory on every report, not just live-DB sessions** — Nick's prohibition 6 requires it for sessions touching the live DB; making the field unconditional (with a `no live database operations this session` value) is strictly stronger: a missing line is now always a visible protocol violation, never an ambiguous "did this session touch the DB?" question. Mirrors how WIKI COVERAGE CHECK works.
3. **Untouched restatement-adjacent sites, deliberately:** migration stub-file comments (already correct, editing migrations is itself forbidden), MANUAL_SETUP_CHECKLIST.md's decision-of-record (records that the DB is shared, not the prohibitions), PROGRESS.md/ARCHIVE historical entries (history is immutable), BUILD_INDEX.md (no shared-DB restatement exists there).
4. **Read-only queries against foreign tables explicitly carved out** in the BLAST RADIUS confirmed-value definition — the mandatory collision check *requires* reading the shared schema; without the carve-out, prohibition 4 and the confirmation line would contradict each other.
5. **Log cap exercised** — creating this file made 6; `2026-07-22_05.md` condensed into `ARCHIVE.md` (top, newest-first) and deleted.
6. **PROGRESS.md milestone** — a governance change affecting all future sessions is an explicit milestone trigger; 6th entry rolled "2026-07-21 — Supabase Infrastructure Activated (Wave 1, Session 2)" verbatim into `PROGRESS_ARCHIVE.md` (appended, chronological oldest-on-top).

## Friction / what would have improved the session

- None. Nick's instruction was near-10 prompt quality: exact rule text, named mirror sites, named the report-line requirement, cited the existing stub-file precedent. The only judgment calls were placement style and the always-mandatory report-line strengthening (disclosed above and in the report).

## WIKI NOTE

WIKI NOTE: none

## Session-end checklist

- STATE.yml overwritten completely; arrays reviewed — all 3 nick_pending entries still genuinely open, known_issues npm-audit entry still true (not re-checked; no dependency changes), warnings[0] rewritten for Rule 13 ✔
- This log written (NN=10; highest existing for 2026-07-22 was 09) ✔
- Log cap: _05 condensed into ARCHIVE.md and deleted ✔
- PROGRESS.md updated (governance milestone); oldest entry rolled into PROGRESS_ARCHIVE.md verbatim ✔
- ARCHITECTURE.md updated (Shared-Database Constraints + schema-inventory framing — governance, not structural) ✔
- DESIGN_SYSTEM.md not touched (no token changes) ✔
- Build files not touched (no build items worked) ✔
- Commit to main ✔
