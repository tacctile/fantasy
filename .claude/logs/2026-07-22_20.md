# Session Log — 2026-07-22_20

**Session type:** feature-build (autonomous, BUILD_PROTOCOL.md)
**Fold declared (at session start, before Clarify):** 03a_draft_assistant_static_board.md → the full
"ADP schema + ingestion" sub-section (all 9 items) as one independently verifiable artifact — the ADP
ingestion pipeline (migration → types → service with unmapped-tracking/upsert/isolation → sync_runs
logging → cron route → dev runner). The migration alone verifies nothing; the service can't run without
the table; verification needs the runner. Matches the Folding Policy's sub-section-as-artifact example
and Wave 2's cadence.
**Disclosed fold judgment call:** `api.sleeper.com` is a new HOST; the policy's "first touch of a new
external service" hard stop was judged not to apply — it is a new endpoint of the already-integrated
Sleeper service, live-verified twice on 2026-07-22 and wiki-covered, no auth. Declared in chat with a
veto offer; Nick did not take it. Live-verification-failure remained a hard stop (none occurred).

**Wave self-location:** Wave 2 has no `[ ]` items (9 ESPN `[!]` external-timing, cron cadence `[~]`,
finality promotion `[>]`) → first unchecked item is 03a's adp_rankings migration, exactly as STATE.yml
predicted. Manual Setup Flag: both Wave 3a items `[x]` — no stop.

**Wiki pages read (5 — cap overridden under Rule 12, disclosed):**
1. `sleeper-api/projections-endpoint` — STATE.yml-mandated covering page for the ADP surface
2. `league-mechanics/average-draft-position` (build file list)
3. `league-mechanics/multi-platform-adp-divergence` (build file list)
4. `schema-reference/league-configuration-data-model` (build file list)
5. `sleeper-api/nfl-state-endpoint` (partial, targeted) — coverage-check read for the season-year
   derivation decision (`league_season` vs `season`), made BEFORE writing the resolver. This is the
   Rule 12 override working as designed: a decision surfaced mid-build, the covering page was read
   before code.

---

## Item 1 — adp_rankings migration

**Clarify (Nick, this session):** (1) LONG per-format rows — composite PK
`(sleeper_player_id, adp_source, season_year, scoring_format)`, amending the build item's
pre-source-confirmation constraint `(sleeper_player_id, adp_source, season_year)` (correctness
amendment: the confirmed source carries 12+ per-format adp_* fields, open inventory); (2) positional
rank DERIVED AT INGESTION (source provides no positional ADP); (3) CURRENT SNAPSHOT ONLY (no history
rows; drift data knowingly not captured — Nick informed it is unrecoverable later).

**Coverage map (recorded before code):** long-row shape ← Clarify + projections-endpoint open-field-set
decision; sleeper_player_id text FK cascade ← Schema Rule #4 + players PK; scoring_format
unconstrained text ← pattern-ingestion decision; adp_overall check >0 and <999 ← 999.0-sentinel
decision (schema backstop); positional_rank nullable ← Clarify + source limitation; no
league_id/platform ← Schema Rules #1/#2 govern league-scoped tables, ADP is source/season/format-global
market data (sync_runs scoping precedent); season_year in key ← Rule #3; adp_source check ('sleeper')
extensible-by-migration ← sync_runs source-check precedent; RLS fantasy_owner_all + set_updated_at +
read-path index ← ARCHITECTURE RLS layer + sync_runs migration conventions; FK-index omission ←
Postgres prefix rule (PK leads with sleeper_player_id). **Declared silence:** the literal source
identifier string 'sleeper' — naming decision consistent with the platform enum value.

**Rule 13:** collision check via live `list_tables` — full 60-table public listing, `adp_rankings`
absent everywhere; 11 fantasy tables matched ARCHITECTURE.md's inventory exactly. Migration
`20260722175619_create_adp_rankings_table.sql` via `supabase migration new`; pushed; table live
(61st table, RLS enabled); `npm run gen:types` regenerated.

## Items 2–6 — types, client, ingestion service (the service cluster)

**Clarify (Nick):** (1) module home `services/adp/` (feature-named; keeps services/sleeper/ purely the
documented-API surface; honest if the source ever changes); (2) position set QB/RB/WR/TE/K/DEF (IDP
positions skipped — no IDP league; adp_idp fields riding on fetched players still ingest by pattern).
Items 4–6 had zero open questions (stated plainly per protocol — wiki decisions + Wave 2 precedent
fully specify them).

**Coverage map (before code):** separate client, no multi-host abstraction ← projections-endpoint Key
Decision #4; verified-parameter-combination-only requests (season_type + position[] + order_by; even
order_by kept because omitting it is an unverified variation) ← "only this parameter combination is
verified"; politeness mirrored not inherited (own 250ms gate, no assumed ceilings) ← host-split
section; validate-before-swap ← last-good-snapshot decision + players-catalog precedent; pattern
extraction, open set, absent-field tolerance ← permissive-parser decision; ≥999 sentinel + ≤0 excluded
← sentinel decision; unmapped recorded never minted ← Schema Rule #4 + league-rosters
skippedMissingPlayerIds precedent; positional rank ADP-asc within players.position, sleeper_player_id
tie-break ← Clarify + build file's deterministic-tie-breaker convention; season default
`league_season` ← nfl-state-endpoint Key Decision (read before the resolver was written); stale-row
cleanup after full success ← Nick's current-snapshot sign-off + rosters/standings stale-row precedent +
catalog complete-response-then-reconcile ordering.

**Files:** `src/services/adp/types.ts` (AdpProjectionRecord/AdpProjectionPlayer permissive wire types,
AdpEntry, AdpRow = Insert alias, AdpIngestionResult), `client.ts` (fetchSeasonProjections +
AdpSourceError; module-local pacing gate; 30s timeout; 3-attempt backoff+jitter; same throttle-signal
taxonomy as the documented client, mirrored not shared), `ingestion.ts` (syncAdpRankings: count
existing → fetch → structural validation (array, ≥200 floor, 50-record sample player_id+stats) →
pattern-extract (sentinel/implausible counted) → catalog position lookup (chunked) → unmapped split →
zero-insertable + <50%-of-existing abort gates → positional-rank derivation → chunked upsert on
composite PK → stale-delete `.lt('ingested_at', startedAt)` only after all chunks OK).

**Judgment calls disclosed:** stale-row deletion added as the completion of Nick-signed
current-snapshot semantics (not literal item text — build item only demanded upsert + preserve-on-
failure); projection stats (pts_ppr etc.) deliberately NOT ingested — no registered build item covers
projections (Absolute Rule 3), noted as a possible future registration; the sync-runs `counts` JSONB
carries only numbers, so unmapped IDs go to server logs/result object, not sync_runs.

## Item 7 — sync_runs extension

Constraint name verified live against pg_constraint (`sync_runs_source_check`) before writing
migration `20260722180354_extend_sync_runs_source_for_adp.sql` (drop + re-add check with
'adp_ingestion'; ALTER touches only fantasy-owned sync_runs). `SyncRunSource` union extended.
Recording rides `runTrackedAdpIngestion` (added to ingestion.ts): never-throws wrapper writing the
start/finish pair — source 'adp_ingestion', platform 'sleeper', league_id null (global-run catalog
precedent). Manual script path deliberately does not record (sync-players precedent).

## Item 8 — cron route + scheduling

**Clarify (Nick):** piggyback on sync-leagues — both Hobby cron slots taken (catalog 09:00, league-state
10:00 UTC). The daily sync-leagues run now triggers ADP ingestion after the league loop in an isolated
containment boundary (own sync_runs row; failure logged + `adpOk:false` in body; league-state status
code unaffected either way; ADP runs last so it can never delay league sync). Dedicated
`GET /api/cron/sync-adp` built regardless: requireCronSecret fail-closed, counts-only response, 500 on
failure, NO vercel.json entry — on-demand surface + mount point if a slot opens at season start.

## Item 9 — dev runner

`scripts/sync-adp.ts` + `npm run sync:adp [-- <season_year>]` (tsx --env-file pattern); calls the
service directly.

---

## Verification (live)

- Migrations: both pushed cleanly; adp_rankings live with RLS; sync_runs check extended.
- `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean; /api/cron/sync-adp registered.
- Live ingestion run 1: 3,292 records fetched (6 positions, season 2026 — resolved from league_season),
  362 records with real ADP, 2,946 entries across 10 formats (2qb, dynasty_2qb, dynasty_half_ppr,
  dynasty_ppr, dynasty_std, half_ppr, idp, idp_1qb, ppr, std), 36,526 sentinel-skipped, 0 implausible,
  0 unmapped players, 2,946 upserted, 0 stale.
- SQL verification: 2,946 rows / 10 formats / 0 sentinel leaks (no adp_overall ≥999 or ≤0); PPR top-8
  coherent — Bijan Robinson 1.4/RB1 (matches the wiki's twice-verified sample), Gibbs RB2, Chase WR1;
  positional ranks interleave correctly with overall ADP.
- Live run 2 (idempotency): identical counts, 2,946 upserted in place, 0 stale — safe to re-run proven.
- Notable live observations vs wiki: `adp_rookie` and bare `adp_dynasty` were ALL-sentinel in this pull
  (consistent with the wiki's vestigial-field note — they never appear in formatsSeen); `idp`/`idp_1qb`
  carry real values on fetched offensive-position players.
- Deployed verification of the new route: after this session's push (see completion report) —
  401 unauthenticated + authenticated 200 run against production.

## Files read this session (and why)

Protocol order: MASTER_CONTEXT (auto), BUILD_PROTOCOL, STATE.yml, BUILD_INDEX,
MANUAL_SETUP_CHECKLIST, wiki/index, wiki/ROUTING, 03a build file. Wiki: the 5 pages listed above.
Source: sleeper/client.ts + players-catalog.ts + nfl-state.ts + sync-runs.ts (patterns), types.ts
(SleeperNflState grep), cron sync-players + sync-leagues routes (route pattern + piggyback edit),
scripts/sync-players.ts (runner pattern), package.json, sync_runs migration (conventions),
ARCHITECTURE.md (Rule 13 inventory + update targets), PROGRESS.md, logs listing (NN computation),
ARCHIVE.md head/tail (rotation format).

## Session-end bookkeeping

- Build file: all 9 sub-section items `[x]` with item-specific annotations; file header stays ⬜→🟡
  territory (draft-board data layer, route/page shell, player list, states sub-sections remain).
- BUILD_INDEX registry row 03a flipped to 🟡.
- STATE.yml overwritten completely; ADP-related stale phrasing in warnings updated (ingestion now
  built); nick_pending reviewed entry-by-entry — all six carried entries remain genuinely live.
- ARCHITECTURE.md: Status, directory tree, scripts line, Database Schema (adp_rankings entry +
  sync_runs source), Service API Reference (services/adp), cron routes list.
- PROGRESS.md: milestone entry (first Wave 3a feature live); oldest entry (Wave 2 catalog) rolled
  verbatim into PROGRESS_ARCHIVE.md (5-entry cap).
- Log cap: this file is the 6th → 2026-07-22_15.md key decisions concatenated into logs/ARCHIVE.md,
  original deleted.
- Blast radius: DDL = create adp_rankings (collision-checked) + alter sync_runs' own check constraint —
  both fantasy-owned per ARCHITECTURE.md inventory. DML writes: adp_rankings, sync_runs. Reads:
  players, leagues (route), pg_constraint/pg catalog (read-only verification). Only fantasy-owned
  tables touched.

WIKI NOTE: see completion report — projections-endpoint's field-inventory/sentinel observations
confirmed at scale by this session's full pull (evidence for two of its open questions); the 03a build
file's "endpoint is wiki-silent" phrasing inside item 3's annotation was stale at session start (page
existed since the same-day wiki-maintenance session) — noted in the checklist annotation, no wiki edit
made.
