# .claude/STATE.yml
# Living "what happened last session" file. Overwritten completely at the end of every session — never patched.
# Last Updated: 2026-07-22

current_focus: "Governance session (non-code): the Folding Policy is now adopted — it replaces the one-item-per-session cadence for all future build sessions. Born from a two-turn capacity audit earlier this same day (turn 1: Wave 1 schema folding audit; turn 2: per-wave extension across all six build files with fold ceilings, ~206 remaining items mapped to ~45-52 folded sessions). Canonical full text lives in BUILD_PROTOCOL.md (new 'Folding Policy' section): fold unit = up to 3 decision-dense items, one mechanical sub-section, or one independently verifiable artifact's full item set, whichever the section's items actually compose; fold conditions (identical WIKI PAGES footprint, per-item artifacts never compressed — coverage-map rows / Clarify batch / verification / build-file annotation stay per-item, fold declared at session start and never extended); hard stops (new wiki category, MANUAL_SETUP '[ ]'/missing state, first touch of a new external service, live verification failure, Clarify answer contradicting a shipped decision); Wave 3b live-behavior ceiling of 2-3 items; five named singleton exceptions (W2 crosswalk resolution, W2 first authenticated ESPN call, W3b queue/auto-pick, W4 scoring engine core, W4 share-token RLS + spectator loader); tail-compression quality tripwire for Nick's report reviews. Dual-location sweep completed per Rule 11: MASTER_CONTEXT.md Absolute Rule 1 (retitled 'Atomic sessions'), BUILD_INDEX.md's Wave Roadmap atomicity line, six internal BUILD_PROTOCOL.md restatements (header cadence, pipeline diagram, session-start step 6, Clarify per-item wording, Build step 1, session-end step 8, 'two adjacent items' edge case), plus the new pattern added to Rule 11's known list and COMPLETION_TEMPLATES.md's dual-location list. Separately, 03b's auto-pick contradiction is RESOLVED by Nick's explicit ruling (amended in 03b_draft_assistant_live_draft.md, datestamped 2026-07-22): auto-pick is purely local — writes ONLY to this app's draft_state via the manual-pick write path (source='manual', same first-write-wins semantics, undoable via the manual-only undo), never calls any Sleeper/ESPN write endpoint; Sleeper's own platform auto-pick governs the real draft when its clock expires; queue/auto-pick consume pick events from draft_state via the existing client-side sync mechanism, no direct WebSocket of their own. Confirmed no MASTER_CONTEXT amendment needed — the ruling reinforces the existing categorical read-only rule; the build file was the outlier. PROGRESS.md milestone entry added (cap roll: Wave 6 registration entry moved to PROGRESS_ARCHIVE.md); log cap triggered (2026-07-22_02.md condensed into ARCHIVE.md, deleted). Next unchecked build item: first item of the 'Schema — integrity' section of 01_foundation.md — under the new Folding Policy the whole integrity section (FK sweep, which is largely verify-and-check-off since every table shipped its FKs at creation; index sweep; auth/RLS owner-write policies) is a legitimate single-session fold: 1 decision-dense item + 2 mechanical ones, with the admin email/password asked via Clarify per MANUAL_SETUP_CHECKLIST's '[~]' state, and owner-write policies pinned to Nick's specific auth.uid() per the shared-prolabel warning below."

build_system:
  index: ".claude/BUILD_INDEX.md"
  files_location: ".claude/build/"
  total_files: 7
  current_build_files:
    - ".claude/build/01_foundation.md"

nick_pending:
  - "Before Wave 3a: verify whether Sleeper actually has a public ADP endpoint, or decide a real alternative source — see MANUAL_SETUP_CHECKLIST.md Wave 3a section."
  - "Before Wave 5's waiver/FAAB recommendations item is built: run a dedicated scoping session to define the actual waiver/FAAB candidate scoring formula (opportunity-based — snap/target/touch share trends — is the recommended direction per wiki/topics/league-mechanics/waiver-wire-faab-strategy.md, but no formula exists yet). See 05_eye_candy.md 'Waiver/FAAB recommendations' section, DEFERRED item."

blockers: []

known_issues:
  - "npm audit reports 6 vulns (4 moderate, 2 high) as of last check — pre-existing 1 moderate postcss + 2 high sharp are transitive inside next@16.2.11's pinned deps (only offered 'fix' is downgrading next to 9.x, which is wrong). All dev-time/build-time, none shipped to clients. Re-check npm audit in later sessions."

warnings:
  - "The Supabase database is the shared multi-app 'prolabel' project. Never run supabase db reset or supabase migration repair against it; new foreign history versions get empty local stub files, never repair. Owner-write RLS policies must match Nick's specific auth.uid(), never the blanket authenticated role. Full rules: ARCHITECTURE.md 'Shared-Database Constraints'."

health: "clean"
