# .claude/STATE.yml
# Living "what happened last session" file. Overwritten completely at the end of every session — never patched.
# Last Updated: 2026-07-21

current_focus: "Wave 1 (Foundation) remains the last completed build work (schema migrations not yet started) — this session did not touch Wave 1 or write any code. This session (non-code, governance/build-file reconciliation) synthesized 12 rounds of external research (6 on draft-day-assistant mechanics, 6 on in-season-analytics UX) into two build files. 03b_draft_assistant_live_draft.md's BPA recommendation engine section was rewritten from an input-only listing into a concrete formula spec: dynamic VORP/VBD replacement-level calculation (ADP-calibrated, recomputed after every pick), roster need kept as an independent non-blended signal, plus three newly-scoped subsystems with zero prior documentation — tier-cliff detection (projection-gap method, live 'N remaining in tier' counter), positional run detection (sliding-window trigger, informational-only, never auto-adjusts rankings), and draft queue/roster-aware auto-pick (Sleeper-only, no multi-platform fallback). 05_eye_candy.md gained presentation-layer guidance for its already-settled luck and playoff-clinch calculations (diverging bar chart + actual-vs-expected framing for luck; status-badge + magic-number-sentence + optional what-if toggle for playoffs, never a probability-shaped visual since the mechanism is deterministic) and for positional breakdowns (bar for single-team-vs-league, heatmap for multi-team, radar explicitly rejected), plus a project-wide chart anti-pattern list (no radar/gauge/pie-for-comparison/scenario-trees/opaque composite scores/>4-series spaghetti lines/decorative 3D). Trade evaluation and waiver/FAAB recommendations — previously unclaimed by any build file — were formally amended into Wave 5's scope (decision documented in 05_eye_candy.md's Scope section): trade evaluation is fully scoped for build (two-sided value ledger + positional-fit delta panel, reusing Wave 5's own positional bar component, per the settled wiki trade-value-calculation.md math); waiver/FAAB display is scoped (tiered grouping, opportunity-share sparklines, paired drop suggestions) but its scoring formula is explicitly flagged DEFERRED — not invented this session, needs its own scoping session. A new 'Admin navigation shell' section (persistent sidebar, command-center home, sticky league/team/week context) was added to Wave 4 (04_league_dashboard.md) as the foundational IA layer Wave 5's six sections and Wave 6 mount into, per the cross-cutting navigation guidance from the research synthesis. BUILD_INDEX.md's Wave 4 and Wave 5 roadmap rows updated to reflect both amendments. Next unchecked build item remains: 'Migration: platform Postgres enum type, values sleeper and espn' — first item of the 'Schema — platform + identity foundation' section of 01_foundation.md."

build_system:
  index: ".claude/BUILD_INDEX.md"
  files_location: ".claude/build/"
  total_files: 7
  current_build_files:
    - ".claude/build/01_foundation.md"

nick_pending:
  - "Before Wave 3a: verify whether Sleeper actually has a public ADP endpoint, or decide a real alternative source — see MANUAL_SETUP_CHECKLIST.md Wave 3a section."
  - "Before Wave 5's waiver/FAAB recommendations item is built: run a dedicated scoping session to define the actual waiver/FAAB candidate scoring formula (opportunity-based — snap/target/touch share trends — is the recommended direction per wiki/topics/league-mechanics/waiver-wire-faab-strategy.md, but no formula exists yet). See 05_eye_candy.md 'Waiver/FAAB recommendations' section, DEFERRED item."

blockers: []

known_issues:
  - "npm audit reports 6 vulns (4 moderate, 2 high) as of last check — pre-existing 1 moderate postcss + 2 high sharp are transitive inside next@16.2.11's pinned deps (only offered 'fix' is downgrading next to 9.x, which is wrong). All dev-time/build-time, none shipped to clients. Re-check npm audit in later sessions."

warnings: []

health: "clean"
