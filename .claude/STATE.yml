# .claude/STATE.yml
# Living "what happened last session" file. Overwritten completely at the end of every session — never patched.
# Last Updated: 2026-07-22

current_focus: "GOVERNANCE session (non-code): Absolute Rule 13 — 'Non-fantasy data is untouchable — no exceptions' — installed at Nick's direct instruction, protecting the ~49 non-fantasy tables in the shared prolabel Supabase database (Elliott pricing = live business production data; also Todd, supplies ordering, others). Canonical full text: MASTER_CONTEXT.md new 'Shared Database Protection' section + Absolute Rule 13. Six permanent prohibitions: (1) never supabase db reset; (2) never migration repair touching foreign history (stub files only); (3) no raw SQL that could reach a non-fantasy table — every migration's table names verified against ARCHITECTURE.md's 'Database Schema (live)' inventory (now the definitive fantasy-owned list) before writing; (4) collision check before any new table name, mandatory forever; (5) ANY blast-radius ambiguity → STOP and ask Nick; (6) mandatory BLAST RADIUS: line on every completion report (new section + line in all three COMPLETION_TEMPLATES.md templates). Rule supersedes efficiency/folding/automation. Dual-Location check completed — all restatement sites updated and consistent: MASTER_CONTEXT.md (section + Rule 13 + Rule 11 pattern list + Session-End Steps + Prompt Format SESSION END block), COMPLETION_TEMPLATES.md (BLAST RADIUS section + 3 templates + Dual-Location pattern list), ARCHITECTURE.md (Shared-Database Constraints upgraded to full operational restatement; schema inventory marked definitive), BUILD_PROTOCOL.md (Build step 2 + Session-End step 7), this file's warnings array. Build queue position UNCHANGED by this session — next unchecked build item: first item of 'Env, secrets, deploy' in 01_foundation.md (.env.example contract) — NOTE for that session: the Vercel project-creation and env-vars items are already done manually per MANUAL_SETUP_CHECKLIST [x] entries (verify-and-check-off, don't re-create), and the health-check item is the wave's actual completion gate (authenticated read from the deployed environment) — it may need Nick able to sign in as nick@prolabelco.com (see nick_pending)."

build_system:
  index: ".claude/BUILD_INDEX.md"
  files_location: ".claude/build/"
  total_files: 7
  current_build_files:
    - ".claude/build/01_foundation.md"

nick_pending:
  - "Before the Wave 1 env/deploy session (next session) reaches the health-check item, or at latest before first real sign-in: make sure you can actually sign in as nick@prolabelco.com — that pre-existing account's password is whatever you saved when it was created 2026-05-15 (last sign-in 2026-06-06); reset via the Supabase dashboard if forgotten. The health-check item may exercise an authenticated-as-admin read."
  - "Before Wave 3a: verify whether Sleeper actually has a public ADP endpoint, or decide a real alternative source — see MANUAL_SETUP_CHECKLIST.md Wave 3a section."
  - "Before Wave 5's waiver/FAAB recommendations item is built: run a dedicated scoping session to define the actual waiver/FAAB candidate scoring formula (opportunity-based — snap/target/touch share trends — is the recommended direction per wiki/topics/league-mechanics/waiver-wire-faab-strategy.md, but no formula exists yet). See 05_eye_candy.md 'Waiver/FAAB recommendations' section, DEFERRED item."

blockers: []

known_issues:
  - "npm audit reports 6 vulns (4 moderate, 2 high) — last checked live 2026-07-22 (types/client session): pre-existing 1 moderate postcss + 2 high sharp are transitive inside next@16.2.11's pinned deps (only offered 'fix' is downgrading next to 9.x, which is wrong). All dev-time/build-time, none shipped to clients. Not re-checked this governance session (no dependency changes). Re-check in later sessions."

warnings:
  - "ABSOLUTE RULE 13 (canonical: MASTER_CONTEXT.md 'Shared Database Protection — Non-Fantasy Data Is Untouchable'): the Supabase database is the shared multi-app 'prolabel' project — ~49 non-fantasy tables incl. live business production data (Elliott). Never supabase db reset; never migration repair touching foreign history (new foreign versions get empty local stub files, never repair); no raw SQL that could reach a non-fantasy table — verify every migration's table names against ARCHITECTURE.md's 'Database Schema (live)' inventory; collision check before any new table name; ANY ambiguity → stop and ask Nick; mandatory BLAST RADIUS: line on every completion report. Owner-write RLS policies must match Nick's specific auth.uid(), never the blanket authenticated role — realized as public.is_fantasy_admin(); rotate the admin there, never by editing per-table policies. Operational restatement: ARCHITECTURE.md 'Shared-Database Constraints'."
  - "src/lib/supabase/database.types.ts is generated — never hand-edit; regenerate via npm run gen:types after every schema migration. It deliberately contains the whole shared public schema (foreign prolabel tables included) — this is expected, not drift."

health: "clean"
