# .claude/STATE.yml
# Living "what happened last session" file. Overwritten completely at the end of every session — never patched.
# Last Updated: 2026-07-22

current_focus: "WAVE 2 CONTINUES (2026-07-22): first 3 items of 02_data_pipeline.md's 'Sleeper — league-scoped sync' sub-section complete as one declared fold, all live-verified against Nick's first real connected league (1257464256710856704, '10 enter 1 Leaves' — season 2025, 10-team full-PPR, status complete; NOTE: Sleeper mints a new ID per season, the 2026 renewal will be a separate connect). Item 1: syncLeagueConfig (src/services/sleeper/league-config.ts) — /league/{id} → leagues (owner_id = admin UUID per Nick; previous_league_id resolved against existing rows only, null otherwise) + league_config (raw as-received + derived_config re-derived in full; te_premium wire key bonus_rec_te is declared wiki silence). Item 2: syncLeagueRosters (league-rosters.ts) — rosters+users → rosters/roster_players/standings; STANDINGS WAS A BUILD-FILE SCOPE GAP, folded in by Nick-signed scope correction (item text amended); three-way bench subtraction, split-fpts recombination, stale-row removal, missing-player skip+record policy (Nick's ruling). Item 3: syncLeagueMatchups (league-matchups.ts) — full-season sweep → matchups + player_scores; PLAYER_SCORES WAS THE SAME SHAPE OF SCOPE GAP, folded in by Nick-signed scope correction (players_points is already league-scored — pure ingestion, distinct from Wave 4's compute-from-raw-stats exclusion); finality slice: status complete → is_final true, in-season promotion deferred to cron sub-section. Verification highlights: starter-sum vs Sleeper's own points exact on all 180 roster-weeks; 0 bad pairing groups; effective_points generated column verified; idempotent re-runs on items 1-2. Runners: npm run sync:league / sync:rosters / sync:matchups (tsx pattern, native ID as arg). NEXT SESSION: 02_data_pipeline.md → 'Sleeper — league-scoped sync' remaining items: (4) draft-state fetch (/draft/{draft_id}/picks) → shared draft_state with source='sleeper_poll' (read sleeper-api/draft-endpoint; league top-level draft_id is not exhaustive — use /league/{id}/drafts; record native_draft_id selection provenance), then (5) per-league orchestrator (dependency order league config → rosters → matchups, per-league failure isolation; also the deferred general client-side rate limiter belongs here where multi-league fan-out first exists). Both fit one fold (same sub-section, sleeper-api category) unless Clarify says otherwise."

build_system:
  index: ".claude/BUILD_INDEX.md"
  files_location: ".claude/build/"
  total_files: 7
  current_build_files:
    - ".claude/build/02_data_pipeline.md"

nick_pending:
  - "Before ESPN work later in Wave 2: decide public vs. private for each ESPN league (public skips cookie auth entirely — check before assuming cookies are needed); generate espn_s2/SWID only for private leagues; have ESPN league ID(s) + season year ready."
  - "When your 2026 Sleeper league renews (new league_id every season): connect it by running sync:league / sync:rosters / sync:matchups with the new ID — the 2025 league (1257464256710856704) is fully ingested and its chain will resolve automatically if the renewal carries previous_league_id."
  - "Before Wave 4's owner UI (moved from the health-check item, which chose the no-sign-in secret-key path): make sure you can sign in as nick@prolabelco.com — that pre-existing account's password is whatever you saved at its 2026-05-15 creation; reset via the Supabase dashboard if forgotten. No session has exercised a real sign-in yet."
  - "Before Wave 3a: verify whether Sleeper actually has a public ADP endpoint, or decide a real alternative source — see MANUAL_SETUP_CHECKLIST.md Wave 3a section."
  - "Before Wave 5's waiver/FAAB recommendations item is built: run a dedicated scoping session to define the actual waiver/FAAB candidate scoring formula (opportunity-based — snap/target/touch share trends — is the recommended direction per wiki/topics/league-mechanics/waiver-wire-faab-strategy.md, but no formula exists yet). See 05_eye_candy.md 'Waiver/FAAB recommendations' section, DEFERRED item."

blockers: []

known_issues:
  - "npm audit reports 6 vulns (4 moderate, 2 high) — last checked live 2026-07-22 (tsx install session): pre-existing 1 moderate postcss + 2 high sharp are transitive inside next@16.2.11's pinned deps (only offered 'fix' is downgrading next to 9.x, which is wrong). All dev-time/build-time, none shipped to clients. No new packages this session. Re-check in later sessions."
  - "derived_config.ir_slot_count counts IR labels in roster_positions — but Nick's own league proves IR capacity can be expressed only via settings.reserve_slots (4 live reserve players, 0 IR labels). Raw settings are preserved in roster_settings_raw so nothing is lost; revisit the derivation (label count OR reserve_slots) when a consumer actually reads ir_slot_count. Flagged as WIKI NOTE evidence on league-endpoint open question #4."

warnings:
  - "ABSOLUTE RULE 13 (canonical: MASTER_CONTEXT.md 'Shared Database Protection — Non-Fantasy Data Is Untouchable'): the Supabase database is the shared multi-app 'prolabel' project — ~49 non-fantasy tables incl. live business production data (Elliott). Never supabase db reset; never migration repair touching foreign history (new foreign versions get empty local stub files, never repair); no raw SQL that could reach a non-fantasy table — verify every migration's table names against ARCHITECTURE.md's 'Database Schema (live)' inventory; collision check before any new table name; ANY ambiguity → stop and ask Nick; mandatory BLAST RADIUS: line on every completion report. Owner-write RLS policies must match Nick's specific auth.uid(), never the blanket authenticated role — realized as public.is_fantasy_admin(); rotate the admin there, never by editing per-table policies. Operational restatement: ARCHITECTURE.md 'Shared-Database Constraints'."
  - "src/lib/supabase/database.types.ts is generated — never hand-edit; regenerate via npm run gen:types after every schema migration. It deliberately contains the whole shared public schema (foreign prolabel tables included) — this is expected, not drift. Trap discovered this session: generated Insert types list stored generated columns (matchups.effective_points) as insertable — Postgres rejects them; sync code must omit them."
  - "src/lib/supabase/admin.ts bypasses RLS (secret key) — server-only, never import from client code; its env var is deliberately not NEXT_PUBLIC_ so a client-bundle import fails loudly instead of leaking."
  - "Sleeper player catalog (/players/nfl): fetch at most ONCE PER DAY — Sleeper's most load-bearing rate rule. syncPlayersCatalog has no hard in-service guard yet (the sync-run tracking table in the cron sub-section is the intended substrate); until the cron item lands, cadence discipline is manual — do not run npm run sync:players more than once a day except for a genuine failure retry. The per-league endpoints (league/rosters/users/matchups) are ordinary-volume calls and not under this rule."

health: "clean"
