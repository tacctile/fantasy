# .claude/STATE.yml
# Living "what happened last session" file. Overwritten completely at the end of every session — never patched.
# Last Updated: 2026-07-22

current_focus: "Wave 1 (Foundation), feature-build session via BUILD_PROTOCOL: third item of the 'Schema — league-scoped state' section of 01_foundation.md is complete and live — matchups table (20260722052726_create_matchups_table.sql), created via supabase migration new and pushed via the allowed 'npx supabase db push' rule (password sourced from .env.local in-shell). Shape (Nick's Clarify decisions, all four recommended options): per-roster weekly grain — one row per roster per week, Sleeper's native response grain, ESPN schedule entries split into two rows per side at Wave 2 ingestion; PK (league_id, week, native_roster_id); composite FK -> rosters plus FK -> leagues, both on delete cascade; platform + season_year duplicated (N:1 precedent). Dual period columns per the espn-api matchup ADR, never collapsed: week (scoring period — Sleeper week / ESPN scoringPeriodId) + matchup_period (ESPN matchupPeriodId; check-enforced = week on sleeper rows, which have no native contest-interval ID). Pairing via native_matchup_id nullable integer (both wire integers; null = bye) — pairings reconstructed by grouping on (league_id, week, native_matchup_id) with group-size validation, never forced home/away pairs. Scores: raw points (nullable, as-received) + custom_points (sleeper-only commissioner override, check-constrained) + stored generated effective_points = coalesce(custom_points, points); ESPN live/projected columns deliberately excluded — exact field names are an open wiki question, Wave 2 adds them via new migration once verified against a live payload. Finality: fetched_at timestamptz + is_final boolean not null default false, promoted only by our own sync policy per the wiki finality heuristic (games complete + correction window + stable repeat reads). is_home boolean espn-only check; ESPN winner field excluded (provisional per wiki, derivable from points + finality). RLS enabled at creation (owner-write policies remain the integrity item's scope), set_updated_at trigger. Pre-flight prolabel collision check passed (no existing matchups table). Wiki coverage: grain/pairing/precedence/finality covered by sleeper-api/matchup-endpoint + espn-api/matchup-response-structure; schema-reference still has no league-scoped-state DDL ADR — silence declared at decision time (third consecutive session), resolved via Clarify — report carries 'gap found and flagged' with the standing WIKI NOTE suggesting a 'league-scoped state tables' DDL addendum page. All objects verified live via read-only queries (columns/nullability, generated column, PK, both FKs + cascade, all 3 checks, RLS, trigger, migration history row). Log cap triggered this session: 2026-07-21_05.md condensed into .claude/logs/ARCHIVE.md and deleted. Next unchecked build item: 'Migration: standings table' — fourth item of the 'Schema — league-scoped state' section of 01_foundation.md."

build_system:
  index: ".claude/BUILD_INDEX.md"
  files_location: ".claude/build/"
  total_files: 7
  current_build_files:
    - ".claude/build/01_foundation.md"

nick_pending:
  - "Before Wave 3a: verify whether Sleeper actually has a public ADP endpoint, or decide a real alternative source — see MANUAL_SETUP_CHECKLIST.md Wave 3a section."
  - "Before Wave 5's waiver/FAAB recommendations item is built: run a dedicated scoping session to define the actual waiver/FAAB candidate scoring formula (opportunity-based — snap/target/touch share trends — is the recommended direction per wiki/topics/league-mechanics/waiver-wire-faab-strategy.md, but no formula exists yet). See 05_eye_candy.md 'Waiver/FAAB recommendations' section, DEFERRED item."

blockers: []

known_issues:
  - "npm audit reports 6 vulns (4 moderate, 2 high) as of last check — pre-existing 1 moderate postcss + 2 high sharp are transitive inside next@16.2.11's pinned deps (only offered 'fix' is downgrading next to 9.x, which is wrong). All dev-time/build-time, none shipped to clients. Re-check npm audit in later sessions."

warnings:
  - "The Supabase database is the shared multi-app 'prolabel' project. Never run supabase db reset or supabase migration repair against it; new foreign history versions get empty local stub files, never repair. Owner-write RLS policies must match Nick's specific auth.uid(), never the blanket authenticated role. Full rules: ARCHITECTURE.md 'Shared-Database Constraints'."

health: "clean"
