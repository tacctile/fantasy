# .claude/STATE.yml
# Living "what happened last session" file. Overwritten completely at the end of every session — never patched.
# Last Updated: 2026-07-22

current_focus: "Wave 1 (Foundation), feature-build session via BUILD_PROTOCOL: second item of the 'Schema — league-scoped state' section of 01_foundation.md is complete and live — roster_players join (20260722051505_create_roster_players_table.sql), created via supabase migration new and pushed by Claude via the allowed 'npx supabase db push' rule (password sourced from .env.local in-shell). Shape: one row per rostered player per team, always keyed by sleeper_player_id (canonical identity per the player-identity-mapping ADR — ESPN playerId resolves through player_id_crosswalk at Wave 2 ingestion, D/ST rows anchor on team-abbreviation IDs); composite PK (league_id, native_roster_id, sleeper_player_id); composite FK -> rosters plus direct FKs -> leagues/players, all on delete cascade; platform + season_year duplicated (N:1 with leagues, rosters precedent). Clarify decisions (Nick, all recommended options): (1) slot model = roster_slot enum (starter/bench/reserve/taxi) normalized once at Wave 2 ingestion (Sleeper bench = three-way subtraction per wiki; ESPN lineupSlotId 20->bench/21->reserve/active->starter) plus native detail columns starter_slot_index (Sleeper starters[] order) and espn_lineup_slot_id (raw), each check-constrained to its platform; (2) ESPN acquisition metadata excluded — no registered v1 consumer; (3) unique (league_id, sleeper_player_id) enforced — a player on two rosters in one league fails loudly at sync rather than double-counting in availability derivations. Current-snapshot semantics, no week column — historical lineups belong to matchups per both roster wiki pages. RLS enabled at creation (owner-write policies remain the integrity item's scope), set_updated_at trigger. Wiki coverage: identity/role semantics covered by sleeper-api/roster-endpoint, espn-api/roster-response-structure, schema-reference/player-identity-mapping; schema-reference still has no league-scoped-state DDL ADR — silence declared at decision time (second consecutive session), resolved via Clarify — report carries 'gap found and flagged' with a WIKI NOTE suggesting a 'league-scoped state tables' DDL addendum page. All objects verified live via read-only queries (columns/nullability, PK, 3 FKs + cascade, unique, both checks, enum values, RLS, trigger, migration history row). Log cap triggered this session: 2026-07-21_04.md condensed into .claude/logs/ARCHIVE.md and deleted. Next unchecked build item: 'Migration: matchups table' — third item of the 'Schema — league-scoped state' section of 01_foundation.md."

build_system:
  index: ".claude/BUILD_INDEX.md"
  files_location: ".claude/build/"
  total_files: 7
  current_build_files:
    - ".claude/build/01_foundation.md"

nick_pending:
  - "Before Wave 3a: verify whether Sleeper actually has a public ADP endpoint, or decide a real alternative source — see MANUAL_SETUP_CHECKLIST.md Wave 3a section."
  - "Before Wave 5's waiver/FAAB recommendations item is built: run a dedicated scoping session to define the actual waiver/FAAB candidate scoring formula (opportunity-based — snap/target/touch share trends — is the recommended direction per wiki/topics/league-mechanics/waiver-wire-faab-strategy.md, but no formula exists yet). See 05_eye_candy.md 'Waiver/FAAB recommendations' section, DEFERRED item."

blockers: []

known_issues:
  - "npm audit reports 6 vulns (4 moderate, 2 high) as of last check — pre-existing 1 moderate postcss + 2 high sharp are transitive inside next@16.2.11's pinned deps (only offered 'fix' is downgrading next to 9.x, which is wrong). All dev-time/build-time, none shipped to clients. Re-check npm audit in later sessions."

warnings:
  - "The Supabase database is the shared multi-app 'prolabel' project. Never run supabase db reset or supabase migration repair against it; new foreign history versions get empty local stub files, never repair. Owner-write RLS policies must match Nick's specific auth.uid(), never the blanket authenticated role. Full rules: ARCHITECTURE.md 'Shared-Database Constraints'."

health: "clean"
