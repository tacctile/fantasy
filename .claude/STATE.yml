# .claude/STATE.yml
# Living "what happened last session" file. Overwritten completely at the end of every session — never patched.
# Last Updated: 2026-07-21

current_focus: "Wave 1 (Foundation), feature-build session via BUILD_PROTOCOL: the entire 'Schema — platform + identity foundation' section of 01_foundation.md is complete and live (Nick approved folding the section's three trivially-adjacent migration items into one session per BUILD_PROTOCOL's edge case). Three migrations created via supabase migration new and applied to the live DB: platform enum (sleeper|espn), players (Sleeper-anchored identity store, text PK, RLS enabled at creation), player_id_crosswalk (five nullable text ID columns, partial unique index on espn_player_id, FK -> players, RLS enabled). All objects verified live via read-only queries. Major discovery: the linked Supabase project tszssadgsxjoymcttlwd is Nick's shared multi-app 'prolabel' database (49 tables from todd/elliott/trip/loan apps, 23 foreign migration-history versions, 2 pre-existing auth users) — Nick confirmed staying in it deliberately. Adopted the no-touch history policy: 23 empty *_prolabel_shared_history_stub.sql files mirror the foreign versions locally so db push works without ever writing to them; supabase migration repair and supabase db reset are permanently forbidden (see ARCHITECTURE.md Shared-Database Constraints + MANUAL_SETUP_CHECKLIST decision of record). DB password handed over as SUPABASE_DB_PASSWORD in .env.local; the auto-permission classifier blocks Claude-executed DB writes, so Nick ran the actual db push in his terminal. Post-report, Nick requested a provenance self-audit of every decision: outcome was that the players column decomposition had come from general knowledge without consulting the sleeper-api wiki category; verification against sleeper-api/players-endpoint + player-data-quirks (read with Nick's explicit authorization) produced corrective migration 20260722042128_add_players_sleeper_reference_fields.sql (applied + verified live): fantasy_positions text[], search_rank, injury_status, search_full_name, birth_date promoted to first-class columns per wiki-assigned roles; no field-name mismatches found; text-not-enum status and no-bye_week both wiki-validated. Next unchecked build item: 'Migration: leagues table' — first item of the 'Schema — league + config' section of 01_foundation.md."

build_system:
  index: ".claude/BUILD_INDEX.md"
  files_location: ".claude/build/"
  total_files: 7
  current_build_files:
    - ".claude/build/01_foundation.md"

nick_pending:
  - "Before Wave 3a: verify whether Sleeper actually has a public ADP endpoint, or decide a real alternative source — see MANUAL_SETUP_CHECKLIST.md Wave 3a section."
  - "Before Wave 5's waiver/FAAB recommendations item is built: run a dedicated scoping session to define the actual waiver/FAAB candidate scoring formula (opportunity-based — snap/target/touch share trends — is the recommended direction per wiki/topics/league-mechanics/waiver-wire-faab-strategy.md, but no formula exists yet). See 05_eye_candy.md 'Waiver/FAAB recommendations' section, DEFERRED item."
  - "Optional workflow improvement: add a Bash allow rule for 'npx supabase db push' (Claude Code settings) so future migration sessions can apply migrations directly — the auto-permission classifier currently blocks Claude-executed DB writes, so Nick runs each push himself in a terminal."

blockers: []

known_issues:
  - "npm audit reports 6 vulns (4 moderate, 2 high) as of last check — pre-existing 1 moderate postcss + 2 high sharp are transitive inside next@16.2.11's pinned deps (only offered 'fix' is downgrading next to 9.x, which is wrong). All dev-time/build-time, none shipped to clients. Re-check npm audit in later sessions."

warnings:
  - "The Supabase database is the shared multi-app 'prolabel' project. Never run supabase db reset or supabase migration repair against it; new foreign history versions get empty local stub files, never repair. Owner-write RLS policies must match Nick's specific auth.uid(), never the blanket authenticated role. Full rules: ARCHITECTURE.md 'Shared-Database Constraints'."

health: "clean"
