# .claude/STATE.yml
# Living "what happened last session" file. Overwritten completely at the end of every session — never patched.
# Last Updated: 2026-07-21

current_focus: "Wave 1 (Foundation), feature-build session via BUILD_PROTOCOL: third and final item of the 'Schema — league + config' section of 01_foundation.md is complete and live — league_config table (20260722045532_create_league_config_table.sql), created via supabase migration new and pushed by Claude via the allowed 'npx supabase db push' rule (password sourced from .env.local in-shell). Shape per the schema-reference/league-configuration-data-model ADR: league_id uuid PK doubling as FK -> leagues.platform_league_uuid on delete cascade (1:1 extension of the league row), scoring_settings_raw/roster_settings_raw/derived_config jsonb all not null, timestamps, set_updated_at trigger, RLS enabled at creation (owner-write policies remain the integrity item's scope). Clarify decisions (Nick): ADR-exact three-column shape — no platform/season_year duplication, the one-row-per-platform-season-league parent already carries both, satisfying Schema Rules 2/3 intent; all three JSONB columns NOT NULL; FK on delete cascade. ADR silences (nullability, on-delete, timestamps) were declared at decision time and resolved via Clarify + repo pattern — report carries 'gap found and flagged' with a WIKI NOTE suggesting a short ADR addendum recording the three DDL choices. All objects verified live via read-only queries (columns/nullability, PK, FK-cascade, RLS, trigger, migration history row). Log cap triggered this session: 2026-07-21_02.md condensed into .claude/logs/ARCHIVE.md and deleted. The 'Schema — league + config' section is now fully checked. Next unchecked build item: 'Migration: rosters table' — first item of the 'Schema — league-scoped state' section of 01_foundation.md."

build_system:
  index: ".claude/BUILD_INDEX.md"
  files_location: ".claude/build/"
  total_files: 7
  current_build_files:
    - ".claude/build/01_foundation.md"

nick_pending:
  - "Before Wave 3a: verify whether Sleeper actually has a public ADP endpoint, or decide a real alternative source — see MANUAL_SETUP_CHECKLIST.md Wave 3a section."
  - "Before Wave 5's waiver/FAAB recommendations item is built: run a dedicated scoping session to define the actual waiver/FAAB candidate scoring formula (opportunity-based — snap/target/touch share trends — is the recommended direction per wiki/topics/league-mechanics/waiver-wire-faab-strategy.md, but no formula exists yet). See 05_eye_candy.md 'Waiver/FAAB recommendations' section, DEFERRED item."

blockers: []

known_issues:
  - "npm audit reports 6 vulns (4 moderate, 2 high) as of last check — pre-existing 1 moderate postcss + 2 high sharp are transitive inside next@16.2.11's pinned deps (only offered 'fix' is downgrading next to 9.x, which is wrong). All dev-time/build-time, none shipped to clients. Re-check npm audit in later sessions."

warnings:
  - "The Supabase database is the shared multi-app 'prolabel' project. Never run supabase db reset or supabase migration repair against it; new foreign history versions get empty local stub files, never repair. Owner-write RLS policies must match Nick's specific auth.uid(), never the blanket authenticated role. Full rules: ARCHITECTURE.md 'Shared-Database Constraints'."

health: "clean"
