# .claude/STATE.yml
# Living "what happened last session" file. Overwritten completely at the end of every session — never patched.
# Last Updated: 2026-07-22

current_focus: "WAVE 1 COMPLETE (2026-07-22): the health-check session (single-item fold, the file's last item) shipped /api/health + the deferred secret-key admin client (src/lib/supabase/admin.ts — Nick's Clarify resolved the deliberately-deferred auth-approach question: secret-key, public route, minimal { ok } response), and the deployed completion gate PASSED — production fantasy-virid-three.vercel.app/api/health returned 200 {\"ok\":true} on a real authenticated read (deploy dpl_Bg1TUmWx6c28gxC3po4qx1WiqwDE from 2e44b40, READY ~22s). That also functionally proved all three Supabase env vars in Vercel Production (the proof the env-vars item deferred by design). 01_foundation.md Status 🟢 Complete; BUILD_INDEX registry row + Wave Roadmap flipped 🟢. NEXT SESSION: open 02_data_pipeline.md (Wave 2 — first not-fully-checked file in roadmap order). CRITICAL Wave 2 notes: (a) ALL Wave 2 MANUAL_SETUP_CHECKLIST items are still [ ] open — per the Manual Setup Flag, the session must check which ones the first unchecked item actually depends on BEFORE Clarify and halt with a MANUAL SETUP REQUIRED block if a needed one is still open (Sleeper league ID is likely needed immediately; ESPN items only when ESPN work starts); (b) two named singleton exceptions live in Wave 2 — ESPN↔Sleeper crosswalk resolution, and the first authenticated ESPN call (also a first-touch-of-new-external-service hard stop); (c) Sleeper first, ESPN second — build/validate the no-auth trivial case before the fragile cookie-auth integration, per the wave's own scope line; (d) the admin client (bypasses RLS) now exists for sync-job writes — server-only."

build_system:
  index: ".claude/BUILD_INDEX.md"
  files_location: ".claude/build/"
  total_files: 7
  current_build_files:
    - ".claude/build/02_data_pipeline.md"

nick_pending:
  - "NEXT SESSION (Wave 2 start): its MANUAL_SETUP items are all still [ ] open — have at least one real Sleeper league ID ready (needed almost immediately for sync work); decide public vs. private for each ESPN league (public skips cookie auth entirely — check before assuming cookies are needed); generate espn_s2/SWID only for private leagues. The session will halt per the Manual Setup Flag if a needed item is still open."
  - "Before Wave 4's owner UI (moved from the health-check item, which chose the no-sign-in secret-key path): make sure you can sign in as nick@prolabelco.com — that pre-existing account's password is whatever you saved at its 2026-05-15 creation; reset via the Supabase dashboard if forgotten. No session has exercised a real sign-in yet."
  - "Before Wave 3a: verify whether Sleeper actually has a public ADP endpoint, or decide a real alternative source — see MANUAL_SETUP_CHECKLIST.md Wave 3a section."
  - "Before Wave 5's waiver/FAAB recommendations item is built: run a dedicated scoping session to define the actual waiver/FAAB candidate scoring formula (opportunity-based — snap/target/touch share trends — is the recommended direction per wiki/topics/league-mechanics/waiver-wire-faab-strategy.md, but no formula exists yet). See 05_eye_candy.md 'Waiver/FAAB recommendations' section, DEFERRED item."

blockers: []

known_issues:
  - "npm audit reports 6 vulns (4 moderate, 2 high) — last checked live 2026-07-22 (types/client session): pre-existing 1 moderate postcss + 2 high sharp are transitive inside next@16.2.11's pinned deps (only offered 'fix' is downgrading next to 9.x, which is wrong). All dev-time/build-time, none shipped to clients. Not re-checked since (no dependency changes in the three sessions after). Re-check in later sessions."

warnings:
  - "ABSOLUTE RULE 13 (canonical: MASTER_CONTEXT.md 'Shared Database Protection — Non-Fantasy Data Is Untouchable'): the Supabase database is the shared multi-app 'prolabel' project — ~49 non-fantasy tables incl. live business production data (Elliott). Never supabase db reset; never migration repair touching foreign history (new foreign versions get empty local stub files, never repair); no raw SQL that could reach a non-fantasy table — verify every migration's table names against ARCHITECTURE.md's 'Database Schema (live)' inventory; collision check before any new table name; ANY ambiguity → stop and ask Nick; mandatory BLAST RADIUS: line on every completion report. Owner-write RLS policies must match Nick's specific auth.uid(), never the blanket authenticated role — realized as public.is_fantasy_admin(); rotate the admin there, never by editing per-table policies. Operational restatement: ARCHITECTURE.md 'Shared-Database Constraints'."
  - "src/lib/supabase/database.types.ts is generated — never hand-edit; regenerate via npm run gen:types after every schema migration. It deliberately contains the whole shared public schema (foreign prolabel tables included) — this is expected, not drift."
  - "src/lib/supabase/admin.ts bypasses RLS (secret key) — server-only, never import from client code; its env var is deliberately not NEXT_PUBLIC_ so a client-bundle import fails loudly instead of leaking."

health: "clean"
