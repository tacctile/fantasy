# .claude/STATE.yml
# Living "what happened last session" file. Overwritten completely at the end of every session — never patched.
# Last Updated: 2026-07-22

current_focus: "WAVE 3B — BPA RECOMMENDATION ENGINE ITEMS 1-3 COMPLETE (2026-07-22, feature-build; fold declared at session start = the standard 3-decision-dense-item cap, live-behavior ceiling N/A per prior ruling). New pure calculation layer src/services/bpa/ (no I/O, deterministic throughout — item 4's per-pick recompute is a pure re-call): scoring.ts (parseScoringWeights/scoreProjectionStats — THE projection-scoring home, adp_*/pts_* structurally excluded per canon; key-namespace match scoring_settings↔projection stats CONFIRMED empirically live, 22/38 keys intersect on the real league) + replacement.ts (item 1, greedy league-wide fill: dedicated then flex narrowest-first, flex pooled across eligible positions — Nick-signed resolution of the VBD wiki page's open pooling question; startable-only demand, bench arrives via ADP calibration — Nick-signed; declared wiki silence on REC_FLEX/WRRB_FLEX/IDP_FLEX eligibility, unknown labels excluded+surfaced) + calibration.ts (item 2, ADP-window depth: depth_p = position players with adp_overall <= structural startable window, replacement = that depth-th by projection; REPLACES structural when computable, structural carried for visible reasoning; flagged fallbacks league-level structural_only + position-level zero-carrier — live: K/DEF have ZERO in-window ppr carriers on the real market and fall back exactly as signed; RB market-deepens 23→35/206.0→161.5) + base-value.ts (item 3, VORP = projection − operative line; negatives kept; unrankable positions excluded+surfaced; zero open Clarify questions — declared plainly per protocol). Verified 62/62 pure + three live read-only full-chain runs on the real league (top-15 = plausible 2026 PPR board: Bijan 182.6/Gibbs/Nacua/Chase/CMC, zero QBs in top 25 for the 1QB league). tsc/lint/build clean; three commits (one per item, 32acc9a/ef94e69/4bf5921). Four wiki conflicts/notes flagged in the report (VBD configurable-baseline vs build file; VORP fixed-rank vs computed fill — build file wins both, Nick-signed; flex-label eligibility silence; 'half above/below' gloss ≈ 29-45% live). SELF-LOCATION FOR NEXT SESSION: 03b BPA item 4 = dynamic recompute after every pick (the pure layer already supports it by construction — item 4 is the wiring/semantics: recompute off the livePicks snapshot, never cache a pre-draft baseline) + likely fold items 5 (independent roster-need signal — never blended into VORP ranking, hard design constraint) and 6 (server-side top-N query/action). Items 7-9 (panel UI, one-click draft wire into handleDraft, recompute-on-any-source) probably the session after. BPA sub-section 3/9. league-mechanics wiki category already entered this session — next session re-reads value-based-drafting (dynamic recalc guidance) + roster-construction pages as needed."

build_system:
  index: ".claude/BUILD_INDEX.md"
  files_location: ".claude/build/"
  total_files: 8
  current_build_files:
    - ".claude/build/03b_draft_assistant_live_draft.md"  # 🟡 — write path 4/4 [x], orchestration 3/3 [x], client-side live sync 5/5 [x], UI extensions 5/5 [x], projections ingestion 3/3 [x], BPA engine 3/9 (items 1-3 [x] 2026-07-22: src/services/bpa/ pure layer — scoring/replacement/calibration/base-value); next: BPA item 4 (dynamic recompute) + items 5-6 as the likely fold
    - ".claude/build/04_league_dashboard.md"  # 🟡 — data-access 5/5 [x], freshness [x], 4 display components [x], page assembly [x], league selector [x]; next fold: nav-shell sub-section (share-link panel deferred to the share-token singleton)
    - ".claude/build/03a_draft_assistant_static_board.md"  # 🟡 — all five sub-sections complete; sole remainder = bye-week [>] (schedule-source-gated); goes 🟢 when that ships or is re-ruled
    - ".claude/build/02_data_pipeline.md"  # ESPN residue [!], cron cadence [~], finality promotion [>] (Nick-signed deferral) — nothing self-locatable here

nick_pending:
  - "QUICK VISUAL CHECK on your next sign-in (carried — this session added no UI; all checks unchanged from last session): the right sidebar has a 'Recent picks' section under the rosters (newest first, '170 recorded' on the 2025 league): each row reads pick number, player name (the 10 team defenses read e.g. 'Philadelphia Eagles · DEF', not a raw abbreviation), 'R<round> · <team>', and a muted manual/sleeper badge; each roster card's totals line ends with '· 17 drafted'; position fill lines include drafted-but-unrostered players. With a draft session started: click Draft on a player → after it confirms, the feed's top row shows it with a 'manual' badge and an Undo button (only on that row) → Undo removes it, the row heals to available, and a toast names the player. CARRIED strip/status checks: 'Draft live · Sleeper · synced Ns ago' ticking; the pick/round/on-clock strip ('Draft complete · all 170 picks recorded' on the 2025 league); background-tab pause/catch-up. CARRIED optimistic-fold checks: Draft button → roster picker → 'Drafting…' → 'Drafted · Pick N'; conflicts roll back with a toast. CARRIED live-sync checks: picks land within ~5s; 40 drafted-but-unrostered players read dimmed 'Drafted · Pick N'; 'Available only' excludes them. CARRIED orchestration checks: Start draft → pulsing teal dot + destructive End draft. CARRIED dashboard checks at /leagues/<id>: header, week pills, mirrored matchup cards (unofficial chips + as-of captions), standings, power rankings, ?player= side sheet; default week 18 shows 'No opponent' singles — pick week 17 for full H2H."
  - "FINALITY PROMOTION — build scheduling (design Nick-signed + registered 2026-07-22 as the [>] item in 02_data_pipeline.md's cron sub-section; see known_issues for the mechanism): build as its own standalone fold, OR bundle with the season-start cadence session below — whichever comes first. HARD DEADLINE: before the 2026 NFL week 1→2 rollover (~2026-09-15; promotion first actually needed ~2026-09-18). Flip the item [>]→[ ] when its build session starts."
  - "BLOCKED ON EXTERNAL TIMING — nothing to do until ~mid-August 2026 (recorded 2026-07-22; expected clear ~2026-08-19 → 2026-09-02): Wave 2 ESPN manual items (public/private per league, espn_s2/SWID for private leagues, league IDs + season year). ESPN leagues are commissioner-locked until 2026 season setup. When they open: resolve the three items in a plain chat, flip them in MANUAL_SETUP_CHECKLIST.md, and flip 02_data_pipeline.md's 9 ESPN items [!]→[ ] so build sessions self-locate onto them again. Per the backsweep practice (BUILD_PROTOCOL Amendment Hygiene), that plain chat must also sweep the confirmations against already-registered 02/03c item text for staleness (03c_draft_assistant_espn_and_auction.md is the restructured ESPN+auction file, flip its [!] items back to [ ] alongside 02's) — and against Wave 4's shipped surfaces (platform-agnostic by construction) — and against 3b's shipped write path (recordManualPick is platform-agnostic; 03c's auction path extends it via the existing amount field on the same handleDraft seam) — and against the draft_sessions/poll orchestration (activateDraftSession/runActiveDraftPoll return skipped_non_sleeper for non-Sleeper leagues by design; 03c's ESPN poll path extends runRecordedDraftPoll; the ESPN in-draft rate-limit judgment call in MANUAL_SETUP's Wave 3b items applies to that extension) — and against the client-side live sync seams (listDraftPicks/live-picks merge/optimistic overlay are platform-agnostic; 03c inherits the same snapshot/merge/optimistic path unchanged — every RecordedPick carries catalog-joined playerFullName/playerPosition via the players embed, platform-agnostic by construction since draft_state rows always reference sleeper_player_id via the crosswalk; 03c's ESPN poller writes inherit the join for free) — and against the DraftOrderMeta on-clock path (slot_to_roster_id/reversal_round are Sleeper draft-object fields; ESPN draft-order projection will need its own source per the espn-api draft-detail wiki when 03c builds — the strip degrades to pick/round honestly for leagues without order metadata, so nothing breaks meanwhile) — and against the projections/BPA layer AS NOW SHIPPED (player_projections is source-checked 'sleeper'-only today; ESPN leagues' BPA uses the same Sleeper-sourced projections automatically — projections and the whole src/services/bpa/ pure layer are keyed by sleeper_player_id and league_config only, so the crosswalk covers ESPN rosters; ADP calibration needs the ESPN league's derived_config to resolve a format via resolveAdpScoringFormat, which is platform-agnostic — sweep any BPA item text that assumed otherwise)."
  - "SEASON START (~Sept 2026): revisit league-sync cadence — the specced 15-minute cron needs Vercel Pro (~$20/mo) or an alternative trigger; currently daily on Hobby per your 2026-07-22 decision (MANUAL_SETUP decision of record). Flipping 02_data_pipeline.md's cron-schedule item [~]→[x] happens when the sub-daily cadence ships. If Pro is bought, /api/cron/sync-adp is the ready mount point for a dedicated ADP+projections schedule slot (currently piggybacked on sync-leagues; the fused run feeds both adp_rankings and player_projections from one fetch). This session is also the fallback vehicle for the finality-promotion build (entry above) if it hasn't shipped as its own fold by then. NOTE: live-draft polling does NOT depend on this — it's client-driven (2026-07-22 orchestration decision) and needs no cron slot at any plan tier."
  - "BYE-WEEK [>] ITEM (03a) still awaits its schedule-source decision (plain chat: pick a schedule source, then a build session ships table+ingestion+row wiring — flips 03a 🟢). No longer blocking anything, but it's the only remainder between 03a and 🟢. Related small observation (logged in _38): the 3a board pool's fullName is also null for DST rows (renders the abbreviation fallback) — the item-4 name compose exists in draft-picks.ts if a future correctness pass wants to reuse it for getDraftBoardData."
  - "When your 2026 Sleeper league renews (new league_id every season): run npm run sync:league -- <new_id> once to connect it; every daily league-state cron run (and npm run sync:sleeper) covers it from then on. The 2025 league (1257464256710856704) is fully ingested including its 170-pick draft. Note: with auto-land live, the renewed league (season 2026 > 2025) becomes the DASHBOARD you land on after sign-in — expected behavior, newest season sorts first. Draft-day note: the renewed league is where Start draft + live polling + click-to-draft + the live strip/status indicator/roster panel/recent-picks feed — and the BPA recommendations panel once items 7-9 ship — will actually run in anger."
  - "Before Wave 5's waiver/FAAB recommendations item is built: run a dedicated scoping session to define the actual waiver/FAAB candidate scoring formula (opportunity-based — snap/target/touch share trends — is the recommended direction per wiki/topics/league-mechanics/waiver-wire-faab-strategy.md, but no formula exists yet)."

blockers:
  - "EXTERNAL TIMING (dated 2026-07-22): 02_data_pipeline.md's two ESPN sub-sections (9 items, [!]) are blocked until ESPN leagues open for the 2026 season — commissioner-locked, expected ~2026-08-19 → 2026-09-02. Not resolvable by Nick or any build session before then; do NOT guess visibility or fabricate placeholder league IDs (Nick's explicit instruction). Downstream: Wave 3c inherits this blocker (ESPN draft mechanics + auction). Does not stall the roadmap: 3b and Wave 4 both have buildable [ ] items."

known_issues:
  - "npm audit reports 6 vulns (4 moderate, 2 high) — last re-checked live 2026-07-22 (this session added no dependencies): pre-existing 1 moderate postcss + 2 high sharp are transitive inside next@16.2.11's pinned deps (only offered 'fix' is downgrading next to 9.x, which is wrong). All dev-time/build-time, none shipped to clients. Re-check in later sessions."
  - "FINALITY-PROMOTION GAP (surfaced 2026-07-22 by the cron session; investigated + designed + registered 2026-07-22 non-code session — still UNBUILT, mechanism unchanged until its build session): in-season, matchups/player_scores rows get is_final=true only when league lifecycle status reads 'complete' at sync time; the scheduled sync is current-week-only, so finished weeks stay is_final=false all season. Covering wiki: sleeper-api/matchup-endpoint (stat-correction window + finality Key Decision #3). Nick-signed design (2026-07-22): previous-week re-sync + promotion inside the daily league-state cron (week-advance = games-complete proxy; Friday-or-later UTC stable-read match = trigger — both Rule 12 genuine-silence sign-offs) + league-complete backstop sweep; needs a caller-driven finality extension to syncLeagueMatchups. Registered as the [>] item in 02_data_pipeline.md's cron sub-section. Zero live consequence until the season's first week rollover; HARD DEADLINE ~2026-09-15/18 (build scheduling in nick_pending). WAVE 4 STATUS: UI surfacing side SHIPPED and MOUNTED (UnofficialChip + AsOfTime render is_final/fetched_at honestly) — in-season, most rows will honestly read 'unofficial' until promotion ships; correct display, not a bug."

warnings:
  - "ABSOLUTE RULE 13 (canonical: MASTER_CONTEXT.md 'Shared Database Protection — Non-Fantasy Data Is Untouchable'): the Supabase database is the shared multi-app 'prolabel' project — ~49 non-fantasy tables incl. live business production data (Elliott). Never supabase db reset; never migration repair touching foreign history (new foreign versions get empty local stub files, never repair); no raw SQL that could reach a non-fantasy table — verify every migration's table names against ARCHITECTURE.md's 'Database Schema (live)' inventory; collision check before any new table name (the generated-types check alone is not the live check); ANY ambiguity → stop and ask Nick; mandatory BLAST RADIUS: line on every completion report. Owner-write RLS policies must match Nick's specific auth.uid(), never the blanket authenticated role — realized as public.is_fantasy_admin(); rotate the admin there, never by editing per-table policies. Operational restatement: ARCHITECTURE.md 'Shared-Database Constraints'. WAVE 4 SPECIFIC: the share-token RLS sub-section adds SELECT policies on six fantasy tables — a named singleton fold (Folding Policy exception #5) with its boundary tests; owner policies stay pinned to is_fantasy_admin(). draft_sessions (and draft_state) are NEVER on any spectator-policy list, at any wave — and player_projections/adp_rankings have no registered spectator consumer either (the BPA layer is admin-only). Related standing decline (2026-07-22 live-sync Clarify): Supabase Realtime was declined for the live board partly because it would touch the shared project's realtime publication — interval re-fetch needs no shared-infra config."
  - "CLIENT-SIDE LIVE SYNC SEAMS (updated 2026-07-22, UI-extensions fold — sub-section 5/5 + UI 5/5): the live board path is ticker → pollActiveDraft (returns full listDraftPicks snapshot per executed poll) → shell's fingerprint-deduped livePicks state (+ optimistic pendingPicks overlay + pollHealth aggregation + DraftOrderMeta state) → live-picks.ts mergePicksIntoPlayers → BOTH board regions; the roster panel and recent-picks feed additionally read livePicks DIRECTLY for pick→team attribution (the merge deliberately carries no roster attribution). Extend THIS path for remaining 03b items — never a second poll driver, never a second merge, never a second pick-write UI path, never a second tick-outcome reporter, never a second order-metadata source, never a second pick-identity source (every RecordedPick path carries catalog-joined playerFullName/playerPosition via PICK_SELECT's players embed — DST names composed from split name fields per sleeper-api/dst-and-free-agents; never read pick identity from anywhere else) and never a second undo UI path (the feed's Undo button → shell handleUndo → undoManualPick action is THE surfacing; local snapshot heal on success is deliberate — the next tick would self-heal anyway). Key invariants: 'drafted' availability AND draftPending are produced ONLY by the client merge; a confirmed snapshot row ALWAYS wins over pending; 'rostered' wins over the whole overlay; overlay applies regardless of session active (Nick-signed overlay-always — the feed's visible-whenever-picks-exist gating follows it); out-of-pool picks are skipped by the MERGE (pool membership stays the 3a rule) but fully surfaced by the FEED and counted by the roster panel (union-by-player: rostered baseline wins, fill via pool position else joined position — Nick-signed backsweep; ST/BN/IR stay roster-sync-only; draftedCount counts every attributed pick); pending picks are panel- and feed-invisible (confirmed-only, Nick-signed); full-snapshot-per-tick is load-bearing (undo deletions self-heal — never switch to incremental); adoptPick is the ONE early-adoption path into the snapshot; next pick number = computeNextPickNumber; latestManualPickNumber mirrors the server's undo target — keep them in lockstep if undo semantics ever change. ON-CLOCK IS A PROJECTION (Nick-signed): projectOnClock is DISPLAY ONLY — never a write, validation, or auto-pick input. rosterLabel (player-row.tsx, exported) is THE roster display convention on the draft surface (picker, strip, panel-created teams, feed). BPA NOTE (items 7-9 upcoming): the recommendations panel wires its one-click draft into handleDraft/the same action (build-file mandate) and recomputes off the SAME livePicks snapshot — never a second pick-event feed."
  - "BPA PURE-LAYER CENTRALIZATION (live 2026-07-22, items 1-3): src/services/bpa/ is the ONE home of projection scoring and VORP math — scoring.ts parseScoringWeights/scoreProjectionStats is THE projection-scoring application (adp_*/pts_* key families structurally excluded — never used as league values; scores PROJECTED value only, actual player_scores stay ingested platform-scored per canon); replacement.ts computeReplacementLevels is THE replacement-level calc (greedy fill, flex pooled, startable-only — Nick-signed trio); calibration.ts calibrateReplacementLevels is THE market calibration (ADP-window depth from sentinel-filtered adp_rankings ONLY — never player_projections.stats; replaces structural, both carried; flagged structural fallbacks at league + position level, no cross-format fallback ever); base-value.ts computeBaseValues is THE VORP ranking (negatives kept; unrankable positions excluded+surfaced). Everything is pure/deterministic BY DESIGN — item 4's per-pick recompute is a re-call against the shrinking pool, so never add caching, I/O, or a second scoring/replacement/VORP derivation anywhere downstream (items 4-9 consume these functions; item 5's need signal must stay a SEPARATE signal, never multiplied into base value — hard build-file constraint). Layout parsing stays parseRosterSlotLayout and format mapping stays resolveAdpScoringFormat (both draft-board.ts) — bpa/ imports them, never re-derives."
  - "DRAFT-SESSION ORCHESTRATION CENTRALIZATION (live 2026-07-22): services/draft-sessions.ts is the ONE home of the is_draft_active semantics — the 6h soft TTL is computed at read time there (DRAFT_ACTIVE_TTL_HOURS; no job ever mutates the row) and runActiveDraftPoll's TTL-aware check is the AUTHORITATIVE gate on elevated polling (clients only trigger; a stale tab can never force a Sleeper request — and a hidden tab makes no requests at all). Elevated cadence is client-driven at 5s (DraftPollTicker → pollActiveDraft action) because Hobby crons are daily-only — never add a cron schedule for it; /api/cron/sync-draft stays the unscheduled manual sweep; normal cadence = the daily league-state chain. Every EXECUTED poll records a sync_runs draft_poll row (Nick-signed); declined ticks record nothing by definition. Request budget per executed tick is up to 3 paced Sleeper calls (drafts array + picks + the detail-endpoint fallback) — far inside the client's 250ms global gate and Sleeper's ~1,000 req/min ceiling at 5s cadence. The snapshot's players-embed join adds DB width only, zero extra Sleeper calls."
  - "MANUAL-PICK WRITE PATH CENTRALIZATION (live 2026-07-22): src/services/draft-picks.ts recordManualPick is the ONE local pick-write path — 03b's auto-pick MUST write through it (build-file mandate), and no second draft_state insert path may ever appear outside it and the platform pollers. Its ONE UI caller is the shell's handleDraft (optimistic path) — future draft triggers (BPA panel one-click item, auto-pick, 03c auction bid entry) wire into handleDraft/the same action, never a parallel submit path. Server-action auth pattern to reuse: the gate lives INSIDE each action (getAdminAuthState → typed unauthorized) + RLS as the second wall; owner actions use the RLS server client, never the secret-key client. draft_state rows are immutable-once-written — conflict responses return the authoritative row, which the shell adopts. Undo has its UI (the recent-picks feed's single-row Undo button, session-active only) — extend that surfacing, never add a second undo control."
  - "PLAYER_SCORES IS INGESTED, NEVER COMPUTED (Nick-signed canon, re-confirmed 2026-07-22 by the Wave 4 amendment): platform-scored values land as-received (Sleeper players_points — league-scored by Sleeper per each league's own settings; ESPN as-received when unblocked). There is NO compute-from-raw-stats path anywhere in v1 for ACTUAL scores. PROJECTIONS ARE THE SANCTIONED EXCEPTION BY DESIGN (Nick's 2026-07-22 decision of record, MANUAL_SETUP Wave 3b): player_projections stores component projection stats, and the BPA engine scores them with league_config scoring_settings_raw weights at read time — NOW REALIZED as src/services/bpa/scoring.ts (items 1-3, 2026-07-22) — that is projected value, never a substitute for actual player_scores, and preset pts_* format buckets are never used as league values in either domain (structurally excluded in parseScoringWeights). Never invent score rows for unrostered weeks (FA-week gap accepted for v1)."
  - "DASHBOARD QUERY SERVICE CENTRALIZATION (live 2026-07-22): src/services/dashboard.ts is the ONE home of (a) the standings ordering — fetchRankedStandings, wins desc → points_for desc → native_roster_id asc, Nick-signed under declared wiki silence — and (b) the power-ranking formula (All-Play record; regular-season weeks via raw playoff_week_start; ties=half-wins; lowConfidence <6 weeks); listScoredWeeks is likewise the ONE week-range/default-week source for dashboard surfaces. The Wave 4 display components (src/components/dashboard/) render these results AS-IS. The spectator loader passes its own share_token-scoped client (shared data-access logic is sanctioned; shared UI never is). Boundary discipline: the module never selects share_token/owner_id/native_league_id and has zero draft_state references — keep it that way when extending."
  - "ADMIN/SPECTATOR UI BOUNDARY (Wave 4, live 2026-07-22): src/components/dashboard/ is ADMIN-surface UI — the spectator surface must import ZERO components from it (or from draft-board/ or ui/dropdown-menu's draft usages); spectator gets its own components (04's spectator sub-section). Shared data-access (services/dashboard.ts) is the ONLY sanctioned sharing. Within the admin surface, cross-feature reuse is fine (PlayerCard reuses draft-board PositionBadge/InjuryChip; the dashboard page reuses LeagueSelector via subPath — Nick-signed). Freshness primitives: UnofficialChip + AsOfTime (components/dashboard/) are THE rendering of is_final/fetched_at — reuse them, never invent a second treatment (muted tier, never a status color; deterministic UTC captions). Slot-by-slot matchup pairing is impossible from synced data (was_starter only) — never fake a slot spine. The player-card open mechanism is the URL-driven ?player= sheet — future admin surfaces reuse this pattern/URL contract; query params deliberately do not survive a league switch."
  - "DARK MODE ONLY IS LOAD-BEARING IN CODE (live 2026-07-22): globals.css holds the app's single always-on token set in :root — deliberately NO light block, NO .dark override block, NO theme toggle; the `dark` class on <html> is a permanent marker keeping vendored shadcn dark:-variant tunings active. Never add a light token set, a prefers-color-scheme branch, or remove that class. ui/sonner.tsx hardcodes theme='dark' for the same reason — never wire it to a theme system. --radius stays 0.625rem (Nick-signed): containers rounded-xl, buttons/chips pills. input.tsx's fill is bg-well by design. Position identity tokens --pos-qb/rb/wr/te/k/def are badges/chips ONLY — never row tints; RB is deliberately green, not the brand teal (teal = interactive/live only; sanctioned for positive deltas — and for the Draft live indicator, which is exactly the live semantic; the 'Drafting…' pending cue and the status indicator/degraded text are deliberately muted-tier, NOT teal; the feed's source badges are likewise muted-tier)."
  - "OWNER AUTH SURFACE (live 2026-07-22) — three walls, keep all three: (1) src/proxy.ts (Next 16 proxy, NOT middleware.ts) refreshes the Supabase session per matched request and 307s unauth /leagues/* to /login — its matcher deliberately EXCLUDES /api so the cron Bearer surface never sees cookie plumbing. (2) The (admin) layout gate uses getAdminAuthState (src/lib/supabase/auth.ts → is_fantasy_admin RPC) — never hardcode the admin UUID in app code; a signed-in non-fantasy prolabel user is a REAL case and must get the zero-data NotAuthorizedCard. (3) Per-table RLS. Unauth/non-admin responses must never contain league data; both league routes' error.tsx boundaries render NO league data. ROOT AUTO-LAND targets the dashboard (/leagues/<id>). Server actions add a FOURTH gate point: auth INSIDE each action — six actions follow it (submitManualPick/undoManualPick/startDraftSession/endDraftSession/pollActiveDraft + login's signOut pattern). WAVE 4 SPECIFIC: the spectator surface is a genuinely separate rendering path (share_token-gated, zero admin imports)."
  - "src/lib/supabase/database.types.ts is generated — never hand-edit; regenerate via npm run gen:types after every schema migration. It deliberately contains the whole shared public schema (foreign prolabel tables included) — expected, not drift. Trap: generated Insert types list stored generated columns (matchups.effective_points) as insertable — Postgres rejects them; sync code must omit them."
  - "src/lib/supabase/admin.ts bypasses RLS (secret key) — server-only, never import from client code; its env var is deliberately not NEXT_PUBLIC_ so a client-bundle import fails loudly instead of leaking. The draft-board and dashboard routes deliberately do NOT use it — owner pages read through the RLS server client as the signed-in admin, and owner server actions write through it the same way."
  - "Sleeper player catalog (/players/nfl): at most ONCE PER DAY — Sleeper's most load-bearing rate rule. The cron route (/api/cron/sync-players) enforces this with a hard guard on sync_runs (running/success within 24h → skip; failure rows allow a genuine retry; guard read error → fail closed). BUT the manual script path (npm run sync:players) calls the service directly and BYPASSES the guard — manual cadence discipline still applies there. The orchestrator still deliberately EXCLUDES the catalog. Per-league endpoints remain paced by the client's global 250ms min-gap gate — which also absorbs the 5s live-draft polling (≤3 requests per executed tick incl. the draft-detail fallback — 0.6 req/s worst case per league, far inside the ~1,000 req/min ceiling)."
  - "api.sleeper.com (ADP/projections surface) is NOT api.sleeper.app/v1: undocumented, no contract, can change shape or vanish without notice — its consumer is the quarantined src/services/adp/ ONLY (own client, own pacing gate; documented-host rate knowledge does NOT transfer). The projections-source decision (2026-07-22) LANDED on this endpoint: the SAME fused ingestion run/fetch feeds BOTH adp_rankings AND player_projections — one request, each table with its own validate-before-swap gates and last-good-snapshot preservation; never add a second api.sleeper.com client or a separate projections fetch. 999.0 in any adp_* field is a 'no ADP in this format' sentinel — never data in adp_rankings; player_projections.stats deliberately retains the RAW object (sentinels included) as a snapshot — ADP is never read from player_projections (the BPA calibration reads sentinel-filtered adp_rankings only, enforced in calibration.ts's contract), and BPA consumers read component stats only. The adp_* field inventory is OPEN — ingestion is pattern-based; never hardcode a closed format list anywhere downstream. Covering wiki: sleeper-api/projections-endpoint."
  - "League-format→ADP-format mapping is CENTRALIZED in resolveAdpScoringFormat (src/services/draft-board.ts, Nick-signed 2026-07-22): superflex→'2qb' precedence, then ppr buckets; dynasty/IDP unmapped in v1; NO cross-format fallback anywhere. Any future consumer calls this function — never re-derives its own mapping (bpa/calibration.ts's live callers do exactly this). Same centralization rule for roster-slot layouts: parseRosterSlotLayout (same module) is the ONE parser of roster_settings_raw — bpa/replacement.ts consumes its output, never re-derives. ADP-absence classification is likewise centralized in deriveAdpNoticeKind (src/components/draft-board/adp-notice.tsx). The projection-scoring centralization the BPA sub-section required is now REALIZED: src/services/bpa/scoring.ts (see the BPA PURE-LAYER warning above)."

health: "clean"
