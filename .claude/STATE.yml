# .claude/STATE.yml
# Living "what happened last session" file. Overwritten completely at the end of every session — never patched.
# Last Updated: 2026-07-23

current_focus: "WAVE 3B — POSITIONAL-RUN sub-section COMPLETE 4/4 (2026-07-23, feature-build) with item 3, the non-blocking inline run badge near the position filter. Own fold ('a single client UI surface,' one independently verifiable artifact); could not fold forward — the next sub-section (draft queue + auto-pick) is NAMED SINGLETON fold exception #3. READ-ONLY session — no migration, no DB access at all; blast radius = client UI wiring + one temp pure-render harness (scripts/verify-run-badge.tsx, deleted, no DB/network). Built (5 edits): NEW src/components/draft-board/positional-run-badges.tsx `PositionalRunBadges({runBoard, tiers})` rendered in BoardToolbar immediately AFTER the position-filter chip group — appear-only (null when activeRuns empty), one muted pill per active run in the pure layer's count-desc→pos-asc order, reused PositionBadge (position identity color, NO new token) + 'run', title tooltip 'N POS in the last W picks · M left in the best available tier' (tier clause omitted when tiers null). Runs computed CLIENT-SIDE in the shell via detectPositionalRuns in a useMemo over livePicks (RecordedPick→RunPick via playerPosition; CONFIRMED picks only — a pending optimistic pick has no catalog position and isn't a market fact) — THE one run-detection source (there was zero consumer before; item 4's decoupling preserved: NOT routed through fetchBpaRecommendations). THREE Clarify decisions Nick-signed: (1) DATA FLOW = lift the BPA panel's already-computed context.tiers UP to the shell via a new OPTIONAL onTiers callback on BpaRecommendationsPanel (reuses the ONE summarizeTiers result — no second tier compute; reports null while loading/errored so the badge never shows stale depth; the SAME fetch, keyed on state); shell holds it as panelTiers and threads it down beside the run board through PlayerBoard→BoardToolbar as runTiers; degrades honestly to run-flag-only when the sidebar/panel is unmounted below `lg` (tiers null → tooltip drops the tier clause). Rejected: run-flag-only (loses the demand-vs-panic signal), badge inside the panel (contradicts 'near the position filter'). (2) CONTENT = muted chip + tooltip (not adaptive inline wording, not dot-only). (3) EMPTY/MULTIPLE = appear-only, one per run (not an always-present 'No runs' area). The tier pairing IS the wiki's tier-cliff refinement — a run matters materially only when it crosses a tier boundary (positional-run-detection-draft-day.md Key Decision), so the tooltip pairs the raw count with how much of the best available tier is left. FULL demand-vs-panic classification (opposite enter/fade-vs-wait responses) stays OUT of scope — unchanged standing WIKI NOTE / scope question in nick_pending. WIKI COVERAGE: complete — non-blocking-badge posture + tier-boundary-significance covered by positional-run-detection-draft-day.md; thresholds are declared silence (already shipped, unchanged); styling is DESIGN_SYSTEM discipline (TierDepthStrip precedent), not a wiki decision. Verified: tsc/lint/build clean + 18/18 static-render harness (react-dom/server, deleted) — appear-only empty string / single+multiple runs / count-desc DOM ordering / tier-clause present-vs-null-degraded / K-DEF never flagged / sliding-window activate-then-scroll-out; the underlying pure layers (detectPositionalRuns, summarizeTiers) were already live-verified on the real 170-pick draft in prior sessions. SELF-LOCATION FOR NEXT SESSION: first unchecked = the DRAFT QUEUE + AUTO-PICK sub-section (4 items) — NAMED SINGLETON fold exception #3 (live draft-day semantics; local-only auto-pick's interaction with first-write-wins deserves undivided attention). HARD CONSTRAINTS already recorded: auto-pick MUST write through recordManualPick, source='manual', undoable via the same undo — NEVER a Sleeper write (Sleeper's own platform auto-pick makes the real pick, the poller ingests it); the queue is stored independently of and never silently reordered by the BPA engine; queue/auto-pick consume pick events from the shared draft_state via the EXISTING merge path (no second poll driver, no direct draft-API/WebSocket); roster-construction-aware auto-pick skips a queue entry that would break a hard roster rule rather than blindly taking queue position 1. After that, RESILIENCE (2 items — read-only fallback to Wave 3a static behavior + the durable race/recompute/run-reset/auto-pick test suite). 03b stays 🟡 (positional-run 4/4 [x], queue/auto-pick 0/4, resilience 0/2 remain)."

build_system:
  index: ".claude/BUILD_INDEX.md"
  files_location: ".claude/build/"
  total_files: 8
  current_build_files:
    - ".claude/build/03b_draft_assistant_live_draft.md"  # 🟡 — write path 4/4 [x], orchestration 3/3 [x], client-side live sync 5/5 [x], UI extensions 5/5 [x], projections ingestion 3/3 [x], BPA engine 9/9 [x], TIER-CLIFF 4/4 [x], POSITIONAL-RUN 4/4 [x] (item 3 badge complete 2026-07-23: PositionalRunBadges in BoardToolbar, runs computed client-side in the shell over livePicks, tiers lifted from the BPA panel via onTiers; NO poll/query/VORP coupling); next: DRAFT QUEUE + AUTO-PICK (4 items, NAMED SINGLETON fold exception #3) then RESILIENCE (2)
    - ".claude/build/04_league_dashboard.md"  # 🟡 — data-access 5/5 [x], freshness [x], 4 display components [x], page assembly [x], league selector [x]; next fold: nav-shell sub-section (share-link panel deferred to the share-token singleton)
    - ".claude/build/03a_draft_assistant_static_board.md"  # 🟡 — all five sub-sections complete; sole remainder = bye-week [>] (schedule-source-gated); goes 🟢 when that ships or is re-ruled
    - ".claude/build/02_data_pipeline.md"  # ESPN residue [!], cron cadence [~], finality promotion [>] (Nick-signed deferral) — nothing self-locatable here

nick_pending:
  - "VISUAL CHECK BACKLOG — NEW this session: the POSITIONAL-RUN BADGE. On the live draft board, in the filter toolbar right after the QB/RB/WR/TE/K/DEF position chips, a small muted pill 'RB run' (position chip + 'run') appears when a run fires — 2 QB or 2 TE, or 3 RB or 3 WR, inside the last 6 picks; K/DEF never trigger. Hover it for 'N POS in the last W picks · M left in the best available tier' (the tier clause is present only when the right sidebar/BPA panel is visible — below `lg` width it degrades to just the count). Appear-only: nothing shows when no run is active; one pill per simultaneous run, most-intense first. On the COMPLETED 2025 draft it shows nothing (the last 6 picks are the K/DEF tail — correct). STILL OUTSTANDING to eyeball from prior sessions: (a) the 'Best-tier depth' counter strip at the TOP of the BPA panel (QB/RB/WR/TE badges = players remaining in each position's BEST still-available tier; hover for 'N in the best available tier · K tiers remaining'; uniform muted; near-exhausted on the completed draft QB 9/RB 1/WR 2/TE 1). (b) the BPA 'Recommendations' section (8 candidates: position badge, name, team · ADP, right-aligned signed VORP; 'Set my team' picker adds need badges 'Fills starter need' amber / 'Bench depth' / 'Position full'; per-row Draft → roster dropdown one-click; completed-draft need reads 'Position full'). (c) live strip/status ('Draft live · Sleeper · synced Ns ago', pick/round/on-clock, background-tab pause/catch-up), recent-picks feed + Undo, roster cards' drafted totals, optimistic Draft fold, live-sync (~5s land, drafted-unrostered dimmed). Dashboard at /leagues/<id>: header, week pills, matchup cards, standings, power rankings, ?player= sheet."
  - "OPTIONAL FUTURE — superflex/best-ball run sensitivity (deferred 2026-07-23 by Nick's Clarify): positional-run-detection-draft-day.md Key Decision mandates materially different run-sensitivity for superflex/2QB (QB scarcity is structural — proactive early QB is correct, NOT a run to react to) and best-ball (weight run risk higher — no in-season waiver correction). The shipped detectPositionalRuns applies uniform thresholds across formats because no superflex/best-ball league is connected (live league is 1QB redraft). When a superflex league connects (or if desired sooner), make the QB threshold format-aware via resolveAdpScoringFormat (superflex → QB doesn't flag as a run, or a higher bar) — a small change to runs.ts's threshold selection + a test + the badge picks it up for free. Not blocking; its own tiny fold. Also a WIKI NOTE + potential 03b scope amendment (see next item)."
  - "OPTIONAL FUTURE / SCOPE QUESTION — demand-vs-panic run classification (WIKI NOTE, unchanged 2026-07-23): positional-run-detection-draft-day.md Key Decisions mandate classifying runs into demand-driven (tier-cliff) vs panic-driven (contagion / ahead-of-ADP) with OPPOSITE recommended responses (enter/fade vs wait). The registered 03b positional-run sub-section (now COMPLETE 4/4) scoped only a count-based flag + a non-blocking badge that COMPOSES the tier-drain signal in its tooltip — it does NOT do the full classification with opposite recommendations. If the fuller wiki model is wanted for v1 it needs a NEW registered build item / Nick-signed scope amendment — not an oversight, a decision of record. Decide whenever; not blocking the remaining 03b items."
  - "OPTIONAL FUTURE — panel 'remember my team' per league: you chose re-pick-each-session (2026-07-22 Clarify), so the BPA panel forgets your team on remount. If you later want per-league memory, that's a small future migration (a my-roster-per-league column/setting) + a tiny panel read — flag it whenever, its own small fold. Not blocking anything."
  - "FINALITY PROMOTION — build scheduling (design Nick-signed + registered 2026-07-22 as the [>] item in 02_data_pipeline.md's cron sub-section; mechanism in known_issues): build as its own standalone fold OR bundle with the season-start cadence session — whichever comes first. HARD DEADLINE: before the 2026 NFL week 1→2 rollover (~2026-09-15; promotion first actually needed ~2026-09-18). Flip the item [>]→[ ] when its build session starts."
  - "BLOCKED ON EXTERNAL TIMING — nothing to do until ~mid-August 2026 (expected clear ~2026-08-19 → 2026-09-02): Wave 2 ESPN manual items (public/private per league, espn_s2/SWID for private, league IDs + season year). ESPN leagues commissioner-locked until 2026 season setup. When they open: resolve in a plain chat, flip them in MANUAL_SETUP_CHECKLIST.md, flip 02_data_pipeline.md's 9 ESPN items + 03c's [!]→[ ]. Per the backsweep practice (BUILD_PROTOCOL Amendment Hygiene) that plain chat must sweep the confirmations against already-registered 02/03c text AND against shipped Wave 4 surfaces (platform-agnostic), 3b's write path (recordManualPick platform-agnostic; 03c auction extends via the amount field on the same handleDraft seam), the draft_sessions/poll orchestration (skipped_non_sleeper by design; 03c ESPN poll extends runRecordedDraftPoll), the client-side live sync seams (listDraftPicks/merge/optimistic overlay platform-agnostic via crosswalk), the DraftOrderMeta on-clock path (slot_to_roster_id is Sleeper-specific; ESPN needs its own draft-order source), the projections/BPA/tier-cliff layer AS SHIPPED (player_projections source-checked 'sleeper'-only today; ESPN leagues' BPA + tiers + the tier counter use the same Sleeper-sourced projections automatically — keyed by sleeper_player_id + league_config), AND the run-detection layer + its badge AS SHIPPED (runs.ts + PositionalRunBadges are platform-agnostic — they read pick_number + catalog position off draft_state regardless of source; ESPN picks flow through the same listDraftPicks → RecordedPick → RunPick mapping, so runs and the badge surface for ESPN leagues automatically once their picks land; sweep any item text that assumed otherwise)."
  - "SEASON START (~Sept 2026): revisit league-sync cadence — the specced 15-minute cron needs Vercel Pro (~$20/mo) or an alternative trigger; currently daily on Hobby per your 2026-07-22 decision. Flip 02_data_pipeline.md's cron-schedule item [~]→[x] when sub-daily ships. If Pro is bought, /api/cron/sync-adp is the ready mount point for a dedicated ADP+projections slot. NOTE: live-draft polling + the BPA panel's per-pick recompute + tier recompute + the tier counter + run detection + the run badge do NOT depend on this — all client-driven, no cron slot at any tier; they read Supabase only (run detection is pure over an already-fetched snapshot, no Supabase read of its own)."
  - "BYE-WEEK [>] ITEM (03a) still awaits its schedule-source decision (plain chat: pick a schedule source, then a build session ships table+ingestion+row wiring — flips 03a 🟢). No longer blocking anything, the only remainder between 03a and 🟢. Related small observation: the 3a board pool's fullName is also null for DST rows (renders the abbreviation fallback); the item-4 name compose exists in draft-picks.ts if a future correctness pass wants to reuse it for getDraftBoardData."
  - "When your 2026 Sleeper league renews (new league_id every season): run npm run sync:league -- <new_id> once to connect it; every daily league-state cron run (and npm run sync:sleeper) covers it after. The 2025 league (1257464256710856704) is fully ingested incl. its 170-pick draft. With auto-land live, the renewed league (season 2026 > 2025) becomes the DASHBOARD you land on after sign-in (newest season sorts first). Draft-day note: the renewed league is where Start draft + live polling + click-to-draft + the live strip/status/roster/recent-picks feed + the BPA panel + the tier counter + the run badge run in anger. The BPA + tier + run layer uses the LATEST ingested projections/ADP season (2026) regardless of the league's own season, so it is already current-market."
  - "Before Wave 5's waiver/FAAB recommendations item is built: run a dedicated scoping session to define the actual waiver/FAAB candidate scoring formula (opportunity-based — snap/target/touch share trends — is the recommended direction per wiki/topics/league-mechanics/waiver-wire-faab-strategy.md, but no formula exists yet)."

blockers:
  - "EXTERNAL TIMING (dated 2026-07-22): 02_data_pipeline.md's two ESPN sub-sections (9 items, [!]) are blocked until ESPN leagues open for the 2026 season — commissioner-locked, expected ~2026-08-19 → 2026-09-02. Not resolvable by Nick or any build session before then; do NOT guess visibility or fabricate placeholder league IDs (Nick's explicit instruction). Downstream: Wave 3c inherits this blocker (ESPN draft mechanics + auction). Does not stall the roadmap: 3b (queue/auto-pick next, then resilience) and Wave 4 both have buildable [ ] items."

known_issues:
  - "npm audit reports 6 vulns (4 moderate, 2 high) — last re-checked live 2026-07-22 (this session added no dependencies): pre-existing 1 moderate postcss + 2 high sharp are transitive inside next@16.2.11's pinned deps (only offered 'fix' is downgrading next to 9.x, which is wrong). All dev-time/build-time, none shipped to clients. Re-check in later sessions."
  - "FINALITY-PROMOTION GAP (surfaced 2026-07-22 by the cron session; investigated + designed + registered 2026-07-22 — still UNBUILT, mechanism unchanged until its build session): in-season, matchups/player_scores rows get is_final=true only when league lifecycle status reads 'complete' at sync time; the scheduled sync is current-week-only, so finished weeks stay is_final=false all season. Covering wiki: sleeper-api/matchup-endpoint (stat-correction window + finality Key Decision #3). Nick-signed design (2026-07-22): previous-week re-sync + promotion inside the daily league-state cron (week-advance = games-complete proxy; Friday-or-later UTC stable-read match = trigger — both Rule 12 genuine-silence sign-offs) + league-complete backstop sweep; needs a caller-driven finality extension to syncLeagueMatchups. Registered as the [>] item in 02_data_pipeline.md's cron sub-section. Zero live consequence until the season's first week rollover; HARD DEADLINE ~2026-09-15/18 (build scheduling in nick_pending). WAVE 4 STATUS: UI surfacing side SHIPPED and MOUNTED (UnofficialChip + AsOfTime render is_final/fetched_at honestly)."

warnings:
  - "ABSOLUTE RULE 13 (canonical: MASTER_CONTEXT.md 'Shared Database Protection — Non-Fantasy Data Is Untouchable'): the Supabase database is the shared multi-app 'prolabel' project — ~49 non-fantasy tables incl. live business production data (Elliott). Never supabase db reset; never migration repair touching foreign history (new foreign versions get empty local stub files, never repair); no raw SQL that could reach a non-fantasy table — verify every migration's table names against ARCHITECTURE.md's 'Database Schema (live)' inventory; collision check before any new table name; ANY ambiguity → stop and ask Nick; mandatory BLAST RADIUS: line on every completion report. Owner-write RLS policies must match Nick's specific auth.uid() via public.is_fantasy_admin(), never the blanket authenticated role. THIS SESSION (positional-run item 3, the badge): READ-ONLY — no migration, NO DB access at all; blast radius = client UI wiring + one temp pure-render harness (scripts/verify-run-badge.tsx, deleted; react-dom/server, no DB/network). WAVE 4 SPECIFIC: the share-token RLS sub-section adds SELECT policies on six fantasy tables — a named singleton fold (Folding Policy exception #5) with its boundary tests; owner policies stay pinned to is_fantasy_admin(). draft_sessions/draft_state are NEVER on any spectator-policy list, at any wave — and player_projections/adp_rankings have no spectator consumer either (the whole BPA + tier-cliff + run-detection layer, incl. the run badge, is admin-only)."
  - "CLIENT-SIDE LIVE SYNC SEAMS (updated 2026-07-23): the live board path is ticker → pollActiveDraft (returns full listDraftPicks snapshot per executed poll) → shell's fingerprint-deduped livePicks state (+ optimistic pendingPicks overlay + pollHealth aggregation + DraftOrderMeta state + panelTiers state) → live-picks.ts mergePicksIntoPlayers → BOTH board regions; the roster panel and recent-picks feed additionally read livePicks DIRECTLY for pick→team attribution; the BPA recommendations panel reads livePicks as a useEffect DEPENDENCY (refetch on reference change — recomputes VORP AND tiers by construction) AND now reports its context.tiers UP via an onTiers callback → shell panelTiers. THE POSITIONAL-RUN BADGE (item 3, shipped 2026-07-23) is the newest livePicks consumer: the shell computes detectPositionalRuns in a useMemo over livePicks (RecordedPick→RunPick via playerPosition, CONFIRMED picks only) — a NEW read of the EXISTING snapshot, NOT a new poll driver / merge / pick-event feed / tier compute; it is THE one run-detection source (was zero before). The run board + panelTiers thread down through PlayerBoard→BoardToolbar→PositionalRunBadges. Run detection is deliberately decoupled from the value query (item 4) — NOT routed through fetchBpaRecommendations. Extend THIS path for the remaining 03b items (queue/auto-pick) — never a second poll driver, never a second merge, never a second pick-write UI path (handleDraft via the DraftablePlayer seam — auto-pick reuses recordManualPick, NOT a parallel submit), never a second tick-outcome reporter, never a second order-metadata source, never a second pick-identity source, never a second undo UI path, never a second tier compute/board data source, never a second run-detection source. Key invariants: 'drafted' availability AND draftPending are produced ONLY by the client merge; a confirmed snapshot row ALWAYS wins over pending; 'rostered' wins over the whole overlay; out-of-pool picks are skipped by the MERGE but surfaced by the FEED and counted by the roster panel; ON-CLOCK IS A PROJECTION (projectOnClock is DISPLAY ONLY). rosterLabel (player-row.tsx, exported) is THE roster display convention on the draft surface."
  - "BPA + TIER-CLIFF + RUN-DETECTION LAYER CENTRALIZATION (updated 2026-07-23): src/services/bpa/ is the ONE home of projection scoring + VORP math + tier detection + run detection + the recommendations query; src/components/draft-board/bpa-recommendations-panel.tsx is its ONE recommendations/tier UI surface (TierDepthStrip is a SUB-COMPONENT there); src/components/draft-board/positional-run-badges.tsx is the ONE run-badge UI surface. PURE sub-layer (no I/O): scoring.ts; replacement.ts computeReplacementLevels (+ EXPORTS FLEX_ELIGIBILITY); calibration.ts; base-value.ts (VORP); board.ts computeBpaBoard (THE composition scoring→replacement→calibration→base-value→TIERS; pure, NO cache; recompute = re-call against the shrinking pool); need.ts (SEPARATE roster-need signal; NEVER a value input); tiers.ts (per-position gap-based tiers; summarizeTiers → per-position {tierCount, topTierSize, topTierTopProjection}; a PARALLEL DISPLAY signal, NEVER an input to base value or ordering); runs.ts detectPositionalRuns (POSITIONAL-RUN detection — pure, ZERO imports, a THIRD parallel display signal alongside need + tiers; count-based sliding window over the last 6 picks by pick_number, RUN_COUNT_THRESHOLD {QB 2/TE 2/RB 3/WR 3}, map-presence IS the run-detectable set so K/DEF/other never flag; consumes minimal RunPick {pickNumber, position}; returns {…, byPosition, activeRuns}; NEVER feeds VORP/ordering). I/O sub-layer: recommendations.ts getBpaRecommendations (the ONE BPA query) → fetchBpaRecommendations action (draft/actions.ts, 6th action). UI wiring: the BPA panel CONSUMES fetchBpaRecommendations only; TierDepthStrip renders context.tiers[pos].topTierSize; the panel ALSO reports context.tiers up via onTiers. RUN DETECTION IS NOT ROUTED THROUGH THE QUERY — the shell computes runs client-side on livePicks and the badge pairs them with the lifted panelTiers (the SAME summarizeTiers result — no second tier compute). NEVER add caching, a second scoring/replacement/VORP/board/TIER/RUN derivation, a second projections/ADP read path, a second recommendations query, a second draft-submit path, or a second run-detection source anywhere downstream; need, tiers, AND runs stay SEPARATE parallel signals, never multiplied into base value. Layout parsing stays parseRosterSlotLayout and format mapping stays resolveAdpScoringFormat (both draft-board.ts). Projection/ADP season = latest ingested for the source (current-market), independent of the league's own season."
  - "MANUAL-PICK WRITE PATH CENTRALIZATION (live 2026-07-22): src/services/draft-picks.ts recordManualPick is the ONE local pick-write path — 03b's auto-pick MUST write through it (build-file mandate), no second draft_state insert path outside it and the platform pollers. Its UI callers wire through the shell's handleDraft (optimistic path) — the main player row AND the BPA panel both call handleDraft, which accepts the minimal DraftablePlayer shape ({sleeperPlayerId, fullName} in services/draft-board.ts); future draft triggers (auto-pick, 03c auction bid) do the same, never a parallel submit. Server-action auth pattern (6 actions incl. read-only fetchBpaRecommendations): the gate lives INSIDE each action (getAdminAuthState → typed unauthorized/reason) + RLS as the second wall; owner actions use the RLS server client, never the secret-key client. draft_state rows immutable-once-written — conflict responses return the authoritative row, adopted by the shell. Undo has its UI (the recent-picks feed's single-row Undo, session-active only) — extend that surfacing, never a second undo control. listDraftPicks (draft-picks.ts) is THE ordered pick-snapshot read (players-embed joined; leagueId = platform_league_uuid, NOT the native id) — the run-detection layer's RunPick input maps off it (via the shell's livePicks, which is seeded by listDraftPicks and refreshed by the poll), never a second pick read."
  - "PLAYER_SCORES IS INGESTED, NEVER COMPUTED (Nick-signed canon): platform-scored values land as-received (Sleeper players_points — league-scored by Sleeper per each league's settings; ESPN as-received when unblocked). There is NO compute-from-raw-stats path for ACTUAL scores in v1. PROJECTIONS ARE THE SANCTIONED EXCEPTION (Nick's 2026-07-22 decision): player_projections stores component projection stats, and the BPA engine scores them with league_config weights at read time (src/services/bpa/scoring.ts) — projected value, never a substitute for actual player_scores; preset pts_* buckets never used as league values. Tier detection (tiers.ts) reads those same league-scored projections (via base-value output); run detection (runs.ts) reads NO projections at all — only draft_state pick order + catalog position. Never invent score rows for unrostered weeks (FA-week gap accepted for v1)."
  - "DASHBOARD QUERY SERVICE CENTRALIZATION (live 2026-07-22): src/services/dashboard.ts is the ONE home of the standings ordering (fetchRankedStandings, wins desc → points_for desc → native_roster_id asc, Nick-signed under declared wiki silence) and the power-ranking formula (All-Play record; regular-season weeks via raw playoff_week_start; ties=half-wins; lowConfidence <6 weeks); listScoredWeeks is the ONE week-range/default-week source. The Wave 4 display components render these AS-IS. The spectator loader passes its own share_token-scoped client (shared data-access is sanctioned; shared UI never is). Boundary: the module never selects share_token/owner_id/native_league_id and has zero draft_state references — keep it that way."
  - "ADMIN/SPECTATOR UI BOUNDARY (Wave 4, live 2026-07-22): src/components/dashboard/ is ADMIN-surface UI — the spectator surface imports ZERO components from it (or from draft-board/ or ui/dropdown-menu's draft usages); spectator gets its own components (04's spectator sub-section). The BPA panel, the tier counter, AND the run badge are likewise ADMIN-only — no spectator consumer, ever. Shared data-access (services/dashboard.ts) is the ONLY sanctioned sharing. Freshness primitives UnofficialChip + AsOfTime (components/dashboard/) are THE rendering of is_final/fetched_at. The player-card open mechanism is the URL-driven ?player= sheet — future admin surfaces reuse this pattern."
  - "DARK MODE ONLY IS LOAD-BEARING IN CODE (live 2026-07-22): globals.css holds the app's single always-on token set in :root — NO light block, NO .dark override, NO theme toggle; the `dark` class on <html> is a permanent marker keeping vendored shadcn dark:-variant tunings active. Never add a light token set, a prefers-color-scheme branch, or remove that class. ui/sonner.tsx hardcodes theme='dark'. --radius stays 0.625rem (Nick-signed). Position identity tokens --pos-qb/rb/wr/te/k/def are badges/chips ONLY — never row tints; teal = interactive/live only. BPA need badges: muted-tier chips, 'starter' borrows --warning, teal untouched. The tier counter (TierDepthStrip) AND the positional-run badge (PositionalRunBadges, shipped 2026-07-23) both follow this: uniform muted, tabular where numeric, PositionBadge reused for position color, NO new token, NO scarcity/alarm color emphasis, non-blocking (a run is a weighted signal, not a 'draft now' trigger — positional-run-detection-draft-day.md)."
  - "OWNER AUTH SURFACE (live 2026-07-22) — three walls, keep all three: (1) src/proxy.ts (Next 16 proxy, NOT middleware.ts) refreshes the Supabase session per matched request and 307s unauth /leagues/* to /login — matcher EXCLUDES /api. (2) The (admin) layout gate uses getAdminAuthState (src/lib/supabase/auth.ts → is_fantasy_admin RPC) — never hardcode the admin UUID; a signed-in non-fantasy prolabel user is a REAL case and gets the zero-data NotAuthorizedCard. (3) Per-table RLS. ROOT AUTO-LAND targets the dashboard (/leagues/<id>). Server actions add a FOURTH gate: auth INSIDE each action — SIX actions (submitManualPick/undoManualPick/startDraftSession/endDraftSession/pollActiveDraft/fetchBpaRecommendations + login's signOut); the read-only fetchBpaRecommendations returns {ok:false, reason:'unauthorized'} + RLS as the second wall (live-verified: anon → league_not_found, zero data)."
  - "src/lib/supabase/database.types.ts is generated — never hand-edit; regenerate via npm run gen:types after every schema migration. It deliberately contains the whole shared public schema (foreign prolabel tables included) — expected, not drift. Trap: generated Insert types list stored generated columns (matchups.effective_points) as insertable — Postgres rejects them; sync code must omit them. Note: the leagues PK column is platform_league_uuid (NOT `id`) — listDraftPicks takes that UUID, not the native league id. getBpaRecommendations reads player_projections with a PAGINATED .range loop (1000/page) — the 3292-row season snapshot exceeds PostgREST's default single-select page cap; never rely on an unbounded select for that table (tier detection reuses that same paginated pool — no separate read; run detection reads NO Supabase at all — it's pure over the shell's already-fetched livePicks snapshot)."
  - "src/lib/supabase/admin.ts bypasses RLS (secret key) — server-only, never import from client code; its env var is deliberately not NEXT_PUBLIC_ so a client-bundle import fails loudly. The draft-board, dashboard, and BPA/tier/run routes/actions deliberately do NOT use it — owner pages read through the RLS server client as the signed-in admin. Temp verification scripts (deleted after use) may use the admin client for read-only checks (blast radius = SELECTs) — though this session's harness (scripts/verify-run-badge.tsx, deleted) hit NO DB at all, only react-dom/server rendering of the badge over synthetic inputs."
  - "Sleeper player catalog (/players/nfl): at most ONCE PER DAY — Sleeper's most load-bearing rate rule. The cron route enforces it with a hard guard on sync_runs. The manual script path (npm run sync:players) bypasses the guard — manual cadence discipline applies. Per-league endpoints remain paced by the client's global 250ms min-gate (which also absorbs 5s live-draft polling, ≤3 requests per executed tick). The BPA panel's per-pick recompute, tier recompute, the tier counter, run detection, AND the run badge make NO Sleeper calls — the BPA/tier layer reads Supabase only, and run detection reads nothing at all (pure over an already-fetched snapshot), so they add zero to the Sleeper budget."
  - "api.sleeper.com (ADP/projections surface) is NOT api.sleeper.app/v1: undocumented, no contract, can change shape or vanish — its consumer is the quarantined src/services/adp/ ONLY (own client, own pacing gate). The SAME fused ingestion run feeds BOTH adp_rankings AND player_projections from one fetch; never a second api.sleeper.com client or a separate projections fetch. 999.0 in any adp_* field is a 'no ADP in this format' sentinel — never data in adp_rankings; player_projections.stats retains the RAW object (sentinels included). ADP is never read from player_projections (calibration reads sentinel-filtered adp_rankings only; getBpaRecommendations reads ADP carriers from adp_rankings, projection component stats from player_projections — never crossed). The adp_* field inventory is OPEN — never hardcode a closed format list. Covering wiki: sleeper-api/projections-endpoint."
  - "League-format→ADP-format mapping is CENTRALIZED in resolveAdpScoringFormat (src/services/draft-board.ts, Nick-signed): superflex→'2qb' precedence, then ppr buckets; dynasty/IDP unmapped in v1; NO cross-format fallback. Any consumer calls this function — never re-derives (getBpaRecommendations + bpa/calibration.ts's callers do exactly this; the DEFERRED superflex run-sensitivity refinement will call it too). Same centralization for roster-slot layouts: parseRosterSlotLayout (same module) is the ONE parser of roster_settings_raw — bpa/replacement.ts, need.ts, getBpaRecommendations consume its output, never re-derive. FLEX_ELIGIBILITY is the ONE flex-eligibility home (exported from bpa/replacement.ts) — need.ts imports it, never re-derives. ADP-absence classification is centralized in deriveAdpNoticeKind (src/components/draft-board/adp-notice.tsx). DraftablePlayer (Pick<DraftBoardPlayer,'sleeperPlayerId'|'fullName'>, draft-board.ts) is the ONE minimal shape the shared handleDraft consumes; RunPick ({pickNumber, position}, bpa/runs.ts) is the analogous minimal shape run detection consumes off livePicks/listDraftPicks — non-table draft surfaces reuse these, never a cast or a parallel submit/read."

health: "clean"
