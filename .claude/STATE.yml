# .claude/STATE.yml
# Living "what happened last session" file. Overwritten completely at the end of every session — never patched.
# Last Updated: 2026-07-22

current_focus: "WAVE 3A COMPLETE except the bye-week [>] deferral (2026-07-22, see log _25): the 'States + resilience' sub-section (3 items, one fold — BUILD_PROTOCOL self-located session) shipped, closing 03a's last buildable items. Shipped: (1) route-segment loading.tsx skeleton mirroring the board's real density (shadcn skeleton primitive via CLI — its bg-muted matches DESIGN_SYSTEM's documented skeleton surface; CSS pulse only); (2) empty states (NICK-SIGNED x2: banner-over-rostered-list — real data never hidden; command+schedule next-action copy): adp-notice.tsx with pure deriveAdpNoticeKind — three honest variants in healing-order precedence (no snapshot → sync:adp; format unresolved → sync:league; format zero rows → explicit empty state per the format-leakage rule, never another format's numbers), full-region BoardEmptyState for a truly empty pool (banner suppressed, no double-messaging), and the designed zero-filter-match state replacing player-table's inline fallback ('Clear filters' resets filters, never sort); (3) route-segment error.tsx (NICK-SIGNED x2: per-surface degradation posture — no aggregate banner; retry + back-to-leagues fallback): generic copy, raw errors never rendered (digest = log-correlation ref only), reset() + / escape link so a persistently failing league can't dead-end navigation. ESPN-gap degradation verified STRUCTURAL: data gaps degrade to nulls + per-surface notices (harness-proved no-throw on null layout/config paths), only real failures reach the boundary, zero platform branching. VERIFIED: lint/tsc/production build clean; 14/14 functional checks (5 deriveAdpNoticeKind branches incl. precedence, null-path no-throws, live: healthy league raises NO false notice, boundary fields absent, 404 path intact). NOT eyeballed: all authed surfaces (same Rule 13 posture — no session minted). SELF-LOCATION NOTE FOR NEXT SESSION: no [ ] item exists in 01/02/03a — next in roadmap order is 03b (ESPN-blocked). Nothing is self-locatable until the ESPN block clears (~mid-Aug) OR Nick rules on starting Wave 4 ahead of 3b OR the bye-week schedule-source decision lands (see nick_pending). Carried timing notes: ADP piggyback first fires with the 2026-07-23 10:00 UTC sync-leagues run (check sync_runs source='adp_ingestion' + adpOk); catalog cron 2026-07-23 09:00 UTC correctly records 'skipped'; first real scheduled catalog fetch ~2026-07-24 09:00 UTC."

build_system:
  index: ".claude/BUILD_INDEX.md"
  files_location: ".claude/build/"
  total_files: 7
  current_build_files:
    - ".claude/build/03a_draft_assistant_static_board.md"  # 🟡 — all five sub-sections complete; sole remainder = bye-week [>] (schedule-source-gated); goes 🟢 when that ships or is re-ruled
    - ".claude/build/02_data_pipeline.md"  # ESPN residue [!], cron cadence [~], finality promotion [>] (Nick-signed deferral) — nothing self-locatable here

nick_pending:
  - "QUICK VISUAL CHECK on your next sign-in (updated 2026-07-22 by the states/resilience fold): everything from the player-list fold's check still applies (rows, position badge colors, injury chips, filter chips, sort headers, roster/need sidebar) PLUS the new states: the loading skeleton flashes on first board load (should mirror the table layout, no reflow jump when data lands); filter to gibberish (search 'zzz') to see the zero-match empty state + Clear filters button. The ADP banner and error fallback should NOT appear on the healthy league — if a warning banner shows on your real league, that's a bug, flag it. All token/build/logic-verified only — authed surfaces sit behind your auth wall (Rule 13, shared auth namespace — no session minted)."
  - "WAVE-ORDER DECISION NEEDED before the next build session can self-locate (new 2026-07-22, states/resilience fold): 01/02/03a now have zero [ ] items. Next in roadmap order is 03b — ESPN-blocked until ~mid-Aug. Options, your call in a plain chat: (a) authorize Wave 4 (league dashboard + spectator surface) starting ahead of 3b — BUILD_INDEX's dependency notes gate 3b on ESPN but are silent on 4-before-3b (the 2026-07-22_16 session deliberately left this boundary decision to you); (b) resolve the bye-week [>] item's schedule-source decision (plain chat: pick a schedule source, then a build session ships table+ingestion+row wiring — also flips 03a 🟢); (c) hold builds until the ESPN block clears. (a) and (b) are both buildable now; (c) idles the roadmap ~4 weeks."
  - "FINALITY PROMOTION — build scheduling (design Nick-signed + registered 2026-07-22 as the [>] item in 02_data_pipeline.md's cron sub-section; see known_issues for the mechanism): build as its own standalone fold, OR bundle with the season-start cadence session below — whichever comes first. HARD DEADLINE: before the 2026 NFL week 1→2 rollover (~2026-09-15; promotion first actually needed ~2026-09-18). Flip the item [>]→[ ] when its build session starts."
  - "BLOCKED ON EXTERNAL TIMING — nothing to do until ~mid-August 2026 (recorded 2026-07-22; expected clear ~2026-08-19 → 2026-09-02): Wave 2 ESPN manual items (public/private per league, espn_s2/SWID for private leagues, league IDs + season year). ESPN leagues are commissioner-locked until 2026 season setup. When they open: resolve the three items in a plain chat, flip them in MANUAL_SETUP_CHECKLIST.md, and flip 02_data_pipeline.md's 9 ESPN items [!]→[ ] so build sessions self-locate onto them again. Per the backsweep practice (BUILD_PROTOCOL Amendment Hygiene), that plain chat must also sweep the confirmations against already-registered 02/03b item text for staleness."
  - "SEASON START (~Sept 2026): revisit league-sync cadence — the specced 15-minute cron needs Vercel Pro (~$20/mo) or an alternative trigger; currently daily on Hobby per your 2026-07-22 decision (MANUAL_SETUP decision of record). Flipping 02_data_pipeline.md's cron-schedule item [~]→[x] happens when the sub-daily cadence ships. If Pro is bought, /api/cron/sync-adp is the ready mount point for a dedicated ADP schedule slot (currently piggybacked on sync-leagues). This session is also the fallback vehicle for the finality-promotion build (entry above) if it hasn't shipped as its own fold by then."
  - "When your 2026 Sleeper league renews (new league_id every season): run npm run sync:league -- <new_id> once to connect it; every daily league-state cron run (and npm run sync:sleeper) covers it from then on. The 2025 league (1257464256710856704) is fully ingested including its 170-pick draft. Note: with auto-land live, the renewed league (season 2026 > 2025) becomes the board you land on after sign-in — expected behavior, newest season sorts first."
  - "Before Wave 5's waiver/FAAB recommendations item is built: run a dedicated scoping session to define the actual waiver/FAAB candidate scoring formula (opportunity-based — snap/target/touch share trends — is the recommended direction per wiki/topics/league-mechanics/waiver-wire-faab-strategy.md, but no formula exists yet)."

blockers:
  - "EXTERNAL TIMING (dated 2026-07-22): 02_data_pipeline.md's two ESPN sub-sections (9 items, [!]) are blocked until ESPN leagues open for the 2026 season — commissioner-locked, expected ~2026-08-19 → 2026-09-02. Not resolvable by Nick or any build session before then; do NOT guess visibility or fabricate placeholder league IDs (Nick's explicit instruction). Downstream: Wave 3b inherits this blocker (needs Wave 2 ESPN). Wave 3a is COMPLETE except its schedule-source-gated bye-week [>] — with 03b next in roadmap order and blocked, no build session can self-locate until the wave-order decision in nick_pending resolves."

known_issues:
  - "npm audit reports 6 vulns (4 moderate, 2 high) — last checked live 2026-07-22 (tsx install session): pre-existing 1 moderate postcss + 2 high sharp are transitive inside next@16.2.11's pinned deps (only offered 'fix' is downgrading next to 9.x, which is wrong). All dev-time/build-time, none shipped to clients. No new deps since (states/resilience fold touched zero packages — the shadcn skeleton add generated a file only). Re-check in later sessions."
  - "FINALITY-PROMOTION GAP (surfaced 2026-07-22 by the cron session; investigated + designed + registered 2026-07-22 non-code session — still UNBUILT, mechanism unchanged until its build session): in-season, matchups/player_scores rows get is_final=true only when league lifecycle status reads 'complete' at sync time; the scheduled sync is current-week-only, so finished weeks stay is_final=false all season. Covering wiki: sleeper-api/matchup-endpoint (stat-correction window + finality Key Decision #3) — the policy was already decided; only the mechanism was missing. Nick-signed design (2026-07-22): previous-week re-sync + promotion inside the daily league-state cron (week-advance = games-complete proxy; Friday-or-later UTC stable-read match = trigger — both Rule 12 genuine-silence sign-offs) + league-complete backstop sweep; needs a caller-driven finality extension to syncLeagueMatchups. Registered as the [>] item in 02_data_pipeline.md's cron sub-section. Zero live consequence until the season's first week rollover; HARD DEADLINE ~2026-09-15/18 (build scheduling in nick_pending)."

warnings:
  - "ABSOLUTE RULE 13 (canonical: MASTER_CONTEXT.md 'Shared Database Protection — Non-Fantasy Data Is Untouchable'): the Supabase database is the shared multi-app 'prolabel' project — ~49 non-fantasy tables incl. live business production data (Elliott). Never supabase db reset; never migration repair touching foreign history (new foreign versions get empty local stub files, never repair); no raw SQL that could reach a non-fantasy table — verify every migration's table names against ARCHITECTURE.md's 'Database Schema (live)' inventory; collision check before any new table name; ANY ambiguity → stop and ask Nick; mandatory BLAST RADIUS: line on every completion report. Owner-write RLS policies must match Nick's specific auth.uid(), never the blanket authenticated role — realized as public.is_fantasy_admin(); rotate the admin there, never by editing per-table policies. Operational restatement: ARCHITECTURE.md 'Shared-Database Constraints'."
  - "DARK MODE ONLY IS LOAD-BEARING IN CODE (live 2026-07-22): globals.css holds the app's single always-on token set in :root — there is deliberately NO light block, NO .dark override block, NO theme toggle, and the `dark` class on <html> (layout.tsx) is a permanent marker that keeps vendored shadcn dark:-variant tunings active. Never add a light token set, a prefers-color-scheme branch, or remove that class ('simplifying' it away silently reverts every dark: tuning in the vendored primitives). New shadcn primitives added via CLI will carry dark: classes — they work as-is under the permanent marker; do not strip them. --radius stays 0.625rem (Nick-signed): containers use the rounded-xl step (14px), buttons/chips are pills (rounded-full — button.tsx base class, edited deliberately). input.tsx's fill is bg-well by design (inputs are wells darker than background), not a drift from shadcn upstream. Position identity tokens --pos-qb/rb/wr/te/k/def (2026-07-22) are badges/chips ONLY — never row tints; RB is deliberately green, not the brand teal (teal = interactive/live only)."
  - "OWNER AUTH SURFACE (live 2026-07-22) — three walls, keep all three: (1) src/proxy.ts (Next 16 proxy, NOT middleware.ts) refreshes the Supabase session per matched request and 307s unauth /leagues/* to /login — its matcher deliberately EXCLUDES /api so the cron Bearer surface never sees cookie plumbing; do not 'simplify' the matcher to include it. (2) The (admin) layout gate uses getAdminAuthState (src/lib/supabase/auth.ts → is_fantasy_admin RPC) — never hardcode the admin UUID in app code; a signed-in non-fantasy prolabel user is a REAL case (shared auth namespace) and must get the zero-data NotAuthorizedCard. (3) Per-table RLS. Unauth/non-admin responses must never contain league data — the /login page and root router render zero league fields. NEW (states fold): the draft route's error.tsx renders NO league data either — generic copy + digest ref only."
  - "src/lib/supabase/database.types.ts is generated — never hand-edit; regenerate via npm run gen:types after every schema migration. It deliberately contains the whole shared public schema (foreign prolabel tables included) — expected, not drift. Trap: generated Insert types list stored generated columns (matchups.effective_points) as insertable — Postgres rejects them; sync code must omit them."
  - "src/lib/supabase/admin.ts bypasses RLS (secret key) — server-only, never import from client code; its env var is deliberately not NEXT_PUBLIC_ so a client-bundle import fails loudly instead of leaking. The draft-board route deliberately does NOT use it — owner pages read through the RLS server client (server.ts) as the signed-in admin."
  - "Sleeper player catalog (/players/nfl): at most ONCE PER DAY — Sleeper's most load-bearing rate rule. The cron route (/api/cron/sync-players) enforces this with a hard guard on sync_runs (running/success within 24h → skip; failure rows allow a genuine retry; guard read error → fail closed, no fetch). BUT the manual script path (npm run sync:players) calls the service directly and BYPASSES the guard — manual cadence discipline still applies there. The orchestrator still deliberately EXCLUDES the catalog. Per-league endpoints remain paced by the client's global 250ms min-gap gate."
  - "api.sleeper.com (ADP/projections surface) is NOT api.sleeper.app/v1: undocumented, no contract, can change shape or vanish without notice — its consumer is the quarantined src/services/adp/ ONLY (own client, own pacing gate; documented-host rate knowledge does NOT transfer; never parameterize the documented client to reach it). Ingestion is validate-before-swap: a failed/implausible fetch never overwrites the last good adp_rankings snapshot. 999.0 in any adp_* field is a 'no ADP in this format' sentinel — never data (schema check backstops it). The adp_* field inventory is OPEN (10 formats carried real values live 2026-07-22; adp_rookie and bare adp_dynasty were all-sentinel) — ingestion is pattern-based; never hardcode a closed format list anywhere downstream. Covering wiki: sleeper-api/projections-endpoint."
  - "League-format→ADP-format mapping is CENTRALIZED in resolveAdpScoringFormat (src/services/draft-board.ts, Nick-signed 2026-07-22): superflex→'2qb' precedence, then ppr buckets; dynasty/IDP unmapped in v1; NO cross-format fallback anywhere (average-draft-position format-leakage rule). Any future consumer needing a league's ADP format calls this function — never re-derives its own mapping. Same centralization rule for roster-slot layouts: parseRosterSlotLayout (same module, 2026-07-22) is the ONE parser of roster_settings_raw — Wave 3b BPA reuses it, never re-derives. NEW (states fold): ADP-absence classification is likewise centralized in deriveAdpNoticeKind (src/components/draft-board/adp-notice.tsx) — future surfaces reuse it, never re-derive their own no-ADP logic."

health: "clean"
