# .claude/STATE.yml
# Living "what happened last session" file. Overwritten completely at the end of every session — never patched.
# Last Updated: 2026-07-22

current_focus: "WAVE 3B — TIER-CLIFF DETECTION items 1-3 COMPLETE (2026-07-22, feature-build; fold = one independently verifiable artifact 'the tier-detection layer', items 1-3, headlessly verifiable like the prior BPA pure/query sessions). READ-ONLY session — no migration, no writes, SELECTs only. THREE items, all pure/compute: item 1 = new src/services/bpa/tiers.ts computePositionTiers — pure per-position gap-based tier boundaries over the base-value ranked pool; tiers ONLY players at/above each position's operative replacement line (Nick's Clarify = down-to-replacement depth scope; full-tail tiering manufactures noise per league-size-scarcity-effects.md); a break sits where the consecutive projected-points gap exceeds TIER_GAP_MULTIPLE[pos] × the position's MEDIAN gap (Nick's Clarify = multiple-of-typical-gap not absolute per adp-ecr-differential.md; MEDIAN over mean is a live-verified robustness fix so one dominant elite-separation gap can't swallow QB/TE secondary cliffs; mean fallback when median 0; within-position projection gaps ≡ base-VORP gaps so no z-score step). Item 2 = per-position multiples (RB 3.0 / WR 3.2 / QB 2.0 / TE 1.5 / K,DEF 2.0 / default 2.5) tuned to the build-file target bands, Nick's Clarify = fixed multiple with counts as a SANITY CHECK never adaptive-to-band (wiki rejects rigid tier-count enforcement); deep positions carry the higher multiple (dense tiny-median tail) yet a SMALLER absolute point threshold = the 'tighter for RB/WR' sensitivity. Item 3 = composed into computeBpaBoard (BpaBoard gains `tiers`) off the same base-value ranking so tiers recompute against the shrinking undrafted pool on the SAME per-pick re-call as VORP (no separate cache/driver — STATE centralization); surfaced through getBpaRecommendations (each row carries within-position `tier`, context carries whole-board per-position tier summaries via summarizeTiers — count + 'N in the best available tier' — the data item 4's counter renders). NUMERIC THRESHOLDS = DECLARED WIKI SILENCE (league-size-scarcity-effects.md Key Decision explicitly refuses specific thresholds; the METHOD, position-sensitivity direction, and live-recompute mandate ARE wiki-specified) → live-tuned platform constants, disclosed at decision time. Verified: tsc/lint/build clean + 11/11 pure edge tests (empty/single/2-player-degenerate/huge-gap-split/median-robustness/determinism/multi-position) + LIVE read-only on the real league (temp scripts/verify-tiers.ts harness, deleted): full pre-draft 2026 PPR pool hits every target band (QB 3 / TE 4 [band 3-5], RB 8 / WR 9 [band 6-9]); full-pool vs real-170-drafted tier structure genuinely DIVERGES (RB 8→10 tiers, QB 3→2) = dynamic per-pick recompute proven, not a snapshot; ordering INVARIANT vs value-only (tiers never reorder VORP — parallel display signal). EDGE (documented): a position with <3 tiered players collapses to one tier (relative-gap degenerate; harmless — down-to-replacement gives real positions 10-40+ players). SELF-LOCATION FOR NEXT SESSION: 03b tier-cliff item 4 = the board-UI 'N remaining in this tier' counter per position (the highest-leverage tier piece) — a CLIENT surface consuming the already-surfaced context.tiers + per-row `tier`; likely its own fold (UI, live-behavior-adjacent — the counter rides the existing per-pick recompute, no new poll/sync driver). NEXT WIKI: league-mechanics (already entered) — league-size-scarcity-effects.md's 'surface tier breaks as a weighted input, never a rigid draft-now trigger' constrains the counter's UX (informational, non-blocking, like the run-detection philosophy). Tier-cliff 3/4 — 03b stays 🟡 (tier-cliff item 4, positional-run, queue/auto-pick, resilience remain)."

build_system:
  index: ".claude/BUILD_INDEX.md"
  files_location: ".claude/build/"
  total_files: 8
  current_build_files:
    - ".claude/build/03b_draft_assistant_live_draft.md"  # 🟡 — write path 4/4 [x], orchestration 3/3 [x], client-side live sync 5/5 [x], UI extensions 5/5 [x], projections ingestion 3/3 [x], BPA engine 9/9 [x], tier-cliff 3/4 [x] (items 1-3 complete 2026-07-22: pure per-position median-gap tier detection + target-band calibration + recompute-alongside-VORP composition/surfacing); next: tier-cliff item 4 (board-UI 'N remaining in this tier' counter — client, consumes the already-surfaced context.tiers)
    - ".claude/build/04_league_dashboard.md"  # 🟡 — data-access 5/5 [x], freshness [x], 4 display components [x], page assembly [x], league selector [x]; next fold: nav-shell sub-section (share-link panel deferred to the share-token singleton)
    - ".claude/build/03a_draft_assistant_static_board.md"  # 🟡 — all five sub-sections complete; sole remainder = bye-week [>] (schedule-source-gated); goes 🟢 when that ships or is re-ruled
    - ".claude/build/02_data_pipeline.md"  # ESPN residue [!], cron cadence [~], finality promotion [>] (Nick-signed deferral) — nothing self-locatable here

nick_pending:
  - "VISUAL CHECK BACKLOG (no NEW UI this session — tier-cliff items 1-3 were pure compute/query surfacing, nothing visible changed; the tier COUNTER is item 4, next session). Still outstanding from the prior session to eyeball on your next sign-in at /leagues/<id>/draft: the BPA 'Recommendations' section at the TOP of the right sidebar (8 candidates: position badge, name, team · ADP, right-aligned signed VORP; a 'Set my team' picker that adds need badges 'Fills starter need' amber / 'Bench depth' / 'Position full' muted ALONGSIDE the value, never reordering; Draft button per row → roster dropdown = same one-click flow as the main table). On the completed 2025 draft every badge reads 'Position full' (roster complete) and the panel shows best REMAINING players — correct, not a bug; try the renewed 2026 league during a live draft for varying badges. Also still valid to sanity-check whenever: live strip/status ('Draft live · Sleeper · synced Ns ago', pick/round/on-clock, background-tab pause/catch-up), recent-picks feed + Undo, roster cards' drafted totals, optimistic Draft fold, live-sync (~5s land, drafted-unrostered dimmed). Dashboard at /leagues/<id>: header, week pills, matchup cards, standings, power rankings, ?player= sheet (default week 18 → 'No opponent' singles; week 17 for full H2H)."
  - "OPTIONAL FUTURE — panel 'remember my team' per league: you chose re-pick-each-session (2026-07-22 Clarify), so the panel forgets your team on remount. If you later want per-league memory, that's a small future migration (a my-roster-per-league column/setting) + a tiny panel read — flag it whenever, its own small fold. Not blocking anything."
  - "FINALITY PROMOTION — build scheduling (design Nick-signed + registered 2026-07-22 as the [>] item in 02_data_pipeline.md's cron sub-section; mechanism in known_issues): build as its own standalone fold OR bundle with the season-start cadence session — whichever comes first. HARD DEADLINE: before the 2026 NFL week 1→2 rollover (~2026-09-15; promotion first actually needed ~2026-09-18). Flip the item [>]→[ ] when its build session starts."
  - "BLOCKED ON EXTERNAL TIMING — nothing to do until ~mid-August 2026 (expected clear ~2026-08-19 → 2026-09-02): Wave 2 ESPN manual items (public/private per league, espn_s2/SWID for private, league IDs + season year). ESPN leagues commissioner-locked until 2026 season setup. When they open: resolve in a plain chat, flip them in MANUAL_SETUP_CHECKLIST.md, flip 02_data_pipeline.md's 9 ESPN items + 03c's [!]→[ ]. Per the backsweep practice (BUILD_PROTOCOL Amendment Hygiene) that plain chat must sweep the confirmations against already-registered 02/03c text AND against shipped Wave 4 surfaces (platform-agnostic), 3b's write path (recordManualPick platform-agnostic; 03c auction extends via the amount field on the same handleDraft seam), the draft_sessions/poll orchestration (skipped_non_sleeper by design; 03c ESPN poll extends runRecordedDraftPoll), the client-side live sync seams (listDraftPicks/merge/optimistic overlay platform-agnostic via crosswalk), the DraftOrderMeta on-clock path (slot_to_roster_id is Sleeper-specific; ESPN needs its own draft-order source per espn-api draft-detail wiki — strip degrades to pick/round honestly meanwhile), AND the projections/BPA/tier-cliff layer AS SHIPPED (player_projections source-checked 'sleeper'-only today; ESPN leagues' BPA + tiers use the same Sleeper-sourced projections automatically — the whole src/services/bpa/ layer incl. tiers.ts + the panel are keyed by sleeper_player_id + league_config only, config load resolves any league's derived_config/weights/layout, drafted set comes from draft_state regardless of source; sweep any item text that assumed otherwise)."
  - "SEASON START (~Sept 2026): revisit league-sync cadence — the specced 15-minute cron needs Vercel Pro (~$20/mo) or an alternative trigger; currently daily on Hobby per your 2026-07-22 decision. Flip 02_data_pipeline.md's cron-schedule item [~]→[x] when sub-daily ships. If Pro is bought, /api/cron/sync-adp is the ready mount point for a dedicated ADP+projections slot. This session is also the fallback vehicle for the finality-promotion build if it hasn't shipped as its own fold by then. NOTE: live-draft polling + the BPA panel's per-pick recompute + tier recompute do NOT depend on this — all client-driven, no cron slot at any tier; they read Supabase only (no Sleeper calls)."
  - "BYE-WEEK [>] ITEM (03a) still awaits its schedule-source decision (plain chat: pick a schedule source, then a build session ships table+ingestion+row wiring — flips 03a 🟢). No longer blocking anything, the only remainder between 03a and 🟢. Related small observation: the 3a board pool's fullName is also null for DST rows (renders the abbreviation fallback); the item-4 name compose exists in draft-picks.ts if a future correctness pass wants to reuse it for getDraftBoardData."
  - "When your 2026 Sleeper league renews (new league_id every season): run npm run sync:league -- <new_id> once to connect it; every daily league-state cron run (and npm run sync:sleeper) covers it after. The 2025 league (1257464256710856704) is fully ingested incl. its 170-pick draft. With auto-land live, the renewed league (season 2026 > 2025) becomes the DASHBOARD you land on after sign-in (newest season sorts first). Draft-day note: the renewed league is where Start draft + live polling + click-to-draft + the live strip/status/roster/recent-picks feed + the BPA panel + (once item 4 ships) the tier counter run in anger. The BPA + tier layer uses the LATEST ingested projections/ADP season (2026) regardless of the league's own season, so it is already current-market."
  - "Before Wave 5's waiver/FAAB recommendations item is built: run a dedicated scoping session to define the actual waiver/FAAB candidate scoring formula (opportunity-based — snap/target/touch share trends — is the recommended direction per wiki/topics/league-mechanics/waiver-wire-faab-strategy.md, but no formula exists yet)."

blockers:
  - "EXTERNAL TIMING (dated 2026-07-22): 02_data_pipeline.md's two ESPN sub-sections (9 items, [!]) are blocked until ESPN leagues open for the 2026 season — commissioner-locked, expected ~2026-08-19 → 2026-09-02. Not resolvable by Nick or any build session before then; do NOT guess visibility or fabricate placeholder league IDs (Nick's explicit instruction). Downstream: Wave 3c inherits this blocker (ESPN draft mechanics + auction). Does not stall the roadmap: 3b (tier-cliff item 4 next, then positional-run, queue/auto-pick, resilience) and Wave 4 both have buildable [ ] items."

known_issues:
  - "npm audit reports 6 vulns (4 moderate, 2 high) — last re-checked live 2026-07-22 (this session added no dependencies): pre-existing 1 moderate postcss + 2 high sharp are transitive inside next@16.2.11's pinned deps (only offered 'fix' is downgrading next to 9.x, which is wrong). All dev-time/build-time, none shipped to clients. Re-check in later sessions."
  - "FINALITY-PROMOTION GAP (surfaced 2026-07-22 by the cron session; investigated + designed + registered 2026-07-22 — still UNBUILT, mechanism unchanged until its build session): in-season, matchups/player_scores rows get is_final=true only when league lifecycle status reads 'complete' at sync time; the scheduled sync is current-week-only, so finished weeks stay is_final=false all season. Covering wiki: sleeper-api/matchup-endpoint (stat-correction window + finality Key Decision #3). Nick-signed design (2026-07-22): previous-week re-sync + promotion inside the daily league-state cron (week-advance = games-complete proxy; Friday-or-later UTC stable-read match = trigger — both Rule 12 genuine-silence sign-offs) + league-complete backstop sweep; needs a caller-driven finality extension to syncLeagueMatchups. Registered as the [>] item in 02_data_pipeline.md's cron sub-section. Zero live consequence until the season's first week rollover; HARD DEADLINE ~2026-09-15/18 (build scheduling in nick_pending). WAVE 4 STATUS: UI surfacing side SHIPPED and MOUNTED (UnofficialChip + AsOfTime render is_final/fetched_at honestly) — in-season most rows honestly read 'unofficial' until promotion ships; correct display, not a bug."

warnings:
  - "ABSOLUTE RULE 13 (canonical: MASTER_CONTEXT.md 'Shared Database Protection — Non-Fantasy Data Is Untouchable'): the Supabase database is the shared multi-app 'prolabel' project — ~49 non-fantasy tables incl. live business production data (Elliott). Never supabase db reset; never migration repair touching foreign history (new foreign versions get empty local stub files, never repair); no raw SQL that could reach a non-fantasy table — verify every migration's table names against ARCHITECTURE.md's 'Database Schema (live)' inventory; collision check before any new table name; ANY ambiguity → stop and ask Nick; mandatory BLAST RADIUS: line on every completion report. Owner-write RLS policies must match Nick's specific auth.uid() via public.is_fantasy_admin(), never the blanket authenticated role — rotate the admin there, never per-table. Operational restatement: ARCHITECTURE.md 'Shared-Database Constraints'. THIS SESSION (tier-cliff items 1-3): READ-ONLY — no migration, no writes; blast radius = SELECTs only (getBpaRecommendations reads + the deleted verify-tiers.ts harness). WAVE 4 SPECIFIC: the share-token RLS sub-section adds SELECT policies on six fantasy tables — a named singleton fold (Folding Policy exception #5) with its boundary tests; owner policies stay pinned to is_fantasy_admin(). draft_sessions/draft_state are NEVER on any spectator-policy list, at any wave — and player_projections/adp_rankings have no spectator consumer either (the whole BPA + tier-cliff layer is admin-only). Related standing decline (2026-07-22 live-sync Clarify): Supabase Realtime was declined partly because it would touch the shared project's realtime publication — interval re-fetch needs no shared-infra config."
  - "CLIENT-SIDE LIVE SYNC SEAMS (updated 2026-07-22): the live board path is ticker → pollActiveDraft (returns full listDraftPicks snapshot per executed poll) → shell's fingerprint-deduped livePicks state (+ optimistic pendingPicks overlay + pollHealth aggregation + DraftOrderMeta state) → live-picks.ts mergePicksIntoPlayers → BOTH board regions; the roster panel and recent-picks feed additionally read livePicks DIRECTLY for pick→team attribution; the BPA recommendations panel reads livePicks as a useEffect DEPENDENCY only — a reference change triggers its fetchBpaRecommendations refetch (which now ALSO recomputes tiers by construction, no extra call), never a second poll driver/merge/pick-event feed. Extend THIS path for remaining 03b items — never a second poll driver, never a second merge, never a second pick-write UI path (the panel's one-click draft reuses handleDraft via the DraftablePlayer seam — NOT a parallel submit), never a second tick-outcome reporter, never a second order-metadata source, never a second pick-identity source (every RecordedPick carries catalog-joined playerFullName/playerPosition via PICK_SELECT's players embed — DST names composed from split name fields per sleeper-api/dst-and-free-agents), never a second undo UI path. When tier-cliff item 4 (the board counter) lands it CONSUMES the already-surfaced context.tiers/per-row tier from fetchBpaRecommendations — NOT a second tier compute or a second board data source. Key invariants: 'drafted' availability AND draftPending are produced ONLY by the client merge; a confirmed snapshot row ALWAYS wins over pending; 'rostered' wins over the whole overlay; out-of-pool picks are skipped by the MERGE but surfaced by the FEED and counted by the roster panel; ON-CLOCK IS A PROJECTION (projectOnClock is DISPLAY ONLY). rosterLabel (player-row.tsx, exported) is THE roster display convention on the draft surface."
  - "BPA + TIER-CLIFF LAYER CENTRALIZATION (live 2026-07-22): src/services/bpa/ is the ONE home of projection scoring + VORP math + tier detection + the recommendations query; src/components/draft-board/bpa-recommendations-panel.tsx is its ONE UI surface today (the tier-counter UI, item 4, will be the second — consuming the same query, never a new derivation). PURE sub-layer (no I/O): scoring.ts parseScoringWeights/scoreProjectionStats (THE projection-scoring application; adp_*/pts_* structurally excluded); replacement.ts computeReplacementLevels (greedy fill, flex pooled, startable-only) + EXPORTS FLEX_ELIGIBILITY as the single flex-eligibility home; calibration.ts calibrateReplacementLevels (ADP-window depth from sentinel-filtered adp_rankings ONLY); base-value.ts computeBaseValues (VORP; negatives kept; unrankable positions excluded+surfaced); board.ts computeBpaBoard (THE composition scoring→replacement→calibration→base-value→TIERS; pure, NO cache; recompute = re-call against the shrinking pool); need.ts computeRosterNeed/classifyPositionNeed (SEPARATE roster-need signal, generic bench, starter/bench/full; NEVER a value input); tiers.ts computePositionTiers/summarizeTiers (NEW 2026-07-22 — per-position gap-based tiers over the base-value ranking; down-to-replacement scope; break = gap > TIER_GAP_MULTIPLE[pos] × MEDIAN gap [mean fallback]; a PARALLEL DISPLAY signal like need, NEVER an input to base value or ordering; live-tuned multiples under declared wiki silence). I/O sub-layer: recommendations.ts getBpaRecommendations (the ONE BPA query) → fetchBpaRecommendations action (draft/actions.ts, 6th action) — each row now carries `tier`, context carries `tiers` (whole-board per-position summaries via summarizeTiers). UI: the panel CONSUMES fetchBpaRecommendations only. NEVER add caching, a second scoring/replacement/VORP/board/TIER derivation, a second projections/ADP read path, a second recommendations query, or a second draft-submit path anywhere downstream; need AND tiers stay SEPARATE parallel signals, never multiplied into base value. Layout parsing stays parseRosterSlotLayout and format mapping stays resolveAdpScoringFormat (both draft-board.ts) — bpa/ imports them, never re-derives. Projection/ADP season = latest ingested for the source (current-market), independent of the league's own season."
  - "MANUAL-PICK WRITE PATH CENTRALIZATION (live 2026-07-22): src/services/draft-picks.ts recordManualPick is the ONE local pick-write path — 03b's auto-pick MUST write through it (build-file mandate), no second draft_state insert path outside it and the platform pollers. Its UI callers wire through the shell's handleDraft (optimistic path) — the main player row AND the BPA panel both call handleDraft, which accepts the minimal DraftablePlayer shape ({sleeperPlayerId, fullName} in services/draft-board.ts); future draft triggers (auto-pick, 03c auction bid) do the same, never a parallel submit. Server-action auth pattern (6 actions incl. read-only fetchBpaRecommendations): the gate lives INSIDE each action (getAdminAuthState → typed unauthorized/reason) + RLS as the second wall; owner actions use the RLS server client, never the secret-key client. draft_state rows immutable-once-written — conflict responses return the authoritative row, adopted by the shell. Undo has its UI (the recent-picks feed's single-row Undo, session-active only) — extend that surfacing, never a second undo control."
  - "PLAYER_SCORES IS INGESTED, NEVER COMPUTED (Nick-signed canon): platform-scored values land as-received (Sleeper players_points — league-scored by Sleeper per each league's settings; ESPN as-received when unblocked). There is NO compute-from-raw-stats path for ACTUAL scores in v1. PROJECTIONS ARE THE SANCTIONED EXCEPTION (Nick's 2026-07-22 decision): player_projections stores component projection stats, and the BPA engine scores them with league_config weights at read time (src/services/bpa/scoring.ts) — projected value, never a substitute for actual player_scores; preset pts_* buckets never used as league values. Tier detection (tiers.ts) reads those same league-scored projections (via base-value output), never raw stats. Never invent score rows for unrostered weeks (FA-week gap accepted for v1)."
  - "DASHBOARD QUERY SERVICE CENTRALIZATION (live 2026-07-22): src/services/dashboard.ts is the ONE home of the standings ordering (fetchRankedStandings, wins desc → points_for desc → native_roster_id asc, Nick-signed under declared wiki silence) and the power-ranking formula (All-Play record; regular-season weeks via raw playoff_week_start; ties=half-wins; lowConfidence <6 weeks); listScoredWeeks is the ONE week-range/default-week source. The Wave 4 display components render these AS-IS. The spectator loader passes its own share_token-scoped client (shared data-access is sanctioned; shared UI never is). Boundary: the module never selects share_token/owner_id/native_league_id and has zero draft_state references — keep it that way."
  - "ADMIN/SPECTATOR UI BOUNDARY (Wave 4, live 2026-07-22): src/components/dashboard/ is ADMIN-surface UI — the spectator surface imports ZERO components from it (or from draft-board/ or ui/dropdown-menu's draft usages); spectator gets its own components (04's spectator sub-section). The BPA panel AND the coming tier-counter UI are likewise ADMIN-only — no spectator consumer, ever. Shared data-access (services/dashboard.ts) is the ONLY sanctioned sharing. Freshness primitives UnofficialChip + AsOfTime (components/dashboard/) are THE rendering of is_final/fetched_at. The player-card open mechanism is the URL-driven ?player= sheet — future admin surfaces reuse this pattern."
  - "DARK MODE ONLY IS LOAD-BEARING IN CODE (live 2026-07-22): globals.css holds the app's single always-on token set in :root — NO light block, NO .dark override, NO theme toggle; the `dark` class on <html> is a permanent marker keeping vendored shadcn dark:-variant tunings active. Never add a light token set, a prefers-color-scheme branch, or remove that class. ui/sonner.tsx hardcodes theme='dark'. --radius stays 0.625rem (Nick-signed). Position identity tokens --pos-qb/rb/wr/te/k/def are badges/chips ONLY — never row tints; teal = interactive/live only. BPA need badges: muted-tier chips, 'starter' borrows --warning, teal untouched. The tier-counter UI (item 4, next) must follow the same discipline — informational, non-blocking (league-size-scarcity-effects.md: tier breaks are a weighted input, never a rigid draft-now trigger), tabular-nums, no new tokens without adding them to the set first."
  - "OWNER AUTH SURFACE (live 2026-07-22) — three walls, keep all three: (1) src/proxy.ts (Next 16 proxy, NOT middleware.ts) refreshes the Supabase session per matched request and 307s unauth /leagues/* to /login — matcher EXCLUDES /api. (2) The (admin) layout gate uses getAdminAuthState (src/lib/supabase/auth.ts → is_fantasy_admin RPC) — never hardcode the admin UUID; a signed-in non-fantasy prolabel user is a REAL case and gets the zero-data NotAuthorizedCard. (3) Per-table RLS. ROOT AUTO-LAND targets the dashboard (/leagues/<id>). Server actions add a FOURTH gate: auth INSIDE each action — SIX actions (submitManualPick/undoManualPick/startDraftSession/endDraftSession/pollActiveDraft/fetchBpaRecommendations + login's signOut); the read-only fetchBpaRecommendations returns {ok:false, reason:'unauthorized'} + RLS as the second wall (live-verified: anon → league_not_found, zero data)."
  - "src/lib/supabase/database.types.ts is generated — never hand-edit; regenerate via npm run gen:types after every schema migration. It deliberately contains the whole shared public schema (foreign prolabel tables included) — expected, not drift. Trap: generated Insert types list stored generated columns (matchups.effective_points) as insertable — Postgres rejects them; sync code must omit them. Note: getBpaRecommendations reads player_projections with a PAGINATED .range loop (1000/page) — the 3292-row season snapshot exceeds PostgREST's default single-select page cap; never rely on an unbounded select for that table (tier detection reuses that same paginated pool — no separate read)."
  - "src/lib/supabase/admin.ts bypasses RLS (secret key) — server-only, never import from client code; its env var is deliberately not NEXT_PUBLIC_ so a client-bundle import fails loudly. The draft-board, dashboard, and BPA/tier routes/actions deliberately do NOT use it — owner pages read through the RLS server client as the signed-in admin. Temp verification scripts (deleted after use) may use the admin client for read-only checks (blast radius = SELECTs)."
  - "Sleeper player catalog (/players/nfl): at most ONCE PER DAY — Sleeper's most load-bearing rate rule. The cron route enforces it with a hard guard on sync_runs. The manual script path (npm run sync:players) bypasses the guard — manual cadence discipline applies. Per-league endpoints remain paced by the client's global 250ms min-gate (which also absorbs 5s live-draft polling, ≤3 requests per executed tick). The BPA panel's per-pick recompute AND tier recompute make NO Sleeper calls — they read Supabase only (fetchBpaRecommendations→getBpaRecommendations→computeBpaBoard incl. tiers), so they add zero to the Sleeper budget."
  - "api.sleeper.com (ADP/projections surface) is NOT api.sleeper.app/v1: undocumented, no contract, can change shape or vanish — its consumer is the quarantined src/services/adp/ ONLY (own client, own pacing gate). The SAME fused ingestion run feeds BOTH adp_rankings AND player_projections from one fetch; never a second api.sleeper.com client or a separate projections fetch. 999.0 in any adp_* field is a 'no ADP in this format' sentinel — never data in adp_rankings; player_projections.stats retains the RAW object (sentinels included). ADP is never read from player_projections (calibration reads sentinel-filtered adp_rankings only; getBpaRecommendations reads ADP carriers from adp_rankings, projection component stats from player_projections — never crossed). The adp_* field inventory is OPEN — never hardcode a closed format list. Covering wiki: sleeper-api/projections-endpoint."
  - "League-format→ADP-format mapping is CENTRALIZED in resolveAdpScoringFormat (src/services/draft-board.ts, Nick-signed): superflex→'2qb' precedence, then ppr buckets; dynasty/IDP unmapped in v1; NO cross-format fallback. Any consumer calls this function — never re-derives (getBpaRecommendations + bpa/calibration.ts's callers do exactly this). Same centralization for roster-slot layouts: parseRosterSlotLayout (same module) is the ONE parser of roster_settings_raw — bpa/replacement.ts, need.ts, getBpaRecommendations consume its output, never re-derive. FLEX_ELIGIBILITY is the ONE flex-eligibility home (exported from bpa/replacement.ts) — need.ts imports it, never re-derives. ADP-absence classification is centralized in deriveAdpNoticeKind (src/components/draft-board/adp-notice.tsx). DraftablePlayer (Pick<DraftBoardPlayer,'sleeperPlayerId'|'fullName'>, draft-board.ts) is the ONE minimal shape the shared handleDraft consumes — non-table draft surfaces reuse it, never a cast or a parallel submit."

health: "clean"
