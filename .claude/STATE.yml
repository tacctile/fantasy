# .claude/STATE.yml
# Living "what happened last session" file. Overwritten completely at the end of every session — never patched.
# Last Updated: 2026-07-22

current_focus: "Wave 1 (Foundation), feature-build session via BUILD_PROTOCOL: first item of the 'Schema — league-scoped state' section of 01_foundation.md is complete and live — rosters table (20260722050430_create_rosters_table.sql), created via supabase migration new and pushed by Claude via the allowed 'npx supabase db push' rule (password sourced from .env.local in-shell). Shape: composite PK (league_id, native_roster_id) per the sleeper-api/roster-endpoint '(league_id, roster_id) pair' and espn-api/team-owner-member-id '(league, season, teamId)' identity decisions (season carried by the parent leagues row); league_id uuid FK -> leagues.platform_league_uuid on delete cascade; native_roster_id integer as-received (Sleeper roster_id / ESPN teamId are both wire integers); platform enum + season_year (Schema Rules 2/3 — duplicated here since rosters is N:1 with leagues, unlike 1:1 league_config); owner_native_id text nullable (Sleeper owner_id / ESPN primaryOwner; orphaned rosters normal) + co_owner_native_ids text[] not null default '{}' (full co-owner list, primary never by array position); nullable team_name/owner_display_name display columns for Wave 2 sync; timestamps + set_updated_at trigger; RLS enabled at creation (owner-write policies remain the integrity item's scope). Clarify decisions (Nick, all recommended options): composite PK, integer native ID, first-class owner columns, display columns now. Wiki coverage: identity principles covered by sleeper-api/espn-api pages; schema-reference has no rosters DDL ADR — silence declared at decision time, resolved via Clarify — report carries 'gap found and flagged' with a WIKI NOTE suggesting a schema-reference 'league-scoped state tables' DDL addendum. All objects verified live via read-only queries (columns/nullability, PK, FK-cascade, RLS, trigger, migration history row). Log cap triggered this session: 2026-07-21_03.md condensed into .claude/logs/ARCHIVE.md and deleted. Next unchecked build item: 'Migration: roster_players (or equivalent join)' — second item of the 'Schema — league-scoped state' section of 01_foundation.md."

build_system:
  index: ".claude/BUILD_INDEX.md"
  files_location: ".claude/build/"
  total_files: 7
  current_build_files:
    - ".claude/build/01_foundation.md"

nick_pending:
  - "Before Wave 3a: verify whether Sleeper actually has a public ADP endpoint, or decide a real alternative source — see MANUAL_SETUP_CHECKLIST.md Wave 3a section."
  - "Before Wave 5's waiver/FAAB recommendations item is built: run a dedicated scoping session to define the actual waiver/FAAB candidate scoring formula (opportunity-based — snap/target/touch share trends — is the recommended direction per wiki/topics/league-mechanics/waiver-wire-faab-strategy.md, but no formula exists yet). See 05_eye_candy.md 'Waiver/FAAB recommendations' section, DEFERRED item."

blockers: []

known_issues:
  - "npm audit reports 6 vulns (4 moderate, 2 high) as of last check — pre-existing 1 moderate postcss + 2 high sharp are transitive inside next@16.2.11's pinned deps (only offered 'fix' is downgrading next to 9.x, which is wrong). All dev-time/build-time, none shipped to clients. Re-check npm audit in later sessions."

warnings:
  - "The Supabase database is the shared multi-app 'prolabel' project. Never run supabase db reset or supabase migration repair against it; new foreign history versions get empty local stub files, never repair. Owner-write RLS policies must match Nick's specific auth.uid(), never the blanket authenticated role. Full rules: ARCHITECTURE.md 'Shared-Database Constraints'."

health: "clean"
