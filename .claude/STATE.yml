# .claude/STATE.yml
# Living "what happened last session" file. Overwritten completely at the end of every session — never patched.
# Last Updated: 2026-07-22

current_focus: "WAVE 2 — ESPN BLOCKED ON EXTERNAL TIMING; BUILD PATH RE-ROUTED (2026-07-22, non-code session): Nick cannot resolve the Wave 2 ESPN manual items — the 2026 season hasn't opened and ESPN leagues are commissioner-locked until season setup (expected to clear ~2026-08-19 → 2026-09-02, Nick's 4–6-week estimate; re-check mid-August). Recorded as a dated blocker in MANUAL_SETUP_CHECKLIST.md (Wave 2 note + decision of record) and 02_data_pipeline.md (both ESPN sub-sections' 9 items flipped to [!] with dated notes). Nick's explicit instruction: never guess league visibility or fabricate placeholder ESPN league IDs — ESPN waits on real data from real leagues. NEXT SESSION: 02_data_pipeline.md 'Cron / polling strategy' sub-section, first item (authenticated Vercel Cron routes) — with ESPN items [!], it is the first unchecked [ ] item, so BUILD_PROTOCOL self-location lands there naturally. Its MANUAL_SETUP token item was reclassified [ ]→[~] (the item's own text says the build session generates the token; Nick only confirms storage — ask in that item's Clarify batch). Fold note for next session: first Vercel Cron wiring is plausibly 'first touch of a new external service' (Folding Policy hard stop) — declare the fold with that in mind. After the cron sub-section: Wave 3a (BUILD_INDEX dependency note: needs only Wave 1 schema + Wave 2 Sleeper sync, both done — does NOT need ESPN), gated only on Wave 3a's own manual items (ADP source verification + which leagues populate the board — plain-chat-resolvable now, not season-gated; see nick_pending). Wave 3b and all 9 ESPN items stay blocked until leagues open."

build_system:
  index: ".claude/BUILD_INDEX.md"
  files_location: ".claude/build/"
  total_files: 7
  current_build_files:
    - ".claude/build/02_data_pipeline.md"

nick_pending:
  - "BLOCKED ON EXTERNAL TIMING — nothing to do until ~mid-August 2026 (recorded 2026-07-22; expected clear ~2026-08-19 → 2026-09-02): Wave 2 ESPN manual items (public/private per league, espn_s2/SWID for private leagues, league IDs + season year). ESPN leagues are commissioner-locked until 2026 season setup. When they open: resolve the three items in a plain chat, flip them in MANUAL_SETUP_CHECKLIST.md, and flip 02_data_pipeline.md's 9 ESPN items [!]→[ ] so build sessions self-locate onto them again."
  - "UNBLOCKS WAVE 3a — plain-chat-resolvable NOW, not season-gated: (1) verify whether Sleeper actually has a public ADP endpoint, or decide a real alternative source (e.g. FantasyPros + any API key it needs); (2) confirm which league(s) populate the draft board. Both are Wave 3a MANUAL_SETUP items. With ESPN timing-blocked, Wave 3a is the main build runway after the cron sub-section — resolving these two determines how far the build can go during the 4–6-week ESPN wait."
  - "When your 2026 Sleeper league renews (new league_id every season): run npm run sync:league -- <new_id> once to connect it, then npm run sync:sleeper covers it in every full run (config/rosters/matchups/draft — pre-draft state is handled gracefully). The 2025 league (1257464256710856704) is fully ingested including its 170-pick draft."
  - "Before Wave 4's owner UI: make sure you can sign in as nick@prolabelco.com — that pre-existing account's password is whatever you saved at its 2026-05-15 creation; reset via the Supabase dashboard if forgotten. No session has exercised a real sign-in yet."
  - "Before Wave 5's waiver/FAAB recommendations item is built: run a dedicated scoping session to define the actual waiver/FAAB candidate scoring formula (opportunity-based — snap/target/touch share trends — is the recommended direction per wiki/topics/league-mechanics/waiver-wire-faab-strategy.md, but no formula exists yet)."

blockers:
  - "EXTERNAL TIMING (dated 2026-07-22): 02_data_pipeline.md's two ESPN sub-sections (9 items, now [!]) are blocked until ESPN leagues open for the 2026 season — commissioner-locked, expected ~2026-08-19 → 2026-09-02. Not resolvable by Nick or any build session before then; do NOT guess visibility or fabricate placeholder league IDs (Nick's explicit instruction). Downstream: Wave 3b inherits this blocker (needs Wave 2 ESPN). NOT blocked: the cron/polling sub-section (next up — ESPN-independent, wires the existing Sleeper orchestrator) and Wave 3a (ESPN-independent per BUILD_INDEX; has its own plain-chat manual items, see nick_pending)."

known_issues:
  - "npm audit reports 6 vulns (4 moderate, 2 high) — last checked live 2026-07-22 (tsx install session): pre-existing 1 moderate postcss + 2 high sharp are transitive inside next@16.2.11's pinned deps (only offered 'fix' is downgrading next to 9.x, which is wrong). All dev-time/build-time, none shipped to clients. No new packages since. Re-check in later sessions."
  - "derived_config.ir_slot_count counts IR labels in roster_positions — but Nick's own league proves IR capacity can be expressed only via settings.reserve_slots (4 live reserve players, 0 IR labels). Raw settings are preserved in roster_settings_raw so nothing is lost; revisit the derivation (label count OR reserve_slots) when a consumer actually reads ir_slot_count. Flagged as WIKI NOTE evidence on league-endpoint open question #4."

warnings:
  - "ABSOLUTE RULE 13 (canonical: MASTER_CONTEXT.md 'Shared Database Protection — Non-Fantasy Data Is Untouchable'): the Supabase database is the shared multi-app 'prolabel' project — ~49 non-fantasy tables incl. live business production data (Elliott). Never supabase db reset; never migration repair touching foreign history (new foreign versions get empty local stub files, never repair); no raw SQL that could reach a non-fantasy table — verify every migration's table names against ARCHITECTURE.md's 'Database Schema (live)' inventory; collision check before any new table name; ANY ambiguity → stop and ask Nick; mandatory BLAST RADIUS: line on every completion report. Owner-write RLS policies must match Nick's specific auth.uid(), never the blanket authenticated role — realized as public.is_fantasy_admin(); rotate the admin there, never by editing per-table policies. Operational restatement: ARCHITECTURE.md 'Shared-Database Constraints'."
  - "src/lib/supabase/database.types.ts is generated — never hand-edit; regenerate via npm run gen:types after every schema migration. It deliberately contains the whole shared public schema (foreign prolabel tables included) — expected, not drift. Trap: generated Insert types list stored generated columns (matchups.effective_points) as insertable — Postgres rejects them; sync code must omit them."
  - "src/lib/supabase/admin.ts bypasses RLS (secret key) — server-only, never import from client code; its env var is deliberately not NEXT_PUBLIC_ so a client-bundle import fails loudly instead of leaking."
  - "Sleeper player catalog (/players/nfl): fetch at most ONCE PER DAY — Sleeper's most load-bearing rate rule. syncPlayersCatalog has no hard in-service guard yet (the sync-run tracking table in the cron sub-section is the intended substrate); until then cadence discipline is manual — do not run npm run sync:players more than once a day except for a genuine failure retry. The orchestrator (sync:sleeper) deliberately EXCLUDES the catalog for this reason. Per-league endpoints are ordinary-volume and now paced by the client's global 250ms min-gap gate."

health: "clean"
