# .claude/STATE.yml
# Living "what happened last session" file. Overwritten completely at the end of every session — never patched.
# Last Updated: 2026-07-22

current_focus: "WAVE 2 BEGUN (2026-07-22): the 'Sleeper — client + player catalog' sub-section of 02_data_pipeline.md is complete as one declared fold (3 items) — typed server-only Sleeper HTTP client (src/services/sleeper/client.ts: sleeperGet + SleeperApiError, backoff per sleeper-api/rate-limits), catalog sync service (players-catalog.ts: validate-before-persist, 500-row chunked upsert, mark-absent pass only after a completed response), and catalog-presence columns on players (migration 20260722134911: is_active_in_catalog + catalog_last_seen_at — distinct from Sleeper's own status field). LIVE VERIFIED: first-ever Sleeper call synced 12,200 players into production players in ~33s via npm run sync:players (new tsx runner, scripts/sync-players.ts); D/ST abbreviation IDs (32 = position DEF), metadata's cross-provider ID block, and full column mapping all confirmed by read-only SQL checks. Clarify decisions: services live in src/services/<platform>/; tsx runner for manual invocation; NO snapshot/diff table (Nick ruled build-file scope over the players-endpoint page's snapshot-retention Key Decision — conflict flagged in report); flag+last-seen presence mechanism (wiki-silent, declared). NEXT SESSION: 02_data_pipeline.md → 'Sleeper — league-scoped sync' sub-section, first item = league metadata fetch (/league/{league_id}) → league_config. That sub-section REQUIRES a real Sleeper league ID (MANUAL_SETUP Wave 2 item, still [ ] open) — per the Manual Setup Flag, halt with a MANUAL SETUP REQUIRED block if Nick doesn't have one ready at Clarify. Notes: (a) the non-zero mark-inactive path hasn't fired yet (provably-correct 0 on first run; exercised naturally by a future daily run — never re-fetch the catalog same-day, once-per-day is Sleeper's most load-bearing rule); (b) general client-side rate limiter deliberately deferred to the league-scoped session where multi-league fan-out first exists (disclosed, not dropped); (c) ESPN singletons and cron-secret dependencies unchanged, later sub-sections."

build_system:
  index: ".claude/BUILD_INDEX.md"
  files_location: ".claude/build/"
  total_files: 7
  current_build_files:
    - ".claude/build/02_data_pipeline.md"

nick_pending:
  - "NEXT SESSION (Sleeper league-scoped sync): have at least one real (or throwaway test) Sleeper league ID ready — the sub-section's first item cannot start without it (MANUAL_SETUP Wave 2 item still [ ] open; the session will halt per the Manual Setup Flag if it's still open at Clarify)."
  - "Before ESPN work later in Wave 2: decide public vs. private for each ESPN league (public skips cookie auth entirely — check before assuming cookies are needed); generate espn_s2/SWID only for private leagues; have ESPN league ID(s) + season year ready."
  - "Before Wave 4's owner UI (moved from the health-check item, which chose the no-sign-in secret-key path): make sure you can sign in as nick@prolabelco.com — that pre-existing account's password is whatever you saved at its 2026-05-15 creation; reset via the Supabase dashboard if forgotten. No session has exercised a real sign-in yet."
  - "Before Wave 3a: verify whether Sleeper actually has a public ADP endpoint, or decide a real alternative source — see MANUAL_SETUP_CHECKLIST.md Wave 3a section."
  - "Before Wave 5's waiver/FAAB recommendations item is built: run a dedicated scoping session to define the actual waiver/FAAB candidate scoring formula (opportunity-based — snap/target/touch share trends — is the recommended direction per wiki/topics/league-mechanics/waiver-wire-faab-strategy.md, but no formula exists yet). See 05_eye_candy.md 'Waiver/FAAB recommendations' section, DEFERRED item."

blockers: []

known_issues:
  - "npm audit reports 6 vulns (4 moderate, 2 high) — re-checked live 2026-07-22 (this session's tsx install): unchanged; pre-existing 1 moderate postcss + 2 high sharp are transitive inside next@16.2.11's pinned deps (only offered 'fix' is downgrading next to 9.x, which is wrong). All dev-time/build-time, none shipped to clients. tsx added 3 packages, no new findings. Re-check in later sessions."

warnings:
  - "ABSOLUTE RULE 13 (canonical: MASTER_CONTEXT.md 'Shared Database Protection — Non-Fantasy Data Is Untouchable'): the Supabase database is the shared multi-app 'prolabel' project — ~49 non-fantasy tables incl. live business production data (Elliott). Never supabase db reset; never migration repair touching foreign history (new foreign versions get empty local stub files, never repair); no raw SQL that could reach a non-fantasy table — verify every migration's table names against ARCHITECTURE.md's 'Database Schema (live)' inventory; collision check before any new table name; ANY ambiguity → stop and ask Nick; mandatory BLAST RADIUS: line on every completion report. Owner-write RLS policies must match Nick's specific auth.uid(), never the blanket authenticated role — realized as public.is_fantasy_admin(); rotate the admin there, never by editing per-table policies. Operational restatement: ARCHITECTURE.md 'Shared-Database Constraints'."
  - "src/lib/supabase/database.types.ts is generated — never hand-edit; regenerate via npm run gen:types after every schema migration. It deliberately contains the whole shared public schema (foreign prolabel tables included) — this is expected, not drift."
  - "src/lib/supabase/admin.ts bypasses RLS (secret key) — server-only, never import from client code; its env var is deliberately not NEXT_PUBLIC_ so a client-bundle import fails loudly instead of leaking."
  - "Sleeper player catalog (/players/nfl): fetch at most ONCE PER DAY — Sleeper's most load-bearing rate rule. syncPlayersCatalog has no hard in-service guard yet (the sync-run tracking table in the cron sub-section is the intended substrate); until the cron item lands, cadence discipline is manual — do not run npm run sync:players more than once a day except for a genuine failure retry."

health: "clean"
