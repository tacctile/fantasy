# .claude/STATE.yml
# Living "what happened last session" file. Overwritten completely at the end of every session — never patched.
# Last Updated: 2026-07-23

current_focus: "WAVE 4 nav-shell sub-section COMPLETE (4/4 [x]) — the 'Admin navigation shell and information architecture' sub-section of 04_league_dashboard.md, folded as ONE independently-verifiable IA artifact (4 items; not a named singleton; live-behavior ceiling N/A — pure IA/UX, no live sync). READ-ONLY session: NO migration, NO schema, NO DB writes; the only DB contact is the command-center home's read-only getStandings/getPowerRankings through the RLS server client. THREE Nick-signed Clarify (one batch — the decisions are interdependent, jointly shaping all four items): (1) ROUTE TOPOLOGY = command-center HOME at the league root /leagues/[id]; the full standings/matchups/power dashboard MOVED to /leagues/[id]/dashboard; root auto-land now lands on the home. (2) SIDEBAR OWNS GLOBAL CHROME = the persistent sidebar owns the league selector + sign-out + section nav; the dashboard shell header AND the 3b draft-board header were de-duplicated (Nick explicitly accepted touching the 3b header). (3) FUTURE SLOTS = visible disabled 'coming soon' rows for Wave 5/6. BUILT: NEW src/components/dashboard/admin-sidebar.tsx (client; usePathname active-highlight, PRIMARY_NAV Home/Dashboard/Draft [Home 'exact', rest 'prefix'], FUTURE_NAV 8 Wave5/6 disabled 'Soon' rows with the wave # kept in config as the documented mount point, reused LeagueSelector subPath='' + SignOutButton; tablet/PC-first labelled rail md+ / compact icon top-bar below; tokens active=bg-muted, coming-soon=muted/40, no teal, no new token). NEW src/components/dashboard/command-center.tsx (3 snapshot cards — Standings top-3 + Power top-3 [rank-delta chevron + all-play win%, provisional caveat on lowConfidence] deep-linking to /dashboard, Draft Board deep-link to /draft; renders getStandings/getPowerRankings AS-IS, no second ordering/formula; empty arrays → honest pre-sync copy; tabular-nums). NEW src/app/(admin)/leagues/[leagueId]/layout.tsx (server; fetches listConnectedLeagues once past the auth gate, 404s unknown league, renders AdminSidebar + content column; a SEGMENT layout so it persists across Home/Dashboard/Draft — sticky league context — and the league-list fetch runs once per league, not per section-nav). [leagueId]/page.tsx overwritten to the home; [leagueId]/loading.tsx rewritten to the home cards. NEW [leagueId]/dashboard/page.tsx = the MOVED dashboard (minus the listConnectedLeagues fetch + LeagueSelector headerActions; closeHref → /leagues/[id]/dashboard?week=N) + dashboard/loading.tsx (moved skeleton, selector/sign-out shimmer dropped). [leagueId]/error.tsx copy generalized (covers home + dashboard). DE-DUP: league-dashboard-shell.tsx dropped headerActions/draft-link/sign-out (header=identity only); draft-board-header.tsx dropped LeagueSelector+SignOut+the unused `leagues` prop; draft-board-shell.tsx + draft/page.tsx dropped the leagues/ConnectedLeague prop chain + the draft page's listConnectedLeagues fetch (the layout owns it); draft/loading.tsx header shimmer slimmed; root page.tsx auto-land comment updated (target /leagues/<first> unchanged — now lands on the home). RELATIVE ?week=/?player= links in the dashboard components follow the route move automatically — only the page-constructed closeHref needed the /dashboard segment (grep-confirmed before moving). Verified: tsc + lint + build all clean (routes: /leagues/[id] home, /leagues/[id]/dashboard, /leagues/[id]/draft), npm test 14/14, runtime boot smoke (next dev + curl: /login 200, / and /leagues/<uuid> → 307 /login — the proxy gate catches the new routes, the layout doesn't throw at request time). Authed nav-shell render (sidebar + home cards) needs Nick's owner session → VISUAL CHECK BACKLOG (same handoff as every prior admin-UI item; Rule 13, no session minted). SELF-LOCATION FOR NEXT SESSION: 04's first unchecked item in file order is the share-link settings panel (line 54), which RIDES the share-token RLS named singleton #5. So the next self-locatable fold is NAMED SINGLETON #5 — share-token RLS SELECT policies (six tables) + no-draft_state policy + regenerate/revoke action + spectator data loader + the three boundary tests, folded together, nothing else. It is a MIGRATION session → Rule 13 blast-radius applies (owner policies stay pinned to is_fantasy_admin(); draft_sessions/draft_state NEVER on any spectator policy list). The share-link settings panel (admin UI for the regenerate action) + the spectator UI components + spectator page composition are separate subsequent folds after #5."

build_system:
  index: ".claude/BUILD_INDEX.md"
  files_location: ".claude/build/"
  total_files: 8
  current_build_files:
    - ".claude/build/04_league_dashboard.md"  # 🟡 — data-access 5/5 [x], freshness [x], 4 display components [x], page assembly [x], league selector [x], NAV-SHELL 4/4 [x] (this session). Remaining: share-link settings panel [ ] (rides singleton #5), share-token infra 3 items [ ] (= named singleton #5), spectator surface 7 items [ ], resilience/tests 4 items [ ]. NEXT self-locatable = named singleton #5 (share-token RLS + spectator loader + boundary tests — a MIGRATION fold, Rule 13)
    - ".claude/build/03b_draft_assistant_live_draft.md"  # 🟢 COMPLETE 2026-07-23. Nothing left here. (This session de-duplicated its draft-board header/shell — removed the selector+sign-out+leagues prop, now owned by the Wave 4 sidebar — but changed no live-draft behavior.)
    - ".claude/build/03a_draft_assistant_static_board.md"  # 🟡 — all five sub-sections complete; sole remainder = bye-week [>] (schedule-source-gated); goes 🟢 when that ships or is re-ruled
    - ".claude/build/02_data_pipeline.md"  # ESPN residue [!], cron cadence [~], finality promotion [>] (Nick-signed deferral) — nothing self-locatable here

nick_pending:
  - "VISUAL CHECK BACKLOG — Wave 4's admin surfaces + Wave 3b's live-draft surfaces have never been eyeballed against a real signed-in session on real data. NEW THIS SESSION (nav-shell, needs an owner sign-in to see): the persistent left sidebar (Home/Dashboard/Draft nav with active highlight; the reused league selector; sign-out; 8 disabled Wave5/6 'coming soon' rows) on tablet/PC, and the compact icon top-bar below md; the command-center HOME at /leagues/<id> (Standings + Power snapshot cards + a Draft Board card, each deep-linking); the MOVED dashboard now at /leagues/<id>/dashboard (header slimmed to league identity — selector/sign-out live in the sidebar); the slimmed draft-board header (selector/sign-out removed; keeps ADP + session toggle + live status). Also still unverified visually: the 3b live-draft surfaces (draft queue + auto-pick pill, positional-run badge, tier-depth counter + BPA recommendations, live strip/status, recent-picks feed + Undo, roster cards, optimistic Draft fold, live-sync dimming) and the resilience degradation (only observable by forcing an error). The completed 2025 draft has no live clock, so live-draft timing behaviors still can't be fully exercised until a real 2026 draft."
  - "POST-LOGIN LANDING INCONSISTENCY (surfaced 2026-07-23, NOT changed — Nick's call): src/app/login/actions.ts still lands a fresh sign-in on the first league's DRAFT board (/leagues/<id>/draft) — a SEPARATE Nick-signed decision from root auto-land, which now lands on the command-center HOME. Both are valid routes; leaving it untouched respects the signed decision and the fold boundary (login landing isn't a nav-shell item). If you'd prefer post-login to also land on the home now that it exists, it's a one-line change to landingPath() in login/actions.ts — decide whenever; not blocking."
  - "OPTIONAL FUTURE — superflex/best-ball run sensitivity (deferred 2026-07-23 by Nick's Clarify): positional-run-detection-draft-day.md Key Decision mandates different run-sensitivity for superflex/2QB (QB scarcity structural — proactive early QB is correct, not a run) and best-ball. The shipped detectPositionalRuns applies uniform thresholds because no superflex/best-ball league is connected (live league is 1QB redraft). When one connects, make the QB threshold format-aware via resolveAdpScoringFormat — small change to runs.ts + a runs.test.ts case (the durable suite now exists) + the badge picks it up for free. NOTE: auto-pick caps ARE already format-aware for free (superflex lifts QB's positionCap via SUPER_FLEX flex eligibility). Not blocking; its own tiny fold. Also a WIKI NOTE."
  - "OPTIONAL FUTURE / SCOPE QUESTION — demand-vs-panic run classification (WIKI NOTE, unchanged 2026-07-23): positional-run-detection-draft-day.md mandates classifying runs into demand-driven (tier-cliff) vs panic-driven (contagion/ahead-of-ADP) with OPPOSITE recommended responses (enter/fade vs wait). The shipped positional-run sub-section does a count-based flag + a badge that COMPOSES the tier-drain signal in its tooltip — NOT the full classification. If the fuller wiki model is wanted for v1 it needs a NEW registered build item / Nick-signed scope amendment. Decide whenever; not blocking."
  - "OPTIONAL FUTURE — per-league memory for 'my team' AND the queue: both are client-side/re-pick-each-session by decision (my team 2026-07-22 Clarify; the queue is localStorage per league, so it survives reloads but not a cleared browser / another device). If you later want durable cross-device state, that's a small future migration (a per-league my-roster + a draft_queue table) + tiny reads — its own small fold. Not blocking."
  - "FINALITY PROMOTION — build scheduling (design Nick-signed + registered 2026-07-22 as the [>] item in 02_data_pipeline.md's cron sub-section; mechanism in known_issues): build as its own standalone fold OR bundle with the season-start cadence session. HARD DEADLINE: before the 2026 NFL week 1→2 rollover (~2026-09-15; promotion first actually needed ~2026-09-18). Flip the item [>]→[ ] when its build session starts."
  - "BLOCKED ON EXTERNAL TIMING — nothing to do until ~mid-August 2026 (expected clear ~2026-08-19 → 2026-09-02): Wave 2 ESPN manual items (public/private per league, espn_s2/SWID for private, league IDs + season year). ESPN leagues commissioner-locked until 2026 season setup. When they open: resolve in a plain chat, flip them in MANUAL_SETUP_CHECKLIST.md, flip 02_data_pipeline.md's 9 ESPN items + 03c's [!]→[ ]. Per the backsweep practice (BUILD_PROTOCOL Amendment Hygiene) that plain chat must sweep the confirmations against already-registered 02/03c text AND against ALL shipped Wave 4 + Wave 3b surfaces (platform-agnostic by construction): recordManualPick, draft_sessions/poll orchestration, client live-sync seams, the DraftOrderMeta on-clock path (slot_to_roster_id is Sleeper-specific; ESPN needs its own draft-order source, which auto-pick's away-trigger also depends on), the projections/BPA/tier/run layer, the QUEUE + AUTO-PICK layer, the RESILIENCE layer, AND now the Wave 4 admin dashboard + NAV SHELL + command-center home (all platform-agnostic — ESPN leagues join listConnectedLeagues → the sidebar selector, the home cards, and the moved dashboard automatically once connected; nothing ESPN-specific was baked into the nav shell). Sweep any item text that assumed otherwise."
  - "SEASON START (~Sept 2026): revisit league-sync cadence — the specced 15-minute cron needs Vercel Pro (~$20/mo) or an alternative trigger; currently daily on Hobby per your 2026-07-22 decision. Flip 02_data_pipeline.md's cron-schedule item [~]→[x] when sub-daily ships. If Pro is bought, /api/cron/sync-adp is the ready mount point. NOTE: live-draft polling + BPA/tier/run/counter recompute + run badge + queue/auto-pick all do NOT depend on this — client-driven, no cron slot at any tier."
  - "BYE-WEEK [>] ITEM (03a) still awaits its schedule-source decision (plain chat: pick a schedule source, then a build session ships table+ingestion+row wiring — flips 03a 🟢). No longer blocking anything, the only remainder between 03a and 🟢. Related small observation: the 3a board pool's fullName is also null for DST rows (renders the abbreviation fallback); the item-4 name compose exists in draft-picks.ts if a future correctness pass wants to reuse it for getDraftBoardData."
  - "When your 2026 Sleeper league renews (new league_id every season): run npm run sync:league -- <new_id> once to connect it; every daily league-state cron run (and npm run sync:sleeper) covers it after. The 2025 league (1257464256710856704) is fully ingested incl. its 170-pick draft. With auto-land live, the renewed league (season 2026 > 2025) becomes the COMMAND-CENTER HOME you land on after sign-in (its full dashboard is one click away at /dashboard, the draft board at /draft, both from the sidebar). Draft-day note: the renewed league is where Start draft + live polling + click-to-draft + the live strip/status/roster/recent-picks feed + the BPA panel + the tier counter + the run badge + the draft queue + auto-pick + the read-only resilience fallback all run in anger. The BPA + tier + run layer uses the LATEST ingested projections/ADP season (2026) regardless of the league's own season, so it is already current-market."
  - "Before Wave 5's waiver/FAAB recommendations item is built: run a dedicated scoping session to define the actual waiver/FAAB candidate scoring formula (opportunity-based — snap/target/touch share trends — is the recommended direction per wiki/topics/league-mechanics/waiver-wire-faab-strategy.md, but no formula exists yet)."

blockers:
  - "EXTERNAL TIMING (dated 2026-07-22): 02_data_pipeline.md's two ESPN sub-sections (9 items, [!]) are blocked until ESPN leagues open for the 2026 season — commissioner-locked, expected ~2026-08-19 → 2026-09-02. Not resolvable by Nick or any build session before then; do NOT guess visibility or fabricate placeholder league IDs (Nick's explicit instruction). Downstream: Wave 3c inherits this blocker (ESPN draft mechanics + auction). Does not stall the roadmap: with 3b 🟢 and the Wave 4 nav-shell shipped, the buildable frontier is WAVE 4's share-token RLS named singleton #5 (share-token infra + spectator loader + boundary tests) — plus 02's finality-promotion [>] and 03a's bye-week [>] whenever their plain-chat decisions land."

known_issues:
  - "npm audit reports 6 vulns (4 moderate, 2 high) — last re-checked live 2026-07-23 after adding vitest (count UNCHANGED); NO new deps added this session (nav-shell reused lucide-react + existing components). Pre-existing 1 moderate postcss + 2 high sharp are transitive inside next@16.2.11's pinned deps (only offered 'fix' is downgrading next to 9.x, which is wrong). All dev-time/build-time, none shipped to clients. Re-check in later sessions."
  - "FINALITY-PROMOTION GAP (surfaced + designed + registered 2026-07-22 — still UNBUILT): in-season, matchups/player_scores rows get is_final=true only when league lifecycle status reads 'complete' at sync time; the scheduled sync is current-week-only, so finished weeks stay is_final=false all season. Covering wiki: sleeper-api/matchup-endpoint (finality Key Decision #3). Nick-signed design: previous-week re-sync + promotion inside the daily league-state cron (week-advance = games-complete proxy; Friday-or-later UTC stable-read match = trigger) + league-complete backstop sweep; needs a caller-driven finality extension to syncLeagueMatchups. Registered as the [>] item in 02_data_pipeline.md's cron sub-section. Zero live consequence until the season's first week rollover; HARD DEADLINE ~2026-09-15/18. WAVE 4 UI side SHIPPED (UnofficialChip + AsOfTime render is_final/fetched_at honestly)."

warnings:
  - "ABSOLUTE RULE 13 (canonical: MASTER_CONTEXT.md 'Shared Database Protection'): the Supabase database is the shared multi-app 'prolabel' project — ~49 non-fantasy tables incl. live business production data (Elliott). Never supabase db reset; never migration repair touching foreign history (new foreign versions get empty local stub files); no raw SQL that could reach a non-fantasy table — verify every migration's table names against ARCHITECTURE.md's 'Database Schema (live)' inventory; collision check before any new table name; ANY ambiguity → stop and ask Nick; mandatory BLAST RADIUS: line on every report. Owner-write RLS must match Nick's specific auth.uid() via public.is_fantasy_admin(), never the blanket authenticated role. THIS SESSION (nav-shell): ZERO DB contact beyond the command-center home's read-only getStandings/getPowerRankings via the RLS server client — NO migration, NO schema, NO writes. NEXT FOLD IS A MIGRATION (named singleton #5, share-token RLS): it adds SELECT policies on SIX fantasy tables (leagues, league_config, rosters, matchups, standings, player_scores) gated on a valid share_token + an owner-only regenerate/revoke action — verify every table name against the live inventory before writing; draft_sessions/draft_state are NEVER on any spectator-policy list; player_projections/adp_rankings have no spectator consumer either; owner policies stay pinned to is_fantasy_admin(). That is the exact fold Rule 13 exists for — run the blast-radius check and STOP on any ambiguity."
  - "TESTING INFRASTRUCTURE (Vitest, since 2026-07-23): Vitest is THE durable test runner (vitest.config.ts at repo root — node env, @/→src alias mirroring tsconfig, include src/**/*.test.ts; scripts test=vitest run, test:watch=vitest). Tests are CO-LOCATED as *.test.ts beside source — typechecked + linted, never imported by a route so next build excludes them from the bundle. RULE-13 DISCIPLINE FOR TESTS: never write to the shared prolabel DB from a test — the DB race is tested against a FAKE in-memory Supabase client (draft-picks.test.ts models the (league_id,pick_number) ON-CONFLICT-DO-NOTHING PK via a synchronous check-and-insert). Extend this suite, don't recreate throwaway tsx harnesses. Current: 14 tests / 5 files (draft-picks race, board recompute, tiers recompute, runs window, draft-queue auto-pick) — UNCHANGED this session (nav-shell touched no tested module; the suite was re-run 14/14 as a regression check). The share-token boundary tests (singleton #5) are the next additions — model them against the fake client / RLS assertions, never a live write."
  - "ADMIN NAV SHELL + IA (live 2026-07-23): the persistent admin frame lives in src/app/(admin)/leagues/[leagueId]/layout.tsx → src/components/dashboard/admin-sidebar.tsx (client, usePathname). It OWNS the global chrome (LeagueSelector subPath='' + SignOutButton) — the dashboard shell header and the draft-board header no longer render either (de-duplicated; the draft header/shell/page dropped their `leagues`/ConnectedLeague prop chain). Route topology: /leagues/[id] = command-center HOME (command-center.tsx), /leagues/[id]/dashboard = the full standings/matchups/power dashboard (MOVED), /leagues/[id]/draft = the board; root + post-nothing auto-land go to the home (login/actions.ts still → /draft, see nick_pending). Wave 5/6 sections mount by lighting a FUTURE_NAV slot in admin-sidebar.tsx (the wave # is the documented mount point) + adding their /leagues/[id]/<section> route — they extend this shell, never build their own nav frame. Sticky context = the league selector in the sidebar (persists across sections); week/team stay page-local by design (URL-driven RSC; no cross-section client store — that would fight the LeagueSelector param-drop contract)."
  - "CLIENT-SIDE LIVE SYNC SEAMS (3b, unchanged 2026-07-23): the live board path is ticker → pollActiveDraft (full listDraftPicks snapshot per executed poll) → shell's fingerprint-deduped livePicks state (+ optimistic pendingPicks overlay + pollHealth + DraftOrderMeta + panelTiers + selfRosterId + the queue store). Every live-enhancement region is wrapped in its OWN ErrorBoundary; shell derivations feeding the static table are guarded at the source (mergedPlayers→base 3a players, runBoard→EMPTY_RUN_BOARD, prune try/caught, fireAutoPick disarms on fault via a 0ms hidden-tab timer). Extend THIS path — never a second poll driver, merge, pick-write UI path (handleDraft→recordManualPick; auto-pick reuses it), undo/tier/board/run/QUEUE source. Invariants: 'drafted'/draftPending only from the client merge; a confirmed snapshot row always wins over pending; 'rostered' wins over the whole overlay; ON-CLOCK IS A PROJECTION (projectOnClock — display + auto-pick's my-turn check; first-write-wins is the real arbiter). rosterLabel (player-row.tsx) is THE roster display convention. NOTE: the draft-board header shed the selector/sign-out this session (Wave 4 nav-shell) — no live-behavior change."
  - "MANUAL-PICK WRITE PATH CENTRALIZATION (3b, unchanged 2026-07-23): src/services/draft-picks.ts recordManualPick is the ONE local pick-write path — with durable unit tests (first-write-wins vs a fake client, exactly one accepted). AUTO-PICK writes through it (fireAutoPick → the SAME handleDraft → submitManualPick → recordManualPick, source='manual', undoable, conflict-safe — can NEVER write to Sleeper or desync a real draft). One attempt per pick number (autoPickAttemptRef), disarms on a selection fault. Server-action auth pattern (6 actions incl. read-only fetchBpaRecommendations): gate INSIDE each action (getAdminAuthState) + RLS as the second wall. listDraftPicks is THE ordered pick-snapshot read. Undo has its ONE UI (recent-picks feed)."
  - "BPA + TIER + RUN + QUEUE/AUTO-PICK LAYER CENTRALIZATION (3b, unchanged 2026-07-23): src/services/bpa/ is the ONE home of projection scoring + VORP + tier + run detection + the recommendations query (pure sub-layer + recommendations.ts I/O) — under durable Vitest coverage (board/tiers/runs). src/components/draft-board/ pure client layers: live-picks.ts, live-status.ts, draft-queue.ts (queue ops + localStorage store + roster-aware auto-pick; positionCap reads FLEX_ELIGIBILITY from replacement.ts; AUTO_PICK_BENCH_MARGIN + AUTO_PICK_DWELL_MS are declared-wiki-silence live-tuned constants). UI homes: bpa-recommendations-panel.tsx (recommendations + TierDepthStrip), positional-run-badges.tsx, draft-queue-panel.tsx, queue-toggle.tsx. Run detection is NOT routed through the value query; the queue is NOT reordered by the engine; need/tiers/runs stay SEPARATE parallel signals never multiplied into base value. NEVER add caching, a second scoring/VORP/tier/run/queue derivation, a second projections read, a second recommendations query, a second draft-submit path, or a second run/queue source. Layout parsing stays parseRosterSlotLayout, format mapping stays resolveAdpScoringFormat (both draft-board.ts)."
  - "PLAYER_SCORES IS INGESTED, NEVER COMPUTED (Nick-signed canon): platform-scored values land as-received (Sleeper players_points; ESPN as-received when unblocked). NO compute-from-raw-stats path for ACTUAL scores in v1. PROJECTIONS ARE THE SANCTIONED EXCEPTION: player_projections stores component projection stats, and the BPA engine scores them with league_config weights at read time (bpa/scoring.ts) — projected value, never a substitute for actual player_scores. Tier detection reads those same league-scored projections; run detection reads NO projections (only draft_state pick order + catalog position); the QUEUE/AUTO-PICK layer reads NO projections either. The Wave 4 dashboard + command-center home render player_scores/matchups/standings AS-RECEIVED (getStandings/getPowerRankings — the all-play power formula is computed from stored effective_points, never from raw stats). Never invent score rows for unrostered weeks (FA-week gap accepted for v1)."
  - "ADMIN/SPECTATOR UI BOUNDARY (Wave 4): src/components/dashboard/ is ADMIN-surface UI — the spectator surface (04's spectator sub-section, unbuilt) imports ZERO components from it (or from draft-board/ or ui/dropdown-menu's draft usages); spectator gets its own components. The NEW admin-sidebar.tsx + command-center.tsx are likewise ADMIN-only — no spectator consumer, ever (the spectator surface is a separate mobile-first rendering path, no login, no nav shell). The BPA panel, tier counter, run badge, queue/auto-pick panel, AND the draft board's resilience fallbacks are all ADMIN-only. Shared data-access (services/dashboard.ts) is the ONLY sanctioned sharing — the spectator loader (singleton #5) MAY reuse getStandings/getMatchups/getPowerRankings/getPlayerCard with a share_token-scoped client. Freshness primitives UnofficialChip + AsOfTime are THE rendering of is_final/fetched_at. The player-card open mechanism is the URL-driven ?player= sheet (on the dashboard route now, not the league root)."
  - "DARK MODE ONLY IS LOAD-BEARING IN CODE (live 2026-07-22): globals.css holds the app's single always-on token set in :root — NO light block, NO .dark override, NO theme toggle; the `dark` class on <html> keeps vendored shadcn dark:-variant tunings active. Never add a light token set, a prefers-color-scheme branch, or remove that class. ui/sonner.tsx hardcodes theme='dark'. --radius stays 0.625rem. Position identity tokens --pos-* are badges/chips ONLY; teal = interactive/live only; amber (--warning) = roster-need. The NAV SHELL follows this: active nav row = bg-muted (NOT teal — a nav selection isn't 'live'), coming-soon rows = muted-foreground/40, the command-center snapshot cards + rank-delta chevrons are uniform muted/tabular, NO new token. TierDepthStrip + PositionalRunBadges + the QUEUE UI + the resilience RegionFallback likewise: uniform muted, PositionBadge reused for position color."
  - "OWNER AUTH SURFACE (live 2026-07-22, extended 2026-07-23): three walls, keep all three: (1) src/proxy.ts (Next 16 proxy, NOT middleware.ts) refreshes the Supabase session per matched request and 307s unauth /leagues/* to /login — matcher EXCLUDES /api. (2) The (admin) layout gate uses getAdminAuthState (is_fantasy_admin RPC) — never hardcode the admin UUID; a signed-in non-fantasy prolabel user gets the zero-data NotAuthorizedCard. (3) Per-table RLS. NEW 2026-07-23: the [leagueId]/layout.tsx sidebar shell sits INSIDE the (admin) gate (renders only for the admin) and 404s an unknown league; it is NOT a fourth auth wall, just IA. ROOT AUTO-LAND targets the command-center home (/leagues/<id>); login/actions.ts still lands post-sign-in on /leagues/<id>/draft (separate Nick-signed decision — see nick_pending). Server actions add a FOURTH gate: auth INSIDE each action — SIX draft/BPA actions + login's signOut. The share-token spectator surface (singleton #5, unbuilt) is a SEPARATE no-login rendering path gated by share_token, never these walls."
  - "src/lib/supabase/database.types.ts is generated — never hand-edit; regenerate via npm run gen:types after every schema migration. It deliberately contains the whole shared public schema (foreign prolabel tables included) — expected, not drift. Trap: generated Insert types list stored generated columns (matchups.effective_points) as insertable — Postgres rejects them; sync code must omit them. Note: the leagues PK column is platform_league_uuid (NOT `id`). getBpaRecommendations reads player_projections with a PAGINATED .range loop (1000/page). The queue/auto-pick layer + the nav shell + the command-center home read NO Supabase of their own beyond the existing dashboard services; the test suite reads NO real Supabase (fake in-memory client — Rule 13)."
  - "src/lib/supabase/admin.ts bypasses RLS (secret key) — server-only, never import from client code. The draft-board, dashboard, BPA/tier/run, queue/auto-pick, AND the new nav-shell/command-center routes/actions deliberately do NOT use it — owner pages read/write through the RLS server client as the signed-in admin. Only /api/health + temp verification scripts (deleted after use) use the admin client. The Vitest suite uses NO Supabase client at all (pure functions + a fake in-memory client for the race)."
  - "Sleeper player catalog (/players/nfl): at most ONCE PER DAY — the cron route enforces it with a hard guard on sync_runs; the manual script path (npm run sync:players) bypasses the guard — manual cadence discipline applies. Per-league endpoints paced by the client's global 250ms min-gate (also absorbs 5s live-draft polling, ≤3 requests per executed tick). The BPA panel's per-pick recompute, tier recompute, the tier counter, run detection, the run badge, the queue/auto-pick, AND the Wave 4 dashboard + nav shell + command-center home make NO Sleeper calls — they read Supabase (or nothing/localStorage) only."
  - "api.sleeper.com (ADP/projections surface) is NOT api.sleeper.app/v1: undocumented, no contract — its consumer is the quarantined src/services/adp/ ONLY (own client, own pacing gate). The SAME fused ingestion run feeds BOTH adp_rankings AND player_projections from one fetch; never a second api.sleeper.com client or a separate projections fetch. 999.0 in any adp_* field is a 'no ADP in this format' sentinel — never data in adp_rankings; player_projections.stats retains the RAW object. ADP is never read from player_projections. Covering wiki: sleeper-api/projections-endpoint."
  - "League-format→ADP-format mapping is CENTRALIZED in resolveAdpScoringFormat (src/services/draft-board.ts, Nick-signed): superflex→'2qb' precedence, then ppr buckets; dynasty/IDP unmapped in v1; NO cross-format fallback. Any consumer calls this function — never re-derives. Same centralization for roster-slot layouts: parseRosterSlotLayout (same module) is the ONE parser of roster_settings_raw. FLEX_ELIGIBILITY is the ONE flex-eligibility home (exported from bpa/replacement.ts). DraftablePlayer (draft-board.ts) is the ONE minimal shape the shared handleDraft consumes; RunPick (bpa/runs.ts) the analogous minimal shape run detection consumes. ConnectedLeague (draft-board.ts) is the ONE connected-league shape — the nav-shell layout, the dashboard, and the login landing all read listConnectedLeagues, never re-query the leagues list ad hoc."

health: "clean"
