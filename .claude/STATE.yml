# .claude/STATE.yml
# Living "what happened last session" file. Overwritten completely at the end of every session — never patched.
# Last Updated: 2026-07-23

current_focus: "WAVE 4 SHARE-TOKEN DATA-EXPOSURE BOUNDARY COMPLETE (named-singleton #5) — the read-only share-link Access Model enforcement layer is live and verified. MIGRATION session, Rule 13 applied (blast radius confirmed: only fantasy-owned tables/functions). THREE Nick-signed Clarify (one batch, interdependent): (1) SPECTATOR TABLES = 8, not the build file's 6 — the reused getMatchups/getPlayerCard getters also read players (global catalog) + roster_players (league membership); Nick-signed correctness amendment added spectator SELECT policies to both. (2) RLS MECHANISM = anon key + x-share-token header + RLS policies `TO anon` as the actual data-exposure wall (not a secret-admin-scoped loader). (3) REGENERATE TOKEN GEN = DB generate_share_token() via RPC. BUILT — Migration 20260723072917: current_share_token() (STABLE, reads request.headers GUC → x-share-token, null-safe), regenerate_share_token(uuid) (SECURITY INVOKER; RLS UPDATE policy authorizes), grant select to anon on the 8 tables, spectator_share_read SELECT policies TO anon on all 8 (leagues by share_token; league_config/rosters/roster_players/matchups/standings/player_scores by league_id subselect; players by exists-valid-league) — all predicates (select current_share_token()) InitPlan-wrapped. Migration 20260723074134 (follow-up): revoke execute regenerate_share_token from anon (Supabase grants function EXECUTE to anon via default privileges, so revoke-from-public alone left anon able to invoke — a no-op the RLS UPDATE policy already blocked, but the revoke makes the boundary match its comment). NEW src/lib/supabase/spectator.ts (createClient(shareToken): anon, session-less, publishable key + x-share-token header; path-disambiguated per convention). NEW src/services/spectator.ts (loadSpectatorDashboard/loadSpectatorPlayerCard: resolve token→league via the anon client, then reuse dashboard.ts getters; pickWeek latest/honor-valid/null; invalid/blank/revoked → invalid_token; imports NOTHING draft-related). NEW src/app/(admin)/leagues/[leagueId]/dashboard/actions.ts (regenerateShareToken(leagueId): in-action getAdminAuthState gate → RPC via RLS server client → revalidate; typed regenerated/unauthorized/not_found). NEW src/services/spectator.test.ts (9 durable tests, RLS-enforcing recording fake client injected via vi.mock, Rule 13 no-live-write: draft_state/draft_sessions never queried, cross-league isolation A-vs-B per token, invalid/blank/bad-token not-found, week selection, player-card resolve+not-found). Verified: tsc+lint+build clean, npm test 23/23 (14 prior + 9 new), types regenerated. LIVE READ-ONLY RLS VERIFICATION on the real league (temp script deleted): anon+valid-token unlocks all 8 tables scoped (leagues 1/rosters 10/roster_players 174/matchups 180/standings 10/player_scores 1000/players 1000/league_config 1); draft_state 0 + draft_sessions 0 EVEN WITH the valid token; wrong-token → all 0; no-token → all 0; anon rpc regenerate → 42501 permission denied; regenerate round-trip revoked the old link (anon+OLD → 0), new link worked (anon+NEW → 1), then restored the exact prior token (net-zero, anon+OLD → 1). SELF-LOCATION FOR NEXT SESSION: 04's next unchecked item in file order is the SHARE-LINK SETTINGS PANEL UI (line 54 — copyable spectator URL + a regenerate button calling the now-existing regenerateShareToken action, on the admin dashboard only). That is the immediate next self-locatable fold (one small admin-UI surface). The fold AFTER it is the SPECTATOR UI SURFACE (route /share/[share_token] + the 5 mobile-first spectator components + page composition + the loading/empty/error resilience item + line 81's no-admin-markup render test) — a separate mobile-first rendering path that imports ZERO admin components and uses services/spectator.ts as its only data seam; likely its own artifact fold (possibly split). NO named singleton remains in Wave 4."

build_system:
  index: ".claude/BUILD_INDEX.md"
  files_location: ".claude/build/"
  total_files: 8
  current_build_files:
    - ".claude/build/04_league_dashboard.md"  # 🟡 — data-access 5/5 [x], freshness [x], 4 display components [x], page assembly [x], league selector [x], nav-shell 4/4 [x], SHARE-TOKEN INFRA 3/3 [x] + spectator data loader [x] + 2 data-boundary tests [x] (this session). Remaining: share-link settings panel UI [ ] (action exists — this is the panel), spectator surface 6 UI items [ ] (route + 4 components + composition), resilience loading/empty/error states [ ] + no-admin-markup render test [ ] (line 81, rides the spectator UI). NEXT self-locatable = share-link settings panel UI (line 54), then the spectator UI surface fold. NO named singleton left here.
    - ".claude/build/03b_draft_assistant_live_draft.md"  # 🟢 COMPLETE 2026-07-23. Nothing left.
    - ".claude/build/03a_draft_assistant_static_board.md"  # 🟡 — all five sub-sections complete; sole remainder = bye-week [>] (schedule-source-gated); goes 🟢 when that ships or is re-ruled
    - ".claude/build/02_data_pipeline.md"  # ESPN residue [!], cron cadence [~], finality promotion [>] (Nick-signed deferral) — nothing self-locatable here

nick_pending:
  - "SHARE LINK IS NOW TECHNICALLY ISSUABLE (new 2026-07-23): the share-token boundary is live, so a league's read-only spectator link works the moment its share_token is put in a /share URL — BUT the spectator UI page (/share/[token]) and the admin settings panel to copy/regenerate the link are NOT built yet (next folds). The MANUAL_SETUP 'decide when/whether to share the read-only spectator link' item stays open — your call, and there's nothing to actually hand a leaguemate until the spectator page ships. No action needed now."
  - "VISUAL CHECK BACKLOG — Wave 4's admin surfaces + Wave 3b's live-draft surfaces have never been eyeballed against a real signed-in owner session. Outstanding: the nav-shell (persistent sidebar Home/Dashboard/Draft + active highlight + reused selector + sign-out + 8 disabled Wave5/6 'coming soon' rows on tablet/PC, compact icon top-bar below md); the command-center HOME at /leagues/<id>; the MOVED dashboard at /leagues/<id>/dashboard; the slimmed draft-board header; ALL the 3b live-draft surfaces (draft queue + auto-pick pill, positional-run badge, tier-depth counter + BPA panel, live strip/status, recent-picks feed + Undo, roster cards, optimistic Draft fold, live-sync dimming); resilience degradation (only observable by forcing an error). NOTHING from THIS session is user-visible yet (the share-token boundary is DB/service-layer + the regenerate action has no UI button until the settings panel ships). The completed 2025 draft has no live clock, so live-draft timing behaviors still can't be fully exercised until a real 2026 draft."
  - "POST-LOGIN LANDING INCONSISTENCY (surfaced 2026-07-23, NOT changed — Nick's call): src/app/login/actions.ts still lands a fresh sign-in on the first league's DRAFT board (/leagues/<id>/draft) while root auto-land now lands on the command-center HOME. Both valid routes; leaving it respects the signed decision + fold boundary. If you'd prefer post-login to also land on the home, it's a one-line change to landingPath() in login/actions.ts. Not blocking."
  - "OPTIONAL FUTURE — superflex/best-ball run sensitivity (deferred 2026-07-23): positional-run-detection-draft-day.md mandates format-aware run sensitivity for superflex/2QB + best-ball; shipped detectPositionalRuns applies uniform thresholds (live league is 1QB redraft, no superflex connected). When one connects, make the QB threshold format-aware via resolveAdpScoringFormat — small change to runs.ts + a runs.test.ts case + the badge picks it up free. Auto-pick caps ARE already format-aware for free. Also a WIKI NOTE. Not blocking; its own tiny fold."
  - "OPTIONAL FUTURE / SCOPE QUESTION — demand-vs-panic run classification (WIKI NOTE, unchanged 2026-07-23): positional-run-detection-draft-day.md mandates classifying runs into demand-driven (tier-cliff) vs panic-driven (contagion/ahead-of-ADP) with OPPOSITE responses. Shipped positional-run sub-section does a count flag + a badge that COMPOSES the tier-drain signal in its tooltip — NOT the full classification. If wanted for v1 it needs a NEW registered build item / Nick-signed scope amendment. Not blocking."
  - "OPTIONAL FUTURE — per-league memory for 'my team' AND the draft queue: both client-side/re-pick-each-session by decision (queue is localStorage per league — survives reloads, not a cleared browser / another device). Durable cross-device state = a small future migration (per-league my-roster + draft_queue table) + tiny reads. Not blocking."
  - "FINALITY PROMOTION — build scheduling (design Nick-signed + registered 2026-07-22 as the [>] item in 02_data_pipeline.md's cron sub-section; mechanism in known_issues): build as its own standalone fold OR bundle with the season-start cadence session. HARD DEADLINE: before the 2026 NFL week 1→2 rollover (~2026-09-15; promotion first actually needed ~2026-09-18). Flip the item [>]→[ ] when its build session starts."
  - "BLOCKED ON EXTERNAL TIMING — nothing to do until ~mid-August 2026 (expected clear ~2026-08-19 → 2026-09-02): Wave 2 ESPN manual items (public/private per league, espn_s2/SWID for private, league IDs + season year). ESPN leagues commissioner-locked until 2026 season setup. When they open: resolve in a plain chat, flip them in MANUAL_SETUP_CHECKLIST.md, flip 02_data_pipeline.md's 9 ESPN items + 03c's [!]→[ ]. Per the backsweep practice (BUILD_PROTOCOL Amendment Hygiene) that plain chat must sweep the confirmations against already-registered 02/03c text AND against ALL shipped Wave 4 + Wave 3b surfaces (platform-agnostic by construction): recordManualPick, draft_sessions/poll orchestration, client live-sync seams, the DraftOrderMeta on-clock path (slot_to_roster_id is Sleeper-specific; ESPN needs its own draft-order source), the projections/BPA/tier/run layer, the QUEUE + AUTO-PICK layer, the RESILIENCE layer, the Wave 4 admin dashboard + NAV SHELL + command-center home, AND now the SHARE-TOKEN BOUNDARY + spectator loader (all platform-agnostic — an ESPN league joins listConnectedLeagues → the sidebar/home/dashboard automatically, and its share_token gets spectator RLS the same as any league; the loader reuses the platform-agnostic dashboard getters). Sweep any item text that assumed otherwise."
  - "SEASON START (~Sept 2026): revisit league-sync cadence — the specced 15-minute cron needs Vercel Pro (~$20/mo) or an alternative trigger; currently daily on Hobby per the 2026-07-22 decision. Flip 02_data_pipeline.md's cron-schedule item [~]→[x] when sub-daily ships. If Pro is bought, /api/cron/sync-adp is the ready mount point. NOTE: live-draft polling + BPA/tier/run/counter recompute + run badge + queue/auto-pick all do NOT depend on this (client-driven, no cron slot at any tier)."
  - "BYE-WEEK [>] ITEM (03a) still awaits its schedule-source decision (plain chat: pick a schedule source, then a build session ships table+ingestion+row wiring — flips 03a 🟢). No longer blocking anything. Related: the 3a board pool's fullName is also null for DST rows (renders the abbreviation fallback); the item-4 name compose exists in draft-picks.ts if a future correctness pass wants to reuse it for getDraftBoardData."
  - "When your 2026 Sleeper league renews (new league_id every season): run npm run sync:league -- <new_id> once to connect it; every daily league-state cron run (and npm run sync:sleeper) covers it after. The 2025 league (1257464256710856704) is fully ingested incl. its 170-pick draft. With auto-land live, the renewed league (season 2026 > 2025) becomes the COMMAND-CENTER HOME you land on after sign-in (full dashboard + draft board one click away in the sidebar). Its share_token automatically gets the spectator RLS the moment the league row exists — the /share link works as soon as the spectator page ships. The BPA + tier + run layer uses the LATEST ingested projections/ADP season (2026) regardless of the league's own season, so it is already current-market."
  - "Before Wave 5's waiver/FAAB recommendations item is built: run a dedicated scoping session to define the actual waiver/FAAB candidate scoring formula (opportunity-based — snap/target/touch share trends — is the recommended direction per wiki/topics/league-mechanics/waiver-wire-faab-strategy.md, but no formula exists yet)."

blockers:
  - "EXTERNAL TIMING (dated 2026-07-22): 02_data_pipeline.md's two ESPN sub-sections (9 items, [!]) are blocked until ESPN leagues open for the 2026 season — commissioner-locked, expected ~2026-08-19 → 2026-09-02. Not resolvable by Nick or any build session before then; do NOT guess visibility or fabricate placeholder league IDs (Nick's explicit instruction). Downstream: Wave 3c inherits this blocker. Does not stall the roadmap: with 3b 🟢, the nav-shell shipped, and the share-token boundary now live, the buildable frontier is WAVE 4's remaining UI — the share-link settings panel, then the spectator UI surface (route + mobile-first components + composition + resilience states) — plus 02's finality-promotion [>] and 03a's bye-week [>] whenever their plain-chat decisions land."

known_issues:
  - "npm audit reports 6 vulns (4 moderate, 2 high) — UNCHANGED 2026-07-23 (no deps added this session — the spectator client reuses the existing @supabase/supabase-js). Pre-existing 1 moderate postcss + 2 high sharp are transitive inside next@16.2.11's pinned deps (only offered 'fix' is downgrading next to 9.x, which is wrong). All dev-time/build-time, none shipped to clients. Re-check in later sessions."
  - "FINALITY-PROMOTION GAP (surfaced + designed + registered 2026-07-22 — still UNBUILT): in-season, matchups/player_scores rows get is_final=true only when league lifecycle status reads 'complete' at sync time; the scheduled sync is current-week-only, so finished weeks stay is_final=false all season. Covering wiki: sleeper-api/matchup-endpoint (finality Key Decision #3). Nick-signed design: previous-week re-sync + promotion inside the daily league-state cron (week-advance = games-complete proxy; Friday-or-later UTC stable-read match = trigger) + league-complete backstop sweep; needs a caller-driven finality extension to syncLeagueMatchups. Registered as the [>] item in 02_data_pipeline.md's cron sub-section. Zero live consequence until the season's first week rollover; HARD DEADLINE ~2026-09-15/18. WAVE 4 UI side SHIPPED (UnofficialChip + AsOfTime render is_final/fetched_at honestly, incl. through the spectator getters)."

warnings:
  - "ABSOLUTE RULE 13 (canonical: MASTER_CONTEXT.md 'Shared Database Protection'): the Supabase database is the shared multi-app 'prolabel' project — ~49 non-fantasy tables incl. live business production data (Elliott). Never supabase db reset; never migration repair touching foreign history (new foreign versions get empty local stub files); no raw SQL that could reach a non-fantasy table — verify every migration's table names against ARCHITECTURE.md's 'Database Schema (live)' inventory; collision check before any new table name; ANY ambiguity → stop and ask Nick; mandatory BLAST RADIUS: line on every report. Owner-write RLS must match Nick's specific auth.uid() via public.is_fantasy_admin(), never the blanket authenticated role. THIS SESSION (share-token boundary, MIGRATION): blast radius confirmed = ONLY fantasy-owned objects — spectator SELECT RLS + anon GRANT on the 8 fantasy tables (leagues, league_config, rosters, roster_players, matchups, standings, player_scores, players — all in the live inventory) + 2 new fantasy functions (current_share_token, regenerate_share_token). NO foreign prolabel object referenced/altered; owner fantasy_owner_all policies + is_fantasy_admin() UNCHANGED; draft_state/draft_sessions got NO spectator policy (verified live: 0 rows to anon even with a valid token). Anon policies exist ONLY on fantasy tables — zero blast radius to foreign tables."
  - "SHARE-TOKEN DATA-EXPOSURE BOUNDARY (live 2026-07-23): RLS is THE wall, not app code. Transport = the x-share-token request header → public.current_share_token() (reads the PostgREST request.headers GUC). spectator_share_read (FOR SELECT TO anon) on EIGHT tables gates access to exactly the token's league (+ the global players catalog to any valid-token holder). draft_state/draft_sessions NEVER get a spectator policy — unreachable to anon at any wave. The spectator client (src/lib/supabase/spectator.ts createClient(token)) is anon + session-less + the header; the loader (src/services/spectator.ts) resolves token→league then reuses the dashboard.ts getters and imports NOTHING draft-related. Owner regenerate == revoke via regenerate_share_token(uuid) RPC (SECURITY INVOKER; RLS UPDATE policy is authz; EXECUTE revoked from anon). Extend this boundary by adding a table's spectator SELECT policy in a NEW migration + granting anon SELECT — never by routing spectator reads through the secret admin client, never by adding a draft_state/draft_sessions policy, never by widening owner policies. share_token is NOT read by the sync pipeline (syncLeagueConfig never touches it on conflict), so regenerating is inert to ingestion."
  - "ADMIN/SPECTATOR UI BOUNDARY (Wave 4): src/components/dashboard/ + draft-board/ are ADMIN-surface UI — the UNBUILT spectator surface (04's spectator sub-section: /share/[token] route + its own mobile-first components) imports ZERO components from them; spectator gets its own components, its own rendering path, no login/nav-shell. The ONLY sanctioned sharing is data-access: services/spectator.ts is the spectator data seam and it MAY reuse services/dashboard.ts getters (getStandings/getMatchups/getPowerRankings/getPlayerCard/listScoredWeeks) with the token-scoped anon client — never a dashboard/draft-board React component. The regenerateShareToken action + its (unbuilt) settings panel are ADMIN-only. Freshness primitives UnofficialChip + AsOfTime are THE rendering of is_final/fetched_at and the spectator components will reuse the freshness DATA (not necessarily the admin components). Player-card open on the admin side is the URL-driven ?player= sheet (on the dashboard route)."
  - "TESTING INFRASTRUCTURE (Vitest, since 2026-07-23): THE durable runner (vitest.config.ts — node env, @/→src alias, include src/**/*.test.ts; scripts test=vitest run, test:watch=vitest). Tests CO-LOCATED as *.test.ts, typechecked + linted, never imported by a route. RULE-13 DISCIPLINE: never write to the shared prolabel DB from a test — DB behavior is modeled against a FAKE in-memory Supabase client. Current: 23 tests / 6 files (draft-picks race, board recompute, tiers recompute, runs window, draft-queue auto-pick, + NEW spectator.test.ts — 9 boundary tests against an RLS-ENFORCING recording fake client injected via vi.mock of lib/supabase/spectator: draft tables never queried, cross-league isolation per token, invalid/blank/bad-token not-found, week selection, player-card resolve). Extend this suite; the fake-client pattern (draft-picks.test.ts = PK ON-CONFLICT; spectator.test.ts = RLS scoping keyed on the presented token) is the template — never a live write, never a throwaway tsx harness."
  - "CLIENT-SIDE LIVE SYNC SEAMS (3b, unchanged 2026-07-23): live board path = ticker → pollActiveDraft (full listDraftPicks snapshot) → shell's fingerprint-deduped livePicks (+ optimistic pendingPicks + pollHealth + DraftOrderMeta + selfRosterId + queue store). Every live-enhancement region wrapped in its OWN ErrorBoundary; shell derivations feeding the static table guarded at source. Extend THIS path — never a second poll driver, merge, pick-write UI, undo/tier/board/run/QUEUE source. Invariants: 'drafted'/draftPending only from the client merge; confirmed snapshot row wins over pending; 'rostered' wins the overlay; ON-CLOCK IS A PROJECTION (projectOnClock; first-write-wins is the real arbiter). rosterLabel (player-row.tsx) is THE roster display convention."
  - "MANUAL-PICK WRITE PATH CENTRALIZATION (3b, unchanged): src/services/draft-picks.ts recordManualPick is the ONE local pick-write path (durable unit tests). AUTO-PICK writes through it (fireAutoPick → handleDraft → submitManualPick → recordManualPick, source='manual', undoable, conflict-safe — can NEVER write to Sleeper). Server-action auth pattern: gate INSIDE each action (getAdminAuthState) + RLS as the second wall; NOW SEVEN server actions incl. regenerateShareToken (dashboard/actions.ts) + fetchBpaRecommendations. listDraftPicks is THE ordered pick-snapshot read. Undo has its ONE UI (recent-picks feed)."
  - "BPA + TIER + RUN + QUEUE/AUTO-PICK LAYER CENTRALIZATION (3b, unchanged): src/services/bpa/ is the ONE home of projection scoring + VORP + tier + run detection + recommendations (durable Vitest board/tiers/runs). src/components/draft-board/ pure client layers: live-picks.ts, live-status.ts, draft-queue.ts. NEVER add caching, a second scoring/VORP/tier/run/queue derivation, a second projections read, a second recommendations query, a second draft-submit path, or a second run/queue source. Layout parsing = parseRosterSlotLayout, format mapping = resolveAdpScoringFormat (both draft-board.ts)."
  - "PLAYER_SCORES IS INGESTED, NEVER COMPUTED (Nick-signed canon): platform-scored values land as-received (Sleeper players_points; ESPN as-received when unblocked). NO compute-from-raw-stats path for ACTUAL scores in v1. PROJECTIONS ARE THE SANCTIONED EXCEPTION: player_projections stores component projection stats, scored by league_config weights at read time (bpa/scoring.ts) — projected value, never a substitute for actual player_scores. The Wave 4 dashboard + command-center home + the SPECTATOR loader all render player_scores/matchups/standings AS-RECEIVED (getStandings/getPowerRankings/getMatchups/getPlayerCard — all-play power computed from stored effective_points, never raw stats). Never invent score rows for unrostered weeks (FA-week gap accepted for v1, rendered not_rostered)."
  - "DARK MODE ONLY IS LOAD-BEARING IN CODE (live 2026-07-22): globals.css holds the app's single always-on token set in :root — NO light block, NO .dark override, NO theme toggle; the `dark` class on <html> keeps vendored shadcn dark:-variant tunings active. Never add a light token set, a prefers-color-scheme branch, or remove that class. ui/sonner.tsx hardcodes theme='dark'. --radius stays 0.625rem. Position identity tokens --pos-* are badges/chips ONLY; teal = interactive/live only; amber (--warning) = roster-need. The spectator surface (unbuilt) is mobile-first but stays on this same single dark token set (shadcn tokens only per its build items — no new palette)."
  - "OWNER AUTH SURFACE (live 2026-07-22, extended 2026-07-23): three walls — (1) src/proxy.ts (Next 16 proxy) refreshes the session per matched request + 307s unauth /leagues/* → /login; matcher EXCLUDES /api. (2) The (admin) layout gate uses getAdminAuthState (is_fantasy_admin RPC). (3) Per-table RLS (owner fantasy_owner_all + NOW the spectator_share_read anon layer on 8 tables). Server actions add a FOURTH gate: auth INSIDE each action — SEVEN actions now incl. regenerateShareToken. The share-token SPECTATOR surface is a SEPARATE no-login rendering path gated by share_token via RLS (current_share_token/x-share-token header), never these owner walls — it never signs in, never hits proxy's /leagues gate (it will live at /share/[token]), never the (admin) layout. ROOT AUTO-LAND → command-center home; login/actions.ts still lands post-sign-in on /draft (separate Nick-signed decision — see nick_pending)."
  - "src/lib/supabase/database.types.ts is generated — never hand-edit; regenerate via npm run gen:types after every schema migration. It deliberately contains the whole shared public schema (foreign prolabel tables + functions like todd_*/verify_pricing_password included) — expected, not drift. Now includes current_share_token + regenerate_share_token. Trap: generated Insert types list stored generated columns (matchups.effective_points) as insertable — Postgres rejects them; sync code must omit them. leagues PK column is platform_league_uuid (NOT `id`). getBpaRecommendations reads player_projections with a PAGINATED .range loop (1000/page)."
  - "src/lib/supabase/ has FOUR client factories, all named createClient, path-disambiguated (Supabase-docs convention): client.ts (browser publishable), server.ts (RSC/action cookie-bridged publishable), admin.ts (secret key, bypasses RLS, server-only — never client), and NOW spectator.ts (anon publishable + x-share-token header, session-less, server-only — the spectator loader's sole client). admin.ts bypasses RLS so the draft-board/dashboard/BPA/nav-shell/spectator paths deliberately do NOT use it — owner pages read/write through the RLS server client as the signed-in admin; the spectator loader reads through the anon spectator client as the RLS wall. Only /api/health + temp verification scripts (deleted after use) use the admin client. The Vitest suite uses NO real Supabase client (pure functions + fake in-memory clients — Rule 13)."
  - "Sleeper player catalog (/players/nfl): at most ONCE PER DAY (cron route hard-guards on sync_runs; manual script bypasses the guard — cadence discipline applies). Per-league endpoints paced by the client's 250ms min-gate. The Wave 4 dashboard + nav shell + command-center home + the SPECTATOR loader make NO Sleeper calls — they read Supabase (owner via RLS server client, spectator via the anon token-scoped client) only. api.sleeper.com (ADP/projections) is quarantined in src/services/adp/ ONLY; its consumers (BPA/tier) are admin-only, never the spectator surface. resolveAdpScoringFormat + parseRosterSlotLayout are the ONE format/layout parsers; FLEX_ELIGIBILITY (bpa/replacement.ts) the ONE flex-eligibility home; ConnectedLeague (draft-board.ts) the ONE connected-league shape (nav-shell/dashboard/login all read listConnectedLeagues)."

health: "clean"
