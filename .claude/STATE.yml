# .claude/STATE.yml
# Living "what happened last session" file. Overwritten completely at the end of every session — never patched.
# Last Updated: 2026-07-22

current_focus: "WAVE 3B — LIVE STATUS INDICATOR + LIVE-DRAFT STRIP FOLD COMPLETE (2026-07-22, feature-build, 2-item fold declared at session start per the live-behavior ceiling): client-side live sync item 5 + Live-board-UI-extensions item 2. Shipped: (1) Toolbar live status indicator — DraftPollTicker gained a required onStatus callback reporting each EXECUTED tick (skipped/stand-down ticks report nothing; polling authority stays server-side); pure live-status.ts layer (LivePollHealth/applyTickReport/isDegraded/formatSyncAge, live-picks pattern); shell aggregates + resets on session re-arm (render-time epoch adjustment — lint's set-state-in-effect rule shaped both this and the indicator's no-immediate-setNow design); LiveStatusIndicator renders via a new statusSlot on DraftSessionToggle. All Nick-signed: beside the Draft live dot, degraded at 2 consecutive failures (muted tier, instant recovery), relative live-updating 'synced Ns ago', hidden when inactive + 'Paused — tab in background' while hidden. Verified 18/18 pure. CLIENT-SIDE LIVE SYNC SUB-SECTION NOW 5/5 COMPLETE. (2) Live-draft strip (pick/round/on-clock) — Nick-signed CORRECTNESS AMENDMENT: order source is the selected draft object's slot_to_roster_id via the poll path (the item's 'from league_config' premise was stale — derived_config has no draft-order concept per the ADR), never persisted. DraftOrderMeta extracted in syncLeagueDraftState → rides DraftPollSyncOutcome.success → ticker onDraftOrder (successful syncs only; failures keep last known) → shell (stringify bail-out) → pure projectOnClock (snake/linear, reversal_round flip for rounds>=reversal DECLARED as the 3RR interpretation, contiguous-map guard so a partial map reads unknown instead of misprojecting, auction honestly unknown, 'complete' past rounds×slots) → LiveDraftStrip between header and board (session-active only; Pick N = computeNextPickNumber(snapshot ∪ pending), Round = ceil(pick/league_size) omitted when size unknown, on-clock label via player-row's now-exported rosterLabel). Projection accepted plain-label (Nick-signed — recorded picks stay ground truth). LIVE FINDING: the drafts-ARRAY element does NOT carry slot_to_roster_id (only user-keyed draft_order — matches the wiki's three-layer split); added best-effort GET /draft/{draft_id} detail fallback (one extra paced request per executed sync; failure degrades metadata, never the sync). Verified 22/22 incl. projectOnClock matching ALL 170 recorded picks on the real league (non-identity slot map: 3→5, 9→3) and pick 171 → complete; net-zero DB (picksWritten=0). tsc/lint/build clean both items; commits d225e29 + 3c0d7af (per-item granularity) + session-end commit. Log _32 rotated to ARCHIVE. SELF-LOCATION FOR NEXT SESSION: 03b's first unchecked item = Live-board-UI-extensions item 3 (extend the Wave 3a roster/positional-need panel to update live as picks land from any source — NOTE: RosterPanel currently derives from 'rostered' rows only and drafted/pending overlay rows are invisible to it BY DESIGN (the merge marks them 'drafted' without roster attribution in the player pool) — but every livePicks RecordedPick carries nativeRosterId, so the live extension derives from livePicks + rosters props already in the shell; expect a Clarify on how live picks blend with the static rostered baseline on a completed-season league). Then item 4 (recent-picks feed — natural home for the still-UI-less undo action per the standing warning; surfaces out-of-pool picks the merge skips) and item 5 (available-only liveness — already live via the merge, likely verification annotation only). Live-behavior ceiling still applies (2–3 items); declare the fold at session start. 04's nav-shell sub-section remains the alternate self-location point."

build_system:
  index: ".claude/BUILD_INDEX.md"
  files_location: ".claude/build/"
  total_files: 8
  current_build_files:
    - ".claude/build/03b_draft_assistant_live_draft.md"  # 🟡 — write path 4/4 [x], orchestration 3/3 [x], client-side live sync 5/5 [x] COMPLETE, UI extensions 2/5 [x] (2026-07-22); next: UI extensions items 3–5 per next session's declared fold (live roster panel, recent-picks feed, available-only verification)
    - ".claude/build/04_league_dashboard.md"  # 🟡 — data-access 5/5 [x], freshness [x], 4 display components [x], page assembly [x], league selector [x]; next fold: nav-shell sub-section (share-link panel deferred to the share-token singleton)
    - ".claude/build/03a_draft_assistant_static_board.md"  # 🟡 — all five sub-sections complete; sole remainder = bye-week [>] (schedule-source-gated); goes 🟢 when that ships or is re-ruled
    - ".claude/build/02_data_pipeline.md"  # ESPN residue [!], cron cadence [~], finality promotion [>] (Nick-signed deferral) — nothing self-locatable here

nick_pending:
  - "QUICK VISUAL CHECK on your next sign-in (carried + extended): NEW this session — with a draft session started, the header live cluster reads 'Draft live · Sleeper · syncing…' then 'synced Ns ago' ticking every second after the first 5s poll; a slim strip appears between the header and the board reading 'Pick N · Round R · On the clock: <team>' (on-clock appears after the first poll tick; on the completed 2025 league it should instead read 'Draft complete · all 170 picks recorded'); backgrounding the tab and returning should briefly show the paused/stale state then catch up. CARRIED optimistic-fold checks: available rows' Status cells show a Draft button while live; clicking opens the roster picker; chosen row flips 'Drafting…' → 'Drafted · Pick N'; conflicts/failures roll back with a bottom-corner sonner toast. CARRIED live-sync checks: picks land within ~5s without refresh; 40 drafted-but-unrostered players on the 2025 league read dimmed 'Drafted · Pick N' (overlay-always, honest history); 'Available only' excludes them. CARRIED orchestration checks: Start draft (outline, Play icon) → pulsing teal 'Draft live' dot + destructive End draft. CARRIED dashboard checks at /leagues/<id>: header, week pills, mirrored matchup cards (unofficial chips + as-of captions), standings, power rankings, ?player= side sheet; default week 18 shows 'No opponent' singles — pick week 17 for full H2H. CARRIED draft-board checks: rows, badges, filter chips, sort headers, roster/need sidebar, skeleton, empty states; no ADP/error banner on the healthy league."
  - "FINALITY PROMOTION — build scheduling (design Nick-signed + registered 2026-07-22 as the [>] item in 02_data_pipeline.md's cron sub-section; see known_issues for the mechanism): build as its own standalone fold, OR bundle with the season-start cadence session below — whichever comes first. HARD DEADLINE: before the 2026 NFL week 1→2 rollover (~2026-09-15; promotion first actually needed ~2026-09-18). Flip the item [>]→[ ] when its build session starts."
  - "BLOCKED ON EXTERNAL TIMING — nothing to do until ~mid-August 2026 (recorded 2026-07-22; expected clear ~2026-08-19 → 2026-09-02): Wave 2 ESPN manual items (public/private per league, espn_s2/SWID for private leagues, league IDs + season year). ESPN leagues are commissioner-locked until 2026 season setup. When they open: resolve the three items in a plain chat, flip them in MANUAL_SETUP_CHECKLIST.md, and flip 02_data_pipeline.md's 9 ESPN items [!]→[ ] so build sessions self-locate onto them again. Per the backsweep practice (BUILD_PROTOCOL Amendment Hygiene), that plain chat must also sweep the confirmations against already-registered 02/03c item text for staleness (03c_draft_assistant_espn_and_auction.md is the restructured ESPN+auction file, flip its [!] items back to [ ] alongside 02's) — and against Wave 4's shipped surfaces (platform-agnostic by construction) — and against 3b's shipped write path (recordManualPick is platform-agnostic; 03c's auction path extends it via the existing amount field on the same handleDraft seam) — and against the draft_sessions/poll orchestration (activateDraftSession/runActiveDraftPoll return skipped_non_sleeper for non-Sleeper leagues by design; 03c's ESPN poll path extends runRecordedDraftPoll; the ESPN in-draft rate-limit judgment call in MANUAL_SETUP's Wave 3b items applies to that extension) — and against the client-side live sync seams (listDraftPicks/live-picks merge/optimistic overlay are platform-agnostic; 03c inherits the same snapshot/merge/optimistic path unchanged) — and NEW: against the DraftOrderMeta on-clock path (slot_to_roster_id/reversal_round are Sleeper draft-object fields; ESPN draft-order projection will need its own source per the espn-api draft-detail wiki when 03c builds — the strip degrades to pick/round honestly for leagues without order metadata, so nothing breaks meanwhile)."
  - "SEASON START (~Sept 2026): revisit league-sync cadence — the specced 15-minute cron needs Vercel Pro (~$20/mo) or an alternative trigger; currently daily on Hobby per your 2026-07-22 decision (MANUAL_SETUP decision of record). Flipping 02_data_pipeline.md's cron-schedule item [~]→[x] happens when the sub-daily cadence ships. If Pro is bought, /api/cron/sync-adp is the ready mount point for a dedicated ADP schedule slot (currently piggybacked on sync-leagues). This session is also the fallback vehicle for the finality-promotion build (entry above) if it hasn't shipped as its own fold by then. NOTE: live-draft polling does NOT depend on this — it's client-driven (2026-07-22 orchestration decision) and needs no cron slot at any plan tier."
  - "BYE-WEEK [>] ITEM (03a) still awaits its schedule-source decision (plain chat: pick a schedule source, then a build session ships table+ingestion+row wiring — flips 03a 🟢). No longer blocking anything, but it's the only remainder between 03a and 🟢."
  - "When your 2026 Sleeper league renews (new league_id every season): run npm run sync:league -- <new_id> once to connect it; every daily league-state cron run (and npm run sync:sleeper) covers it from then on. The 2025 league (1257464256710856704) is fully ingested including its 170-pick draft. Note: with auto-land live, the renewed league (season 2026 > 2025) becomes the DASHBOARD you land on after sign-in — expected behavior, newest season sorts first. Draft-day note: the renewed league is where Start draft + live polling + click-to-draft + the live strip/status indicator will actually run in anger."
  - "Before Wave 5's waiver/FAAB recommendations item is built: run a dedicated scoping session to define the actual waiver/FAAB candidate scoring formula (opportunity-based — snap/target/touch share trends — is the recommended direction per wiki/topics/league-mechanics/waiver-wire-faab-strategy.md, but no formula exists yet)."

blockers:
  - "EXTERNAL TIMING (dated 2026-07-22): 02_data_pipeline.md's two ESPN sub-sections (9 items, [!]) are blocked until ESPN leagues open for the 2026 season — commissioner-locked, expected ~2026-08-19 → 2026-09-02. Not resolvable by Nick or any build session before then; do NOT guess visibility or fabricate placeholder league IDs (Nick's explicit instruction). Downstream: Wave 3c inherits this blocker (ESPN draft mechanics + auction). Does not stall the roadmap: 3b and Wave 4 both have buildable [ ] items."

known_issues:
  - "npm audit reports 6 vulns (4 moderate, 2 high) — last re-checked live 2026-07-22 (optimistic fold; this session added no dependencies): pre-existing 1 moderate postcss + 2 high sharp are transitive inside next@16.2.11's pinned deps (only offered 'fix' is downgrading next to 9.x, which is wrong). All dev-time/build-time, none shipped to clients. Re-check in later sessions."
  - "FINALITY-PROMOTION GAP (surfaced 2026-07-22 by the cron session; investigated + designed + registered 2026-07-22 non-code session — still UNBUILT, mechanism unchanged until its build session): in-season, matchups/player_scores rows get is_final=true only when league lifecycle status reads 'complete' at sync time; the scheduled sync is current-week-only, so finished weeks stay is_final=false all season. Covering wiki: sleeper-api/matchup-endpoint (stat-correction window + finality Key Decision #3). Nick-signed design (2026-07-22): previous-week re-sync + promotion inside the daily league-state cron (week-advance = games-complete proxy; Friday-or-later UTC stable-read match = trigger — both Rule 12 genuine-silence sign-offs) + league-complete backstop sweep; needs a caller-driven finality extension to syncLeagueMatchups. Registered as the [>] item in 02_data_pipeline.md's cron sub-section. Zero live consequence until the season's first week rollover; HARD DEADLINE ~2026-09-15/18 (build scheduling in nick_pending). WAVE 4 STATUS: UI surfacing side SHIPPED and MOUNTED (UnofficialChip + AsOfTime render is_final/fetched_at honestly) — in-season, most rows will honestly read 'unofficial' until promotion ships; correct display, not a bug."

warnings:
  - "ABSOLUTE RULE 13 (canonical: MASTER_CONTEXT.md 'Shared Database Protection — Non-Fantasy Data Is Untouchable'): the Supabase database is the shared multi-app 'prolabel' project — ~49 non-fantasy tables incl. live business production data (Elliott). Never supabase db reset; never migration repair touching foreign history (new foreign versions get empty local stub files, never repair); no raw SQL that could reach a non-fantasy table — verify every migration's table names against ARCHITECTURE.md's 'Database Schema (live)' inventory; collision check before any new table name; ANY ambiguity → stop and ask Nick; mandatory BLAST RADIUS: line on every completion report. Owner-write RLS policies must match Nick's specific auth.uid(), never the blanket authenticated role — realized as public.is_fantasy_admin(); rotate the admin there, never by editing per-table policies. Operational restatement: ARCHITECTURE.md 'Shared-Database Constraints'. WAVE 4 SPECIFIC: the share-token RLS sub-section adds SELECT policies on six fantasy tables — a named singleton fold (Folding Policy exception #5) with its boundary tests; owner policies stay pinned to is_fantasy_admin(). draft_sessions (and draft_state) are NEVER on any spectator-policy list, at any wave. Related standing decline (2026-07-22 live-sync Clarify): Supabase Realtime was declined for the live board partly because it would touch the shared project's realtime publication — interval re-fetch needs no shared-infra config."
  - "CLIENT-SIDE LIVE SYNC SEAMS (updated 2026-07-22, status/strip fold — sub-section now 5/5): the live board path is ticker → pollActiveDraft (returns full listDraftPicks snapshot per executed poll) → shell's fingerprint-deduped livePicks state (+ optimistic pendingPicks overlay + NEW pollHealth aggregation + NEW DraftOrderMeta state) → live-picks.ts mergePicksIntoPlayers → BOTH board regions. Extend THIS path for remaining 03b items — never a second poll driver, never a second merge, never a second pick-write UI path, and now also: never a second tick-outcome reporter (onStatus is THE channel; ticker reports EXECUTED ticks only — skipped/stand-down ticks report nothing by design) and never a second order-metadata source (DraftOrderMeta rides DraftPollSyncOutcome.success via onDraftOrder; failed syncs keep the last known order deliberately). Key invariants: 'drafted' availability AND draftPending are produced ONLY by the client merge; a confirmed snapshot row ALWAYS wins over pending; 'rostered' wins over the whole overlay; overlay applies regardless of session active (Nick-signed overlay-always); out-of-pool picks skipped by the merge (recent-picks feed item surfaces them later); pollActiveDraft deliberately does NOT revalidate on picks; full-snapshot-per-tick is load-bearing (undo deletions self-heal — never switch to incremental); adoptPick is the ONE early-adoption path into the snapshot; next pick number = computeNextPickNumber (max of snapshot ∪ pending, +1) — the server's (league_id, pick_number) constraint is the real arbiter. ON-CLOCK IS A PROJECTION (Nick-signed): projectOnClock (live-picks.ts) computes from slot_to_roster_id + snake/reversal math for DISPLAY ONLY — recorded picks are ground truth, the projection must never drive a write, a validation, or an auto-pick decision. Wire truth (verified live 2026-07-22): the /league/{id}/drafts ARRAY elements omit slot_to_roster_id (they carry only user-keyed draft_order — deliberately unused); syncLeagueDraftState's resolveDraftOrderMeta falls back to GET /draft/{draft_id} best-effort. rosterLabel (player-row.tsx, now exported) is THE roster display convention on the draft surface."
  - "DRAFT-SESSION ORCHESTRATION CENTRALIZATION (live 2026-07-22): services/draft-sessions.ts is the ONE home of the is_draft_active semantics — the 6h soft TTL is computed at read time there (DRAFT_ACTIVE_TTL_HOURS; no job ever mutates the row) and runActiveDraftPoll's TTL-aware check is the AUTHORITATIVE gate on elevated polling (clients only trigger; a stale tab can never force a Sleeper request — and a hidden tab makes no requests at all). Elevated cadence is client-driven at 5s (DraftPollTicker → pollActiveDraft action) because Hobby crons are daily-only — never add a cron schedule for it; /api/cron/sync-draft stays the unscheduled manual sweep; normal cadence = the daily league-state chain. Every EXECUTED poll records a sync_runs draft_poll row (Nick-signed); declined ticks record nothing by definition. Request budget per executed tick is now up to 3 paced Sleeper calls (drafts array + picks + the detail-endpoint fallback when the array omits slot_to_roster_id — in practice always, so 3) — still far inside the client's 250ms global gate and Sleeper's ~1,000 req/min ceiling at 5s cadence."
  - "MANUAL-PICK WRITE PATH CENTRALIZATION (live 2026-07-22): src/services/draft-picks.ts recordManualPick is the ONE local pick-write path — 03b's auto-pick MUST write through it (build-file mandate), and no second draft_state insert path may ever appear outside it and the platform pollers. Its ONE UI caller is the shell's handleDraft (optimistic path) — future draft triggers (BPA panel one-click item, auto-pick, 03c auction bid entry) wire into handleDraft/the same action, never a parallel submit path. Server-action auth pattern to reuse: the gate lives INSIDE each action (getAdminAuthState → typed unauthorized) + RLS as the second wall; owner actions use the RLS server client, never the secret-key client. draft_state rows are immutable-once-written — conflict responses return the authoritative row, which the shell adopts. Undo has a service+action but still NO UI — surface it deliberately in the recent-picks feed item (its natural home), not ad hoc."
  - "PLAYER_SCORES IS INGESTED, NEVER COMPUTED (Nick-signed canon, re-confirmed 2026-07-22 by the Wave 4 amendment): platform-scored values land as-received (Sleeper players_points — league-scored by Sleeper per each league's own settings; ESPN as-received when unblocked). There is NO compute-from-raw-stats path anywhere in v1 and NO raw-stats table/sync/wiki-covered source. Never 'add' scoring computation, never substitute Sleeper's generic pts_std/pts_ppr preset fields for league-scored values, and never invent score rows for unrostered weeks (FA-week gap accepted for v1 — rendered as explicit not_rostered entries)."
  - "DASHBOARD QUERY SERVICE CENTRALIZATION (live 2026-07-22): src/services/dashboard.ts is the ONE home of (a) the standings ordering — fetchRankedStandings, wins desc → points_for desc → native_roster_id asc, Nick-signed under declared wiki silence — and (b) the power-ranking formula (All-Play record; regular-season weeks via raw playoff_week_start; ties=half-wins; lowConfidence <6 weeks); listScoredWeeks is likewise the ONE week-range/default-week source for dashboard surfaces. The Wave 4 display components (src/components/dashboard/) render these results AS-IS. The spectator loader passes its own share_token-scoped client (shared data-access logic is sanctioned; shared UI never is). Boundary discipline: the module never selects share_token/owner_id/native_league_id and has zero draft_state references — keep it that way when extending."
  - "ADMIN/SPECTATOR UI BOUNDARY (Wave 4, live 2026-07-22): src/components/dashboard/ is ADMIN-surface UI — the spectator surface must import ZERO components from it (or from draft-board/ or ui/dropdown-menu's draft usages); spectator gets its own components (04's spectator sub-section). Shared data-access (services/dashboard.ts) is the ONLY sanctioned sharing. Within the admin surface, cross-feature reuse is fine (PlayerCard reuses draft-board PositionBadge/InjuryChip; the dashboard page reuses LeagueSelector via subPath — Nick-signed). Freshness primitives: UnofficialChip + AsOfTime (components/dashboard/) are THE rendering of is_final/fetched_at — reuse them, never invent a second treatment (muted tier, never a status color; deterministic UTC captions). Slot-by-slot matchup pairing is impossible from synced data (was_starter only) — never fake a slot spine. The player-card open mechanism is the URL-driven ?player= sheet — future admin surfaces reuse this pattern/URL contract; query params deliberately do not survive a league switch."
  - "DARK MODE ONLY IS LOAD-BEARING IN CODE (live 2026-07-22): globals.css holds the app's single always-on token set in :root — deliberately NO light block, NO .dark override block, NO theme toggle; the `dark` class on <html> is a permanent marker keeping vendored shadcn dark:-variant tunings active. Never add a light token set, a prefers-color-scheme branch, or remove that class. ui/sonner.tsx hardcodes theme='dark' for the same reason — never wire it to a theme system. --radius stays 0.625rem (Nick-signed): containers rounded-xl, buttons/chips pills. input.tsx's fill is bg-well by design. Position identity tokens --pos-qb/rb/wr/te/k/def are badges/chips ONLY — never row tints; RB is deliberately green, not the brand teal (teal = interactive/live only; sanctioned for positive deltas — and for the Draft live indicator, which is exactly the live semantic; the 'Drafting…' pending cue and the status indicator/degraded text are deliberately muted-tier, NOT teal)."
  - "OWNER AUTH SURFACE (live 2026-07-22) — three walls, keep all three: (1) src/proxy.ts (Next 16 proxy, NOT middleware.ts) refreshes the Supabase session per matched request and 307s unauth /leagues/* to /login — its matcher deliberately EXCLUDES /api so the cron Bearer surface never sees cookie plumbing. (2) The (admin) layout gate uses getAdminAuthState (src/lib/supabase/auth.ts → is_fantasy_admin RPC) — never hardcode the admin UUID in app code; a signed-in non-fantasy prolabel user is a REAL case and must get the zero-data NotAuthorizedCard. (3) Per-table RLS. Unauth/non-admin responses must never contain league data; both league routes' error.tsx boundaries render NO league data. ROOT AUTO-LAND targets the dashboard (/leagues/<id>). Server actions add a FOURTH gate point: auth INSIDE each action — six actions follow it (submitManualPick/undoManualPick/startDraftSession/endDraftSession/pollActiveDraft + login's signOut pattern). WAVE 4 SPECIFIC: the spectator surface is a genuinely separate rendering path (share_token-gated, zero admin imports)."
  - "src/lib/supabase/database.types.ts is generated — never hand-edit; regenerate via npm run gen:types after every schema migration. It deliberately contains the whole shared public schema (foreign prolabel tables included) — expected, not drift. Trap: generated Insert types list stored generated columns (matchups.effective_points) as insertable — Postgres rejects them; sync code must omit them."
  - "src/lib/supabase/admin.ts bypasses RLS (secret key) — server-only, never import from client code; its env var is deliberately not NEXT_PUBLIC_ so a client-bundle import fails loudly instead of leaking. The draft-board and dashboard routes deliberately do NOT use it — owner pages read through the RLS server client as the signed-in admin, and owner server actions write through it the same way."
  - "Sleeper player catalog (/players/nfl): at most ONCE PER DAY — Sleeper's most load-bearing rate rule. The cron route (/api/cron/sync-players) enforces this with a hard guard on sync_runs (running/success within 24h → skip; failure rows allow a genuine retry; guard read error → fail closed). BUT the manual script path (npm run sync:players) calls the service directly and BYPASSES the guard — manual cadence discipline still applies there. The orchestrator still deliberately EXCLUDES the catalog. Per-league endpoints remain paced by the client's global 250ms min-gap gate — which also absorbs the 5s live-draft polling (now ≤3 requests per executed tick incl. the draft-detail fallback — 0.6 req/s worst case per league, far inside the ~1,000 req/min ceiling)."
  - "api.sleeper.com (ADP/projections surface) is NOT api.sleeper.app/v1: undocumented, no contract, can change shape or vanish without notice — its consumer is the quarantined src/services/adp/ ONLY (own client, own pacing gate; documented-host rate knowledge does NOT transfer). Ingestion is validate-before-swap: a failed/implausible fetch never overwrites the last good adp_rankings snapshot. 999.0 in any adp_* field is a 'no ADP in this format' sentinel — never data. The adp_* field inventory is OPEN — ingestion is pattern-based; never hardcode a closed format list anywhere downstream. Covering wiki: sleeper-api/projections-endpoint."
  - "League-format→ADP-format mapping is CENTRALIZED in resolveAdpScoringFormat (src/services/draft-board.ts, Nick-signed 2026-07-22): superflex→'2qb' precedence, then ppr buckets; dynasty/IDP unmapped in v1; NO cross-format fallback anywhere. Any future consumer calls this function — never re-derives its own mapping. Same centralization rule for roster-slot layouts: parseRosterSlotLayout (same module) is the ONE parser of roster_settings_raw — Wave 3b BPA reuses it, never re-derives. ADP-absence classification is likewise centralized in deriveAdpNoticeKind (src/components/draft-board/adp-notice.tsx)."

health: "clean"
