# .claude/STATE.yml
# Living "what happened last session" file. Overwritten completely at the end of every session — never patched.
# Last Updated: 2026-07-22

current_focus: "WAVE 3A — TWO SUB-SECTIONS COMPLETE (2026-07-22, two folds same conversation, second Nick-directed): (1) 'ADP schema + ingestion' (9 items — see log _20): adp_rankings live (long per-format rows), quarantined src/services/adp/ pipeline against api.sleeper.com, sync_runs 'adp_ingestion' source, /api/cron/sync-adp (secret-gated, unscheduled) + daily piggyback on the 10:00 UTC sync-leagues cron; production-verified (2,946 rows / 10 formats / season 2026; deployed route 401-unauth + 200-auth with sync_runs telemetry row). (2) 'Draft-board data layer' (6 items — see log _21): src/services/draft-board.ts getDraftBoardData() — merged ADP-ordered pool (Nick-signed: ADP carriers ∪ league-rostered), explicit availability from roster_players with team attribution, league context from derived_config, and resolveAdpScoringFormat() — the Nick-signed bucketed league-format→scoring_format mapping (superflex→'2qb' precedence; ppr≥1→'ppr', 0<ppr<1→'half_ppr', 0→'std'; dynasty/IDP unmapped v1; NO cross-format fallback), which CLOSED the format-mapping known_issue from the first fold. ADP season = latest ingested snapshot (Nick-signed). Boundary discipline live-asserted: share_token/owner_id/native_league_id absent from every response; malformed/unknown league_id → typed not_found. Live-verified against the real league: 276-player pool, top-5 in market order, all invariants pass. GOVERNANCE (Nick-directed this session): BUILD_PROTOCOL.md gained 'Amendment Hygiene — Standing Practices' — (1) confirmed-decision backsweep (confirmations sweep back against already-registered items at confirmation time, not build time); (2) capacity-collision annotation (items implying capacity-limited resources carry the documented constraint in their own text so it surfaces at Clarify). NEXT BUILD SESSION: 03a's next unchecked item — the 'Draft-board route + page shell' sub-section (admin-only RSC route + tablet/PC-first shell + league selector + header/toolbar; 4 items). Note for that session: the route needs an auth decision (server client + real sign-in vs interim approach) — nick_pending's sign-in entry is now load-bearing; getDraftBoardData takes the caller's Supabase client precisely so the route layer owns this choice. First scheduled ADP piggyback fires with the 2026-07-23 10:00 UTC sync-leagues run — check its sync_runs row (source='adp_ingestion') and adpOk in the response. Catalog cron note (carried): 2026-07-23 09:00 UTC run will correctly record a 'skipped' row; first real scheduled catalog fetch expected 2026-07-24 ~09:00 UTC."

build_system:
  index: ".claude/BUILD_INDEX.md"
  files_location: ".claude/build/"
  total_files: 7
  current_build_files:
    - ".claude/build/03a_draft_assistant_static_board.md"  # 🟡 — ADP + data-layer sub-sections done; route + page shell is the next unchecked sub-section
    - ".claude/build/02_data_pipeline.md"  # ESPN residue [!], cron cadence [~], finality promotion [>] (Nick-signed deferral) — nothing self-locatable here

nick_pending:
  - "FINALITY PROMOTION — build scheduling (design Nick-signed + registered 2026-07-22 as the [>] item in 02_data_pipeline.md's cron sub-section; see known_issues for the mechanism): build as its own standalone fold now that Wave 3a is progressing, OR bundle with the season-start cadence session below — whichever comes first. HARD DEADLINE: before the 2026 NFL week 1→2 rollover (~2026-09-15; promotion first actually needed ~2026-09-18). Flip the item [>]→[ ] when its build session starts."
  - "BLOCKED ON EXTERNAL TIMING — nothing to do until ~mid-August 2026 (recorded 2026-07-22; expected clear ~2026-08-19 → 2026-09-02): Wave 2 ESPN manual items (public/private per league, espn_s2/SWID for private leagues, league IDs + season year). ESPN leagues are commissioner-locked until 2026 season setup. When they open: resolve the three items in a plain chat, flip them in MANUAL_SETUP_CHECKLIST.md, and flip 02_data_pipeline.md's 9 ESPN items [!]→[ ] so build sessions self-locate onto them again. Per the new backsweep practice (BUILD_PROTOCOL Amendment Hygiene), that plain chat must also sweep the confirmations against already-registered 02/03b item text for staleness."
  - "SEASON START (~Sept 2026): revisit league-sync cadence — the specced 15-minute cron needs Vercel Pro (~$20/mo) or an alternative trigger; currently daily on Hobby per your 2026-07-22 decision (MANUAL_SETUP decision of record). Flipping 02_data_pipeline.md's cron-schedule item [~]→[x] happens when the sub-daily cadence ships. If Pro is bought, /api/cron/sync-adp is the ready mount point for a dedicated ADP schedule slot (currently piggybacked on sync-leagues). This session is also the fallback vehicle for the finality-promotion build (entry above) if it hasn't shipped as its own fold by then."
  - "SIGN-IN CHECK — now load-bearing for the NEXT session, not just Wave 4: 03a's route + page shell sub-section builds the admin-only draft-board route, and the data layer deliberately takes the caller's Supabase client so the route owns the auth decision. Make sure you can sign in as nick@prolabelco.com (pre-existing account, created 2026-05-15; reset via the Supabase dashboard if forgotten). No session has exercised a real sign-in yet — expect the route session to ask about the auth approach at Clarify."
  - "When your 2026 Sleeper league renews (new league_id every season): run npm run sync:league -- <new_id> once to connect it; every daily league-state cron run (and npm run sync:sleeper) covers it from then on. The 2025 league (1257464256710856704) is fully ingested including its 170-pick draft."
  - "Before Wave 5's waiver/FAAB recommendations item is built: run a dedicated scoping session to define the actual waiver/FAAB candidate scoring formula (opportunity-based — snap/target/touch share trends — is the recommended direction per wiki/topics/league-mechanics/waiver-wire-faab-strategy.md, but no formula exists yet)."

blockers:
  - "EXTERNAL TIMING (dated 2026-07-22): 02_data_pipeline.md's two ESPN sub-sections (9 items, [!]) are blocked until ESPN leagues open for the 2026 season — commissioner-locked, expected ~2026-08-19 → 2026-09-02. Not resolvable by Nick or any build session before then; do NOT guess visibility or fabricate placeholder league IDs (Nick's explicit instruction). Downstream: Wave 3b inherits this blocker (needs Wave 2 ESPN). NOT blocked: Wave 3a — ADP + data-layer sub-sections shipped 2026-07-22; the remaining 3a sub-sections (route/shell, player list, states) are Sleeper-only-sufficient per 03a scope + BUILD_INDEX dependency notes."

known_issues:
  - "npm audit reports 6 vulns (4 moderate, 2 high) — last checked live 2026-07-22 (tsx install session): pre-existing 1 moderate postcss + 2 high sharp are transitive inside next@16.2.11's pinned deps (only offered 'fix' is downgrading next to 9.x, which is wrong). All dev-time/build-time, none shipped to clients. No new packages since. Re-check in later sessions."
  - "derived_config.ir_slot_count counts IR labels in roster_positions — but Nick's own league proves IR capacity can be expressed only via settings.reserve_slots (4 live reserve players, 0 IR labels). Raw settings are preserved in roster_settings_raw so nothing is lost; revisit the derivation (label count OR reserve_slots) when a consumer actually reads ir_slot_count. NOTE: the draft-board context now EXPOSES ir_slot_count (Nick's league correctly shows 0 there today because its IR capacity lives in reserve_slots) — the roster/positional-need panel item in 03a's player-list sub-section is the first consumer that could make this visible in UI; that session should weigh fixing the derivation. Flagged as WIKI NOTE evidence on league-endpoint open question #4."
  - "FINALITY-PROMOTION GAP (surfaced 2026-07-22 by the cron session; investigated + designed + registered 2026-07-22 non-code session — still UNBUILT, mechanism unchanged until its build session): in-season, matchups/player_scores rows get is_final=true only when league lifecycle status reads 'complete' at sync time; the scheduled sync is current-week-only, so finished weeks stay is_final=false all season. Covering wiki: sleeper-api/matchup-endpoint (stat-correction window + finality Key Decision #3) — the policy was already decided; only the mechanism was missing. Nick-signed design (2026-07-22): previous-week re-sync + promotion inside the daily league-state cron (week-advance = games-complete proxy; Friday-or-later UTC stable-read match = trigger — both Rule 12 genuine-silence sign-offs) + league-complete backstop sweep; needs a caller-driven finality extension to syncLeagueMatchups. Registered as the [>] item in 02_data_pipeline.md's cron sub-section. Zero live consequence until the season's first week rollover; HARD DEADLINE ~2026-09-15/18 (build scheduling in nick_pending)."

warnings:
  - "ABSOLUTE RULE 13 (canonical: MASTER_CONTEXT.md 'Shared Database Protection — Non-Fantasy Data Is Untouchable'): the Supabase database is the shared multi-app 'prolabel' project — ~49 non-fantasy tables incl. live business production data (Elliott). Never supabase db reset; never migration repair touching foreign history (new foreign versions get empty local stub files, never repair); no raw SQL that could reach a non-fantasy table — verify every migration's table names against ARCHITECTURE.md's 'Database Schema (live)' inventory; collision check before any new table name; ANY ambiguity → stop and ask Nick; mandatory BLAST RADIUS: line on every completion report. Owner-write RLS policies must match Nick's specific auth.uid(), never the blanket authenticated role — realized as public.is_fantasy_admin(); rotate the admin there, never by editing per-table policies. Operational restatement: ARCHITECTURE.md 'Shared-Database Constraints'."
  - "src/lib/supabase/database.types.ts is generated — never hand-edit; regenerate via npm run gen:types after every schema migration. It deliberately contains the whole shared public schema (foreign prolabel tables included) — expected, not drift. Trap: generated Insert types list stored generated columns (matchups.effective_points) as insertable — Postgres rejects them; sync code must omit them."
  - "src/lib/supabase/admin.ts bypasses RLS (secret key) — server-only, never import from client code; its env var is deliberately not NEXT_PUBLIC_ so a client-bundle import fails loudly instead of leaking."
  - "Sleeper player catalog (/players/nfl): at most ONCE PER DAY — Sleeper's most load-bearing rate rule. The cron route (/api/cron/sync-players) enforces this with a hard guard on sync_runs (running/success within 24h → skip; failure rows allow a genuine retry; guard read error → fail closed, no fetch). BUT the manual script path (npm run sync:players) calls the service directly and BYPASSES the guard — manual cadence discipline still applies there. The orchestrator still deliberately EXCLUDES the catalog. Per-league endpoints remain paced by the client's global 250ms min-gap gate."
  - "api.sleeper.com (ADP/projections surface) is NOT api.sleeper.app/v1: undocumented, no contract, can change shape or vanish without notice — its consumer is the quarantined src/services/adp/ ONLY (own client, own pacing gate; documented-host rate knowledge does NOT transfer; never parameterize the documented client to reach it). Ingestion is validate-before-swap: a failed/implausible fetch never overwrites the last good adp_rankings snapshot. 999.0 in any adp_* field is a 'no ADP in this format' sentinel — never data (schema check backstops it). The adp_* field inventory is OPEN (10 formats carried real values live 2026-07-22; adp_rookie and bare adp_dynasty were all-sentinel) — ingestion is pattern-based; never hardcode a closed format list anywhere downstream. Covering wiki: sleeper-api/projections-endpoint."
  - "League-format→ADP-format mapping is CENTRALIZED in resolveAdpScoringFormat (src/services/draft-board.ts, Nick-signed 2026-07-22): superflex→'2qb' precedence, then ppr buckets; dynasty/IDP unmapped in v1; NO cross-format fallback anywhere (average-draft-position format-leakage rule). Any future consumer needing a league's ADP format calls this function — never re-derives its own mapping."

health: "clean"
