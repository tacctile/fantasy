# .claude/STATE.yml
# Living "what happened last session" file. Overwritten completely at the end of every session — never patched.
# Last Updated: 2026-07-23

current_focus: "WAVE 3B — DRAFT QUEUE + AUTO-PICK sub-section COMPLETE 4/4 (2026-07-23, feature-build). NAMED SINGLETON fold exception #3 (live draft-day semantics; local auto-pick's interaction with first-write-wins) — all 4 items built as ONE artifact, folded with nothing else. READ-ONLY to the DB: the ONLY write is the EXISTING recordManualPick path auto-pick reuses (no migration, no new DB access, no second poll driver). Four Clarify decisions Nick-signed: (1) STORAGE = CLIENT-SIDE per league (localStorage) — no shared-prolabel-DB migration, matches the re-pick-my-team precedent; (2) HARD ROSTER RULE = per-position caps + fall-through (skip an over-cap position UNLESS every draftable entry is capped, then pick anyway — never fail); (3) AUTO-PICK TRIGGER = armed toggle + away-triggered (on-clock=my roster AND away, routed through the manual path so first-write-wins keeps a real Sleeper draft safe); (4) MY-TEAM IDENTITY = lift selfRosterId to the shell (one shared picker feeds both the BPA need signal and the auto-pick caps). Built: NEW src/components/draft-board/draft-queue.ts (pure ops addToQueue/removeFromQueue/toggleQueue/moveInQueue — reorder is the ONLY order change, always user-driven, NEVER engine-driven; positionCap = dedicated + flex-eligibility [reads FLEX_ELIGIBILITY from replacement.ts, the one home] + per-position bench margin AUTO_PICK_BENCH_MARGIN {QB1/RB4/WR4/TE2/K0/DEF0,default3} — DECLARED WIKI SILENCE per roster-construction-starting-lineups.md, live-tuned like tiers/runs constants; K/DEF cap 1, QB cap 2 in 1QB [build-file 3rd-QB example], superflex lifts QB for free; selectAutoPickCandidate walks draftable → skips at-cap → first-under-cap, else fall-through to top [overCapFallThrough]; null pos/null layout → take top; + a useSyncExternalStore localStorage store: getQueueSnapshot/getQueueServerSnapshot[empty⇒hydration-safe, NO setState-in-effect]/subscribeQueue/setLeagueQueue[THE one writer]). NEW queue-toggle.tsx QueueToggle star on the main player rows AND the BPA recommendation rows (both build the ONE queue; neutral foreground/muted, no new token). NEW draft-queue-panel.tsx DraftQueuePanel in the sidebar below the BPA panel (ordered list, up/down reorder, remove, the auto-pick arm toggle). Item 2: shell effect keyed on livePicks→draftedIds runs pruneDraftedFromQueue — a drafted queued player drops off (promotion implicit) + sonner toast, silent first pass. Item 3 trigger: armed toggle (off by default) fires when projectOnClock(nextPickNumber,draftOrder)===selfRoster AND away (hidden tab immediate, else 12s dwell AUTO_PICK_DWELL_MS), autoPickAttemptRef = one attempt per pick (no retry loop), fires via the shell's SAME handleDraft→recordManualPick. selfRosterId LIFTED from BpaRecommendationsPanel to the shell (controlled props). WIKI COVERAGE: complete — auto-pick hard rule covered by roster-construction-starting-lineups.md; specific cap margins DECLARED SILENCE (that page rejects fixed formulas), disclosed at decision time; storage/trigger/UI are Nick's Clarify + engineering (no covering category, declared). Verified: tsc/lint/build clean + 32/32 pure harness (deleted) — caps incl. superflex, ops, prune promote, selection fresh/3rd-QB-skip/all-capped-fall-through/drafted-skip/unknown-pos/null-layout/K-cap. Timing trigger + UI wiring tsc/build-verified reusing live seams (no live draft running now to drive headlessly). SELF-LOCATION FOR NEXT SESSION: first unchecked = the RESILIENCE sub-section (2 items — read-only fallback to Wave 3a static behavior when live polling/BPA degrades + the durable test suite: concurrent manual+poll race on (league_id,pick_number), BPA/replacement recompute after a pick, tier recompute, run-detection trigger/reset across the window, auto-pick skipping a hard-roster-rule entry). NOT a named singleton; the live-behavior ceiling (2-3 items) fits both. When Resilience ships, 03b goes 🟢. 03b stays 🟡 (write path 4/4 [x], orchestration 3/3 [x], client-side live sync 5/5 [x], UI extensions 5/5 [x], projections 3/3 [x], BPA engine 9/9 [x], TIER-CLIFF 4/4 [x], POSITIONAL-RUN 4/4 [x], QUEUE+AUTO-PICK 4/4 [x], resilience 0/2 remain)."

build_system:
  index: ".claude/BUILD_INDEX.md"
  files_location: ".claude/build/"
  total_files: 8
  current_build_files:
    - ".claude/build/03b_draft_assistant_live_draft.md"  # 🟡 — write path 4/4 [x], orchestration 3/3 [x], client live sync 5/5 [x], UI extensions 5/5 [x], projections 3/3 [x], BPA engine 9/9 [x], TIER-CLIFF 4/4 [x], POSITIONAL-RUN 4/4 [x], QUEUE+AUTO-PICK 4/4 [x] (2026-07-23: client-side localStorage queue + roster-aware auto-pick through recordManualPick; READ-ONLY, no migration); next + LAST = RESILIENCE (2 items) → then 🟢
    - ".claude/build/04_league_dashboard.md"  # 🟡 — data-access 5/5 [x], freshness [x], 4 display components [x], page assembly [x], league selector [x]; next fold: nav-shell sub-section (share-link panel deferred to the share-token singleton)
    - ".claude/build/03a_draft_assistant_static_board.md"  # 🟡 — all five sub-sections complete; sole remainder = bye-week [>] (schedule-source-gated); goes 🟢 when that ships or is re-ruled
    - ".claude/build/02_data_pipeline.md"  # ESPN residue [!], cron cadence [~], finality promotion [>] (Nick-signed deferral) — nothing self-locatable here

nick_pending:
  - "VISUAL CHECK BACKLOG — NEW this session: the DRAFT QUEUE + AUTO-PICK. On the live draft board: (a) every available player row (main table AND the BPA Recommendations panel) has a small STAR toggle in its status cell — click to add/remove from your queue (neutral filled star when queued). (b) A new QUEUE section in the right sidebar, just below the Recommendations panel: the ordered list (1,2,3…) with up/down arrows to reorder and an × to remove; empty state 'star players on the board to build your priority list'. (c) An 'Auto-pick off / Auto-pick armed' pill in that Queue section — only enabled once a draft is live AND you've set your team (the SAME 'Set my team' picker at the top of the Recommendations panel now drives both); hint text explains it drafts the top eligible queued player when you're on the clock and away. Behaviors to eyeball once a REAL draft is live (can't be seen on the completed 2025 draft — no live clock): a queued player drafted by anyone drops off the queue with a brief toast; auto-pick skips a queued player whose position is already stocked (e.g. a 3rd QB) unless nothing else in the queue qualifies; auto-pick fires only when it's your slot on the clock and the tab is hidden (or after a 12s dwell). STILL OUTSTANDING from prior sessions: the positional-run badge near the position chips; the Best-tier depth counter + Recommendations list at the top of the BPA panel; the live strip/status, recent-picks feed + Undo, roster cards, optimistic Draft fold, live-sync dimming. Dashboard at /leagues/<id>: header, week pills, matchup cards, standings, power rankings, ?player= sheet."
  - "OPTIONAL FUTURE — superflex/best-ball run sensitivity (deferred 2026-07-23 by Nick's Clarify): positional-run-detection-draft-day.md Key Decision mandates different run-sensitivity for superflex/2QB (QB scarcity structural — proactive early QB is correct, not a run) and best-ball. The shipped detectPositionalRuns applies uniform thresholds because no superflex/best-ball league is connected (live league is 1QB redraft). When one connects, make the QB threshold format-aware via resolveAdpScoringFormat — small change to runs.ts + a test + the badge picks it up for free. NOTE: the auto-pick caps ARE already format-aware for free (superflex lifts QB's positionCap via SUPER_FLEX flex eligibility). Not blocking; its own tiny fold. Also a WIKI NOTE."
  - "OPTIONAL FUTURE / SCOPE QUESTION — demand-vs-panic run classification (WIKI NOTE, unchanged 2026-07-23): positional-run-detection-draft-day.md mandates classifying runs into demand-driven (tier-cliff) vs panic-driven (contagion/ahead-of-ADP) with OPPOSITE recommended responses (enter/fade vs wait). The shipped positional-run sub-section (COMPLETE 4/4) does a count-based flag + a badge that COMPOSES the tier-drain signal in its tooltip — NOT the full classification. If the fuller wiki model is wanted for v1 it needs a NEW registered build item / Nick-signed scope amendment. Decide whenever; not blocking."
  - "OPTIONAL FUTURE — per-league memory for 'my team' AND the queue: both are client-side/re-pick-each-session by decision (my team 2026-07-22 Clarify; the queue is localStorage per league, so it survives reloads but not a cleared browser / another device). If you later want durable cross-device state, that's a small future migration (a per-league my-roster + a draft_queue table) + tiny reads — its own small fold. Not blocking."
  - "FINALITY PROMOTION — build scheduling (design Nick-signed + registered 2026-07-22 as the [>] item in 02_data_pipeline.md's cron sub-section; mechanism in known_issues): build as its own standalone fold OR bundle with the season-start cadence session. HARD DEADLINE: before the 2026 NFL week 1→2 rollover (~2026-09-15; promotion first actually needed ~2026-09-18). Flip the item [>]→[ ] when its build session starts."
  - "BLOCKED ON EXTERNAL TIMING — nothing to do until ~mid-August 2026 (expected clear ~2026-08-19 → 2026-09-02): Wave 2 ESPN manual items (public/private per league, espn_s2/SWID for private, league IDs + season year). ESPN leagues commissioner-locked until 2026 season setup. When they open: resolve in a plain chat, flip them in MANUAL_SETUP_CHECKLIST.md, flip 02_data_pipeline.md's 9 ESPN items + 03c's [!]→[ ]. Per the backsweep practice (BUILD_PROTOCOL Amendment Hygiene) that plain chat must sweep the confirmations against already-registered 02/03c text AND against shipped Wave 4 surfaces (platform-agnostic), 3b's write path (recordManualPick platform-agnostic; 03c auction extends via the amount field on the same handleDraft seam), the draft_sessions/poll orchestration (skipped_non_sleeper by design; 03c ESPN poll extends runRecordedDraftPoll), the client-side live sync seams (listDraftPicks/merge/optimistic overlay platform-agnostic via crosswalk), the DraftOrderMeta on-clock path (slot_to_roster_id is Sleeper-specific; ESPN needs its own draft-order source — which the AUTO-PICK trigger also depends on via projectOnClock, so ESPN auto-pick's on-clock detection waits on that ESPN draft-order source), the projections/BPA/tier-cliff layer AS SHIPPED (player_projections 'sleeper'-only; ESPN leagues' BPA + tiers + counter use the same Sleeper-sourced projections automatically), the run-detection layer + its badge AS SHIPPED (platform-agnostic — read pick_number + catalog position off draft_state), AND the QUEUE + AUTO-PICK layer AS SHIPPED (the queue is client-side/platform-agnostic; auto-pick writes through the platform-agnostic recordManualPick and reads availability off the platform-agnostic client merge — so ESPN leagues get the queue + roster-aware auto-pick automatically once their picks flow, EXCEPT the on-clock away-trigger which needs the ESPN draft-order source above; sweep any item text that assumed otherwise)."
  - "SEASON START (~Sept 2026): revisit league-sync cadence — the specced 15-minute cron needs Vercel Pro (~$20/mo) or an alternative trigger; currently daily on Hobby per your 2026-07-22 decision. Flip 02_data_pipeline.md's cron-schedule item [~]→[x] when sub-daily ships. If Pro is bought, /api/cron/sync-adp is the ready mount point. NOTE: live-draft polling + BPA/tier/run/tier-counter recompute + run badge + the queue/auto-pick all do NOT depend on this — client-driven, no cron slot at any tier (the queue is pure client localStorage; auto-pick reads an already-fetched snapshot + writes via the existing action)."
  - "BYE-WEEK [>] ITEM (03a) still awaits its schedule-source decision (plain chat: pick a schedule source, then a build session ships table+ingestion+row wiring — flips 03a 🟢). No longer blocking anything, the only remainder between 03a and 🟢. Related small observation: the 3a board pool's fullName is also null for DST rows (renders the abbreviation fallback); the item-4 name compose exists in draft-picks.ts if a future correctness pass wants to reuse it for getDraftBoardData."
  - "When your 2026 Sleeper league renews (new league_id every season): run npm run sync:league -- <new_id> once to connect it; every daily league-state cron run (and npm run sync:sleeper) covers it after. The 2025 league (1257464256710856704) is fully ingested incl. its 170-pick draft. With auto-land live, the renewed league (season 2026 > 2025) becomes the DASHBOARD you land on after sign-in. Draft-day note: the renewed league is where Start draft + live polling + click-to-draft + the live strip/status/roster/recent-picks feed + the BPA panel + the tier counter + the run badge + the draft queue + auto-pick run in anger. The BPA + tier + run layer uses the LATEST ingested projections/ADP season (2026) regardless of the league's own season, so it is already current-market."
  - "Before Wave 5's waiver/FAAB recommendations item is built: run a dedicated scoping session to define the actual waiver/FAAB candidate scoring formula (opportunity-based — snap/target/touch share trends — is the recommended direction per wiki/topics/league-mechanics/waiver-wire-faab-strategy.md, but no formula exists yet)."

blockers:
  - "EXTERNAL TIMING (dated 2026-07-22): 02_data_pipeline.md's two ESPN sub-sections (9 items, [!]) are blocked until ESPN leagues open for the 2026 season — commissioner-locked, expected ~2026-08-19 → 2026-09-02. Not resolvable by Nick or any build session before then; do NOT guess visibility or fabricate placeholder league IDs (Nick's explicit instruction). Downstream: Wave 3c inherits this blocker (ESPN draft mechanics + auction). Does not stall the roadmap: 3b (resilience next, then 🟢) and Wave 4 both have buildable [ ] items."

known_issues:
  - "npm audit reports 6 vulns (4 moderate, 2 high) — last re-checked live 2026-07-22 (no dependencies added since): pre-existing 1 moderate postcss + 2 high sharp are transitive inside next@16.2.11's pinned deps (only offered 'fix' is downgrading next to 9.x, which is wrong). All dev-time/build-time, none shipped to clients. Re-check in later sessions."
  - "FINALITY-PROMOTION GAP (surfaced + designed + registered 2026-07-22 — still UNBUILT): in-season, matchups/player_scores rows get is_final=true only when league lifecycle status reads 'complete' at sync time; the scheduled sync is current-week-only, so finished weeks stay is_final=false all season. Covering wiki: sleeper-api/matchup-endpoint (finality Key Decision #3). Nick-signed design: previous-week re-sync + promotion inside the daily league-state cron (week-advance = games-complete proxy; Friday-or-later UTC stable-read match = trigger) + league-complete backstop sweep; needs a caller-driven finality extension to syncLeagueMatchups. Registered as the [>] item in 02_data_pipeline.md's cron sub-section. Zero live consequence until the season's first week rollover; HARD DEADLINE ~2026-09-15/18. WAVE 4 UI side SHIPPED (UnofficialChip + AsOfTime render is_final/fetched_at honestly)."

warnings:
  - "ABSOLUTE RULE 13 (canonical: MASTER_CONTEXT.md 'Shared Database Protection'): the Supabase database is the shared multi-app 'prolabel' project — ~49 non-fantasy tables incl. live business production data (Elliott). Never supabase db reset; never migration repair touching foreign history (new foreign versions get empty local stub files); no raw SQL that could reach a non-fantasy table — verify every migration's table names against ARCHITECTURE.md's 'Database Schema (live)' inventory; collision check before any new table name; ANY ambiguity → stop and ask Nick; mandatory BLAST RADIUS: line on every report. Owner-write RLS must match Nick's specific auth.uid() via public.is_fantasy_admin(), never the blanket authenticated role. THIS SESSION (queue + auto-pick 4/4): READ-ONLY — no migration, NO new DB access; the only write is the EXISTING recordManualPick path auto-pick reuses (source='manual', first-write-wins), exercised in NO live run this session (pure harness hit no DB and no window). WAVE 4 SPECIFIC: the share-token RLS sub-section adds SELECT policies on six fantasy tables — a named singleton fold (#5) with its boundary tests; owner policies stay pinned to is_fantasy_admin(). draft_sessions/draft_state are NEVER on any spectator-policy list; player_projections/adp_rankings have no spectator consumer either (the whole BPA + tier + run + queue/auto-pick layer is admin-only)."
  - "CLIENT-SIDE LIVE SYNC SEAMS (updated 2026-07-23): the live board path is ticker → pollActiveDraft (full listDraftPicks snapshot per executed poll) → shell's fingerprint-deduped livePicks state (+ optimistic pendingPicks overlay + pollHealth + DraftOrderMeta + panelTiers + selfRosterId + the queue store). livePicks CONSUMERS: live-picks.ts mergePicksIntoPlayers → both board regions; the roster panel + recent-picks feed read livePicks directly; the BPA panel reads it as a useEffect dep (recomputes VORP+tiers, reports tiers up via onTiers); the positional-run badge computes detectPositionalRuns in a useMemo over it. NEW consumers this session: (a) the QUEUE PRUNE effect (livePicks→draftedIds→pruneDraftedFromQueue — a NEW read of the EXISTING snapshot, drops drafted queued players + toasts, NOT a new pick feed); (b) AUTO-PICK reads availability from the ONE client merge (mergedPlayers→availableIds) + the self-roster shape from livePicks, and fires through the shell's SAME handleDraft. The QUEUE ITSELF is client-side localStorage via a useSyncExternalStore adapter in draft-queue.ts (getServerSnapshot empty ⇒ hydration-safe, NO setState-in-effect; setLeagueQueue is THE one writer — toggle/remove/move/prune all route through it). selfRosterId was LIFTED from the BPA panel to the shell (controlled) so ONE 'my team' picker feeds the need signal AND the auto-pick caps. Extend THIS path for the LAST 03b items (resilience) — never a second poll driver, never a second merge, never a second pick-write UI path (handleDraft via the DraftablePlayer seam — auto-pick reuses recordManualPick, NOT a parallel submit), never a second undo UI path, never a second tier/board/run/QUEUE source. Key invariants unchanged: 'drafted'/draftPending produced ONLY by the client merge; a confirmed snapshot row ALWAYS wins over pending; 'rostered' wins over the whole overlay; ON-CLOCK IS A PROJECTION (projectOnClock — DISPLAY plus now the auto-pick away-trigger's my-turn check, still never authoritative: first-write-wins is the real arbiter). rosterLabel (player-row.tsx) is THE roster display convention."
  - "MANUAL-PICK WRITE PATH CENTRALIZATION (updated 2026-07-23): src/services/draft-picks.ts recordManualPick is the ONE local pick-write path. AUTO-PICK now writes through it AS MANDATED (build-file rule realized): the shell's fireAutoPick → the SAME handleDraft (optimistic path) → submitManualPick → recordManualPick, source='manual', undoable via the same undo, first-write-wins conflict-safe (a poller-won pick makes the auto-pick attempt conflict + roll back, so auto-pick can NEVER desync a real Sleeper draft or write to Sleeper). One attempt per pick number (autoPickAttemptRef) — no retry loop. No second draft_state insert path outside recordManualPick and the platform pollers. Its UI callers wire through the shell's handleDraft (main row + BPA panel + now auto-pick); future draft triggers (03c auction bid) do the same. Server-action auth pattern (6 actions incl. read-only fetchBpaRecommendations): gate INSIDE each action (getAdminAuthState) + RLS as the second wall. listDraftPicks is THE ordered pick-snapshot read; the queue prune + auto-pick read the shell's livePicks (seeded by it, refreshed by the poll), never a second pick read. Undo has its ONE UI (recent-picks feed) — extend that, never a second undo control."
  - "BPA + TIER + RUN + QUEUE/AUTO-PICK LAYER CENTRALIZATION (updated 2026-07-23): src/services/bpa/ is the ONE home of projection scoring + VORP + tier + run detection + the recommendations query (pure sub-layer scoring/replacement/calibration/base-value/board/need/tiers/runs; I/O = recommendations.ts). src/components/draft-board/ pure client layers: live-picks.ts (merge/next-pick/on-clock), live-status.ts (poll health), and NEW draft-queue.ts (queue ops + localStorage store + roster-aware auto-pick selection — positionCap reads FLEX_ELIGIBILITY from replacement.ts, the ONE flex-eligibility home, never re-derived; AUTO_PICK_BENCH_MARGIN + AUTO_PICK_DWELL_MS are DECLARED-WIKI-SILENCE live-tuned constants like tiers.ts multiples / runs.ts thresholds). UI homes: bpa-recommendations-panel.tsx (recommendations + TierDepthStrip), positional-run-badges.tsx (run badge), draft-queue-panel.tsx (queue list + reorder + auto-pick arm), queue-toggle.tsx (the shared star). Run detection is NOT routed through the value query; the queue is NOT reordered by the engine; need/tiers/runs stay SEPARATE parallel signals never multiplied into base value; auto-pick's roster caps are a SEPARATE roster-construction heuristic, never a value input. NEVER add caching, a second scoring/VORP/tier/run/queue derivation, a second projections read, a second recommendations query, a second draft-submit path, or a second run/queue source anywhere downstream. Layout parsing stays parseRosterSlotLayout, format mapping stays resolveAdpScoringFormat (both draft-board.ts)."
  - "PLAYER_SCORES IS INGESTED, NEVER COMPUTED (Nick-signed canon): platform-scored values land as-received (Sleeper players_points; ESPN as-received when unblocked). NO compute-from-raw-stats path for ACTUAL scores in v1. PROJECTIONS ARE THE SANCTIONED EXCEPTION: player_projections stores component projection stats, and the BPA engine scores them with league_config weights at read time (bpa/scoring.ts) — projected value, never a substitute for actual player_scores. Tier detection reads those same league-scored projections (via base-value output); run detection reads NO projections (only draft_state pick order + catalog position); the QUEUE/AUTO-PICK layer reads NO projections either (queue = localStorage ids; auto-pick reads catalog position + the client-merge availability + draft_state pick order). Never invent score rows for unrostered weeks (FA-week gap accepted for v1)."
  - "DASHBOARD QUERY SERVICE CENTRALIZATION (live 2026-07-22): src/services/dashboard.ts is the ONE home of the standings ordering (fetchRankedStandings) and the power-ranking formula (All-Play); listScoredWeeks is the ONE week-range/default-week source. The Wave 4 display components render these AS-IS. The spectator loader passes its own share_token-scoped client. Boundary: the module never selects share_token/owner_id/native_league_id and has zero draft_state references — keep it that way."
  - "ADMIN/SPECTATOR UI BOUNDARY (Wave 4, live 2026-07-22): src/components/dashboard/ is ADMIN-surface UI — the spectator surface imports ZERO components from it (or from draft-board/ or ui/dropdown-menu's draft usages); spectator gets its own components (04's spectator sub-section). The BPA panel, the tier counter, the run badge, AND the queue/auto-pick panel are likewise ADMIN-only — no spectator consumer, ever. Shared data-access (services/dashboard.ts) is the ONLY sanctioned sharing. Freshness primitives UnofficialChip + AsOfTime are THE rendering of is_final/fetched_at. The player-card open mechanism is the URL-driven ?player= sheet."
  - "DARK MODE ONLY IS LOAD-BEARING IN CODE (live 2026-07-22): globals.css holds the app's single always-on token set in :root — NO light block, NO .dark override, NO theme toggle; the `dark` class on <html> keeps vendored shadcn dark:-variant tunings active. Never add a light token set, a prefers-color-scheme branch, or remove that class. ui/sonner.tsx hardcodes theme='dark'. --radius stays 0.625rem. Position identity tokens --pos-* are badges/chips ONLY; teal = interactive/live only; amber (--warning) = roster-need. TierDepthStrip + PositionalRunBadges + the QUEUE UI all follow this: uniform muted, tabular where numeric, PositionBadge reused for position color, NO new token — the queue star is neutral foreground/muted (overloads neither teal nor amber), the auto-pick arm pill reuses the primary/secondary chip states, non-blocking (a run/tier/queue is a weighted signal, not a 'draft now' trigger)."
  - "OWNER AUTH SURFACE (live 2026-07-22) — three walls, keep all three: (1) src/proxy.ts (Next 16 proxy, NOT middleware.ts) refreshes the Supabase session per matched request and 307s unauth /leagues/* to /login — matcher EXCLUDES /api. (2) The (admin) layout gate uses getAdminAuthState (is_fantasy_admin RPC) — never hardcode the admin UUID; a signed-in non-fantasy prolabel user gets the zero-data NotAuthorizedCard. (3) Per-table RLS. ROOT AUTO-LAND targets the dashboard (/leagues/<id>). Server actions add a FOURTH gate: auth INSIDE each action — SIX actions (submitManualPick/undoManualPick/startDraftSession/endDraftSession/pollActiveDraft/fetchBpaRecommendations + login's signOut). Auto-pick adds NO new action — it reuses submitManualPick via handleDraft."
  - "src/lib/supabase/database.types.ts is generated — never hand-edit; regenerate via npm run gen:types after every schema migration. It deliberately contains the whole shared public schema (foreign prolabel tables included) — expected, not drift. Trap: generated Insert types list stored generated columns (matchups.effective_points) as insertable — Postgres rejects them; sync code must omit them. Note: the leagues PK column is platform_league_uuid (NOT `id`). getBpaRecommendations reads player_projections with a PAGINATED .range loop (1000/page). The queue/auto-pick layer reads NO Supabase of its own (queue = localStorage; auto-pick reads the shell's already-fetched livePicks + the client merge, and writes via the existing submitManualPick action)."
  - "src/lib/supabase/admin.ts bypasses RLS (secret key) — server-only, never import from client code. The draft-board, dashboard, BPA/tier/run, and queue/auto-pick routes/actions deliberately do NOT use it — owner pages read/write through the RLS server client as the signed-in admin. Temp verification scripts (deleted after use) may use the admin client for read-only checks — though THIS session's harness (scripts/verify-draft-queue.ts, deleted) hit NO DB and NO window at all, only pure functions over synthetic inputs."
  - "Sleeper player catalog (/players/nfl): at most ONCE PER DAY — the cron route enforces it with a hard guard on sync_runs; the manual script path (npm run sync:players) bypasses the guard — manual cadence discipline applies. Per-league endpoints paced by the client's global 250ms min-gate (also absorbs 5s live-draft polling, ≤3 requests per executed tick). The BPA panel's per-pick recompute, tier recompute, the tier counter, run detection, the run badge, AND the queue/auto-pick make NO Sleeper calls — the BPA/tier layer reads Supabase only, run detection + the queue read nothing/localStorage, and auto-pick's write goes through the existing draft_state action (no Sleeper write, ever)."
  - "api.sleeper.com (ADP/projections surface) is NOT api.sleeper.app/v1: undocumented, no contract — its consumer is the quarantined src/services/adp/ ONLY (own client, own pacing gate). The SAME fused ingestion run feeds BOTH adp_rankings AND player_projections from one fetch; never a second api.sleeper.com client or a separate projections fetch. 999.0 in any adp_* field is a 'no ADP in this format' sentinel — never data in adp_rankings; player_projections.stats retains the RAW object. ADP is never read from player_projections. Covering wiki: sleeper-api/projections-endpoint."
  - "League-format→ADP-format mapping is CENTRALIZED in resolveAdpScoringFormat (src/services/draft-board.ts, Nick-signed): superflex→'2qb' precedence, then ppr buckets; dynasty/IDP unmapped in v1; NO cross-format fallback. Any consumer calls this function — never re-derives. Same centralization for roster-slot layouts: parseRosterSlotLayout (same module) is the ONE parser of roster_settings_raw — bpa/replacement.ts, need.ts, getBpaRecommendations, AND the auto-pick positionCap all consume its RosterSlotLayout output, never re-derive. FLEX_ELIGIBILITY is the ONE flex-eligibility home (exported from bpa/replacement.ts) — need.ts AND draft-queue.ts's positionCap import it, never re-derive. DraftablePlayer (draft-board.ts) is the ONE minimal shape the shared handleDraft consumes (auto-pick passes a full DraftBoardPlayer, which satisfies it); RunPick (bpa/runs.ts) is the analogous minimal shape run detection consumes — non-table draft surfaces reuse these, never a cast or a parallel submit/read."

health: "clean"
