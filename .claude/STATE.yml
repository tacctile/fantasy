# .claude/STATE.yml
# Living "what happened last session" file. Overwritten completely at the end of every session — never patched.
# Last Updated: 2026-07-22

current_focus: "Wave 1 (Foundation), feature-build session via BUILD_PROTOCOL: sole item of the 'Schema — draft state (shared, not staged)' section of 01_foundation.md is complete and live — the section is fully checked. draft_state table + draft_pick_source enum (20260722055716_create_draft_state_table.sql), created via supabase migration new and pushed via the allowed 'npx supabase db push' rule (password sourced from .env.local in-shell). Shape (Nick's Clarify decisions): draft formats locked as snake/linear + auction only — dynasty/multi-draft-per-season explicitly out of scope, one draft per league row assumed; PK (league_id, pick_number) natural composite — the spec'd first-write-wins unique constraint itself (rosters/matchups/player_scores precedent); native_draft_id text nullable records Sleeper draft_id provenance as-received (never-deleted aborted/restarted drafts; Wave 2 poller selects by season/type/status and records its choice; null for ESPN — no distinct draft ID in mDraftDetail — and manual; sleeper-only check); native_roster_id integer not null composite FK -> rosters (every pick attributed; Sleeper roster_id is the wiki-decided ownership field — never draft_slot/picked_by — / ESPN teamId); amount numeric nullable for auction winning bids per Nick's explicit instruction (Sleeper metadata.amount string coerced at ingestion / ESPN bidAmount; null for snake picks); otherwise spec-exact — is_keeper and draft_slot/picked_by deliberately excluded (no registered v1 consumer). pick_number/round as-received (Sleeper pick_no/round, ESPN overallPickNumber/roundId) — never snake-order arithmetic per both wiki pages. source draft_pick_source enum (manual|sleeper_poll|espn_poll) with source_matches_platform coherence check (my judgment call from the enum semantics + platform-check precedent, disclosed in report). No fetched_at/is_final — first-write-wins rows are immutable, no correction-window semantics. platform + season_year duplicated (N:1 precedent); all 3 FKs cascade; RLS enabled at creation; set_updated_at trigger. Pre-flight prolabel collision check passed (no *draft* table or type). Wiki coverage: wire shapes fully covered by sleeper-api/draft-endpoint + espn-api/draft-detail-response-structure; schema-reference still has no league-scoped-state DDL ADR — silence declared at decision time (sixth consecutive session), resolved via precedent + Clarify — report carries 'gap found and flagged' with the standing WIKI NOTE suggesting a 'league-scoped state tables' DDL addendum page. All objects verified live via read-only queries (columns/nullability/defaults, PK, all 3 FKs + cascade, both checks, enum values, RLS, trigger, migration history row). Log cap triggered this session: 2026-07-22_01.md condensed into .claude/logs/ARCHIVE.md and deleted. Next unchecked build item: first item of the 'Schema — integrity' section of 01_foundation.md — 'Add foreign keys from every league-scoped table back to leagues... and from every player-scoped table back to players'; note every table created so far already carries these FKs from creation, so that session is largely a verify-and-check-off plus any genuinely missing pieces, before the indexes item and the RLS/auth item (which will ask for the fantasy app admin email/password per MANUAL_SETUP_CHECKLIST [~])."

build_system:
  index: ".claude/BUILD_INDEX.md"
  files_location: ".claude/build/"
  total_files: 7
  current_build_files:
    - ".claude/build/01_foundation.md"

nick_pending:
  - "Before Wave 3a: verify whether Sleeper actually has a public ADP endpoint, or decide a real alternative source — see MANUAL_SETUP_CHECKLIST.md Wave 3a section."
  - "Before Wave 5's waiver/FAAB recommendations item is built: run a dedicated scoping session to define the actual waiver/FAAB candidate scoring formula (opportunity-based — snap/target/touch share trends — is the recommended direction per wiki/topics/league-mechanics/waiver-wire-faab-strategy.md, but no formula exists yet). See 05_eye_candy.md 'Waiver/FAAB recommendations' section, DEFERRED item."

blockers: []

known_issues:
  - "npm audit reports 6 vulns (4 moderate, 2 high) as of last check — pre-existing 1 moderate postcss + 2 high sharp are transitive inside next@16.2.11's pinned deps (only offered 'fix' is downgrading next to 9.x, which is wrong). All dev-time/build-time, none shipped to clients. Re-check npm audit in later sessions."

warnings:
  - "The Supabase database is the shared multi-app 'prolabel' project. Never run supabase db reset or supabase migration repair against it; new foreign history versions get empty local stub files, never repair. Owner-write RLS policies must match Nick's specific auth.uid(), never the blanket authenticated role. Full rules: ARCHITECTURE.md 'Shared-Database Constraints'."

health: "clean"
