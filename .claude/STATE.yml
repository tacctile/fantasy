# .claude/STATE.yml
# Living "what happened last session" file. Overwritten completely at the end of every session — never patched.
# Last Updated: 2026-07-22

current_focus: "Wave 1 'Types + client wiring' section COMPLETE — both items folded into one session per the Folding Policy (fold declared before Clarify; per-item Clarify batches). (1) Generated types: src/lib/supabase/database.types.ts via npx supabase gen types typescript --linked --schema public (canonical gen:types npm script added); full public-schema output committed as-is per Nick's ruling — the shared prolabel project's ~49 foreign tables ride along as inert generated types (CLI has no per-table filter). Verified: all 10 fantasy tables + 3 enums + both functions present; tsc clean. (2) Client utilities: @supabase/ssr@0.12.3 + @supabase/supabase-js@2.110.8; src/lib/supabase/client.ts (createClient — createBrowserClient<Database>, publishable key) + server.ts (async createClient — createServerClient<Database>, await cookies(), getAll/setAll with RSC set no-op catch). Nick's Clarify rulings: same-name createClient in both files disambiguated by import path (Supabase-docs convention); no barrel index.ts; secret-key admin client deliberately DEFERRED to Wave 2 (no registered consumer — the env/deploy health-check session decides its own auth approach). Verified: tsc + npm run build pass; temporary type probe confirmed .from('leagues')/.from('draft_state') resolve exact row/enum types through both clients (deleted after passing). WIKI COVERAGE CHECK: complete (no domain/schema decision made — tooling/convention work; ROUTING/index searched, no covering category, declared at decision time). Next unchecked build item: first item of 'Env, secrets, deploy' in 01_foundation.md (.env.example contract) — NOTE for that session: the Vercel project-creation and env-vars items are already done manually per MANUAL_SETUP_CHECKLIST [x] entries (verify-and-check-off, don't re-create), and the health-check item is the wave's actual completion gate (authenticated read from the deployed environment) — it may need Nick able to sign in as nick@prolabelco.com (see nick_pending)."

build_system:
  index: ".claude/BUILD_INDEX.md"
  files_location: ".claude/build/"
  total_files: 7
  current_build_files:
    - ".claude/build/01_foundation.md"

nick_pending:
  - "Before the Wave 1 env/deploy session (next session) reaches the health-check item, or at latest before first real sign-in: make sure you can actually sign in as nick@prolabelco.com — that pre-existing account's password is whatever you saved when it was created 2026-05-15 (last sign-in 2026-06-06); reset via the Supabase dashboard if forgotten. The health-check item may exercise an authenticated-as-admin read."
  - "Before Wave 3a: verify whether Sleeper actually has a public ADP endpoint, or decide a real alternative source — see MANUAL_SETUP_CHECKLIST.md Wave 3a section."
  - "Before Wave 5's waiver/FAAB recommendations item is built: run a dedicated scoping session to define the actual waiver/FAAB candidate scoring formula (opportunity-based — snap/target/touch share trends — is the recommended direction per wiki/topics/league-mechanics/waiver-wire-faab-strategy.md, but no formula exists yet). See 05_eye_candy.md 'Waiver/FAAB recommendations' section, DEFERRED item."

blockers: []

known_issues:
  - "npm audit reports 6 vulns (4 moderate, 2 high) — re-checked live 2026-07-22 after installing @supabase/ssr + @supabase/supabase-js: count unchanged, the new packages introduced nothing. Pre-existing 1 moderate postcss + 2 high sharp are transitive inside next@16.2.11's pinned deps (only offered 'fix' is downgrading next to 9.x, which is wrong). All dev-time/build-time, none shipped to clients. Re-check in later sessions."

warnings:
  - "The Supabase database is the shared multi-app 'prolabel' project. Never run supabase db reset or supabase migration repair against it; new foreign history versions get empty local stub files, never repair. Owner-write RLS policies must match Nick's specific auth.uid(), never the blanket authenticated role — now realized as public.is_fantasy_admin(); rotate the admin there, never by editing per-table policies. Full rules: ARCHITECTURE.md 'Shared-Database Constraints'."
  - "src/lib/supabase/database.types.ts is generated — never hand-edit; regenerate via npm run gen:types after every schema migration. It deliberately contains the whole shared public schema (foreign prolabel tables included) — this is expected, not drift."

health: "clean"
