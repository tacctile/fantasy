# .claude/STATE.yml
# Living "what happened last session" file. Overwritten completely at the end of every session — never patched.
# Last Updated: 2026-07-21

current_focus: "Wave 1 (Foundation) — Supabase project + migration workflow section of 01_foundation.md remains the last completed build work (schema migrations not yet started). This session (non-code, governance audit + fix) implemented growth-control rules for the .claude/ governance system: PROGRESS.md capped at 5 entries (2-4 line summaries), 2 oldest entries migrated to new .claude/PROGRESS_ARCHIVE.md; .claude/logs/ negation applied to .gitignore (!.claude/logs/) so session logs now sync via git across Nick's machines, with a new 5-file cap and .claude/logs/ARCHIVE.md rollover rule for when a 6th log would be created (only 3 logs exist currently, cap not yet triggered); STATE.yml pruning now an explicit standing rule (arrays must be actively reviewed and trimmed each session, not just appended-to); build files (.claude/build/*.md) now have an explicit amendment norm (corrections only post-registration, no running commentary); and a portability statement was added to MASTER_CONTEXT.md confirming the repo must be fully reconstructable from git alone on any machine. All changes applied to MASTER_CONTEXT.md (System Goal section, Build File Amendment Norm section, Session-End Steps, Prompt Format SESSION END block) and COMPLETION_TEMPLATES.md (PROGRESS.md field notes) per the Dual-Location Instruction Rule. Next unchecked build item remains: 'Migration: platform Postgres enum type, values sleeper and espn' — first item of the 'Schema — platform + identity foundation' section of 01_foundation.md."

build_system:
  index: ".claude/BUILD_INDEX.md"
  files_location: ".claude/build/"
  total_files: 7
  current_build_files:
    - ".claude/build/01_foundation.md"

nick_pending:
  - "Before Wave 3a: verify whether Sleeper actually has a public ADP endpoint, or decide a real alternative source — see MANUAL_SETUP_CHECKLIST.md Wave 3a section."

blockers: []

known_issues:
  - "npm audit reports 6 vulns (4 moderate, 2 high) as of last check — pre-existing 1 moderate postcss + 2 high sharp are transitive inside next@16.2.11's pinned deps (only offered 'fix' is downgrading next to 9.x, which is wrong). All dev-time/build-time, none shipped to clients. Re-check npm audit in later sessions."

warnings: []

health: "clean"
