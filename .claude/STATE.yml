# .claude/STATE.yml
# Living "what happened last session" file. Overwritten completely at the end of every session — never patched.
# Last Updated: 2026-07-31
#
# AUTHORING RULES:
#   current_focus: 3-5 lines MAX. A pointer, not a narrative. State: what just completed, what is next, any active blocker.
#     Implementation detail belongs in .claude/logs/, not here. Replace completely each session — never append.
#   nick_pending / known_issues / blockers: active, unresolved items only. Prune stale entries every session.
#   warnings: REMOVED. Architectural invariants live permanently in MASTER_CONTEXT.md and ARCHITECTURE.md — not here.

current_focus: |
  LAST COMPLETED: Wave 5's Shared foundations sub-section — all 4 items (05 lines 36-39 [x]). 05 is now 🟡.
  Chart layer exists with no feature section on it yet: components/charts/ (no library — hand-rolled, zero client JS, shared by BOTH surfaces)
  and services/league-context.ts (now the home of RosterSlotLayout, moved out of draft-board.ts, which re-exports it).
  No migration, no DB write, no live-DB read. 112/112 tests, tsc/lint/build clean.
  NEXT: Wave 5's "Score charts" sub-section (7 items) — first real consumer of the new primitives. No blocker.

build_system:
  index: ".claude/BUILD_INDEX.md"
  files_location: ".claude/build/"
  total_files: 8
  current_build_files:
    - ".claude/build/05_eye_candy.md"   # 🟡 — Shared foundations done; Score charts is the frontier
    - ".claude/build/03a_draft_assistant_static_board.md"  # 🟡 — bye-week [>] only, schedule-source-gated
    - ".claude/build/02_data_pipeline.md"  # ESPN [!] external-blocked, cron cadence [~], finality promotion [>]

nick_pending:
  - "VISUAL CHECK BACKLOG — never eyeballed in a real authed session on real data: all Wave 4 admin surfaces (nav-shell, command-center home, dashboard, share-link panel), all Wave 3b live-draft surfaces, and the spectator page /share/<token>. Nothing in this repo has been seen on a real phone yet. The completed 2025 draft has no live clock, so live-draft timing can't be fully exercised until a real 2026 draft. NEW this session: the chart primitives have never been rendered at all — they compile and their maths is tested, but no screen mounts them until Score charts ships."
  - "SPECTATOR PAGE IS LIVE and fails gracefully. MANUAL_SETUP's 'decide when/whether to share the link' is a real decision you can make — worth opening one on your phone first (see visual-check backlog)."
  - "POST-LOGIN LANDING: login/actions.ts still lands on /draft; root auto-land lands on command-center home. One-line fix in landingPath() if you want them consistent. Not blocking."
  - "FINALITY PROMOTION hard deadline ~2026-09-15/18 (before week 1→2 rollover). Registered as [>] in 02_data_pipeline.md. Flip [>]→[ ] when its build session starts."
  - "ESPN UNBLOCK (~2026-08-19 → 2026-09-02): when leagues open, resolve in plain chat → flip MANUAL_SETUP + 02's 9 ESPN items + 03c's [!]→[ ]. Backsweep required against all shipped Wave 4 + 3b surfaces (all platform-agnostic — ESPN leagues join automatically). getLeagueContext is platform-agnostic too: an ESPN league's slotLayout degrades to null until its native roster shape parses, exactly as an unsynced Sleeper league does."
  - "SEASON START (~Sept 2026): sub-daily cron cadence needs Vercel Pro or alternative trigger. Currently daily on Hobby. Flip 02's cron item [~]→[x] when sub-daily ships."
  - "BYE-WEEK [>] (03a): plain-chat schedule-source decision → build session → 03a 🟢. DST fullName null issue also noted (abbreviation fallback renders; compose exists in draft-picks.ts)."
  - "SLEEPER LEAGUE RENEWAL: when 2026 league_id is live, run npm run sync:league -- <new_id> once. Auto-land and share_token RLS both pick it up automatically."
  - "WAVE 5 WAIVER FORMULA: run a scoping session before building the waiver/FAAB recommendations item — no formula exists yet. Still the one thing gating a clean run at Wave 5; every other Wave 5 section is buildable now."
  - "WAVE 5 PLAYOFF FIELDS (new 2026-07-31): playoff team count / seeding rules are NOT in derived_config and were deliberately left unresolved. The playoff-rules resolver item needs a real answer for where they live — getPowerRankings' raw playoff_week_start read is the precedent. Surfaces at that item's Clarify, not before."
  - "OPTIONAL: superflex/best-ball run sensitivity (runs.ts QB threshold); demand-vs-panic run classification (needs new build item); per-league durable my-team + draft queue (small future migration)."

blockers:
  - "EXTERNAL TIMING (2026-07-22): ESPN commissioner-locked until ~2026-08-19 → 2026-09-02. Blocks 02 ESPN sub-sections (9 items [!]) and 03c. Buildable frontier is unaffected: Wave 5, plus 02's finality-promotion [>] and 03a's bye-week [>]."

known_issues:
  - "npm audit: vulnerabilities reported in transitive next@16.2.11 build-time deps, none client-shipped. Re-check in later sessions."
  - "FINALITY PROMOTION GAP: matchups/player_scores rows stay is_final=false all season until the [>] build item ships. Zero live consequence until week 1→2 rollover (~2026-09-15/18). Both Wave 4 surfaces render is_final/fetched_at honestly."
  - "NO LIVE-DATA RUN, TWO SESSIONS RUNNING: this container still has no .env.local, so getLeagueContext has never executed against the real Supabase — it is verified by tsc/lint/build + a fake-client suite that also asserts its column boundary. Nothing in it writes, and the query shapes mirror draft-board.ts's already-live-verified reads, so the risk is low but the gap is real."
  - "CHART PRIMITIVES ARE UNRENDERED: components/charts/ compiles and its pure layers are covered by 51 new tests, but no route mounts any of it yet. Visual correctness (spacing, label collisions, tooltip placement) is genuinely unverified until Score charts ships — the dataviz standard's 'render it and look at it' step could not be performed this session."

health: "clean"
