# .claude/STATE.yml
# Living "what happened last session" file. Overwritten completely at the end of every session — never patched.
# Last Updated: 2026-07-21

current_focus: "Wave 1 (Foundation), feature-build session via BUILD_PROTOCOL: first two items of the 'Schema — league + config' section of 01_foundation.md are complete and live (Nick approved folding the two trivially-adjacent items per BUILD_PROTOCOL's edge case). Two migrations created via supabase migration new and applied to the live DB by Claude directly: 20260722044457_create_leagues_table.sql (leagues — platform_league_uuid uuid PK default gen_random_uuid(), platform enum, season_year integer, native_league_id text stored as-received, previous_platform_league_uuid nullable self-FK on delete set null, name text, share_token text not null unique, owner_id uuid nullable FK -> auth.users on delete set null pending admin-user creation, timestamps, unique (platform, native_league_id, season_year) per the ADR's one-row-per-platform-season-league rule, RLS enabled, set_updated_at trigger) and 20260722044500_create_share_token_generation.sql (public.generate_share_token() = 32 random bytes hex via extensions.gen_random_bytes, pgcrypto v1.3 pre-installed on the shared project, set as share_token column default). All objects verified live via read-only queries (columns/constraints/RLS/trigger/function output/migration history). Workflow change: Nick approved a project-level Bash allow rule for 'npx supabase db push*' (committed in .claude/settings.json) — Claude now runs migration pushes directly, password sourced from .env.local in-shell; the prior per-session manual-push handoff is retired. Wiki coverage map recorded pre-implementation (log 2026-07-21_06); declared general-knowledge fills (UUID mechanism, season_year integer type, FK on-delete behaviors, token length/encoding) flagged as genuine wiki silence at decision time — report carries 'gap found and flagged'. Log cap triggered this session: 2026-07-21_01.md condensed into .claude/logs/ARCHIVE.md (created) and deleted. Next unchecked build item: 'Migration: league_config table' — third item of the 'Schema — league + config' section of 01_foundation.md, per schema-reference/league-configuration-data-model.md."

build_system:
  index: ".claude/BUILD_INDEX.md"
  files_location: ".claude/build/"
  total_files: 7
  current_build_files:
    - ".claude/build/01_foundation.md"

nick_pending:
  - "Before Wave 3a: verify whether Sleeper actually has a public ADP endpoint, or decide a real alternative source — see MANUAL_SETUP_CHECKLIST.md Wave 3a section."
  - "Before Wave 5's waiver/FAAB recommendations item is built: run a dedicated scoping session to define the actual waiver/FAAB candidate scoring formula (opportunity-based — snap/target/touch share trends — is the recommended direction per wiki/topics/league-mechanics/waiver-wire-faab-strategy.md, but no formula exists yet). See 05_eye_candy.md 'Waiver/FAAB recommendations' section, DEFERRED item."

blockers: []

known_issues:
  - "npm audit reports 6 vulns (4 moderate, 2 high) as of last check — pre-existing 1 moderate postcss + 2 high sharp are transitive inside next@16.2.11's pinned deps (only offered 'fix' is downgrading next to 9.x, which is wrong). All dev-time/build-time, none shipped to clients. Re-check npm audit in later sessions."

warnings:
  - "The Supabase database is the shared multi-app 'prolabel' project. Never run supabase db reset or supabase migration repair against it; new foreign history versions get empty local stub files, never repair. Owner-write RLS policies must match Nick's specific auth.uid(), never the blanket authenticated role. Full rules: ARCHITECTURE.md 'Shared-Database Constraints'."

health: "clean"
