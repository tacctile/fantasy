# .claude/STATE.yml
# Living "what happened last session" file. Overwritten completely at the end of every session — never patched.
# Last Updated: 2026-07-22

current_focus: "Wave 1 env/deploy fold complete (items 1-4 of the 'Env, secrets, deploy' section, 2026-07-22): (1) .env.example contract committed — modern Supabase key pair + SUPABASE_DB_PASSWORD marked CLI-only (Nick's Clarify) + ESPN_S2/ESPN_SWID Wave 2 placeholders (Nick's Clarify) carrying espn-api/format-requirements wire rules (SWID literal braces, single-layer encoding, never re-encode); (2) Vercel project verified live via MCP — 'fantasy' (prj_SaeX3FOGNaO9X2AhQaNkOoKHippO) on team nicks-projects-e170820e, nextjs preset, GitHub link proven by deployment metadata (source: git from tacctile/fantasy); (3) env vars verified as the manual-setup record (MCP has no env-var listing tool — functional proof is the health-check item by design); (4) initial deploy confirmed — the env-contract push (0f94ac4) auto-deployed to production and reached READY (~23s build); auto-deploy on push to main is the standing pipeline. Fold ended at item 4 per the Folding Policy hard stop (first touch of a new external service = first Vercel deploy). NEXT UNCHECKED ITEM (next session): the minimal server-side health-check route performing a trivial authenticated Supabase read from the DEPLOYED environment — the wave's actual completion gate and the LAST Wave 1 item. Notes for that session: (a) it may need Nick able to sign in as nick@prolabelco.com (see nick_pending) OR it decides its own auth approach — the secret-key admin client was deliberately deferred 'until the env/deploy health-check session decides its own auth approach' (ARCHITECTURE.md, Supabase Client Wiring); (b) completing it finishes 01_foundation.md — mark the file's Status header 🟢 Complete and update BUILD_INDEX.md's registry row + Wave Roadmap status, then the next session opens 02_data_pipeline.md (Wave 2 — note its MANUAL_SETUP items are all still open: Sleeper league ID, ESPN public/private decision, cookies); (c) each session-end push now triggers a production deploy — expected, not an anomaly."

build_system:
  index: ".claude/BUILD_INDEX.md"
  files_location: ".claude/build/"
  total_files: 7
  current_build_files:
    - ".claude/build/01_foundation.md"

nick_pending:
  - "NEXT SESSION (health-check item): make sure you can actually sign in as nick@prolabelco.com — that pre-existing account's password is whatever you saved when it was created 2026-05-15 (last sign-in 2026-06-06); reset via the Supabase dashboard if forgotten. The health-check item may exercise an authenticated-as-admin read — though the session may instead rule for a secret-key server-side read (that decision was deliberately deferred to it)."
  - "Before Wave 2 (next after the health check): the Wave 2 MANUAL_SETUP items are all still open — have at least one real Sleeper league ID ready; decide public vs. private for each ESPN league (public skips cookie auth); generate espn_s2/SWID for any private league."
  - "Before Wave 3a: verify whether Sleeper actually has a public ADP endpoint, or decide a real alternative source — see MANUAL_SETUP_CHECKLIST.md Wave 3a section."
  - "Before Wave 5's waiver/FAAB recommendations item is built: run a dedicated scoping session to define the actual waiver/FAAB candidate scoring formula (opportunity-based — snap/target/touch share trends — is the recommended direction per wiki/topics/league-mechanics/waiver-wire-faab-strategy.md, but no formula exists yet). See 05_eye_candy.md 'Waiver/FAAB recommendations' section, DEFERRED item."

blockers: []

known_issues:
  - "npm audit reports 6 vulns (4 moderate, 2 high) — last checked live 2026-07-22 (types/client session): pre-existing 1 moderate postcss + 2 high sharp are transitive inside next@16.2.11's pinned deps (only offered 'fix' is downgrading next to 9.x, which is wrong). All dev-time/build-time, none shipped to clients. Not re-checked in the two sessions since (no dependency changes). Re-check in later sessions."

warnings:
  - "ABSOLUTE RULE 13 (canonical: MASTER_CONTEXT.md 'Shared Database Protection — Non-Fantasy Data Is Untouchable'): the Supabase database is the shared multi-app 'prolabel' project — ~49 non-fantasy tables incl. live business production data (Elliott). Never supabase db reset; never migration repair touching foreign history (new foreign versions get empty local stub files, never repair); no raw SQL that could reach a non-fantasy table — verify every migration's table names against ARCHITECTURE.md's 'Database Schema (live)' inventory; collision check before any new table name; ANY ambiguity → stop and ask Nick; mandatory BLAST RADIUS: line on every completion report. Owner-write RLS policies must match Nick's specific auth.uid(), never the blanket authenticated role — realized as public.is_fantasy_admin(); rotate the admin there, never by editing per-table policies. Operational restatement: ARCHITECTURE.md 'Shared-Database Constraints'."
  - "src/lib/supabase/database.types.ts is generated — never hand-edit; regenerate via npm run gen:types after every schema migration. It deliberately contains the whole shared public schema (foreign prolabel tables included) — this is expected, not drift."

health: "clean"
