# .claude/STATE.yml
# Living "what happened last session" file. Overwritten completely at the end of every session — never patched.
# Last Updated: 2026-07-22

current_focus: "Wave 1 (Foundation), feature-build session via BUILD_PROTOCOL: fifth (final) item of the 'Schema — league-scoped state' section of 01_foundation.md is complete and live — the section is now fully checked. player_scores table (20260722054425_create_player_scores_table.sql), created via supabase migration new and pushed via the allowed 'npx supabase db push' rule (password sourced from .env.local in-shell). Shape (Nick's Clarify decisions, all four recommended options): one row per rostered player per league row per week; PK (league_id, sleeper_player_id, week) with season_year duplicated as a non-PK column (matchups precedent — a leagues row is already per-season, so season_year in the PK would be redundant vs. the build item's literal 4-column scoping text). Sources per wiki: Sleeper matchup endpoint players_points map (league-scored by Sleeper, never generic pts_std/pts_ppr presets); ESPN player stat records selected at ingestion by all three discriminators (scoringPeriodId + statSourceId + statSplitTypeId). week = stat-accumulation interval; contest-interval grouping stays on matchups. Full attribution: native_roster_id integer not null (composite FK -> rosters; every row is inherently rostered — free-agent scores never appear in these payloads; the only historical per-week attribution record since roster_players is current-snapshot) + was_starter boolean not null (Sleeper starters[] membership / ESPN lineupSlotId vs. league_config active slot set). Single points numeric not null (zero as-received, negatives legitimate); ESPN projected/live columns deliberately excluded (field names are a declared wiki open question — Wave 2 adds via new migration once verified live). Finality mirrors matchups exactly: fetched_at nullable + is_final not null default false, promoted only by our own sync policy. FKs -> leagues and players both cascade; platform + season_year duplicated (N:1 precedent). RLS enabled at creation (owner-write policies remain the integrity item's scope), set_updated_at trigger. Pre-flight prolabel collision check passed (no existing *score* table). Wiki coverage: source shapes fully covered by sleeper-api/matchup-endpoint + espn-api/roster-response-structure + espn-api/matchup-response-structure; schema-reference still has no league-scoped-state DDL ADR — silence declared at decision time (fifth consecutive session), resolved via precedent + Clarify — report carries 'gap found and flagged' with the standing WIKI NOTE suggesting a 'league-scoped state tables' DDL addendum page. All objects verified live via read-only queries (columns/nullability/defaults, PK, all 3 FKs + cascade, RLS, trigger, migration history row). Log cap triggered this session: 2026-07-21_07.md condensed into .claude/logs/ARCHIVE.md and deleted. Next unchecked build item: 'Migration: draft_state table' — sole item of the 'Schema — draft state (shared, not staged)' section of 01_foundation.md, keyed by league_id with unique (league_id, pick_number) for first-write-wins semantics."

build_system:
  index: ".claude/BUILD_INDEX.md"
  files_location: ".claude/build/"
  total_files: 7
  current_build_files:
    - ".claude/build/01_foundation.md"

nick_pending:
  - "Before Wave 3a: verify whether Sleeper actually has a public ADP endpoint, or decide a real alternative source — see MANUAL_SETUP_CHECKLIST.md Wave 3a section."
  - "Before Wave 5's waiver/FAAB recommendations item is built: run a dedicated scoping session to define the actual waiver/FAAB candidate scoring formula (opportunity-based — snap/target/touch share trends — is the recommended direction per wiki/topics/league-mechanics/waiver-wire-faab-strategy.md, but no formula exists yet). See 05_eye_candy.md 'Waiver/FAAB recommendations' section, DEFERRED item."

blockers: []

known_issues:
  - "npm audit reports 6 vulns (4 moderate, 2 high) as of last check — pre-existing 1 moderate postcss + 2 high sharp are transitive inside next@16.2.11's pinned deps (only offered 'fix' is downgrading next to 9.x, which is wrong). All dev-time/build-time, none shipped to clients. Re-check npm audit in later sessions."

warnings:
  - "The Supabase database is the shared multi-app 'prolabel' project. Never run supabase db reset or supabase migration repair against it; new foreign history versions get empty local stub files, never repair. Owner-write RLS policies must match Nick's specific auth.uid(), never the blanket authenticated role. Full rules: ARCHITECTURE.md 'Shared-Database Constraints'."

health: "clean"
