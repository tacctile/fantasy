# .claude/STATE.yml
# Living "what happened last session" file. Overwritten completely at the end of every session — never patched.
# Last Updated: 2026-07-22

current_focus: "Wave 1 'Schema — integrity' section COMPLETE — the entire Wave 1 schema block is now built and verified live. First session run under the Folding Policy (full-section fold, 3 items, declared before Clarify): (1) FK sweep — verify-and-check-off, no migration: all 18 FKs confirmed live via pg_constraint exactly as designed (7 league-scoped tables + leagues self-ref → platform_league_uuid; 4 player-scoped tables → players.sleeper_player_id; 5 composite (league_id, native_roster_id) → rosters; leagues.owner_id → auth.users; cascade throughout, set-null on the two leagues-side refs). (2) Index sweep — 20260722063616_add_integrity_indexes.sql: 17 single-column indexes (previous_platform_league_uuid — the one unindexed FK; sleeper_player_id on roster_players/player_scores/draft_state; platform + season_year on the 6 N:1 tables + season_year on leagues); no duplicate index where a PK/unique btree already leads with the column (declared wiki silence on index composition — no covering category). (3) Admin auth + RLS — 20260722064037_add_owner_rls_policies.sql: LIVE DISCOVERY changed the plan — nick@prolabelco.com already existed as one of the 2 pre-existing prolabel auth users (created 2026-05-15, UUID 1d5dab52-9b93-4544-a99b-6d4dc3c84a66); duplicate emails impossible, so Nick ruled to REUSE it as the fantasy admin (no new user, no password handoff ever needed — policies only need the UUID; decision of record added to MANUAL_SETUP_CHECKLIST.md, admin-auth item now [x]). public.is_fantasy_admin() helper (stable, search_path='', the single rotation point) + one fantasy_owner_all policy per table x10: FOR ALL TO authenticated, using/with check = (select public.is_fantasy_admin()) — full owner CRUD per Nick's Clarify ruling (literal writes-only would break the wave's health-check read gate; spectator share-token READ policies remain Wave 4). Verified live structurally (10/10 policies, RLS 10/10, function pins UUID, history rows) and functionally (admin-uid insert+select pass rolled back; other prolabel user 42501; anon 42501); security advisors: zero fantasy-owned lints. npm run build passes. Next unchecked build item: first item of 'Types + client wiring' in 01_foundation.md (supabase gen types + browser/server client utilities) — a 2-item sub-section, plausible single fold under the Folding Policy; after that only 'Env, secrets, deploy' remains in Wave 1 (note: its Vercel project-creation/env-vars items are already done manually per MANUAL_SETUP_CHECKLIST [x] entries — that session verifies and checks off rather than re-creating)."

build_system:
  index: ".claude/BUILD_INDEX.md"
  files_location: ".claude/build/"
  total_files: 7
  current_build_files:
    - ".claude/build/01_foundation.md"

nick_pending:
  - "Before the Wave 1 env/deploy session (or first real sign-in): make sure you can actually sign in as nick@prolabelco.com — that pre-existing account's password is whatever you saved when it was created 2026-05-15 (last sign-in 2026-06-06); reset via the Supabase dashboard if forgotten. The health-check item may exercise an authenticated-as-admin read."
  - "Before Wave 3a: verify whether Sleeper actually has a public ADP endpoint, or decide a real alternative source — see MANUAL_SETUP_CHECKLIST.md Wave 3a section."
  - "Before Wave 5's waiver/FAAB recommendations item is built: run a dedicated scoping session to define the actual waiver/FAAB candidate scoring formula (opportunity-based — snap/target/touch share trends — is the recommended direction per wiki/topics/league-mechanics/waiver-wire-faab-strategy.md, but no formula exists yet). See 05_eye_candy.md 'Waiver/FAAB recommendations' section, DEFERRED item."

blockers: []

known_issues:
  - "npm audit reports 6 vulns (4 moderate, 2 high) as of last check — pre-existing 1 moderate postcss + 2 high sharp are transitive inside next@16.2.11's pinned deps (only offered 'fix' is downgrading next to 9.x, which is wrong). All dev-time/build-time, none shipped to clients. Re-check npm audit in later sessions."

warnings:
  - "The Supabase database is the shared multi-app 'prolabel' project. Never run supabase db reset or supabase migration repair against it; new foreign history versions get empty local stub files, never repair. Owner-write RLS policies must match Nick's specific auth.uid(), never the blanket authenticated role — now realized as public.is_fantasy_admin(); rotate the admin there, never by editing per-table policies. Full rules: ARCHITECTURE.md 'Shared-Database Constraints'."

health: "clean"
