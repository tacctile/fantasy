# .claude/STATE.yml
# Living "what happened last session" file. Overwritten completely at the end of every session — never patched.
# Last Updated: 2026-07-21

current_focus: "Wave 1 (Foundation) — Supabase project + migration workflow section of 01_foundation.md remains the last completed build work (schema migrations not yet started). This session (guided walkthrough, non-code) cleared all four open Wave 1 manual-setup items plus one cross-cutting governance decision, so no manual-setup item will halt an upcoming build session: (1) Supabase DB password reset via dashboard and saved to Nick's password manager — ready for the first 'supabase db push' session to request live; (2) Vercel project 'fantasy' created and connected to tacctile/fantasy on GitHub, Next.js framework preset auto-detected; (3) the three Supabase env vars (NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY, SUPABASE_SECRET_KEY) added to Vercel for Production + Preview; (4) admin Supabase Auth credentials decided — real email (nick@prolabelco.com) + a password Nick chose and saved, ready for the Wave 1 RLS/auth build session to request live; (5) Nick authorized adding '!.claude/logs/' to the repo-root .gitignore so session logs get committed and are visible across his VS Code/Cowork/claude.ai environments — decision recorded in MANUAL_SETUP_CHECKLIST.md's new 'Decisions of record' section, but the .gitignore edit itself was NOT applied this session (out of scope for this non-code session) and is now open work for the next session that touches governance/build files. Next unchecked build item remains: 'Migration: platform Postgres enum type, values sleeper and espn' — first item of the 'Schema — platform + identity foundation' section of 01_foundation.md."

build_system:
  index: ".claude/BUILD_INDEX.md"
  files_location: ".claude/build/"
  total_files: 7
  current_build_files:
    - ".claude/build/01_foundation.md"

nick_pending:
  - "Apply the '!.claude/logs/' negation to the repo-root .gitignore — Nick authorized this 2026-07-21 (see MANUAL_SETUP_CHECKLIST.md Decisions of record); not yet applied, needs a session that touches .gitignore to actually make the edit and commit it."
  - "Before Wave 3a: verify whether Sleeper actually has a public ADP endpoint, or decide a real alternative source — see MANUAL_SETUP_CHECKLIST.md Wave 3a section."

blockers: []

known_issues:
  - "npm audit reports 6 vulns (4 moderate, 2 high) as of last check — pre-existing 1 moderate postcss + 2 high sharp are transitive inside next@16.2.11's pinned deps (only offered 'fix' is downgrading next to 9.x, which is wrong). All dev-time/build-time, none shipped to clients. Re-check npm audit in later sessions."

warnings: []

health: "clean"
