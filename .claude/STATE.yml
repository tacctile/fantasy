# .claude/STATE.yml
# Living "what happened last session" file. Overwritten completely at the end of every session — never patched.
# Last Updated: 2026-07-22

current_focus: "Wave 1 (Foundation), feature-build session via BUILD_PROTOCOL: fourth item of the 'Schema — league-scoped state' section of 01_foundation.md is complete and live — standings table (20260722053523_create_standings_table.sql), created via supabase migration new and pushed via the allowed 'npx supabase db push' rule (password sourced from .env.local in-shell). Shape (Nick's Clarify decisions, all four recommended options): season-cumulative current snapshot — one row per roster per league, PK (league_id, native_roster_id) mirroring rosters, Wave 2 sync overwrites in place; no weekly history (v1 rejects season-over-season views; per-week points live on matchups). Minimal five columns: wins/losses/ties integer + points_for/points_against numeric, all not null default 0 (a fresh league genuinely is 0-0 with 0.00 points — zero is a true state, deliberately diverging from matchups' nullable as-received points). Points columns hold the single recombined decimal per the sleeper-api/roster-endpoint ADR (fpts + fpts_decimal/100 recombined at ingestion, split wire pair never stored); ESPN totals land as-received from mTeam. ESPN rank/playoff-seed and Sleeper waiver/transaction counters deliberately excluded — no registered v1 consumer, and ESPN's exact record field names are a wiki-declared open question (mStandings undocumented); Wave 2 adds them via new migration once verified against a live payload. No fetched_at — standings have no finality concept; updated_at + trigger suffice. Composite FK -> rosters + FK -> leagues, both on delete cascade; platform + season_year duplicated (N:1 precedent). RLS enabled at creation (owner-write policies remain the integrity item's scope), set_updated_at trigger. Pre-flight prolabel collision check passed (no existing standings table). Wiki coverage: Sleeper figures/recombination covered by sleeper-api/roster-endpoint, ESPN sourcing covered by espn-api/view-parameter-reference; schema-reference still has no league-scoped-state DDL ADR — silence declared at decision time (fourth consecutive session), resolved via precedent + Clarify — report carries 'gap found and flagged' with the standing WIKI NOTE suggesting a 'league-scoped state tables' DDL addendum page. All objects verified live via read-only queries (columns/nullability/defaults, PK, both FKs + cascade, RLS, trigger, migration history row). Log cap triggered this session: 2026-07-21_06.md condensed into .claude/logs/ARCHIVE.md and deleted. Next unchecked build item: 'Migration: player_scores table' — fifth (final) item of the 'Schema — league-scoped state' section of 01_foundation.md, composite scoping on (league_id, sleeper_player_id, season_year, week)."

build_system:
  index: ".claude/BUILD_INDEX.md"
  files_location: ".claude/build/"
  total_files: 7
  current_build_files:
    - ".claude/build/01_foundation.md"

nick_pending:
  - "Before Wave 3a: verify whether Sleeper actually has a public ADP endpoint, or decide a real alternative source — see MANUAL_SETUP_CHECKLIST.md Wave 3a section."
  - "Before Wave 5's waiver/FAAB recommendations item is built: run a dedicated scoping session to define the actual waiver/FAAB candidate scoring formula (opportunity-based — snap/target/touch share trends — is the recommended direction per wiki/topics/league-mechanics/waiver-wire-faab-strategy.md, but no formula exists yet). See 05_eye_candy.md 'Waiver/FAAB recommendations' section, DEFERRED item."

blockers: []

known_issues:
  - "npm audit reports 6 vulns (4 moderate, 2 high) as of last check — pre-existing 1 moderate postcss + 2 high sharp are transitive inside next@16.2.11's pinned deps (only offered 'fix' is downgrading next to 9.x, which is wrong). All dev-time/build-time, none shipped to clients. Re-check npm audit in later sessions."

warnings:
  - "The Supabase database is the shared multi-app 'prolabel' project. Never run supabase db reset or supabase migration repair against it; new foreign history versions get empty local stub files, never repair. Owner-write RLS policies must match Nick's specific auth.uid(), never the blanket authenticated role. Full rules: ARCHITECTURE.md 'Shared-Database Constraints'."

health: "clean"
