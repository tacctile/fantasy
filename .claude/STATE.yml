# .claude/STATE.yml
# Living "what happened last session" file. Overwritten completely at the end of every session — never patched.
# Last Updated: 2026-07-23

current_focus: "WAVE 3B COMPLETE — 03b_draft_assistant_live_draft.md is 🟢. Final sub-section = RESILIENCE 2/2 [x], folded as ONE artifact (2 items; NOT a named singleton; fit the live-behavior ceiling 2-3). READ-ONLY session: no migration, and in fact ZERO DB contact (item 1 is client render-resilience; item 2's race test drives recordManualPick against a FAKE in-memory client, the other four tests are pure). Item 1 (read-only fallback to Wave 3a static behavior on live-layer degradation) — 3 Nick-signed Clarify: (1) PER-REGION error boundaries; (2) a live-MERGE failure falls back to the 3a base `players` pool (table stays usable read-only); (3) degraded regions show a QUIET INLINE muted notice. Built: NEW src/components/ui/error-boundary.tsx — the app's ONE React class component (fallback + resetKeys that clears the error when fresh data lands so a region revived by the next pick recovers; catches render/commit throws only — async/event faults handled at their own surfaces; never renders raw error content). Each live-enhancement region wrapped in its OWN ErrorBoundary in the shell (LiveDraftStrip, BpaRecommendationsPanel, DraftQueuePanel, RosterPanel, RecentPicksFeed → muted RegionFallback notice) + the run badge in board-toolbar.tsx + the LiveStatusIndicator in draft-board-header.tsx (both fallback=null, appear-only micro-elements — silence matches their resting state, disclosed judgment call). Shell derivations feeding the STATIC table guarded at the source (a child boundary can't catch a shell-render throw): mergedPlayers try/catch → base 3a `players`; runBoard try/catch → new EMPTY_RUN_BOARD const; prune effect try/caught; fireAutoPick disarms auto-pick on a selection fault (its hidden-tab fire moved to a 0ms timer so the disarm setState never runs synchronously in the effect — react-hooks/set-state-in-effect fix). Poll-network degradation was already handled (ticker catches + degraded indicator). Item 2 (durable test suite) — 3 Nick-signed Clarify: (1) VITEST as the permanent runner (repo had NONE — every prior verification was a deleted tsx harness; the Waves 4-6 foundation; devDeps only, no runtime/DB impact); (2) FAKE in-memory Supabase client for the DB race per Rule 13 (models the (league_id,pick_number) ON-CONFLICT-DO-NOTHING PK via a synchronous check-and-insert); (3) 5 REQUIRED cases only, co-located *.test.ts. Added vitest devDep + test/test:watch scripts + vitest.config.ts (node env, @/→src alias mirroring tsconfig, include src/**/*.test.ts). Five files, 14 tests ALL PASS: draft-picks.test.ts (3 writers race one pick → exactly 1 accepted + 2 conflicts on the winner; poller-already-won; uncontested accept), bpa/board.test.ts (computeBpaBoard deepens replacement + raises a survivor VORP as the pool shrinks — the no-cached-baseline mandate), bpa/tiers.test.ts (2 tiers → 1 when the cliff-defining pick leaves), bpa/runs.test.ts (window trigger + reset-on-scroll-out + shallow-QB threshold + K/DEF never flagged), draft-queue.test.ts (auto-pick skips a 3rd QB, falls through when all capped, takes an under-cap entry). Verified: npm test 14/14 + tsc (now typechecks the test files + vitest.config) + lint + build all clean; the test files stay OUT of the route bundle. SELF-LOCATION FOR NEXT SESSION: 03b is 🟢, so the first self-locatable unchecked item moves to WAVE 4 (04_league_dashboard.md) — the nav-shell sub-section (the persistent admin sidebar navigation shell + command-center home; the share-link panel rides the share-token RLS named singleton #5, NOT this fold). 02's ESPN residue stays [!] external-timing, 03a's bye-week stays [>] schedule-gated, 03c stays 🔴 ESPN-blocked. Every 03b sub-section now [x]: write path 4/4, orchestration 3/3, client live sync 5/5, UI extensions 5/5, projections 3/3, BPA engine 9/9, tier-cliff 4/4, positional-run 4/4, queue+auto-pick 4/4, RESILIENCE 2/2."

build_system:
  index: ".claude/BUILD_INDEX.md"
  files_location: ".claude/build/"
  total_files: 8
  current_build_files:
    - ".claude/build/04_league_dashboard.md"  # 🟡 — NEXT self-locatable: data-access 5/5 [x], freshness [x], 4 display components [x], page assembly [x], league selector [x]; next fold = the nav-shell sub-section (share-link panel deferred to the share-token RLS singleton #5)
    - ".claude/build/03b_draft_assistant_live_draft.md"  # 🟢 COMPLETE 2026-07-23 (all sub-sections [x]; resilience 2/2 shipped this session — reusable ErrorBoundary + Vitest durable suite). Nothing left here.
    - ".claude/build/03a_draft_assistant_static_board.md"  # 🟡 — all five sub-sections complete; sole remainder = bye-week [>] (schedule-source-gated); goes 🟢 when that ships or is re-ruled
    - ".claude/build/02_data_pipeline.md"  # ESPN residue [!], cron cadence [~], finality promotion [>] (Nick-signed deferral) — nothing self-locatable here

nick_pending:
  - "VISUAL CHECK BACKLOG — Wave 3b is feature-complete but its live-draft surfaces have never been eyeballed against a REAL live draft (the completed 2025 draft has no live clock). On the live board: draft queue (star toggles on player rows AND BPA rows; sidebar Queue section with reorder/remove; the Auto-pick off/armed pill enabled only once a draft is live AND 'my team' is set); the positional-run badge near the position chips; the Best-tier depth counter + Recommendations list at the top of the BPA panel; the live strip/status, recent-picks feed + Undo, roster cards, optimistic Draft fold, live-sync dimming. NEW this session (resilience) but INVISIBLE in normal operation: if a live region throws it degrades to a muted 'X unavailable' notice while the static board survives, and the player table falls back to the pre-live static pool if the merge ever fails — only observable by forcing an error, so nothing to routinely eyeball. Dashboard at /leagues/<id>: header, week pills, matchup cards, standings, power rankings, ?player= sheet."
  - "OPTIONAL FUTURE — superflex/best-ball run sensitivity (deferred 2026-07-23 by Nick's Clarify): positional-run-detection-draft-day.md Key Decision mandates different run-sensitivity for superflex/2QB (QB scarcity structural — proactive early QB is correct, not a run) and best-ball. The shipped detectPositionalRuns applies uniform thresholds because no superflex/best-ball league is connected (live league is 1QB redraft). When one connects, make the QB threshold format-aware via resolveAdpScoringFormat — small change to runs.ts + a test (the durable suite now exists — add a runs.test.ts case) + the badge picks it up for free. NOTE: the auto-pick caps ARE already format-aware for free (superflex lifts QB's positionCap via SUPER_FLEX flex eligibility). Not blocking; its own tiny fold. Also a WIKI NOTE."
  - "OPTIONAL FUTURE / SCOPE QUESTION — demand-vs-panic run classification (WIKI NOTE, unchanged 2026-07-23): positional-run-detection-draft-day.md mandates classifying runs into demand-driven (tier-cliff) vs panic-driven (contagion/ahead-of-ADP) with OPPOSITE recommended responses (enter/fade vs wait). The shipped positional-run sub-section does a count-based flag + a badge that COMPOSES the tier-drain signal in its tooltip — NOT the full classification. If the fuller wiki model is wanted for v1 it needs a NEW registered build item / Nick-signed scope amendment. Decide whenever; not blocking."
  - "OPTIONAL FUTURE — per-league memory for 'my team' AND the queue: both are client-side/re-pick-each-session by decision (my team 2026-07-22 Clarify; the queue is localStorage per league, so it survives reloads but not a cleared browser / another device). If you later want durable cross-device state, that's a small future migration (a per-league my-roster + a draft_queue table) + tiny reads — its own small fold. Not blocking."
  - "FINALITY PROMOTION — build scheduling (design Nick-signed + registered 2026-07-22 as the [>] item in 02_data_pipeline.md's cron sub-section; mechanism in known_issues): build as its own standalone fold OR bundle with the season-start cadence session. HARD DEADLINE: before the 2026 NFL week 1→2 rollover (~2026-09-15; promotion first actually needed ~2026-09-18). Flip the item [>]→[ ] when its build session starts."
  - "BLOCKED ON EXTERNAL TIMING — nothing to do until ~mid-August 2026 (expected clear ~2026-08-19 → 2026-09-02): Wave 2 ESPN manual items (public/private per league, espn_s2/SWID for private, league IDs + season year). ESPN leagues commissioner-locked until 2026 season setup. When they open: resolve in a plain chat, flip them in MANUAL_SETUP_CHECKLIST.md, flip 02_data_pipeline.md's 9 ESPN items + 03c's [!]→[ ]. Per the backsweep practice (BUILD_PROTOCOL Amendment Hygiene) that plain chat must sweep the confirmations against already-registered 02/03c text AND against ALL shipped Wave 4 + Wave 3b surfaces (platform-agnostic by construction): recordManualPick (platform-agnostic; 03c auction extends via the amount field on the same handleDraft seam), draft_sessions/poll orchestration (skipped_non_sleeper by design; 03c ESPN poll extends runRecordedDraftPoll), client-side live sync seams (listDraftPicks/merge/optimistic overlay platform-agnostic via crosswalk), the DraftOrderMeta on-clock path (slot_to_roster_id is Sleeper-specific; ESPN needs its own draft-order source — which the AUTO-PICK away-trigger also depends on via projectOnClock, so ESPN auto-pick's on-clock detection waits on that ESPN draft-order source), the projections/BPA/tier-cliff layer (player_projections 'sleeper'-only; ESPN leagues' BPA/tiers/counter use the same Sleeper projections automatically), the run-detection layer + badge (platform-agnostic — reads pick_number + catalog position off draft_state), the QUEUE + AUTO-PICK layer (queue is client-side/platform-agnostic; auto-pick writes through platform-agnostic recordManualPick + reads the platform-agnostic client merge — EXCEPT the on-clock away-trigger, per above), AND the RESILIENCE layer as shipped (the per-region ErrorBoundary + the merge/runBoard fallbacks are UI-generic and platform-agnostic — ESPN leagues get the same graceful degradation for free; the Vitest suite is pure/fake-client and platform-independent). Sweep any item text that assumed otherwise."
  - "SEASON START (~Sept 2026): revisit league-sync cadence — the specced 15-minute cron needs Vercel Pro (~$20/mo) or an alternative trigger; currently daily on Hobby per your 2026-07-22 decision. Flip 02_data_pipeline.md's cron-schedule item [~]→[x] when sub-daily ships. If Pro is bought, /api/cron/sync-adp is the ready mount point. NOTE: live-draft polling + BPA/tier/run/tier-counter recompute + run badge + the queue/auto-pick all do NOT depend on this — client-driven, no cron slot at any tier."
  - "BYE-WEEK [>] ITEM (03a) still awaits its schedule-source decision (plain chat: pick a schedule source, then a build session ships table+ingestion+row wiring — flips 03a 🟢). No longer blocking anything, the only remainder between 03a and 🟢. Related small observation: the 3a board pool's fullName is also null for DST rows (renders the abbreviation fallback); the item-4 name compose exists in draft-picks.ts if a future correctness pass wants to reuse it for getDraftBoardData."
  - "When your 2026 Sleeper league renews (new league_id every season): run npm run sync:league -- <new_id> once to connect it; every daily league-state cron run (and npm run sync:sleeper) covers it after. The 2025 league (1257464256710856704) is fully ingested incl. its 170-pick draft. With auto-land live, the renewed league (season 2026 > 2025) becomes the DASHBOARD you land on after sign-in. Draft-day note: the renewed league is where Start draft + live polling + click-to-draft + the live strip/status/roster/recent-picks feed + the BPA panel + the tier counter + the run badge + the draft queue + auto-pick + the read-only resilience fallback all run in anger. The BPA + tier + run layer uses the LATEST ingested projections/ADP season (2026) regardless of the league's own season, so it is already current-market."
  - "Before Wave 5's waiver/FAAB recommendations item is built: run a dedicated scoping session to define the actual waiver/FAAB candidate scoring formula (opportunity-based — snap/target/touch share trends — is the recommended direction per wiki/topics/league-mechanics/waiver-wire-faab-strategy.md, but no formula exists yet)."

blockers:
  - "EXTERNAL TIMING (dated 2026-07-22): 02_data_pipeline.md's two ESPN sub-sections (9 items, [!]) are blocked until ESPN leagues open for the 2026 season — commissioner-locked, expected ~2026-08-19 → 2026-09-02. Not resolvable by Nick or any build session before then; do NOT guess visibility or fabricate placeholder league IDs (Nick's explicit instruction). Downstream: Wave 3c inherits this blocker (ESPN draft mechanics + auction). Does not stall the roadmap: with 3b now 🟢, the buildable frontier is WAVE 4 (nav-shell next) — plus 02's finality-promotion [>] and 03a's bye-week [>] whenever their plain-chat decisions land."

known_issues:
  - "npm audit reports 6 vulns (4 moderate, 2 high) — re-checked live 2026-07-23 after adding vitest (32 pkgs added, count UNCHANGED — vitest introduced no new vulns): pre-existing 1 moderate postcss + 2 high sharp are transitive inside next@16.2.11's pinned deps (only offered 'fix' is downgrading next to 9.x, which is wrong). All dev-time/build-time, none shipped to clients. Re-check in later sessions."
  - "FINALITY-PROMOTION GAP (surfaced + designed + registered 2026-07-22 — still UNBUILT): in-season, matchups/player_scores rows get is_final=true only when league lifecycle status reads 'complete' at sync time; the scheduled sync is current-week-only, so finished weeks stay is_final=false all season. Covering wiki: sleeper-api/matchup-endpoint (finality Key Decision #3). Nick-signed design: previous-week re-sync + promotion inside the daily league-state cron (week-advance = games-complete proxy; Friday-or-later UTC stable-read match = trigger) + league-complete backstop sweep; needs a caller-driven finality extension to syncLeagueMatchups. Registered as the [>] item in 02_data_pipeline.md's cron sub-section. Zero live consequence until the season's first week rollover; HARD DEADLINE ~2026-09-15/18. WAVE 4 UI side SHIPPED (UnofficialChip + AsOfTime render is_final/fetched_at honestly)."

warnings:
  - "ABSOLUTE RULE 13 (canonical: MASTER_CONTEXT.md 'Shared Database Protection'): the Supabase database is the shared multi-app 'prolabel' project — ~49 non-fantasy tables incl. live business production data (Elliott). Never supabase db reset; never migration repair touching foreign history (new foreign versions get empty local stub files); no raw SQL that could reach a non-fantasy table — verify every migration's table names against ARCHITECTURE.md's 'Database Schema (live)' inventory; collision check before any new table name; ANY ambiguity → stop and ask Nick; mandatory BLAST RADIUS: line on every report. Owner-write RLS must match Nick's specific auth.uid() via public.is_fantasy_admin(), never the blanket authenticated role. THIS SESSION (resilience 2/2): ZERO DB contact — item 1 is client render-resilience, item 2's race test drives recordManualPick against a FAKE in-memory client (no Supabase), the other four tests are pure functions. No migration, no reads, no writes. WAVE 4 SPECIFIC (for the nav-shell fold coming next): read-only nav UI; the share-token RLS sub-section (a LATER named singleton #5) adds SELECT policies on six fantasy tables with its boundary tests; owner policies stay pinned to is_fantasy_admin(). draft_sessions/draft_state are NEVER on any spectator-policy list; player_projections/adp_rankings have no spectator consumer either."
  - "TESTING INFRASTRUCTURE (NEW 2026-07-23, resilience item 2): Vitest is THE durable test runner (vitest.config.ts at repo root — node env, @/→src alias mirroring tsconfig, include src/**/*.test.ts; scripts `test`=vitest run, `test:watch`=vitest). Tests are CO-LOCATED as *.test.ts beside source — typechecked by tsc and linted, but never imported by a route so next build excludes them from the bundle. RULE-13 DISCIPLINE FOR TESTS: never write to the shared prolabel DB from a test — the DB race is tested against a FAKE in-memory Supabase client (draft-picks.test.ts: a chainable+thenable query builder modelling the (league_id,pick_number) ON-CONFLICT-DO-NOTHING PK via a synchronous check-and-insert). Extend this suite, don't recreate throwaway tsx harnesses (the old pattern). Current: 14 tests / 5 files (draft-picks race, board recompute, tiers recompute, runs window, draft-queue auto-pick)."
  - "CLIENT-SIDE LIVE SYNC SEAMS (updated 2026-07-23): the live board path is ticker → pollActiveDraft (full listDraftPicks snapshot per executed poll) → shell's fingerprint-deduped livePicks state (+ optimistic pendingPicks overlay + pollHealth + DraftOrderMeta + panelTiers + selfRosterId + the queue store). livePicks CONSUMERS: live-picks.ts mergePicksIntoPlayers → both board regions; the roster panel + recent-picks feed read livePicks directly; the BPA panel reads it as a useEffect dep; the positional-run badge computes detectPositionalRuns in a useMemo over it; the queue-prune effect (livePicks→draftedIds) + auto-pick (availability from the ONE client merge, roster shape from livePicks) read it. RESILIENCE (this session): every live-enhancement region is wrapped in its OWN ErrorBoundary (per-region, Nick's Clarify) so a render fault degrades only that region while the static board survives; the shell's own derivations that feed the static table are guarded AT THE SOURCE because a child boundary can't catch a shell-render throw — mergedPlayers falls back to the base 3a `players`, runBoard to EMPTY_RUN_BOARD, the prune effect is try/caught, fireAutoPick disarms on fault (its hidden-tab fire is a 0ms timer so the disarm setState never runs synchronously in the effect). Extend THIS path — never a second poll driver, never a second merge, never a second pick-write UI path (handleDraft via the DraftablePlayer seam — auto-pick reuses recordManualPick), never a second undo/tier/board/run/QUEUE source. Key invariants unchanged: 'drafted'/draftPending produced ONLY by the client merge; a confirmed snapshot row ALWAYS wins over pending; 'rostered' wins over the whole overlay; ON-CLOCK IS A PROJECTION (projectOnClock — display + the auto-pick away-trigger's my-turn check, never authoritative: first-write-wins is the real arbiter). rosterLabel (player-row.tsx) is THE roster display convention."
  - "MANUAL-PICK WRITE PATH CENTRALIZATION (updated 2026-07-23): src/services/draft-picks.ts recordManualPick is the ONE local pick-write path — now with DURABLE unit tests (draft-picks.test.ts: first-write-wins race vs a fake client, exactly one accepted). AUTO-PICK writes through it (shell fireAutoPick → the SAME handleDraft → submitManualPick → recordManualPick, source='manual', undoable, first-write-wins conflict-safe — a poller-won pick makes the auto-pick attempt conflict + roll back, so auto-pick can NEVER desync a real Sleeper draft or write to Sleeper). One attempt per pick number (autoPickAttemptRef), and it DISARMS on a selection fault (resilience). No second draft_state insert path outside recordManualPick and the platform pollers. Server-action auth pattern (6 actions incl. read-only fetchBpaRecommendations): gate INSIDE each action (getAdminAuthState) + RLS as the second wall. listDraftPicks is THE ordered pick-snapshot read. Undo has its ONE UI (recent-picks feed)."
  - "BPA + TIER + RUN + QUEUE/AUTO-PICK LAYER CENTRALIZATION (updated 2026-07-23): src/services/bpa/ is the ONE home of projection scoring + VORP + tier + run detection + the recommendations query (pure sub-layer scoring/replacement/calibration/base-value/board/need/tiers/runs; I/O = recommendations.ts) — now under durable Vitest coverage (board.test.ts recompute, tiers.test.ts recompute, runs.test.ts window). src/components/draft-board/ pure client layers: live-picks.ts (merge/next-pick/on-clock), live-status.ts (poll health), draft-queue.ts (queue ops + localStorage store + roster-aware auto-pick selection — positionCap reads FLEX_ELIGIBILITY from replacement.ts, the ONE flex-eligibility home; AUTO_PICK_BENCH_MARGIN + AUTO_PICK_DWELL_MS are DECLARED-WIKI-SILENCE live-tuned constants — draft-queue.test.ts covers the cap/skip/fall-through). UI homes: bpa-recommendations-panel.tsx (recommendations + TierDepthStrip), positional-run-badges.tsx (run badge), draft-queue-panel.tsx (queue list + reorder + auto-pick arm), queue-toggle.tsx (the shared star). Run detection is NOT routed through the value query; the queue is NOT reordered by the engine; need/tiers/runs stay SEPARATE parallel signals never multiplied into base value; auto-pick's roster caps are a SEPARATE roster-construction heuristic, never a value input. NEVER add caching, a second scoring/VORP/tier/run/queue derivation, a second projections read, a second recommendations query, a second draft-submit path, or a second run/queue source. Layout parsing stays parseRosterSlotLayout, format mapping stays resolveAdpScoringFormat (both draft-board.ts)."
  - "PLAYER_SCORES IS INGESTED, NEVER COMPUTED (Nick-signed canon): platform-scored values land as-received (Sleeper players_points; ESPN as-received when unblocked). NO compute-from-raw-stats path for ACTUAL scores in v1. PROJECTIONS ARE THE SANCTIONED EXCEPTION: player_projections stores component projection stats, and the BPA engine scores them with league_config weights at read time (bpa/scoring.ts) — projected value, never a substitute for actual player_scores. Tier detection reads those same league-scored projections (via base-value output); run detection reads NO projections (only draft_state pick order + catalog position); the QUEUE/AUTO-PICK layer reads NO projections either. Never invent score rows for unrostered weeks (FA-week gap accepted for v1)."
  - "ADMIN/SPECTATOR UI BOUNDARY (Wave 4, live 2026-07-22): src/components/dashboard/ is ADMIN-surface UI — the spectator surface imports ZERO components from it (or from draft-board/ or ui/dropdown-menu's draft usages); spectator gets its own components (04's spectator sub-section). The BPA panel, tier counter, run badge, queue/auto-pick panel, AND the draft board's resilience fallbacks are likewise ADMIN-only — no spectator consumer, ever. Shared data-access (services/dashboard.ts) is the ONLY sanctioned sharing. Freshness primitives UnofficialChip + AsOfTime are THE rendering of is_final/fetched_at. The player-card open mechanism is the URL-driven ?player= sheet. NEXT FOLD is the Wave 4 nav-shell (persistent admin sidebar + command-center home) — an admin surface; the share-link/spectator surface is separate and rides the share-token RLS singleton."
  - "DARK MODE ONLY IS LOAD-BEARING IN CODE (live 2026-07-22): globals.css holds the app's single always-on token set in :root — NO light block, NO .dark override, NO theme toggle; the `dark` class on <html> keeps vendored shadcn dark:-variant tunings active. Never add a light token set, a prefers-color-scheme branch, or remove that class. ui/sonner.tsx hardcodes theme='dark'. --radius stays 0.625rem. Position identity tokens --pos-* are badges/chips ONLY; teal = interactive/live only; amber (--warning) = roster-need. TierDepthStrip + PositionalRunBadges + the QUEUE UI + the resilience RegionFallback notice all follow this: uniform muted, tabular where numeric, PositionBadge reused for position color, NO new token — the queue star is neutral foreground/muted, the auto-pick arm pill reuses the primary/secondary chip states, and the RegionFallback degraded notice is muted-foreground (no new token). Non-blocking."
  - "OWNER AUTH SURFACE (live 2026-07-22) — three walls, keep all three: (1) src/proxy.ts (Next 16 proxy, NOT middleware.ts) refreshes the Supabase session per matched request and 307s unauth /leagues/* to /login — matcher EXCLUDES /api. (2) The (admin) layout gate uses getAdminAuthState (is_fantasy_admin RPC) — never hardcode the admin UUID; a signed-in non-fantasy prolabel user gets the zero-data NotAuthorizedCard. (3) Per-table RLS. ROOT AUTO-LAND targets the dashboard (/leagues/<id>). Server actions add a FOURTH gate: auth INSIDE each action — SIX actions (submitManualPick/undoManualPick/startDraftSession/endDraftSession/pollActiveDraft/fetchBpaRecommendations + login's signOut). Auto-pick adds NO new action — it reuses submitManualPick via handleDraft."
  - "src/lib/supabase/database.types.ts is generated — never hand-edit; regenerate via npm run gen:types after every schema migration. It deliberately contains the whole shared public schema (foreign prolabel tables included) — expected, not drift. Trap: generated Insert types list stored generated columns (matchups.effective_points) as insertable — Postgres rejects them; sync code must omit them. Note: the leagues PK column is platform_league_uuid (NOT `id`). getBpaRecommendations reads player_projections with a PAGINATED .range loop (1000/page). The queue/auto-pick layer reads NO Supabase of its own; the test suite reads NO real Supabase (the DB race test uses a FAKE in-memory client — Rule 13)."
  - "src/lib/supabase/admin.ts bypasses RLS (secret key) — server-only, never import from client code. The draft-board, dashboard, BPA/tier/run, and queue/auto-pick routes/actions deliberately do NOT use it — owner pages read/write through the RLS server client as the signed-in admin. The Vitest suite uses NO Supabase client at all (pure functions + a fake in-memory client for the race). Temp verification scripts (deleted after use) may still use the admin client for read-only checks, but durable coverage now lives in the co-located *.test.ts suite instead of throwaway harnesses."
  - "Sleeper player catalog (/players/nfl): at most ONCE PER DAY — the cron route enforces it with a hard guard on sync_runs; the manual script path (npm run sync:players) bypasses the guard — manual cadence discipline applies. Per-league endpoints paced by the client's global 250ms min-gate (also absorbs 5s live-draft polling, ≤3 requests per executed tick). The BPA panel's per-pick recompute, tier recompute, the tier counter, run detection, the run badge, AND the queue/auto-pick make NO Sleeper calls — the BPA/tier layer reads Supabase only, run detection + the queue read nothing/localStorage, and auto-pick's write goes through the existing draft_state action (no Sleeper write, ever)."
  - "api.sleeper.com (ADP/projections surface) is NOT api.sleeper.app/v1: undocumented, no contract — its consumer is the quarantined src/services/adp/ ONLY (own client, own pacing gate). The SAME fused ingestion run feeds BOTH adp_rankings AND player_projections from one fetch; never a second api.sleeper.com client or a separate projections fetch. 999.0 in any adp_* field is a 'no ADP in this format' sentinel — never data in adp_rankings; player_projections.stats retains the RAW object. ADP is never read from player_projections. Covering wiki: sleeper-api/projections-endpoint."
  - "League-format→ADP-format mapping is CENTRALIZED in resolveAdpScoringFormat (src/services/draft-board.ts, Nick-signed): superflex→'2qb' precedence, then ppr buckets; dynasty/IDP unmapped in v1; NO cross-format fallback. Any consumer calls this function — never re-derives. Same centralization for roster-slot layouts: parseRosterSlotLayout (same module) is the ONE parser of roster_settings_raw — bpa/replacement.ts, need.ts, getBpaRecommendations, AND the auto-pick positionCap all consume its RosterSlotLayout output, never re-derive. FLEX_ELIGIBILITY is the ONE flex-eligibility home (exported from bpa/replacement.ts) — need.ts AND draft-queue.ts's positionCap import it, never re-derive. DraftablePlayer (draft-board.ts) is the ONE minimal shape the shared handleDraft consumes (auto-pick passes a full DraftBoardPlayer, which satisfies it); RunPick (bpa/runs.ts) is the analogous minimal shape run detection consumes — non-table draft surfaces reuse these, never a cast or a parallel submit/read."

health: "clean"
