# .claude/STATE.yml
# Living "what happened last session" file. Overwritten completely at the end of every session — never patched.
# Last Updated: 2026-07-22

current_focus: "WAVE 3B — BPA RECOMMENDATION ENGINE COMPLETE 9/9 (2026-07-22, feature-build; fold = one independently verifiable artifact 'the BPA recommendations panel', items 7-9, sitting exactly at the client-side live-behavior ceiling of 3). The engine crossed from a headless query layer (items 1-6) into a WORKING draft-assist surface. THREE items: item 7 = new src/components/draft-board/bpa-recommendations-panel.tsx ('use client') at the TOP of the board sidebar (Nick's placement) — fetchBpaRecommendations(topN=8, Nick's count), top-N rows ranked PURELY by base VORP with market ADP beside the value and an independent roster-need badge (NEED_LABELS starter='Fills starter need'/bench='Bench depth'/full='Position full', verbatim build-file copy) shown ALONGSIDE never merged into the ranking; muted-tier chips ('starter' borrows bg-warning/10 text-warning like RosterPanel's need flag, 'bench'/'full' muted, teal untouched); tabular-nums on VORP/ADP; reuses PositionBadge + rosterLabel (no dup); my-team picker over rosters sets selfRosterId (Nick's Clarify — NO persisted marker, re-pick each session; value-only + no need badges until a team is picked); honest loading(first-load-only)/error(typed per ok:false reason)/empty/structural_only-ADP-note states. Item 8 = rows wire into the shell's SAME handleDraft via the roster dropdown (never a parallel submit — build-file + STATE mandate); realized by narrowing handleDraft's param to the new DraftablePlayer (Pick<DraftBoardPlayer,'sleeperPlayerId'|'fullName'> in services/draft-board.ts — the minimal shape the handler consumes) so the panel reuses the one optimistic write path with no cast/duplication; PlayerBoard/PlayerRow keep passing full DraftBoardPlayer (contravariantly assignable); pending players read 'Drafting…', Draft control on the same draftEnabled gate as the table. Item 9 = a useEffect keyed on [leagueId, livePicks, selfRosterId] re-invokes fetchBpaRecommendations off the SAME shell-owned snapshot (never a second pick-event feed); the shell's fingerprint bail-out means a steady 5s poll returning an identical snapshot keeps the same reference and does NOT refetch — only a real pick change (manual/Sleeper poll/undo/optimistic-adopt) flips the reference; the shrinking-pool recompute lives in getBpaRecommendations (pool = projections − draft_state), not cached in the panel; monotonic request-seq ref drops out-of-order responses; stale-while-revalidate keeps the prior list across refetches (no flicker, and avoids a synchronous set-state-in-effect). Verified: tsc/lint/build clean + LIVE read-only on the real league (temp scripts/verify-bpa-panel.ts harness, deleted): topN=8 returns exactly 8, calibration=adp, pool 3122 (3292−170 drafted), value-only need=null throughout, with-selfRosterId need annotations present AND ordering/VORP invariant vs value-only (need NEVER reorders — item 5 constraint), need null only when no self roster; all 'full' = the documented correct-not-bug fully-drafted extreme (roster 1's completed 17-player roster: starters 10/10, bench 0). HONEST LIMITATION: data contract live-verified; the authenticated browser render (picker, stale-while-revalidate, item 8/9 wiring) is tsc+build-verified and reuses already-live seams, not driven headlessly this session. SELF-LOCATION FOR NEXT SESSION: 03b Tier-cliff detection sub-section (4 items — pure tier-boundary calc per position, target tier counts, recompute-alongside-VORP, live 'N remaining in this tier' counter). Items 1-3 are pure compute; item 4 is the board-UI counter. Likely fold = the pure calc trio (items 1-3, one verifiable artifact) OR the whole 4-item sub-section if it composes; assess at session start. NEXT WIKI: league-mechanics (already the entered category) — ADP/tier-breaks/positional-scarcity pages cover tier-boundary thresholds; check ROUTING for a tier-specific page before inventing per-position gap thresholds (Rule 12). BPA engine sub-section 9/9 — 03b stays 🟡 (tier-cliff, positional-run, queue/auto-pick, resilience remain)."

build_system:
  index: ".claude/BUILD_INDEX.md"
  files_location: ".claude/build/"
  total_files: 8
  current_build_files:
    - ".claude/build/03b_draft_assistant_live_draft.md"  # 🟡 — write path 4/4 [x], orchestration 3/3 [x], client-side live sync 5/5 [x], UI extensions 5/5 [x], projections ingestion 3/3 [x], BPA engine 9/9 [x] (items 1-9 complete 2026-07-22: pure layer + query layer + fetchBpaRecommendations action + the recommendations panel/one-click-draft/per-pick-recompute); next: Tier-cliff detection sub-section (4 items — pure tier-boundary calc, target counts, recompute-alongside, live in-tier counter)
    - ".claude/build/04_league_dashboard.md"  # 🟡 — data-access 5/5 [x], freshness [x], 4 display components [x], page assembly [x], league selector [x]; next fold: nav-shell sub-section (share-link panel deferred to the share-token singleton)
    - ".claude/build/03a_draft_assistant_static_board.md"  # 🟡 — all five sub-sections complete; sole remainder = bye-week [>] (schedule-source-gated); goes 🟢 when that ships or is re-ruled
    - ".claude/build/02_data_pipeline.md"  # ESPN residue [!], cron cadence [~], finality promotion [>] (Nick-signed deferral) — nothing self-locatable here

nick_pending:
  - "QUICK VISUAL CHECK on your next sign-in (this session ADDED UI — the BPA recommendations panel is now live). NEW this session at /leagues/<id>/draft: the right sidebar now leads with a 'Recommendations' section at the TOP (above Rosters) — 8 candidates, each row = position badge, player name, team · ADP, and a right-aligned signed VORP number ('+215.5' etc., tabular). A 'Set my team' button (top-right of the panel) opens a roster picker; pick your team and each row gains a need badge ('Fills starter need' amber / 'Bench depth' / 'Position full' muted) shown ALONGSIDE the VORP, never changing the order; a 'Clear' entry resets it. Sub-line reads 'Ranked by value over replacement' (+ 'set your team for roster fit' until you pick, + 'market ADP unavailable' only if a format has no ADP). With a draft session started, each recommendation row also shows a Draft button → roster dropdown (same one-click flow as the main table); a player mid-pick reads 'Drafting…'. NOTE on the 2025 league (completed 170-pick draft): the panel correctly shows the best REMAINING players (elites all drafted) and, once you pick a team, every badge reads 'Position full' — that roster is already complete (starters 10/10, bench 0); it's the mechanism at the fully-drafted extreme, not a bug. Try it on the renewed 2026 league during a live draft to see live badges vary. CARRIED strip/status checks: 'Draft live · Sleeper · synced Ns ago' ticking; the pick/round/on-clock strip ('Draft complete · all 170 picks recorded' on 2025); background-tab pause/catch-up; Recent-picks feed (newest first, manual/sleeper badges, single-row Undo when session active); roster cards' '· 17 drafted' totals + position fill including drafted-but-unrostered players; optimistic Draft fold (roster picker → 'Drafting…' → 'Drafted · Pick N', conflicts roll back with a toast); live-sync (picks land ~5s, 40 drafted-unrostered dimmed, 'Available only' excludes them). CARRIED dashboard checks at /leagues/<id>: header, week pills, mirrored matchup cards (unofficial chips + as-of captions), standings, power rankings, ?player= side sheet; default week 18 shows 'No opponent' singles — pick week 17 for full H2H."
  - "OPTIONAL FUTURE — panel 'remember my team' per league: you chose re-pick-each-session (2026-07-22 Clarify), so the panel forgets your team when the board remounts. If you later want it to REMEMBER your team per league, that's a small future migration (a my-roster-per-league column/setting) + a tiny panel change to read it — flag it whenever and it can be its own small fold. Not blocking anything; the re-pick flow is fully functional."
  - "FINALITY PROMOTION — build scheduling (design Nick-signed + registered 2026-07-22 as the [>] item in 02_data_pipeline.md's cron sub-section; see known_issues for the mechanism): build as its own standalone fold, OR bundle with the season-start cadence session below — whichever comes first. HARD DEADLINE: before the 2026 NFL week 1→2 rollover (~2026-09-15; promotion first actually needed ~2026-09-18). Flip the item [>]→[ ] when its build session starts."
  - "BLOCKED ON EXTERNAL TIMING — nothing to do until ~mid-August 2026 (recorded 2026-07-22; expected clear ~2026-08-19 → 2026-09-02): Wave 2 ESPN manual items (public/private per league, espn_s2/SWID for private leagues, league IDs + season year). ESPN leagues are commissioner-locked until 2026 season setup. When they open: resolve the three items in a plain chat, flip them in MANUAL_SETUP_CHECKLIST.md, and flip 02_data_pipeline.md's 9 ESPN items [!]→[ ] so build sessions self-locate onto them again. Per the backsweep practice (BUILD_PROTOCOL Amendment Hygiene), that plain chat must also sweep the confirmations against already-registered 02/03c item text for staleness (03c_draft_assistant_espn_and_auction.md is the restructured ESPN+auction file, flip its [!] items back to [ ] alongside 02's) — and against Wave 4's shipped surfaces (platform-agnostic by construction) — and against 3b's shipped write path (recordManualPick is platform-agnostic; 03c's auction path extends it via the existing amount field on the same handleDraft seam) — and against the draft_sessions/poll orchestration (activateDraftSession/runActiveDraftPoll return skipped_non_sleeper for non-Sleeper leagues by design; 03c's ESPN poll path extends runRecordedDraftPoll; the ESPN in-draft rate-limit judgment call in MANUAL_SETUP's Wave 3b items applies to that extension) — and against the client-side live sync seams (listDraftPicks/live-picks merge/optimistic overlay are platform-agnostic; every RecordedPick carries catalog-joined playerFullName/playerPosition via the players embed, platform-agnostic since draft_state always references sleeper_player_id via the crosswalk) — and against the DraftOrderMeta on-clock path (slot_to_roster_id/reversal_round are Sleeper draft-object fields; ESPN draft-order projection needs its own source per the espn-api draft-detail wiki when 03c builds — the strip degrades to pick/round honestly meanwhile) — and against the projections/BPA layer AS NOW SHIPPED (player_projections is source-checked 'sleeper'-only today; ESPN leagues' BPA uses the same Sleeper-sourced projections automatically — projections, the whole src/services/bpa/ layer, AND the bpa-recommendations-panel are keyed by sleeper_player_id and league_config only, so the crosswalk covers ESPN rosters; getBpaRecommendations/fetchBpaRecommendations/the panel are platform-agnostic — config load resolves any league's derived_config/weights/layout, and the drafted set comes from draft_state regardless of source; sweep any BPA item text that assumed otherwise)."
  - "SEASON START (~Sept 2026): revisit league-sync cadence — the specced 15-minute cron needs Vercel Pro (~$20/mo) or an alternative trigger; currently daily on Hobby per your 2026-07-22 decision (MANUAL_SETUP decision of record). Flipping 02_data_pipeline.md's cron-schedule item [~]→[x] happens when the sub-daily cadence ships. If Pro is bought, /api/cron/sync-adp is the ready mount point for a dedicated ADP+projections schedule slot (currently piggybacked on sync-leagues; the fused run feeds both adp_rankings and player_projections from one fetch). This session is also the fallback vehicle for the finality-promotion build (entry above) if it hasn't shipped as its own fold by then. NOTE: live-draft polling and the BPA panel's per-pick recompute do NOT depend on this — both are client-driven (2026-07-22 orchestration decision) and need no cron slot at any plan tier; the panel's recompute reads Supabase only (no Sleeper calls)."
  - "BYE-WEEK [>] ITEM (03a) still awaits its schedule-source decision (plain chat: pick a schedule source, then a build session ships table+ingestion+row wiring — flips 03a 🟢). No longer blocking anything, but it's the only remainder between 03a and 🟢. Related small observation (logged in _38): the 3a board pool's fullName is also null for DST rows (renders the abbreviation fallback) — the item-4 name compose exists in draft-picks.ts if a future correctness pass wants to reuse it for getDraftBoardData."
  - "When your 2026 Sleeper league renews (new league_id every season): run npm run sync:league -- <new_id> once to connect it; every daily league-state cron run (and npm run sync:sleeper) covers it from then on. The 2025 league (1257464256710856704) is fully ingested including its 170-pick draft. Note: with auto-land live, the renewed league (season 2026 > 2025) becomes the DASHBOARD you land on after sign-in — expected, newest season sorts first. Draft-day note: the renewed league is where Start draft + live polling + click-to-draft + the live strip/status indicator/roster panel/recent-picks feed AND the now-shipped BPA recommendations panel (items 7-9) will run in anger. The BPA layer uses the LATEST ingested projections/ADP season (2026) regardless of the league's own season, so it is already current-market."
  - "Before Wave 5's waiver/FAAB recommendations item is built: run a dedicated scoping session to define the actual waiver/FAAB candidate scoring formula (opportunity-based — snap/target/touch share trends — is the recommended direction per wiki/topics/league-mechanics/waiver-wire-faab-strategy.md, but no formula exists yet)."

blockers:
  - "EXTERNAL TIMING (dated 2026-07-22): 02_data_pipeline.md's two ESPN sub-sections (9 items, [!]) are blocked until ESPN leagues open for the 2026 season — commissioner-locked, expected ~2026-08-19 → 2026-09-02. Not resolvable by Nick or any build session before then; do NOT guess visibility or fabricate placeholder league IDs (Nick's explicit instruction). Downstream: Wave 3c inherits this blocker (ESPN draft mechanics + auction). Does not stall the roadmap: 3b (Tier-cliff detection next) and Wave 4 both have buildable [ ] items."

known_issues:
  - "npm audit reports 6 vulns (4 moderate, 2 high) — last re-checked live 2026-07-22 (this session added no dependencies): pre-existing 1 moderate postcss + 2 high sharp are transitive inside next@16.2.11's pinned deps (only offered 'fix' is downgrading next to 9.x, which is wrong). All dev-time/build-time, none shipped to clients. Re-check in later sessions."
  - "FINALITY-PROMOTION GAP (surfaced 2026-07-22 by the cron session; investigated + designed + registered 2026-07-22 non-code session — still UNBUILT, mechanism unchanged until its build session): in-season, matchups/player_scores rows get is_final=true only when league lifecycle status reads 'complete' at sync time; the scheduled sync is current-week-only, so finished weeks stay is_final=false all season. Covering wiki: sleeper-api/matchup-endpoint (stat-correction window + finality Key Decision #3). Nick-signed design (2026-07-22): previous-week re-sync + promotion inside the daily league-state cron (week-advance = games-complete proxy; Friday-or-later UTC stable-read match = trigger — both Rule 12 genuine-silence sign-offs) + league-complete backstop sweep; needs a caller-driven finality extension to syncLeagueMatchups. Registered as the [>] item in 02_data_pipeline.md's cron sub-section. Zero live consequence until the season's first week rollover; HARD DEADLINE ~2026-09-15/18 (build scheduling in nick_pending). WAVE 4 STATUS: UI surfacing side SHIPPED and MOUNTED (UnofficialChip + AsOfTime render is_final/fetched_at honestly) — in-season, most rows will honestly read 'unofficial' until promotion ships; correct display, not a bug."

warnings:
  - "ABSOLUTE RULE 13 (canonical: MASTER_CONTEXT.md 'Shared Database Protection — Non-Fantasy Data Is Untouchable'): the Supabase database is the shared multi-app 'prolabel' project — ~49 non-fantasy tables incl. live business production data (Elliott). Never supabase db reset; never migration repair touching foreign history (new foreign versions get empty local stub files, never repair); no raw SQL that could reach a non-fantasy table — verify every migration's table names against ARCHITECTURE.md's 'Database Schema (live)' inventory; collision check before any new table name (the generated-types check alone is not the live check); ANY ambiguity → stop and ask Nick; mandatory BLAST RADIUS: line on every completion report. Owner-write RLS policies must match Nick's specific auth.uid(), never the blanket authenticated role — realized as public.is_fantasy_admin(); rotate the admin there, never by editing per-table policies. Operational restatement: ARCHITECTURE.md 'Shared-Database Constraints'. THIS SESSION (BPA items 7-9): READ-ONLY — no migration, no writes; blast radius = SELECTs only (getBpaRecommendations reads + the deleted verification harness). WAVE 4 SPECIFIC: the share-token RLS sub-section adds SELECT policies on six fantasy tables — a named singleton fold (Folding Policy exception #5) with its boundary tests; owner policies stay pinned to is_fantasy_admin(). draft_sessions (and draft_state) are NEVER on any spectator-policy list, at any wave — and player_projections/adp_rankings have no spectator consumer either (the BPA layer, INCLUDING getBpaRecommendations/fetchBpaRecommendations AND the bpa-recommendations-panel, is admin-only). Related standing decline (2026-07-22 live-sync Clarify): Supabase Realtime was declined for the live board partly because it would touch the shared project's realtime publication — interval re-fetch needs no shared-infra config."
  - "CLIENT-SIDE LIVE SYNC SEAMS (updated 2026-07-22): the live board path is ticker → pollActiveDraft (returns full listDraftPicks snapshot per executed poll) → shell's fingerprint-deduped livePicks state (+ optimistic pendingPicks overlay + pollHealth aggregation + DraftOrderMeta state) → live-picks.ts mergePicksIntoPlayers → BOTH board regions; the roster panel and recent-picks feed additionally read livePicks DIRECTLY for pick→team attribution (the merge deliberately carries no roster attribution); the BPA recommendations panel (items 7-9, SHIPPED 2026-07-22) reads livePicks as a useEffect DEPENDENCY only — a reference change triggers its fetchBpaRecommendations refetch (item 9 recompute), never a second poll driver/merge/pick-event feed. Extend THIS path for remaining 03b items — never a second poll driver, never a second merge, never a second pick-write UI path (the panel's one-click draft reuses the shell's handleDraft via the DraftablePlayer seam — NOT a parallel submit), never a second tick-outcome reporter, never a second order-metadata source, never a second pick-identity source (every RecordedPick path carries catalog-joined playerFullName/playerPosition via PICK_SELECT's players embed — DST names composed from split name fields per sleeper-api/dst-and-free-agents; never read pick identity from anywhere else) and never a second undo UI path. Key invariants: 'drafted' availability AND draftPending are produced ONLY by the client merge; a confirmed snapshot row ALWAYS wins over pending; 'rostered' wins over the whole overlay; out-of-pool picks are skipped by the MERGE but surfaced by the FEED and counted by the roster panel; ON-CLOCK IS A PROJECTION (projectOnClock is DISPLAY ONLY — never a write/validation/auto-pick input). rosterLabel (player-row.tsx, exported) is THE roster display convention on the draft surface (picker, strip, panel-created teams, feed, AND the BPA panel's my-team picker + Draft dropdown)."
  - "BPA LAYER CENTRALIZATION (live 2026-07-22, items 1-9 COMPLETE): src/services/bpa/ is the ONE home of projection scoring + VORP math + the recommendations query; src/components/draft-board/bpa-recommendations-panel.tsx is its ONE UI surface. PURE sub-layer (items 1-5, no I/O): scoring.ts parseScoringWeights/scoreProjectionStats (THE projection-scoring application; adp_*/pts_* structurally excluded); replacement.ts computeReplacementLevels (greedy fill, flex pooled, startable-only) + EXPORTS FLEX_ELIGIBILITY as the single flex-eligibility home; calibration.ts calibrateReplacementLevels (ADP-window depth from sentinel-filtered adp_rankings ONLY — never player_projections.stats); base-value.ts computeBaseValues (VORP; negatives kept; unrankable positions excluded+surfaced); board.ts computeBpaBoard (item 4 — THE composition; pure, NO cache; recompute = re-call against the shrinking pool passed by the caller); need.ts computeRosterNeed/classifyPositionNeed (item 5 — SEPARATE roster-need signal, generic bench, starter/bench/full; NEVER a value input). I/O sub-layer (item 6): recommendations.ts getBpaRecommendations (the ONE BPA query) → fetchBpaRecommendations action (draft/actions.ts, 6th action). UI (items 7-9): the panel CONSUMES fetchBpaRecommendations only — renders VORP ranking + ADP + the need badge alongside (NeedKind→NEED_LABELS; muted-tier chips, 'starter' borrows --warning, teal reserved), one-click draft via the shell's shared handleDraft (DraftablePlayer seam), per-pick recompute via a livePicks-keyed useEffect (request-seq guard + stale-while-revalidate). NEVER add caching, a second scoring/replacement/VORP/board derivation, a second projections/ADP read path, a second recommendations query, or a second draft-submit path anywhere downstream; item 5's need signal stays SEPARATE, never multiplied into base value. Layout parsing stays parseRosterSlotLayout and format mapping stays resolveAdpScoringFormat (both draft-board.ts) — bpa/ imports them, never re-derives. Projection/ADP season = latest ingested for the source (current-market, draft-board precedent), independent of the league's own season."
  - "MANUAL-PICK WRITE PATH CENTRALIZATION (live 2026-07-22): src/services/draft-picks.ts recordManualPick is the ONE local pick-write path — 03b's auto-pick MUST write through it (build-file mandate), and no second draft_state insert path may ever appear outside it and the platform pollers. Its UI callers wire through the shell's handleDraft (optimistic path) — the main player row AND the BPA recommendations panel (item 8) both call handleDraft, which accepts the minimal DraftablePlayer shape ({sleeperPlayerId, fullName} in services/draft-board.ts); future draft triggers (auto-pick, 03c auction bid entry) do the same, never a parallel submit path. Server-action auth pattern to reuse (6 actions incl. the read-only fetchBpaRecommendations): the gate lives INSIDE each action (getAdminAuthState → typed unauthorized/reason) + RLS as the second wall; owner actions use the RLS server client, never the secret-key client. draft_state rows are immutable-once-written — conflict responses return the authoritative row, which the shell adopts. Undo has its UI (the recent-picks feed's single-row Undo button, session-active only) — extend that surfacing, never add a second undo control."
  - "PLAYER_SCORES IS INGESTED, NEVER COMPUTED (Nick-signed canon, re-confirmed 2026-07-22 by the Wave 4 amendment): platform-scored values land as-received (Sleeper players_points — league-scored by Sleeper per each league's own settings; ESPN as-received when unblocked). There is NO compute-from-raw-stats path anywhere in v1 for ACTUAL scores. PROJECTIONS ARE THE SANCTIONED EXCEPTION BY DESIGN (Nick's 2026-07-22 decision of record): player_projections stores component projection stats, and the BPA engine scores them with league_config scoring_settings_raw weights at read time — REALIZED as src/services/bpa/scoring.ts and consumed by getBpaRecommendations (item 6) → the panel (items 7-9) — that is projected value, never a substitute for actual player_scores, and preset pts_* format buckets are never used as league values in either domain. Never invent score rows for unrostered weeks (FA-week gap accepted for v1)."
  - "DASHBOARD QUERY SERVICE CENTRALIZATION (live 2026-07-22): src/services/dashboard.ts is the ONE home of the standings ordering (fetchRankedStandings, wins desc → points_for desc → native_roster_id asc, Nick-signed under declared wiki silence) and the power-ranking formula (All-Play record; regular-season weeks via raw playoff_week_start; ties=half-wins; lowConfidence <6 weeks); listScoredWeeks is likewise the ONE week-range/default-week source. The Wave 4 display components (src/components/dashboard/) render these results AS-IS. The spectator loader passes its own share_token-scoped client (shared data-access logic is sanctioned; shared UI never is). Boundary discipline: the module never selects share_token/owner_id/native_league_id and has zero draft_state references — keep it that way when extending."
  - "ADMIN/SPECTATOR UI BOUNDARY (Wave 4, live 2026-07-22): src/components/dashboard/ is ADMIN-surface UI — the spectator surface must import ZERO components from it (or from draft-board/ or ui/dropdown-menu's draft usages); spectator gets its own components (04's spectator sub-section). The BPA recommendations panel (bpa-recommendations-panel.tsx) is likewise ADMIN-only — no spectator consumer, ever. Shared data-access (services/dashboard.ts) is the ONLY sanctioned sharing. Within the admin surface, cross-feature reuse is fine. Freshness primitives: UnofficialChip + AsOfTime (components/dashboard/) are THE rendering of is_final/fetched_at. The player-card open mechanism is the URL-driven ?player= sheet — future admin surfaces reuse this pattern/URL contract."
  - "DARK MODE ONLY IS LOAD-BEARING IN CODE (live 2026-07-22): globals.css holds the app's single always-on token set in :root — deliberately NO light block, NO .dark override block, NO theme toggle; the `dark` class on <html> is a permanent marker keeping vendored shadcn dark:-variant tunings active. Never add a light token set, a prefers-color-scheme branch, or remove that class. ui/sonner.tsx hardcodes theme='dark' for the same reason. --radius stays 0.625rem (Nick-signed): containers rounded-xl, buttons/chips pills. Position identity tokens --pos-qb/rb/wr/te/k/def are badges/chips ONLY — never row tints; teal = interactive/live only (sanctioned for positive deltas and the Draft live indicator). The BPA need badges (item 7, shipped) follow this: muted-tier chips, 'starter' borrows --warning (RosterPanel's actionable-need color), teal untouched — need classification is NOT a live/positive semantic."
  - "OWNER AUTH SURFACE (live 2026-07-22) — three walls, keep all three: (1) src/proxy.ts (Next 16 proxy, NOT middleware.ts) refreshes the Supabase session per matched request and 307s unauth /leagues/* to /login — its matcher deliberately EXCLUDES /api. (2) The (admin) layout gate uses getAdminAuthState (src/lib/supabase/auth.ts → is_fantasy_admin RPC) — never hardcode the admin UUID in app code; a signed-in non-fantasy prolabel user is a REAL case and must get the zero-data NotAuthorizedCard. (3) Per-table RLS. Unauth/non-admin responses must never contain league data. ROOT AUTO-LAND targets the dashboard (/leagues/<id>). Server actions add a FOURTH gate point: auth INSIDE each action — SIX actions follow it (submitManualPick/undoManualPick/startDraftSession/endDraftSession/pollActiveDraft/fetchBpaRecommendations + login's signOut pattern); the read-only fetchBpaRecommendations returns {ok:false, reason:'unauthorized'} + RLS as the second wall (live-verified: anon → league_not_found, zero data)."
  - "src/lib/supabase/database.types.ts is generated — never hand-edit; regenerate via npm run gen:types after every schema migration. It deliberately contains the whole shared public schema (foreign prolabel tables included) — expected, not drift. Trap: generated Insert types list stored generated columns (matchups.effective_points) as insertable — Postgres rejects them; sync code must omit them. Note: getBpaRecommendations reads player_projections with a PAGINATED .range loop (1000/page) — the 3292-row season snapshot exceeds PostgREST's default single-select page cap; never rely on an unbounded select for that table."
  - "src/lib/supabase/admin.ts bypasses RLS (secret key) — server-only, never import from client code; its env var is deliberately not NEXT_PUBLIC_ so a client-bundle import fails loudly. The draft-board, dashboard, and BPA-recommendations routes/actions deliberately do NOT use it — owner pages read through the RLS server client as the signed-in admin, and owner server actions write/read through it the same way (fetchBpaRecommendations included). Temp verification scripts (deleted after use) may use the admin client for read-only checks (blast radius = SELECTs)."
  - "Sleeper player catalog (/players/nfl): at most ONCE PER DAY — Sleeper's most load-bearing rate rule. The cron route enforces this with a hard guard on sync_runs. BUT the manual script path (npm run sync:players) bypasses the guard — manual cadence discipline applies. Per-league endpoints remain paced by the client's global 250ms min-gate — which also absorbs the 5s live-draft polling (≤3 requests per executed tick, 0.6 req/s worst case per league). The BPA panel's per-pick recompute (items 7-9) makes NO Sleeper calls — it reads Supabase only (fetchBpaRecommendations→getBpaRecommendations), so it adds zero to the Sleeper budget."
  - "api.sleeper.com (ADP/projections surface) is NOT api.sleeper.app/v1: undocumented, no contract, can change shape or vanish — its consumer is the quarantined src/services/adp/ ONLY (own client, own pacing gate). The SAME fused ingestion run feeds BOTH adp_rankings AND player_projections from one fetch; never a second api.sleeper.com client or a separate projections fetch. 999.0 in any adp_* field is a 'no ADP in this format' sentinel — never data in adp_rankings; player_projections.stats retains the RAW object (sentinels included). ADP is never read from player_projections (calibration.ts reads sentinel-filtered adp_rankings only, and getBpaRecommendations reads ADP carriers from adp_rankings, projection component stats from player_projections — never crossed). The adp_* field inventory is OPEN — never hardcode a closed format list. Covering wiki: sleeper-api/projections-endpoint."
  - "League-format→ADP-format mapping is CENTRALIZED in resolveAdpScoringFormat (src/services/draft-board.ts, Nick-signed 2026-07-22): superflex→'2qb' precedence, then ppr buckets; dynasty/IDP unmapped in v1; NO cross-format fallback anywhere. Any consumer calls this function — never re-derives (getBpaRecommendations and bpa/calibration.ts's callers do exactly this). Same centralization for roster-slot layouts: parseRosterSlotLayout (same module) is the ONE parser of roster_settings_raw — bpa/replacement.ts and need.ts and getBpaRecommendations consume its output, never re-derive. FLEX_ELIGIBILITY is now the ONE flex-eligibility home (exported from bpa/replacement.ts) — need.ts imports it, never re-derives. ADP-absence classification is centralized in deriveAdpNoticeKind (src/components/draft-board/adp-notice.tsx). DraftablePlayer (Pick<DraftBoardPlayer,'sleeperPlayerId'|'fullName'>, draft-board.ts) is the ONE minimal shape the shared handleDraft consumes — non-table draft surfaces reuse it, never a cast or a parallel submit."

health: "clean"
