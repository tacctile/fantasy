# BUILD_INDEX.md
**Master build router — tacctile/fantasy**
**Last Updated:** 2026-07-23 (Wave 3b progressing: **Positional-run detection COMPLETE 4/4 `[x]`** — item 3, the non-blocking inline run badge near the position filter, shipped as its own fold (one client UI surface; the next sub-section, queue/auto-pick, is NAMED SINGLETON exception #3). New `src/components/draft-board/positional-run-badges.tsx` `PositionalRunBadges` rendered in `BoardToolbar` right after the position-filter chips: runs computed CLIENT-SIDE by the pure `detectPositionalRuns` on the shell's `livePicks` (RecordedPick→RunPick via `playerPosition`, confirmed picks only) — THE one run-detection source, NOT routed through `fetchBpaRecommendations` (item-4 decoupling preserved). Three Clarify Nick-signed: (1) data flow = lift the BPA panel's already-computed `context.tiers` up via a new optional `onTiers` callback (reuses the ONE summarizeTiers result — no second tier compute; degrades to run-only when the sidebar is hidden below `lg`); (2) muted chip + tooltip pairing the count with top-tier depth ("N POS in the last W picks · M left in the best available tier" — the wiki's tier-cliff refinement composed at the UI); (3) appear-only, one badge per active run in count-desc order. No new token; non-blocking (a run is a weighted signal, never a "draft now" interrupt). Verified: tsc/lint/build clean + 18/18 static-render harness (react-dom/server, deleted). Full demand-vs-panic classification stays out of scope (standing WIKI NOTE / scope question). Next unchecked = the DRAFT QUEUE + AUTO-PICK sub-section (4 items, NAMED SINGLETON fold exception #3) then RESILIENCE (2). Prior: **Positional-run detection items 1/2/4 `[x]` — sub-section 3/4** — "the pure run-detection layer" folded as one independently verifiable artifact (headlessly verifiable like the BPA pure/tier sessions; READ-ONLY, no migration; NEW in-season-management wiki category — the boundary that correctly ended the prior league-mechanics folds): new `src/services/bpa/runs.ts` `detectPositionalRuns` — pure, deterministic, ZERO imports; flags a run when same-position picks in a sliding window of the last 6 picks (by `pick_number`) meet `RUN_COUNT_THRESHOLD` {QB 2/TE 2 shallow, RB 3/WR 3 deep}, where map-presence IS the run-detectable set so K/DEF/other are never flagged (they occupy window slots, late-draft noise omitted — same scope the tier counter uses); null positions occupy a slot, no count; undo self-heals; minimal `RunPick {pickNumber, position}` input (decoupled from RecordedPick, DraftablePlayer precedent). Three Clarify Nick-signed: (1) COUNT-ONLY flag — the wiki's tier-crossing "does this run matter" refinement is composed at item 3's badge next session by pairing the flag with the already-shipped `summarizeTiers` topTierSize (keeps this a standalone signal); (2) window 6 / QB-TE 2 / RB-WR 3 / K-DEF excluded; (3) superflex/2QB format-sensitivity DEFERRED (no superflex league connected — the wiki Key Decision mandating it is a WIKI NOTE + future refinement, not silently filled). Item 4 (informational-only, no VORP coupling) verified STRUCTURALLY by grep both directions — `runs.ts` imports nothing from the value pipeline and nothing imports it; a parallel display signal like need.ts/tiers.ts, never an input to ordering. Exact thresholds are DECLARED WIKI SILENCE (positional-run-detection-draft-day.md Open Q1 — tunable-pending-backtesting → live-tuned constants, same posture as tiers.ts's gap multiples). Verified: 26/26 synthetic edge cases + live read-only on the real 170-pick 2025 draft (32 plausible run-activation events — RB run at pick 4, WR at 8, QB at 31 on 2-in-window; K/DEF tail correctly no-runs); tsc/lint/build clean; temp harness deleted. Next unchecked = positional-run item 3 (non-blocking inline badge near the position filter — client UI computing runs on the shell's livePicks, composing the flag with the tier counter), then queue/auto-pick (5 items, NAMED SINGLETON fold exception #3) + resilience (2). Prior: **Tier-cliff detection COMPLETE 4/4 `[x]`** — item 4 = the `TierDepthStrip` per-position "N remaining in the best available tier" counter, a client sub-component in the BPA panel over the already-surfaced `context.tiers` (no new poll/query/tier-compute driver — rides the panel's per-pick refetch). Four Nick-signed Clarify calls: placement in-panel above the ranked list, top-tier size only (tierCount/top-projection in the tooltip), QB/RB/WR/TE only (K/DEF noise-prone tails omitted), and uniform muted with NO nearly-exhausted emphasis (purest reading of league-size-scarcity-effects.md — tier breaks are a weighted input, never a rigid "draft now" trigger; keeps amber = roster-need and teal = live/interactive). Verified: tsc/lint/build clean + live read-only harness (deleted) on the real 170-drafted league — topTierSize populated QB 9/RB 1/WR 2/TE 1, diverging from the full-pool bands = counter data proven dynamic. Next unchecked = the positional-run detection sub-section (4 items — a NEW wiki category boundary, in-season-management per ROUTING.md "Positional run / reach vs. value detection"; item 1 = pure sliding-window run-detection). Prior: **Tier-cliff items 1–3 `[x]`** — "the tier-detection layer" folded as one independently verifiable artifact (headlessly verifiable like the prior BPA pure/query sessions; READ-ONLY, no migration): new `src/services/bpa/tiers.ts` `computePositionTiers` — pure per-position gap-based tier boundaries over the base-value ranked pool, tiering ONLY players at/above each position's operative replacement line (Nick's Clarify = down-to-replacement depth scope), break where the consecutive projected-points gap exceeds `TIER_GAP_MULTIPLE[pos] × the position's MEDIAN gap` (Nick's Clarify = multiple-of-typical-gap not absolute; MEDIAN over mean is a live-verified robustness fix so one dominant elite-separation gap can't swallow QB/TE secondary cliffs; within-position projection gaps ≡ base-VORP gaps → no z-score). Multiples (RB 3.0/WR 3.2/QB 2.0/TE 1.5/K,DEF 2.0/default 2.5) live-tuned to the build-file target bands — Nick's Clarify = fixed multiple, counts as a sanity check, NOT adaptive-to-band (wiki rejects rigid tier-count enforcement); exact thresholds are DECLARED WIKI SILENCE (league-size-scarcity-effects.md Key Decision refuses specific numeric thresholds — the method/sensitivity/live-recompute ARE wiki-specified). Composed into `computeBpaBoard` (`BpaBoard` gains `tiers`, recomputes against the shrinking pool on the same per-pick re-call as VORP) + surfaced through `getBpaRecommendations` (each row carries within-position `tier`, context carries whole-board per-position tier summaries via `summarizeTiers` — item 4's counter data). Verified: tsc/lint/build clean + 11/11 pure edge tests + live read-only on the real league (full pre-draft 2026 PPR pool hits every target band QB 3/TE 4/RB 8/WR 9; full-vs-170-drafted tier structure DIVERGES = dynamic recompute proven; ordering invariant vs value-only — tiers never reorder VORP). Tier-cliff item 4 shipped this session (see the Last-Updated lead); the sub-section is now complete 4/4. Prior: **BPA recommendation engine items 7–9 `[x]` — sub-section COMPLETE 9/9** — "the BPA recommendations panel" folded as one verifiable artifact at the client-side live-behavior ceiling: new `src/components/draft-board/bpa-recommendations-panel.tsx` at the TOP of the board sidebar (Nick's placement) rendering `fetchBpaRecommendations(topN=8, Nick's count)` top-N ranked PURELY by base VORP with market ADP beside the value and an independent roster-need badge (NEED_LABELS starter/bench/full — muted-tier chips, 'starter' borrows `--warning` like RosterPanel, teal reserved) shown ALONGSIDE never merged into the ranking, tabular-nums; my-team picker over rosters sets `selfRosterId` (Nick's Clarify — no persisted marker, re-pick each session; value-only + no badges until picked); item 8 = rows wire into the shell's SAME `handleDraft` via the new `DraftablePlayer` seam (`Pick<DraftBoardPlayer,'sleeperPlayerId'|'fullName'>`, services/draft-board.ts — no cast/duplication, never a parallel submit); item 9 = a `livePicks`-keyed `useEffect` re-invokes the query off the SAME shell snapshot (shrinking-pool recompute lives in getBpaRecommendations; the shell's fingerprint bail-out means a steady 5s poll never refetches — only real pick changes do; request-seq guard + stale-while-revalidate). Verified live read-only on the real league (topN=8 → 8, calibration=adp, pool 3122 [3292−170], need annotations ordering/VORP-invariant vs value-only — need NEVER reorders, all 'full' = the documented fully-drafted extreme); tsc/lint/build clean; read-only session, no migration. Next unchecked = Tier-cliff detection sub-section (4 items — pure tier-boundary calc, target counts, recompute-alongside, live in-tier counter). Prior: **BPA recommendation engine items 4–6 `[x]`** (6/9) — "the BPA query layer" folded as one independently verifiable artifact: `board.ts` (item 4 `computeBpaBoard` — THE composition of scoring→replacement→calibration→base-value; pure, no cache; the shrinking pool is realized by the caller passing the current undrafted set — full-demand-against-shrinking-pool recompute model disclosed/Nick-signed), `need.ts` (item 5 `computeRosterNeed`/`classifyPositionNeed` — pure per-team greedy fill; generic-bench model; need kinds starter/bench/full; a SEPARATE signal never blended into VORP; reuses the now-exported `FLEX_ELIGIBILITY` single home), `recommendations.ts` (item 6 `getBpaRecommendations` — first I/O in bpa/: loads config/paginated projections[.range loop >1000-safe]/format ADP carriers/`listDraftPicks`, shrinks pool, runs the board, annotates each top-N row with need; default N=10, optional `selfRosterId`—Nick's Clarify [no "my team" marker in schema, need null when absent]; typed reasons; ADP absence=structural_only not a failure; admin-only, no spectator consumer) + the `fetchBpaRecommendations` server action (6th action, same in-action auth+RLS gate). Verified 39/39 (pure item-4/5 + live read-only item-6 on the real league: pool 3292→3122 after 170 picks, calibration=adp, pool excludes all 170 drafted, need annotations ordering-invariant vs value-only, anon→league_not_found RLS wall); tsc/lint/build clean; read-only session, no migration. Next unchecked = BPA items 7–9 (panel UI + one-click draft wire into handleDraft + recompute-on-any-source — the client-facing sub-section; item 7 supplies `selfRosterId`; live-behavior ceiling may apply). Prior: **BPA items 1–3 `[x]`** — the pure VORP calculation layer `src/services/bpa/` (fold = the standard 3-decision-dense-item cap; league-mechanics wiki category entered — 8 pages read this session under Rule 12 coverage-check overrides): `scoring.ts` (THE projection-scoring home — league `scoring_settings_raw` weights × component stats, `adp_*`/`pts_*` structurally excluded, key-namespace match confirmed empirically live 22/38), `replacement.ts` (item 1: greedy league-wide fill, flex pooled across eligible positions — Nick-signed resolution of the VBD wiki page's open pooling question; startable-only demand, Nick-signed; declared silence on non-standard flex-label eligibility), `calibration.ts` (item 2: ADP-window depth from sentinel-filtered `adp_rankings` — market sets depth, projections set value; replaces structural with flagged league-/position-level structural fallbacks — live: K/DEF zero-carrier fallback observed, RB deepens 23→35), `base-value.ts` (item 3: VORP ranking, negatives kept, unrankable positions surfaced). Verified 62/62 pure + three live full-chain runs (top-15 = plausible 2026 PPR board, zero QBs in top 25 on the 1QB league); tsc/lint/build clean; one commit per item. Next unchecked = BPA item 4 (dynamic per-pick recompute — the pure layer already supports it by construction) + likely fold items 5–6. Prior: the **Projections-ingestion sub-section `[x]` 3/3** — registered that same session as a Nick-signed correctness amendment when the Manual Setup Flag tripped on BPA item 1 and Nick resolved the projections-source decision in-session (decision of record, `MANUAL_SETUP_CHECKLIST.md` Wave 3b: source = the same quarantined api.sleeper.com endpoint; landing = component stats scored by `league_config` weights at read time — preset `pts_*` buckets never used as league values; fused single fetch; full `stats` jsonb + provenance; telemetry on the existing `adp_ingestion` run's counts): new `player_projections` table (migration `20260723023127`, collision-checked against live information_schema, owner-RLS) + fused persist inside `services/adp/ingestion.ts` (own gates, both tables' gates before either writes, ADP swap first) — live-verified 3292 records → 3292 rows, idempotent re-run, bogus-year abort with ZERO writes, `sync_runs` counts extension confirmed. Next unchecked = BPA recommendation engine item 1 (9 decision-dense items; needs the league-mechanics wiki category — the new-category boundary that capped this fold; projections data now live). Prior: the UI-extensions items 3–5 fold `[x]` — **Live-board-UI-extensions sub-section complete 5/5**: (3) live roster/positional-need panel — `computeRosterFill` blends confirmed picks union-by-player (rostered baseline wins; Nick-signed), per-team "N drafted" attribution via `nativeRosterId`, ST/BN/IR stay roster-sync-only, verified 31/31 pure + 8/8 live; (4) recent-picks feed below the rosters (newest first, visible whenever picks exist) + every `RecordedPick` path now carries catalog-joined name/position (DST names composed from split name fields per the wiki — live finding, 170/170 re-verified) + undo finally has its UI (single-row Undo on the feed, session-active only) + Nick-signed backsweep: joined positions let out-of-pool picks feed position fill; (5) available-only liveness — verification-only, 6/6. Next unchecked = the BPA recommendation engine sub-section (9 items; needs the league-mechanics wiki category + a projections-source decision — see STATE.yml). Prior: the status-indicator + live-strip fold `[x]` — client-side live sync 5/5 complete (ticker `onStatus`, pure `live-status.ts`, `LiveStatusIndicator`) + UI extensions item 2 (live-draft strip pick/round/on-clock; order source = the draft object's `slot_to_roster_id` via the poll path with a `/draft/{id}` detail fallback; `projectOnClock` validated against all 170 real picks). Prior: the optimistic manual-pick fold `[x]` — client-side live sync items 3–4 + the player-row Draft action folded as one artifact (Nick-signed): Status-cell Draft button → per-click roster picker (Base UI dropdown-menu vendored), optimistic pending overlay through the one merge path ("Drafting…" cue, `draftPending` client-only), conflict rollback adopting the authoritative row + sonner toast (new dep, app-wide Toaster); live-verified 21/21 against the real league, net-zero. Client-side live sync now 4/5 (`[ ]` remainder: toolbar status indicator — needs a ticker onStatus callback); UI extensions 1/5. Prior: the active-draft polling orchestration sub-section `[x]` — new admin-only `draft_sessions` table (6h soft TTL, owner RLS only), toolbar Start/End control, client-driven 5s elevated polling (Nick-signed — Hobby crons are daily-only) with every executed poll recorded in `sync_runs`; live-verified 37/37. Next unchecked item = client-side live sync (fold 2–3 items max per the live-behavior ceiling). Prior: manual click-to-draft write path sub-section `[x]` — `recordManualPick`/`undoLastManualPick` service + auth-gated server actions, first-write-wins live-verified against the real league; 03b ⬜→🟡. Prior: RESTRUCTURED: `03b_draft_assistant_live_draft.md` split into Sleeper-snake-only scope (unblocked, buildable now) and a new `03c_draft_assistant_espn_and_auction.md` (ESPN client/auth/crosswalk/polling + all auction mechanics, blocked pending ESPN commissioner unlock ~mid-August 2026) — Sleeper supports snake draft only, ESPN will support both snake and auction once unblocked. Wave 4 build progressing: data-access layer, freshness surfacing, the four display components, and the page-assembly + league-selector fold are all `[x]` — the admin dashboard is mounted at `/leagues/[leagueId]` with root auto-land re-signed onto it. Next unchecked items: the nav-shell sub-section; the share-link panel rides the share-token RLS singleton. Standing context: Nick's wave-order ruling runs Wave 4 ahead of ESPN-blocked 3c; the scoring-engine sub-section was cut by Nick-signed correctness amendment (`player_scores` is ingested platform-scored, never computed). Prior state: Wave 3a 🟡 — all five sub-sections complete, sole remainder the schedule-source-gated bye-week `[>]`; `02_data_pipeline.md` 🟡: 9 ESPN items `[!]` external-timing, cron cadence `[~]`, finality promotion `[>]`; Wave 1 complete — `01_foundation.md` 🟢)

Nothing gets built without a registered build file. No exceptions. Every new feature requires a registered build file in this index before a single line of code is written.

---

## Session Start Protocol

Every Claude Code session reads files in this exact order:

1. `.claude/MASTER_CONTEXT.md` — rules, stack, constraints
2. `.claude/STATE.yml` — what happened last session
3. `.claude/BUILD_INDEX.md` — this file, build registry and wiki category map
4. `wiki/index.md` and `wiki/ROUTING.md` — identify relevant wiki category, then read up to 3 pages from that category before beginning work (the Wiki Coverage Rule — Absolute Rule 12, canonical text in `BUILD_PROTOCOL.md` — overrides this cap whenever a decision isn't fully specified by pages already read)
5. Only the build file(s) listed in `STATE.yml → current_build_files` — each build file lists the specific wiki pages to consult in its own WIKI PAGES section
6. Only the source files listed in the prompt

Do not read files not listed above unless the prompt explicitly requires them.

---

## Status Legend

⬜ Not started &nbsp;&nbsp; 🟡 In progress &nbsp;&nbsp; 🟢 Complete &nbsp;&nbsp; 🔴 Blocked &nbsp;&nbsp; 🔵 Needs revisit

Checklist item states: `[ ]` not started · `[x]` complete · `[~]` in progress · `[>]` deferred · `[-]` cut · `[!]` blocked

---

## Wave Roadmap

Atomic sessions per the Folding Policy (canonical text in `BUILD_PROTOCOL.md`): one folded unit per fresh Claude Code session — up to 3 decision-dense items, one mechanical sub-section, or one independently verifiable artifact's full item set, subject to that policy's hard stops and named singleton exceptions. No scope bleed beyond the declared fold.

| Wave | Name | Status | Scope |
| ---- | ---- | ------ | ----- |
| 1 | Foundation | 🟢 | Supabase schema (with `league_id`, `platform`, `season_year`, player-identity mapping, `league_config` per MASTER_CONTEXT.md Schema Rules), env/secrets setup, initial Vercel deploy — complete 2026-07-22, deployed health-check gate passed |
| 2 | Data Pipeline | 🟡 | Sleeper sync (build/validate first — no-auth, trivial case), then ESPN cookie-auth integration (harder, isolate failures defensively), cron/polling strategy |
| 3a | Draft Assistant — Static Board | 🟡 | Static draft board UI, ADP ingestion, no live polling — all five sub-sections complete 2026-07-22; only the bye-week `[>]` deferral remains |
| 3b | Draft Assistant — Live Draft (Sleeper Snake) | 🟡 | Manual click-to-draft AND live Sleeper draft polling ship together — both write to the same shared `draft_state` table, first-write-wins, no staged manual-first/poller-later sequencing. BPA/VORP recommendation engine, tier-cliff detection, positional runs, queue/auto-pick. Sleeper snake only — no ESPN, no auction (Sleeper doesn't support auction on this platform). Depends on 3a only — unblocked, self-locatable now (restructured 2026-07-22, split from the original combined file) |
| 3c | Draft Assistant — ESPN Live Draft + Auction | 🔴 | ESPN client/cookie-auth/crosswalk sync/live polling/draft-state writes, plus all auction-draft mechanics (nomination/bid state, budget tracking, auction valuation) — auction applies to ESPN only. Extends 3b's shared shell (manual-pick path, live board UI, BPA engine) rather than duplicating it. Depends on 3b and Wave 2's ESPN integration. Blocked pending ESPN commissioner unlock (~mid-August 2026), same external timing as `02_data_pipeline.md`'s ESPN sub-sections (registered 2026-07-22) |
| 4 | League Dashboard | 🟡 | Standings, matchups, power rankings, player cards. Includes the read-only share-link surface (per `MASTER_CONTEXT.md` Access Model) — same dashboard data, gated by `share_token` instead of owner auth, not a separate later build. Also owns the admin surface's persistent sidebar navigation shell and command-center home, which Wave 5 and Wave 6 mount into. Scoring is NOT computed here — `player_scores` arrives platform-scored from Wave 2's sync (scoring-engine sub-section cut 2026-07-22, Nick-signed) |
| 5 | Eye Candy | ⬜ | Score charts, lucky/unlucky tracker, positional breakdowns, playoff picture, trade evaluation, waiver/FAAB recommendations (trade + waiver amended into scope 2026-07-21 — see 05_eye_candy.md scope note; waiver scoring formula itself deferred to a follow-up session) |
| 6 | Report + Tools | ⬜ | League report generator, free agent board, PWA manifest/service worker |

**Dependency notes:** Wave 3b depends only on Wave 3a (static board UI/data layer) — it has zero ESPN dependency and is unblocked/self-locatable now. Wave 3c depends on Wave 3b (extends its shared shell) and on Wave 2's ESPN integration being live and isolated (its failure must not break Wave 3a/3b's Sleeper-sourced features); 3c is blocked until ESPN's commissioner lock clears. Wave 3a can start once Wave 1's schema and Wave 2's Sleeper sync are done — it does not need ESPN. Wave 4 does not depend on 3b, 3c, or ESPN — Nick authorized it to run ahead of ESPN-blocked draft-live work (2026-07-22 ruling); its data needs are served by the live Sleeper pipeline, and ESPN leagues join its surfaces automatically once connected. 3c's position in roadmap order is unchanged from the original 3b slot — it resumes when the ESPN block clears.

---

## Build Files Registry

Each feature gets a numbered file at `.claude/build/NN_FEATURE_NAME.md`, registered here with a status glyph, before any code is written.

| # | File | Wave | Status |
| - | ---- | ---- | ------ |
| 01 | [01_foundation.md](build/01_foundation.md) | 1 | 🟢 |
| 02 | [02_data_pipeline.md](build/02_data_pipeline.md) | 2 | 🟡 |
| 03a | [03a_draft_assistant_static_board.md](build/03a_draft_assistant_static_board.md) | 3a | 🟡 |
| 03b | [03b_draft_assistant_live_draft.md](build/03b_draft_assistant_live_draft.md) | 3b | 🟡 |
| 03c | [03c_draft_assistant_espn_and_auction.md](build/03c_draft_assistant_espn_and_auction.md) | 3c | 🔴 |
| 04 | [04_league_dashboard.md](build/04_league_dashboard.md) | 4 | 🟡 |
| 05 | [05_eye_candy.md](build/05_eye_candy.md) | 5 | ⬜ |
| 06 | [06_report_and_tools.md](build/06_report_and_tools.md) | 6 | ⬜ |

---

## Wiki Category Map

| Category | Covers |
| -------- | ------ |
| `player-evaluation` | Statistical models for player performance, opportunity share, efficiency |
| `team-scheme` | Team-level tendencies, scheme identity, offensive line, coaching |
| `league-mechanics` | Scoring formats, roster construction, draft strategy, ADP, trade value |
| `in-season-management` | Injury tracking, matchup analysis, start/sit, rest-of-season rankings |
| `sleeper-api` | Sleeper endpoint structure, player ID format, rate limits |
| `espn-api` | ESPN cookie auth, undocumented view params, rate limits, endpoint structure |
| `schema-reference` | League scoping conventions, player ID mapping, `league_config` data model |
